---
phase: quick-260611-h36
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/relayer/src/lib/memory-cache.ts
  - apps/relayer/src/lib/__tests__/memory-cache.test.ts
  - apps/relayer/src/lib/cache.ts
  - apps/relayer/src/lib/subgraph-client.ts
  - apps/relayer/src/lib/__tests__/subgraph-breaker.test.ts
  - apps/relayer/src/lib/ens-resolver.ts
  - apps/relayer/src/lib/follow-graph.ts
  - apps/relayer/src/routes/feed.ts
  - apps/relayer/src/routes/live-state.ts
  - apps/relayer/src/routes/profile.ts
  - apps/relayer/src/routes/settle.ts
  - apps/relayer/src/routes/duels.ts
  - apps/relayer/src/routes/duel-live-state.ts
  - apps/relayer/src/routes/calls-dup-check.ts
  - apps/relayer/src/routes/paymaster-policy.ts
  - apps/relayer/src/routes/social-link.ts
  - apps/relayer/src/routes/admin-paymaster.ts
  - apps/relayer/src/workers/synthetic-event-handler.ts
  - apps/relayer/src/workers/settlement-poller.ts
  - apps/relayer/src/workers/__tests__/settlement-poller.test.ts
  - apps/relayer/src/workers/settlement-watcher.ts
  - apps/relayer/src/lib/secret-manager.ts
  - apps/relayer/src/types.ts
  - apps/relayer/src/index.ts
autonomous: true
requirements: [QUICK-260611-H36]

must_haves:
  truths:
    - "GET /api/feed serves cached items from the in-process L1 even when Upstash Redis is unreachable — no empty feed during Redis quota exhaustion"
    - "A subgraph failure/429/timeout opens a circuit breaker; while open, queries short-circuit to last-known-good memory snapshots (<=1h old) and feed responses carry _source:'stale'"
    - "An expired Pyth price-target call is discovered by ascending getCall chain-polling (no Redis, no subgraph required) and settled via settlePythCall when SETTLEMENT_SIGNER_PRIVATE_KEY is present"
    - "With no signer configured the poller runs IDLE: single P1 startup alert + per-tick would-settle dry-run logs, zero transactions, key never logged"
    - "The 5 hard-500 Redis paths degrade gracefully: dup-check skips cache, paymaster POST fails closed (deny), paymaster-count GET returns {count:null,degraded:true}, unlink-purge still 200s, synthetic nonce falls back to memory, admin cap PATCH returns 503 cache_unavailable"
    - "The idle BullMQ settlement worker no longer polls Redis 24/7 — gated behind SETTLEMENT_BULLMQ_ENABLED (default false)"
    - "All 264 existing relayer tests stay green (D-15) and the new memory-cache / breaker / poller unit tests pass"
  artifacts:
    - path: "apps/relayer/src/lib/memory-cache.ts"
      provides: "Bounded TTL map with stale retrieval (L1 cache primitive)"
      exports: ["MemoryCache", "memoryCache"]
    - path: "apps/relayer/src/lib/cache.ts"
      provides: "L1-first getCached/setCached adapter over memory + error-guarded Redis"
      exports: ["getCached", "setCached"]
    - path: "apps/relayer/src/workers/settlement-poller.ts"
      provides: "Redis-free setInterval settlement watcher (chain-polling discovery + Pyth settle)"
      exports: ["startSettlementPoller"]
    - path: "apps/relayer/src/lib/subgraph-client.ts"
      provides: "Circuit breaker + last-known-good snapshots around executeQuery; queryAcceptedChallengeIds"
      contains: "SUBGRAPH_BREAKER_COOLDOWN_MS"
  key_links:
    - from: "apps/relayer/src/routes/feed.ts"
      to: "apps/relayer/src/lib/cache.ts"
      via: "getCached/setCached replacing direct redis.get/set"
      pattern: "getCached|setCached"
    - from: "apps/relayer/src/lib/subgraph-client.ts"
      to: "apps/relayer/src/lib/memory-cache.ts"
      via: "last-good snapshot store keyed by hash(query+variables)"
      pattern: "MemoryCache"
    - from: "apps/relayer/src/workers/settlement-poller.ts"
      to: "apps/relayer/src/workers/oracle-adapters/pyth-adapter.ts"
      via: "settlePythCall(callId, updateData, acceptedChallengeIds, ...)"
      pattern: "settlePythCall"
    - from: "apps/relayer/src/index.ts"
      to: "apps/relayer/src/workers/settlement-poller.ts"
      via: "onReady startSettlementPoller; BullMQ watcher gated by SETTLEMENT_BULLMQ_ENABLED"
      pattern: "startSettlementPoller|SETTLEMENT_BULLMQ_ENABLED"
---

<objective>
Relayer resilience overhaul (LIVE OUTAGE response, 2026-06-11): Upstash quota exhausted (500K/500K, resets ~Jul 1) means zero caching, which lets The Graph Studio endpoint 429-storm, which renders feed/profile/positions EMPTY despite 15 on-chain calls. Auto-settlement is structurally inert (enqueueSettlement has no producer, index.ts walletClient is a throw-stub, BullMQ needs the dead Redis), and the idle BullMQ worker polls Redis 24/7 — the prime quota-burn suspect. Call #15 (ETH>=$2,300 Pyth priceTarget) expires 2026-06-12 10:08 UTC with nothing to settle it.

Five fixes, apps/relayer ONLY, no deploy: (1) in-process bounded L1 memory cache, (2) L1-first cache adapter on all 8 Redis cache sites, (3) subgraph circuit breaker with last-known-good snapshots, (4) Redis-free chain-polling settlement poller + BullMQ gate-off, (5) error guards on 5 hard-500 Redis paths.

Purpose: feed/profile/receipts stay rendered through Redis + subgraph outages (Core Value: the receipt must always be visible), and expired Pyth calls actually settle. The relayer is a SINGLE Fly machine, so an in-process memory cache is a legitimate L1.
Output: 4 new files (memory-cache, cache helper, settlement-poller, + tests), surgical edits to subgraph-client, 11 routes/workers, secret-manager/types/index wiring. 3 atomic commits.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@apps/relayer/src/index.ts
@apps/relayer/src/lib/redis.ts
@apps/relayer/src/lib/subgraph-client.ts
@apps/relayer/src/workers/settlement-watcher.ts
@apps/relayer/src/workers/notification-fanout.ts
@apps/relayer/src/workers/oracle-adapters/pyth-adapter.ts
@apps/relayer/src/lib/secret-manager.ts
@apps/relayer/src/workers/alerts.ts
</context>

<verified_facts>
Facts confirmed against source 2026-06-11 (audit line numbers verified — no drift):

- **Redis singleton:** `getRedis()` in `lib/redis.ts:27`; tests inject ioredis-mock via `vi.mock` of the redis module + `_setRedisForTesting`. New code MUST call `getRedis()` lazily inside functions (never capture at module top-level) so existing test mocks keep working.
- **8 cache sites (key / get / set / TTL):**
  - `routes/feed.ts` — `feed:firstpage` / :85 / :203 / EX 10 (both already try/catch'd)
  - `routes/live-state.ts` — `livestate:{id}` / :253 / :511 / EX 4 (`CACHE_TTL_SECONDS`). The `statusVersionKey` read at :374 is NOT a response cache — leave it as-is.
  - `routes/profile.ts` — `profile:{addr}` / :216 (wrapped in `withTimeout(…, 2000)`) / :431 / EX 60
  - `lib/ens-resolver.ts` — ENS name / :57 / :86 / EX 86400; stores raw string with `'::null::'` sentinel (NOT JSON)
  - `routes/settle.ts` — :290 / :348 / EX 60
  - `routes/duels.ts` — duels_list / :215 / :314 / EX 10
  - `routes/duel-live-state.ts` — :208 / :266 + :385 / EX 4
  - `routes/calls-dup-check.ts` — `dup-check:{hash}` / :153 (UNGUARDED — throws 500) / set ~:220 / EX 60
  - `lib/follow-graph.ts` (backs feed-from-your-x/fc) — :110 / :193 / EX 1h (`FOLLOW_GRAPH_CACHE_TTL_SECONDS`)
- **5 hard-500 paths:** `calls-dup-check.ts:153` (unguarded get); `paymaster-policy.ts:135` POST `getPaymasterCount` → `upstash-counter.ts:54` unguarded `redis.get` throws (also `registerSenderMapping` at :165 → unguarded `redis.set`); `paymaster-policy.ts:191` GET same; `social-link.ts:312` unguarded `redis.del`; `synthetic-event-handler.ts:94` unguarded SETNX; `admin-paymaster.ts:51` unguarded `redis.set`.
- **subgraph-client.ts:** single choke point `executeQuery<T>` (private, :271-308) — reads `SUBGRAPH_STUDIO_URL`, 10s `AbortSignal.timeout`, throws on !res.ok / GraphQL errors / no data. ~10 exported query functions all route through it. `SubgraphFeedResponse` has `items/nextCursor/_meta`. feed.ts builds `_source: racedResult.source` ('subgraph'|'fallback').
- **settlement-watcher.ts:** `CALL_REGISTRY_ABI` (19-field getCall tuple, :67-101) and `ADAPTER_MAP_ABI` (:107-118) are module-private consts — add `export` to reuse. `getAcceptedChallengeIds` (:165) is exported but uses raw fetch (bypasses any breaker). `enqueueSettlement` (:254) has zero callers anywhere — confirmed dead producer.
- **pyth-adapter.ts:** `settlePythCall({callId, updateData, acceptedChallengeIds, walletClient, publicClient, settlementManagerAddress})` exported at :334 — checks SM ETH balance, reads `getUpdateFee`, calls `SM.settle` with value. `PythAdapter` class: `new PythAdapter(hermesClient, {maxRetries, retryIntervalMs, confidenceThresholdNumerator: 200})`; `fetchAndVerify({priceId, callId})` returns `{status: PythAdapterStatus.SettlementDelayed | DisputeWindowOpened | <ok>, updateData?}`. priceId derivation: `call.assetA.toString(16).padStart(64, '0')` (watcher case 0, :477).
- **CallStatus ordinals (live-state.ts:125-128):** Live=0, Settled=1, Disputed=2, CallerExited=3. getCall on a nonexistent id returns a zeroed tuple (caller == zero address), NO revert (live-state.ts:319 relies on this).
- **notification-fanout.ts architecture to mirror:** `startX(config) → handle {stop(), getStats()}`; setInterval with `tick().catch` safety net; per-item try/catch inside tick; init re-seed pattern.
- **alerts.ts:** `AlertEvent` union — `settle_failed` is P0. index.ts:366 already repurposes `tvl_approach` (P1) with `{category:'infra', subsystem, message}` payload for infra alerts — reuse that pattern for the poller IDLE alert so the `AlertEvent` union and `test/alerts.test.ts` stay untouched. Use `sendAlertSafe` from workers (never throws).
- **secret-manager.ts:** optional secrets via `fetchSecret(name)` batch (:163-187); dev falls back to `process.env`; fetched values are mirrored into `process.env`. `RelayerEnv` type lives in `src/types.ts`.
- **index.ts:** pino redact paths at :94-107 (must add `SETTLEMENT_SIGNER_PRIVATE_KEY`); throw-stub walletClient at :235; `startSettlementWatcher` in onReady at :318-333; onClose stops the watcher at :386-392. RPC env pattern: `fallback([http(process.env.RPC_URL_ARBITRUM_SEPOLIA ?? process.env.ARBITRUM_SEPOLIA_RPC_URL), http()])` (:214-217).
- **Tests:** suite = `pnpm --filter @call-it/relayer exec vitest run` (264 pass / 1 skip baseline). Route tests mock the redis module with ioredis-mock and mock viem with a fallback passthrough (`vi.mock('viem', async (importOriginal) …`). Vitest isolates module registry per test FILE, so cross-file L1 pollution is impossible; within-file pollution is handled via a `_clearAllForTesting()` export.
- **Addresses:** read from `@call-it/shared` constants (`CALL_REGISTRY_ARBITRUM_SEPOLIA`, `SETTLEMENT_MANAGER_ARBITRUM_SEPOLIA`) exactly as settlement-watcher.ts does. (Reference only: CR 0xc79bB19dBCA44D8b467b9f7bbb191b56e9fb3CB0, SM 0x2E26eEb3b4CC9FA49B543846ea2E01B7600897e7.)
</verified_facts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: memory-cache.ts L1 primitive + lib/cache.ts L1-first adapter (with unit tests)</name>
  <files>apps/relayer/src/lib/memory-cache.ts, apps/relayer/src/lib/__tests__/memory-cache.test.ts, apps/relayer/src/lib/cache.ts</files>
  <behavior>
    memory-cache.test.ts (vitest, `vi.useFakeTimers()`):
    - Test 1: `set(k, v, 1000)` → `get(k)` returns v; advance 1001ms → `get(k)` returns undefined (TTL expiry).
    - Test 2: fill to maxEntries (construct with small max, e.g. `new MemoryCache(3)`) then set a 4th key → oldest-inserted key evicted, newest 3 retained.
    - Test 3: after TTL expiry but within staleHorizonMs → `getStale(k)` returns `{value, fresh: false, ageMs > ttl}`; while fresh → `{value, fresh: true}`; past staleHorizonMs (advance > 1h) → `getStale(k)` returns null.
    - Test 4: re-`set` of an existing key refreshes both value and insertion order (not evicted as oldest).
  </behavior>
  <action>
    **1. NEW `apps/relayer/src/lib/memory-cache.ts`** — bounded TTL map. The relayer is a SINGLE Fly machine (fly.toml, auto_stop_machines=false), so an in-process map is a legitimate L1 — state the rationale in the header comment.

    ```
    export interface StaleResult&lt;T&gt; { value: T; fresh: boolean; ageMs: number }
    export class MemoryCache {
      constructor(maxEntries = 500)
      get&lt;T&gt;(key: string): T | undefined            // fresh-only (now &lt; expiresAt)
      set&lt;T&gt;(key: string, value: T, ttlMs: number): void
      getStale&lt;T&gt;(key: string, staleHorizonMs = 3_600_000): StaleResult&lt;T&gt; | null
      delete(key: string): void
      _clearAllForTesting(): void
    }
    export const memoryCache = new MemoryCache();   // shared default instance (used by lib/cache.ts)
    ```

    Implementation: a `Map<string, {value, expiresAt, insertedAt}>`. Eviction: on `set` of a NEW key when size >= maxEntries, delete the Map's first (oldest-inserted) key — JS Maps preserve insertion order. On `set` of an EXISTING key, delete-then-set to refresh insertion order. `getStale`: entry exists and `now - insertedAt <= staleHorizonMs` → return `{value, fresh: now < expiresAt, ageMs: now - insertedAt}`; otherwise delete the entry and return null. No timers/intervals — purely lazy expiry (zero idle CPU; this module must not create background work).

    **2. Write the failing tests first** (`src/lib/__tests__/memory-cache.test.ts` per `<behavior>` — colocate with the existing `src/lib/__tests__/x-write-client.test.ts` pattern), run them RED, then implement, run GREEN.

    **3. NEW `apps/relayer/src/lib/cache.ts`** — L1-first read-through/write-through helper used by Task 2:

    ```
    export async function getCached&lt;T&gt;(key: string): Promise&lt;T | null&gt;
    export async function setCached&lt;T&gt;(key: string, value: T, ttlSeconds: number): Promise&lt;void&gt;
    ```

    - `getCached`: (a) `memoryCache.get<T>(key)` → hit returns immediately; (b) else `getRedis().get(key)` wrapped in try/catch AND bounded with the existing `withTimeout(…, 2000)` helper (same one profile.ts:216 imports — locate and reuse, do not duplicate) — on ANY Redis error/timeout, `logger.warn({event:'cache_redis_read_failed', key})` and return null; (c) on a Redis hit, `JSON.parse` (parse failure = miss, return null), backfill the L1 via `memoryCache.set(key, parsed, ttlSeconds*1000)`, return parsed.
    - `setCached`: ALWAYS `memoryCache.set(key, value, ttlSeconds*1000)` first (memory write cannot fail); then best-effort `getRedis().set(key, JSON.stringify(value), 'EX', ttlSeconds)` in try/catch — on error `logger.warn({event:'cache_redis_write_failed', key})`, never throw.
    - CRITICAL: call `getRedis()` lazily INSIDE each function body (never at module scope) so the existing tests' `vi.mock` of the redis module keeps applying. Import `getLogger` lazily-safe the same way other libs do.
    - Returns/accepts parsed objects (not JSON strings) — call sites in Task 2 drop their own `JSON.parse`/`JSON.stringify`.
  </action>
  <verify>
    <automated>cd "/c/Users/woshv/Desktop/Call it" && pnpm --filter @call-it/relayer exec vitest run src/lib/__tests__/memory-cache.test.ts && pnpm --filter @call-it/relayer build</automated>
  </verify>
  <done>MemoryCache passes TTL-expiry, FIFO-eviction, stale-retrieval, and re-set-order tests under fake timers; lib/cache.ts compiles and exports getCached/setCached with guarded lazy getRedis(); relayer tsc build green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire L1-first caching into all 8 cache sites + guard the 5 hard-500 Redis paths</name>
  <files>apps/relayer/src/routes/feed.ts, apps/relayer/src/routes/live-state.ts, apps/relayer/src/routes/profile.ts, apps/relayer/src/lib/ens-resolver.ts, apps/relayer/src/routes/settle.ts, apps/relayer/src/routes/duels.ts, apps/relayer/src/routes/duel-live-state.ts, apps/relayer/src/routes/calls-dup-check.ts, apps/relayer/src/lib/follow-graph.ts, apps/relayer/src/routes/paymaster-policy.ts, apps/relayer/src/routes/social-link.ts, apps/relayer/src/workers/synthetic-event-handler.ts, apps/relayer/src/routes/admin-paymaster.ts</files>
  <action>
    **Part A — L1 adapter on the 8 cache sites.** For each site listed in `<verified_facts>`, replace the direct `redis.get(key)` read with `await getCached<T>(key)` and the `redis.set(key, json, 'EX', ttl)` write with `await setCached(key, responseObject, ttl)`. Rules:
    - Keep every cache KEY string and TTL second-value EXACTLY as today (feed:firstpage 10s; livestate 4s; profile 60s; ENS 24h/86400; settle 60s; duels 10s; duel-live-state 4s; dup-check 60s; follow-graph 1h). Keep all response shapes, headers (`x-source: 'cache'` on hits), and log events byte-identical — these are contract surfaces for the web app and existing tests.
    - Drop the per-site `JSON.parse`/`JSON.stringify` (the helper owns serialization). For `profile.ts:216`, the helper's internal 2s timeout replaces the route-level `withTimeout` wrapper on that read.
    - `lib/ens-resolver.ts` keeps its `'::null::'` sentinel semantics but stores it through the helper (the value becomes the JSON string `"::null::"` in Redis — safe: it is an ephemeral cache; legacy non-JSON entries fail JSON.parse in the helper and read as misses).
    - `live-state.ts:374` (`statusVersionKey` read) is NOT a response cache — do not convert it; just ensure it cannot throw out of the handler (wrap in try/catch defaulting sv to '0' if it isn't already guarded).
    - Do NOT touch other non-cache Redis ops (paymaster-counter INCRBY integrity, upstash-counter SETNX idempotency, stylus-deactivation locks, auto-post statusVersion INCR) — quick-260610-sr0's no-commandTimeout rationale in redis.ts applies.

    **Part B — guard the 5 hard-500 paths** (all logging via existing `getLogger()` structured events):
    1. `routes/calls-dup-check.ts:153` — wrap the `redis.get` (now `getCached`, already guarded by the helper — verify the call site no longer throws) AND the result-cache write at ~:220 so a Redis outage skips the cache and proceeds straight to the on-chain `activeDuplicateHashes` check (which already fail-opens).
    2. `routes/paymaster-policy.ts:135` (POST /paymaster/policy) — wrap `getPaymasterCount(privyUserId)` + `registerSenderMapping` in try/catch: on Redis error, **fail closed** — log `{event:'paymaster_policy_redis_down', decision:'deny'}` and return the existing JSON-RPC deny shape (`error: {code: -32000, message: 'sponsorship-cap-exceeded'}` — reuse the exact deny response so the bundler contract is unchanged). Rationale comment: cannot verify the 5-tx lifetime cap without the counter, so sponsoring would be an unbounded spend.
    3. `routes/paymaster-policy.ts:191` (GET /api/paymaster-count) — wrap `getPaymasterCount`: on Redis error return `200 {count: null, capacity: 5, remaining: null, degraded: true}` and log warn.
    4. `routes/social-link.ts:312` — make the unlink-purge `redis.del(followCacheKey(...))` best-effort: `.catch` → `logger.warn({event:'unlink_redis_purge_failed'})`; the Postgres delete (the durable store, step 1) already succeeded, so still return 200 `{purged: true}`. Comment: the SocialUnlinked watcher backstop (AUTH-17) re-purges Redis when it recovers.
    5. `workers/synthetic-event-handler.ts:94` — wrap the SETNX nonce claim: on Redis ERROR (not on `acquired !== 'OK'`, which stays a 400 replay-reject), fall back to an in-memory nonce check using a dedicated module-level `new MemoryCache(1000)`: if `get(nonceKey)` exists → 400 nonce-replay; else `set(nonceKey, '1', NONCE_TTL_SECONDS*1000)` and proceed. Comment WHY this is sound: single Fly machine — there is no second process a replay could race against; the memory fallback preserves the replay guard during Redis outages instead of 500ing the synthetic-alert CI probe.
    6. `routes/admin-paymaster.ts:51` — wrap `redis.set('paymaster:cap', ...)` in try/catch: on error return `503 {error: 'cache_unavailable'}` and log error. (A silently-unsaved cap would be an invisible safety-limit failure — surface it to the operator.)

    **Part C — existing-suite compatibility sweep.** Run the FULL relayer suite. Where an existing test fails because the L1 now serves a value the test expected to be re-fetched after flushing the Redis mock (within-file pollution only — vitest isolates files), add `memoryCache._clearAllForTesting()` (and the synthetic-nonce instance's clear if needed — export `_clearNonceCacheForTesting` from synthetic-event-handler) to that test file's existing `beforeEach`. Do NOT weaken or delete any existing assertion. Expected hot spots: `__tests__/feed.test.ts`, `__tests__/calls-dup-check.test.ts`, `__tests__/profile.test.ts`, `__tests__/paymaster-policy.test.ts`, `test/social-link-route.test.ts`.

    **Commit (1 of 3, covers Tasks 1+2):** `feat(quick-260611-h36): in-process L1 cache + Redis guards on hard-500 paths` — stage ONLY the files listed in this task + Task 1 (explicit `git add` paths under apps/relayer/src; see plan-wide constraints for the never-stage list).
  </action>
  <verify>
    <automated>cd "/c/Users/woshv/Desktop/Call it" && pnpm --filter @call-it/relayer exec vitest run && pnpm --filter @call-it/relayer build</automated>
  </verify>
  <done>All 8 cache sites read L1-first and survive a dead Redis (helper guards); the 5 hard-500 paths return their specified degraded responses instead of 500; full relayer suite green (264 baseline preserved, D-15); build exit 0; commit 1 created with only intended files staged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Subgraph circuit breaker + last-known-good snapshots in subgraph-client.ts</name>
  <files>apps/relayer/src/lib/subgraph-client.ts, apps/relayer/src/lib/__tests__/subgraph-breaker.test.ts, apps/relayer/src/routes/feed.ts</files>
  <behavior>
    subgraph-breaker.test.ts (mock `globalThis.fetch` via `vi.stubGlobal`, set `SUBGRAPH_STUDIO_URL` env, fake timers, call exported `_resetBreakerForTesting()` in beforeEach):
    - Test 1 (opens on failure): fetch rejects (or returns 429) → query throws → breaker open; an immediately-following query does NOT hit fetch again (fetch call count unchanged) and throws (no snapshot yet).
    - Test 2 (stale served while open): a successful query stores last-good; then a failure opens the breaker; the next identical query returns the last-good data WITHOUT calling fetch, and the with-meta variant reports `stale: true`.
    - Test 3 (half-open probe after cooldown): advance fake time past SUBGRAPH_BREAKER_COOLDOWN_MS (120000 default) → next query DOES call fetch (probe); on success the breaker closes and result is fresh (`stale: false`); on failure it re-opens (subsequent query again short-circuits).
    - Test 4 (stale horizon): last-good older than 1h (advance time) is NOT served — query throws exactly as the no-snapshot case.
  </behavior>
  <action>
    All inside `lib/subgraph-client.ts` (single choke point — every exported query function benefits automatically):

    1. Rename the existing private `executeQuery` to `executeQueryUpstream` (body unchanged: SUBGRAPH_STUDIO_URL, Bearer key, 10s AbortSignal — keep quick-260610-sr0 behavior).
    2. Add breaker state (module-level): `let breakerOpenUntil = 0;` + a dedicated `const lastGood = new MemoryCache(200);` (own instance — never the shared `memoryCache`, so feed-route keys can't evict snapshots). Cooldown from `process.env.SUBGRAPH_BREAKER_COOLDOWN_MS`, default `120_000` (read per-call so tests can vary it). Export `_resetBreakerForTesting()` (zeroes breakerOpenUntil + clears lastGood).
    3. New internal `executeQueryWithMeta<T>(query, variables): Promise<{data: T; stale: boolean}>`:
       - snapshot key = `createHash('sha1').update(query + JSON.stringify(variables)).digest('hex')` (node:crypto).
       - If `Date.now() < breakerOpenUntil` (circuit OPEN): `lastGood.getStale<T>(key, 3_600_000)` → if present, `logger.warn({event:'subgraph_breaker_stale_served', key, ageMs})` and return `{data, stale: true}`; else THROW `new Error('subgraph circuit open — no last-good snapshot')` so every caller's existing catch/degrade path fires exactly as today.
       - Else attempt `executeQueryUpstream`. SUCCESS → `lastGood.set(key, data, 30_000)` (fresh window irrelevant — getStale's 1h horizon governs), reset `breakerOpenUntil = 0`, return `{data, stale: false}`. FAILURE (any throw: network, 429 non-ok, GraphQL errors, AbortSignal timeout) → `breakerOpenUntil = Date.now() + cooldown`, log `{event:'subgraph_breaker_opened', cooldownMs}`, then try the stale path above; if no snapshot, rethrow the original error. (Half-open is implicit: once `Date.now() >= breakerOpenUntil` the next call IS the probe.)
       - Note: while OPEN, `getAcceptedChallengeIds` in settlement-watcher.ts is untouched (raw fetch, own [] fallback) — fine; only the poller's NEW path (step 5) routes through the breaker.
    4. Reimplement `executeQuery<T>` as `(await executeQueryWithMeta<T>(q, v)).data` — zero churn for the ~10 existing callers.
    5. Staleness surfacing for the feed: add optional `_stale?: boolean` to `SubgraphFeedResponse`; `queryFeed` uses `executeQueryWithMeta` and sets `_stale: true` when served from snapshot. In `routes/feed.ts`, when `racedResult.source === 'subgraph' && racedResult.data._stale` → set `_source: 'stale'` in the response body and `x-source: stale` header (the EXISTING `_source` field per task spec — additive value, shape unchanged). Strip `_stale` from the serialized response body (delete before building responseData) so the wire shape stays exactly `{items, nextCursor, _source}`.
    6. NEW export `queryAcceptedChallengeIds(callId: bigint): Promise<bigint[]>` — same GraphQL as settlement-watcher's `getAcceptedChallengeIds` (`challenges(where:{callId, status:"Accepted"}){challengeId}`) but routed through `executeQuery` (→ breaker + Studio Bearer key). THROWS on failure — the Task 4 poller wraps it with the []-fallback + warn. (Settlement-watcher's own copy stays untouched.)
    7. Write the `<behavior>` tests RED first where practical, then implement to GREEN.
  </action>
  <verify>
    <automated>cd "/c/Users/woshv/Desktop/Call it" && pnpm --filter @call-it/relayer exec vitest run src/lib/__tests__/subgraph-breaker.test.ts && pnpm --filter @call-it/relayer exec vitest run && pnpm --filter @call-it/relayer build</automated>
  </verify>
  <done>Breaker opens on failure, short-circuits to last-good (&lt;=1h) during cooldown, half-open probes after cooldown, throws unchanged when no snapshot; feed responses carry _source:'stale' when snapshot-served; queryAcceptedChallengeIds exported; full suite + build green. Commit (2 of 3): `feat(quick-260611-h36): subgraph circuit breaker with last-known-good snapshots`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Redis-free settlement-poller worker + index.ts wiring + BullMQ gate-off</name>
  <files>apps/relayer/src/workers/settlement-poller.ts, apps/relayer/src/workers/__tests__/settlement-poller.test.ts, apps/relayer/src/workers/settlement-watcher.ts, apps/relayer/src/lib/secret-manager.ts, apps/relayer/src/types.ts, apps/relayer/src/index.ts</files>
  <behavior>
    settlement-poller.test.ts (mirror existing worker-test style: `vi.mock` of pyth-adapter, subgraph-client, alerts modules; publicClient/walletClient as plain objects with `vi.fn()` readContract/writeContract; drive via `handle.tick()` directly — no real timers):
    - Test 1 (frontier discovery): getCall mocked → ids 1..3 return populated tuples, id 4 returns zeroed tuple (caller=0x000…0) → after tick, 3 calls tracked, exactly 4 getCall probes, frontier stays at 4 on the next tick (no re-probe of settled/terminal ids).
    - Test 2 (Pyth settle dispatch): expired Live Pyth call (adapterMap→0, fetchAndVerify→ok+updateData) → `settlePythCall` called once with {callId, updateData, acceptedChallengeIds from mocked queryAcceptedChallengeIds, walletClient, publicClient, settlementManagerAddress}.
    - Test 3 (non-Pyth skip): adapterMap→2 (DefiLlama) → structured skip log path taken, `settlePythCall` NEVER called, and the skip is logged at most once per call across two ticks.
    - Test 4 (IDLE dry-run): config without walletClient → `settlement_poller_would_settle` log per candidate, zero fetchAndVerify/settlePythCall calls.
    - Test 5 (attempt cap → P0): fetchAndVerify returns SettlementDelayed repeatedly → attempts increment per tick; on the 30th attempt `sendAlertSafe('settle_failed', …)` fired exactly once and the call enters a 10-min backoff (next immediate tick does not retry it).
    - Test 6 (subgraph failure fallback): queryAcceptedChallengeIds rejects → settle proceeds with `[]` + warn log.
  </behavior>
  <action>
    **1. settlement-watcher.ts (additive only):** add `export` to `CALL_REGISTRY_ABI` and `ADAPTER_MAP_ABI`. Nothing else changes — the watcher and `enqueueSettlement` stay intact for the gated BullMQ path.

    **2. NEW `apps/relayer/src/workers/settlement-poller.ts`** — mirror notification-fanout architecture (`start… → {stop(), tick(), getStats()}`, setInterval + `tick().catch` safety net, per-call try/catch so a tick never throws). HEADER COMMENT must document: (a) why this exists (BullMQ producer was never wired + Redis quota outage = settlement structurally inert; chain-polling needs neither Redis nor the subgraph for discovery), (b) in-memory attempt counters/backoffs RESET ON RESTART — acceptable tradeoff: retries restart from 0 and the 30-attempt P0 may re-fire after a reboot, (c) single-machine assumption.

    Config: `{ publicClient: PublicClient; walletClient?: WalletClient; callRegistryAddress?: Address (default CALL_REGISTRY_ARBITRUM_SEPOLIA); settlementManagerAddress?: Address (default SETTLEMENT_MANAGER_ARBITRUM_SEPOLIA); hermesUrl? (default 'https://hermes.pyth.network'); intervalMs? (default Number(process.env.SETTLEMENT_POLLER_INTERVAL_MS) || 60_000) }`. Constants: `MAX_ATTEMPTS = 30`, `BACKOFF_MS = 600_000`, `MAX_PROBES_PER_TICK = 50`.

    Module state (in the start closure): `frontier = 1n` (next unprobed id); `known: Map<string, {expiry: bigint; status: number; marketType: number; eventSubtype: number; assetA: bigint; terminal: boolean}>`; `attempts: Map<string, number>`; `backoffUntil: Map<string, number>`; `skipLogged: Set<string>`; `alerted: Set<string>`. One `PythAdapter` instance (HermesClient(hermesUrl), `{maxRetries: 30, retryIntervalMs: 60_000, confidenceThresholdNumerator: 200}` — mirrors watcher).

    Tick algorithm:
    1. **Discover (no subgraph):** while probes < MAX_PROBES_PER_TICK, `getCall(frontier)` via CALL_REGISTRY_ABI; if `caller` is the zero address → stop (VERIFIED: nonexistent id returns zeroed tuple, no revert); else record fields in `known`, mark `terminal: status === 1 || status === 2` (Settled/Disputed), `frontier++`.
    2. **Candidates:** known ids where `!terminal`, `status in {0 Live, 3 CallerExited}` (ordinals per live-state.ts:125-128), `now >= Number(expiry) * 1000`, and `now >= (backoffUntil.get(id) ?? 0)`. Re-read getCall for each candidate to confirm it is STILL unsettled (someone may settle externally); update `known`; newly-terminal ids are dropped + counters cleaned.
    3. **IDLE mode (no walletClient):** per candidate log `{event:'settlement_poller_would_settle', callId, expiry, status}` and continue — NO Hermes fetch, NO writes (dry-run).
    4. **Adapter routing:** read `SM.adapterMap(marketType, eventSubtype)` via the exported ADAPTER_MAP_ABI. If `!== 0` (not Pyth) → log once per call (`skipLogged` guard) `{event:'settlement_poller_skip_non_pyth', callId, marketType, eventSubtype, adapterType}` with message noting the criteria store is empty for these (known product gap) — do NOT alert, do NOT count attempts.
    5. **Pyth settle:** priceId = `call.assetA.toString(16).padStart(64, '0')`; `pythAdapter.fetchAndVerify({priceId, callId})`. `SettlementDelayed` (wide confidence) OR any transient throw → `attempts++`; when attempts reaches 30 → `sendAlertSafe('settle_failed', {callId, reason:'poller_max_attempts', attempts: 30})` ONCE (`alerted` guard) and set `backoffUntil = now + BACKOFF_MS`; after the cap, each further failure re-arms the 10-min backoff (no re-alert). Otherwise: `acceptedChallengeIds = await queryAcceptedChallengeIds(callId).catch(err => { warn log; return [] })` (Task 3 export, through the breaker); then `settlePythCall({callId, updateData, acceptedChallengeIds, walletClient, publicClient, settlementManagerAddress})`. Success → log `settlement_poller_settled` + txHash, mark terminal, clear counters. Failure → counts as an attempt (same path as above).

    Startup (inside `startSettlementPoller`): if no walletClient → `logger.warn({event:'settlement_poller_idle_mode'}, 'SETTLEMENT_SIGNER_PRIVATE_KEY absent — dry-run only')` + ONE `sendAlertSafe('tvl_approach', {category:'infra', subsystem:'settlement-poller', message:'poller IDLE: no settlement signer configured — expired calls will NOT be settled'})` (P1, reuses index.ts:366's infra-payload pattern — `AlertEvent` union and alerts tests untouched). NEVER log the account address or any key material anywhere in this file.

    **3. Signer plumbing:** add `SETTLEMENT_SIGNER_PRIVATE_KEY` to the OPTIONAL `fetchSecret` batch in `secret-manager.ts` (~:163-187) and to `RelayerEnv` in `src/types.ts` as `string | undefined`. In `index.ts`: add `'SETTLEMENT_SIGNER_PRIVATE_KEY'` to the pino redact paths (:94-107). After env init, if `env.SETTLEMENT_SIGNER_PRIVATE_KEY` is set, build `settlementPollerWalletClient = createWalletClient({ account: privateKeyToAccount(key as \`0x${string}\`), chain: arbitrumSepolia, transport: fallback([http(process.env.RPC_URL_ARBITRUM_SEPOLIA ?? process.env.ARBITRUM_SEPOLIA_RPC_URL), http()]) })` (imports: `createWalletClient` from viem, `privateKeyToAccount` from viem/accounts) — wrap construction in try/catch (malformed key → undefined + sanitized error log WITHOUT the key value); else leave undefined. The key is never printed, never committed, never set by us (operator sets the Fly secret later).

    **4. index.ts onReady wiring:** start the poller (mirroring the auto-post pattern): `if (process.env.SETTLEMENT_POLLER_ENABLED !== 'false') { try { pollerHandle = startSettlementPoller({ publicClient: notificationFanoutClient, walletClient: settlementPollerWalletClient, intervalMs: … }) } catch … }` (default ON). GATE the old BullMQ watcher: wrap the existing `startSettlementWatcher` block (:318-333) in `if (process.env.SETTLEMENT_BULLMQ_ENABLED === 'true')` — default FALSE — with a comment explaining BOTH reasons: (a) `enqueueSettlement` has no producer anywhere, so the queue worker can never receive a job, and (b) the idle BullMQ worker polls Upstash 24/7 and is the prime suspect for the 500K-command quota burn; `else` branch logs `{event:'settlement_watcher_bullmq_disabled'}`. Keep the throw-stub `settlementWalletClient` (:235) — it now only feeds the gated BullMQ path. In `onClose`, also stop `pollerHandle` if started.

    **5. Tests:** write `src/workers/__tests__/settlement-poller.test.ts` per `<behavior>` (RED→GREEN). Mock modules: `vi.mock('../oracle-adapters/pyth-adapter.js', …)` (PythAdapter class with controllable fetchAndVerify + settlePythCall fn + re-export PythAdapterStatus enum values used), `vi.mock('../../lib/subgraph-client.js', …)` (queryAcceptedChallengeIds), `vi.mock('../alerts.js', …)` (sendAlertSafe). publicClient.readContract `vi.fn` dispatches on `functionName` ('getCall' → tuple fixtures keyed by callId arg; 'adapterMap' → configured adapter). Zeroed-tuple fixture: all-zero fields with caller `0x0000000000000000000000000000000000000000`.

    **Commit (3 of 3):** `feat(quick-260611-h36): Redis-free chain-polling settlement poller + BullMQ gate` — stage only this task's six files.
  </action>
  <verify>
    <automated>cd "/c/Users/woshv/Desktop/Call it" && pnpm --filter @call-it/relayer exec vitest run src/workers/__tests__/settlement-poller.test.ts && pnpm --filter @call-it/relayer exec vitest run && pnpm --filter @call-it/relayer build</automated>
  </verify>
  <done>Poller discovers calls by ascending getCall (stops at zeroed tuple), settles expired Pyth calls via settlePythCall with breaker-routed acceptedChallengeIds ([] fallback), skips non-Pyth with one structured log per call, runs IDLE dry-run without a signer (single P1 alert), caps at 30 attempts → settle_failed P0 + 10-min backoff; BullMQ watcher startup gated default-off; key redacted + never logged; full suite + build green; commit 3 created.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| relayer → Upstash Redis | now treated as UNRELIABLE infrastructure — every call guarded |
| relayer → The Graph Studio | untrusted + rate-limited input; breaker caps blast radius; contract remains authority for challenge IDs (SM validates on-chain) |
| relayer → Arbitrum Sepolia (signer) | hot key signs SM.settle transactions |
| bundler → /paymaster/policy | sponsorship spend decision under degraded state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-h36-01 | Information disclosure | SETTLEMENT_SIGNER_PRIVATE_KEY (index.ts, poller) | mitigate | optional secret-manager load; added to pino redact paths; account/key never logged; never committed/staged/set by us |
| T-h36-02 | Elevation of privilege | paymaster-policy POST during Redis outage | mitigate | fail-closed: deny sponsorship when the cap counter is unreadable (Task 2 Part B-2) |
| T-h36-03 | Spoofing/replay | synthetic-event-handler nonce when Redis down | mitigate | in-memory nonce fallback preserves the replay guard; sound on a single Fly machine (documented in code) |
| T-h36-04 | Tampering (integrity) | stale subgraph snapshots presented as fresh | mitigate | `_source:'stale'` marker on feed + 1h hard stale horizon; receipts' outcome truth stays on-chain (querySettledFields already fail-safe-neutral) |
| T-h36-05 | DoS | unbounded in-process cache growth | mitigate | MemoryCache FIFO bound (500 default; 200 breaker; 1000 nonce), lazy expiry, no timers |
| T-h36-06 | DoS | poller RPC amplification on public RPC | mitigate | MAX_PROBES_PER_TICK=50, terminal-id skip set, expiry-aware candidate filter, 60s tick, fallback transport |
| T-h36-SC | Tampering | npm/pip/cargo installs | accept | zero new dependencies in this task (node:crypto + existing viem/bullmq/ioredis/vitest only) |
</threat_model>

<verification>
Plan-wide gates (NO deploy in this task):
1. `pnpm --filter @call-it/relayer build` — exit 0.
2. `pnpm --filter @call-it/relayer exec vitest run` — full suite green: 264 existing tests preserved (D-15) + new memory-cache (T1), subgraph-breaker (T3), settlement-poller (T4) suites passing.
3. `git log --oneline -3` shows the 3 atomic commits scoped `quick-260611-h36`; `git show --stat` for each confirms ONLY apps/relayer/src files (+ this plan's .planning docs if the workflow commits them separately).
4. Grep gate — no secret leakage: `grep -rn "SETTLEMENT_SIGNER_PRIVATE_KEY" apps/relayer/src | grep -v "process.env\|redact\|fetchSecret\|RelayerEnv\|SETTLEMENT_SIGNER_PRIVATE_KEY'" | grep -cv "^\s*//"` → no line logs or hardcodes a key value.
</verification>

<success_criteria>
- Feed/profile/live-state/settle/duels/dup-check/follow-graph reads survive BOTH a dead Redis and an open subgraph breaker, serving L1/last-good data (feed marked `_source:'stale'` when snapshot-served) instead of empty/500.
- The 5 previously hard-500 Redis paths return their specified degraded responses (skip-cache, fail-closed deny, degraded:true, best-effort 200, memory-nonce, 503 cache_unavailable).
- settlement-poller discovers and (with signer) settles expired Pyth calls with zero Redis/subgraph hard dependencies; IDLE mode is loud (P1 alert + dry-run logs) but inert; non-Pyth calls produce structured skip logs.
- BullMQ settlement worker no longer starts by default (SETTLEMENT_BULLMQ_ENABLED gate) — Upstash quota burn from idle polling stops on next deploy.
- Build green, full suite green, 3 atomic commits, nothing from the never-stage list touched.
</success_criteria>

<constraints>
- **apps/relayer ONLY.** No web, contracts, subgraph, or deploy changes. NO `flyctl deploy` in this task.
- **1-3 atomic commits** (mapped: commit 1 = Tasks 1+2, commit 2 = Task 3, commit 3 = Task 4). Stage with explicit paths.
- **NEVER stage:** `packages/contracts/lib/openzeppelin-contracts` (submodule), `call it frontend/`, `docs/`, `evidence/`, `.claude/`, `.planning/config.json`, any `.gitignore`, `apps/relayer/src/scripts/soak-*.sh`, `apps/web/tests/visual-smoke.spec.ts-snapshots/`.
- **SETTLEMENT_SIGNER_PRIVATE_KEY** is never printed, never committed, never set by us.
- Windows box — run all shell commands through the Bash tool (Git Bash), absolute paths.
- Read contract addresses from `@call-it/shared` constants — never hardcode (reference values in task spec are for cross-checking only).
- Keep all cache keys, TTLs, response shapes, and log-event names byte-identical except where a task explicitly changes them.
</constraints>

<output>
Create `.planning/quick/260611-h36-relayer-resilience-overhaul/260611-h36-SUMMARY.md` when done.
</output>
