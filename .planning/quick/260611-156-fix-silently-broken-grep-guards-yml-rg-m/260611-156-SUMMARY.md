---
phase: quick-260611-156
plan: 01
subsystem: ci
tags: [github-actions, ripgrep, grep-guards, usdc, solidity-pragma, safety-12, safety-13]

requires:
  - phase: quick-260610-vab
    provides: phase-1-gates negative-test fixture allowlist (the 4-file pattern mirrored here)
provides:
  - grep-guards.yml rg checks as REAL failing gates (ripgrep installed, fail-loud on missing binary, non-vacuous file selection)
  - phase-1-gates.yml IN-03 real-failure NEXT_PUBLIC_NETWORK guard, IN-04 grep -vF literal path filters, IN-05 signin-smoke timeout-minutes
affects: [ci, all-future-pushes]

tech-stack:
  added: []
  patterns:
    - "ubuntu-latest does NOT ship ripgrep — every rg-using workflow needs an apt install step + a `command -v rg` fail-loud gate (exit 127 inside `if rg` / `$(rg ... || true)` reads as 'no match')"
    - "ripgrep ANDs --type filters with positive --glob patterns — `--type ts --type js --glob '**/*.rs'` selects NOTHING; use pure positive globs"

key-files:
  created: []
  modified:
    - .github/workflows/grep-guards.yml
    - .github/workflows/phase-1-gates.yml
    - packages/contracts/test/SettleTrace.t.sol

key-decisions:
  - "rg --type/--glob AND-trap CONFIRMED real by planted-address negative test — applied the plan's contingency: pure positive globs ts/tsx/js/jsx/mjs/cjs/rs"
  - "Pattern refined 0xff970a61 -> 0xff970a61[0-9a-f] (mirrors phase-1-gates 0xFF970A61[A-Fa-f0-9]+) to honestly triage 2 tracked guard self-references WITHOUT forbidden path exclusions; real address pastes (30 more hex chars) always trip"
  - "SettleTrace.t.sol floating pragma ^0.8.30 -> =0.8.30 fixed (not excluded) — real violation surfaced by the now-working solidity-pragma guard"

requirements-completed: [SAFETY-12, SAFETY-13, OPS-21, OPS-22]

duration: ~22min
completed: 2026-06-11
---

# Quick Task 260611-156: Fix Silently-Broken grep-guards.yml rg Checks Summary

**grep-guards.yml rg checks restored from green-but-vacuous (exit 127 swallowed since creation) to real failing gates — ripgrep installed + fail-loud gates + non-vacuous globs, proven by planted-address negative tests locally and log-level CI evidence; phase-1-gates IN-03/04/05 closed; master green end-to-end**

## What Was Done

### Task 1 — grep-guards.yml (commit d188706)

- **Install ripgrep** step added as the first step of both `usdc-paste` and `solidity-pragma` jobs (`sudo apt-get update && sudo apt-get install -y ripgrep`). ubuntu-latest does not ship rg; every check had exited 127 ("command not found") and read as green since workflow creation (evidence: run 27308922117).
- **Fail-loud gate** `command -v rg >/dev/null || { echo "::error::..."; exit 1; }` added as the first line of all 4 rg-using run blocks (3 usdc-paste steps + 1 solidity-pragma step) so a future runner change can never silently no-op again.
- **AND-semantics trap CONFIRMED REAL**: with the original `--type ts --type js --glob '**/*.rs'`, a planted bridged-address `.ts` file did NOT trip (rg ANDs type filters with positive globs → zero files selected). Applied the plan's contingency: pure positive globs `**/*.ts **/*.tsx **/*.js **/*.jsx **/*.mjs **/*.cjs **/*.rs` (strengthen-only — strict superset of the intended coverage).
- **Fixture exclusions** (the only new path exclusions, mirroring the phase-1-gates allowlist from quick-260610-vab): step 1 `!packages/shared/test/usdc.test.ts`; step 2 `!packages/contracts/src/constants/USDC.sol` + `!packages/contracts/test/USDC.t.sol`; step 3 needed none (confirmed locally — its `(af88d065|75faf114)` pattern never matches the bridged fixture value).
- **Local proofs (all hard gates passed)**: planted `.ts`, `.rs`, and `.sol` files containing `0xFF970A61A04b1cA14834A43f5dE4533eBDDB5CC8` each tripped their step's exact command; all 4 commands clean on the unplanted tree; scratch files deleted and confirmed absent via `git status --porcelain` before any `git add`.
- `env-network` job: untouched (diff shows additions only in the two rg jobs).

### Task 2 — phase-1-gates.yml (same commit)

- **IN-03**: local probe exited 0 (guard found — `apps/web/lib/privy-config.ts` matches testnet/mainnet inside the searched paths), so the violation branch was promoted to a real failure: `::error` + `exit 1`.
- **IN-04**: all 8 allowlist filter lines (`usdc-paste-guard` job + phase-1-complete-gate step h) changed `grep -v` → `grep -vF` (literal path matching; `grep -c 'grep -vF'` = 8).
- **IN-05**: `timeout-minutes: 15` added to the `signin-smoke` job.

### Task 3 — CI proof + master landing

- Test branch `ci/quick-260611-156-grep-guards-rg` pushed → 4 runs ALL green: Grep Guards 27314313716, Phase 1 Gates 27314313728 (signin-smoke included; complete-gate skipped as designed — tag/dispatch-gated), Contracts Tests 27314313734 (compile-proves the SettleTrace pragma pin), CI 27314313764.
- **Log-level proof (beyond green)** on run 27314313716: `command not found` count = **0**; both "Setting up ripgrep" install markers present; all 5 PASS lines emitted (3 usdc-paste steps + pragma + env-network) — rg genuinely executed.
- ff-only merge → master push `0d690bf..d188706`; master Grep Guards run **27314813100 = success** on headSha d188706. Master never red.
- Test branch deleted locally and on origin.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Vacuous rg file selection — applied the plan's documented contingency**
- **Found during:** Task 1 negative test (i)
- **Issue:** `--type ts --type js --glob '**/*.rs'` selects zero files (rg ANDs types with positive globs); planted `.ts` address did not trip
- **Fix:** pure positive globs covering ts/tsx/js/jsx/mjs/cjs/rs; re-proven until all 3 planted-file negative tests tripped
- **Commit:** d188706

**2. [Rule 1 - Bug] Pattern refined to `0xff970a61[0-9a-f]` (honest triage of 2 tracked self-references)**
- **Found during:** Task 1 local run with working globs
- **Issue:** two TRACKED files contain the bare 10-char prefix with no continuation hex char — `scripts/phase-0-smoke.ts` (embeds the guard's own rg command string) and `apps/web/tests/onboarding.spec.ts` (`expect(source).not.toMatch(/0xFF970A61/i)` negative assertion). Path-excluding them is forbidden (only the 3 documented fixtures allowed).
- **Fix:** require ≥1 continuation hex char — exactly mirrors phase-1-gates' already-reviewed `0xFF970A61[A-Fa-f0-9]+` pattern. NOT a detection loosening: any real paste of the bridged address carries 30 more hex chars and always trips (proven by all 3 planted-file tests). Documented in the workflow comment.
- **Commit:** d188706

**3. [Rule 1 - Bug] Real pragma violation fixed: `packages/contracts/test/SettleTrace.t.sol`**
- **Found during:** Task 1 local solidity-pragma run (iii)
- **Issue:** `pragma solidity ^0.8.30;` (floating) — a genuine violation the broken guard had been missing
- **Fix:** pinned to `=0.8.30` per the plan's "fix project source, don't exclude" instruction (compile-identical: foundry.toml pins solc 0.8.30). Committed alongside the workflows (required — the now-working guard would otherwise be red on CI). Compile + test proven by the green Contracts Tests run 27314313734.
- **Files modified:** packages/contracts/test/SettleTrace.t.sol (1 line, beyond the plan's files_modified list)
- **Commit:** d188706

## Verification Evidence

- Local: 4/4 exact rg commands clean on tracked files; negative tests tripped for planted .ts/.rs/.sol; `git status` confirmed zero tmp-negative-test residue before staging; both workflow files YAML-parse clean.
- CI branch: 4/4 runs green; grep-guards log shows 0 `command not found`, 2 successful ripgrep installs, 5 PASS lines.
- Master: Grep Guards run 27314813100 success on d188706; branch deleted local + origin; master never red.
- Strengthen-only audit: new exclusions = exactly the 3 documented fixtures; `!.git/**` hygiene NOT needed (never tripped); env-network job byte-identical; pattern refinement mirrors the existing reviewed phase-1-gates pattern.

## Known Stubs

None.

## Threat Flags

None — no new security surface; T-q156-01/02/03 mitigations from the plan's threat register all landed (install+fail-loud gate, negative-test-proven globs, exclusions limited to the 3 fixtures + grep -vF hardening).

## Self-Check: PASSED

- .github/workflows/grep-guards.yml — FOUND (modified, committed d188706)
- .github/workflows/phase-1-gates.yml — FOUND (modified, committed d188706)
- packages/contracts/test/SettleTrace.t.sol — FOUND (pragma =0.8.30, committed d188706)
- Commit d188706 — FOUND on master (origin/master updated 0d690bf..d188706)
- Master Grep Guards conclusion — success (run 27314813100)
