---
phase: quick-260611-156
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/grep-guards.yml
  - .github/workflows/phase-1-gates.yml
autonomous: true
requirements: [SAFETY-12, SAFETY-13, OPS-21, OPS-22]
must_haves:
  truths:
    - "The grep-guards usdc-paste and solidity-pragma jobs actually execute rg on CI (logs show no 'command not found'; apt install step succeeded)"
    - "A bridged USDC.e address planted in a checked .ts path is caught by the local rg command (negative test trips) before the fix is pushed"
    - "grep-guards is green on the test branch AND on master after ff-merge, with zero new violations on the current tree"
    - "phase-1-gates NEXT_PUBLIC_NETWORK guard step can actually fail (violation branch exits 1), USDC allowlist path filters use literal matching (grep -vF), and signin-smoke has timeout-minutes"
    - "Guards are only STRENGTHENED — the only new exclusions are the three documented negative-test fixture files"
  artifacts:
    - path: ".github/workflows/grep-guards.yml"
      provides: "ripgrep install steps + fail-loud rg presence gates + 3 fixture exclusions"
      contains: "ripgrep"
    - path: ".github/workflows/phase-1-gates.yml"
      provides: "IN-03 real-failure NEXT_PUBLIC_NETWORK guard, IN-04 grep -vF literal path filters, IN-05 signin-smoke timeout-minutes"
      contains: "timeout-minutes"
  key_links:
    - from: ".github/workflows/grep-guards.yml"
      to: "ubuntu-latest runner PATH"
      via: "apt-get install ripgrep + command -v rg fail-loud gate"
      pattern: "command -v rg"
    - from: ".github/workflows/grep-guards.yml usdc-paste"
      to: "negative-test fixture allowlist"
      via: "rg --glob '!...' exclusions mirroring phase-1-gates (quick-260610-vab)"
      pattern: "usdc\\.test\\.ts"
---

<objective>
Fix the silently-broken `.github/workflows/grep-guards.yml` CI workflow (every rg-based check no-ops with `rg: command not found`, exit 127 reads as "no match" in the `if rg ...; then` pattern — evidence: run 27308922117) and close three deferred Infos in `.github/workflows/phase-1-gates.yml` (IN-03/04/05 from 260610-vab-REVIEW.md).

Purpose: grep-guards is the ONLY home of the ADR-0001 two-address USDC allowlist, the strong repo-wide pragma pin (exact `=0.8.30`, not just no-floating), .js/.rs USDC.e coverage, and the env-network guard. It has been green-but-vacuous since creation. Restoring it closes a real SAFETY-12/SAFETY-13 enforcement gap.

Output: both workflows fixed, proven locally (including negative tests), proven on CI on a test branch with log-level evidence that rg executed, then ff-merged to master with a green master run. No red master at any point.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.github/workflows/grep-guards.yml
@.github/workflows/phase-1-gates.yml
@.planning/quick/260610-vab-make-ci-playwright-step-a-real-gate-cove/deferred-items.md
</context>

<constraints>
- Guards may only be STRENGTHENED. The ONLY permitted new source-file exclusions are the three documented negative-test fixtures: `packages/shared/test/usdc.test.ts`, `packages/contracts/src/constants/USDC.sol`, `packages/contracts/test/USDC.t.sol` (mirrors the phase-1-gates allowlist from quick-260610-vab). Do NOT delete the workflow or any check. `env-network` job: leave untouched.
- Every exact rg command MUST be run locally (rg is installed on this Windows box — run via Git Bash from repo root, e.g. `bash -c "rg ..."`) BEFORE pushing. Local pass + negative-test trip are hard gates.
- No red master: all CI proving happens on a test branch; master only receives an ff-merge of a commit already proven green.
- Stage ONLY the two workflow files. The working tree has unrelated dirty/untracked files (e.g. `call it frontend/`, evidence/, docs/) — never `git add -A`.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Fix grep-guards.yml (install rg, fail loud, fixture exclusions) and prove every command locally</name>
  <files>.github/workflows/grep-guards.yml</files>
  <action>
Edit `.github/workflows/grep-guards.yml`:

1. **Install ripgrep** — add as the FIRST step of both the `usdc-paste` and `solidity-pragma` jobs (before checkout):

   ```
   - name: Install ripgrep
     run: sudo apt-get update && sudo apt-get install -y ripgrep
   ```

   Keep `apt-get update` (stale package indexes on Azure mirrors cause 404s without it).

2. **Fail-loud rg gate** — add as the first line of every rg-using `run:` block (all 3 steps of `usdc-paste`, the 1 check step of `solidity-pragma` — 4 blocks total), so a future runner change can never silently no-op again:

   `command -v rg >/dev/null || { echo "::error::ripgrep missing on runner — install step failed or was removed"; exit 1; }`

   This is required even with the install step: exit 127 inside `if rg ...; then` and inside `VIOLATIONS=$(rg ... || true)` is otherwise swallowed.

3. **Fixture exclusions** (the known trap — once rg executes, these checks WILL trip on three legitimate negative-test fixtures added after the workflow was written):
   - `usdc-paste` step 1 (ts/js/rs check): add `--glob '!packages/shared/test/usdc.test.ts'` alongside the existing `!packages/shared/src/constants/usdc.ts` exclusion.
   - `usdc-paste` step 2 (.sol check): add `--glob '!packages/contracts/src/constants/USDC.sol'` and `--glob '!packages/contracts/test/USDC.t.sol'`.
   - `usdc-paste` step 3 (two-address allowlist): expected to need NO new exclusions — its pattern `(af88d065|75faf114)[0-9a-fA-F]{32}` does not match the bridged `0xff970a61...` fixture value, and the two known-good addresses are stripped by `grep -iv`. Confirm via the local run; only add a fixture exclusion if the local run proves one of the three fixture files trips it.
   - Update the step comments to name the full allowlist (mirroring the usdc-paste-guard comment block in phase-1-gates.yml).

4. **MANDATORY local verification** — run every exact rg command from the edited file via Git Bash from repo root (copy the command verbatim, including all globs/flags):
   - (i) All four commands must report zero violations on the current tree with the extended exclusions. CAVEAT: the local tree has untracked junk CI will never see (e.g. `call it frontend/`) and `--hidden --no-ignore` may surface `.git/` noise. A match in a path absent from `git ls-files` is not a violation — but note it, and treat the CI run in Task 3 as the authority. Do NOT add exclusions for local-only junk. If `.git/` itself produces matches, `--glob '!.git/**'` is acceptable hygiene (not a source-coverage loosening); add it only if it actually trips.
   - (ii) **Negative test (hard gate):** create scratch file `apps/web/tmp-negative-test.ts` containing the literal line `const bad = '0xFF970A61A04b1cA14834A43f5dE4533eBDDB5CC8';` — NEVER committed. Run usdc-paste step 1's command: it MUST report the match. **Known rg trap:** `--type ts --type js --glob '**/*.rs'` are AND'd in ripgrep — a positive glob restricts the type filter, likely making the step vacuous (matches NOTHING) even with rg installed. If the negative test does NOT trip, replace the file-selection flags in step 1 with pure positive globs covering at least the same set: `--glob '**/*.ts' --glob '**/*.tsx' --glob '**/*.js' --glob '**/*.jsx' --glob '**/*.mjs' --glob '**/*.cjs' --glob '**/*.rs'` (strengthen-only), then re-run until it trips. Also verify .rs coverage: create scratch `packages/contracts/tmp-negative-test.rs` with the same address line, confirm step 1 catches it, since .rs coverage is one of the reasons this workflow exists. Then run usdc-paste step 2's command against a scratch `packages/contracts/tmp-negative-test.sol` containing the address — must trip. DELETE all scratch files immediately after, and confirm with `git status` that no tmp-negative-test file remains anywhere.
   - (iii) Run the solidity-pragma command locally — expect clean (every committed .sol outside node_modules/out/target/lib uses exactly `pragma solidity =0.8.30;`). If violations surface in tracked files, triage honestly: fix the offending pragma if it is project source, or report the finding — do NOT blanket-exclude paths to make it pass.
  </action>
  <verify>
    <automated>bash -c "cd '/c/Users/woshv/Desktop/Call it' && rg --hidden --no-ignore --glob '**/*.sol' --glob '!**/node_modules/**' --glob '!**/out/**' --glob '!**/target/**' --glob '!packages/contracts/src/constants/USDC.sol' --glob '!packages/contracts/test/USDC.t.sol' --ignore-case '0xff970a61' . ; test $? -eq 1"</automated>
  </verify>
  <done>grep-guards.yml has install steps in both rg jobs, fail-loud gates in all 4 rg run blocks, exactly the documented fixture exclusions added; all 4 commands run locally clean on tracked files; negative tests for .ts, .rs, and .sol planted addresses all trip; scratch files deleted and confirmed absent via git status.</done>
</task>

<task type="auto">
  <name>Task 2: Fix phase-1-gates.yml deferred Infos IN-03/04/05</name>
  <files>.github/workflows/phase-1-gates.yml</files>
  <action>
Three targeted edits to `.github/workflows/phase-1-gates.yml`:

1. **IN-03 — NEXT_PUBLIC_NETWORK guard can never fail** (step "Assert NEXT_PUBLIC_NETWORK guard in env schema" in the `phase-1-complete-gate` job, currently `::notice` + exit 0 on both branches). First run the step's grep locally:
   `grep -r "NEXT_PUBLIC_NETWORK" apps/web/src/ apps/web/app/ apps/web/lib/ 2>/dev/null | grep -q "testnet\|mainnet"; echo $?`
   - If it exits 0 (guard found — expected: the web env schema validates NEXT_PUBLIC_NETWORK, which signin-smoke sets): promote the violation branch to a real failure — `echo "::error::NEXT_PUBLIC_NETWORK guard not found in web app env schema (D-09/OPS-21 network separation)"; exit 1`.
   - If it exits non-zero, the searched directories are likely wrong: locate the actual env-schema file via `git grep -l NEXT_PUBLIC_NETWORK -- apps/web` and fix the step's search paths to cover it, THEN promote to exit 1 as above.
   - Only if NEXT_PUBLIC_NETWORK validation genuinely does not exist anywhere in apps/web (do not expect this): leave it advisory but rename the step to start with "Advisory:" so it no longer masquerades as an assert. Never land a step that is guaranteed-red on master.

2. **IN-04 — unanchored `grep -v` path filters with unescaped dots**: change all four `grep -v "packages/..."` allowlist filter lines to `grep -vF "packages/..."` (fixed-string literal match) in BOTH occurrences: the `usdc-paste-guard` job step "Assert no bridged USDC.e address" AND the identical step (h) inside `phase-1-complete-gate` (8 lines total). Do not change the filter paths themselves.

3. **IN-05 — `signin-smoke` lacks a timeout**: add `timeout-minutes: 15` to the `signin-smoke` job (job level, next to `runs-on:`). The job builds web + runs one Playwright spec; 15 minutes caps a hung webServer (job default is 360).
  </action>
  <verify>
    <automated>bash -c "cd '/c/Users/woshv/Desktop/Call it' && grep -c 'grep -vF' .github/workflows/phase-1-gates.yml | grep -qx 8 && grep -A3 'signin-smoke:' .github/workflows/phase-1-gates.yml | grep -q 'timeout-minutes: 15'"</automated>
  </verify>
  <done>NEXT_PUBLIC_NETWORK step has a real failure path (or is explicitly renamed Advisory with documented justification); all 8 allowlist filter lines use grep -vF; signin-smoke has timeout-minutes: 15. Local grep for the IN-03 guard confirmed which branch the current tree takes.</done>
</task>

<task type="auto">
  <name>Task 3: Prove on CI via test branch, then ff-merge to master and confirm master green</name>
  <files>.github/workflows/grep-guards.yml, .github/workflows/phase-1-gates.yml</files>
  <action>
1. Create branch and commit ONLY the two workflow files:
   ```
   git switch -c ci/quick-260611-156-grep-guards-rg
   git add .github/workflows/grep-guards.yml .github/workflows/phase-1-gates.yml
   git commit -m "ci(quick-260611-156): fix silently-broken grep-guards rg checks + phase-1-gates IN-03/04/05"
   git push -u origin ci/quick-260611-156-grep-guards-rg
   ```
   Before committing, `git status` must show no tmp-negative-test files and no other staged paths.

2. The push triggers BOTH workflows: grep-guards (`branches: ['**']`) and phase-1-gates per-push jobs (its `paths:` includes `.github/workflows/phase-1-gates.yml`; `phase-1-complete-gate` itself stays skipped — tag/dispatch-gated, so the promoted IN-03 step is proven by review + the next gate run, not this push). Poll with bounded calls (no unbounded watch loops):
   `gh run list --branch ci/quick-260611-156-grep-guards-rg --limit 5 --json databaseId,name,status,conclusion`
   then `gh run view <id> --json status,conclusion,jobs` until both runs complete. The signin-smoke job takes several minutes — poll with sleeps between calls, bounded at ~20 attempts.

3. **REQUIRED beyond green** — pull the grep-guards run logs and prove rg actually executed:
   `gh run view <grep-guards-run-id> --log | grep -iE "command not found|Install ripgrep|PASS:"`
   Must show: the "Install ripgrep" steps succeeded, ZERO `command not found` occurrences, and the PASS lines for usdc-paste (3 steps) and solidity-pragma. If anything fails, fix forward on the branch (honoring the strengthen-only constraint) and re-push until green with proven execution.

4. Land on master:
   ```
   git switch master
   git merge --ff-only ci/quick-260611-156-grep-guards-rg
   git push origin master
   ```
   The master push triggers grep-guards again — poll that run to completion and confirm green (same `gh run view` pattern; re-check logs for `command not found` only if the run is red).

5. Cleanup: `git branch -d ci/quick-260611-156-grep-guards-rg && git push origin --delete ci/quick-260611-156-grep-guards-rg`.
  </action>
  <verify>
    <automated>gh run list --branch master --workflow "Grep Guards" --limit 1 --json conclusion,headSha --jq '.[0].conclusion == "success"'</automated>
  </verify>
  <done>grep-guards and phase-1-gates per-push runs green on the test branch with log evidence that rg executed (install succeeded, no `command not found`); master ff-merged and its grep-guards run green; test branch deleted locally and on origin; master never red.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pushed code → CI guards | grep-guards/phase-1-gates are the enforcement layer for SAFETY-12/SAFETY-13 invariants; untrusted future commits cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-q156-01 | Tampering | grep-guards usdc-paste/solidity-pragma jobs | mitigate | apt install ripgrep + `command -v rg` fail-loud gate in every rg run block — runner-image drift can never silently no-op the guard again |
| T-q156-02 | Repudiation (false pass) | rg `--type` + positive `--glob` conjunction (vacuous file selection) | mitigate | mandatory local negative tests (.ts/.rs/.sol planted address must trip) before push; pure-glob replacement contingency |
| T-q156-03 | Elevation (guard loosening) | rg/grep exclusion lists | mitigate | exclusions limited to the 3 documented fixture paths; phase-1-gates filters hardened to `grep -vF`; strengthen-only constraint in plan |
</threat_model>

<verification>
- Local: all 4 grep-guards rg commands clean on tracked files; negative tests trip for .ts, .rs, .sol planted bridged address; scratch files deleted (git status clean of tmp-negative-test*).
- CI test branch: grep-guards green AND logs prove rg executed (install step succeeded, zero `command not found`); phase-1-gates per-push jobs green.
- Master: ff-merge only after branch is green; master grep-guards run green; branch deleted local + origin.
- Diff review: no exclusion added beyond the 3 documented fixtures (+ optional `!.git/**` hygiene if it actually tripped); env-network job byte-identical.
</verification>

<success_criteria>
- grep-guards.yml rg checks are real failing gates: rg installed, fail-loud on missing binary, fixture allowlist matches phase-1-gates.
- phase-1-gates.yml: IN-03 step has a real failure path (or explicit Advisory rename with justification), IN-04 uses grep -vF in all 8 filter lines, IN-05 signin-smoke capped at 15 minutes.
- Master green end-to-end; no guard loosened.
</success_criteria>

<output>
Create `.planning/quick/260611-156-fix-silently-broken-grep-guards-yml-rg-m/260611-156-SUMMARY.md` when done.
</output>
