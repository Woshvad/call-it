---
phase: quick-260611-fo1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/call/[id]/page.tsx
autonomous: true
requirements: [QUICK-260611-FO1]

must_haves:
  truths:
    - "Following a call from the call page no longer fails with 'tx failed' when allowance(user -> FollowFadeMarket) is zero: handleFollow reads the USDC allowance and submits an exact-amount approve to FFM_ADDR before the follow write"
    - "Fading a call gets the identical allowance + exact-approve guard in handleFade before the fade write"
    - "A reverted approve transaction throws a clear Error (surfaced by FollowFadeModal's existing onSubmit catch) instead of proceeding to a doomed deposit"
    - "The existing follow/fade writeContractAsync calls remain byte-identical in address/abi/functionName/args shape"
    - "Web build and the full vitest suite (206 tests) stay green"
  artifacts:
    - path: "apps/web/app/call/[id]/page.tsx"
      provides: "allowance read + exact USDC approve to FollowFadeMarket inside both handleFollow and handleFade, between ensureActiveChain() and the deposit write"
      contains: "readContract(wagmiConfig"
  key_links:
    - from: "apps/web/app/call/[id]/page.tsx"
      to: "wagmi/actions + @/lib/wagmi"
      via: "readContract / waitForTransactionReceipt actions bound to wagmiConfig (callable inside useCallback, unlike hooks)"
      pattern: "from 'wagmi/actions'"
    - from: "handleFollow / handleFade"
      to: "USDC approve(FFM_ADDR, amountIn)"
      via: "writeContractAsync with USDC_ALLOWANCE_ABI when allowance < amountIn"
      pattern: "args: \\[FFM_ADDR, amountIn\\]"
---

<objective>
Fix the live follow/fade "tx failed" failure (verified 2026-06-11, real Rabby wallet on Sepolia): `handleFollow` and `handleFade` in the call receipt page call `FollowFadeMarket.follow/fade` directly with NO USDC approve step. `FollowFadeMarket._deposit` pulls USDC via `safeTransferFrom`, so with `allowance(user -> FFM) == 0` the wallet pre-simulation reverts and the user sees "tx failed". Every other USDC-pulling write in the app already has an approve step (publish, dispute bond, challenge create, duel accept) — follow/fade is the only gap.

Purpose: make follow/fade work for any wallet that has never approved USDC to FollowFadeMarket (i.e., every user today).
Output: one edited file — `apps/web/app/call/[id]/page.tsx` — with an allowance-check + exact-approve block in both handlers, mirroring the proven pattern in `usePublishCall.ts:195-222`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@apps/web/app/call/[id]/page.tsx
@apps/web/app/new/hooks/usePublishCall.ts

Existing identifiers in `apps/web/app/call/[id]/page.tsx` to REUSE (do not redefine):
- `USDC_ALLOWANCE_ABI` (line ~260) — already contains BOTH `allowance` and `approve` entries
- `FFM_ADDR` (line ~227, alias of `FOLLOW_FADE_MARKET_ADDRESS`) and `USDC_ADDR` (line ~237, alias of `USDC_ADDRESS`)
- `ACTIVE_CHAIN_ID` (imported from `@/lib/chain`)
- `userAddress` — `const { address: userAddress } = useAccount();` at line ~1257 in the main component scope (the same scope holding handleFollow/handleFade). Type: `` `0x${string}` | undefined ``
- `writeContractAsync` — from `useWriteContract()` at line ~1438, already a dep of both handlers
- `handleFollow` at lines ~1462-1471, `handleFade` at lines ~1473-1482

Proven approve pattern to mirror — `apps/web/app/new/hooks/usePublishCall.ts`:
- imports `readContract, waitForTransactionReceipt` from `'wagmi/actions'` and `wagmiConfig` from `'@/lib/wagmi'`
- local type `ActiveChainId = (typeof wagmiConfig)['chains'][number]['id']` (line 78) used to cast `chainId` on the wagmi/actions calls
- flow: readContract allowance -> if short, writeContract approve (exact amount) -> waitForTransactionReceipt -> throw on `status !== 'success'` (lines 195-222)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add USDC allowance + exact-approve to handleFollow and handleFade, then verify build + tests and commit</name>
  <files>apps/web/app/call/[id]/page.tsx</files>
  <action>
Edit `apps/web/app/call/[id]/page.tsx` ONLY. Three changes:

1. Imports + type (module top). Add `import { readContract, waitForTransactionReceipt } from 'wagmi/actions';` and `import { wagmiConfig } from '@/lib/wagmi';` (the page currently imports neither — confirm before adding; the existing `'wagmi'` import on line ~62 is hooks-only and stays untouched). Add a module-scope type alias `ActiveChainId` defined as `(typeof wagmiConfig)['chains'][number]['id']`, mirroring usePublishCall.ts:78 — needed because `ACTIVE_CHAIN_ID` from `@/lib/chain` is a broader union than wagmiConfig's chain ids and the wagmi/actions calls type-check against the config.

2. handleFollow (line ~1462) AND handleFade (line ~1473) — identical block in each, inserted AFTER the existing `await ensureActiveChain();` and BEFORE the existing `writeContractAsync` deposit call:
   - Guard: if `userAddress` is falsy, `throw new Error('Connect your wallet first.')`.
   - Read allowance via the wagmi/actions function (NOT a hook — these run inside useCallback): call `readContract(wagmiConfig, {...})` with `address: USDC_ADDR`, `chainId: ACTIVE_CHAIN_ID as ActiveChainId`, `abi: USDC_ALLOWANCE_ABI`, `functionName: 'allowance'`, `args: [userAddress, FFM_ADDR]`; treat the result as `bigint`.
   - If `allowance < amountIn`: submit the approve via the EXISTING hook `writeContractAsync` with `chainId: ACTIVE_CHAIN_ID`, `address: USDC_ADDR`, `abi: USDC_ALLOWANCE_ABI`, `functionName: 'approve'`, `args: [FFM_ADDR, amountIn]` — EXACT amount, NOT infinite/maxUint256, matching every other approve in the repo. Then `waitForTransactionReceipt(wagmiConfig, { hash: approveHash, chainId: ACTIVE_CHAIN_ID as ActiveChainId })` and if `receipt.status !== 'success'` throw `new Error('USDC approval failed — try again.')`.
   - The existing follow/fade `writeContractAsync` call stays byte-identical after this block.
   - Dep arrays: add `userAddress` to both useCallback dep arrays -> `[callId, userAddress, writeContractAsync]`.
   - Error handling: do NOT add try/catch — let errors propagate; FollowFadeModal's onSubmit catch (packages/ui/src/compound/FollowFadeModal.tsx:185-212) already surfaces error names/messages in the modal.

3. Comments: update the JSDoc/comment above each handler to note approve-then-deposit and why: FollowFadeMarket._deposit pulls USDC via safeTransferFrom, and allowance(user -> FFM) was zero — live "tx failed" 2026-06-11 (quick-260611-fo1).

DO NOT touch: the FFM write args/shape, FollowFadeModal, contracts, relayer, packages/ui, packages/shared, or `apps/web/app/api/frame/tx/[callId]/route.ts` (the Warpcast frame tx path also lacks approval but is the deferred Phase-10 tap-to-transact workstream — recorded as a known issue below, not fixed here).

Verify (Bash tool / Git Bash, from repo root — Windows box, do not use PowerShell for pnpm/git):
- `pnpm --filter @call-it/web build` exits 0
- `pnpm --filter @call-it/web exec vitest run` exits 0 (existing 206 tests stay green — this is wiring inside a component callback; no new fake-DOM tests per D-15)

Commit (Bash tool): stage ONLY `apps/web/app/call/[id]/page.tsx`. Commit message exactly:
`fix(quick-260611-fo1): follow/fade approve USDC to FollowFadeMarket before deposit (transferFrom had zero allowance)`
NEVER stage: `packages/contracts/lib/openzeppelin-contracts` (submodule), `call it frontend/`, `docs/`, `evidence/`, `.claude/`, `.planning/config.json`, any `.gitignore`, `apps/relayer/src/scripts/soak-*.sh`, `apps/web/tests/visual-smoke.spec.ts-snapshots/`.
  </action>
  <verify>
    <automated>cd "/c/Users/woshv/Desktop/Call it" && pnpm --filter @call-it/web build && pnpm --filter @call-it/web exec vitest run</automated>
  </verify>
  <done>Both handleFollow and handleFade read allowance(userAddress -> FFM_ADDR) and exact-approve USDC to FFM_ADDR before the deposit write; approve receipt status checked with a clear throw on revert; deposit writeContractAsync calls byte-identical; deps arrays include userAddress; web build + 206-test vitest suite green; single-file commit landed with the exact message above.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser wallet -> FollowFadeMarket | user-signed USDC approve + deposit cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-fo1-01 | Elevation of Privilege | USDC approve in handleFollow/handleFade | mitigate | exact-amount approve (`amountIn`), never infinite — matches the repo-wide approve convention (usePublishCall, dispute bond, challenge, duel) |
| T-fo1-02 | Tampering | approve receipt handling | mitigate | `waitForTransactionReceipt` + `status !== 'success'` throw before the deposit write — no deposit ever submitted on a failed approval |
</threat_model>

<verification>
- `pnpm --filter @call-it/web build` exits 0
- `pnpm --filter @call-it/web exec vitest run` exits 0 (206 tests, no new failures)
- `git show --stat HEAD` shows exactly one file changed: `apps/web/app/call/[id]/page.tsx`
- Grep gate: `grep -c "readContract(wagmiConfig" "apps/web/app/call/[id]/page.tsx"` returns >= 2 (one per handler)
</verification>

<success_criteria>
- A wallet with zero USDC allowance to FollowFadeMarket can follow or fade from the call page: the handler now prompts an exact-amount approve first, then the deposit — no more wallet pre-simulation "tx failed"
- No other file modified; FFM write args/shape unchanged; modal error surfacing unchanged
- Build + full web test suite green (D-15 gate)
</success_criteria>

<notes>
KNOWN ISSUE (out of scope, do not fix): `apps/web/app/api/frame/tx/[callId]/route.ts` (Warpcast frame tx path) also submits FFM deposits without an approve step. That path is part of the deferred Phase-10 in-Warpcast tap-to-transact workstream (D-01) and is intentionally left untouched here.

Self-follow/self-fade by the caller is legitimate — the contract has no self-follow ban (initPool seeds both pool sides at creation; first-position share math is sound). No UI gate should be added.
</notes>

<output>
Create `.planning/quick/260611-fo1-follow-fade-approve/260611-fo1-SUMMARY.md` when done.
</output>
