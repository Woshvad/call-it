---
phase: quick
plan: 260611-uf9
slug: target-above-current-guard
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/new/lib/target-guard.ts
  - apps/web/app/new/components/PriceTargetFields.tsx
  - apps/web/app/new/page.tsx
  - apps/web/tests/target-guard.test.ts
  - apps/relayer/src/lib/hermes-spot.ts
  - apps/relayer/src/routes/calls-preflight.ts
  - apps/relayer/src/lib/__tests__/hermes-spot.test.ts
autonomous: true
requirements: [QUICK-260611-UF9]
must_haves:
  truths:
    - "Composer: a manually-entered price target at or below the live current Pyth price shows an inline error on the target field AND Publish never opens the confirm modal (STRICT > rule — equality blocked; marketType 'priceTarget' only; user requirement 2026-06-11)"
    - "Composer: the inline guard error clears reactively when the user raises the target above the live price or the 30s price refresh moves below the target — derived render, never a one-shot setError that can go stale"
    - "Composer: when the live price is unavailable (usePythPrice error/null — D-07) the client-side check is SKIPPED — publish proceeds and the relayer layer enforces; no price is ever fabricated and publish is never blocked client-side on missing data"
    - "Relayer preflight: marketType 'priceTarget' rejects 422 (field 'targetValue', code 'target_not_above_current') when BigInt(targetValue) <= live Hermes price normalized to 1e8 — including the target == current equality boundary"
    - "Relayer preflight: Hermes unreachable/malformed/timeout → FAIL-CLOSED 422 (code 'price_unverifiable', copy 'Could not verify current price — try again shortly.') — never fail-open"
    - "Pyth expo normalization to 1e8 is exact and verified with a non-(-8) expo test case (multiply path expo=-6 AND floor-divide path expo=-10)"
    - "spreadVs and event market types are untouched by both layers; +pct chips remain upside-only (already can never violate)"
  artifacts:
    - path: "apps/web/app/new/lib/target-guard.ts"
      provides: "Pure guard predicate + message builder (no React, node-env testable)"
      exports: ["targetGuardViolation", "targetGuardMessage"]
    - path: "apps/web/app/new/page.tsx"
      provides: "Lifted usePythPrice call + onPublish early-return gate before setIsModalOpen"
      contains: "targetGuardViolation"
    - path: "apps/web/app/new/components/PriceTargetFields.tsx"
      provides: "price/status as props (hook removed) + derived inline guard error in the targetValue error slot"
      contains: "targetGuardMessage"
    - path: "apps/relayer/src/lib/hermes-spot.ts"
      provides: "Hermes spot fetch + exact 1e8 normalization + fail-closed guard evaluator"
      exports: ["normalizePythPriceTo1e8", "getSpotPrice1e8", "evaluateTargetGuard"]
    - path: "apps/relayer/src/routes/calls-preflight.ts"
      provides: "priceTarget spot-price gate aggregated into the existing D-31 errors[] → 422 path"
      contains: "price_unverifiable"
    - path: "apps/web/tests/target-guard.test.ts"
      provides: "Pure unit tests + source-assertion pins for the web wiring"
    - path: "apps/relayer/src/lib/__tests__/hermes-spot.test.ts"
      provides: "Normalization/expo, boundary, fail-closed, mock-HermesClient tests + route wiring pins"
  key_links:
    - from: "apps/web/app/new/page.tsx"
      to: "apps/web/app/new/lib/target-guard.ts"
      via: "targetGuardViolation early-return in onPublish before setIsModalOpen(true)"
      pattern: "targetGuardViolation"
    - from: "apps/web/app/new/components/PriceTargetFields.tsx"
      to: "apps/web/app/new/lib/target-guard.ts"
      via: "derived guard render from price prop + useWatch targetValue"
      pattern: "targetGuardViolation"
    - from: "apps/relayer/src/routes/calls-preflight.ts"
      to: "apps/relayer/src/lib/hermes-spot.ts"
      via: "getSpotPrice1e8 + evaluateTargetGuard for marketType 'priceTarget'"
      pattern: "getSpotPrice1e8"
    - from: "apps/relayer/src/routes/calls-preflight.ts"
      to: "apps/relayer/src/lib/with-timeout.ts"
      via: "withTimeout(…, 4_000, …) bounding the Hermes leg"
      pattern: "withTimeout"
---

<objective>
Block trivially-true price-target calls at creation. V1 settles >= ONLY (SettlementManager.sol:718 — no direction field), so a call like "ETH ≥ $37" while ETH trades at $2,500 is a guaranteed CALLED IT = free rep farming. Rule: marketType 'priceTarget' requires targetValue STRICTLY ABOVE the current live Pyth price at creation. No margin band — a target barely above current is a legitimate coin-flip. Two enforcement layers: (1) web composer inline validation with the already-fetched live price, (2) relayer preflight server-side enforcement that cannot be bypassed via UI, fail-closed when Hermes is unreachable.

On-chain enforcement is OUT OF SCOPE (contracts deployed; an oracle read at creation is contracts-v2). spreadVs and event market types untouched.

Purpose: protect reputation-score integrity — the core product is unfakeable receipts (USER REQUIREMENT 2026-06-11).
Output: new pure guard modules + wiring in composer and preflight route + tests on both apps. Single atomic code commit; NO push.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@apps/web/app/new/components/PriceTargetFields.tsx   — currently owns usePythPrice (line 37) + the targetValue error slot (lines 128-132); chips already upside-only
@apps/web/app/new/hooks/usePythPrice.ts               — returns { price: number|null, status } — USD float; D-07: null on any failure, refresh every 30s, failed refresh CLEARS price
@apps/web/app/new/lib/hermes-price.ts                 — formatUsdPrice (display formatter to reuse in the error copy)
@apps/web/app/new/lib/target-scale.ts                 — usdToTargetValue: USD float → 1e8 bigint (the unit bridge for the comparison)
@apps/web/app/new/page.tsx                            — onPublish (line 177-180: loadToken → setIsModalOpen) is the modal gate point; PriceTargetFields rendered at lines 279 (quote mode) and 397 (standard)
@apps/web/app/new/lib/web-call-schema.ts              — READ ONLY: documents why superRefine CANNOT host this check (no live-price access) — do not touch
@apps/web/app/new/lib/preflight-body.ts               — READ ONLY: proves body.assetA reaches the relayer as the 0x-prefixed Pyth feed id for priceTarget (line 62-67)
@apps/relayer/src/routes/calls-preflight.ts           — errors[] aggregation + PreflightFieldError shape {field, code, message} + single 422 return at section 5 (lines 384-397); imports use .js suffixes (ESM)
@apps/relayer/src/lib/with-timeout.ts                 — withTimeout(promise, ms, label) — rejects TimeoutError; reuse for the Hermes leg
@apps/relayer/src/workers/oracle-adapters/pyth-adapter.ts — HermesClient usage precedent: getLatestPriceUpdates([priceId]) → updates.parsed?.[0]?.price with string mantissa; constructed as new HermesClient(url, {})
@apps/relayer/src/workers/__tests__/pyth-adapter.test.ts  — mock-HermesClient injection pattern to follow
@apps/relayer/src/routes/__tests__/profile-registry-address.test.ts — source-assertion route-test precedent (readFileSync + toContain pins)
@apps/web/tests/target-scale.test.ts                  — web vitest precedent (real imports + source pins; vitest cwd is apps/web)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Web composer guard — pure target-guard module, lifted price hook, derived inline error, modal gate</name>
  <files>apps/web/app/new/lib/target-guard.ts, apps/web/app/new/components/PriceTargetFields.tsx, apps/web/app/new/page.tsx, apps/web/tests/target-guard.test.ts</files>
  <behavior>
    - targetGuardViolation(target 1e8 bigint, priceUsd float): true when target <= price scaled to 1e8 (below AND equality both violate — strict > rule); false when target strictly above
    - targetGuardViolation returns false when priceUsd is null OR targetValue is undefined (D-07: missing price → client check skipped, never blocks)
    - targetGuardMessage(2500) contains '2,500.00' (formatUsdPrice) and the copy 'Target must be above the current price'
    - Source pins: page.tsx contains 'targetGuardViolation' AND 'usePythPrice' (hook lifted); PriceTargetFields.tsx contains 'targetGuardMessage' and does NOT contain 'usePythPrice' (hook removed from component)
  </behavior>
  <action>
ARCHITECTURE DECISION (planner — documented for the executor): the check CANNOT live in webCreateCallSchema.superRefine (no live-price access, and that module is the D-29 parity surface). It also must NOT be a one-shot RHF setError: with zodResolver + mode 'onChange', every resolver run REPLACES the errors object, wiping manual errors mid-typing, and a setError'd message would NOT clear when the 30s price refresh moves the price (stale-error UX violation). Instead: (a) a DERIVED inline render in PriceTargetFields that is reactive by construction, and (b) a hard early-return gate in page.tsx's onPublish so the confirm modal never opens while the violation holds.

1. NEW `apps/web/app/new/lib/target-guard.ts` — pure module, no React (header comment: quick-260611-uf9, strict-above rule, SettlementManager v1 settles >= only per SettlementManager.sol:718, D-07 skip-on-missing-price contract). Two exports:
   - `targetGuardViolation(targetValue: bigint | undefined, priceUsd: number | null): boolean` — returns false when targetValue is undefined OR priceUsd is null (client check skipped — the relayer layer enforces; never fabricate). Otherwise scale the price via `usdToTargetValue(priceUsd)` from `./target-scale` (returns bigint | undefined — treat undefined as "no price" → false) and return `targetValue <= scaled`. Compare in 1e8 bigint space — NEVER float-compare. Equality IS a violation (a target equal to current is already at-or-above per the settlement rule).
   - `targetGuardMessage(priceUsd: number): string` — exact copy: "Target must be above the current price ($" + `formatUsdPrice(priceUsd)` (from `./hermes-price`) + ") — calls settle at-or-above target".

2. `apps/web/app/new/page.tsx` — LIFT the price hook so one fetch loop serves both the display and the gate:
   - Import `usePythPrice` and `targetGuardViolation`. After the `marketType = watch('marketType')` line, call `const { price: livePrice, status: livePriceStatus } = usePythPrice(marketType === 'priceTarget' ? formValues.assetA : undefined)` — passing undefined for non-priceTarget types keeps the hook idle (its feedId-null branch) so spreadVs/event add no fetch traffic.
   - Pass `price={livePrice} status={livePriceStatus}` to BOTH `<PriceTargetFields …>` render sites (quote mode ~line 279 and standard mode ~line 397).
   - In `onPublish`, BEFORE `await loadToken()` / `setIsModalOpen(true)`: read `const values = form.getValues()`; if `values.marketType === 'priceTarget' && targetGuardViolation(values.targetValue, livePrice)` → return early (modal never opens; the derived inline error in PriceTargetFields is already visible for exactly the duration of the violation — no setError, see decision note above; add a brief comment saying so). CRITICAL: update the useCallback dependency array to include `form` and `livePrice` — the current deps are `[loadToken]` and a stale `livePrice` closure would gate against an old price.
   - When `livePrice` is null the gate passes (D-07) and the relayer preflight enforces; a relayer 422 on field 'targetValue' already renders inline via usePublishCall's existing fieldErrors → setError mapping — no changes needed there.

3. `apps/web/app/new/components/PriceTargetFields.tsx` — accept the price as props, render the derived error:
   - Extend `PriceTargetFieldsProps` with `price: number | null` and `status: PythPriceStatus` (import the type from `../hooks/usePythPrice`). DELETE the internal `useWatch` for assetA (it only fed the hook) and the `usePythPrice` import/call — all existing price display logic (loading line, "Current price ·" line, chips visibility) reads the props unchanged.
   - Add `const targetValue = useWatch({ control, name: 'targetValue' })` and derive `const guardViolated = targetGuardViolation(targetValue, price)`.
   - Input border: extend the existing error style condition to `errors.targetValue || guardViolated`.
   - Below the existing `errors.targetValue` block, render the guard message div ONLY when `guardViolated && !errors.targetValue` (never stack two red lines): same mono/11px/accent-loss style as the existing error render, text `targetGuardMessage(price!)` (price is non-null whenever guardViolated is true). This is reactive by construction: target edits and 30s price refreshes both re-derive — error clears the moment the user raises the target or the price drops below it.
   - Chips need no change — they compute upside-only targets and can never violate.

4. NEW `apps/web/tests/target-guard.test.ts` (vitest, follows target-scale.test.ts conventions — real imports + source pins, cwd is apps/web):
   - Real-import unit tests per the behavior block: below-current → true; equality boundary → true; strictly above (e.g. target $2,500.01 vs price $2,500) → false; price null → false (D-07 skip); targetValue undefined → false; message contains the formatted price and copy.
   - Source-assertion pins (readFileSync): `app/new/page.tsx` contains 'targetGuardViolation' and 'usePythPrice'; `app/new/components/PriceTargetFields.tsx` contains 'targetGuardMessage' and does NOT contain 'usePythPrice'. These pin the lift + both wiring points without mounting React (precedent: chain-pinning.test.ts / presentation-sweep.test.ts).
  </action>
  <verify>
    <automated>cd "C:\Users\woshv\Desktop\Call it" && pnpm --filter @call-it/web test && pnpm --filter @call-it/web build</automated>
  </verify>
  <done>target-guard.test.ts green (boundary cases + D-07 skip + source pins); full web suite green (existing pins in target-scale.test.ts and new-call-publish.spec.ts tolerate the refactor — verified they only assert component-name presence and absence of stale 1e6 patterns); web build green. A target ≤ current live price shows the inline error and Publish cannot open the confirm modal; missing price skips the client check.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Relayer preflight enforcement — hermes-spot lib, fail-closed route gate, tests</name>
  <files>apps/relayer/src/lib/hermes-spot.ts, apps/relayer/src/routes/calls-preflight.ts, apps/relayer/src/lib/__tests__/hermes-spot.test.ts</files>
  <behavior>
    - normalizePythPriceTo1e8: expo -8 → identity; expo -6 → ×100 (exact multiply path, the non-(-8) verification case); expo -10 → floor ÷100
    - evaluateTargetGuard(target, spot): spot null → { ok:false, code:'price_unverifiable' } (FAIL-CLOSED); target < spot → rejected 'target_not_above_current'; target == spot → rejected (equality boundary); target > spot → { ok:true }
    - getSpotPrice1e8 with a mock HermesClient: good parsed response → normalized bigint; client throws → null; missing/malformed parsed → null; zero/negative mantissa → null (never throws)
    - Source pins: calls-preflight.ts contains 'getSpotPrice1e8', 'evaluateTargetGuard', 'withTimeout', and the gate is conditioned on 'priceTarget'
  </behavior>
  <action>
SCAN RESULT (planner verified): no Hermes helper exists in apps/relayer/src/lib/ — Hermes usage lives in workers (pyth-adapter.ts, settlement-watcher.ts constructs `new HermesClient(url, {})` with default 'https://hermes.pyth.network'). Create the lib helper here; do NOT duplicate client setup inside the route.

1. NEW `apps/relayer/src/lib/hermes-spot.ts` (ESM — relative imports use .js suffix; header comment: quick-260611-uf9, used by the preflight trivially-true guard). Three exports plus a lazy module-level singleton:
   - `normalizePythPriceTo1e8(mantissa: bigint, expo: number): bigint` — Pyth prices carry (price, expo); expo is TYPICALLY -8 but must never be assumed. Exact integer math: shift = expo + 8; shift >= 0 → `mantissa * 10n ** BigInt(shift)`; shift < 0 → `mantissa / 10n ** BigInt(-shift)` (floor). Comment why floor is EXACT for the strict-above rule at integer 1e8 granularity: for an integer target, target > floor(spot) ⟺ target > spot, so no precision is lost at the boundary.
   - `evaluateTargetGuard(targetValue: bigint, spot1e8: bigint | null)` returning a discriminated union `{ ok: true } | { ok: false; code: 'price_unverifiable' | 'target_not_above_current'; message: string }`. spot null → fail-closed `price_unverifiable` with message "Could not verify current price — try again shortly." (comment: FAIL-CLOSED is deliberate — fail-open would let users farm guaranteed CALLED ITs whenever Hermes blips, defeating the integrity guard). `targetValue <= spot1e8` → `target_not_above_current` with message "Target must be above the current price ($X) — calls settle at-or-above target." where X = `Number(spot1e8) / 1e8` formatted via toLocaleString('en-US', { maximumFractionDigits: 2 }) for values >= 1, and { maximumFractionDigits: 8 } for sub-$1 assets (PEPE/BONK class). Equality rejects (strict >).
   - `getSpotPrice1e8(feedId: string, client?: HermesClient): Promise<bigint | null>` — uses the passed client or a lazy module-level singleton `new HermesClient(process.env['HERMES_URL'] ?? 'https://hermes.pyth.network', {})` (import HermesClient from '@pythnetwork/hermes-client' — already a dependency). Calls `getLatestPriceUpdates([feedId])` (feedId arrives 0x-prefixed — Hermes accepts that form, same as the web's hermes-price.ts notes). Defensive parse mirroring pyth-adapter.ts: read `updates.parsed?.[0]?.price`; require string `price` mantissa and number `expo`; `BigInt(price)` must be > 0n; return the normalized 1e8 bigint, or null on ANY failure/malformed/non-positive value. NEVER throws — Hermes is an untrusted boundary.

2. `apps/relayer/src/routes/calls-preflight.ts` — add the gate as its own numbered subsection between the schema parse (section 1) and the duplicate-hash computation (section 2), so a guaranteed-true call is caught before any RPC reads, but push into the existing `errors[]` array (do NOT early-return) so it aggregates with the other D-31 field errors through the single 422 at section 5:
   - Imports: `{ getSpotPrice1e8, evaluateTargetGuard }` from '../lib/hermes-spot.js' and `{ withTimeout }` from '../lib/with-timeout.js'.
   - Gate (only when `input.marketType === 'priceTarget'`): for priceTarget, `input.assetA` is GUARANTEED to be the 0x-prefixed Pyth feed id (buildPreflightBody aborts pre-network on unresolvable assets — apps/web/app/new/lib/preflight-body.ts:62-67). Fetch `spot` via `withTimeout(getSpotPrice1e8(input.assetA), 4_000, 'hermes spot price')` inside try/catch — TimeoutError or any rejection → spot = null. Then `const guard = evaluateTargetGuard(BigInt(input.targetValue), spot)`; if `!guard.ok` push `{ field: 'targetValue', code: guard.code, message: guard.message }` onto `errors`. Field 'targetValue' is a VISIBLE composer field — the web's existing 422 → setError mapping renders it inline with zero web changes.
   - Route comment block documenting: (a) the trivially-true guard rationale (v1 settles >= only — SettlementManager.sol:718; target at/below current = guaranteed win = rep farming), (b) the FAIL-CLOSED decision and why, (c) the 4s timeout bound so preflight never hangs on Hermes, (d) scope: on-chain enforcement is contracts-v2 — a direct on-chain createCall bypasses this route entirely; accepted residual for v1.
   - Add the new gate line to the route's top-of-file gate-sequence doc comment (it enumerates Gate Zod / 6.1 / 6.2 / 6.3 / CALL-34/35/36).

3. NEW `apps/relayer/src/lib/__tests__/hermes-spot.test.ts` (vitest; mock-HermesClient injection per apps/relayer/src/workers/__tests__/pyth-adapter.test.ts; source pins per routes/__tests__/profile-registry-address.test.ts):
   - All cases from the behavior block: normalization at expo -8 / -6 / -10; evaluateTargetGuard below/equality/above/null-fail-closed; getSpotPrice1e8 success, throw → null, malformed → null, zero mantissa → null (mock client object with a vi.fn() getLatestPriceUpdates, cast to HermesClient).
   - Source pins on '../../routes/calls-preflight.ts': contains 'getSpotPrice1e8', 'evaluateTargetGuard', 'withTimeout', 'priceTarget', and 'price_unverifiable' — pins the fail-closed wiring so a future refactor cannot silently drop the server-side guard.
  </action>
  <verify>
    <automated>cd "C:\Users\woshv\Desktop\Call it" && pnpm --filter @call-it/relayer test && pnpm --filter @call-it/relayer build</automated>
  </verify>
  <done>hermes-spot.test.ts green: rejects target < current AND target == current, accepts target > current, Hermes failure fail-closed, expo -6/-10 normalization exact; full relayer suite + tsc build green; rejection shape matches the existing PreflightFieldError contract on a visible field.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → relayer preflight | untrusted request body (targetValue, assetA) — the web layer is advisory only |
| relayer → Hermes | untrusted upstream JSON (mantissa/expo); availability not guaranteed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-uf9-01 | Tampering | web composer guard bypass (curl/devtools straight to preflight) | mitigate | Layer 2 — server-side gate in calls-preflight.ts is authoritative; web layer is UX only |
| T-uf9-02 | DoS / Tampering | Hermes outage or malformed response during preflight | mitigate | FAIL-CLOSED rejection ('price_unverifiable') + defensive never-throw parse + withTimeout 4s bound |
| T-uf9-03 | Elevation (rep farming) | direct on-chain createCall bypassing the relayer entirely | accept | On-chain oracle read at creation is contracts-v2 (contracts deployed, immutable); documented in route comment; both product paths (web + Mini App) go through preflight |
| T-uf9-04 | Spoofing | stale/fabricated client price anchoring the user | mitigate | D-07 contract preserved — null price renders nothing and skips the client check; server re-fetches its own spot price |

No package installs in this plan — package legitimacy gate not applicable.
</threat_model>

<verification>
From repo root:
1. `pnpm --filter @call-it/web test` — green (target-guard.test.ts + full existing suite incl. target-scale/new-call-publish pins)
2. `pnpm --filter @call-it/web build` — green
3. `pnpm --filter @call-it/relayer test` — green (hermes-spot.test.ts + full existing suite)
4. `pnpm --filter @call-it/relayer build` — green
If either build fails on a missing `@call-it/shared` dist (gitignored), run `pnpm --filter @call-it/shared build` first.
packages/ui untouched.
</verification>

<success_criteria>
- Both layers enforce STRICT target > current-price for marketType 'priceTarget' only (equality blocked); spreadVs/event untouched
- Web: inline targetValue error + modal blocked while violation holds; reactive clear; D-07 skip when price unavailable
- Relayer: 422 on field 'targetValue' with distinct codes; fail-closed on Hermes failure; expo normalization exact (non-(-8) tested)
- All four gates green; no fenced scope reductions
</success_criteria>

<execution_constraints>
- **Commit code only — do NOT push.** Operator authorizes push/deploy separately. The SUMMARY must note the relayer guard is INERT until the relayer's Fly deploy (web deploys via Vercel on push; relayer requires the local flyctl deploy per MEMORY).
- **Parallel-session WIP exists in the working tree** (e.g. `.planning/quick/260611-u1l-live-call-card/` untracked, possibly other apps/web or apps/relayer files). Run `git status` FIRST; stage ONLY the 7 files this plan touches by explicit path; NEVER `git add -A` or `git add .`.
- Single atomic code commit, e.g.: `fix(quick-260611-uf9): trivially-true call guard — price-target must be strictly above live Pyth price (web inline + relayer preflight fail-closed)`. Code commit separate from docs (orchestrator commits planning docs).
</execution_constraints>

<output>
Create `.planning/quick/260611-uf9-target-above-current-guard/SUMMARY.md` when done (include the relayer-inert-until-Fly-deploy note).
</output>
