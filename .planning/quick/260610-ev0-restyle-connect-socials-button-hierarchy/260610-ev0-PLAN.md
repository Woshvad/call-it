---
phase: quick-260610-ev0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/components/SocialLinkControls.tsx
  - apps/web/app/onboarding/socials/page.tsx
autonomous: true
requirements: [AUTH-07, AUTH-08]

must_haves:
  truths:
    - "On CONNECT SOCIALS (Screen 2), the Link Farcaster button renders filled-yellow (primary), matching Link Twitter / X directly above it"
    - "The Continue button renders outlined (secondary), no longer filled-yellow"
    - "The Skip for now control renders as plain muted text — no border, no offset shadow, no hover/active translate"
    - "Skip for now still calls handleSkip, still disables while skipping/continuing, still respects the 44px mobile touch target"
    - "onboarding.spec.ts still finds the literal strings 'skip-socials-button' AND 'Skip for now' in the page source"
  artifacts:
    - path: "apps/web/app/components/SocialLinkControls.tsx"
      provides: "Link Farcaster button as primary intent"
      contains: "data-testid=\"link-farcaster-button\""
    - path: "apps/web/app/onboarding/socials/page.tsx"
      provides: "Continue (secondary) + Skip for now (plain text)"
      contains: "data-testid=\"skip-socials-button\""
  key_links:
    - from: "apps/web/app/onboarding/socials/page.tsx"
      to: "handleSkip"
      via: "onClick on the plain-text Skip control"
      pattern: "void handleSkip\\(\\)"
---

<objective>
Restyle the CONNECT SOCIALS onboarding screen (Screen 2) button hierarchy. PURE COSMETIC — no logic, handler, routing, or copy changes.

Three edits across exactly two files:
1. SocialLinkControls.tsx — Link Farcaster button `intent="secondary"` → `intent="primary"` (filled yellow, matches Link Twitter / X).
2. socials/page.tsx — Continue button `intent="primary"` → `intent="secondary"` (outlined).
3. socials/page.tsx — Skip for now button converted from the `@call-it/ui` neobrutalist Button to plain muted text.

Resulting hierarchy: Link Twitter/X + Link Farcaster (both filled yellow = primary link CTAs) → Continue (outlined = secondary proceed) → Skip for now (plain text = tertiary opt-out).

Purpose: Sharpen the visual priority on Screen 2 so linking reads as the encouraged action, proceeding is secondary, and skipping is a low-emphasis escape hatch.
Output: Two edited files, committed atomically to master (not pushed).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

# The two files being edited
@apps/web/app/components/SocialLinkControls.tsx
@apps/web/app/onboarding/socials/page.tsx

# The shared CVA Button (intents: primary=filled yellow `bg-brand-accent text-black`;
# secondary=outlined `bg-transparent text-white border-white`). Base hardcodes
# border-2 border-black + offset shadow + hover/active translate.
@packages/ui/src/primitives/Button.tsx

# Source-text assertions that MUST keep passing (line ~155-158):
# expect(source).toContain('skip-socials-button')  AND  expect(source).toContain('Skip for now')
@apps/web/tests/onboarding.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap button hierarchy on the CONNECT SOCIALS screen (3 edits, 2 files)</name>
  <files>apps/web/app/components/SocialLinkControls.tsx, apps/web/app/onboarding/socials/page.tsx</files>
  <action>
Make exactly three edits. Touch NOTHING else.

EDIT 1 — apps/web/app/components/SocialLinkControls.tsx (the `!isFarcasterLinked` branch, the `<Button ... data-testid="link-farcaster-button">` near line 371-380): change ONLY `intent="secondary"` to `intent="primary"`. Leave `size="md"`, `onClick={handleLinkFarcaster}`, `disabled={fcStatus === 'pending'}`, the `data-testid`, the `style={{ flex: 1, ...touchTarget }}`, and the `{fcStatus === 'pending' ? 'Connecting...' : 'Link Farcaster'}` label untouched. Do NOT touch the Twitter button, the unlink buttons, the `farcaster-linked-tag`, sizes, or anything else in this file.

EDIT 2 — apps/web/app/onboarding/socials/page.tsx (the Continue `<Button>` near line 114-125, `data-testid="socials-continue-button"`): change ONLY `intent="primary"` to `intent="secondary"`. Keep `size="md"`, `onClick`, `disabled={isContinuing || isSkipping}`, `data-testid`, the `style={isMobile ? { minHeight: '44px' } : undefined}`, and the `{isContinuing ? 'Saving...' : 'Continue'}` label exactly as-is.

EDIT 3 — apps/web/app/onboarding/socials/page.tsx (the Skip `<Button>` near line 127-139, `data-testid="skip-socials-button"`): convert it from the `@call-it/ui` `Button` to a native `<button>` rendering as plain muted text. PREFERRED implementation is a native `<button>` (cleaner than overriding the Button base CVA via twMerge). The native button MUST preserve ALL of: `data-testid="skip-socials-button"`, `onClick={() => { void handleSkip(); }}`, `disabled={isSkipping || isContinuing}`, and the visible label `{isSkipping ? 'Skipping...' : 'Skip for now'}`. Style it inline (consistent with this file's inline-style convention) as plain muted text: color `#A1A1AA` (brand-muted), `fontFamily: 'monospace'`, `fontSize: '0.8rem'`, `background: 'transparent'`, `border: 'none'`, `boxShadow: 'none'`, `cursor: 'pointer'`, and the mobile touch target `minHeight: '44px'` only when `isMobile`. Add a subtle hover affordance (e.g. `textDecoration: 'underline'` on hover, or a brightness change) and disabled handling that reads as `opacity: 0.5` when `isSkipping || isContinuing`. A simple, robust approach: compute `disabled = isSkipping || isContinuing` once, set `opacity: disabled ? 0.5 : 1`, and use `onMouseEnter`/`onMouseLeave` to toggle an underline (or use a tiny CSS rule) — keep it minimal.

Do NOT remove the `import { Button } from '@call-it/ui';` line — the Continue button still uses `Button`, so the import stays used.
  </action>
  <verify>
    <automated>pnpm --filter @call-it/web exec tsc --noEmit</automated>
  </verify>
  <done>
- SocialLinkControls.tsx: the `link-farcaster-button` Button uses `intent="primary"`; the Twitter button and everything else are byte-identical to before.
- socials/page.tsx: the `socials-continue-button` Button uses `intent="secondary"`; the Skip control is a native `<button>` rendering plain muted text with no border/shadow/translate.
- The Skip native button still has `data-testid="skip-socials-button"`, the `Skip for now` / `Skipping...` label, `onClick={() => { void handleSkip(); }}`, `disabled={isSkipping || isContinuing}`, and a 44px mobile touch target.
- `import { Button } from '@call-it/ui';` remains present and used by the Continue button.
- No NEW TypeScript errors introduced (tsc --noEmit shows no new errors attributable to these two files; pre-existing unrelated errors, if any, are unchanged).
- The page source still contains the literal strings `skip-socials-button` AND `Skip for now` (so onboarding.spec.ts line ~155-158 still passes).
  </done>
</task>

</tasks>

<verification>
1. Confirm exactly two files changed: `git status --porcelain apps/web/app/components/SocialLinkControls.tsx apps/web/app/onboarding/socials/page.tsx` shows both as modified; NO other file in this diff.
2. Lint/typecheck — inspect `apps/web/package.json` scripts and run the web typecheck/lint (e.g. `pnpm --filter @call-it/web lint` and/or `pnpm --filter @call-it/web exec tsc --noEmit`). No NEW errors from these edits.
3. Grep the edited page source for `skip-socials-button` and `Skip for now` — BOTH must still be present (onboarding.spec.ts asserts on these literal strings).
4. Eyeball the diff: EDIT 1 is a single `intent` value change; EDIT 2 is a single `intent` value change; EDIT 3 swaps a `<Button>` for a native `<button>` preserving testid/label/handler/disabled.
</verification>

<success_criteria>
- Link Farcaster renders filled-yellow (primary); Continue renders outlined (secondary); Skip for now renders plain muted text.
- All handlers, routing, copy, sizes, and mobile touch targets unchanged.
- onboarding.spec.ts source-text assertions for `skip-socials-button` and `Skip for now` still pass.
- No new TS/lint errors.
- ONLY the two named files are modified.
</success_criteria>

<commit_instructions>
CRITICAL GIT RULES (a background soak has unrelated uncommitted files in evidence/, scripts, docs — they MUST NOT be committed):
- Stage ONLY the two edited files with an explicit path list:
  `git add apps/web/app/components/SocialLinkControls.tsx apps/web/app/onboarding/socials/page.tsx`
- NEVER use `git add -A`, `git add .`, or `git add -u`.
- Commit the CODE change atomically to master. DO NOT push (the operator deploys web via Vercel on their own cadence; the orchestrator commits docs separately).
- Suggested message: `style(quick-260610-ev0): restyle CONNECT SOCIALS button hierarchy (Farcaster→primary, Continue→secondary, Skip→plain text)`
</commit_instructions>

<output>
Create `.planning/quick/260610-ev0-restyle-connect-socials-button-hierarchy/260610-ev0-SUMMARY.md` when done
</output>
