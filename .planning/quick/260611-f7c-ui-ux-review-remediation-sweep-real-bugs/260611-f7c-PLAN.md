---
phase: quick-260611-f7c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/profile/[address]/settings/page.tsx
  - apps/web/app/duel/[challengeId]/page.tsx
  - apps/web/components/FeedList.tsx
  - packages/ui/src/compound/CallCard.tsx
  - apps/web/app/leaderboard/LeaderboardClient.tsx
  - apps/web/app/profile/[address]/ProfileClient.tsx
  - apps/web/app/disputes/page.tsx
  - apps/web/app/duels/page.tsx
  - apps/web/app/components/ChallengeFormModal.tsx
  - apps/web/app/new/components/PublishConfirmModal.tsx
  - apps/web/app/call/[id]/page.tsx
  - apps/web/components/ProfileTabs.tsx
  - apps/web/app/disputes/layout.tsx
  - apps/web/app/new/components/AdvancedSettings.tsx
  - apps/web/app/new/components/DeadlinePicker.tsx
  - apps/web/app/components/AppShell.tsx
  - apps/web/app/components/Icon.tsx
  - apps/web/app/components/Sidebar.tsx
autonomous: true
requirements: [QUICK-260611-F7C]

must_haves:
  truths:
    - "Clicking the Save Handle button on /profile/[address]/settings never references a stale isPending() helper — disabled state comes straight from isWritingHandle (F-A1)"
    - "No code path on the duel page can render a fabricated 50/50 consensus split — missing/zero reserves degrade to the 'consensus data unavailable' notice (F-A2, D-07)"
    - "Feed cards never display a fabricated 50% conviction — missing conviction hides the conviction line entirely (F-A3, D-07)"
    - "Leaderboard error state is a contained brutal-card with a working RETRY (router.refresh) and 'board' copy; empty state is a contained brutal-card with a + NEW CALL CTA (F-B4, F-B5)"
    - "Leaderboard h1 reads 'Top of Book' and no longer collides with the home feed's 'The Tape' h1 (F-B6)"
    - "Profile empty state, disputes loading skeletons, and duels empty copy match the home-feed containment/skeleton patterns (F-B7, F-B8, F-B9)"
    - "All four modals (ChallengeFormModal, PublishConfirmModal, DisputeModal, ProvenanceModal) expose role=dialog, aria-modal, aria-labelledby, and close on Escape with Escape inert while a transaction/publish is in flight (F-C10..C12)"
    - "ProfileTabs, duel accents, and disputes layout use design tokens (var(--accent-win), var(--accent-duel), var(--border-active), var(--font-mono)) instead of hardcoded hex/monospace (F-D13..D15)"
    - "No requirement ID leaks into user-facing copy; deadline picker says 'Settlement window'; hamburger uses a menu glyph; Duels nav uses the duel glyph distinct from Disputes' book (F-E16..E19)"
    - "Receipt-page rendering on /call/[id] (stamps, h1, byline, P&L, share CTAs) is byte-identical except modal a11y attributes inside DisputeModal/ProvenanceModal"
  artifacts:
    - path: "packages/ui/src/compound/CallCard.tsx"
      provides: "conviction?: number optional prop + conditional CONVICTION row render"
      contains: "conviction?:"
    - path: "apps/web/app/components/Icon.tsx"
      provides: "'menu' glyph in IconName union + paths registry"
      contains: "'menu'"
    - path: "apps/web/app/leaderboard/LeaderboardClient.tsx"
      provides: "brutal-card error state with RETRY + brutal-card empty state with /new CTA + 'Top of Book' h1"
      contains: "Top of Book"
  key_links:
    - from: "apps/web/components/FeedList.tsx"
      to: "packages/ui/src/compound/CallCard.tsx"
      via: "conviction passed through without ?? 50 fabrication"
      pattern: "conviction"
    - from: "apps/web/app/leaderboard/LeaderboardClient.tsx"
      to: "next/navigation router.refresh"
      via: "RETRY button re-running the RSC fetch"
      pattern: "router\\.refresh"
    - from: "apps/web/app/components/Sidebar.tsx"
      to: "apps/web/app/components/Icon.tsx"
      via: "Duels nav entry uses the existing 'duel' crossed-swords glyph"
      pattern: "icon: 'duel'"
---

<objective>
Fix every verified finding from the senior-designer UI/UX review of apps/web EXCEPT the explicitly deferred receipt-page visual issues on /call/[id]: 3 real bugs (settings disabled-state, fabricated duel consensus fallback, fabricated 50% conviction), error/empty/loading state unification to the home-feed pattern, a consistent a11y pass on all four modals, design-token compliance, and copy/icon fixes.

Purpose: the review found honesty violations (D-07: never fake data), a latent bug, inconsistent failure states, and a11y gaps — all cheap to fix now, all user-visible polish for the testnet demo.
Output: 3 atomic commits on master touching 18 files (17 in apps/web + 1 in packages/ui), web + ui test suites green, web build green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@apps/web/app/globals.css
@apps/web/app/page.tsx
</context>

<constraints_critical>
- **DEFERRED — DO NOT TOUCH:** receipt-page rendering on /call/[id] (double PENDING RESULT stamp, "Call" h1, byline, green +$0.00 P&L, duplicate share CTAs). The ONLY allowed changes in `apps/web/app/call/[id]/page.tsx` are a11y attributes + Escape handling inside the `DisputeModal` (line ~574) and `ProvenanceModal` (line ~944) component functions.
- No new dependencies. No `display: grid` in any shared/OG-adjacent component (Satori ban). Keep radius-0 brutalist styling and the existing token system (`apps/web/app/globals.css`: `--accent-win` #E8F542, `--accent-loss`, `--accent-duel` #A855F7, `--accent-warning`, `--border-active` #2E2E42, `--font-display/--font-sans/--font-mono`, primitives `.btn/.brutal-card/.pill/.tab/.label-overline`).
- `@call-it/ui` exports source directly (`"exports": { ".": "./src/index.ts" }`) — no dist rebuild needed after editing CallCard; web compiles it in-place.
- One commit per task, conventional format `fix(quick-260611-f7c): ...`.
</constraints_critical>

<tasks>

<task type="auto">
  <name>Task 1: Real bugs + error/empty/loading state unification (F-A1..A3, F-B4..B9)</name>
  <files>apps/web/app/profile/[address]/settings/page.tsx, apps/web/app/duel/[challengeId]/page.tsx, packages/ui/src/compound/CallCard.tsx, apps/web/components/FeedList.tsx, apps/web/app/leaderboard/LeaderboardClient.tsx, apps/web/app/profile/[address]/ProfileClient.tsx, apps/web/app/disputes/page.tsx, apps/web/app/duels/page.tsx</files>
  <action>
**F-A1 — settings/page.tsx:** Line 165: `disabled={isPending()}` calls a hoisted helper defined at lines 232-234 (`function isPending() { return isWritingHandle; }`) — it does not crash (function declarations hoist) but it is dead indirection flagged as a bug. Replace with `disabled={isWritingHandle}` (the renamed `isPending` from `useWriteContract()` at line 70) and DELETE the `isPending()` function at lines 232-234.

**F-A2 — duel/[challengeId]/page.tsx, fabricated consensus (D-07):** Lines 569-591 compute reserves with `const followReserveFromState = 0n` placeholders and `const callerPct = total === 0n ? 50 : ...` at line 590. The render at lines 1037-1068 already gates on `total > 0n` with a "consensus data unavailable" fallback — but the `? 50` fabrication is a live landmine and the comment at line 572 ("Fallback to 50/50 when data unavailable") documents the violation. Fix so no path can EVER render a fabricated split:
  - Change line 590-591 to nullable: `const callerPct = total > 0n ? Number((followReserve * 100n) / total) : null;` and `const challengerPct = callerPct !== null ? 100 - callerPct : null;`
  - Gate the consensus block render on `callerPct !== null` instead of `total > 0n` (lines 1037, plus the aria-label/flexBasis usages at 1058-1061 — TypeScript will force the narrowing).
  - Upgrade the unavailable notice (lines 1065-1067) to mono uppercase to match the page's label voice: `CONSENSUS DATA UNAVAILABLE` (keep `var(--text-tertiary)`, fontSize 11, className="mono", letterSpacing '0.08em').
  - Rewrite the comments at lines 571-573 to state the honest-degradation contract (D-07: reserves missing → hidden bar + notice, never an even split).

**F-A3 — fabricated 50% conviction (D-07):** `CallCard` (packages/ui/src/compound/CallCard.tsx) declares `conviction: number` REQUIRED (line 30) and renders `CONVICTION · {call.conviction}%` unconditionally (lines 214-218). Fix in two halves:
  - CallCard: change to `conviction?: number;` and wrap the conviction `<span>` (lines 214-218) in `{typeof call.conviction === 'number' && (...)}` so a missing value hides the entire CONVICTION row item. Update the D-07 doc comment at the top to note conviction degrades-to-hidden.
  - FeedList.tsx line 47: replace `conviction: item.conviction ?? 50` with `conviction: typeof item.conviction === 'number' ? item.conviction : undefined`. Do NOT fabricate any default.
  - Check `packages/ui/__tests__/call-card-states.test.tsx` — baseCall passes `conviction: 70` so existing assertions hold; ADD one assertion that a call without `conviction` does not render the text `CONVICTION` (mirror the existing test style in that file).

**F-B4/B5/B6 — LeaderboardClient.tsx:** This is a dumb props renderer; data + fetchError arrive from page.tsx's RSC fetch, so there is NO client refetch — retry must be `router.refresh()`:
  - Add `import { useRouter } from 'next/navigation';` and `const router = useRouter();` in the component.
  - Line 93: h1 `The Tape · Top of book` → `Top of Book` (keep the existing `.sub` line unchanged — it is already board-appropriate).
  - Error state (lines 119-132): replace the uncontained left-border line with the home-feed `TapeError` pattern from apps/web/app/page.tsx lines 70-100: a `.brutal-card` with `borderColor: 'var(--accent-loss)'`, column flex, centered, padding '48px 24px', a mono uppercase message — copy: `Couldn't load the board. Retry.` — and a `<button type="button" className="btn outline-white" onClick={() => router.refresh()} style={{ minHeight: 44 }}>RETRY</button>` (match the duels-page retry button at apps/web/app/duels/page.tsx line 142 for the button treatment).
  - Empty state (lines 135-144): wrap in the `EmptyTape` containment pattern (page.tsx lines 42-66): `.brutal-card`, centered column, padding '64px 24px', `label-overline` headline (keep `NOTHING ON THE TAPE YET` voice but board-specific: `NO CALLERS RANKED YET`), keep the honest secondary line ("Make a call to get on the board."), and add `import Link from 'next/link';` + `<Link href="/new" className="btn cream" style={{ textDecoration: 'none' }}>+ NEW CALL</Link>`.

**F-B7 — ProfileClient.tsx line 294:** replace `<div className="label-overline">No calls on record yet.</div>` with a centered empty block matching the EmptyTape pattern: `.brutal-card`, column flex, centered, padding '48px 24px', `label-overline` line `NO CALLS ON RECORD YET`, secondary line in `var(--text-secondary)` fontSize 14 ("Every call is permanent and public. Go on record."), and a `<Link href="/new" className="btn cream" style={{ textDecoration: 'none' }}>Make your first call</Link>`. `Link` is already imported in this file (used at line 289).

**F-B8 — disputes/page.tsx lines 615-620:** replace the lone "Loading disputes…" text line with hard-edge skeleton blocks copying the duel-page skeleton recipe (duel/[challengeId]/page.tsx lines 596-601): a column flex gap 16 containing `<div style={{ height: 24, background: 'var(--bg-tertiary)', width: 200 }} />` plus two `<div style={{ height: 120, background: 'var(--bg-secondary)', border: '1px solid var(--border-subtle)' }} />` blocks. Keep the same `isLoading && disputes.length === 0` guard.

**F-B9 — duels/page.tsx lines 158-160:** change the empty-state secondary line from `Challenge a call to start one.` to `Open any call and hit CHALLENGE to start a 1v1.` Leave the headline, CTA link, and containment as-is (already correct).
  </action>
  <verify>
    <automated>cd "C:/Users/woshv/Desktop/Call it" && pnpm --filter @call-it/ui test && pnpm --filter @call-it/web test</automated>
  </verify>
  <done>Settings button disabled via isWritingHandle with the dead helper removed; duel page has no `? 50` pct fabrication and the unavailable notice is mono-uppercase; CallCard conviction optional + hidden when absent with a new ui test covering it; FeedList passes conviction through honestly; leaderboard error/empty are contained brutal-cards with RETRY (router.refresh) and + NEW CALL CTA, h1 is "Top of Book"; profile empty block has a CTA; disputes loading shows skeleton blocks; duels empty copy is actionable. Both test suites pass. Commit: `fix(quick-260611-f7c): real bugs + error/empty/loading state unification (settings disabled, D-07 consensus/conviction honesty, board states)`.</done>
</task>

<task type="auto">
  <name>Task 2: Modal a11y pass — dialog semantics + guarded Escape on all four modals (F-C10..C12)</name>
  <files>apps/web/app/components/ChallengeFormModal.tsx, apps/web/app/new/components/PublishConfirmModal.tsx, apps/web/app/call/[id]/page.tsx</files>
  <action>
Apply ONE consistent pattern to all four modals. For each: (a) `role="dialog"` + `aria-modal="true"` on the modal panel element, (b) `aria-labelledby` pointing at a new `id` on the existing title element, (c) a window `keydown` effect that closes on Escape, inert while a transaction/publish is in flight — mirroring each modal's existing in-flight semantics. Do NOT change backdrop-click behavior, focus management, or any visual rendering.

Shared Escape effect shape (adapt per modal):
`useEffect(() => { if (!open) return; const onKey = (e: KeyboardEvent) => { if (e.key === 'Escape' && !inFlight) onClose(); }; window.addEventListener('keydown', onKey); return () => window.removeEventListener('keydown', onKey); }, [open, inFlight, onClose]);`

**1. ChallengeFormModal (apps/web/app/components/ChallengeFormModal.tsx):**
  - In-flight = `approving || approveConfirming || challenging` plus the challenge receipt-wait flag if one exists (check around line 227 where `approveConfirming` comes from `useWaitForTransactionReceipt`; there is a `challengeTxHash` at line 154 — include its `isLoading` if a receipt wait consumes it).
  - Title: the `<h2>CHALLENGE {callerHandle}</h2>` at line 389 → add `id="challenge-modal-title"`.
  - Panel: the inner cream panel div (the one with maxWidth 'min(620px, ...)' at lines ~350-366) gets `role="dialog" aria-modal="true" aria-labelledby="challenge-modal-title"`.
  - This component is always mounted with hooks at top (useState lines 147-155) — place the Escape effect near the other effects; gate on whatever open/visibility prop the component uses (read the props — if it renders null when closed, gate the effect the same way).

**2. PublishConfirmModal (apps/web/app/new/components/PublishConfirmModal.tsx):**
  - RULES-OF-HOOKS GOTCHA: `if (!isOpen) return null;` at line 49 sits BEFORE the JSX — the new `useEffect` MUST be placed ABOVE that early return (immediately after the destructured props). Add `import { useEffect } from 'react';`.
  - In-flight = `isPublishing` (matches the existing backdrop guard at line 61 and the Cancel button at line 238). Escape calls `onCancel`.
  - Title: the display-voice `<span>` at lines 89-100 (`{isSigning ? 'Signing transaction…' : 'Go on record'}`) → add `id="publish-confirm-title"` (a span id is a valid aria-labelledby target).
  - Panel: the `.modal-panel` div at line 65 gets `role="dialog" aria-modal="true" aria-labelledby="publish-confirm-title"`.

**3. DisputeModal (apps/web/app/call/[id]/page.tsx, component at line 574):**
  - In-flight = `isApproving || approveConfirming || isSubmitting` (state at lines 586-587, 605).
  - Place the Escape effect alongside the existing effects (lines 610-631), gated on `open`.
  - Title: `<h2>RAISE DISPUTE</h2>` at lines 749-751 → add `id="dispute-modal-title"`.
  - Panel: the cream panel div opening around line 740-746 (the one ending at the comment "Header — FINAL · CONFIRM mono voice + close") gets `role="dialog" aria-modal="true" aria-labelledby="dispute-modal-title"`.

**4. ProvenanceModal (same file, component at line 944):**
  - RULES-OF-HOOKS GOTCHA: `if (!open) return null;` at line 948 — place the Escape effect after the `useState` at line 946 and BEFORE the early return. Add `useEffect` to the existing react import if not already there (line 27 imports useState/useEffect/useCallback — already available).
  - No transaction exists here — Escape closes unconditionally (inFlight = false; simplify the condition away).
  - Title: `<h2>ORACLE PROOF</h2>` at lines 1001-1003 → add `id="provenance-modal-title"`.
  - Panel: the cream panel div at lines 984-998 gets `role="dialog" aria-modal="true" aria-labelledby="provenance-modal-title"`.

**HARD CONSTRAINT:** in `apps/web/app/call/[id]/page.tsx`, change NOTHING outside the two modal component function bodies — no receipt stamps, h1, byline, P&L, or share CTA edits.
  </action>
  <verify>
    <automated>cd "C:/Users/woshv/Desktop/Call it" && bash -c "c=\$(grep -c 'role=\"dialog\"' 'apps/web/app/components/ChallengeFormModal.tsx' 'apps/web/app/new/components/PublishConfirmModal.tsx' 'apps/web/app/call/[id]/page.tsx' | awk -F: '{s+=\$2} END {print s}'); [ \"\$c\" -eq 4 ] && echo DIALOGS-OK || (echo \"expected 4 dialogs, got \$c\" && exit 1)" && pnpm --filter @call-it/web test</automated>
  </verify>
  <done>All four modals carry role="dialog", aria-modal="true", aria-labelledby wired to an id on their real title element, and close on Escape — with Escape inert during approve/challenge/dispute/publish in-flight states matching each modal's existing backdrop/Cancel guards. Zero receipt-rendering diffs in call/[id]/page.tsx outside the two modal functions. Web tests pass. Commit: `fix(quick-260611-f7c): modal a11y — dialog semantics + guarded Escape on all four modals`.</done>
</task>

<task type="auto">
  <name>Task 3: Token compliance + copy/icon fixes (F-D13..D15, F-E16..E19)</name>
  <files>apps/web/components/ProfileTabs.tsx, apps/web/app/duel/[challengeId]/page.tsx, apps/web/app/disputes/layout.tsx, apps/web/app/new/components/AdvancedSettings.tsx, apps/web/app/new/components/DeadlinePicker.tsx, apps/web/app/components/Icon.tsx, apps/web/app/components/AppShell.tsx, apps/web/app/components/Sidebar.tsx</files>
  <action>
**F-D13 — ProfileTabs.tsx:** lines 51, 72: `fontFamily: 'monospace'` → `fontFamily: 'var(--font-mono)'`. Lines 54, 57: `'#E8F542'` → `'var(--accent-win)'` (both the active color and the `2px solid #E8F542` borderBottom template).

**F-D14 — duel/[challengeId]/page.tsx lines 59-60:** `const CALLER_ACCENT = '#E8F542'` → `'var(--accent-win)'`; `const DUEL_ACCENT = '#A855F7'` → `'var(--accent-duel)'`. VERIFIED SAFE: every usage (lines 843, 856, 868-869, 941-974, 1041-1052, 1082-1102) is a CSS style value or border template literal — no canvas/SVG fill attrs needing raw hex. Leave the derived `rgba(232,245,66,0.05)` / `rgba(168,85,247,0.06)` tint literals untouched (rgba cannot wrap a var). Update the stale comment at line 1057 ("challenger #A855F7" → "challenger var(--accent-duel)") so grep gates stay clean.

**F-D15 — disputes/layout.tsx:** line 20 `border: '3px solid #2E2E42'` → `'3px solid var(--border-active)'`; lines 27-30 all four `'4px solid #E8F542'` → `'4px solid var(--accent-win)'`. Update the comments at lines 5 and 25 that name the raw hexes ("4px #E8F542 corner brackets" → "4px accent-win corner brackets"; same treatment line 5) so the grep gate below passes. Leave `backgroundColor: '#09090E'` as-is (out of finding scope).

**F-E16 — AdvancedSettings.tsx lines 75-77:** user-facing helper reads `Allow 1v1 challenges (CALL-64)` — remove the requirement ID from the visible string (`Allow 1v1 challenges`) and keep traceability as a JSX comment on the line above: `{/* CALL-64: open-to-challenges toggle */}`.

**F-E17 — DeadlinePicker.tsx lines 146-155:** replace the jargon line `↳ Hash bucket: {bucketLabel}` with `↳ Settlement window: {bucketLabel}` and add a second helper line below it (same mono 11px `var(--text-tertiary)` treatment): `Your call settles at the start of this UTC day — local times may differ.` Update `aria-label="UTC hash bucket"` → `aria-label="UTC settlement window"` and the code comment at line 146 accordingly (keep the PITFALL-12 / CALL-46 reference in the comment). Then grep `apps/web/tests` and any vitest files for `Hash bucket` and update copy assertions to the new string if any exist (update expectations only — never revert the copy).

**F-E18 — menu glyph:** in Icon.tsx, add `'menu'` to the `IconName` union (line 19-43) and to the `paths()` registry: `case 'menu': return <path d="M3 6h18M3 12h18M3 18h18" />;` (three equal lines — distinct from 'feed' whose third line is short). In AppShell.tsx line 118, change the mobile hamburger `<Icon name="feed" size={16} />` → `<Icon name="menu" size={16} />`. The desktop/sidebar 'feed' usages stay.

**F-E19 — Sidebar.tsx line 79:** the Duels entry `icon: 'book'` → `icon: 'duel'` — the crossed-swords glyph ALREADY EXISTS in Icon.tsx (case 'duel', line 74-75); no new icon needed. Disputes (line 101) keeps 'book'. Result: feed/duel/book are three distinct glyph meanings.
  </action>
  <verify>
    <automated>cd "C:/Users/woshv/Desktop/Call it" && bash -c "set -e; ! grep -q '#E8F542' apps/web/components/ProfileTabs.tsx apps/web/app/disputes/layout.tsx; ! grep -q \"ACCENT = '#\" 'apps/web/app/duel/[challengeId]/page.tsx'; grep -q \"icon: 'duel'\" apps/web/app/components/Sidebar.tsx; grep -q 'name=\"menu\"' apps/web/app/components/AppShell.tsx; echo TOKENS-OK" && pnpm --filter @call-it/web test && pnpm --filter @call-it/web build</automated>
  </verify>
  <done>Zero hardcoded #E8F542/#2E2E42/'monospace' left in the three token-scoped files (comments reworded so plain grep stays clean); duel accents resolve through var(--accent-win)/var(--accent-duel); CALL-64 lives only in a code comment; deadline picker says "Settlement window" with the UTC helper line and matching aria-label; hamburger renders the new 'menu' glyph; Duels nav renders the crossed-swords 'duel' glyph while Disputes keeps 'book'. Web tests and the production build pass. Commit: `fix(quick-260611-f7c): token compliance + copy/icon fixes (accents to vars, settlement-window copy, menu/duel glyphs)`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | UI-only remediation: no new untrusted input paths, no package installs, no API/contract surface changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-f7c-01 | Tampering | Escape-key close on transaction modals | mitigate | Escape handlers are inert while approve/challenge/dispute/publish is in flight -- mirrors each modal's existing backdrop/Cancel guard so a stray keypress cannot dismiss progress UI mid-money-flow |
| T-f7c-02 | Information Disclosure | D-07 honesty fixes (consensus, conviction) | mitigate | Fabricated 50/50 split and 50% conviction removed -- the UI can no longer misrepresent market state when data is missing |
</threat_model>

<verification>
1. `pnpm --filter @call-it/ui test` -- CallCard optional-conviction behavior covered (new assertion) and existing card-state tests green.
2. `pnpm --filter @call-it/web test` -- full web unit suite green (copy assertions updated only where the copy change was the intent, e.g. "Hash bucket").
3. `pnpm --filter @call-it/web build` -- Next.js production build exits 0 (catches the TS narrowing from nullable callerPct and the optional conviction prop).
4. `git diff` on `apps/web/app/call/[id]/page.tsx` shows changes ONLY inside the DisputeModal and ProvenanceModal component functions (a11y attrs + Escape effects) -- zero receipt-rendering lines touched.
5. Grep gates: no `#E8F542` in ProfileTabs/disputes-layout; no `ACCENT = '#` in the duel page; `icon: 'duel'` present in Sidebar; `name="menu"` present in AppShell; `role="dialog"` appears exactly 4 times across the three modal files.
</verification>

<success_criteria>
- All 19 findings (F-A1..F-E19) fixed as specified; the deferred receipt-page visuals on /call/[id] are untouched.
- 3 atomic commits, one per task, conventional `fix(quick-260611-f7c): ...` format.
- ui + web test suites and the web production build all pass.
- No new dependencies; no `display: grid` introduced; radius-0 brutalist treatment and the globals.css token system preserved.
</success_criteria>

<output>
Create `.planning/quick/260611-f7c-ui-ux-review-remediation-sweep-real-bugs/260611-f7c-SUMMARY.md` when done.
</output>
