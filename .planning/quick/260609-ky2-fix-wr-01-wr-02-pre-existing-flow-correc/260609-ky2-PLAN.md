---
phase: quick-260609-ky2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/new/hooks/usePublishCall.ts
  - apps/web/app/new/page.tsx
  - packages/ui/src/compound/FollowFadeModal.tsx
autonomous: true
requirements: [WR-01, WR-02]

must_haves:
  truths:
    - "In quote mode (?quote=), a successful publish surfaces the UI-28 quote-success screen instead of silently returning to the composer (WR-01)."
    - "A failed/errored publish in quote mode does NOT surface the success screen (WR-01)."
    - "After a SlippageExceeded revert in FollowFadeModal, the Retry button is disabled until fresh reserve props arrive from the parent poll, so Retry cannot resubmit the identical stale minSharesOut (WR-02)."
    - "packages/ui FollowFadeModal stays free of matchMedia/useIsMobile (Pitfall 2) and uses no new props."
  artifacts:
    - path: "apps/web/app/new/hooks/usePublishCall.ts"
      provides: "publish() that resolves a terminal status object"
      contains: "status"
    - path: "apps/web/app/new/page.tsx"
      provides: "onConfirmPublish branching on publish() return value, not the stale publishStep closure"
    - path: "packages/ui/src/compound/FollowFadeModal.tsx"
      provides: "Retry gated on a reserves-changed-since-revert signal"
  key_links:
    - from: "apps/web/app/new/page.tsx"
      to: "apps/web/app/new/hooks/usePublishCall.ts"
      via: "const result = await publish(values); branch on result.status"
      pattern: "result.*status.*success"
    - from: "packages/ui/src/compound/FollowFadeModal.tsx (Retry button)"
      to: "parent reserve props (followReserve/fadeReserve/followTotalShares/fadeTotalShares)"
      via: "useEffect watching reserve props clears the post-revert lock"
      pattern: "useEffect"
---

<objective>
Fix two pre-existing flow-correctness bugs surfaced by the Phase-9 code review (`09-REVIEW.md` WR-01 + WR-02). Both are logic bugs unrelated to the mobile-responsive layout work and were intentionally left out of the Phase-9 diff.

- WR-01: `onConfirmPublish` reads `publishStep` from a stale `useCallback` closure, so the UI-28 quote-success screen never appears on a successful quote.
- WR-02: FollowFadeModal's SlippageExceeded "Retry" never gets fresh reserves before recomputing `minSharesOut`, so Retry just reverts again.

Purpose: Restore correct quote-success UX and make slippage-retry meaningful instead of a guaranteed re-revert.
Output: Edits to `usePublishCall.ts`, `new/page.tsx`, and `FollowFadeModal.tsx`, with two atomic per-task commits.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/09-mobile-responsive-on-7-critical-pages/09-REVIEW.md

# WR-01 — current stale-closure code + the hook that backs publish()
@apps/web/app/new/page.tsx
@apps/web/app/new/hooks/usePublishCall.ts

# WR-02 — the slippage handler + handleReset; the parents are call/[id] and duel/[challengeId]
@packages/ui/src/compound/FollowFadeModal.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: WR-01 — branch onConfirmPublish on a publish() return value, not the stale publishStep closure</name>
  <files>apps/web/app/new/hooks/usePublishCall.ts, apps/web/app/new/page.tsx</files>
  <behavior>
    - On a successful publish in quote mode (`?quote=`), `onConfirmPublish` surfaces the UI-28 success screen (`setQuotePosted({ quoteCallId: quoteId ?? '' })`).
    - On a failed/errored publish (wallet not connected, preflight 422, contract/userOp error), `onConfirmPublish` does NOT surface the success screen.
    - Non-quote mode behavior is unchanged (publish still redirects to /profile/{address} on success).
  </behavior>
  <action>
    In `apps/web/app/new/hooks/usePublishCall.ts`: change `publish` so it resolves a terminal status object instead of `Promise&lt;void&gt;`. Define a small result type (e.g. `PublishResult = { status: 'success' | 'error' }`) and change the `publish` callback's declared return type from `Promise&lt;void&gt;` to `Promise&lt;PublishResult&gt;`. At every terminal exit of the existing flow, return the matching status: the early `if (!address)` guard returns `{ status: 'error' }`; the success path (after `setState(... step: 'success' ...)` and the toast/redirect) returns `{ status: 'success' }`; the outer `catch` block (after it sets the error state + shows the toast) returns `{ status: 'error' }`. Do not change the existing `setState` step transitions, the preflight/paymaster logic, the toast calls, or the `router.push` — only add `return` values. Keep `step` exported as before (PublishConfirmModal still consumes `publishStep`). Export the `PublishResult` type so the page can branch on it if it wants a typed result.

    In `apps/web/app/new/page.tsx`: rewrite `onConfirmPublish` to branch on the return value rather than the closed-over `publishStep` const. Capture `const result = await publish(values);`, then `setIsModalOpen(false);`, then `if (isQuoteMode && result?.status === 'success') setQuotePosted({ quoteCallId: quoteId ?? '' });`. Remove `publishStep` from the `onConfirmPublish` dependency array (it is no longer read inside the callback). Leave `publishStep` itself in scope — it is still passed as the `publishStep` prop to both `PublishConfirmModal` instances. This is the WR-01 fix per `09-REVIEW.md` lines 82-96.
  </action>
  <verify>
    <automated>pnpm --filter @call-it/web build</automated>
  </verify>
  <done>`pnpm --filter @call-it/web build` exits 0; `publish()` resolves `{ status }`; `onConfirmPublish` branches on `result.status === 'success'` and no longer references `publishStep` inside the callback body or its dependency array. Commit edited files only (never `git add -A`).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: WR-02 — gate FollowFadeModal Retry until fresh reserves arrive after a SlippageExceeded revert</name>
  <files>packages/ui/src/compound/FollowFadeModal.tsx</files>
  <behavior>
    - After a SlippageExceeded revert, the slippage panel still shows the "Price moved…" message and the recomputed estimate.
    - The Retry / Confirm submit button is DISABLED immediately after the revert and stays disabled until the parent pushes new reserve props (any of followReserve / fadeReserve / followTotalShares / fadeTotalShares changes), at which point Retry re-enables so the recomputed minSharesOut reflects the new reserves.
    - The button label still reads `Retry {sideLabel}` while in the slippage state; the disabled state reuses the existing not-allowed styling (no new colors/props).
    - No matchMedia / useIsMobile / window read is introduced (Pitfall 2 — shared package feeds the Satori/@vercel/og Node build).
  </behavior>
  <action>
    Edit `packages/ui/src/compound/FollowFadeModal.tsx` only. Add internal state that tracks "a slippage revert happened and we are waiting for fresh reserves" — e.g. a boolean `awaitingFreshReserves` set `true` inside the `if (isSlippage)` branch of `handleSubmit` (alongside `setSlippageHit(true)`), in place of the no-op "clear the refreshed* overrides" lines (143-146) whose comment already admits they deliver nothing. Add a `useEffect` whose dependency array is `[followReserve, fadeReserve, followTotalShares, fadeTotalShares]` that, when `awaitingFreshReserves` is true, sets `awaitingFreshReserves` back to `false` (fresh props have arrived from the parent's 5s `useReadContracts` poll). Skip the effect's clear on the first render after open if needed by guarding on `awaitingFreshReserves` being true.

    Fold `awaitingFreshReserves` into the submit button's disabled gate: the Confirm/Retry button must be disabled when `awaitingFreshReserves` is true. Extend the existing `isValid` computation (or the button's `disabled`/styling expression) so that while `awaitingFreshReserves` is true the button is non-interactive and uses the existing disabled visual (the same path as `!isValid` today). Do NOT add new props to `FollowFadeModalProps`; the parents (`apps/web/app/call/[id]/page.tsx` and `apps/web/app/duel/[challengeId]/page.tsx`) already feed fresh reserve props on a 5s poll, so the gate resolves automatically — this is the "minimum, acceptable" fix from `09-REVIEW.md` line 102 (disable Retry until fresh props arrive).

    Document the dependency in the file's top-of-file doc comment (and/or an inline comment at the gate): "Retry depends on the parent's reserve-prop refetch (≤5s poll, D-07). Retry stays disabled until new reserve props arrive so the recomputed minSharesOut reflects post-revert reserves; a full parent-driven `onRefreshReserves(await)` callback is the larger alternative and is intentionally deferred." Also fix the stale comment at lines 139-146 / the `handleReset` comment so the source no longer claims clearing overrides triggers a refresh.

    Keep `handleReset` working: ensure leaving the slippage state (or a fresh-reserve arrival) does not leave the button permanently disabled.
  </action>
  <verify>
    <automated>pnpm --filter @call-it/ui build</automated>
  </verify>
  <done>`pnpm --filter @call-it/ui build` exits 0; FollowFadeModal disables Retry after a SlippageExceeded revert and re-enables it only once a reserve prop changes; no new props on `FollowFadeModalProps`; no `matchMedia`/`useIsMobile`/`window` in the file; the dependency is documented in-file. Commit edited file only (never `git add -A`).</done>
</task>

</tasks>

<verification>
Both package builds must pass from a clean checkout of the edited files:

```
pnpm --filter @call-it/ui build   # exit 0
pnpm --filter @call-it/web build  # exit 0
```

`@call-it/web` depends on `@call-it/ui`; build `@call-it/ui` first (or rely on the monorepo build order) so the web build picks up the modal change.
</verification>

<success_criteria>
- WR-01: In quote mode, a successful publish shows the UI-28 quote-success screen; a failed publish does not. `onConfirmPublish` no longer reads the stale `publishStep` closure.
- WR-02: After a SlippageExceeded revert, Retry is disabled until fresh reserves arrive, eliminating the "Retry reverts again" loop. No new modal props; `packages/ui` stays free of `matchMedia`/`useIsMobile`.
- Both `pnpm --filter @call-it/ui build` and `pnpm --filter @call-it/web build` exit 0.
- Two atomic commits (Task 1, Task 2). Only edited files staged — never `git add -A`/`git add .` (background soak has unrelated uncommitted files).
- No other 09-REVIEW finding touched (CR-01/WR-03/WR-05/WR-06 already fixed; WR-04/WR-07/IN-* out of scope).
</success_criteria>

<output>
Create `.planning/quick/260609-ky2-fix-wr-01-wr-02-pre-existing-flow-correc/260609-ky2-SUMMARY.md` when done.
</output>
