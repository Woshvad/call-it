---
phase: quick
plan: 260605-a4i
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/relayer/src/workers/notification-fanout.ts
autonomous: true
requirements:
  - SOCIAL-23
  - SOCIAL-24
  - WR-05

must_haves:
  truths:
    - "The notification fan-out worker survives every 30s tick on Alchemy free-tier Arbitrum Sepolia (~120 new blocks/tick) without throwing on eth_getLogs"
    - "Each getLogs call targets an explicit numeric toBlock with a span of at most 9 blocks (Alchemy free-tier hard cap is 10; 9 gives unambiguous safety margin)"
    - "When a backlog exists that exceeds maxWindowsPerTick windows, the worker drains across multiple ticks rather than pinning at an unadvanceable gap"
    - "lastBlockSeen advances on every successful window; a getLogs failure breaks out of the window loop and retries the same window next tick (unchanged from existing error contract)"
    - "processChallengeNotifications runs exactly once per tick after the window loop, preserving SOCIAL-40/41/42"
    - "All existing behaviour is preserved: boot seeding, idempotent ON CONFLICT DO NOTHING inserts, Redis statusVersion bump, sendAlertSafe, getStats(), stopped guards, non-fatal error semantics"
    - "relayer typecheck and vitest suite pass with zero new errors"
  artifacts:
    - path: "apps/relayer/src/workers/notification-fanout.ts"
      provides: "Chunked getLogs fan-out with env-configurable block span and window cap"
      contains: "NOTIFICATION_FANOUT_BLOCK_SPAN"
  key_links:
    - from: "startNotificationFanout tick()"
      to: "publicClient.getLogs"
      via: "chunk loop: fromBlock..toBlock always explicit bigints, never 'latest'"
      pattern: "getLogs.*fromBlock.*toBlock"
---

<objective>
Fix the notification-fanout worker's eth_getLogs free-tier failure on Alchemy Arbitrum Sepolia.

Purpose: Alchemy free tier hard-caps eth_getLogs at 10 blocks per request on Arbitrum. The worker's tick() issues a single getLogs with toBlock:'latest', spanning ~120 new blocks per 30s tick (0.25s Arbitrum blocks). Every call throws. The catch does not advance lastBlockSeen, so the gap grows every tick and the worker is permanently stuck. CallerExited push notifications (SOCIAL-23/24) and the statusVersion OG cache-bust never fire.

Output: A single rewritten tick() inside startNotificationFanout that chunks the getLogs scan into <=9-block windows per call, bounded to maxWindowsPerTick windows per tick so the per-tick work is finite. The module const MAX_BLOCK_RANGE_PER_TICK and all toBlock:'latest' logic are removed. Everything else in the file is preserved byte-for-byte.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@apps/relayer/src/workers/notification-fanout.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite tick() to chunk getLogs into free-tier-safe windows</name>
  <files>apps/relayer/src/workers/notification-fanout.ts</files>
  <action>
Make the following surgical changes to notification-fanout.ts. Read the current file first (already in context), then apply each change in order.

--- CHANGE 1: Remove the module-level const ---

Delete the line:
  const MAX_BLOCK_RANGE_PER_TICK = 10_000n;

It is line 52 in the current file (the first const after the imports block, before ZERO_ADDRESS).

--- CHANGE 2: Update the file-header doc block ---

Replace the WR-05 comment text in the top doc block. The existing text says:
  "Watches CallerExited events from the FollowFadeMarket contract via viem getLogs (polling every 30s)."

Leave that sentence. Then update the second sentence of the description (the one starting "Phase 3 extension") — it is already correct, leave it alone.

What needs updating is the comment on the old MAX_BLOCK_RANGE_PER_TICK const line (now deleted). Add instead a comment on the new tunables in CHANGE 3 below; no separate header change needed beyond the const deletion and new inline comments.

--- CHANGE 3: Add tunables inside startNotificationFanout, just after the opening brace ---

Immediately after:
  export function startNotificationFanout(config: NotificationFanoutConfig): NotificationFanoutHandle {
    const { publicClient, ffmAddress, intervalMs = 30_000 } = config;

Add these lines (insert before `let lastBlockSeen`):

  // Free-tier-safe chunking tunables (WR-05 — Alchemy free tier caps eth_getLogs
  // at 10 blocks per request on Arbitrum; 9n gives an unambiguous safety margin).
  // Raise NOTIFICATION_FANOUT_BLOCK_SPAN and NOTIFICATION_FANOUT_MAX_WINDOWS_PER_TICK
  // when the relayer is upgraded to a paid Alchemy plan.
  const blockSpan: bigint = (() => {
    const raw = process.env['NOTIFICATION_FANOUT_BLOCK_SPAN'];
    if (!raw) return 9n;
    const n = Number(raw);
    if (!Number.isFinite(n) || n <= 0) return 9n;
    return BigInt(Math.floor(n));
  })();
  const maxWindowsPerTick: number = (() => {
    const raw = process.env['NOTIFICATION_FANOUT_MAX_WINDOWS_PER_TICK'];
    if (!raw) return 50;
    const n = Number(raw);
    if (!Number.isFinite(n) || n <= 0) return 50;
    return Math.floor(n);
  })();

--- CHANGE 4: Rewrite tick() ---

Replace the entire body of the tick() function — from the opening brace to the closing brace inclusive. The tick() function signature stays unchanged:
  async function tick(): Promise<void> {

New body (replaces everything between the opening and closing braces of tick()):

    if (stopped) return;

    // Wait for initialization to complete
    await initPromise;

    // WR-05: if init failed, retry seeding the head before scanning. Until we
    // have a real head we cannot safely choose a fromBlock, so skip this tick.
    if (!initialized) {
      try {
        lastBlockSeen = await publicClient.getBlockNumber();
        initialized = true;
        logger.info(
          { event: 'notification_fanout_init_recovered', lastBlockSeen: lastBlockSeen.toString() },
          'Re-seeded lastBlockSeen after failed init — skipping this tick to avoid full-chain scan',
        );
      } catch (err) {
        const message = err instanceof Error ? err.message : String(err);
        logger.warn(
          { event: 'notification_fanout_error', error: message, phase: 'init_retry' },
          'getBlockNumber still failing — skipping tick (will retry next interval)',
        );
        errors++;
      }
      return;
    }

    logger.info(
      { event: 'notification_fanout_tick', lastBlockSeen: lastBlockSeen.toString() },
      'Notification fan-out tick',
    );

    // WR-05: read current head once per tick. On failure, skip the tick entirely
    // (same semantics as today's catch-of-get_logs — do not advance lastBlockSeen).
    let head: bigint;
    try {
      head = await publicClient.getBlockNumber();
    } catch (err) {
      const message = err instanceof Error ? err.message : String(err);
      logger.error(
        { event: 'notification_fanout_error', error: message, phase: 'get_head' },
        'getBlockNumber failed — skipping tick',
      );
      errors++;
      // Skip to challenge notifications — they are subgraph/time-windowed and
      // independent of block range.
      if (config.subgraphUrl) {
        try {
          await processChallengeNotifications(config);
        } catch (err2) {
          const msg2 = err2 instanceof Error ? err2.message : String(err2);
          logger.warn(
            { event: 'notification_fanout_error', error: msg2, phase: 'challenge_notifications' },
            'Challenge notification processing failed — will retry next tick',
          );
        }
      }
      return;
    }

    // Chunk getLogs into <=blockSpan windows so no single call exceeds the
    // Alchemy free-tier 10-block limit. maxWindowsPerTick bounds per-tick work
    // and rate-limit exposure; a backlog drains across successive ticks.
    let windows = 0;
    while (lastBlockSeen < head && windows < maxWindowsPerTick && !stopped) {
      const from = lastBlockSeen + 1n;
      const to = from + blockSpan - 1n > head ? head : from + blockSpan - 1n;

      let logs: Awaited<ReturnType<typeof publicClient.getLogs>>;
      try {
        logs = await publicClient.getLogs({
          address: ffmAddress,
          event: CALLER_EXITED_EVENT,
          fromBlock: from,
          toBlock: to,
        });
      } catch (err) {
        const message = err instanceof Error ? err.message : String(err);
        logger.error(
          {
            event: 'notification_fanout_error',
            error: message,
            lastBlockSeen: lastBlockSeen.toString(),
            phase: 'get_logs',
          },
          'getLogs failed — will retry next tick',
        );
        errors++;
        break; // Do NOT advance lastBlockSeen — retry same window next tick
      }

      // Process each CallerExited event in this window
      for (const log of logs) {
        if (stopped) break;
        try {
          await processCallerExitedEvent(log, config);
          totalEventsProcessed++;

          // Track highest block seen within the window
          if (log.blockNumber !== null && log.blockNumber !== undefined) {
            if (log.blockNumber > lastBlockSeen) {
              lastBlockSeen = log.blockNumber;
            }
          }
        } catch (err) {
          const message = err instanceof Error ? err.message : String(err);
          logger.error(
            { event: 'notification_fanout_error', error: message, phase: 'event_processing' },
            'Error processing CallerExited event — skipping',
          );
          errors++;
          // Do NOT throw — interval must keep running
        }
      }

      lastBlockSeen = to;
      windows++;
    }

    // If the loop stopped because the window cap was hit while there is still
    // a gap, log an info event so backlog drain is visible but not alarming.
    if (lastBlockSeen < head && windows >= maxWindowsPerTick) {
      logger.info(
        {
          event: 'notification_fanout_catching_up',
          remaining: (head - lastBlockSeen).toString(),
          windowsThisTick: windows,
        },
        'Window cap reached — backlog will drain across future ticks',
      );
    }

    // ── Phase 3: Challenge event notifications ────────────────────────────
    // Query subgraph for new challenges proposed/accepted/rejected since last tick.
    // Skipped if subgraphUrl is empty (dev without subgraph); errors are non-fatal.
    if (config.subgraphUrl) {
      try {
        await processChallengeNotifications(config);
      } catch (err) {
        const message = err instanceof Error ? err.message : String(err);
        logger.warn(
          { event: 'notification_fanout_error', error: message, phase: 'challenge_notifications' },
          'Challenge notification processing failed — will retry next tick',
        );
        // Non-fatal — do NOT increment errors that block the worker
      }
    }

--- END OF CHANGES ---

Do NOT change anything outside tick() and the startNotificationFanout opening lines. Do NOT change:
- The NotificationFanoutConfig or NotificationFanoutHandle exported interfaces
- The processCallerExitedEvent function
- The processChallengeNotifications function
- The queryHolders function
- The resolveCallerHandle function
- The CALLER_EXITED_EVENT const
- The PROFILE_REGISTRY_ABI const
- The ZERO_ADDRESS const
- The initPromise boot seeding block
- The setInterval call
- The stop() and getStats() implementations
- Any imports

The only top-level addition is removing MAX_BLOCK_RANGE_PER_TICK; the only intra-function additions are the two tunables in startNotificationFanout and the rewritten tick() body.
  </action>
  <verify>
    <automated>cd '/c/Users/woshv/Desktop/Call it' && pnpm -C apps/relayer build 2>&1 | tail -20</automated>
  </verify>
  <done>
    pnpm -C apps/relayer build exits 0 (tsc --build clean). The file contains no reference to MAX_BLOCK_RANGE_PER_TICK and no use of toBlock: 'latest' inside tick(). grep -c "MAX_BLOCK_RANGE_PER_TICK" apps/relayer/src/workers/notification-fanout.ts returns 0. grep -c "NOTIFICATION_FANOUT_BLOCK_SPAN" apps/relayer/src/workers/notification-fanout.ts returns at least 1.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify relayer vitest suite passes and commit</name>
  <files>apps/relayer/src/workers/notification-fanout.ts</files>
  <action>
Run the relayer vitest suite to confirm no regression. Then commit the single changed file atomically.

1. Run: pnpm -C apps/relayer test

   Expected: all existing tests pass; zero new failures. If the suite was already failing before this change on an unrelated test, note it and continue — this fix must not introduce any NEW failures.

2. Commit only the worker file:
   git add apps/relayer/src/workers/notification-fanout.ts
   git commit -m "fix(relayer): chunk getLogs into <=9-block windows for Alchemy free-tier Arbitrum

Alchemy free tier hard-caps eth_getLogs at 10 blocks per request on Arbitrum.
The previous tick() issued a single call with toBlock:'latest' spanning ~120
blocks per tick; every call threw, lastBlockSeen never advanced, and the worker
permanently failed. CallerExited push notifications (SOCIAL-23/24) and the
statusVersion OG cache-bust were silently dead.

Fix: remove MAX_BLOCK_RANGE_PER_TICK; replace the single getLogs call with a
bounded chunk loop (default blockSpan=9, maxWindowsPerTick=50) that always
issues explicit numeric fromBlock/toBlock. A getLogs failure breaks the loop
and retries the same window next tick. processChallengeNotifications still runs
once per tick after the loop. All existing behaviour preserved.

Env tunables (raise when upgrading to paid Alchemy tier):
  NOTIFICATION_FANOUT_BLOCK_SPAN (default 9)
  NOTIFICATION_FANOUT_MAX_WINDOWS_PER_TICK (default 50)

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"

3. Run: git show --stat HEAD

   Confirm the commit touches exactly one file: apps/relayer/src/workers/notification-fanout.ts.
  </action>
  <verify>
    <automated>cd '/c/Users/woshv/Desktop/Call it' && pnpm -C apps/relayer test 2>&1 | tail -20</automated>
  </verify>
  <done>
    vitest exits with zero new failures compared to pre-change baseline. git log --oneline -1 shows the fix commit. git show --stat HEAD confirms only notification-fanout.ts changed.
  </done>
</task>

</tasks>

<verification>
End-state checks after both tasks complete:

1. grep -c "MAX_BLOCK_RANGE_PER_TICK" apps/relayer/src/workers/notification-fanout.ts
   Expected: 0

2. grep -c "toBlock.*latest" apps/relayer/src/workers/notification-fanout.ts
   Expected: 0 (the string "latest" must not appear inside tick(); the outer catch fallback that previously fell back to 'latest' is also gone)

3. grep -c "NOTIFICATION_FANOUT_BLOCK_SPAN" apps/relayer/src/workers/notification-fanout.ts
   Expected: >= 1

4. grep -c "notification_fanout_catching_up" apps/relayer/src/workers/notification-fanout.ts
   Expected: 1

5. pnpm -C apps/relayer build exits 0

6. pnpm -C apps/relayer test exits 0 (or exits with the same pre-existing failure set; zero new failures)

7. git show --stat HEAD touches exactly one file
</verification>

<success_criteria>
- Alchemy free-tier eth_getLogs calls no longer throw: every getLogs window is <=9 blocks, well under the 10-block hard cap
- lastBlockSeen advances correctly on every successful window, draining backlogs across ticks instead of pinning
- CallerExited fan-out (SOCIAL-23/24) and Redis statusVersion bump (D-09) resume firing on Sepolia
- processChallengeNotifications (SOCIAL-40/41/42) continues running once per tick, unaffected
- relayer build and test suite clean; single atomic commit on master
- Operator can raise blockSpan/maxWindowsPerTick via env vars when upgrading to a paid Alchemy plan — no code change needed
</success_criteria>

<output>
After completion, create .planning/quick/260605-a4i-fix-notification-fanout-eth-getlogs-free/260605-a4i-SUMMARY.md
</output>
