---
phase: quick-260611-m72
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "apps/web/app/profile/[address]/settings/page.tsx"
autonomous: true
requirements: [AUTH-35]
must_haves:
  truths:
    - "Save Handle aligns the wallet to the active chain (ensureActiveChain) and pins chainId on the write — the one write site missed by the 17-site chain sweep is now covered"
    - "'Handle saved on-chain.' renders ONLY after waitForTransactionReceipt returns status 'success' (mined), never on submission"
    - "User rejection, no-gas wallet, and on-chain revert each surface an honest inline error near the input instead of silently resetting the button"
    - "The Save Handle button shows Saving.../disabled for the WHOLE flow (submit through receipt), driven by local state"
  artifacts:
    - path: "apps/web/app/profile/[address]/settings/page.tsx"
      provides: "Receipt-gated async handleSetDisplayHandle with chain guard + honest error taxonomy"
      contains: "ensureActiveChain"
  key_links:
    - from: "apps/web/app/profile/[address]/settings/page.tsx"
      to: "apps/web/lib/ensure-chain.ts"
      via: "ensureActiveChain() awaited before writeContractAsync"
      pattern: "await ensureActiveChain\\(\\)"
    - from: "apps/web/app/profile/[address]/settings/page.tsx"
      to: "wagmi/actions"
      via: "waitForTransactionReceipt(wagmiConfig, { hash, chainId: ACTIVE_CHAIN_ID })"
      pattern: "waitForTransactionReceipt"
    - from: "apps/web/app/profile/[address]/settings/page.tsx"
      to: "apps/web/app/new/lib/call-created-log.ts"
      via: "isUserRejection(err) in the catch block"
      pattern: "isUserRejection"
---

<objective>
Fix the silent-failure Save Handle flow in profile settings. Live failure 2026-06-11: user changed their username; on-chain displayHandle is still "" for both wallets (cast-verified on ProfileRegistry 0xF66C0AFEf03b43338FC5aE282e45C0Cf6A3c4820). `handleSetDisplayHandle` fires the sync `writeContract` mutate fire-and-forget with (1) no ensureActiveChain/chainId pin, (2) no error surfacing — rejection/no-gas/chain-mismatch/revert all silently reset the button, (3) a success indicator (`handleWriteSuccess` = hook `isSuccess`) that means SUBMITTED, not MINED.

Purpose: the user's username change must either land on-chain or tell them honestly why it didn't.
Output: one file changed — `apps/web/app/profile/[address]/settings/page.tsx` — following the proven call-page write pattern (ensureActiveChain → writeContractAsync with chainId → waitForTransactionReceipt).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@apps/web/app/profile/[address]/settings/page.tsx
@apps/web/lib/ensure-chain.ts
@apps/web/app/new/lib/call-created-log.ts

Proven pattern reference (read lines 1462-1545 only): @apps/web/app/call/[id]/page.tsx
— `handleFollow`/`handleFade` there show the exact sequence: `await ensureActiveChain()` → `writeContractAsync({ chainId: ACTIVE_CHAIN_ID, ... })` → `waitForTransactionReceipt(wagmiConfig, { hash, chainId: ACTIVE_CHAIN_ID as ActiveChainId })` → `receipt.status !== 'success'` throws. Also copy its imports: `waitForTransactionReceipt` (and the call page also uses `readContract`, not needed here) from `'wagmi/actions'`, `wagmiConfig` from `'@/lib/wagmi'`, `ACTIVE_CHAIN_ID` from `'@/lib/chain'`, and the local `type ActiveChainId = (typeof wagmiConfig)['chains'][number]['id']` cast used on the actions-API chainId.

Confirmed facts (already verified during planning — do not re-derive):
- `isUserRejection` IS exported from `apps/web/app/new/lib/call-created-log.ts` (walks the viem error chain for `UserRejectedRequestError`). Reuse it; do not reimplement.
- `handleError` already renders near the input in the page's existing error style (mono 0.75rem, `var(--accent-loss)`, lines ~174-178). No new error UI element needed — just route all failure paths through `setHandleError`.
- Current hook destructure is `const { writeContract, isPending: isWritingHandle, isSuccess: handleWriteSuccess } = useWriteContract();` — both `isWritingHandle` and `handleWriteSuccess` must be replaced (isPending only covers submission, isSuccess lies about mining).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite handleSetDisplayHandle — chain guard + receipt-gated success + honest errors</name>
  <files>apps/web/app/profile/[address]/settings/page.tsx</files>
  <action>
Rework the Display Handle section of the settings page (the ONLY file touched; all other sections — custody, export, socials, address book — untouched):

1. **Imports.** Add: `ensureActiveChain` from `'@/lib/ensure-chain'`; `ACTIVE_CHAIN_ID` from `'@/lib/chain'`; `waitForTransactionReceipt` from `'wagmi/actions'`; `wagmiConfig` from `'@/lib/wagmi'`; `isUserRejection` from `'@/app/new/lib/call-created-log'`. Check existing imports first — none of these are currently imported. Add a local `type ActiveChainId = (typeof wagmiConfig)['chains'][number]['id'];` for the actions-API chainId cast, mirroring the call page.

2. **State.** Change the hook destructure to `const { writeContractAsync } = useWriteContract();` (drop `isPending`/`isSuccess` — both are dishonest here). Add two local state vars: `const [isSavingHandle, setIsSavingHandle] = useState(false);` (spans the ENTIRE flow: chain switch → submit → mined receipt) and `const [handleSaved, setHandleSaved] = useState(false);` (true ONLY after a mined success receipt).

3. **Handler.** Make `handleSetDisplayHandle` async. Keep the two existing validations (empty → 'Handle cannot be empty'; >50 chars → AUTH-42 message) verbatim. Then: `setHandleError(''); setHandleSaved(false); setIsSavingHandle(true);` and a try/catch/finally:
   - try: `await ensureActiveChain();` then `const hash = await writeContractAsync({ abi: profileRegistryAbi, address: PROFILE_REGISTRY_ADDR, functionName: 'setDisplayHandle', args: [handleInput], chainId: ACTIVE_CHAIN_ID });` then `const receipt = await waitForTransactionReceipt(wagmiConfig, { hash, chainId: ACTIVE_CHAIN_ID as ActiveChainId });`. If `receipt.status !== 'success'` → `setHandleError('Save failed on-chain — try again.')`; else `setHandleSaved(true)`.
   - catch (err): honest taxonomy in this priority order:
     a. `isUserRejection(err)` → `setHandleError('Transaction rejected — your username was not saved.')`
     b. error-chain message contains 'insufficient funds' (case-insensitive — embedded wallet has 0 ETH, this is the live failure mode) → `setHandleError('This wallet has no Sepolia ETH for gas — switch to your funded wallet or grab a faucet drip, then retry.')`
     c. otherwise → trimmed honest message: prefer viem `BaseError.shortMessage` when `err` is a viem BaseError, else the first line of `Error.message`, else fallback `'Save failed — try again.'`. Note `ensureActiveChain` throws its own human-actionable wrong-network message — path (c) surfaces it as-is.
   - finally: `setIsSavingHandle(false);`

4. **Render wiring.** Button: `disabled={isSavingHandle}`, label `isSavingHandle ? 'Saving...' : 'Save Handle'`, and the cursor/opacity inline styles keyed off `isSavingHandle` (replace every `isWritingHandle` reference). Success line: render on `handleSaved` (replace `handleWriteSuccess`) — copy stays 'Handle saved on-chain.' which is now TRUE (receipt-gated). The existing `handleError` paragraph already renders near the input — leave its style, it now receives all failure paths.

5. **Header comment.** Update the file's header doc (the AUTH-35 line) to document the new sequence: "Handle edit — ensureActiveChain → setDisplayHandle (chainId-pinned writeContractAsync) → waitForTransactionReceipt; success indicator is MINED-receipt-gated (AUTH-35, fixed quick-260611-m72 after live silent-failure 2026-06-11)".

Do NOT add fake-DOM component tests for this wiring (per task spec). Do NOT touch any other file.

**Commit (single, atomic):** `fix(quick-260611-m72): settings Save Handle — chain guard + receipt-gated success + honest errors (was fire-and-forget)` — stage ONLY `apps/web/app/profile/[address]/settings/page.tsx`. Usual never-stage list applies (.env*, evidence/, node_modules, etc.). No push.
  </action>
  <verify>
    <automated>pnpm --filter @call-it/web build (exit 0) && pnpm --filter @call-it/web exec vitest run (all green — 236-test/26-file baseline, no regressions)</automated>
  </verify>
  <done>
    - handleSetDisplayHandle is async: ensureActiveChain → writeContractAsync with chainId: ACTIVE_CHAIN_ID → waitForTransactionReceipt → receipt.status gate
    - 'Handle saved on-chain.' renders only from local post-receipt state (handleSaved), never from hook isSuccess
    - Catch block distinguishes user rejection / insufficient funds / other with the exact honest copy above; all paths land in the existing handleError inline render
    - Button Saving.../disabled spans the full flow via local isSavingHandle
    - Web build exit 0; vitest run all green at baseline
    - Exactly one commit with only the settings page.tsx staged; not pushed
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user wallet → chain | User-signed setDisplayHandle tx; input is the raw handle string |
| wallet provider → page | Error objects from Privy/viem rendered into the page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-m72-01 | Spoofing | success indicator | mitigate | Success state gated on mined receipt.status === 'success' — eliminates the lying "saved" indicator (this IS the fix) |
| T-m72-02 | Tampering | wrong-chain write | mitigate | ensureActiveChain() + chainId pin on writeContractAsync + waitForTransactionReceipt chainId — tx cannot land on an unintended chain |
| T-m72-03 | Information Disclosure | raw error messages in UI | mitigate | Path (c) renders viem shortMessage / first line only, trimmed — no full RPC payload/stack dumped to the DOM |
| T-m72-04 | Tampering | handle input | accept | 50-char client cap kept; ProfileRegistry enforces on-chain validation authoritatively (AUTH-42); no new input surface added |
</threat_model>

<verification>
- `pnpm --filter @call-it/web build` exits 0
- `pnpm --filter @call-it/web exec vitest run` all green (236/26-file baseline)
- `git show --stat HEAD` shows exactly one file: `apps/web/app/profile/[address]/settings/page.tsx`
- Grep the page: `ensureActiveChain`, `waitForTransactionReceipt`, `isUserRejection`, `chainId: ACTIVE_CHAIN_ID` all present; `handleWriteSuccess` and `isWritingHandle` absent
</verification>

<success_criteria>
- The one write site missed by the 17-site chain sweep now follows the proven call-page pattern (guard + pin + receipt)
- No silent-failure path remains: every failure mode (rejection, no gas, wrong network, revert, failed receipt) sets an honest inline error
- Success copy is truthful: rendered only after the tx is MINED successfully
- Build + test gates green; single atomic commit, no push
</success_criteria>

<output>
Create `.planning/quick/260611-m72-settings-save-handle-honesty/260611-m72-SUMMARY.md` when done
</output>
