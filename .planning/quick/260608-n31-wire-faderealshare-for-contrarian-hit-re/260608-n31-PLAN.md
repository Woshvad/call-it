---
phase: quick-260608-n31
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/lib/relayer-client.ts
  - apps/web/app/og/[callId]/route.ts
  - apps/web/tests/og-thumbnail-200px.spec.ts
  - apps/web/tests/visual-smoke.spec.ts
  - apps/web/playwright.config.ts
autonomous: true
requirements: [SHARE-01, SHARE-10]

must_haves:
  truths:
    - "The settled OG card can render CONTRARIAN HIT because fadeRealShare is computed from real subgraph Position deposits instead of hardcoded 0"
    - "getSettledFields returns a fadeRealShare in [0,1] computed via BigInt sums (fade / (fade+follow)), and stays null/0 on any error (SHARE-10 never-500 preserved)"
    - "The SC1 200px VARIANTS array maps each of the 5 outcome words to its correct seeded call ID with CONTRARIAN HIT un-deferred"
    - "apps/web typecheck (tsc --noEmit) exits 0, including the previously-broken visual-smoke.spec.ts reducedMotion line"
  artifacts:
    - path: "apps/web/lib/relayer-client.ts"
      provides: "SettledFields.fadeRealShare field + subgraph Position-based computation"
      contains: "fadeRealShare"
    - path: "apps/web/app/og/[callId]/route.ts"
      provides: "settled card consumes settledFields.fadeRealShare (no longer hardcoded 0)"
      contains: "settledFields.fadeRealShare"
    - path: "apps/web/tests/og-thumbnail-200px.spec.ts"
      provides: "remapped SC1 VARIANTS with CONTRARIAN HIT un-deferred"
      contains: "CONTRARIAN HIT"
  key_links:
    - from: "apps/web/app/og/[callId]/route.ts"
      to: "getSettledFields().fadeRealShare"
      via: "settled-card outcome-word computation"
      pattern: "settledFields\\.fadeRealShare"
    - from: "apps/web/lib/relayer-client.ts"
      to: "subgraph Position entities"
      via: "GraphQL positions(where: callId) read + BigInt fade-share math"
      pattern: "positions\\(first"
---

<objective>
Phase 7 pre-move-on cleanup. Three independent code edits in apps/web that together unblock the CONTRARIAN HIT outcome word on the settled OG card and leave the web typecheck clean.

Purpose: The settled OG card currently hardcodes `fadeRealShare: 0`, but outcome-word.ts requires `fadeRealShare >= 0.5` for CONTRARIAN HIT — so that word is structurally unrenderable. Wiring the real fade share from subgraph Position deposits closes the last Phase 7 SC1 deferral, lets the 200px legibility matrix cover all 5 outcome words, and clears a pre-existing Phase 1 typecheck error so the full web `tsc --noEmit` is green before moving on.

Output:
- apps/web/lib/relayer-client.ts — `fadeRealShare` field on SettledFields + subgraph computation in getSettledFields
- apps/web/app/og/[callId]/route.ts — settled card consumes `settledFields.fadeRealShare`
- apps/web/tests/og-thumbnail-200px.spec.ts — remapped VARIANTS, CONTRARIAN HIT un-deferred
- apps/web/tests/visual-smoke.spec.ts + apps/web/playwright.config.ts — type-safe reducedMotion fix
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

# Source files (already read during planning — paths for the executor)
@apps/web/lib/relayer-client.ts
@apps/web/app/og/[callId]/route.ts
@apps/web/lib/outcome-word.ts
@apps/web/tests/og-thumbnail-200px.spec.ts
@apps/web/tests/visual-smoke.spec.ts
@apps/web/playwright.config.ts
@packages/subgraph/schema.graphql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire fadeRealShare into the settled OG card</name>
  <files>apps/web/lib/relayer-client.ts, apps/web/app/og/[callId]/route.ts</files>
  <action>
ROOT CAUSE: app/og/[callId]/route.ts (~line 862) passes a hardcoded `fadeRealShare: 0` into getOutcomeWordResult(), while lib/outcome-word.ts requires `fadeRealShare >= 0.5` for CONTRARIAN HIT — so the word is currently unrenderable. DO NOT edit outcome-word.ts; its COLD-CALL (repDelta <= 3) priority over CONTRARIAN HIT is correct and must be preserved.

PART (a) — apps/web/lib/relayer-client.ts:
1. Add `fadeRealShare: number | null;` to the `SettledFields` interface (after `repFallback`), with a one-line doc comment noting it is the real (non-virtual) fade share of the settled fade+follow pool, range [0,1], used for the CONTRARIAN HIT threshold (D-08, >= 0.5).
2. Add `fadeRealShare: null` to the `empty` fallback object inside getSettledFields so the SHARE-10 never-500 fallback still type-checks and degrades safely.
3. Extend SETTLED_FIELDS_QUERY to also read Position entities for this call. Position.callId is a String, but the existing query var $callId is `ID!`. Add a SEPARATE variable `$callIdStr: String!` to the query signature and add the positions selection: `positions(first: 1000, where: { callId: $callIdStr }) { side usdcDeposited }`. Pass BOTH variables in the fetch body variables object: `{ callId: String(callId), callIdStr: String(callId) }`. (Per the task brief, a separate String var keeps it type-correct; a second query is also acceptable but the single-query route is cleaner.)
4. Extend the response-typing cast for the json `data` object to include `positions?: Array<{ side?: string | null; usdcDeposited?: string | null }>`.
5. After extracting `d`, compute the fade share with BigInt to avoid float drift on raw USDC amounts: iterate `d.positions ?? []`; for each entry with a numeric/decimal usdcDeposited string, accumulate `fadeSum += BigInt(usdcDeposited)` when `side === 'fade'` and `followSum += BigInt(usdcDeposited)` when `side === 'follow'`. Guard each `BigInt(...)` so a malformed value cannot throw out of the success path (skip non-parseable entries). Then `const denom = fadeSum + followSum;` and `const fadeRealShare = denom > 0n ? Number(fadeSum) / Number(denom) : 0;`.
6. Return `fadeRealShare` in the success object alongside the existing fields. On the catch path (and the `!res.ok` / `errors` / missing-data paths) keep returning `empty` so fadeRealShare is null on error — preserving the existing SHARE-10 never-500 contract.

PART (b) — apps/web/app/og/[callId]/route.ts (in the `isOnChainSettled` branch, ~lines 856-865):
1. Replace the hardcoded `fadeRealShare: 0,` argument to getOutcomeWordResult with `fadeRealShare: settledFields.fadeRealShare ?? 0,`.
2. Update the misleading comment block immediately above (currently says fadeRealShare "stays 0 here" / "Phase 7 will wire it") to state that fadeRealShare is now read from the subgraph Position pool via getSettledFields (real non-virtual fade share), degrading to 0 on absence. Do not change any other argument in the getOutcomeWordResult call (callerWon, repDelta, viewerIsWinningFader stay as-is).

GUARDRAILS: do not edit outcome-word.ts; keep the OG route's SHARE-10 never-500 fallback intact; do not touch the duel query/path.
  </action>
  <verify>
    <automated>pnpm --filter @call-it/web exec tsc --noEmit ; echo exit=$?</automated>
  </verify>
  <done>SettledFields has a `fadeRealShare: number | null` field; getSettledFields computes it via BigInt sums over subgraph Position deposits and returns null/0 on error; the OG settled branch passes `settledFields.fadeRealShare ?? 0` into getOutcomeWordResult; outcome-word.ts is unchanged; tsc --noEmit reports no new errors for these two files.</done>
</task>

<task type="auto">
  <name>Task 2: Remap SC1 200px VARIANTS + un-defer CONTRARIAN HIT</name>
  <files>apps/web/tests/og-thumbnail-200px.spec.ts</files>
  <action>
Update the SC1 200px legibility matrix to the verified live Arbitrum-Sepolia seeded call IDs (these render correctly once Task 1 wires fadeRealShare): call 9 = fade$2/follow$1 → fadeRealShare 0.667 + repDelta +10 → CONTRARIAN HIT; call 2 = fade$1/follow$2 → 0.333 + repDelta +8 → CALLED IT; call 11 → COLD CALL; call 14 → LOUD AND WRONG; call 14 ?as=fader → FADED CORRECTLY.

1. Replace the `VARIANTS` array contents (currently lines ~99-105) with all five entries set to `deferred: false`:
   - `{ word: 'CALLED IT', url: '/og/2', deferred: false }`
   - `{ word: 'LOUD AND WRONG', url: '/og/14', deferred: false }`
   - `{ word: 'CONTRARIAN HIT', url: '/og/9', deferred: false }`  (was '/og/8', deferred: true)
   - `{ word: 'COLD CALL', url: '/og/11', deferred: false }`
   - `{ word: 'FADED CORRECTLY', url: '/og/14?as=fader', deferred: false }`
2. Update the comment block above VARIANTS (lines ~86-98): change CALLED IT to call 2; change the CONTRARIAN HIT line so it points at call 9 (fade$2/follow$1, majority real fade, now wired via fadeRealShare from subgraph Positions) and REMOVE the "DEFERRED — the OG route hardcodes fadeRealShare:0" explanation entirely. Keep COLD CALL=11, LOUD AND WRONG=14, FADED CORRECTLY=14?as=fader accurate.
3. KEEP the `test.skip(v.deferred, ...)` line, the `BASELINES_READY`/`OG_200PX_BASELINES` env gate, and the unconditional SHARE-01 1200×630 DIMENSION_VARIANTS block exactly as-is.

GUARDRAILS: do NOT delete, regenerate, or update any baseline PNGs (the orchestrator does that after deploy); do NOT run playwright; do NOT change DIMENSION_VARIANTS.
  </action>
  <verify>
    <automated>pnpm --filter @call-it/web exec tsc --noEmit ; echo exit=$?</automated>
  </verify>
  <done>VARIANTS maps CALLED IT→/og/2, LOUD AND WRONG→/og/14, CONTRARIAN HIT→/og/9 (deferred:false), COLD CALL→/og/11, FADED CORRECTLY→/og/14?as=fader; the deferred CONTRARIAN-HIT comment is removed; test.skip(v.deferred) and the env gate remain; no baseline PNGs touched.</done>
</task>

<task type="auto">
  <name>Task 3: Fix pre-existing visual-smoke reducedMotion typecheck error</name>
  <files>apps/web/tests/visual-smoke.spec.ts, apps/web/playwright.config.ts</files>
  <action>
`test.use({ reducedMotion: 'reduce' })` at apps/web/tests/visual-smoke.spec.ts line 94 errors under @playwright/test 1.60 ("'reducedMotion' does not exist in type 'Fixtures<...>'"). This is a pre-existing Phase 1 error unrelated to Phase 7; clearing it makes the full web typecheck green.

Preferred type-safe fix (no `as any`):
1. In apps/web/playwright.config.ts, add `reducedMotion: 'reduce'` to the existing top-level `use: {}` block (alongside `baseURL` and `trace`). This is a valid `PlaywrightTestConfig` use option, and a global reduced-motion setting is correct for deterministic visual snapshots across all suites.
2. In apps/web/tests/visual-smoke.spec.ts, remove the offending `test.use({ reducedMotion: 'reduce' })` line (line 94) and its "Disable animations for deterministic snapshots" comment, or keep the comment as a one-liner noting reducedMotion is now set globally in playwright.config.ts. Keep the `test.describe('Visual smoke: Phase 1 baseline pages', ...)` wrapper and all four tests inside it unchanged.

Do NOT use `as any` or `@ts-expect-error`. Do NOT alter any other suite's behavior.
  </action>
  <verify>
    <automated>pnpm --filter @call-it/web exec tsc --noEmit ; echo exit=$?</automated>
  </verify>
  <done>The reducedMotion: 'reduce' option lives in playwright.config.ts `use`; the failing test.use line is removed from visual-smoke.spec.ts; no `as any`/`@ts-expect-error` used; the four visual-smoke tests are otherwise unchanged.</done>
</task>

</tasks>

<verification>
Single gate covering all three tasks — run from repo root via the bash tool (/usr/bin/bash):

```
pnpm --filter @call-it/web exec tsc --noEmit ; echo exit=$?
```

Expected: `exit=0` (no TypeScript errors anywhere in apps/web, including the previously-broken visual-smoke.spec.ts:94).

Do NOT run playwright, do NOT generate/update baseline PNGs, do NOT deploy — those are orchestrator follow-ups after this plan.

Git hygiene: never stage the submodule `packages/contracts/lib/openzeppelin-contracts`; this plan only touches files under apps/web.
</verification>

<success_criteria>
- apps/web `tsc --noEmit` exits 0.
- getSettledFields computes fadeRealShare from subgraph Position deposits (BigInt fade/(fade+follow)) and the OG settled branch consumes `settledFields.fadeRealShare ?? 0` — CONTRARIAN HIT is now renderable.
- SHARE-10 never-500 fallback preserved (fadeRealShare null/0 on any error).
- SC1 VARIANTS remapped to live seeded IDs with CONTRARIAN HIT un-deferred; deferred-state comment removed; env gate + test.skip(v.deferred) retained; no baseline PNGs touched.
- visual-smoke.spec.ts:94 reducedMotion error resolved type-safely (option moved to playwright.config.ts use block, no `as any`).
- outcome-word.ts unchanged.
</success_criteria>

<output>
Create `.planning/quick/260608-n31-wire-faderealshare-for-contrarian-hit-re/260608-n31-SUMMARY.md` when done.
</output>
