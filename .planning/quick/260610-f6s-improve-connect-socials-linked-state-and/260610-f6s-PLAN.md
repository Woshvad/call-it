---
phase: quick-260610-f6s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/components/SocialLinkControls.tsx
autonomous: true
requirements: [AUTH-07]

must_haves:
  truths:
    - "When Twitter/X is linked, a full-width on-brand indicator shows '✓ @<username> linked' (or '✓ X linked' when username is undefined), not a tiny pill"
    - "When Farcaster is linked, a full-width on-brand '✓ Farcaster linked' indicator shows, not a tiny pill"
    - "'Linked' is shown only ONCE for Twitter — the StatusLine 'ok' state is suppressed while the new indicator conveys the linked state"
    - "Clicking 'Link Farcaster' opens a real Warpcast flow: a QR panel on desktop (scan with phone) or an 'Open in Warpcast' redirect on mobile — it NEVER hangs forever on 'Connecting…'"
    - "If the Farcaster channel times out (timeout: 300_000) or errors, an inline error shows and 'Link Farcaster' retries — no silent hang"
    - "The Cancel control on the Farcaster connect panel resets the flow to idle (setFcStatus('idle'))"
    - "Every Farcaster SDK/network call stays best-effort and surfaces inline errors — it NEVER throws into the page (additive-only guarantee, Pitfall 5/16)"
    - "The Twitter/X link+unlink path, sign-in, postTwitterLink, unlink handlers, and Providers.tsx are untouched and still function"
  artifacts:
    - path: "apps/web/app/components/SocialLinkControls.tsx"
      provides: "Full-width linked indicators (Twitter + Farcaster) + real Warpcast QR/redirect connect flow with timeout + inline error"
      contains: "QRCode"
  key_links:
    - from: "apps/web/app/components/SocialLinkControls.tsx"
      to: "@farcaster/auth-kit"
      via: "import { QRCode } from '@farcaster/auth-kit' + useSignIn url/isPolling/timeout destructure"
      pattern: "from '@farcaster/auth-kit'"
    - from: "handleLinkFarcaster"
      to: "useSignIn().connect + signIn"
      via: "await connect() then signIn() inside try/catch"
      pattern: "await connect\\(\\)"
    - from: "SocialLinkControls"
      to: "user.linkedAccounts twitter_oauth.username"
      via: "find((a) => a.type === 'twitter_oauth') as { username?: string }"
      pattern: "twitter_oauth"
---

<objective>
Improve the CONNECT SOCIALS onboarding controls in a single file: (A) replace the two tiny linked-state pills with full-width on-brand "✓ … linked" indicators (Twitter shows the X username), and (B) fix the Farcaster "Link Farcaster" button that currently hangs forever on "Connecting…" by wiring the real Auth Kit Warpcast flow (QR on desktop, redirect on mobile) with a timeout and graceful inline error.

Purpose: The current Farcaster control destructures only `{ signIn, connect }` and ignores `url`, so the QR/redirect channel is never surfaced — the button spins indefinitely. The linked state is also visually weak (a tiny pill) and never shows the user's handle.
Output: A single edited file, `apps/web/app/components/SocialLinkControls.tsx`, committed atomically to master (not pushed).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@apps/web/app/components/SocialLinkControls.tsx
@apps/web/app/onboarding/handle/page.tsx
@apps/web/app/hooks/useIsMobile.ts
@packages/ui/src/primitives/Button.tsx
@packages/ui/src/primitives/Tag.tsx

# Verified API facts (@farcaster/auth-kit@0.8.2, confirmed against installed dist/auth-kit.d.ts):
#   useSignIn(args) => { signIn, signOut, connect, reconnect, isConnected, isSuccess,
#     isPolling, isError, error, channelToken, url, appClient, data, validSignature }
#   connect: () => Promise<void>   (awaitable — resolves once channel url is ready)
#   UseSignInArgs accepts: timeout (ms), interval, onSuccess, onStatusResponse, onError
#   QRCode is a top-level export: import { QRCode } from '@farcaster/auth-kit'
#     props: { uri: string; size?: number; ecl?; logoUrl?; logoMargin?; logoSize? }  (no provider needed, no new dependency)
#   Twitter username (handle/page.tsx:32-35):
#     const twitterAccount = user?.linkedAccounts.find((a) => a.type === 'twitter_oauth')
#       as { type: 'twitter_oauth'; username?: string } | undefined;
#     twitterAccount?.username
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-width linked-state indicators (Change A — cosmetic + username)</name>
  <files>apps/web/app/components/SocialLinkControls.tsx</files>
  <action>
Replace the two tiny `<Tag intent="success">` linked-state pills with full-width on-brand "linked" indicators, and suppress the duplicate Twitter StatusLine 'ok' line.

1. Derive the Twitter username inside the component (above the return), reusing the canonical handle/page.tsx pattern: find the linkedAccounts entry of type 'twitter_oauth' cast as `{ type: 'twitter_oauth'; username?: string } | undefined`, then read `.username`. Compute a display string: `twitterUsername ? '✓ @' + twitterUsername + ' linked' : '✓ X linked'`.

2. In the Twitter linked branch, replace `<Tag intent="success" data-testid="twitter-linked-tag">X Linked</Tag>` with a FULL-WIDTH indicator element (a div/span styled to span the row like the link buttons do via `flex: 1`, NOT a small inline pill). Use the on-brand success token already in this file (`color: '#22C55E'`) with a subtle bordered brand surface (e.g. `border: '2px solid #22C55E'`, transparent background, mono font, left-aligned, padding to match a button height, `flex: 1`). Render the computed Twitter display string. KEEP `data-testid="twitter-linked-tag"`. KEEP the settings-mode Unlink button (the `{showUnlink && (...)}` block) exactly as-is, beside the indicator.

3. In the Farcaster linked branch, replace `<Tag intent="success" data-testid="farcaster-linked-tag">FC Linked</Tag>` with the same full-width indicator style, text `✓ Farcaster linked` (generic — username not reliably available). KEEP `data-testid="farcaster-linked-tag"`. KEEP the settings-mode Unlink button as-is.

4. Suppress the duplicate "linked" for Twitter: the new indicator already conveys the linked state, so do NOT render the StatusLine 'ok' line for Twitter. Easiest path: pass a status to `<StatusLine ... testId="twitter" />` that is the real twStatus EXCEPT map 'ok'→'idle' (e.g. `status={twStatus === 'ok' ? 'idle' : twStatus}`), keeping pending + error inline. Do NOT change the StatusLine component itself or the Farcaster StatusLine contract beyond what Task 2 requires.

If `Tag` becomes unused after both replacements, remove it from the `@call-it/ui` import to avoid a no-unused-vars lint/typecheck warning; if still used elsewhere, leave the import. Do NOT touch Button.tsx, Tag.tsx, or any file other than SocialLinkControls.tsx.
  </action>
  <verify>
    <automated>pnpm --filter @call-it/web exec tsc --noEmit 2>&1 | Select-String "SocialLinkControls"</automated>
  </verify>
  <done>Twitter linked state renders a full-width '✓ @<username> linked' (or '✓ X linked') indicator with data-testid="twitter-linked-tag"; Farcaster linked state renders a full-width '✓ Farcaster linked' indicator with data-testid="farcaster-linked-tag"; the Twitter StatusLine no longer shows a second "Linked." line; settings-mode Unlink buttons unchanged; tsc --noEmit reports NO new errors referencing SocialLinkControls.tsx.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Real Warpcast connect flow with timeout + inline error (Change B — the bug fix)</name>
  <files>apps/web/app/components/SocialLinkControls.tsx</files>
  <action>
Wire the real Auth Kit Warpcast flow so "Link Farcaster" surfaces a QR (desktop) or redirect (mobile) instead of hanging forever. ADDITIVE-ONLY: every SDK/network call stays best-effort and must NEVER throw into the page (Pitfall 5/16). Do NOT touch the Twitter handlers, postTwitterLink, postFarcasterLink, the unlink handlers, or Providers.tsx.

1. IMPORT: add `QRCode` to the existing `@farcaster/auth-kit` import: `import { useSignIn, QRCode } from '@farcaster/auth-kit';`.

2. DESTRUCTURE: change `const { signIn, connect } = useSignIn({...})` to also pull `url`, `isPolling`, `isError`, `error`, `channelToken`: `const { signIn, connect, url, isPolling, isError, error: fcSignInError, channelToken } = useSignIn({ timeout: 300_000, onSuccess: ..., onError: ... });`. KEEP the existing `onSuccess` (calls postFarcasterLink with nonce/domain/message/signature) and `onError` (sets fcError + fcStatus='error') exactly as they are. Add `timeout: 300_000` to the args object so the channel errors out via onError after 5 minutes instead of hanging forever. (`isError`/`fcSignInError`/`channelToken` may be unused for now — if any triggers a no-unused-vars error in this strict tsconfig, omit those specific names from the destructure and keep only `url` + `isPolling`; the timeout + onError already cover the failure path. Prefer the minimal destructure `{ signIn, connect, url, isPolling }` if the extras lint.)

3. handleLinkFarcaster(): make it an async callback. `setFcStatus('pending'); setFcError(null);` then inside try/catch: `await connect();` (await so the channel `url` is ready) then `signIn();`. On any throw, `setFcError('Could not open the Farcaster sign-in flow — try again.'); setFcStatus('error');`. NEVER rethrow. Update the useCallback deps to `[connect, signIn]`. Because connect is now awaited, the onClick wrapper must call it as `() => { void handleLinkFarcaster(); }`.

4. RENDER — connect panel: in the Farcaster NOT-linked branch, when `fcStatus === 'pending' && url && !isFarcasterLinked`, REPLACE the "Link Farcaster" button with an inline connect panel (a bordered brand-surface container, `flex: 1`, full width):
   - DESKTOP (`isMobile === false`): `<QRCode uri={url} size={176} />`, a caption "Scan with the Warpcast app on your phone", a muted "Waiting for approval…" line rendered only while `isPolling`, a small fallback anchor `<a href={url} target="_blank" rel="noopener noreferrer">Open link</a>`, and a plain-text "Cancel" button/link that does `setFcStatus('idle')`. Optional `data-testid="farcaster-qr-panel"`.
   - MOBILE (`isMobile === true`): a full-width primary "Open in Warpcast" action that does `window.location.href = url` (use `window.open(url, '_blank')` only if you prefer a new tab — either is acceptable), a caption "Approve in Warpcast, then return here", a "Waiting for approval…" line while `isPolling`, and the same "Cancel" that does `setFcStatus('idle')`. Optional `data-testid="farcaster-open-warpcast"`.
   - While `fcStatus === 'pending'` but `url` is NOT yet ready (connect still resolving), keep showing the existing button with its "Connecting…" label — the panel only appears once `url` is set. Practically: keep the existing `<Button>` as the default, and short-circuit to the panel only when the `pending && url` condition holds.

5. SUCCESS / ERROR: on success, `postFarcasterLink` sets `fcStatus='ok'` → the `isFarcasterLinked`/'ok' branch hides the panel and the Task-1 "✓ Farcaster linked" indicator shows (the linkedAccounts list updates after the relayer submits; the panel's `!isFarcasterLinked` guard plus the status reset handle the transition). On error/timeout, `fcStatus='error'` → the existing Farcaster `<StatusLine status={fcStatus} errorMsg={fcError} testId="farcaster" />` shows fcError inline, and the default "Link Farcaster" button is visible again so the user can retry. KEEP `data-testid="link-farcaster-button"` on that button.

Keep all styling consistent with the file's existing inline-style + brand-token approach (the file uses inline styles, mono font, the success green `#22C55E`, error red `#ef4444`). Do NOT import new style libraries and do NOT add a dependency — QRCode ships inside @farcaster/auth-kit, already installed.
  </action>
  <verify>
    <automated>pnpm --filter @call-it/web exec tsc --noEmit 2>&1 | Select-String "SocialLinkControls"</automated>
  </verify>
  <done>The `@farcaster/auth-kit` import includes `QRCode` and resolves; useSignIn destructures `url` (+ isPolling) and the args include `timeout: 300_000`; handleLinkFarcaster awaits connect() then calls signIn() inside try/catch and never rethrows; when pending && url is set the button is replaced by a QR panel (desktop) or "Open in Warpcast" redirect (mobile) with a Cancel that resets to idle; on timeout/error the inline fcError shows and "Link Farcaster" (data-testid="link-farcaster-button") returns for retry; tsc --noEmit reports NO new errors referencing SocialLinkControls.tsx.</done>
</task>

<task type="auto">
  <name>Task 3: Typecheck-gated atomic single-file commit to master (no push)</name>
  <files>apps/web/app/components/SocialLinkControls.tsx</files>
  <action>
Final verification and an atomic single-file commit. CRITICAL: a background soak has unrelated uncommitted files in the working tree — you MUST stage ONLY the one changed file.

1. Run the web typecheck and confirm NO new errors reference SocialLinkControls.tsx: `pnpm --filter @call-it/web exec tsc --noEmit`. Pre-existing unrelated errors are acceptable ONLY if they do not reference SocialLinkControls.tsx (in particular, pre-existing farcaster *test-file* errors are acceptable). If any new error references SocialLinkControls.tsx, fix it before committing.

2. Stage ONLY the one file with an explicit path — NEVER `git add -A`, `git add .`, or `git add -u`:
   `git add apps/web/app/components/SocialLinkControls.tsx`

3. Confirm the staging is clean: run `git status --porcelain` and verify the ONLY staged (index) entry is `apps/web/app/components/SocialLinkControls.tsx`. If anything else is staged, `git restore --staged <those paths>` until only the one file remains staged. (Unrelated files remaining UNstaged in the working tree is expected and fine.)

4. Commit atomically to master. DO NOT push. End the commit message with the Co-Authored-By trailer:
   `git commit -m "fix(quick-260610-f6s): real Warpcast Farcaster link flow + full-width linked-state indicators" -m "" -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"`

Do NOT update ROADMAP.md. Do NOT push. Do NOT touch any file other than SocialLinkControls.tsx.
  </action>
  <verify>
    <automated>git show --stat --name-only HEAD 2>&1 | Select-String "SocialLinkControls.tsx"</automated>
  </verify>
  <done>`pnpm --filter @call-it/web exec tsc --noEmit` shows no new errors referencing SocialLinkControls.tsx; exactly one file (apps/web/app/components/SocialLinkControls.tsx) is in the HEAD commit; commit message ends with the Co-Authored-By trailer; nothing pushed; ROADMAP.md untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Farcaster relay channel | The Auth Kit `url`/`channelToken` opens a relay channel; the user approves SIWF in the Warpcast app (untrusted external app), result polled back into the page |
| browser → relayer `/api/social/link/farcaster` | Existing path (untouched); the relayer re-verifies the SIWF signature server-side against FARCASTER_AUTH_DOMAIN |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-f6s-01 | Denial of Service | useSignIn channel hangs forever ("Connecting…") | mitigate | Add `timeout: 300_000` so the channel errors via onError instead of hanging; surface inline retry — the core bug being fixed |
| T-f6s-02 | Tampering | rendered `url` anchor / window redirect | accept | `url` is supplied by the trusted @farcaster/auth-kit channel (not user input); fallback anchor uses `rel="noopener noreferrer"`; no value reduction |
| T-f6s-03 | Information Disclosure | error objects surfaced inline | mitigate | Only fixed friendly copy is shown (fcError strings); raw AuthClientError is not rendered to the user |
| T-f6s-04 | Elevation of Privilege | client-supplied SIWF proof | accept | Out of scope for this file — the relayer already re-verifies the signature server-side (Pitfall 3); client cannot self-link without the relayer's server-side check |
| T-f6s-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependency — QRCode ships inside the already-installed @farcaster/auth-kit@0.8.2; no install task in this plan |
</threat_model>

<verification>
- `pnpm --filter @call-it/web exec tsc --noEmit` — no NEW errors referencing SocialLinkControls.tsx (pre-existing unrelated/farcaster test-file errors acceptable).
- The `QRCode` import resolves from '@farcaster/auth-kit' (confirmed exported in the installed 0.8.2 dist types).
- `git status --porcelain` before commit shows ONLY apps/web/app/components/SocialLinkControls.tsx staged; the HEAD commit contains exactly that one file; not pushed; ROADMAP.md untouched.

LIVE-OPS CAVEAT (NOT a code change, cannot be verified headlessly): completing a REAL Farcaster link end-to-end requires (1) the relayer up with `FARCASTER_AUTH_DOMAIN` matching the live web domain (call-it-web-sepolia.vercel.app), and (2) a human scanning the QR with the Warpcast app on a phone. The deliverable of this plan is the client-side QR/redirect flow plus a graceful inline error/timeout — i.e. no more silent hang. The full successful-link handshake is operator/human-verified, not part of automated verification here.
</verification>

<success_criteria>
- Twitter linked state shows a full-width '✓ @<username> linked' (or '✓ X linked') indicator; Farcaster linked state shows a full-width '✓ Farcaster linked' indicator; both keep their data-testids.
- "Linked" appears once for Twitter (StatusLine 'ok' suppressed); pending/error StatusLines still work.
- "Link Farcaster" opens a real Warpcast flow (desktop QR / mobile redirect) and never hangs forever; timeout + onError surface an inline error and allow retry.
- Cancel resets to idle; every Farcaster call is best-effort and never throws into the page.
- Twitter/X path, sign-in, unlink handlers, StatusLine contract (beyond the Twitter 'ok' suppression), and Providers.tsx are untouched.
- Exactly one file committed atomically to master; not pushed; ROADMAP.md untouched.
</success_criteria>

<output>
Create `.planning/quick/260610-f6s-improve-connect-socials-linked-state-and/260610-f6s-SUMMARY.md` when done
</output>
