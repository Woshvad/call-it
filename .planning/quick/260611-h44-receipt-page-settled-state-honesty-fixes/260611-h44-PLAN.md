---
phase: quick-260611-h44
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/call/[id]/page.tsx
  - apps/web/app/globals.css
  - apps/web/tests/responsive.spec.ts
autonomous: true
requirements: [QUICK-260611-H44]

must_haves:
  truths:
    - "Settled receipt with callerPnl === 0 renders '$0.00' in var(--text-secondary) with no sign — never '+$0.00' in win-yellow (locked decision 1, D-07 honesty)"
    - "Settled receipt with repDelta === 0 renders the rep delta in var(--text-secondary) with no '+'; count-up animation preserved (locked decision 1)"
    - "Caller rep chip renders var(--text-secondary) when globalRep is 0 or negative; accent-win only when > 0 (locked decision 1)"
    - "A settled call resolving to SETTLED_NEUTRAL_WORD renders the small dispute-pattern heading 'Pending result' + mono explainer — no giant Stamp; data-outcome-word attribute preserved on the replacement element (locked decision 2)"
    - "An unresolved caller (no profile handle, no live-state handle) never renders '#N' styled as identity — neutral avatar + muted mono 'caller unresolved' byline (locked decision 3, D-07/AUTH-44)"
    - "SHARE AS FRAME is demoted to a small mono text link under the primary cream button — lowercase 'or share as a Farcaster frame ↗', not a sibling button (locked decision 4)"
    - "Real verdict words (CALLED IT / LOUD AND WRONG / etc.) keep the full stamp treatment unchanged"
  artifacts:
    - path: "apps/web/app/call/[id]/page.tsx"
      provides: "All four settled-branch fixes (zero-neutral stats, neutral pending block, handleResolved byline, demoted frame link)"
      contains: "handleResolved"
    - path: "apps/web/app/globals.css"
      provides: "Hover-underline rule for the demoted frame text link"
      contains: "receipt-frame-link"
  key_links:
    - from: "apps/web/app/call/[id]/page.tsx"
      to: "apps/web/tests/responsive.spec.ts"
      via: "data-outcome-word attribute on neutral pending block (stable Playwright hook)"
      pattern: "data-outcome-word"
    - from: "apps/web/app/call/[id]/page.tsx"
      to: "apps/web/tests/responsive.spec.ts"
      via: "[data-receipt-action-row] full-width-at-mobile assertion — demoted link must remain full-width on mobile"
      pattern: "data-receipt-action-row"
---

<objective>
Settled-receipt honesty fixes on the call receipt page (QUICK-260611-H44). Four USER-LOCKED design decisions from the senior-design review — implement exactly, do not revisit:

1. Zero-neutral money/rep: P&L, Rep Δ, and the caller rep chip stop rendering zero as a win (no "+$0.00" in win-yellow).
2. Neutral PENDING RESULT state drops the giant stamp for the small dispute-pattern heading + explainer.
3. Unresolved-caller byline stops fabricating '#14' as a styled identity (D-07 violation).
4. SHARE AS FRAME demoted from sibling button to a small mono text link under the primary.

Purpose: the settled receipt is the Core Value surface ("unfakeable, undeletable") — every fix removes a place where the page visually fabricates a win, an identity, or false equivalence between share actions.
Output: modified `apps/web/app/call/[id]/page.tsx` settled branch, one small globals.css hover rule, and any expectation-only Playwright assertion updates.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@apps/web/app/call/[id]/page.tsx
@apps/web/lib/outcome-word.ts
@apps/web/tests/responsive.spec.ts

Read the settled branch of page.tsx (the `if (isSettled || (isCallerExited && callData?.outcome))` block, ~lines 1816-2230) before editing. Constants already in scope there: `resolveSettledWord`, `SETTLED_NEUTRAL_WORD`, `isNeutralOutcome` (line ~1851), `outcomeStyle` (whose `.hex` equals SETTLED_NEUTRAL_COLOR `#94A3B8` for the neutral word — no new import required), `displayedRepDelta` count-up state (line ~1284), `avatarGradClass`/`avatarInitial` helpers.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the four settled-branch honesty fixes in page.tsx</name>
  <files>apps/web/app/call/[id]/page.tsx, apps/web/app/globals.css</files>
  <action>
All edits in the SETTLED RECEIPT branch of apps/web/app/call/[id]/page.tsx unless noted. Do NOT touch the LIVE branch's `displayHandle` fallback (~line 1794) — locked decision 3 scopes to the settled receipt only. Do NOT change outcome-word.ts word/color constants (spec-locked).

(1) ZERO-NEUTRAL MONEY/REP — per locked decision 1:
- P&L stat (~lines 2080-2087): replace the two-way `callerPnl >= 0n` ternary with a three-way: `callerPnl === 0n` → color `var(--text-secondary)`, display `formatUsdc(0n)` with NO sign; `> 0n` → `var(--accent-win)` with `+` prefix; `< 0n` → `var(--accent-loss)` with `-${formatUsdc(-callerPnl)}` (unchanged).
- Rep Δ stat (~lines 2088-2097): color keys off the REAL `repDelta` value three-way (`=== 0` → `var(--text-secondary)`, `> 0` → accent-win, `< 0` → accent-loss). Sign also keys off real `repDelta` (`repDelta > 0 ? '+' : ''` — negative displayedRepDelta carries its own minus). The displayed NUMBER stays `displayedRepDelta` — keep the UI-45 count-up animation untouched.
- Caller rep chip (~line 1986): replace the hardcoded `var(--accent-win)` with `callerProfile.globalRep > 0 ? 'var(--accent-win)' : 'var(--text-secondary)'` (zero AND negative both muted — accent-win ONLY when > 0).

(2) PENDING/UNRESOLVED SETTLED STATE — NO STAMP — per locked decision 2:
- In the non-disputed branch (~lines 2041-2067), add an `isNeutralOutcome` middle branch (`isDisputed ? ... : isNeutralOutcome ? ... : <existing stamp>`). The neutral branch mirrors the EXACT structural pattern of the isDisputed block directly above it (centered container, `.h-2` heading, one `.mono` 13px explainer line): heading text "Pending result" colored `outcomeStyle.hex` (which is SETTLED_NEUTRAL_COLOR slate `#94A3B8` for the neutral word) with `textTransform: 'uppercase'`; explainer text "Settlement recorded — the final outcome hasn't been confirmed yet. Check back shortly." in `var(--text-secondary)`.
- CRITICAL: put `data-outcome-word={outcomeWord}` on the neutral branch's container div — it is the stable Playwright hook (responsive.spec.ts:239). Do not drop the attribute.
- Real verdict words keep the full Stamp treatment byte-for-byte unchanged (including `outcomeStyle.sub` and `outcomeLozenge` rendering).

(3) UNRESOLVED-CALLER BYLINE — per locked decision 3 (D-07: never fake data; AUTH-44: never render a wallet address):
- Near the existing `handle` declaration (~line 1825), add `const handleResolved = Boolean(callerProfile?.handle ?? callData?.handle);`. Keep `handle` itself unchanged — the share-text builders (~1866-1879) are gated on `callData?.handle`, so they only ever consume a real handle.
- Avatar (~lines 1975-1977): when `handleResolved` is false, do NOT render `avatarGradClass(handle)`/`avatarInitial(handle)` (a grad identity seeded by '#14' is fabricated). Render instead `<span className="avatar lg" aria-hidden="true" style={{ background: 'var(--bg-tertiary)', color: 'var(--text-tertiary)' }}>?</span>`.
- Name slot (~lines 1980-1982): when unresolved, replace the 20px/900 display-font handle with muted mono lowercase: `<span className="mono" style={{ fontSize: 13, color: 'var(--text-tertiary)' }}>caller unresolved</span>`. When resolved, the existing markup is unchanged.
- Sweep the rest of the settled branch for other `handle` consumers rendering the '#N' fallback as identity (grep `handle` within lines 1816-2230) — winners/losers tables use per-position `p.handle` (real relayer data, unaffected). Nothing else should render the fallback; verify, don't refactor.

(4) DEMOTE SHARE AS FRAME — per locked decision 4:
- Action row (~lines 1996-2027): change `[data-receipt-action-row]` to `flexDirection: 'column'` always (primary cream button on top, frame link below); keep mobile full-width for the primary (`width: isMobile ? '100%' : undefined` stays). On desktop, keep the column visually anchored where the row sat (e.g. `alignItems: isMobile ? 'stretch' : 'flex-end'`).
- Replace the `.btn outline-white` "SHARE AS FRAME →" anchor with a small mono text link: text exactly "or share as a Farcaster frame ↗" (lowercase), `className="mono receipt-frame-link"`, fontSize 11.5, color `var(--text-secondary)`, `textDecoration: 'none'`, `minHeight: 44` with `display: 'inline-flex', alignItems: 'center'` (mobile touch target), still `target="_blank" rel="noopener noreferrer"` (keep rel — reverse-tabnabbing guard, threat T-h44-01). On mobile give it `width: '100%'` — the responsive spec (responsive.spec.ts:219-234) asserts EVERY `a`/`button` inside `[data-receipt-action-row]` spans >= viewport-64 at 375px; a full-width anchor keeps that green without touching the spec.
- globals.css: add one rule near the existing `.btn.outline-white` block: `.receipt-frame-link:hover { text-decoration: underline; }`. Inline styles cannot express :hover; this is the minimal sanctioned CSS addition. Flexbox only — no grid.
  </action>
  <verify>
    <automated>cd "C:\Users\woshv\Desktop\Call it" && pnpm --filter @call-it/web build</automated>
  </verify>
  <done>Build passes. Settled branch renders: three-way P&L/RepΔ/rep-chip colors (zero = text-secondary, no sign); neutral word renders dispute-pattern block with data-outcome-word attribute and no Stamp; real words render the unchanged Stamp; unresolved caller shows neutral "?" avatar + mono "caller unresolved" (no grad class seeded by '#N'); frame share is a mono text link under the primary with hover underline and 44px touch target.</done>
</task>

<task type="auto">
  <name>Task 2: Playwright/unit assertion sweep + verification gates</name>
  <files>apps/web/tests/responsive.spec.ts</files>
  <action>
Expectation-only test sweep — no behavior changes beyond Task 1:

1. Grep apps/web/tests/ (and apps/web/app/ test files if any) for 'SHARE AS FRAME', 'data-outcome-word', 'PENDING RESULT':
   - 'SHARE AS FRAME' appears only in a comment in settled-outcome-truth.test.ts — leave it (the unit test asserts resolveSettledWord/buildShareText behavior, which is unchanged).
   - responsive.spec.ts:236-251 ("outcome word legible") asserts fontSize >= 36 on `[data-outcome-word]`. SEEDED_SETTLED_CALL defaults to '14' (CallerLost → real "LOUD AND WRONG" → stamp path), so the assertion remains valid for the seeded call. Confirm this and leave the assertion as-is; add a one-line comment in the spec noting the neutral PENDING RESULT state intentionally renders a small heading (not the stamp) and is exempt from the >=36px expectation — so a future RESPONSIVE_SETTLED_CALL_ID pointing at a neutral call is a seeding error, not a regression.
   - responsive.spec.ts:219-234 ("action row full-width") needs NO assertion change provided Task 1 made the demoted link full-width on mobile inside `[data-receipt-action-row]`. Verify the locator (`button, a` inside the marker) still matches the new structure.
2. Run the unit suite and build gates.
3. Commit as a single atomic commit (or one per task if already committed separately): `fix(quick-260611-h44): settled receipt honesty — zero-neutral stats, pending-state no-stamp, unresolved byline, demoted frame link`.
  </action>
  <verify>
    <automated>cd "C:\Users\woshv\Desktop\Call it" && pnpm --filter @call-it/web test && pnpm --filter @call-it/web build</automated>
  </verify>
  <done>Unit suite and build both pass. No Playwright spec asserts the removed "SHARE AS FRAME →" button; data-outcome-word and action-row specs remain green by construction (seeded call 14 renders the real stamp; demoted link is full-width on mobile). Changes committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| receipt page → external share intents | target=_blank anchors to warpcast.com / x.com |
| relayer /live-state → settled receipt render | untrusted/absent fields drive identity + outcome display |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-h44-01 | Tampering | demoted frame link anchor | mitigate | keep `rel="noopener noreferrer"` on the target=_blank text link (reverse-tabnabbing) |
| T-h44-02 | Spoofing | settled receipt identity/outcome render | mitigate | this plan IS the mitigation — no fabricated '#N' identity, no win-styled zero, no stamp for unconfirmed outcomes (D-07/AUTH-44) |
| T-h44-SC | Tampering | npm/pip/cargo installs | accept | no new packages installed in this plan |
</threat_model>

<verification>
- `pnpm --filter @call-it/web test` — full unit suite green (settled-outcome-truth.test.ts unchanged and passing)
- `pnpm --filter @call-it/web build` — Next.js production build green
- Grep confirms: no remaining `btn outline-white` SHARE AS FRAME in page.tsx; `data-outcome-word` present in both the neutral block and the stamp block; `handleResolved` gates both avatar and name slot
</verification>

<success_criteria>
All four locked decisions implemented exactly as specified in the settled branch of apps/web/app/call/[id]/page.tsx; live branch untouched; outcome-word.ts constants untouched; unit tests + build pass; Playwright expectation hooks (`data-outcome-word`, `[data-receipt-action-row]`) preserved without weakening assertions.
</success_criteria>

<output>
Create `.planning/quick/260611-h44-receipt-page-settled-state-honesty-fixes/260611-h44-SUMMARY.md` when done.
</output>
