---
phase: quick-260611-if0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/new/components/PriceTargetFields.tsx
  - apps/web/app/new/lib/hermes-price.ts
  - apps/web/app/new/page.tsx
  - apps/web/app/new/lib/web-call-schema.ts
  - apps/web/app/new/components/EventFields.tsx
  - apps/web/tests/web-call-schema.test.ts
  - .planning/quick/260611-if0-close-out-hog-findings-direction-honesty/260611-if0-deferred-items.md
autonomous: true
requirements: [QUICK-260611-IF0]

must_haves:
  truths:
    - "Price Target field shows the explicit win condition 'Wins if price closes at or above target at the deadline.' — direction is honest in the composer (on-chain v1 settles >= only, SettlementManager.sol:718)"
    - "Composer receipt preview renders the '≥' relationship for priceTarget, matching server-built receipt lines (relayer-client.ts marketLine)"
    - "webCreateCallSchema rejects every non-ticker assetA/assetB string (0x-64-hex feed id, 0x-40-hex address, numeric, freeform) with 'Use a listed asset (BTC, ETH, SOL…)' BEFORE preflight"
    - "resolveAssetToFeedId 0x-64-hex passthrough + assetToUint256Parity + buildPreflightBody behavior byte-identical (relayer dup-hash parity untouched)"
    - "Below-target direction recorded as a contracts-v2 deferral in 260611-if0-deferred-items.md"
  artifacts:
    - path: ".planning/quick/260611-if0-close-out-hog-findings-direction-honesty/260611-if0-deferred-items.md"
      provides: "contracts-v2 deferral record (below-target direction)"
    - path: "apps/web/app/new/lib/web-call-schema.ts"
      provides: "ticker-only form gate (LISTED_ASSET_MESSAGE on all branches)"
      contains: "LISTED_ASSET_MESSAGE"
    - path: "apps/web/tests/web-call-schema.test.ts"
      provides: "flipped + new assertions pinning the ticker-only gate"
  key_links:
    - from: "apps/web/app/new/lib/web-call-schema.ts"
      to: "@call-it/shared PYTH_FEED_IDS"
      via: "ticker-membership check (the 24 UI-producible symbols)"
      pattern: "PYTH_FEED_IDS"
    - from: "apps/web/app/new/page.tsx"
      to: "Receipt mode=preview"
      via: "previewMarketLine (display-only — NOT preflight/dup-hash input)"
      pattern: "previewMarketLine"
---

<objective>
Close out the two quick-260611-hog findings: (1) direction honesty — the composer must say out loud that price-target calls win ONLY at-or-above target (SettlementManager v1 settles `currentPrice >= target`, SettlementManager.sol:713-722, deployed + immutable; below-target calls are impossible in v1 and must not be implied); (2) web schema tightening — since the AssetSelect dropdown landed, the only UI-producible asset values are the 24 tickers, so webCreateCallSchema's raw-hex/numeric acceptance is a dead path that must be rejected at the form level with a clear inline error.

Purpose: never let a user believe they made a "below X" call that on-chain settles as "at or above X"; never let a dead input class through the form gate.
Output: copy + comment changes in PriceTargetFields/hermes-price/page.tsx, tightened webCreateCallSchema + adapted tests, a deferred-items note recording the contracts-v2 deferral.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@apps/web/app/new/components/PriceTargetFields.tsx
@apps/web/app/new/components/EventFields.tsx
@apps/web/app/new/lib/web-call-schema.ts
@apps/web/app/new/lib/resolve-asset.ts
@apps/web/app/new/lib/hermes-price.ts
@apps/web/tests/web-call-schema.test.ts
@apps/web/app/new/page.tsx

VERIFIED GROUND TRUTH (do not re-derive):
- packages/contracts/src/SettlementManager.sol:713-722 settles price-target as `if (currentPrice >= target) CallerWon; else CallerLost;` — NO direction field exists on-chain; contracts are deployed + immutable. READ-ONLY reference — do NOT touch packages/contracts.
- AssetSelect option values are plain ticker strings ('BTC' …), `keyof typeof PYTH_FEED_IDS` (24 symbols). PriceTargetFields, EventFields, SpreadVsFields all use it — no free-text asset path remains in the UI.
- Quote mode does NOT prefill assetA (page.tsx default `assetA: ''`; quote flow only sets criteriaText) — the 0x-hex form path is producible by NO UI flow.
- previewMarketLine (page.tsx:199-205) feeds ONLY `<Receipt mode="preview">` (lines ~349, ~486). It is display-only — preflight body + calldata + dup-hash derive from buildPreflightBody/canonicalAssetForWire, NOT from this string.
- Server-built receipt lines use the '≥' glyph ("ETH ≥ $1,000,000" — apps/web/lib/relayer-client.ts:146, apps/web/components/FeedList.tsx:36). The only '>=' test fixture is a MOCKED SERVER response in apps/web/tests/quote-composer.spec.ts:109 — it does not pin the preview; leave it untouched.
- EVENT_ASSET_MESSAGE is defined in web-call-schema.ts and imported ONLY by web-call-schema.test.ts (safe to rename).
- UNKNOWN_ASSET_MESSAGE lives in resolve-asset.ts and is used by preflight-body.ts (publish-time abort) + its tests — that layer is PARITY-ADJACENT and stays byte-identical.
- Relayer-parity invariant (bf2 CR-01): the dup-check/preflight body must keep producing the same canonical asset representation the relayer computes. resolveAssetToFeedId / assetToUint256Parity / canonicalAssetForWire / buildPreflightBody are the parity surface — NONE of them change in this plan. Only the RHF form gate (webCreateCallSchema) tightens, which sits BEFORE that surface.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Direction-honesty copy + comments + deferred-items note</name>
  <files>apps/web/app/new/components/PriceTargetFields.tsx, apps/web/app/new/lib/hermes-price.ts, apps/web/app/new/page.tsx, .planning/quick/260611-if0-close-out-hog-findings-direction-honesty/260611-if0-deferred-items.md</files>
  <action>
1. PriceTargetFields.tsx — in the Price Target (USD) field group, add a static helper line as a sibling BETWEEN the `<Controller name="targetValue">` block and the `{errors.targetValue && …}` error div (matching the Asset group's order: control → mono helper → error). Exact copy: "Wins if price closes at or above target at the deadline." Token styles only — reuse the existing helper treatment: `className="mono" style={{ fontSize: 11, color: 'var(--text-tertiary)' }}`. This one line also covers the +pct chip row (chips render directly above the input in the same group) — no separate chip caption needed.

2. PriceTargetFields.tsx — extend the TARGET_CHIP_PCTS doc comment (currently "Direction default is ABOVE…") with the deferral anchor: "Below-target direction is a contracts-v2 feature — SettlementManager v1 settles >= only (SettlementManager.sol:718)."

3. hermes-price.ts — extend the computeChipTarget doc comment (currently ends "Revisit if a direction control ever ships in the composer.") with the same sentence: "Below-target direction is a contracts-v2 feature — SettlementManager v1 settles >= only (SettlementManager.sol:718)."

4. page.tsx — in previewMarketLine (line ~200), change the priceTarget/event relation glyph from `'>='` to `'≥'` so the composer preview matches the server-built receipt lines ("ETH ≥ $1,000,000"). This is display-only (verified: feeds only Receipt mode="preview"); do NOT touch buildPreflightBody, dup-check, or any preflight input. Leave the `'vs'` spreadVs arm and quote-composer.spec.ts:109 (mocked server line) untouched.

5. Create .planning/quick/260611-if0-close-out-hog-findings-direction-honesty/260611-if0-deferred-items.md recording: below-target ("price closes below X") calls require a contracts-v2 change — a direction field through CallRegistry create path + a below branch in SettlementManager settle; v1 deployed contracts settle strictly `currentPrice >= target` (SettlementManager.sol:713-722) and are immutable, so the UI intentionally exposes above-only chips/copy. Note the composer helper copy + comment anchors added by this task as the v1 honesty mitigation.
  </action>
  <verify>
    <automated>cd "C:\Users\woshv\Desktop\Call it" && grep -c "at or above target" apps/web/app/new/components/PriceTargetFields.tsx && grep -c "SettlementManager.sol:718" apps/web/app/new/components/PriceTargetFields.tsx apps/web/app/new/lib/hermes-price.ts && grep -c "≥" apps/web/app/new/page.tsx && test -f ".planning/quick/260611-if0-close-out-hog-findings-direction-honesty/260611-if0-deferred-items.md"</automated>
  </verify>
  <done>Helper copy renders under the Price Target input (token styles); both chip-math sites carry the SettlementManager.sol:718 anchor comment; preview marketLine uses '≥'; deferred-items note exists. No preflight/dup-hash input changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tighten webCreateCallSchema to ticker-only + adapt tests</name>
  <files>apps/web/app/new/lib/web-call-schema.ts, apps/web/app/new/components/EventFields.tsx, apps/web/tests/web-call-schema.test.ts</files>
  <behavior>
    - priceTarget + 'ETH' (any of the 24 tickers, case/trim-insensitive) → passes
    - priceTarget + full 0x-64-hex ETH feed id → FAILS on assetA with LISTED_ASSET_MESSAGE (the dead-path pin), while resolveAssetToFeedId still resolves the same string (lib passthrough intact — covered in tests/resolve-asset.test.ts, do not move/weaken)
    - priceTarget + 'DOGECOIN' → FAILS on assetA with LISTED_ASSET_MESSAGE (was UNKNOWN_ASSET_MESSAGE)
    - spreadVs + unresolvable/missing assetB → FAILS on assetB with LISTED_ASSET_MESSAGE
    - event + 'ETH' → passes
    - event + '0xAbCdEf…01' (0x-40-hex) → FAILS with LISTED_ASSET_MESSAGE (FLIPPED from passing)
    - event + '12345' → FAILS with LISTED_ASSET_MESSAGE (FLIPPED from passing)
    - event + 'SomeNewToken' / '0xNotHex' → still FAIL, message now LISTED_ASSET_MESSAGE
    - exact-copy export test: LISTED_ASSET_MESSAGE === 'Use a listed asset (BTC, ETH, SOL…)'
  </behavior>
  <action>
Update tests FIRST (red), then tighten the schema (green).

1. web-call-schema.ts — replace the per-branch resolveAssetToFeedId / isUintParseableAsset gates with a single local ticker-membership helper: trim the input, uppercase it, check `in PYTH_FEED_IDS` (import PYTH_FEED_IDS from '@call-it/shared'; keep the no-direct-zod-import constraint — ctx code 'custom' stays). Gate: priceTarget assetA, spreadVs assetA AND assetB, event assetA. Rename EVENT_ASSET_MESSAGE → LISTED_ASSET_MESSAGE (same exact copy 'Use a listed asset (BTC, ETH, SOL…)'); use it for ALL branches; drop the now-unused UNKNOWN_ASSET_MESSAGE / resolveAssetToFeedId / isUintParseableAsset imports FROM THIS FILE ONLY. Do NOT gate event assetB (it carries exchange/metric strings like 'binance'/'tvl'). Rewrite the header comment: the form gate is now ticker-only because AssetSelect is the sole asset entry path (quick-260611-hog); resolveAssetToFeedId's 0x-64-hex passthrough remains INTENTIONALLY intact at the resolution/parity layer (preflight-body.ts publish abort + dup-hash parity, bf2 CR-01) — the schema sits in front of it, so relayer parity is unaffected. Add the finding-(1) schema-site comment: "Below-target direction is a contracts-v2 feature — SettlementManager v1 settles >= only (SettlementManager.sol:718); CreateCallInput has no direction field."

2. EventFields.tsx — fix the now-stale Asset comment (lines ~59-61): webCreateCallSchema no longer accepts hex-address/numeric event assets; entry AND schema are both ticker-only.

3. web-call-schema.test.ts — adapt per the behavior list: flip the two event acceptance tests (0x-40-hex, '12345') to expect failure with the exact LISTED_ASSET_MESSAGE; update the priceTarget/spreadVs message assertions from UNKNOWN_ASSET_MESSAGE to LISTED_ASSET_MESSAGE (drop that import); add the dead-path pin test (priceTarget + `PYTH_FEED_IDS.ETH` string fails at form level); update the exact-copy export test; update the header comment to describe the ticker-only gate.

PARITY GUARD (hard constraint): resolve-asset.ts, preflight-body.ts, tests/resolve-asset.test.ts, tests/preflight-body.test.ts must remain byte-identical — `git status --porcelain` must show no entry for them. Do NOT weaken any parity test. FALLBACK RULE: if the full suite reveals a genuine parity-test conflict with the tightening (a parity test asserting the FORM schema accepts 0x-hex — none found during planning), do not weaken it; instead keep the schema unchanged, document the passthrough as intentional in web-call-schema.ts, append the skipped tightening to 260611-if0-deferred-items.md, and report honestly in the SUMMARY.
  </action>
  <verify>
    <automated>cd "C:\Users\woshv\Desktop\Call it" && pnpm --filter @call-it/web test && pnpm --filter @call-it/web build</automated>
  </verify>
  <done>Full web vitest suite green with the flipped/new assertions (no parity test modified — git shows resolve-asset/preflight-body files untouched); web build exits 0; non-ticker asset strings are rejected inline with 'Use a listed asset (BTC, ETH, SOL…)' before any preflight call.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @call-it/web test` — full suite green (baseline 21 files / 197 tests; this plan adapts ~6 assertions + adds ~2 tests in web-call-schema.test.ts only)
- `pnpm --filter @call-it/web build` — exits 0
- `git status --porcelain -- apps/web/app/new/lib/resolve-asset.ts apps/web/app/new/lib/preflight-body.ts apps/web/tests/resolve-asset.test.ts apps/web/tests/preflight-body.test.ts packages apps/relayer` → no tracked modifications (parity surface + contracts + relayer untouched; the two pre-existing untracked soak scripts under apps/relayer are not modifications)
</verification>

<success_criteria>
- Composer states the >= win condition explicitly at the Price Target input; receipt preview uses '≥' consistent with server-built lines; no implied below-target capability anywhere in the price-target flow
- webCreateCallSchema accepts ONLY the 24 AssetSelect tickers for priceTarget/spreadVs/event assetA (+ spreadVs assetB); rejection copy is 'Use a listed asset (BTC, ETH, SOL…)' rendered inline before the confirm modal
- Relayer dup-hash parity surface byte-identical; no contract, relayer, or shared-package changes; no new deps
- Contracts-v2 below-target deferral recorded in 260611-if0-deferred-items.md
</success_criteria>

<output>
Create `.planning/quick/260611-if0-close-out-hog-findings-direction-honesty/260611-if0-SUMMARY.md` when done.
</output>
