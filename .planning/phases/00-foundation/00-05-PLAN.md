---
phase: 00-foundation
plan: 05
type: execute
wave: 4
depends_on: [01, 02, 03, 04]
files_modified:
  - .github/workflows/deploy-web.yml
  - .github/workflows/deploy-relayer.yml
  - .github/workflows/deploy-subgraph.yml
  - .github/workflows/phase-0-gate.yml
  - scripts/phase-0-smoke.ts
  - scripts/test/phase-0-smoke.test.ts
  - package.json
  - docs/phase-0-deploy-checklist.md
autonomous: false
requirements:
  - OPS-06
  - OPS-19
  - OPS-24
  - SAFETY-58
  - SHARE-09
  - SHARE-10
  - SHARE-11
tags:
  - deploy
  - smoke-test
  - ci
  - gate
user_setup:
  - service: vercel
    why: "Per D-04 — frontend host for apps/web; OG Fallback route lives here"
    env_vars:
      - name: VERCEL_TOKEN
        source: "Vercel Dashboard → Settings → Tokens → Create Token (scope: deployment-only)"
      - name: VERCEL_ORG_ID
        source: "Vercel Dashboard → Settings → General → Organization ID"
      - name: VERCEL_PROJECT_ID
        source: "Vercel project Settings → General → Project ID (one per network: call-it-web-sepolia, call-it-web-mainnet)"
    dashboard_config:
      - task: "Create 2 Vercel projects (call-it-web-sepolia, call-it-web-mainnet) linked to the same GitHub repo with different branch deploy targets (sepolia branch / main branch); set NEXT_PUBLIC_NETWORK + NEXT_PUBLIC_CHAIN_ID + NEXT_PUBLIC_OG_BASE_URL per project (Pitfall E mitigation — env vars per project, not per environment)"
        location: "Vercel Dashboard → New Project → Import from GitHub"
  - service: fly-io-deploy
    why: "Per D-01 — relayer + (future Phase 7) OG service host"
    env_vars:
      - name: FLY_API_TOKEN
        source: "Fly.io Dashboard → Account Settings → Access Tokens → Create Token (scope: deploy)"
    dashboard_config:
      - task: "Two Fly apps already created in Plan 00-02 user_setup (call-it-relayer-sepolia + call-it-relayer-mainnet); this plan adds the GH Actions deploy workflows for them"
        location: "Plan 00-02 user_setup; Fly Dashboard"
      - task: "For each Fly app, inject GCP secrets via `flyctl secrets set` — the deploy workflow does this from GCP Secret Manager via `gcloud secrets versions access`"
        location: "Set via GH Actions deploy workflow at deploy time"

must_haves:
  truths:
    - "GitHub Actions `deploy-web.yml` deploys `apps/web` to Vercel on push to the relevant branch (sepolia → call-it-web-sepolia project; main → call-it-web-mainnet project); deployment URL is logged for operator + the smoke test consumes it."
    - "GitHub Actions `deploy-relayer.yml` deploys `apps/relayer` to Fly.io (call-it-relayer-sepolia or call-it-relayer-mainnet) on push to the relevant branch; secrets are injected from GCP Secret Manager via `gcloud secrets versions access` (D-08) at deploy time."
    - "GitHub Actions `deploy-subgraph.yml` deploys `packages/subgraph` to Subgraph Studio for arbitrum-sepolia (Phase 0 scope; Phase 7 promotes to Decentralized Network) using SUBGRAPH_STUDIO_DEPLOY_KEY from GH Secrets."
    - "GitHub Actions `phase-0-gate.yml` is a blocking gate workflow that, on tagged release `phase-0-complete`, runs `pnpm tsx scripts/phase-0-smoke.ts` which exercises all 6 success criteria of Phase 0 end-to-end and exits non-zero on any failure."
    - "`scripts/phase-0-smoke.ts` performs the 6 cross-plan validation steps: (1) pnpm build succeeds across all 6 packages, (2) the 3 grep guards return zero matches on current code, (3) the deployed relayer's /health endpoint returns 200, (4) the deployed subgraph dry-run returns success, (5) the OG Fallback URL returns 200 with X-Variant: fallback header in <100ms, (6) a manual synthetic-alert fire (with auto-seeded HMAC + nonce) verifies the Telegram pipeline within 60s."
  artifacts:
    - path: .github/workflows/deploy-web.yml
      provides: "Vercel deploy workflow for apps/web — Sepolia + mainnet projects routed by branch"
    - path: .github/workflows/deploy-relayer.yml
      provides: "Fly.io deploy workflow for apps/relayer — Sepolia + mainnet apps routed by branch; injects GCP Secret Manager secrets via gcloud"
    - path: .github/workflows/deploy-subgraph.yml
      provides: "Subgraph Studio deploy workflow for packages/subgraph — arbitrum-sepolia in Phase 0"
    - path: .github/workflows/phase-0-gate.yml
      provides: "Tagged-release gate that runs phase-0-smoke.ts end-to-end across all deployed artifacts"
    - path: scripts/phase-0-smoke.ts
      provides: "6-step smoke test: build / grep guards / relayer health / subgraph deploy / OG fallback / synthetic alert"
    - path: docs/phase-0-deploy-checklist.md
      provides: "Operator pre-tag checklist for the phase-0-complete release (env-var diff verification, GCP project structure verification, all 5 KMS keys provisioned in BOTH GCP projects, Telegram bot permission verification)"
      min_lines: 30
  key_links:
    - from: .github/workflows/deploy-relayer.yml
      to: GCP Secret Manager (call-it-sepolia / call-it-mainnet projects)
      via: "google-github-actions/auth + gcloud secrets versions access → flyctl secrets set"
      pattern: "secrets versions access|flyctl secrets set"
    - from: .github/workflows/deploy-web.yml
      to: Vercel
      via: "amondnet/vercel-action@v25 or vercel CLI; project ID per branch"
      pattern: "vercel-action|vercel deploy"
    - from: scripts/phase-0-smoke.ts
      to: scripts/fire-synthetic-alert.ts
      via: "Step 6 invokes the synthetic-alert helper from Plan 00-04 as a subprocess"
      pattern: "fire-synthetic-alert"
    - from: scripts/phase-0-smoke.ts
      to: apps/web/api/og/fallback (deployed Vercel URL)
      via: "HTTP GET with timing measurement; asserts 200 + <100ms + X-Variant: fallback"
      pattern: "og/fallback"
---

## Phase Goal

**As an** operator wrapping Phase 0 and entering Phase 1, **I want to** tag the repo `phase-0-complete` and watch a blocking GitHub Actions workflow exercise all 6 success criteria end-to-end (build + 3 grep guards + relayer /health + subgraph deploy + OG fallback render under 100ms + Telegram synthetic-alert round-trip under 60s) against the live Sepolia deployments, **so that** "Phase 0 is done" means "every claim in the ROADMAP success criteria is mechanically verified — not asserted." Once the gate is green, Phase 1 can land `CallRegistry` against a substrate that is provably ready.

<objective>
Land the deploy workflows for all three deployable surfaces (Vercel for `apps/web`, Fly.io for `apps/relayer`, Subgraph Studio for `packages/subgraph`) and the cross-cutting `phase-0-smoke.ts` gate that proves all 6 ROADMAP Phase 0 success criteria are mechanically green against the actual deployed artifacts (not just local builds).

Purpose: Closes the deploy half of OPS-19 (KMS-backed relayer running in production), OPS-24 (Fastify on Fly.io responding 200 to /health from a public URL), OPS-06 (Better Stack dashboards populated by a real running relayer), SHARE-09/10/11 (OG Fallback live on Vercel with <100ms p95), SAFETY-58 (Safe deploy artifact verified post-deploy). This plan does NOT introduce new requirement IDs — it validates that Plans 00-01 through 00-04 actually work in their deployed state.

Output: 4 GitHub Actions workflows (3 deploys + 1 gate), 1 smoke-test script with 6 verification steps, 1 operator pre-tag checklist. On `git tag phase-0-complete && git push --tags`, the gate workflow runs all 6 steps; if any fail, the tag is invalid and Phase 1 does not start.

**Autonomous: false** — Task 5 is a `checkpoint:human-verify` step where the operator confirms (a) GCP IAM roles are set correctly across BOTH GCP projects, (b) Telegram bot has the privacy + admin + read-messages permissions per Open Question 5 in RESEARCH.md, (c) all 5 Better Stack dashboards visibly show synthetic data per Pitfall G, (d) the Sepolia Safe deployment is real on Arbiscan Sepolia. These are operator-side hosted-resource verifications that cannot be replaced by CLI automation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-foundation/00-CONTEXT.md
@.planning/phases/00-foundation/00-RESEARCH.md
@.planning/phases/00-foundation/00-VALIDATION.md
@CLAUDE.md
@CALL_IT_SPEC1.md
@.planning/phases/00-foundation/00-01-SUMMARY.md
@.planning/phases/00-foundation/00-02-SUMMARY.md
@.planning/phases/00-foundation/00-03-SUMMARY.md
@.planning/phases/00-foundation/00-04-SUMMARY.md

<interfaces>
Consumes from Plan 00-01:
- Root `package.json` scripts (`pnpm turbo run lint test build`)
- `.github/workflows/grep-guards.yml` — the 3 guards this plan re-runs against the build artifacts

Consumes from Plan 00-02:
- `apps/relayer/Dockerfile` + `apps/relayer/fly.toml` — deployable
- Endpoint `/health` for smoke step 3
- Endpoint `/internal/test-alert` for smoke step 6

Consumes from Plan 00-03:
- `packages/subgraph/package.json` — `deploy:sepolia` script
- `apps/web/api/og/fallback/route.ts` — deployable Vercel route for smoke step 5

Consumes from Plan 00-04:
- `scripts/fire-synthetic-alert.ts` — invoked from smoke step 6
- `.github/workflows/synthetic-alert.yml` — daily cron continues to run independently

Establishes (consumed by Phase 1+):
- `scripts/phase-0-smoke.ts` becomes the template for `scripts/phase-N-smoke.ts` in later phases
- `.github/workflows/phase-0-gate.yml` becomes the template for `phase-N-gate.yml`
- `docs/phase-0-deploy-checklist.md` becomes a referenced operator doc for the env-diff ritual (Phase 6) and the mainnet smoke test (Phase 7.5 §19.11)

External (out of plan scope but referenced):
- `google-github-actions/auth@v2` — GitHub OIDC → GCP service account federation; avoids static service-account key in GH Secrets
- `superfly/flyctl-actions@v1.4` (or `flyctl deploy` directly) — Fly.io deploy action
- `amondnet/vercel-action@v25` OR `vercel deploy --prebuilt` CLI — Vercel deploy
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vercel deploy workflow for apps/web with per-network project routing</name>
  <files>.github/workflows/deploy-web.yml</files>
  <read_first>
    - .planning/phases/00-foundation/00-CONTEXT.md D-04 (Vercel for frontend) + D-12 (default Vercel domains in Phase 0)
    - .planning/phases/00-foundation/00-RESEARCH.md Pitfall E (lines 1022–1029) — Vercel env-var Preview→Production drift; OG Fallback uses ZERO NEXT_PUBLIC_* so this route is immune
    - .planning/phases/00-foundation/00-RESEARCH.md §System Architecture Diagram (Vercel block, lines 277–289)
  </read_first>
  <action>Write `.github/workflows/deploy-web.yml`:
  - Triggers: `push` to branches `sepolia` (deploys to call-it-web-sepolia project) and `main` (deploys to call-it-web-mainnet project); plus `workflow_dispatch` with branch input.
  - One job `deploy` per branch:
    1. `actions/checkout@v4`
    2. `pnpm/action-setup@v4` (pnpm 9)
    3. `actions/setup-node@v4` (node 22, cache pnpm)
    4. `pnpm install --frozen-lockfile`
    5. `pnpm turbo run build --filter=@call-it/web` (Turborepo cache + per-affected build)
    6. `vercel deploy --prebuilt --token=$VERCEL_TOKEN --org=$VERCEL_ORG_ID --project=$VERCEL_PROJECT_ID --yes` (use `--prebuilt` to skip Vercel's own build since Turborepo already did it; pass `apps/web/.next` as the prebuilt artifact dir)
    7. Output: capture the deploy URL via `vercel ls --token=$VERCEL_TOKEN --json | jq -r '.[0].url'` and write to `$GITHUB_OUTPUT` (used by phase-0-gate.yml)
  - `env`: `VERCEL_TOKEN`, `VERCEL_ORG_ID` from GH Secrets; `VERCEL_PROJECT_ID` selected per branch via a `if` block (sepolia → `VERCEL_PROJECT_ID_SEPOLIA`; main → `VERCEL_PROJECT_ID_MAINNET`)
  - `permissions: contents: read`
  - Top comment: "Phase 0: deploys apps/web (OG Fallback route only). Phase 1+ adds /, /signin, /onboarding, /new, /profile, /call/[id], /duel/[id], /leaderboard routes. Per D-12 the Vercel default domain (e.g., call-it-web-sepolia.vercel.app) is used until Phase 7's domain cutover."</action>
  <verify>
    <automated>cat .github/workflows/deploy-web.yml | grep -E "on:|push:|sepolia|main" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/deploy-web.yml` exists with `push:` trigger on branches `sepolia` and `main`
    - Workflow uses `--prebuilt` Vercel flag (skips Vercel's own build; relies on Turborepo)
    - Workflow captures deploy URL output to `$GITHUB_OUTPUT` (used by smoke test in Task 4)
    - `permissions: contents: read` declared
    - YAML is valid (parseable via `yq`)
  </acceptance_criteria>
  <done>deploy-web.yml deploys apps/web to the correct Vercel project per branch (sepolia vs main) with Turborepo prebuilt output and per-network VERCEL_PROJECT_ID secret routing.</done>
</task>

<task type="auto">
  <name>Task 2: Fly.io deploy workflow for apps/relayer with GCP Secret Manager → flyctl secret injection</name>
  <files>.github/workflows/deploy-relayer.yml</files>
  <read_first>
    - .planning/phases/00-foundation/00-CONTEXT.md D-01 (Fly.io), D-02 (iad), D-08 (GCP Secret Manager for non-signing secrets), D-09 (separate GCP projects per network)
    - .planning/phases/00-foundation/00-RESEARCH.md §Pattern 4 (Fly.io always-on machine sizing, lines 633–679)
    - apps/relayer/Dockerfile + apps/relayer/fly.toml (Plan 00-02)
    - Google Cloud OIDC federation docs: https://github.com/google-github-actions/auth
  </read_first>
  <action>Write `.github/workflows/deploy-relayer.yml`:
  - Triggers: `push` to branches `sepolia` (deploys to call-it-relayer-sepolia) and `main` (call-it-relayer-mainnet); plus `workflow_dispatch`.
  - Job `deploy`:
    1. `actions/checkout@v4`
    2. Select GCP project ID based on branch: `sepolia` → `call-it-sepolia`; `main` → `call-it-mainnet` (env-var via `if` step)
    3. `google-github-actions/auth@v2` with `workload_identity_provider` from secrets (OIDC federation per V4 ASVS — no static service-account JSON keys in GH; service account binds to repo + branch via WIF condition)
    4. `google-github-actions/setup-gcloud@v2`
    5. Fetch each non-signing secret from GCP Secret Manager via `gcloud secrets versions access latest --secret=<name> --project=$GCP_PROJECT_ID`; store each in step output (NO direct echo to console — use `>> $GITHUB_OUTPUT` with `::add-mask::`). Secrets: `PRIVY_APP_SECRET`, `ALCHEMY_API_KEY`, `RPC_URL_<NETWORK>`, `PINATA_JWT`, `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID_P0`, `TELEGRAM_CHAT_ID_P1`, `BETTERSTACK_SOURCE_TOKEN`, `UPSTASH_REDIS_REST_URL`, `UPSTASH_REDIS_REST_TOKEN`, `RELAYER_INTERNAL_HMAC`. Also: `GCP_KEY_VERSION_NFT_TWAP`, `GCP_KEY_VERSION_DEFILLAMA`, `GCP_KEY_VERSION_CEX`, `GCP_KEY_VERSION_SNAPSHOT_TALLY`, `GCP_KEY_VERSION_OAUTH_PROOF` (key VERSION strings only — actual key MATERIAL stays in KMS per D-06).
    6. `superfly/flyctl-actions/setup-flyctl@master`
    7. `flyctl secrets set <NAME>=<VALUE> --app call-it-relayer-${{ env.NETWORK }}` for each fetched secret (Fly env injection without writing them to disk)
    8. `flyctl deploy --remote-only --app call-it-relayer-${{ env.NETWORK }} --config apps/relayer/fly.toml --dockerfile apps/relayer/Dockerfile --build-arg NETWORK=${{ env.NETWORK }}` (remote-only build so we don't need Docker locally on the runner)
    9. After deploy, run `flyctl status --app call-it-relayer-${{ env.NETWORK }}` and assert at least 1 machine is healthy (check the `Status: passing` in output via `grep`)
    10. Capture the relayer URL (`https://call-it-relayer-${{ env.NETWORK }}.fly.dev`) to `$GITHUB_OUTPUT` for the smoke test in Task 4
  - `env: FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}`
  - `permissions: id-token: write, contents: read` (id-token required for OIDC federation)
  - Top comment: "Phase 0: deploys apps/relayer skeleton. Per D-09, secrets come from GCP Secret Manager in the matching per-network GCP project (call-it-sepolia OR call-it-mainnet) — NOT from GH Secrets — so a compromised GH workflow cannot cross networks. Per Pattern 4, fly.toml runs in iad with auto_stop_machines=false."</action>
  <verify>
    <automated>cat .github/workflows/deploy-relayer.yml | grep -E "secrets versions access|flyctl secrets set|workload_identity_provider" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/deploy-relayer.yml` exists with branch-based GCP project routing (sepolia → call-it-sepolia; main → call-it-mainnet) per D-09
    - Workflow uses `google-github-actions/auth@v2` with `workload_identity_provider` — NO static service-account JSON key in GH Secrets (`grep -c "credentials_json" .github/workflows/deploy-relayer.yml` returns 0)
    - Workflow fetches ≥10 non-signing secrets from GCP Secret Manager via `gcloud secrets versions access` (verifiable via `grep -c "secrets versions access" .github/workflows/deploy-relayer.yml` ≥ 10)
    - Workflow uses `::add-mask::` for fetched secrets (no plaintext echoed to job logs)
    - Workflow deploys via `flyctl deploy --remote-only` (no docker on runner)
    - Workflow asserts `Status: passing` in flyctl status output
    - `permissions: id-token: write` declared (required for OIDC)
    - Every `gcloud secrets versions access` invocation uses `--project=$GCP_PROJECT_ID` (branch-conditional env declared at job top), NEVER a hardcoded literal. Two grep assertions must hold:
      1. `grep -c "gcloud secrets versions access" .github/workflows/deploy-relayer.yml` MUST equal `grep -Ec "gcloud secrets versions access[^|]*(--project=\$GCP_PROJECT_ID|--project=\$\{\{ env.GCP_PROJECT_ID)" .github/workflows/deploy-relayer.yml` (every fetch uses dynamic project routing — no hardcoded project literals anywhere in the workflow)
      2. `grep -cE "--project=call-it-(sepolia|mainnet)" .github/workflows/deploy-relayer.yml` returns 0 EXCEPT inside the single branch-conditional `env:` declaration block that sets `GCP_PROJECT_ID` (verified by inspecting context — assertion is "no hardcoded `--project=call-it-sepolia` or `--project=call-it-mainnet` on any `gcloud` command line")
  </acceptance_criteria>
  <done>deploy-relayer.yml deploys to the correct Fly app per branch with secrets injected from per-network GCP Secret Manager via OIDC federation (no long-lived GH service-account keys); every `gcloud secrets versions access` call uses dynamic `--project=$GCP_PROJECT_ID` routing (no hardcoded project literal); flyctl status assertion confirms machine health post-deploy.</done>
</task>

<task type="auto">
  <name>Task 3: Subgraph Studio deploy workflow + automatic version bumping</name>
  <files>.github/workflows/deploy-subgraph.yml</files>
  <read_first>
    - .planning/phases/00-foundation/00-RESEARCH.md §Pattern 7 (subgraph schema scaffold, lines 830–868)
    - .planning/phases/00-foundation/00-CONTEXT.md (subgraph deploys to Studio for Sepolia in Phase 0; Decentralized Network promotion in Phase 7)
    - packages/subgraph/package.json from Plan 00-03 (the `deploy:sepolia` script)
  </read_first>
  <action>Write `.github/workflows/deploy-subgraph.yml`:
  - Triggers: `push` to branches `sepolia` AND `main`, but ONLY when `paths: ['packages/subgraph/**', '.github/workflows/deploy-subgraph.yml']` changed (Phase 0 deploys to Studio for sepolia regardless of branch since Decentralized Network promotion is Phase 7); plus `workflow_dispatch`.
  - Job `deploy`:
    1. `actions/checkout@v4`
    2. `pnpm/action-setup@v4` + `actions/setup-node@v4` (node 22)
    3. `pnpm install --frozen-lockfile`
    4. `pnpm --filter @call-it/subgraph codegen` (generates AssemblyScript types from schema)
    5. `pnpm --filter @call-it/subgraph build` (`graph build`)
    6. Compute version label: `0.0.${{ github.run_number }}-${{ github.sha:0:7 }}` — monotonically increasing per workflow run, with git SHA suffix for traceability
    7. Authenticate to Studio: `pnpm --filter @call-it/subgraph exec graph auth --studio "${{ secrets.SUBGRAPH_STUDIO_DEPLOY_KEY }}"`
    8. Deploy: `pnpm --filter @call-it/subgraph exec graph deploy --studio call-it-sepolia --version-label "$VERSION_LABEL"`
    9. Capture the deployed query URL (Studio prints it to stdout) and write to `$GITHUB_OUTPUT`
  - `env: SUBGRAPH_STUDIO_DEPLOY_KEY: ${{ secrets.SUBGRAPH_STUDIO_DEPLOY_KEY }}` (per-network deploy key; Studio token for `call-it-sepolia` is different from `call-it-mainnet`)
  - `permissions: contents: read`
  - Top comment: "Phase 0 deploys to Studio for arbitrum-sepolia. Phase 7 promotes to The Graph Decentralized Network on Arbitrum (~3,000 GRT curation; ~$100-300 cost per SUMMARY.md operational budget). Per OPS-01, the subgraph is the primary indexed event source from day 1."</action>
  <verify>
    <automated>cat .github/workflows/deploy-subgraph.yml | grep -E "graph build|graph deploy|SUBGRAPH_STUDIO_DEPLOY_KEY" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/deploy-subgraph.yml` exists with paths filter `packages/subgraph/**`
    - Workflow runs `graph codegen` → `graph build` → `graph auth --studio` → `graph deploy --studio` in order
    - Workflow uses `SUBGRAPH_STUDIO_DEPLOY_KEY` from GH Secrets
    - Workflow computes a versioned label from `github.run_number` + short SHA (traceability)
    - Top comment notes Phase 7 Decentralized Network promotion path
  </acceptance_criteria>
  <done>deploy-subgraph.yml runs the full Studio deploy pipeline with versioned labels traceable to git SHA; ready for `git push` to trigger; per-network DEPLOY_KEY in GH Secrets.</done>
</task>

<task type="auto">
  <name>Task 4: Phase 0 smoke-test script + gate workflow + unit tests</name>
  <files>scripts/phase-0-smoke.ts, .github/workflows/phase-0-gate.yml, scripts/test/phase-0-smoke.test.ts, package.json</files>
  <read_first>
    - .planning/ROADMAP.md Phase 0 success criteria 1–6 (this is the source-of-truth for smoke steps)
    - .planning/phases/00-foundation/00-VALIDATION.md §Per-Task Verification Map (the test commands per req ID)
    - scripts/fire-synthetic-alert.ts (Plan 00-04) — invoked as subprocess in step 6
    - .planning/phases/00-foundation/00-RESEARCH.md §Sampling Rate (lines 1234–1238) — phase gate criteria
  </read_first>
  <action>Write `scripts/phase-0-smoke.ts`:
  - CLI flags: `--network sepolia|arbitrum-one`, `--web-url <url>` (Vercel deploy URL captured by deploy-web), `--relayer-url <url>` (Fly URL from deploy-relayer), `--subgraph-url <url>` (Studio URL from deploy-subgraph), `--require-synthetic-alert` (default true).
  - Steps (each exits 1 on failure with a structured error message; runs all 6 even if earlier ones fail, then exits with the combined status):
  
    **Step 1 — Build green:** `pnpm install --frozen-lockfile && pnpm turbo run lint test build` from repo root; non-zero exit → step 1 fails.
    
    **Step 2 — Grep guards pass:** Run the 3 grep guards from `.github/workflows/grep-guards.yml` locally (extract the rg commands and run them); any match → step 2 fails (asserts current code is still guard-compliant).
    
    **Step 3 — Relayer /health green:** `fetch('${relayerUrl}/health')` → assert 200 + JSON `{ status: 'ok' }` + response time < 2000ms; non-200 → step 3 fails.
    
    **Step 4 — Subgraph deployed:** `fetch('${subgraphUrl}', { method: 'POST', headers: {'Content-Type': 'application/json'}, body: JSON.stringify({ query: '{ _meta { block { number } } }' }) })` → assert response has shape `{ data: { _meta: { block: { number: <int> } } } }`; if `_meta` is missing or `number === null` → step 4 fails (subgraph not indexed).
    
    **Step 5 — OG Fallback p95 < 100ms warm:** 100 sequential GETs to `${webUrl}/api/og/fallback?handle=smoketest` (after 1 warmup); record timings; assert p95 < 100ms AND every response has `Content-Type: image/png` + header `X-Variant: fallback` + body length > 1000 bytes (sanity that it's a real PNG, not an error response).
    
    **Step 6 — Synthetic alert end-to-end (gated by `--require-synthetic-alert`):** Invoke `pnpm tsx scripts/fire-synthetic-alert.ts --event rep_fallback --wait-seconds 60 --expect-chat-id $TELEGRAM_CHAT_ID_P0` as a subprocess; non-zero exit → step 6 fails. (Reuses Plan 00-04 Task 2 work.)
    
  - At the end, print a JSON summary `{ step1: 'pass'|'fail', step2: ..., step3: ..., step4: ..., step5: { p50, p95, p99 }, step6: ..., overall: 'pass'|'fail' }` to stdout; write to `scripts/phase-0-smoke-results-<timestamp>.json` for the SUMMARY artifact.
  - Exit code: 0 if all 6 pass; 1 if any fail.
  
  Write `.github/workflows/phase-0-gate.yml`:
  - Triggers: `push: tags: ['phase-0-complete*']` (tagged release gate); `workflow_dispatch` (manual fire).
  - Job `gate`:
    1. checkout
    2. pnpm + node setup
    3. pnpm install --frozen-lockfile
    4. Reuses deploy workflows' outputs (or accepts inputs from workflow_dispatch for `web-url`, `relayer-url`, `subgraph-url`)
    5. `pnpm tsx scripts/phase-0-smoke.ts --network sepolia --web-url $WEB_URL --relayer-url $RELAYER_URL --subgraph-url $SUBGRAPH_URL --require-synthetic-alert`
    6. On success: post a GitHub release for the tag with the smoke-results JSON as an asset
  - `env`: `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID_P0`, `RELAYER_INTERNAL_HMAC` (from GH Secrets — these are the SAME secrets used by deploy-relayer.yml, sourced from GCP Secret Manager at this workflow's runtime via the same OIDC pattern as Task 2)
  - `permissions: contents: write, id-token: write` (write for release creation; id-token for GCP OIDC)
  
  Write `scripts/test/phase-0-smoke.test.ts` with unit-level tests (mocked HTTP for steps 3, 4, 5 + mocked subprocess for step 6): assert that step 5's percentile computation is correct (vitest supplies a controlled timing array, asserts p95 calculation against known-good math); assert that any single step failure causes overall exit 1; assert that all 6 steps run even when step 1 fails (so the operator gets the full diagnostic picture).
  
  Update root `package.json` to add `"smoke:phase-0": "tsx scripts/phase-0-smoke.ts"` script.</action>
  <verify>
    <automated>pnpm vitest run scripts/test/phase-0-smoke.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run scripts/test/phase-0-smoke.test.ts` reports ≥3 passing tests (percentile math, all-steps-run-even-on-fail, exit-code-on-any-fail), 0 failures
    - `scripts/phase-0-smoke.ts` includes all 6 step functions; each step has its own try-catch + named error
    - `scripts/phase-0-smoke.ts` writes results JSON to `scripts/phase-0-smoke-results-<timestamp>.json`
    - `.github/workflows/phase-0-gate.yml` triggers on tags matching `phase-0-complete*`
    - `.github/workflows/phase-0-gate.yml` has `permissions: contents: write` (for release creation) AND `id-token: write` (for OIDC)
    - `grep -c "scripts/fire-synthetic-alert.ts" scripts/phase-0-smoke.ts` returns ≥ 1 (reuses Plan 00-04 helper)
    - `package.json` root scripts include `"smoke:phase-0"`
  </acceptance_criteria>
  <done>6-step smoke-test script verifies every Phase 0 success criterion end-to-end against deployed artifacts; phase-0-gate.yml fires on `git tag phase-0-complete` and blocks Phase 1 start until green; 3 unit tests on the smoke script's percentile math + failure-mode behavior pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Operator pre-tag checklist verification (hosted-resource items)</name>
  <files>docs/phase-0-deploy-checklist.md</files>
  <read_first>
    - .planning/phases/00-foundation/00-VALIDATION.md §Manual-Only Verifications (lines 117–127) — the 6 items that cannot be automated
    - .planning/phases/00-foundation/00-RESEARCH.md Open Questions 1, 2, 5 (lines 1115–1140) — pre-tag verification points
    - .planning/phases/00-foundation/00-CONTEXT.md D-09, D-10, D-13, D-14, D-15, D-17 (hosted-resource decisions that require operator-side confirmation)
  </read_first>
  <what-built>
    By the end of Tasks 1–4, the Phase 0 substrate is in code + ready to deploy. Before tagging `phase-0-complete`, the operator must verify the 8 hosted-resource items per VALIDATION.md manual-only rows that no CLI automation can confirm.
  </what-built>
  <action>Write `docs/phase-0-deploy-checklist.md` (≥30 lines) documenting the 8 hosted-resource verification items per the `<how-to-verify>` block below. The operator (the user) walks each item, ticks each checkbox in the markdown file, and signs at the bottom with date + commit SHA. This task is a `checkpoint:human-verify` gate — Claude does NOT auto-approve; the operator must explicitly type "approved" after walking the checklist.</action>
  <how-to-verify>
    Write `docs/phase-0-deploy-checklist.md` documenting the 8 items below; then walk the checklist:

    1. **GCP projects + 5 KMS keys per project (D-09, D-06, D-07):**
       - Run `gcloud kms keys list --keyring=attestations --location=us-east1 --project=call-it-sepolia` — returns 5 keys: `nft-twap`, `defillama`, `cex`, `snapshot-tally`, `oauth-proof`
       - Repeat for `--project=call-it-mainnet` — same 5 keys exist
       - Per key: `gcloud kms keys versions list --key=<key> --keyring=attestations --location=us-east1 --project=call-it-sepolia` — at least one ENABLED version exists with `algorithm: EC_SIGN_SECP256K1_SHA256` and `protectionLevel: SOFTWARE` (Pitfall B)
       - Verify the service account `relayer-runtime@call-it-sepolia.iam.gserviceaccount.com` has roles `roles/cloudkms.signer` + `roles/secretmanager.secretAccessor` on the project; verify mainnet equivalent
       - Document in checklist file: 10 confirmation checkboxes (5 keys × 2 projects)

    2. **Telegram bot has correct permissions (Open Question 5, Pitfall D):**
       - Visit https://api.telegram.org/bot$TELEGRAM_BOT_TOKEN/getMe → confirm bot exists
       - Visit https://api.telegram.org/bot$TELEGRAM_BOT_TOKEN/getUpdates?offset=-10 → confirm channel_post entries are returned for the P0 and P1 channels (proves bot has read permission on its own channel posts)
       - In @BotFather: confirm `/setprivacy` is set to "Disabled" so getUpdates works
       - In both Telegram channels: confirm bot is an admin with `can_post_messages` + `can_read_messages` permissions
       - Document in checklist file: 4 confirmation checkboxes

    3. **5 Better Stack dashboards exist + show synthetic data (OPS-06, D-17, Pitfall G):**
       - Log in to Better Stack → Dashboards → verify 5 dashboards by name: Total TVL, Calls/Hour, Settlement Latency, Dispute Rate, Failed Tx Rate
       - For each dashboard, confirm at least 1 synthetic data point is visible (proves the seed cron from Plan 00-04 reached Better Stack)
       - Confirm dashboard sharing is set to "Private" — accessible to operator + co-signers only (D-17)
       - Document in checklist file: 11 confirmation checkboxes (5 dashboards × visible-data + 5 × private-access + 1 × "synthetic" filter toggle works)

    4. **Safe 2-of-3 deployment is real on Arbiscan Sepolia (SAFETY-58):**
       - Visit `https://sepolia.arbiscan.io/address/${SAFE_ADDRESS}` → confirm contract exists
       - Click "Read Contract" → `getOwners()` returns the 3 expected signer addresses
       - `getThreshold()` returns 2
       - The Safe address matches the value committed in `packages/contracts/deployments/safe-sepolia.json` (Plan 00-04 Task 1 output)
       - Document in checklist file: 4 confirmation checkboxes

    5. **Stylus reactivation calendar invites are seeded (D-13, Pitfall C):**
       - Open Google Calendar in browser → confirm 4 events exist at T-30/15/7/1 before the placeholder deploy date set by `seed-calendar.ts`
       - Each event description links `docs/runbooks/stylus-reactivation.md`
       - The 4 event IDs match the values in `packages/shared/src/constants/stylus-calendar.json`
       - Document in checklist file: 3 confirmation checkboxes

    6. **GCP secret structure matches the env-config schema (D-08, Pitfall 5):**
       - Run `gcloud secrets list --project=call-it-sepolia` → returns all ~12 expected secrets per the list in Plan 00-02 Task 1
       - Run same for `call-it-mainnet`
       - Manually verify that `RPC_URL_ARBITRUM_MAINNET` in `call-it-mainnet` project does NOT contain the substring `sepolia` (Pitfall 5 IAM-layered defense)
       - Document in checklist file: 5 confirmation checkboxes


    7. **Pinata account provisioned + smoke-test pin (D-20):**
       - Operator creates a Pinata account (free tier sufficient for Phase 0 smoke pin); generates a JWT API token; stores in GCP Secret Manager as `PINATA_JWT` in BOTH `call-it-sepolia` and `call-it-mainnet` projects
       - Operator runs a one-time smoke-test pin: `curl -X POST -H "Authorization: Bearer $PINATA_JWT" -F "file=@README.md" https://api.pinata.cloud/pinning/pinFileToIPFS` returns 200 with a CID
       - Per D-20: NO application-level pinning lands in Phase 0 — receipt-hash pinning + asset pinning land in Phase 7 when receipts go live. Phase 0's deliverable is JUST the account + smoke pin.
       - Document in checklist file: 3 confirmation checkboxes

    8. **Default Fly + Vercel domains accepted; no real domain (D-05):**
       - Confirm the operator has NOT registered or attempted to register the eventual production domain (e.g., callitapp.xyz) — D-05 explicitly defers the real domain to Phase 7
       - Confirm every URL used by Phase 0–6 code is constructed from env-vars (`NEXT_PUBLIC_OG_BASE_URL`, `RELAYER_URL`, `NEXT_PUBLIC_SUBGRAPH_URL`) — never a hardcoded literal; run `rg --hidden --no-ignore -t ts -t tsx -t yml "callitapp.xyz" --glob "!**/node_modules/**"` from repo root and assert ZERO matches
       - Phase 0 production Vercel deploys land at `call-it-web-sepolia.vercel.app` and `call-it-web-mainnet.vercel.app`; relayer at `call-it-relayer-sepolia.fly.dev` and `call-it-relayer-mainnet.fly.dev` (D-05)
       - Document in checklist file: 3 confirmation checkboxes

    The operator walks all 8 items in `docs/phase-0-deploy-checklist.md`, ticks each checkbox, signs off at the bottom with date + commit SHA. Only then can `git tag phase-0-complete && git push --tags` fire the phase-0-gate.yml workflow.
  </how-to-verify>
  <verify>
    <automated>test -f docs/phase-0-deploy-checklist.md && [ $(wc -l < docs/phase-0-deploy-checklist.md) -ge 30 ] && grep -E "Signed:|Approved:|Operator:" docs/phase-0-deploy-checklist.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/phase-0-deploy-checklist.md` exists with ≥30 lines
    - All 8 verification items are documented as checklist sections with the concrete CLI commands / URLs to verify
    - At the bottom of the file: operator signature block (`Signed: <name> · <date> · commit <sha>`) populated by the operator before approving
    - Operator types "approved" to resume the orchestrator (manual gate)
  </acceptance_criteria>
  <done>Operator has walked all 8 hosted-resource verification items, the checklist file is fully checked-and-signed at the bottom, and the operator has typed "approved" to resume the execute-phase orchestrator. Phase 0 is then clear to tag.</done>
  <resume-signal>Type "approved" after all 8 checklist items are confirmed AND the checklist file is signed at the bottom; OR describe specific items that failed (e.g., "Item 1: only 4 keys in call-it-mainnet — missing oauth-proof").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GH Actions runner → Vercel deploy API | VERCEL_TOKEN scoped to deployment-only (V4) |
| GH Actions runner → Fly.io deploy API | FLY_API_TOKEN scoped to deploy (V4) |
| GH Actions runner → Subgraph Studio | SUBGRAPH_STUDIO_DEPLOY_KEY per-network (V4) |
| GH Actions runner → GCP services | OIDC federation (id-token); NO long-lived service-account JSON keys (V6) |
| `git tag phase-0-complete` → gate workflow | Tag is a public signal; gate ensures it's not a false positive |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-35 | Spoofing / Elevation | GitHub Actions service-account credentials | mitigate | OIDC federation via `google-github-actions/auth@v2` with WIF condition binding to (repo, branch); no static `credentials_json` in GH Secrets (verifiable via grep guard in CI — `grep -c "credentials_json" .github/workflows/*.yml` should be 0). Severity: HIGH if a long-lived key existed — short-lived OIDC tokens cap blast radius to a single workflow run. |
| T-00-36 | Tampering | Vercel / Fly deploy artifacts | mitigate | Per-branch deploy targets (sepolia vs main) prevent cross-network drift; per-network Vercel project IDs + Fly app IDs ensure a sepolia push cannot land on mainnet (Pitfall 5 D-09 layered defense). Severity: CRITICAL — sepolia artifact on mainnet = wrong USDC + wrong chainId at runtime. |
| T-00-37 | Spoofing | `phase-0-complete` tag pushed by attacker | mitigate | Tag triggers gate workflow which calls phase-0-smoke.ts; smoke requires the 6 step infrastructure to be live + Telegram round-trip + relayer /health — attacker cannot fake all 6 unless they've already compromised every component, in which case the tag is the least of the operator's worries. Severity: LOW — gate workflow IS the defense. |
| T-00-38 | Information Disclosure | GCP secret values leaked to GH Actions logs | mitigate | `::add-mask::` directive applied to every fetched secret per Task 2; GH Actions automatically redacts known secrets in logs; relayer's Pino redaction config (Plan 00-02 T-00-11) is a second line. Severity: HIGH if missed — but `::add-mask::` is reliable when applied correctly. |
| T-00-39 | Tampering | Subgraph Studio deploy substitution | mitigate | Per-network SUBGRAPH_STUDIO_DEPLOY_KEY (sepolia vs mainnet); version labels include git SHA so a corrupted deploy is traceable to a specific commit. Severity: MEDIUM. |
| T-00-40 | Repudiation | "Phase 0 is done" claim without proof | mitigate | phase-0-gate.yml emits a GitHub release with the smoke-results JSON attached; permanent audit trail on the repo's Releases page. Severity: LOW. |
| T-00-41 | Denial of Service | Fly deploy failure mid-rollout leaves no relayer running | mitigate | `flyctl deploy --remote-only` uses rolling-deploy by default; min_machines_running=1 in fly.toml means at least one healthy instance stays up; failed deploy → old machine continues serving while new machine fails health-check and is rolled back. Severity: LOW. |
| T-00-42 | Tampering | Operator skipping checkpoint Task 5 | mitigate | Task 5 is `checkpoint:human-verify` with `gate="blocking"` — execute-phase orchestrator must pause and wait for "approved" signal; cannot be silently auto-approved by Claude. Per-item documentation in `docs/phase-0-deploy-checklist.md` provides audit trail. Severity: MEDIUM — operator could theoretically lie, but the checklist requires concrete CLI outputs (gcloud commands), making forgery effortful. |

**No `high` severity threats remain unmitigated.** T-00-35 (GH service-account key) is closed by mandating OIDC. T-00-36 (cross-network deploy) is closed by per-network project IDs. T-00-38 (secret leak in logs) is closed by `::add-mask::` + Pino redaction defense-in-depth.
</threat_model>

<verification>
After all tasks complete (including Task 5 operator sign-off):
1. `pnpm vitest run scripts/test/phase-0-smoke.test.ts` reports ≥3 passing tests
2. Manual: Operator runs `gh workflow run deploy-relayer.yml --ref sepolia` and watches Fly relayer URL respond 200 on `/health`
3. Manual: Operator runs `gh workflow run deploy-web.yml --ref sepolia` and watches Vercel deploy URL serve `/api/og/fallback?handle=smoketest` as a 1200×630 PNG
4. Manual: Operator runs `gh workflow run deploy-subgraph.yml` and confirms Studio shows the new subgraph version
5. Manual: Operator completes all 6 items of `docs/phase-0-deploy-checklist.md`, signs at the bottom, and pushes tag `phase-0-complete-v0.0.1`
6. The phase-0-gate.yml workflow runs and exits 0 (green) — proves all 6 Phase 0 success criteria are mechanically verified
</verification>

<success_criteria>
- All 8 files in `<files_modified>` exist and pass lint
- 3 deploy workflows (web, relayer, subgraph) routed per branch with correct per-network secret/project routing
- phase-0-gate.yml fires on tags matching `phase-0-complete*` and runs the 6-step smoke
- scripts/phase-0-smoke.ts ≥3 unit tests green (percentile math, all-steps-run, exit-code)
- docs/phase-0-deploy-checklist.md documents 8 hosted-resource verification items with concrete CLI/dashboard steps
- Operator signs the checklist after walking all 6 items; gate workflow runs green after sign-off
- No long-lived service-account JSON keys in GH Secrets (OIDC federation only per V6)
</success_criteria>

<output>
After completion AND checkpoint sign-off, create `.planning/phases/00-foundation/00-05-SUMMARY.md` per template. Record:
- Vercel deploy URL (sepolia + mainnet)
- Fly relayer URL (sepolia + mainnet)
- Subgraph Studio query URL (call-it-sepolia)
- Safe Sepolia address (from Plan 00-04 Task 1)
- The phase-0-gate.yml first-green-run GitHub URL (audit trail)
- Smoke-results JSON snapshot (step 5 p50/p95/p99 OG render timings; step 6 Telegram round-trip latency)
- Operator sign-off signature from docs/phase-0-deploy-checklist.md
- ROADMAP.md Phase 0 checkbox can now flip to `[x]` — Phase 1 is unblocked
</output>
