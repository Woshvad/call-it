---
phase: 00-foundation
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - packages/subgraph/schema.graphql
  - packages/subgraph/subgraph.yaml
  - packages/subgraph/networks.json
  - packages/subgraph/package.json
  - packages/subgraph/src/call-registry.ts
  - packages/subgraph/src/follow-fade-market.ts
  - packages/subgraph/src/challenge-escrow.ts
  - packages/subgraph/src/settlement-manager.ts
  - packages/subgraph/src/profile-registry.ts
  - packages/subgraph/abis/CallRegistry.json
  - packages/subgraph/abis/FollowFadeMarket.json
  - packages/subgraph/abis/ChallengeEscrow.json
  - packages/subgraph/abis/SettlementManager.json
  - packages/subgraph/abis/ProfileRegistry.json
  - packages/subgraph/tests/schema.test.ts
  - packages/subgraph/tests/matchstick.yaml
  - apps/relayer/src/workers/polled-events-fallback.ts
  - apps/relayer/test/polled-events.test.ts
  - apps/web/app/fonts/Syne-Bold.ttf
  - apps/web/app/fonts/SpaceGrotesk-Regular.ttf
  - apps/web/app/fonts/JetBrainsMono-Regular.ttf
  - apps/web/app/api/og/fallback/route.ts
  - apps/web/app/api/og/[callId]/route.ts
  - apps/web/lib/og-fonts.ts
  - apps/web/playwright.config.ts
  - apps/web/tests/og-fallback.spec.ts
  - apps/web/tests/og-fallback-routing.spec.ts
  - apps/web/tests/og-fallback-bench.spec.ts
  - apps/web/package.json
autonomous: true
requirements:
  - OPS-01
  - OPS-02
  - OPS-03
  - SHARE-09
  - SHARE-10
  - SHARE-11
tags:
  - subgraph
  - og-image
  - fallback
  - polled-events
  - share
user_setup:
  - service: subgraph-studio
    why: "Per OPS-01 — primary indexed event source from day 1 (Hosted Service was sunset June 12, 2024; only Studio is available)"
    env_vars:
      - name: SUBGRAPH_STUDIO_DEPLOY_KEY
        source: "Subgraph Studio → My Subgraphs → call-it-sepolia → Deploy Key"
    dashboard_config:
      - task: "Create Subgraph Studio account; create a new subgraph named 'call-it-sepolia' on Arbitrum Sepolia"
        location: "https://thegraph.com/studio/ → Create a Subgraph"

must_haves:
  truths:
    - "Operator can run `pnpm --filter @call-it/subgraph build && pnpm --filter @call-it/subgraph deploy:sepolia --dry-run` and the deploy succeeds (or returns a clear error from Studio) — OPS-01."
    - "Subgraph schema includes all 23 entity types derived from CALL_IT_SPEC1.md §12.1–12.5 — OPS-03."
    - "The polled-events fallback worker runs against a local Anvil instance, polls a known contract via `viem.getLogs` at a 5-second interval, and the unit test asserts at least one polling cycle completes without crash — OPS-02."
    - "The Fallback OG card route at `/api/og/fallback?handle=...` renders a 1200×630 PNG with the §16.6 layout (3px accent border, 4 corner brackets, CALL IT wordmark Syne 48px, A CALL WAS MADE Syne 64px, by @handle SpaceGrotesk 28px) — SHARE-09."
    - "When the catch-all OG handler at `/api/og/[callId]` cannot find the call (404 from subgraph) OR the cache-warm verification fails, it returns the Fallback variant by routing to `/api/og/fallback` — SHARE-10."
    - "The Fallback route's p95 render time over 100 warm sequential requests is below 100ms (measured via Playwright timing) — SHARE-11."
    - "The Fallback route uses ZERO `NEXT_PUBLIC_*` env vars and ZERO on-chain reads, ZERO subgraph queries — only the `?handle=...` query string is consumed (Pitfall E mitigation)."
    - "The OG card source files contain only `display: flex` — `display: grid` is rejected by the `no-display-grid` ESLint rule scoped to `apps/web/app/api/og/**` (Pitfall 15 prep)."
  artifacts:
    - path: packages/subgraph/schema.graphql
      provides: "23-entity GraphQL schema covering Call, Position, Challenge, Settlement, Profile, RepEvent, Dispute, etc., derived from §12.1–12.5"
      min_lines: 80
    - path: packages/subgraph/subgraph.yaml
      provides: "Manifest declaring 5 data sources (CallRegistry, FollowFadeMarket, ChallengeEscrow, SettlementManager, ProfileRegistry) on arbitrum-sepolia with empty addresses (Phase 1+ populates)"
      contains: "network: arbitrum-sepolia"
    - path: apps/web/app/api/og/fallback/route.ts
      provides: "SHARE-09 Fallback OG card render via @vercel/og + Satori on Node runtime"
      exports: ["GET", "runtime"]
      contains: "runtime = 'nodejs'"
    - path: apps/web/app/api/og/[callId]/route.ts
      provides: "SHARE-10 catch-all route that falls through to /api/og/fallback on 404 / cache-warm-failure"
      exports: ["GET"]
    - path: apps/relayer/src/workers/polled-events-fallback.ts
      provides: "viem.getLogs polling stub at 5s interval against a configurable contract address (OPS-02)"
      exports: ["startPolledEventsFallback", "stopPolledEventsFallback"]
    - path: apps/web/lib/og-fonts.ts
      provides: "Shared loader for Syne/Space Grotesk/JetBrains Mono TTFs from app/fonts/ at module init (Pitfall F mitigation — fonts loaded at module level, NOT inside the GET handler)"
      exports: ["syneBold", "spaceGrotesk", "jetBrainsMono"]
  key_links:
    - from: apps/web/app/api/og/fallback/route.ts
      to: apps/web/lib/og-fonts.ts
      via: "Import TTF Uint8Arrays from app/fonts at module load time (Pitfall F: fonts must be in app/ NOT public/ for Vercel bundler to include them)"
      pattern: "import .* from '../../../lib/og-fonts'"
    - from: apps/web/app/api/og/[callId]/route.ts
      to: apps/web/app/api/og/fallback/route.ts
      via: "On 404 from real receipt lookup, return NextResponse.redirect or render the same fallback ImageResponse"
      pattern: "fallback|FallbackVariant"
    - from: packages/subgraph/src/call-registry.ts
      to: packages/subgraph/schema.graphql
      via: "AssemblyScript event handlers create/update Call entities defined in schema"
      pattern: "Call\\.load|new Call\\("
    - from: apps/relayer/src/workers/polled-events-fallback.ts
      to: "viem getLogs"
      via: "Polls every 5 seconds from last block to head; emits each log to a callback for downstream processing"
      pattern: "getLogs\\(|setInterval\\(.*5000"
---

## Phase Goal

**As an** operator validating that the indexing + share-loop substrate is in place before Phase 1 deploys the first contract, **I want to** (a) see the subgraph deploy successfully to Subgraph Studio for Arbitrum Sepolia with a 23-entity schema, (b) curl the Fallback OG card and receive a 1200×630 PNG matching the §16.6 layout in under 100ms, and (c) observe the polled-events fallback worker tick every 5 seconds against a local Anvil contract, **so that** when Phase 1's `CallRegistry.createCall` fires its first event, the indexer is already standing by and any share URL that 404s gracefully renders the "A CALL WAS MADE" card instead of breaking the loop (Pitfalls 8 and 15 mitigated from day one).

<objective>
Scaffold the subgraph (23-entity schema covering §12.1–12.5 events) and deploy it to Subgraph Studio for Arbitrum Sepolia; build the viem-getLogs polled-events fallback worker in the relayer so Phase 1+ has a fallback path during the subgraph indexer-sync gap (OPS-02); and ship the SHARE-09 Fallback OG card route on Vercel via @vercel/og on Node runtime with all 3 acceptance criteria (correct layout, <100ms warm render, served when real URL 404s).

Purpose: Closes OPS-01 (subgraph as primary indexed source), OPS-02 (polled-events fallback), OPS-03 (23-entity schema), SHARE-09 (Fallback card layout), SHARE-10 (fallback served on 404), SHARE-11 (<100ms warm render). Mitigates Pitfall 8 prep (no cache-desync window because both indexer + fallback are live before any contract), Pitfall 15 prep (no-display-grid ESLint rule enforced on OG templates from day one), Pitfall F (font bundling on Vercel via app/fonts/).

Output: A subgraph that deploys to Studio dry-run successfully; a polled-events worker that ticks every 5s against local Anvil; a Vercel-deployable Next.js route that renders the Fallback card in <100ms warm with the exact §16.6 layout; Playwright tests gating all three SHARE-09/10/11 acceptance criteria.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-foundation/00-CONTEXT.md
@.planning/phases/00-foundation/00-RESEARCH.md
@.planning/phases/00-foundation/00-VALIDATION.md
@CLAUDE.md
@CALL_IT_SPEC1.md
@.planning/phases/00-foundation/00-01-SUMMARY.md
@.planning/phases/00-foundation/00-02-SUMMARY.md

<interfaces>
Consumes from Plan 00-01:
- `@call-it/shared` exports: `USDC_ARB_NATIVE`, `ARBITRUM_MAINNET_CHAIN_ID`, `ARBITRUM_SEPOLIA_CHAIN_ID`, `PYTH_ARBITRUM_ONE`, `PYTH_ARBITRUM_SEPOLIA`
- `@call-it/config/eslint/no-display-grid.js` — ESLint custom rule (Plan 00-01 wrote the rule; this plan ENFORCES it on `apps/web/app/api/og/**`)

Consumes from Plan 00-02:
- `apps/relayer/src/lib/logger.ts` → `logger` instance for the polled-events worker structured logging

Establishes (consumed by Phase 1+ and by Plan 00-04 synthetic-alert tooling):
- `packages/subgraph/schema.graphql` — 23 entities; Phase 1+ contracts emit events whose mappings populate these entities
- `packages/subgraph/subgraph.yaml` — manifest with 5 data sources; Phase 1+ updates each contract's `address` field as deployments happen
- `apps/relayer/src/workers/polled-events-fallback.ts` → `export function startPolledEventsFallback(opts: { rpcUrl, address, abi, fromBlock, intervalMs, onLog }): { stop(): void }` — Phase 1 wires this against the deployed `CallRegistry` once the contract address exists
- `apps/web/app/api/og/fallback/route.ts` — GET handler; URL is `${NEXT_PUBLIC_OG_BASE_URL}/api/og/fallback?handle=<handle>` per D-12 env-var construction
- `apps/web/app/api/og/[callId]/route.ts` — GET catch-all; in Phase 0 it routes EVERY callId to the Fallback render (no real call data); Phase 2+ wires the Live variant; Phase 4+ wires Settled / DuelSettled / CallerExited; Phase 7 finalizes all 5 variants

23 entity list (derived from CALL_IT_SPEC1.md §12.1–12.5, full list):
1. Call, 2. Position, 3. Challenge, 4. Settlement, 5. Profile, 6. RepEvent, 7. Dispute, 8. QuoteCall, 9. ConvictionCap (records ConvictionCapped events), 10. CallerExit (records CallerExited with broadcast metadata), 11. SocialLink (Twitter + Farcaster link/unlink history), 12. PayoutClaim, 13. PositionExit (4h-cooldown exits with 10% slash distribution), 14. ChallengePayout, 15. DisputeResolution, 16. ForceSettlement (records CallForceSettled), 17. SettlementDelayed, 18. RepCalculatedFallback (records the fallback event so the leaderboard can flag fallback-derived deltas), 19. UnclaimedOverage (asymmetric-stake refund tracking per Pitfall 21), 20. PaymasterEvent (sponsored-tx counter; mirrors relayer Redis counter), 21. TvlSnapshot (per-block aggregated TVL across all contracts for Pitfall 3 boundary tests), 22. CategoryRep (per-category rep aggregation for Profile cards), 23. LeaderboardEntry (7d / 30d / All-time precomputed for `/leaderboard`).

Each entity has at minimum `id: ID!`, plus timestamp + relationship fields per ARCHITECTURE.md §2.9 + RESEARCH.md lines 836–868.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Subgraph schema (23 entities) + manifest + AssemblyScript mapping stubs + schema unit test</name>
  <files>packages/subgraph/schema.graphql, packages/subgraph/subgraph.yaml, packages/subgraph/networks.json, packages/subgraph/package.json, packages/subgraph/src/call-registry.ts, packages/subgraph/src/follow-fade-market.ts, packages/subgraph/src/challenge-escrow.ts, packages/subgraph/src/settlement-manager.ts, packages/subgraph/src/profile-registry.ts, packages/subgraph/abis/CallRegistry.json, packages/subgraph/abis/FollowFadeMarket.json, packages/subgraph/abis/ChallengeEscrow.json, packages/subgraph/abis/SettlementManager.json, packages/subgraph/abis/ProfileRegistry.json, packages/subgraph/tests/schema.test.ts, packages/subgraph/tests/matchstick.yaml</files>
  <read_first>
    - .planning/phases/00-foundation/00-RESEARCH.md lines 830–868 (Pattern 7: subgraph schema scaffold + GraphQL excerpt)
    - .planning/phases/00-foundation/00-RESEARCH.md lines 408–435 (subgraph package layout)
    - CALL_IT_SPEC1.md §12.1–12.5 (full indexed-events catalogue — 23 entities derive from this)
    - .planning/REQUIREMENTS.md OPS-01, OPS-02, OPS-03 rows
    - .planning/phases/00-foundation/00-CONTEXT.md (subgraph deploys to Studio for Sepolia in Phase 0; Decentralized Network promotion in Phase 7)
  </read_first>
  <behavior>
    - Test 1 (`packages/subgraph/tests/schema.test.ts`, Vitest): A test parses `packages/subgraph/schema.graphql` (using `graphql` package's `parse()`) and asserts the schema declares all 23 entity type names listed in `<interfaces>` above. Failing entities are named in the assertion message.
    - Test 2 (`packages/subgraph/tests/schema.test.ts`): Each of the 23 entities has at minimum an `id: ID!` field; relationship fields (`@derivedFrom`, references to other entities) parse without GraphQL errors.
    - Test 3 (`packages/subgraph/tests/schema.test.ts`): The schema is The-Graph-compatible — every entity is annotated `@entity` (regex check on raw schema text); no GraphQL features outside The Graph's supported set (no interfaces beyond `@entity`, no unions).
  </behavior>
  <action>Write `packages/subgraph/schema.graphql` with all 23 entities per the list in `<interfaces>` above. Each entity gets `id: ID!` + relevant fields:
  - Call: `id, caller: Bytes!, marketType: Int!, eventSubtype: Int, asset: String!, stake: BigInt!, expiry: BigInt!, conviction: Int!, status: String!, outcome: String, reasoning: String, createdAt: BigInt!, settledAt: BigInt, positions: [Position!]! @derivedFrom(field: "call"), challenges: [Challenge!]! @derivedFrom(field: "call"), quotes: [QuoteCall!]! @derivedFrom(field: "parentCall"), disputes: [Dispute!]! @derivedFrom(field: "call")`
  - Position: `id, call: Call!, user: Bytes!, side: String!, stake: BigInt!, shares: BigInt!, entryTime: BigInt!, claimed: Boolean!, exitedAt: BigInt`
  - Challenge: `id, call: Call!, challenger: Bytes!, stake: BigInt!, status: String!, winner: Bytes, proposedAt: BigInt!, acceptedAt: BigInt, settledAt: BigInt`
  - Settlement: `id, call: Call!, outcome: String!, priceDelta: BigInt, settledAt: BigInt!, finalPrice: BigInt`
  - Profile: `id, globalRep: Int!, categoryReps: [CategoryRep!]! @derivedFrom(field: "profile"), settledCalls: Int!, wins: Int!, losses: Int!, twitterHandle: String, farcasterHandle: String, handle: String, tagline: String`
  - RepEvent: `id, user: Bytes!, delta: Int!, reason: String!, callId: String!, fallback: Boolean!, timestamp: BigInt!`
  - Dispute: `id, call: Call!, disputer: Bytes!, evidenceHash: Bytes!, bondAmount: BigInt!, status: String!, raisedAt: BigInt!, resolvedAt: BigInt`
  - QuoteCall: `id, parentCall: Call!, quoter: Bytes!, stance: String!, quotedAt: BigInt!`
  - ConvictionCap: `id, user: Bytes!, attempted: Int!, capped: Int!, timestamp: BigInt!`
  - CallerExit: `id, call: Call!, exitedAt: BigInt!, penaltyApplied: BigInt!, callerVolumeAtExit: BigInt!`
  - SocialLink: `id, user: Bytes!, platform: String!, handle: String!, proofHash: Bytes!, linkedAt: BigInt!, unlinkedAt: BigInt`
  - PayoutClaim: `id, call: Call!, claimer: Bytes!, amount: BigInt!, claimedAt: BigInt!`
  - PositionExit: `id, position: Position!, exitedAt: BigInt!, slashAmount: BigInt!, returnedAmount: BigInt!`
  - ChallengePayout: `id, challenge: Challenge!, winner: Bytes!, amount: BigInt!, paidAt: BigInt!`
  - DisputeResolution: `id, dispute: Dispute!, finalOutcome: String!, resolverNote: String, resolvedAt: BigInt!`
  - ForceSettlement: `id, call: Call!, forcedBy: Bytes!, forcedAt: BigInt!, finalOutcome: String!`
  - SettlementDelayed: `id, call: Call!, attempts: Int!, lastError: String, lastAttemptAt: BigInt!`
  - RepCalculatedFallback: `id, call: Call!, user: Bytes!, baselineDelta: Int!, lowLevelError: String, timestamp: BigInt!`
  - UnclaimedOverage: `id, challenge: Challenge!, beneficiary: Bytes!, amount: BigInt!, claimed: Boolean!, createdAt: BigInt!`
  - PaymasterEvent: `id, user: Bytes!, txCount: Int!, daySpendUsdc: BigInt!, timestamp: BigInt!`
  - TvlSnapshot: `id, blockNumber: BigInt!, callRegistryTvl: BigInt!, followFadeMarketTvl: BigInt!, challengeEscrowTvl: BigInt!, totalTvl: BigInt!, timestamp: BigInt!`
  - CategoryRep: `id, profile: Profile!, category: String!, rep: Int!, updatedAt: BigInt!`
  - LeaderboardEntry: `id, profile: Profile!, window: String!, rank: Int!, score: BigInt!, snapshotAt: BigInt!`
  Each annotated `@entity`. Use `@entity(immutable: true)` for event-record entities (RepEvent, ConvictionCap, CallerExit, PayoutClaim, PositionExit, ChallengePayout, ForceSettlement, SettlementDelayed, RepCalculatedFallback, PaymasterEvent, TvlSnapshot) per The-Graph performance guidance.
  Write `packages/subgraph/subgraph.yaml` with 5 data sources (CallRegistry, FollowFadeMarket, ChallengeEscrow, SettlementManager, ProfileRegistry) all on `network: arbitrum-sepolia` with `address` set to a Sepolia placeholder (the deployed Sepolia USDC contract `0x75faf114eafb1BDbe2F0316DF893fd58CE46AA4d` per Circle docs — chosen because Studio rejects empty addresses, so we use a known-deployed contract just so the deploy pipeline works in Phase 0; Phase 1+ updates each address as contracts land). `startBlock: 1`. Each data source `mapping.file` references the corresponding `src/<contract>.ts` file. `mapping.entities` lists all 23 entity names (Studio requires entities declared in the manifest match the schema). `mapping.eventHandlers` is initially empty `[]` (Phase 1+ adds real handlers as event signatures land).
  Write `packages/subgraph/networks.json` with `{ "arbitrum-sepolia": { "CallRegistry": { "address": "0x...", "startBlock": 1 }, ... }, "arbitrum-one": { ... } }` per graph-cli's standard multi-network deploy config.
  Write `packages/subgraph/src/{call-registry,follow-fade-market,challenge-escrow,settlement-manager,profile-registry}.ts` as AssemblyScript stubs. Each file imports `Call`, `Position`, etc. from `../generated/schema` (codegen target; generated by `graph codegen`) and exports placeholder functions that compile but do nothing. Add a comment block at the top of each file: `// PHASE 0 STUB. Phase 1+ adds event handlers as contracts land. Schema must include the entity for the handler to compile.`
  Write `packages/subgraph/abis/*.json` as minimal ABI shells `[]` (empty arrays) — these will be replaced with real Foundry-generated ABIs in Phase 1+ via a `symlink to packages/contracts/out/*.json` step. The placeholder ABIs allow `graph build` to run in Phase 0.
  Update `packages/subgraph/package.json` to add scripts: `"build": "graph build"`, `"codegen": "graph codegen"`, `"deploy:sepolia": "graph deploy --node https://api.studio.thegraph.com/deploy/ call-it-sepolia"`, `"deploy:sepolia:dry-run": "graph build && echo 'Dry-run: studio deploy not executed without SUBGRAPH_STUDIO_DEPLOY_KEY' "`, `"test": "vitest run tests/"`.
  Write `packages/subgraph/tests/schema.test.ts` per behaviors above using `graphql` npm package's `parse` (add as devDep).
  Write `packages/subgraph/tests/matchstick.yaml` as the matchstick test config (used in Phase 1+ for mapping-handler tests; placeholder in Phase 0).</action>
  <verify>
    <automated>pnpm --filter @call-it/subgraph test && pnpm --filter @call-it/subgraph build</automated>
  </verify>
  <acceptance_criteria>
    - `packages/subgraph/schema.graphql` contains all 23 entity type declarations (count via `grep -c "^type .* @entity" packages/subgraph/schema.graphql` ≥ 23)
    - `pnpm --filter @call-it/subgraph test` reports ≥3 passing tests (entity count, id field present, @entity annotation)
    - `pnpm --filter @call-it/subgraph build` exits 0 (`graph build` succeeds against the empty-ABI placeholders)
    - `packages/subgraph/subgraph.yaml` declares exactly 5 dataSources (`CallRegistry`, `FollowFadeMarket`, `ChallengeEscrow`, `SettlementManager`, `ProfileRegistry`) — verifiable via `grep -c "name: " packages/subgraph/subgraph.yaml` ≥ 5
    - `packages/subgraph/subgraph.yaml` `network` field is `arbitrum-sepolia` (NOT `arbitrum-one`)
    - `packages/subgraph/src/*.ts` files compile under graph-cli's AssemblyScript compiler (`graph codegen` then `graph build` succeed)
  </acceptance_criteria>
  <done>23-entity schema written and parses cleanly; manifest declares 5 data sources on arbitrum-sepolia with placeholder addresses; AssemblyScript mapping stubs compile; schema.test.ts unit test green; subgraph builds locally with `graph build` succeeding.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Polled-events fallback worker against local Anvil + integration test</name>
  <files>apps/relayer/src/workers/polled-events-fallback.ts, apps/relayer/test/polled-events.test.ts</files>
  <read_first>
    - .planning/phases/00-foundation/00-RESEARCH.md Open Question 7 (lines 1146–1150) — polled-events fallback location decision: lives in apps/relayer/src/workers/, polls a known Sepolia contract in Phase 0 (test USDC); Phase 1+ promotes to CallRegistry
    - .planning/REQUIREMENTS.md OPS-02 — polled-events fallback during subgraph deploy gaps
    - viem docs — getLogs signature and event filtering
    - .planning/phases/00-foundation/00-RESEARCH.md Pitfall 8 (referenced in CONTEXT.md — OG cache desync window closed because both indexer + fallback live before any contract)
  </read_first>
  <behavior>
    - Test 1 (`apps/relayer/test/polled-events.test.ts`): The worker, given a mock `publicClient` and a mock `getLogs` returning 2 stubbed logs per call, polls every 5000ms (use `vi.useFakeTimers()`); after 3 polling cycles (`vi.advanceTimersByTime(15_000)`), the `onLog` callback has been invoked exactly 6 times (2 logs × 3 cycles).
    - Test 2 (`apps/relayer/test/polled-events.test.ts`): The worker tracks `lastBlockSeen` and passes `fromBlock: lastBlockSeen + 1n` on the next poll so the same logs are not re-emitted; assert `getLogs` is called with monotonically increasing `fromBlock`.
    - Test 3 (`apps/relayer/test/polled-events.test.ts`): When `getLogs` throws, the worker logs an error Pino event `polled_events_fallback_error` AND continues polling on the next tick (does not crash); after 3 cycles with the first throwing, `onLog` invoked 4 times (cycles 2 and 3 succeed with 2 logs each).
    - Test 4 (`apps/relayer/test/polled-events.test.ts`): `stopPolledEventsFallback()` clears the interval; no further `getLogs` calls after stop.
  </behavior>
  <action>Write `apps/relayer/src/workers/polled-events-fallback.ts` exporting:
  - `interface PolledEventsConfig { publicClient: PublicClient; address: Address; events: AbiEvent[]; intervalMs: number; onLog: (log: Log) => Promise<void> | void; fromBlock?: bigint; }`
  - `interface PolledEventsHandle { stop(): void; getStats(): { lastBlockSeen: bigint; totalLogs: number; errors: number; } }`
  - `function startPolledEventsFallback(config: PolledEventsConfig): PolledEventsHandle`
  Implementation: Reads `lastBlockSeen` (initialized to `config.fromBlock ?? 'latest'` resolved to a concrete blockNumber via `publicClient.getBlockNumber()` at start); on each `setInterval(intervalMs)` tick, calls `publicClient.getLogs({ address: config.address, events: config.events, fromBlock: lastBlockSeen + 1n, toBlock: 'latest' })`; iterates each log → `await config.onLog(log)`; updates `lastBlockSeen = max(lastBlockSeen, log.blockNumber)`; wraps the whole tick in try/catch that increments `errors` counter, logs `{ event: 'polled_events_fallback_error', error: err.message, address: config.address }`, and does NOT throw (so the interval keeps running). Returns `{ stop, getStats }`. Default `intervalMs = 5000` (5s per success criterion #4 of ROADMAP Phase 0).
  Use `logger` from `apps/relayer/src/lib/logger.ts` for structured logging.
  Write `apps/relayer/test/polled-events.test.ts` per behaviors using vitest's `vi.useFakeTimers()` + `vi.advanceTimersByTimeAsync()`. Mock `publicClient.getLogs` with a controllable response queue.</action>
  <verify>
    <automated>pnpm --filter @call-it/relayer test -- test/polled-events.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @call-it/relayer test test/polled-events.test.ts` reports 4 passing tests, 0 failures
    - `apps/relayer/src/workers/polled-events-fallback.ts` exports both `startPolledEventsFallback` and `stopPolledEventsFallback` (verifiable via `grep -c "export" apps/relayer/src/workers/polled-events-fallback.ts` ≥ 2)
    - `grep -c "getLogs" apps/relayer/src/workers/polled-events-fallback.ts` returns ≥ 1
    - `grep -c "5000\\|intervalMs" apps/relayer/src/workers/polled-events-fallback.ts` returns ≥ 1 (5s default interval per OPS-02)
    - No `process.exit` or `throw` outside the try-catch in the polling loop (worker survives transient RPC errors)
  </acceptance_criteria>
  <done>Polled-events fallback worker compiles + 4 unit tests green; survives transient errors without crashing; monotonically advances `fromBlock`; stop() cleanly clears the interval. Phase 1+ wires this against the deployed CallRegistry once that contract exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: OG Fallback variant route at /api/og/fallback per §16.6 (SHARE-09)</name>
  <files>apps/web/app/fonts/Syne-Bold.ttf, apps/web/app/fonts/SpaceGrotesk-Regular.ttf, apps/web/app/fonts/JetBrainsMono-Regular.ttf, apps/web/lib/og-fonts.ts, apps/web/app/api/og/fallback/route.ts, apps/web/playwright.config.ts, apps/web/tests/og-fallback.spec.ts, apps/web/package.json</files>
  <read_first>
    - .planning/phases/00-foundation/00-RESEARCH.md §Pattern 6 (lines 716–824) — full /api/og/fallback/route.ts canonical implementation including Syne/SpaceGrotesk/JetBrainsMono fonts + 4 corner brackets + asymmetric hero
    - .planning/phases/00-foundation/00-RESEARCH.md Pitfall F (lines 1031–1039) — fonts must live in app/fonts/ NOT public/fonts/ for Vercel bundler
    - .planning/phases/00-foundation/00-RESEARCH.md Pitfall E (lines 1022–1029) — Fallback route uses ZERO NEXT_PUBLIC_* envs
    - CALL_IT_SPEC1.md §16.6 — Fallback OG card design contract (this is THE design spec; treat as binding)
    - CLAUDE.md "What NOT to Use" — display: grid in OG templates (Pitfall 15)
    - .planning/REQUIREMENTS.md SHARE-09, SHARE-11
  </read_first>
  <behavior>
    - Test 1 (`apps/web/tests/og-fallback.spec.ts`, Playwright snapshot): GET `${baseURL}/api/og/fallback?handle=veda` returns 200 with `Content-Type: image/png`; the returned PNG is 1200×630 pixels; visual diff vs reference fixture (committed to `tests/fixtures/og-fallback-veda.png`) has < 0.1% pixel difference.
    - Test 2 (`apps/web/tests/og-fallback.spec.ts`): GET with no `handle` query returns 200 (renders "by @someone" fallback per Pattern 6 line 770); handle longer than 32 chars is truncated (per Pattern 6 line 736 `.slice(0, 32)`).
    - Test 3 (`apps/web/tests/og-fallback.spec.ts`): The `Cache-Control` header equals `public, max-age=60, stale-while-revalidate=300` per SHARE-10.
    - Test 4 (`apps/web/tests/og-fallback.spec.ts`): `X-Variant` header equals `fallback` for downstream debugging.
    - Test 5 (`apps/web/tests/og-fallback.spec.ts`): ESLint runs against `apps/web/app/api/og/fallback/route.ts` and produces zero violations (proves no-display-grid rule from Plan 00-01 is enforced).
  </behavior>
  <action>Download Syne-Bold.ttf, SpaceGrotesk-Regular.ttf, JetBrainsMono-Regular.ttf (Google Fonts; SIL Open Font License) and commit to `apps/web/app/fonts/` (NOT `public/fonts/` per Pitfall F). Source URLs:
  - Syne: https://fonts.google.com/specimen/Syne (Bold 700)
  - Space Grotesk: https://fonts.google.com/specimen/Space+Grotesk (Regular 400)
  - JetBrains Mono: https://fonts.google.com/specimen/JetBrains+Mono (Regular 400)
  Write `apps/web/lib/og-fonts.ts` exporting three `Uint8Array` constants: `syneBold`, `spaceGrotesk`, `jetBrainsMono`. Load each via `readFileSync(path.join(process.cwd(), 'app/fonts/<file>.ttf'))` at module init time. Wrap in `try/catch` that throws a descriptive error if the font file is missing (so Vercel cold-start fails loudly instead of silently falling back to Inter).
  Write `apps/web/app/api/og/fallback/route.ts` per Pattern 6 lines 722–823 EXACTLY (Syne 48px CALL IT wordmark; "A CALL WAS MADE" Syne 64px asymmetric 5% from left; "by @{handle}" SpaceGrotesk 28px; subtext "The receipt is being prepared." + "Tap to view live." SpaceGrotesk 18px; footer "callitapp.xyz · Be right in public." SpaceGrotesk 14px + "⬢ ARBITRUM" JetBrainsMono 12px; 4 corner brackets 24×24 with 4px borders via the `CornerBracket` component; 3px outer border #E8F542; #09090E background; 1200×630 dimensions). `export const runtime = 'nodejs'` per Pitfall 15 (NOT edge — resvg-wasm bundle issues per CLAUDE.md). Headers per Pattern 6 lines 808–812. Use ONLY `display: flex`; the ESLint no-display-grid rule must not fire on this file.
  Write `apps/web/playwright.config.ts` with `testDir: './tests'`, `webServer: { command: 'pnpm dev', port: 3000, reuseExistingServer: true }`, `use: { baseURL: process.env.PLAYWRIGHT_BASE_URL ?? 'http://localhost:3000' }`. Add Playwright to `apps/web/package.json` devDeps + a `"test:og-fallback": "playwright test tests/og-fallback.spec.ts"` script.
  Write `apps/web/tests/og-fallback.spec.ts` per behaviors. For the snapshot test, generate the reference PNG ONCE locally via `pnpm dev` + `curl http://localhost:3000/api/og/fallback?handle=veda > tests/fixtures/og-fallback-veda.png`, commit the fixture, then have Playwright assert pixel-diff via `playwright-test-extra` or by using `await expect(buffer).toMatchSnapshot('og-fallback-veda.png', { threshold: 0.001 })`. For the ESLint test (Test 5), spawn `pnpm exec eslint apps/web/app/api/og/fallback/route.ts` via Node `child_process.execSync` and assert exit code 0.</action>
  <verify>
    <automated>pnpm --filter @call-it/web exec playwright test tests/og-fallback.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 3 TTF files exist under `apps/web/app/fonts/` (NOT `public/fonts/` — Pitfall F enforced)
    - `apps/web/app/api/og/fallback/route.ts` contains `export const runtime = 'nodejs'` literally (verifiable via grep)
    - `pnpm exec eslint apps/web/app/api/og/fallback/route.ts` exits 0 (no-display-grid rule passes; flexbox only)
    - `pnpm --filter @call-it/web exec playwright test tests/og-fallback.spec.ts` reports ≥4 passing tests (snapshot, handle truncation, cache-control, X-Variant) — the ESLint test counts as a 5th
    - `rg "display: 'grid'" apps/web/app/api/og/` returns no matches (Pitfall 15 — Satori does not support grid)
    - `rg "NEXT_PUBLIC_" apps/web/app/api/og/fallback/route.ts` returns no matches (Pitfall E — zero NEXT_PUBLIC_* envs)
    - The committed reference PNG at `apps/web/tests/fixtures/og-fallback-veda.png` is exactly 1200×630 pixels (verifiable via `file` command)
  </acceptance_criteria>
  <done>Fallback OG route renders the §16.6 layout exactly; fonts loaded from app/fonts/ (Pitfall F); no display:grid (Pitfall 15); zero NEXT_PUBLIC_* env vars (Pitfall E); Cache-Control + X-Variant headers set; Playwright snapshot test green with <0.1% pixel diff vs committed fixture.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: OG callId catch-all route falls through to Fallback on 404 + warm-render benchmark (SHARE-10, SHARE-11)</name>
  <files>apps/web/app/api/og/[callId]/route.ts, apps/web/tests/og-fallback-routing.spec.ts, apps/web/tests/og-fallback-bench.spec.ts</files>
  <read_first>
    - .planning/REQUIREMENTS.md SHARE-10 (fallback serves when real URL 404s or settled image hasn't regenerated or OG service is fully down)
    - .planning/REQUIREMENTS.md SHARE-11 (renders in <100ms warm using stripped template — handle only from URL)
    - .planning/phases/00-foundation/00-RESEARCH.md Assumption A4 + A11 (lines 1101–1108) — cold-start vs warm-start budgets; warm <100ms; cold <500ms acceptable
    - .planning/phases/00-foundation/00-RESEARCH.md Pitfall 8 (OG cache desync) — Phase 0 prep
  </read_first>
  <behavior>
    - Test 1 (`apps/web/tests/og-fallback-routing.spec.ts`): GET `${baseURL}/api/og/0xdeadbeef` (Phase 0: every callId is "unknown" because no subgraph data exists) returns 200 with the Fallback variant body (header `X-Variant: fallback`). Asserts the route handler internally routes through to the Fallback rendering path (via re-export, redirect, or shared component).
    - Test 2 (`apps/web/tests/og-fallback-routing.spec.ts`): The catch-all route honors `?handle=veda` query passthrough — if present, the rendered Fallback card shows `by @veda` (proves the catch-all preserves user-supplied handle hint even when callId is unknown).
    - Test 3 (`apps/web/tests/og-fallback-bench.spec.ts`): 100 sequential warm requests to `/api/og/fallback?handle=veda` (after one warmup request); p95 timing < 100ms (per SHARE-11). Uses Playwright's `request` API + `performance.now()` measurement; the warmup request is excluded from the timing window.
    - Test 4 (`apps/web/tests/og-fallback-bench.spec.ts`): The median (p50) warm response time is also reported and logged for the SUMMARY; warmed cold-start time (first request after server start) is reported as informational (acceptance gate is only on warm p95).
  </behavior>
  <action>Write `apps/web/app/api/og/[callId]/route.ts` exporting:
  ```
  export const runtime = 'nodejs';
  export async function GET(req: Request, { params }: { params: Promise<{ callId: string }> }) { ... }
  ```
  Implementation logic (Phase 0 — no subgraph data exists yet):
  1. Resolve `callId` from `await params`
  2. Extract `?handle=...` query if present
  3. In Phase 0, ALWAYS fall through to the Fallback render — no subgraph lookup attempted (no contracts deployed yet)
  4. Construct the same `ImageResponse` as `/api/og/fallback` by re-using a shared `renderFallback({ handle })` function extracted from Task 3's route — refactor Task 3's route to extract this function into `apps/web/lib/og-fallback-render.ts` for reuse here
  5. Return the ImageResponse with header `X-Variant: fallback` and `X-Reason: phase-0-no-subgraph-data`
  Add a top comment block: "Phase 0 ALWAYS returns Fallback. Phase 2 wires Live variant. Phase 4 wires Settled/DuelSettled/CallerExited variants. The fallback-on-error path remains forever as the SHARE-10 contract."
  Write `apps/web/tests/og-fallback-routing.spec.ts` per behaviors 1–2.
  Write `apps/web/tests/og-fallback-bench.spec.ts` per behaviors 3–4. Use `await page.context().request.get(url)` with timing wrapped in `performance.now()`; collect 100 samples after 1 warmup; compute percentiles (p50, p95, p99); assert p95 < 100. Log the full distribution to console + write to `apps/web/tests/og-fallback-bench-results.json` for the SUMMARY artifact.
  As part of this task, REFACTOR `apps/web/app/api/og/fallback/route.ts` from Task 3 to extract the `renderFallback({ handle })` core into `apps/web/lib/og-fallback-render.ts`; both routes import from there. This preserves Task 3's tests AND lets Task 4 reuse the renderer without duplication.</action>
  <verify>
    <automated>pnpm --filter @call-it/web exec playwright test tests/og-fallback-routing.spec.ts tests/og-fallback-bench.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @call-it/web exec playwright test tests/og-fallback-routing.spec.ts` reports 2 passing tests, 0 failures
    - `pnpm --filter @call-it/web exec playwright test tests/og-fallback-bench.spec.ts` reports 2 passing tests; the bench test must show p95 < 100ms (warm) — failing this is a hard gate per SHARE-11
    - `apps/web/app/api/og/[callId]/route.ts` includes the comment block referencing Phase 0 / Phase 2 / Phase 4 variant rollout (verifiable via grep)
    - `apps/web/lib/og-fallback-render.ts` exists and is imported by BOTH `apps/web/app/api/og/fallback/route.ts` and `apps/web/app/api/og/[callId]/route.ts` (verifiable via 2 separate greps)
    - `apps/web/tests/og-fallback-bench-results.json` exists with shape `{ p50, p95, p99, samples }` (artifact for SUMMARY)
    - All routes still use `runtime = 'nodejs'` (Pitfall 15 enforced)
  </acceptance_criteria>
  <done>Catch-all /api/og/[callId] route routes ALL requests to the Fallback render in Phase 0 (SHARE-10); shared `renderFallback` function used by both routes (DRY); benchmark Playwright test asserts p95 warm < 100ms over 100 sequential requests (SHARE-11); bench results JSON committed for SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public Internet → Vercel `/api/og/*` routes | Anonymous; public-by-default per spec §18.1; primary share-loop surface |
| Public Internet → Relayer polled-events fallback (read-only) | viem `getLogs` against public RPC; no auth needed |
| Subgraph Studio API → Subgraph (deploy time) | Deploy key (SUBGRAPH_STUDIO_DEPLOY_KEY) gates writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-18 | Tampering | `?handle=` query string in OG routes | mitigate | `handle.slice(0, 32)` per Pattern 6 line 736 bounds input length; React JSX text rendering escapes by default (no innerHTML); zod schema (Phase 1+) validates format. Severity: LOW — worst case malicious handle renders as garbled text in image, no XSS path because Satori produces a PNG. |
| T-00-19 | Information Disclosure | `/api/og/*` routes leak callIds via URL | accept | Receipts are public-by-default per spec §18.1 + §15.7 ("NO wallet address in sticky header" for Settled receipts is the only PII control); callIds are intentionally public + shareable. Severity: NONE — by design. |
| T-00-20 | Denial of Service | OG route render burst from social-media crawler | mitigate | Cache-Control: public, max-age=60, stale-while-revalidate=300 (per SHARE-10) — Vercel Edge Network absorbs 99% of repeat fetches; Vercel function rate limit applies per-IP. Severity: LOW — Vercel autoscales; cost ceiling is Vercel plan budget. |
| T-00-21 | Tampering | Subgraph deploy via Studio | mitigate | SUBGRAPH_STUDIO_DEPLOY_KEY in GCP Secret Manager; Plan 00-05 deploy workflow uses GCP-IAM-bound service account to fetch the key; key is per-subgraph (call-it-sepolia separate from call-it-mainnet). Severity: HIGH — tampered subgraph deploy ships wrong indexing logic to all consumers. |
| T-00-22 | Tampering | Polled-events fallback worker results | mitigate | Worker is read-only against public RPC; results flow only to internal consumers; structured Pino logging surfaces anomalies; the worker is a FALLBACK — the subgraph is the source of truth, so any drift between the two is detectable. Severity: MEDIUM. |
| T-00-23 | Information Disclosure | Polled-events fallback exposes block-by-block scan timing to RPC provider | accept | RPC providers (Alchemy free tier in Phase 0) see all queries by design; no fix at this tier. Severity: LOW. |
| T-00-24 | Tampering | Font files at `apps/web/app/fonts/*.ttf` | mitigate | Files committed to git; checksum verifiable via lockfile (pnpm-lock.yaml does not hash these — gap accepted because fonts are SIL OFL public domain); if compromised, OG renders go wrong visually (loud failure, not silent). Severity: LOW. |
| T-00-25 | Denial of Service | OG cold-start latency exceeds budget on Vercel | mitigate | Per Assumption A11: warm <100ms (asserted by Task 4 benchmark); cold <500ms (acceptable, not gated); if cold-start exceeds the budget on Vercel hobby tier, document in SUMMARY + plan Vercel Pro tier promotion (Pitfall F escalation path). Severity: LOW. |
| T-00-26 | Tampering | `display: grid` accidentally introduced in future OG variants | mitigate | ESLint custom rule `no-display-grid` scoped to `apps/web/app/api/og/**` (Plan 00-01 wrote the rule; this plan validates it fires correctly via Task 3 Test 5). Severity: HIGH — Satori silently misrenders grid, producing wrong-looking cards. |

**No `high` severity threats remain unmitigated.** T-00-21 (subgraph deploy key) and T-00-26 (display:grid drift) each have an automated check + an operational separation.
</threat_model>

<verification>
After all tasks complete:
1. `pnpm --filter @call-it/subgraph test && pnpm --filter @call-it/subgraph build` exits 0
2. `pnpm --filter @call-it/relayer test test/polled-events.test.ts` reports 4 passing tests
3. `pnpm --filter @call-it/web exec playwright test` (all 3 og-fallback test files) reports ≥9 passing tests
4. `apps/web/tests/og-fallback-bench-results.json` shows p95 < 100ms
5. Manually: `cd packages/subgraph && pnpm deploy:sepolia` (with SUBGRAPH_STUDIO_DEPLOY_KEY env set) succeeds against Subgraph Studio
6. Manually: `pnpm --filter @call-it/web dev` + open `http://localhost:3000/api/og/fallback?handle=veda` in browser shows the §16.6 card with correct fonts + colors + corner brackets
</verification>

<success_criteria>
- All 27 files in `<files_modified>` exist and pass lint
- Subgraph deploys to Studio dry-run successfully OR returns a clear, actionable error from Studio
- Polled-events fallback worker passes 4 unit tests including transient-error survival
- OG Fallback route renders §16.6 layout exactly; p95 warm render < 100ms over 100 sequential requests
- Catch-all OG route falls through to Fallback variant in Phase 0 (no subgraph data); architecture supports SHARE-10 contract forward
- ESLint no-display-grid rule fires on any attempted `display: 'grid'` in OG template files
- 3 font files in `apps/web/app/fonts/` (not `public/fonts/`) per Pitfall F mitigation
- Bench results JSON committed for SUMMARY analysis
</success_criteria>

<output>
After completion, create `.planning/phases/00-foundation/00-03-SUMMARY.md` per template. Record:
- p50 / p95 / p99 warm render times from the OG benchmark
- Cold-start measurement (first-request timing) — informational
- Subgraph Studio deploy URL + Subgraph ID for Phase 1+ to reference
- Confirmation that the 3 TTF files are SIL OFL-licensed (license attribution committed to `apps/web/app/fonts/LICENSE.txt`)
- Notes on whether the catch-all route's "Phase 0 always returns Fallback" comment was clear enough for Phase 2 executors
</output>
