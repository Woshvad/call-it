---
phase: 00-foundation
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - scripts/deploy-safe.ts
  - scripts/seed-calendar.ts
  - scripts/repoint-calendar.ts
  - scripts/fire-synthetic-alert.ts
  - .github/workflows/synthetic-alert.yml
  - docs/runbooks/stylus-reactivation.md
  - docs/runbooks/multisig-promotion.md
  - docs/runbooks/relayer-key-rotation.md
  - docs/runbooks/env-diff-ritual.md
  - docs/runbooks/nft-twap-sanity.md
  - docs/runbooks/settlement-stuck.md
  - docs/demo-seed-plan.md
  - docs/better-stack-dashboards.md
  - packages/shared/src/constants/stylus-calendar.json
  - apps/relayer/src/workers/stylus-deactivation-watcher.ts
  - apps/relayer/test/stylus-deactivation-watcher.test.ts
  - scripts/test/deploy-safe.test.ts
  - scripts/test/fire-synthetic-alert.test.ts
  - package.json
autonomous: true
requirements:
  - SAFETY-58
  - OPS-06
  - OPS-18
  - OPS-20
tags:
  - multisig
  - safe
  - calendar
  - synthetic-alert
  - runbooks
  - better-stack
user_setup:
  - service: safe-wallet
    why: "Per D-10/D-11/SAFETY-58 — Safe 2-of-3 dry-run on Arbitrum Sepolia with Ledger Nano as deployer; same Ledger keys persist to mainnet in Phase 6"
    env_vars:
      - name: SAFE_SIGNER_1
        source: "Operator's Ledger Nano X/S Plus address (also becomes one of 3 multisig signers per D-11)"
      - name: SAFE_SIGNER_2
        source: "Trusted human #2 hardware wallet address — different brand from signer 1 (e.g., Trezor) per D-10"
      - name: SAFE_SIGNER_3
        source: "Trusted human #3 hardware wallet address"
      - name: RPC_URL_ARBITRUM_SEPOLIA
        source: "Alchemy free-tier RPC URL for arbitrum-sepolia (stored in GCP Secret Manager)"
    dashboard_config:
      - task: "Operator + trusted humans 2 + 3 each generate a fresh Ledger Nano address; record all 3 in this plan's env vars"
        location: "Ledger Live → Manage → New Account → Ethereum (Arbitrum derivation path)"
      - task: "Fund operator's Ledger Nano with ~0.05 ETH on Arbitrum Sepolia (faucet: https://faucet.quicknode.com/arbitrum/sepolia)"
        location: "Arbitrum Sepolia faucet"
  - service: google-calendar
    why: "Per D-13 — belt-and-suspenders for Pitfall 17 (Stylus 365-day reactivation); calendar invites at T-30d/T-15d/T-7d/T-1d before placeholder deploy date"
    env_vars:
      - name: GOOGLE_CALENDAR_OAUTH_TOKEN
        source: "Generated via `pnpm tsx scripts/seed-calendar.ts --setup` which runs the one-time OAuth flow; token stored in GCP Secret Manager"
    dashboard_config:
      - task: "Operator runs `pnpm tsx scripts/seed-calendar.ts --setup` once to grant Calendar API access; token stored in GCP Secret Manager as GOOGLE_CALENDAR_OAUTH_TOKEN"
        location: "Local terminal + browser OAuth flow"
      - task: "After seeding, operator visually confirms 4 events at T-30d/T-15d/T-7d/T-1d exist in Google Calendar UI (one-time post-Wave-3 verification per VALIDATION.md manual-only row)"
        location: "Google Calendar web UI"
  - service: betterstack-dashboards
    why: "Per D-14/OPS-06 — 5 hosted dashboards (Total TVL, calls/hour, settlement latency, dispute rate, failed-tx rate) seeded with synthetic events from Plan 00-02's relayer"
    env_vars: []
    dashboard_config:
      - task: "Create 5 dashboards in Better Stack UI from the templates listed in docs/better-stack-dashboards.md"
        location: "Better Stack → Dashboards → New Dashboard → manually configure 5 widgets per documented schema"
      - task: "Run `pnpm tsx scripts/fire-synthetic-alert.ts --seed-dashboards` (one-time) to inject synthetic Pino events; verify each of the 5 dashboards renders the synthetic data point"
        location: "Local terminal + Better Stack UI"
      - task: "Set dashboard access to operator + co-signers only per D-17 (private; no public status page in v1)"
        location: "Better Stack → Dashboards → Sharing"

must_haves:
  truths:
    - "Operator can run `pnpm tsx scripts/deploy-safe.ts --network sepolia --dry-run` and the Safe 2-of-3 deploy succeeds on Arbitrum Sepolia with the deployer key signed by Ledger Nano per Pattern 8 (SAFETY-58, success criterion #6, Pitfall 6 prep)."
    - "Daily GitHub Actions cron `.github/workflows/synthetic-alert.yml` fires a synthetic Telegram event with a UUID nonce; the verification step polls Telegram getUpdates within 60s and asserts the nonce appears in the P0 channel; if silent, the CI build FAILS (D-16, Pitfall D)."
    - "Google Calendar invites at T-30d / T-15d / T-7d / T-1d exist for a placeholder Stylus reactivation date; the event IDs are stored in `packages/shared/src/constants/stylus-calendar.json` so `scripts/repoint-calendar.ts` can update them in Phase 5 once the real deploy timestamp is known (D-13, Pitfall 17, Pitfall C mitigation)."
    - "5 Better Stack dashboards (Total TVL, calls/hour, settlement latency, dispute rate, failed-tx rate) exist and render synthetic-event data points seeded by `scripts/fire-synthetic-alert.ts --seed-dashboards` per OPS-06; documented configuration in `docs/better-stack-dashboards.md`."
    - "6 operator runbooks exist with concrete steps (not placeholders): stylus-reactivation.md, multisig-promotion.md, relayer-key-rotation.md, env-diff-ritual.md, nft-twap-sanity.md, settlement-stuck.md."
    - "Demo seed plan documents the 10-15 calls × seed scenarios for Phase 6 Sepolia staging (OPS-20) — covering Price target / Spread-vs / 3 event subtypes per success criterion #1 of Phase 6."
    - "Stylus deactivation watcher in the relayer polls `arbitrumActivationExpiry(stylusAddress)` daily and fires P0 alert when <30/15/7/1 days remain (D-13 second-belt + Pitfall C mitigation — independent of Calendar correctness)."
  artifacts:
    - path: scripts/deploy-safe.ts
      provides: "Safe 2-of-3 deploy via @safe-global/protocol-kit + Ledger Nano signer; --dry-run flag for Sepolia Phase 0 use; --execute flag for Phase 6 mainnet"
      contains: "SafeFactory|deploySafe"
    - path: scripts/seed-calendar.ts
      provides: "One-time OAuth setup + 4-event creation at T-30/15/7/1 before placeholder Stylus deploy date; stores event IDs in stylus-calendar.json"
    - path: scripts/repoint-calendar.ts
      provides: "Phase 5 hook: re-points the 4 calendar events to the real deploy timestamp queried from the activated Stylus contract"
    - path: .github/workflows/synthetic-alert.yml
      provides: "Daily 12:00 UTC cron that fires + verifies a Telegram alert end-to-end; build fails if message doesn't land within 60s (D-16, Pitfall D)"
      contains: "cron: '0 12 \\* \\* \\*'"
    - path: scripts/fire-synthetic-alert.ts
      provides: "CI helper: HMAC-signs + POSTs synthetic event to relayer /internal/test-alert; polls Telegram getUpdates for nonce within 60s; exit 0/1"
    - path: docs/runbooks/stylus-reactivation.md
      provides: "Step-by-step Stylus 365-day reactivation procedure with Telegram alert thresholds + Calendar coordination"
      min_lines: 40
    - path: docs/runbooks/multisig-promotion.md
      provides: "Step-by-step Phase 6 multisig ownership transfer procedure (Phase 6 HARD GATE per Delta 2 of SUMMARY.md)"
      min_lines: 40
    - path: docs/runbooks/relayer-key-rotation.md
      provides: "GCP KMS key version rotation procedure; includes the #manual-rep-compensation anchor referenced by OPS-25 alert message"
      min_lines: 30
    - path: docs/runbooks/env-diff-ritual.md
      provides: "Pre-mainnet env diff procedure: compare Vercel + Fly + Studio + GCP secrets across networks; mandatory before Phase 7.5 smoke test"
      min_lines: 30
    - path: docs/runbooks/nft-twap-sanity.md
      provides: "NFT TWAP operator runbook with on-chain TWAP sanity-check script (OPS-18) — Phase 0 ships skeleton with TODO_PHASE_4 markers"
      min_lines: 20
    - path: docs/runbooks/settlement-stuck.md
      provides: "Settle-stuck operator runbook with 3-step diagnosis pattern (subgraph stuck-callId query → Better Stack failed-tx-rate dashboard diagnosis → forceSettle after 7d cooldown per §13.7) — referenced by OPS-14/OPS-15 alert paths"
      min_lines: 20
    - path: docs/better-stack-dashboards.md
      provides: "Documented Better Stack dashboard configuration: 5 dashboards × widget specs derived from Pino structured fields (OPS-06)"
      min_lines: 30
    - path: docs/demo-seed-plan.md
      provides: "Documented 10-15-call seed plan covering all call types for Phase 6 Sepolia staging (OPS-20)"
      min_lines: 25
    - path: apps/relayer/src/workers/stylus-deactivation-watcher.ts
      provides: "Daily cron worker that polls activation expiry and fires P0 alerts at <30/15/7/1 days; second belt for Pitfall 17"
      exports: ["startStylusDeactivationWatcher"]
  key_links:
    - from: scripts/fire-synthetic-alert.ts
      to: apps/relayer/src/routes/internal-test-alert.ts
      via: "HMAC-signed POST with event + nonce + timestamp; relayer verifies HMAC and dispatches sendAlert"
      pattern: "X-Internal-HMAC|internal/test-alert"
    - from: scripts/fire-synthetic-alert.ts
      to: "Telegram Bot API getUpdates"
      via: "Polls every 5s up to 60s; matches nonce in any P0 channel message"
      pattern: "getUpdates|telegram"
    - from: .github/workflows/synthetic-alert.yml
      to: scripts/fire-synthetic-alert.ts
      via: "Daily 12:00 UTC cron runs `pnpm tsx scripts/fire-synthetic-alert.ts --event rep_fallback --wait-seconds 60`"
      pattern: "fire-synthetic-alert.ts"
    - from: scripts/seed-calendar.ts
      to: packages/shared/src/constants/stylus-calendar.json
      via: "After creating 4 events, write { event_t30, event_t15, event_t7, event_t1, placeholder_deploy_date } JSON"
      pattern: "stylus-calendar.json"
    - from: scripts/repoint-calendar.ts
      to: packages/shared/src/constants/stylus-calendar.json
      via: "Phase 5 reads event IDs and calls Google Calendar API .events.update(...) with new dates"
      pattern: "events\\.update"
    - from: apps/relayer/src/workers/stylus-deactivation-watcher.ts
      to: apps/relayer/src/workers/alerts.ts
      via: "Daily check polls Stylus contract activation expiry; fires P0 'stylus_reactivation' alert at threshold crossings"
      pattern: "sendAlert\\(['\"]stylus_reactivation"
---

## Phase Goal

**As an** operator preparing the Phase 6 multisig promotion + Phase 5 Stylus reactivation drill, **I want to** dry-run the Safe 2-of-3 deploy on Sepolia using my Ledger Nano AND see (a) 4 Google Calendar invites for T-30d/T-15d/T-7d/T-1d Stylus reactivation reminders, (b) a daily-firing synthetic-alert CI cron that verifies the Telegram pipeline end-to-end with nonce matching, (c) 5 Better Stack dashboards populated by synthetic events, and (d) 5 written runbooks covering every operational procedure Phase 1+ depends on, **so that** Phase 6 multisig promotion is mechanical (Pitfall 6 closed), Phase 5 Stylus reactivation cannot be silently missed (Pitfall 17 closed), the alert pipeline cannot silently break (Pitfall D closed), and every later phase has the operator-procedural substrate to execute against.

<objective>
Land the operational substrate that Plans 00-01/02/03 do not cover: the Safe 2-of-3 deploy dry-run script with Ledger Nano signer; the Google Calendar seeding for Stylus reactivation with the Phase 5 re-point hook; the daily synthetic-alert CI cron + helper script with Telegram getUpdates verification; the 5 Better Stack dashboards documentation + synthetic-event seeding; the 5 operator runbooks (stylus-reactivation, multisig-promotion, relayer-key-rotation, env-diff-ritual, nft-twap-sanity); the demo seed plan; and the Stylus deactivation watcher (second belt for Pitfall 17 — independent of Calendar correctness).

Purpose: Closes SAFETY-58 (Safe 2-of-3 deploy dry-run, Pitfall 6 prep), OPS-06 (5 Better Stack dashboards), OPS-18 (NFT TWAP runbook skeleton), OPS-20 (demo seed plan), and the operational requirements of D-10/D-11/D-13/D-14/D-15/D-16/D-17. Mitigates Pitfall 17 (Stylus reactivation belt + suspenders), Pitfall C (calendar self-deactivation pattern), Pitfall D (synthetic-alert verification correctness).

Output: An operator who can (a) run the Safe deploy with a Ledger, (b) see 4 Calendar invites + Telegram T-N alerts firing in the future, (c) see the daily synthetic-alert CI cron green, and (d) reference 5 runbooks for the procedures they'll execute in Phases 5/6/7.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-foundation/00-CONTEXT.md
@.planning/phases/00-foundation/00-RESEARCH.md
@.planning/phases/00-foundation/00-VALIDATION.md
@CLAUDE.md
@CALL_IT_SPEC1.md
@.planning/phases/00-foundation/00-01-SUMMARY.md
@.planning/phases/00-foundation/00-02-SUMMARY.md
@.planning/phases/00-foundation/00-03-SUMMARY.md

<interfaces>
Consumes from Plan 00-01:
- `@call-it/shared` — constants module (will gain `stylus-calendar.json` from this plan)
- Repository root `pnpm-workspace.yaml` — adds scripts/ as a workspace-utility location

Consumes from Plan 00-02:
- `apps/relayer/src/routes/internal-test-alert.ts` — HMAC-gated endpoint that the synthetic-alert script invokes
- `apps/relayer/src/workers/alerts.ts` — `sendAlert` function for the stylus-deactivation-watcher to invoke
- `apps/relayer/src/lib/logger.ts` — Pino logger for the watcher

Consumes from Plan 00-03:
- `packages/subgraph/` — completes the indexing substrate so Better Stack dashboards have something to point at (in Phase 0 they show synthetic data; Phase 1+ swaps in real subgraph queries)

Establishes (consumed by Phase 5 + Phase 6 + Phase 7.5):
- `scripts/deploy-safe.ts` — Phase 6 runs this with `--network arbitrum-one --execute` to deploy the production Safe (same Ledger keys persist)
- `scripts/repoint-calendar.ts` — Phase 5 runs this once Stylus is deployed to replace placeholder dates with real activation expiry timestamps
- `docs/runbooks/multisig-promotion.md` — Phase 6 step-by-step ownership transfer
- `docs/runbooks/stylus-reactivation.md` — Phase 5+ ongoing operational doc
- `docs/runbooks/env-diff-ritual.md` — Phase 6 pre-mainnet checklist (mandatory before Phase 7.5)
- `docs/runbooks/nft-twap-sanity.md` — Phase 4 NFT TWAP path operator doc
- `docs/runbooks/relayer-key-rotation.md` — referenced by OPS-25 alert message link
- `docs/demo-seed-plan.md` — Phase 6 Sepolia staging seed list (≥10 calls per success criterion #1)
- `docs/better-stack-dashboards.md` — Phase 1+ updates dashboard widget specs as real data lands
- `packages/shared/src/constants/stylus-calendar.json` — Phase 5 reads this to know which Calendar events to update

External (out of plan scope but referenced):
- `@safe-global/protocol-kit` 4.x: `SafeFactory.init({ provider, signer })` + `safeFactory.deploySafe({ safeAccountConfig: { owners, threshold } })`
- `@safe-global/api-kit` 2.x: `SafeApiKit({ chainId })` for Sepolia transaction service
- `@ledgerhq/hw-app-eth`: Ledger Nano signer integration (clear-signing for EIP-712 + tx)
- `googleapis` package: `google.calendar('v3').events.insert/update`
- Telegram Bot API: `getUpdates` endpoint per `https://api.telegram.org/bot<token>/getUpdates`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Safe 2-of-3 deploy script with Ledger Nano + Sepolia dry-run integration test</name>
  <files>scripts/deploy-safe.ts, scripts/test/deploy-safe.test.ts</files>
  <read_first>
    - .planning/phases/00-foundation/00-RESEARCH.md §Pattern 8 (lines 870–900) — Safe deploy script skeleton with Ledger
    - .planning/phases/00-foundation/00-CONTEXT.md D-10 (2 different HW-wallet brands), D-11 (Ledger Nano X/S Plus for Phase 0-5 deployer)
    - .planning/REQUIREMENTS.md SAFETY-58 (Safe 2-of-3 deploys to Sepolia, dry-run)
    - .planning/phases/00-foundation/00-RESEARCH.md §Don't Hand-Roll (lines 967, 974, 975) — Safe protocol-kit + Ledger viem integration
    - .planning/phases/00-foundation/00-RESEARCH.md Assumption A7 (line 1104) — @safe-global/protocol-kit v4 on Arbitrum Sepolia
  </read_first>
  <behavior>
    - Test 1 (`scripts/test/deploy-safe.test.ts`, gated by `it.skipIf(!process.env.SAFE_SIGNER_1)`): With all 3 signer addresses + Sepolia RPC URL env vars set, running `deploy-safe.ts --network sepolia --dry-run` (NO Ledger required for dry-run path) constructs a valid `SafeAccountConfig` with `owners: [SAFE_SIGNER_1, SAFE_SIGNER_2, SAFE_SIGNER_3]`, `threshold: 2`, and reports the predicted Safe address WITHOUT executing the deploy transaction.
    - Test 2 (`scripts/test/deploy-safe.test.ts`, gated): With `--execute --network sepolia` (still gated) + a Ledger device connected (manual operator step), the script signs with the Ledger, the Safe deploys to Sepolia, the new Safe address is written to `packages/contracts/deployments/safe-sepolia.json`, and `cast call $SAFE_ADDRESS "getOwners()"` returns the 3 signer addresses + `cast call $SAFE_ADDRESS "getThreshold()"` returns 2.
    - Test 3 (unit, not gated): With all 3 signer env vars set but RPC unreachable, the script exits 1 with a clear error message; with one signer missing, exits 1 with "SAFE_SIGNER_X missing".
  </behavior>
  <action>Write `scripts/deploy-safe.ts` per Pattern 8 (RESEARCH.md lines 870–900):
  ```
  // tsx-run; takes --network sepolia|arbitrum-one, --dry-run or --execute, --signer-source ledger|env (default: ledger)
  ```
  Flow:
  1. Parse argv via `node:util.parseArgs`: `--network` ('sepolia' or 'arbitrum-one'), `--dry-run` (boolean), `--execute` (boolean), `--signer-source` ('ledger' | 'env', default 'ledger'). Validate mutually exclusive flags.
  2. Read `SAFE_SIGNER_1`, `SAFE_SIGNER_2`, `SAFE_SIGNER_3` from env; assert each is a valid 0x-prefixed Ethereum address (use viem `isAddress`); fail fast with clear error if any missing or malformed.
  3. Pick `chain` from `@call-it/shared` networks constants: `arbitrum-sepolia` → 421614, `arbitrum-one` → 42161.
  4. Construct `walletClient` via `createWalletClient`:
     - If `--signer-source=ledger`: import `@ledgerhq/hw-app-eth` + `@ledgerhq/hw-transport-node-hid` (Ledger Nano X/S Plus per D-11); construct ledger account via viem `toAccount` with `signMessage`/`signTypedData`/`signTransaction` delegating to Ledger; print "Awaiting Ledger confirmation..." before each signature
     - If `--signer-source=env`: read `DEPLOYER_PRIVATE_KEY` from env (LOCAL DEV ONLY — log a loud warning that this path is forbidden for mainnet per D-11)
     - Reject `--signer-source=env --network=arbitrum-one` combo with exit code 1
  5. Construct `SafeAccountConfig`: `{ owners: [SAFE_SIGNER_1, SAFE_SIGNER_2, SAFE_SIGNER_3], threshold: 2 }`.
  6. Call `SafeFactory.init({ provider: rpcUrl, signer: deployer.address })`; then `safeFactory.deploySafe({ safeAccountConfig })` or just predict address via `predictSafeAddress` if `--dry-run`.
  7. On success: log Safe address + signer set; write to `packages/contracts/deployments/safe-${network}.json` with shape `{ safeAddress, chainId, signers, threshold, deployedAt, deployerAddress, txHash }`; if `--dry-run`, set `txHash: 'dry-run'`.
  8. On failure: log error + exit 1.
  Wire into root `package.json`: `"deploy:safe:sepolia:dry-run": "tsx scripts/deploy-safe.ts --network sepolia --dry-run --signer-source env"` (dev convenience), `"deploy:safe:sepolia": "tsx scripts/deploy-safe.ts --network sepolia --execute --signer-source ledger"` (real Sepolia deploy with Ledger).
  Write `scripts/test/deploy-safe.test.ts` with the 3 tests per behaviors above. Test 1 + Test 2 are gated by env-var presence; Test 3 (unit) runs unconditionally.</action>
  <verify>
    <automated>pnpm tsx scripts/test/deploy-safe.test.ts || pnpm vitest run scripts/test/deploy-safe.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/deploy-safe.ts` parses `--network`, `--dry-run`, `--execute`, `--signer-source` flags
    - `grep -c "owners: \\[SAFE_SIGNER_1" scripts/deploy-safe.ts` returns ≥ 1 (3-signer config wired)
    - `grep -c "threshold: 2" scripts/deploy-safe.ts` returns ≥ 1 (2-of-3 per D-10)
    - `grep -c "@safe-global/protocol-kit" scripts/deploy-safe.ts` returns ≥ 1 (uses Safe SDK per Don't Hand-Roll)
    - `grep -c "@ledgerhq" scripts/deploy-safe.ts` returns ≥ 1 (Ledger integration per D-11)
    - `grep -c "arbitrum-one.*signer-source=env\\|signer-source=env.*arbitrum-one" scripts/deploy-safe.ts` returns ≥ 1 (mainnet+env-key rejection logic exists)
    - When operator runs `pnpm deploy:safe:sepolia:dry-run` with the 3 SAFE_SIGNER env vars + Sepolia RPC, the script predicts a Safe address + writes `safe-sepolia.json` with `txHash: 'dry-run'`
    - Test 3 (unit) passes unconditionally; Tests 1+2 skip cleanly when env absent
  </acceptance_criteria>
  <done>Safe 2-of-3 deploy script handles Sepolia dry-run + Sepolia execute (Ledger) + future Phase 6 mainnet execute (Ledger only); writes deployment manifest JSON; 3 tests (1 unconditional, 2 gated) green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Synthetic alert CI cron + helper script + Telegram getUpdates verification</name>
  <files>scripts/fire-synthetic-alert.ts, .github/workflows/synthetic-alert.yml, scripts/test/fire-synthetic-alert.test.ts</files>
  <read_first>
    - .planning/phases/00-foundation/00-RESEARCH.md §Pattern 9 (lines 902–944) — full workflow + helper script skeleton
    - .planning/phases/00-foundation/00-CONTEXT.md D-16 (daily CI cron + weekly manual fire)
    - .planning/phases/00-foundation/00-RESEARCH.md Pitfall D (lines 1012–1020) — verification must actually check Telegram, not just relayer 200
    - .planning/phases/00-foundation/00-RESEARCH.md Open Question 5 (lines 1136–1139) — Telegram bot getUpdates permission requirement
    - apps/relayer/src/routes/internal-test-alert.ts (Plan 00-02) — HMAC verification contract
  </read_first>
  <behavior>
    - Test 1 (`scripts/test/fire-synthetic-alert.test.ts`, mocked): Running `fire-synthetic-alert.ts --event rep_fallback --wait-seconds 60` against mocked relayer (responds 200) and mocked Telegram getUpdates (returns message containing the nonce within 5s) exits 0 within 10s wall-clock.
    - Test 2 (mocked): If Telegram getUpdates does NOT return a message with the nonce within `--wait-seconds`, the script exits 1 with error "Nonce not seen in P0 channel within 60s — alert pipeline broken" — this is the build-failer per D-16.
    - Test 3 (mocked): If relayer responds non-200, the script exits 1 with the relayer error message; does NOT attempt Telegram polling (no point — the alert can't have fired).
    - Test 4 (mocked): Nonce is a UUID; each invocation generates a fresh nonce; old nonces in the Telegram message history are ignored (regex matches on `nonce:<UUID>` substring).
  </behavior>
  <action>Write `scripts/fire-synthetic-alert.ts` per Pattern 9 skeleton (RESEARCH.md lines 937–944):
  Flow:
  1. Parse argv: `--event <AlertEvent>`, `--wait-seconds <number>`, `--expect-chat-id <chat-id>` (defaults to TELEGRAM_CHAT_ID_P0 from env), `--seed-dashboards` (boolean — synthetic Pino events for OPS-06 dashboard seeding).
  2. Generate `nonce = crypto.randomUUID()`, `timestamp = Math.floor(Date.now() / 1000)`.
  3. Compute HMAC: `hmac = crypto.createHmac('sha256', RELAYER_INTERNAL_HMAC_SECRET).update(JSON.stringify({event, nonce, timestamp})).digest('hex')`.
  4. POST `${RELAYER_URL}/internal/test-alert` with body `{event, nonce, timestamp}` and header `X-Internal-HMAC: ${hmac}`. If non-200, log + exit 1.
  5. Poll Telegram getUpdates every 5 seconds up to `--wait-seconds`: `GET https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/getUpdates?offset=-100` — scan returned messages for `channel_post` entries from `chat.id === expectChatId` whose `text` contains `nonce:${nonce}` OR matches the JSON payload pattern containing the nonce; matched → exit 0; timed out → exit 1 with "Nonce not seen in {channel} within {wait-seconds}s — alert pipeline broken".
  6. Idempotency: each invocation uses a fresh UUID nonce; old nonces in Telegram history are ignored because the regex requires the freshly-generated UUID.
  7. If `--seed-dashboards`: ALSO emit synthetic Pino log lines tagged `synthetic=true` for each of the 5 dashboard data dimensions (tvl, calls_per_hour, settlement_latency_ms, dispute_rate, failed_tx_rate) — these populate Better Stack dashboards per Pitfall G. The relayer's `/internal/test-alert` endpoint should be extended (Plan 00-02 Task 3 already accepts `payload` — log the payload via Pino) to log the synthetic data with the `synthetic=true` tag, which Better Stack widgets can filter.
  Also wire `relayer` side: update `apps/relayer/src/routes/internal-test-alert.ts` (Plan 00-02 already wrote this) to ensure the payload includes `synthetic: true` and the nonce/timestamp gets logged via `logger.info({ event, nonce, timestamp, synthetic: true })` BEFORE `sendAlert` is dispatched — this ensures the synthetic event populates Better Stack dashboards even when synthetic. (If Plan 00-02 already does this, no change needed; otherwise update.)
  Write `.github/workflows/synthetic-alert.yml` per Pattern 9 (RESEARCH.md lines 906–935): `on.schedule.cron: '0 12 * * *'` + `workflow_dispatch`; `jobs.fire-and-verify.runs-on: ubuntu-latest`; steps run setup-node 22 + pnpm install + `pnpm tsx scripts/fire-synthetic-alert.ts --event rep_fallback --wait-seconds 60 --expect-chat-id "$TELEGRAM_CHAT_ID_P0"`. Set `permissions: contents: read`. Inject secrets `RELAYER_URL`, `RELAYER_INTERNAL_HMAC`, `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID_P0` via GitHub repo secrets (operator adds these via `gh secret set` post-Plan-00-05 deploy).
  Write `scripts/test/fire-synthetic-alert.test.ts` with vitest mocks for `fetch` (mock both relayer endpoint + Telegram getUpdates); 4 tests per behaviors above.</action>
  <verify>
    <automated>pnpm vitest run scripts/test/fire-synthetic-alert.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run scripts/test/fire-synthetic-alert.test.ts` reports 4 passing tests, 0 failures
    - `.github/workflows/synthetic-alert.yml` declares cron `'0 12 * * *'` (verifiable via grep)
    - `.github/workflows/synthetic-alert.yml` includes both `schedule:` and `workflow_dispatch:` triggers
    - `grep -c "getUpdates" scripts/fire-synthetic-alert.ts` returns ≥ 1 (Telegram verification actually polls Telegram, not just relayer per Pitfall D)
    - `grep -c "crypto.randomUUID\\|randomBytes" scripts/fire-synthetic-alert.ts` returns ≥ 1 (fresh nonce per run)
    - `grep -c "exit(1)\\|process.exit(1)" scripts/fire-synthetic-alert.ts` returns ≥ 1 (build-failer behavior on missing nonce)
    - When run locally with valid env vars + a real Telegram bot setup, the script fires + verifies a real round-trip (operator-side manual verification step before merging this plan)
  </acceptance_criteria>
  <done>Daily CI cron fires + verifies via Telegram getUpdates; uses fresh UUID nonce per run; build fails when nonce not seen in 60s; 4-test vitest suite green with mocked relayer + Telegram.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Google Calendar seeding for Stylus reactivation + Phase 5 repoint hook + Stylus deactivation watcher (second belt)</name>
  <files>scripts/seed-calendar.ts, scripts/repoint-calendar.ts, packages/shared/src/constants/stylus-calendar.json, apps/relayer/src/workers/stylus-deactivation-watcher.ts, apps/relayer/test/stylus-deactivation-watcher.test.ts</files>
  <read_first>
    - .planning/phases/00-foundation/00-CONTEXT.md D-13 (Google Calendar + Telegram bot duplicate alerts at T-30/15/7/1)
    - .planning/phases/00-foundation/00-RESEARCH.md Pitfall C (lines 1003–1010) — Calendar self-deactivation pattern; stylus-calendar.json with event IDs
    - .planning/phases/00-foundation/00-RESEARCH.md §System Architecture Diagram lines 352–362 (Google Calendar block)
    - .planning/phases/00-foundation/00-RESEARCH.md Open Question 6 (lines 1141–1144) — Google Calendar API write/delete scope
    - CALL_IT_SPEC1.md §10.8 (Stylus 365-day reactivation requirement)
  </read_first>
  <behavior>
    - Test 1 (`apps/relayer/test/stylus-deactivation-watcher.test.ts`): Watcher reads `arbitrumActivationExpiry(stylusAddress)` via mocked viem `publicClient.readContract` returning a timestamp. When `daysRemaining > 30`, no alert fires. When `daysRemaining` is exactly 30 (or just crossed below), one `stylus_reactivation` P0 alert fires with payload `{ daysRemaining: 30, expiryTimestamp, stylusAddress }`.
    - Test 2: Same logic at T-15, T-7, T-1 thresholds — each fires exactly ONE alert per threshold crossing per day (idempotency via Redis `SET NX stylus:alert-fired:T-{N}d:YYYY-MM-DD EX 86400`).
    - Test 3: If `readContract` throws (e.g., Stylus contract not deployed yet — common in Phase 0 when stylusAddress is null/placeholder), the watcher logs the error + emits `stylus_watcher_skipped` Pino event and continues — does NOT crash + does NOT spam alerts. In Phase 0 we expect this path because no Stylus contract exists yet.
  </behavior>
  <action>Write `scripts/seed-calendar.ts`:
  1. CLI flags: `--setup` (one-time OAuth flow), `--placeholder-deploy-date YYYY-MM-DD` (defaults to 90 days from today), `--dry-run`.
  2. `--setup` mode: runs the Google OAuth flow via `googleapis` (open browser, exchange code for refresh token), prints "Save this refresh token to GCP Secret Manager as GOOGLE_CALENDAR_OAUTH_TOKEN" + the token.
  3. Default mode: loads the refresh token from env (`GOOGLE_CALENDAR_OAUTH_TOKEN`); computes 4 event dates as `placeholder_deploy_date - {30, 15, 7, 1} days`; creates 4 Google Calendar events via `google.calendar('v3').events.insert` with titles `[T-{N}d] Stylus Reactivation — placeholder for {date}`, descriptions linking `docs/runbooks/stylus-reactivation.md`, attendees = the operator's email; collects the returned `eventId` for each; writes `packages/shared/src/constants/stylus-calendar.json` with `{ event_t30: <id>, event_t15: <id>, event_t7: <id>, event_t1: <id>, placeholder_deploy_date: <date>, created_at: <iso>, last_updated_via: 'seed' }`.
  4. `--dry-run`: skips API calls, prints what WOULD be created.
  Write `scripts/repoint-calendar.ts`:
  1. CLI flags: `--stylus-deploy-date YYYY-MM-DD` (required; in Phase 5 this comes from querying `cargo stylus get-activation-block` or equivalent), `--dry-run`.
  2. Reads `packages/shared/src/constants/stylus-calendar.json`; reads 4 event IDs; computes new dates as `stylus_deploy_date + 365 days - {30, 15, 7, 1} days`; calls `events.update(calendarId, eventId, { start, end, summary, description })` for each; updates `stylus-calendar.json` `placeholder_deploy_date` → `stylus_deploy_date + 365` and `last_updated_via: 'phase-5-repoint'`.
  3. Phase 5 success criterion explicitly requires running this script — leave a clear comment block at the top of the file: `// Phase 5 task: RUN THIS after Stylus contract activation. Failure to run = wrong calendar dates fire (Pitfall C). The deactivation watcher (apps/relayer/src/workers/stylus-deactivation-watcher.ts) is the second belt that catches this — Telegram alerts fire even if calendar is wrong.`
  Write `packages/shared/src/constants/stylus-calendar.json` as an empty placeholder: `{ "event_t30": null, "event_t15": null, "event_t7": null, "event_t1": null, "placeholder_deploy_date": null, "created_at": null, "last_updated_via": null, "_note": "Populated by scripts/seed-calendar.ts; updated by scripts/repoint-calendar.ts in Phase 5" }`.
  Write `apps/relayer/src/workers/stylus-deactivation-watcher.ts`:
  - Exports `function startStylusDeactivationWatcher(opts: { publicClient, stylusAddress, intervalMs: number; redis }): { stop(): void }`
  - Internal logic: `setInterval(intervalMs)` (default 24h) → `publicClient.readContract({ address: stylusAddress, abi: [{ name: 'arbitrumActivationExpiry', ... }], functionName: 'arbitrumActivationExpiry' })` → compute `daysRemaining = (expiry - now) / 86400`; for each threshold N ∈ [30, 15, 7, 1], if `daysRemaining <= N` AND `daysRemaining > (previous threshold)`, acquire Redis lock `stylus:alert-fired:T-Nd:YYYY-MM-DD` via `SET NX EX 86400`; if acquired, `await sendAlert('stylus_reactivation', { daysRemaining: N, expiryTimestamp, stylusAddress })`.
  - Wrap entire interval body in try/catch; on error, log `stylus_watcher_skipped` Pino event + do NOT throw (handles the Phase 0 "no Stylus contract deployed yet" case gracefully).
  - In Phase 0, `stylusAddress` is `null` (no Stylus deployed yet); the watcher should be `start`-able but should detect null and skip with a Pino info log `stylus_watcher_inactive` once per day. Phase 5 wires the real address.
  Write `apps/relayer/test/stylus-deactivation-watcher.test.ts` with 3 tests per behaviors above using vitest mocks for `publicClient.readContract` + `ioredis-mock` for Redis + spy on `sendAlert`.</action>
  <verify>
    <automated>pnpm --filter @call-it/relayer test -- test/stylus-deactivation-watcher.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @call-it/relayer test test/stylus-deactivation-watcher.test.ts` reports 3 passing tests, 0 failures
    - `scripts/seed-calendar.ts` includes `--setup` mode AND default-creation mode (verifiable via `grep -c "argv.setup" scripts/seed-calendar.ts` ≥ 1)
    - `scripts/repoint-calendar.ts` reads `stylus-calendar.json` AND calls `events.update` (verifiable via 2 separate greps)
    - `packages/shared/src/constants/stylus-calendar.json` exists with all 4 event_t* fields + placeholder_deploy_date field
    - `grep -c "SET NX\\|stylus:alert-fired" apps/relayer/src/workers/stylus-deactivation-watcher.ts` returns ≥ 1 (idempotency lock)
    - `grep -c "sendAlert.*stylus_reactivation" apps/relayer/src/workers/stylus-deactivation-watcher.ts` returns ≥ 1 (P0 alert wired)
    - Operator runs `pnpm tsx scripts/seed-calendar.ts --placeholder-deploy-date 2026-08-21 --dry-run` and the output lists 4 events at T-30, T-15, T-7, T-1 (operator manual verification, not automated)
  </acceptance_criteria>
  <done>Calendar seeding works in --setup + default modes; 4 events stored in stylus-calendar.json; repoint-calendar.ts is Phase 5-ready with clear comments; relayer stylus-deactivation-watcher fires P0 alerts at T-30/15/7/1 thresholds idempotently; Phase 0 null-address handling gracefully skips without crashing.</done>
</task>

<task type="auto">
  <name>Task 4: 5 operator runbooks + Better Stack dashboards doc + demo seed plan</name>
  <files>docs/runbooks/stylus-reactivation.md, docs/runbooks/multisig-promotion.md, docs/runbooks/relayer-key-rotation.md, docs/runbooks/env-diff-ritual.md, docs/runbooks/nft-twap-sanity.md, docs/runbooks/settlement-stuck.md, docs/better-stack-dashboards.md, docs/demo-seed-plan.md</files>
  <read_first>
    - .planning/phases/00-foundation/00-RESEARCH.md lines 462–470 (runbook list)
    - .planning/phases/00-foundation/00-CONTEXT.md D-14 (Better Stack 5 dashboards), D-17 (private to operator + co-signers)
    - .planning/REQUIREMENTS.md OPS-06 (5 dashboards), OPS-18 (NFT TWAP runbook), OPS-20 (demo seed plan)
    - CALL_IT_SPEC1.md §10.7 (paymaster cap operations), §10.8 (Stylus 365d + multisig), §13.7 (forceSettle 7d cooldown), §19.10 (Sepolia ≥48h staging gate)
  </read_first>
  <action>Write each runbook with concrete, executable steps (not "TBD" placeholders):
  
  **`docs/runbooks/stylus-reactivation.md`** (≥40 lines): Title "Stylus Reactivation Runbook (Pitfall 17 mitigation)". Sections: (1) Background — Stylus contracts deactivate 365d post-activation; reactivation is `cargo stylus activate <address>` or `cast send <address> "activateProgram(...)"`. (2) Alert thresholds — T-30d / T-15d / T-7d / T-1d from Calendar + Telegram (D-13). (3) Pre-flight checks — verify Stylus address from `packages/contracts/deployments/`, verify Ledger Nano available, verify ≥0.05 ETH on deployer key. (4) Execution steps — exact cargo-stylus commands with example output. (5) Post-reactivation — query `arbitrumActivationExpiry` via cast, confirm new expiry is now+365d, run `pnpm tsx scripts/repoint-calendar.ts --stylus-deploy-date <new>` to update Calendar invites. (6) Verification — `cast call <address> "isProgramActivated()"` returns true. (7) Escalation — if reactivation fails, the 48h-before-demo cutoff path (§11.6) is triggered: `proxy.upgradeTo(soliditySolidityBaselineAddress)`.
  
  **`docs/runbooks/multisig-promotion.md`** (≥40 lines): Title "Multisig Promotion Runbook (Phase 6 HARD GATE, Pitfall 6 mitigation)". Sections: (1) Pre-conditions — 3 signers identified per D-10; deployer key (Ledger Nano per D-11) is the same one operating Phases 0–5; Safe 2-of-3 already deployed via `scripts/deploy-safe.ts --execute`. (2) Ownership transfer order — CallRegistry → FollowFadeMarket → ChallengeEscrow → SettlementManager → ProfileRegistry → StylusScoreEngine proxy admin. (3) For each contract: `cast send <contract> "transferOwnership(address)" $SAFE_ADDRESS` from deployer Ledger; then a second multisig signer calls `acceptOwnership()` (Ownable2Step per CLAUDE.md). (4) Verification per contract — `cast call <contract> "owner()"` returns Safe address. (5) Failure modes — if a contract uses Ownable v5 single-step instead of Ownable2Step, halt + escalate (this is a Phase 1+ contract bug, not a runbook issue). (6) Post-promotion — `grep -r "deployer-only" .planning/` to confirm no Phase 1+ code expects deployer-key access anymore.
  
  **`docs/runbooks/relayer-key-rotation.md`** (≥30 lines): Title "Relayer KMS Key Rotation Runbook". Sections: (1) When to rotate — quarterly cadence OR on compromise suspicion OR on operator hire/exit. (2) Per-attestation-type rotation (5 keys) — operator runs `gcloud kms keys versions create --key <attestation-type> --location us-east1 --keyring attestations` to add a new key version; update `GCP_KEY_VERSION_<TYPE>` in GCP Secret Manager + Fly secrets via `flyctl secrets set GCP_KEY_VERSION_NFT_TWAP=2`; relayer reboots; `verifyKmsAddress` runs at boot to confirm the new key version produces the same address (REQUIRED — otherwise the new key would invalidate prior attestations; KMS rotation is ADDITIVE, never REPLACE). (3) Old key version is left available for verification but new signings go to the new version. (4) **#manual-rep-compensation** anchor (referenced by OPS-25 alert message link): if `RepCalculatedFallback` fires for a user, the manual compensation procedure is — query subgraph for affected (callId, user) pairs; calculate expected rep delta using the spec'd algorithm; call ProfileRegistry's emergency `setRep(user, newRep)` (Phase 4 contract supports this owner-only path); log to `docs/disputes/<date>-rep-fallback.md` for public commitment.
  
  **`docs/runbooks/env-diff-ritual.md`** (≥30 lines): Title "Env Diff Ritual (Pitfall 5 mitigation; Phase 6 mandatory)". Sections: (1) Why — Sepolia↔mainnet env drift is the highest-likelihood deploy-day footgun (Pitfall 5, Pitfall E, T-00-03). (2) Procedure (run before every Fly deploy from Phase 6 onward): `pnpm tsx scripts/env-diff.ts --source sepolia --target arbitrum-one` reports the SET-DIFFERENCE of Fly secrets + Vercel envs + GCP secrets + Subgraph deploy keys; expected differences (chain ID, RPC URL, multisig address, network-specific KMS keys, subgraph URL) are whitelisted; unexpected differences halt the deploy. (3) Manual review — operator + at least one co-signer review the diff output; both sign off via Telegram. (4) Pre-mainnet additional check: `grep -r "arbitrum-sepolia\\|421614" apps/web/.next apps/relayer/dist` returns ZERO matches in the built artifacts. **Note: `scripts/env-diff.ts` is Phase 6 scope, not Phase 0; this runbook documents the procedure so Phase 6 has a concrete spec.**
  
  **`docs/runbooks/nft-twap-sanity.md`** (≥20 lines): Title "NFT TWAP Sanity-Check Runbook (OPS-18; Phase 4 fills implementation)". Sections: (1) Background — NFT settlement paths use Alchemy `getFloorPrice` + relayer-computed 24h TWAP with ≥12 observations (CALL_IT_SPEC1.md §13.2). (2) Phase 0 skeleton — documents the sanity-check pattern: cross-check TWAP via Reservoir (defunct) → fallback to direct on-chain query via the NFT collection contract's recent sales. (3) TODO_PHASE_4 markers: the actual `scripts/nft-twap-sanity-check.ts` ships in Phase 4 when SettlementManager NFT paths land. (4) For each of the 6 spec'd NFT collections (per spec): list the collection slug + Alchemy contract address slot; Phase 4 populates the addresses.
  
  **`docs/runbooks/settlement-stuck.md`** (≥20 lines): Title "Settlement-Stuck Runbook (OPS-14, OPS-15)". Phase 0 ships this with CONCRETE 3-step diagnosis content (not TODO_PHASE_4 markers — that pattern is rejected per Blocker 2). Required sections: (1) Background — P1 `settle_stuck_25m` fires when a call has been past-expiry for >25min without a successful `settle()` call; Pitfall reference + spec §13.7. (2) Step 1: Query subgraph for the stuck callId — give the EXACT GraphQL query string `{ calls(where: {status: "pending_settle", expiry_lt: <now-1500>}) { id caller asset expiry status } }` against the call-it-sepolia studio endpoint. (3) Step 2: Diagnose via Better Stack `failed-tx-rate` dashboard — operator opens dashboard, filters by `callId == <stuckId>`, inspects `errorType` field to classify (Pyth-confidence-wide / RPC-timeout / contract-revert / out-of-gas / other). (4) Step 3: Trigger `forceSettle` after the 7-day expiry cooldown if necessary — exact `cast send <SettlementManager> "forceSettle(uint256)" <callId>` command + verification via `cast call <contract> "getCall(uint256)" <callId>` confirming `status` is now `force_settled`. (5) Document outcome in `docs/disputes/<date>-settle-stuck-<callId>.md` for public commitment per spec §18.7. Stub MUST contain concrete commands (cast/GraphQL), NOT TODO_PHASE_4 markers — checker rejects skeleton-only content.
  
  **`docs/better-stack-dashboards.md`** (≥30 lines): Title "Better Stack Dashboard Configuration (OPS-06; D-14)". Sections per dashboard:
  1. **Total TVL** — Pino source filter `event:tvl_snapshot`; Y-axis = `totalTvl` (sum of `callRegistryTvl + followFadeMarketTvl + challengeEscrowTvl` per Pitfall 3); 5-minute bucket; line chart; alert threshold: `totalTvl > 0.9 * TVL_CAP_INITIAL_USDC` fires P1 `tvl_approach` via the relayer's alert path (not directly from Better Stack).
  2. **Calls/Hour** — Pino source filter `event:call_created`; count per hour bucket; bar chart.
  3. **Settlement Latency** — Pino source filter `event:call_settled`; Y-axis = `settledAt - expiry` (seconds past expiry to settle); p50/p95/p99 lines; alert threshold: p95 > 25min triggers `settle_stuck_25m`.
  4. **Dispute Rate** — Pino source filter `event:dispute_raised`; count per day; percentage of settled calls disputed.
  5. **Failed Tx Rate** — Pino source filter `level:error AND component:relayer`; count per hour; categorized by `errorType`.
  Each section includes the EXACT Better Stack widget config (data source / filter / aggregation / chart type / time range / alert threshold) so the operator can recreate the dashboard from this doc. Include a top section: "Access control: per D-17 these dashboards are PRIVATE to operator + co-signers; do NOT enable public sharing."
  
  **`docs/demo-seed-plan.md`** (≥25 lines): Title "Sepolia Demo Seed Plan (OPS-20; Phase 6 staging input)". Sections: (1) Goal — seed ≥10 calls across all call types in Phase 6 Sepolia for ≥48h staging (success criterion #1 of Phase 6). (2) Call type matrix — Price target (BTC/USD bull, ETH/USD bear), Spread/vs (ETH outperforms BTC by 5%), Event subtypes (3 of: Snapshot proposal pass, NFT floor < $X, CEX listing, Tally proposal pass, DefiLlama TVL threshold). (3) Per-call inputs — caller (operator's Ledger or test wallet), asset, target, expiry (24h, 72h, 7d for distribution), stake ($5/$25/$100), conviction (50/70/85). (4) Seed steps — each call has its own `scripts/seed-call.ts <call-spec.json>` invocation (Phase 6 owns the script; Phase 0 owns the spec). (5) Verification — subgraph indexes all 10+ calls within 30s; OG cards render for each via /api/og/[callId].</action>
  <verify>
    <automated>test -f docs/runbooks/stylus-reactivation.md && test -f docs/runbooks/multisig-promotion.md && test -f docs/runbooks/relayer-key-rotation.md && test -f docs/runbooks/env-diff-ritual.md && test -f docs/runbooks/nft-twap-sanity.md && test -f docs/runbooks/settlement-stuck.md && test -f docs/better-stack-dashboards.md && test -f docs/demo-seed-plan.md && [ $(wc -l < docs/runbooks/stylus-reactivation.md) -ge 40 ] && [ $(wc -l < docs/runbooks/multisig-promotion.md) -ge 40 ] && [ $(wc -l < docs/runbooks/relayer-key-rotation.md) -ge 30 ] && [ $(wc -l < docs/better-stack-dashboards.md) -ge 30 ] && [ $(wc -l < docs/demo-seed-plan.md) -ge 25 ]</automated>
  </verify>
  <acceptance_criteria>
    - All 8 markdown files exist
    - Each runbook meets its minimum line count: stylus-reactivation ≥40, multisig-promotion ≥40, relayer-key-rotation ≥30, env-diff-ritual ≥30, nft-twap-sanity ≥20, settlement-stuck ≥20 (line count enforced AND content must include the 3-step diagnosis pattern: `grep -c "forceSettle\|GraphQL\|failed-tx-rate" docs/runbooks/settlement-stuck.md` returns ≥3), better-stack-dashboards ≥30, demo-seed-plan ≥25
    - `docs/runbooks/relayer-key-rotation.md` contains the `#manual-rep-compensation` anchor referenced by Plan 00-02's OPS-25 alert link (verifiable via `grep -c "manual-rep-compensation" docs/runbooks/relayer-key-rotation.md` ≥ 1)
    - `docs/better-stack-dashboards.md` documents all 5 dashboards by name: Total TVL, Calls/Hour, Settlement Latency, Dispute Rate, Failed Tx Rate
    - `docs/better-stack-dashboards.md` includes the D-17 private-access note (verifiable via `grep -c "private\\|D-17" docs/better-stack-dashboards.md` ≥ 1)
    - `docs/runbooks/multisig-promotion.md` lists all 6 contracts requiring ownership transfer (CallRegistry, FollowFadeMarket, ChallengeEscrow, SettlementManager, ProfileRegistry, StylusScoreEngine proxy admin)
  </acceptance_criteria>
  <done>8 runbook + dashboard + seed docs ship with concrete steps (not placeholders) totaling ≥250 lines; all referenced anchors (#manual-rep-compensation) exist; Phase 5+/6+/7+ procedures fully documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator local machine → Ledger Nano | USB connection; firmware version controls signing path |
| Operator local machine → Google Calendar API | OAuth refresh token in GCP Secret Manager |
| GitHub Actions runner → Relayer `/internal/test-alert` | HMAC-secret-shared (V4 Access Control) |
| GitHub Actions runner → Telegram Bot API | Bot token in GH Secrets (mirrored from GCP Secret Manager) |
| Operator local machine → Safe Transaction Service | Read-only for dry-run; write requires Ledger signature |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-27 | Spoofing | `scripts/deploy-safe.ts` deployer key | mitigate | Hardware-only signing path for `--network=arbitrum-one --execute` (env-key path explicitly rejected for mainnet per Task 1); Ledger Nano per D-11; 2-of-3 multisig structure means even compromised deployer key cannot solo-execute (Pitfall 6 mitigation). Severity: HIGH if mishandled — but bounded by 2-of-3 design. |
| T-00-28 | Spoofing | `scripts/fire-synthetic-alert.ts` HMAC secret | mitigate | RELAYER_INTERNAL_HMAC_SECRET in GCP Secret Manager + GH Secrets; replay window 5min enforced by relayer (Plan 00-02 Task 3); fresh UUID nonce per invocation. Severity: HIGH if leaked — attacker could fake alerts AND mask real outages. |
| T-00-29 | Tampering | Stylus deactivation watcher false-negative | mitigate | Two independent belts per D-13: (a) Google Calendar invites with email reminders; (b) Relayer cron `stylus-deactivation-watcher.ts` polling `arbitrumActivationExpiry` daily. Either one firing alerts the operator. Severity: CRITICAL if BOTH fail — but probability is very low because they are fully independent systems. |
| T-00-30 | Information Disclosure | Calendar event titles leak Stylus address to Google | accept | Event title contains the placeholder date + the address; Google holds this anyway via the operator's account; no additional disclosure risk beyond the operator's existing trust in Google. Severity: LOW. |
| T-00-31 | Repudiation | Multisig signers can't prove they didn't sign a malicious proposal | mitigate | Safe Transaction Service provides signature audit trail; every multisig tx is logged with each signer's address + timestamp; Telegram P0 alert fires on every multisig tx (Phase 6 wiring) so signers see what they signed. Severity: MEDIUM. |
| T-00-32 | Denial of Service | Synthetic alert CI cron fails due to Telegram outage | accept | If Telegram is down, the CI build fails — which is correct behavior (alert pipeline IS broken); operator manually re-runs after Telegram recovers. Severity: LOW — operator is in the loop; no silent failure. |
| T-00-33 | Tampering | Runbooks edited by attacker to mislead operator | mitigate | Runbooks committed to git; PRs require operator review (this is the GSD workflow); critical step counts (e.g., "transfer ownership of 6 contracts" in multisig-promotion.md) are cross-checked against ROADMAP.md + REQUIREMENTS.md so attacker would need to corrupt multiple files. Severity: MEDIUM. |
| T-00-34 | Elevation of Privilege | Google Calendar OAuth token compromise | mitigate | Token in GCP Secret Manager with IAM-bound access; scope limited to `calendar.events` (not full Calendar admin); rotation procedure documented in `relayer-key-rotation.md` (kept in same key-rotation cycle). Severity: MEDIUM. |

**No `high` severity threats remain unmitigated.** T-00-27 (deployer key compromise) is bounded by the 2-of-3 multisig structure that this very task lays down (Phase 6 closes Pitfall 6). T-00-28 (HMAC leak) is treated like any other production secret. T-00-29 (Stylus reactivation missed) has the deliberate D-13 belt-and-suspenders design.
</threat_model>

<verification>
After all tasks complete:
1. `pnpm vitest run scripts/test/fire-synthetic-alert.test.ts` reports 4 passing tests
2. `pnpm vitest run scripts/test/deploy-safe.test.ts` reports 1 unconditional + 2 gated tests (gated skipped without env)
3. `pnpm --filter @call-it/relayer test test/stylus-deactivation-watcher.test.ts` reports 3 passing tests
4. All 8 markdown files exist + meet minimum line counts (per Task 4 verify command)
5. Manual: Operator runs `pnpm tsx scripts/seed-calendar.ts --setup` then `pnpm tsx scripts/seed-calendar.ts --placeholder-deploy-date 2026-08-21` and confirms 4 Calendar events created + `stylus-calendar.json` populated
6. Manual: Operator runs `pnpm tsx scripts/deploy-safe.ts --network sepolia --execute --signer-source ledger` with Ledger plugged in; Safe deploys to Sepolia; address written to `packages/contracts/deployments/safe-sepolia.json`
7. Manual: Operator manually triggers `.github/workflows/synthetic-alert.yml` via `gh workflow run synthetic-alert.yml` and confirms (a) the workflow succeeds, (b) a Telegram message arrives in P0 channel within 60s
8. Manual: 5 Better Stack dashboards exist + show synthetic event data after `pnpm tsx scripts/fire-synthetic-alert.ts --seed-dashboards` runs
</verification>

<success_criteria>
- All 18 files in `<files_modified>` exist and pass lint
- Safe 2-of-3 deploy script + 3 tests green (1 unconditional, 2 gated by env)
- Synthetic-alert helper script + 4 tests green
- Stylus deactivation watcher + 3 tests green
- 5 runbooks ≥ documented minimum line counts; #manual-rep-compensation anchor exists
- Better Stack dashboards doc lists all 5 dashboards
- Demo seed plan covers all call types per Phase 6 success criterion #1
- `packages/shared/src/constants/stylus-calendar.json` exists as a populated placeholder ready for `seed-calendar.ts` to fill
- GitHub Actions cron workflow declared with `0 12 * * *` schedule + workflow_dispatch
</success_criteria>

<output>
After completion, create `.planning/phases/00-foundation/00-04-SUMMARY.md` per template. Record:
- Sepolia Safe address (if dry-run executed) — Phase 6 reuses same signer set on mainnet
- 4 Calendar event IDs from stylus-calendar.json (if seeded)
- First successful synthetic-alert CI cron run timestamp (proves Pitfall D loop closed)
- Confirmation that all 5 Better Stack dashboards exist + show synthetic data points (D-17 private access)
- Any surfaced Google Calendar OAuth scope issues (Open Question 6 outcome)
- Any surfaced Telegram getUpdates permission issues with the bot reading its own channel (Open Question 5 outcome)
</output>
