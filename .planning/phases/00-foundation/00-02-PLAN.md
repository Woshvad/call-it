---
phase: 00-foundation
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - apps/relayer/src/index.ts
  - apps/relayer/src/env.ts
  - apps/relayer/src/routes/health.ts
  - apps/relayer/src/routes/internal-test-alert.ts
  - apps/relayer/src/routes/admin-paymaster.ts
  - apps/relayer/src/lib/logger.ts
  - apps/relayer/src/lib/redis.ts
  - apps/relayer/src/lib/telegram.ts
  - apps/relayer/src/lib/kms-signer.ts
  - apps/relayer/src/lib/secret-manager.ts
  - apps/relayer/src/lib/der-to-viem-hex.ts
  - apps/relayer/src/workers/alerts.ts
  - apps/relayer/src/workers/paymaster-counter.ts
  - apps/relayer/src/workers/cex-heartbeat.ts
  - apps/relayer/src/workers/synthetic-event-handler.ts
  - apps/relayer/src/types.ts
  - apps/relayer/test/logger.test.ts
  - apps/relayer/test/alerts.test.ts
  - apps/relayer/test/paymaster-counter.test.ts
  - apps/relayer/test/paymaster-alert.test.ts
  - apps/relayer/test/paymaster-admin.test.ts
  - apps/relayer/test/cex-heartbeat.test.ts
  - apps/relayer/test/kms-roundtrip.test.ts
  - apps/relayer/test/health.test.ts
  - apps/relayer/test/allowlist-admin.test.ts
  - apps/relayer/test/der-to-viem-hex.test.ts
  - apps/relayer/Dockerfile
  - apps/relayer/fly.toml
  - apps/relayer/.dockerignore
autonomous: true
requirements:
  - OPS-05
  - OPS-07
  - OPS-08
  - OPS-09
  - OPS-10
  - OPS-11
  - OPS-12
  - OPS-13
  - OPS-14
  - OPS-17
  - OPS-19
  - OPS-24
  - OPS-25
  - OPS-26
  - SAFETY-15
  - SAFETY-16
  - SAFETY-17
tags:
  - relayer
  - fastify
  - kms
  - telegram
  - redis
  - alerts
user_setup:
  - service: gcp-kms
    why: "Per D-06/D-07/D-09 — 5 separate KMS keys per network in 2 GCP projects for attestation signing"
    env_vars:
      - name: GCP_PROJECT_ID
        source: "GCP Console → project picker (call-it-sepolia OR call-it-mainnet)"
      - name: GCP_LOCATION_ID
        source: "Provision keyring in us-east1 (NOT global) per Pitfall B — minimizes Fly iad → KMS latency"
      - name: GCP_KEYRING_ID
        source: "Operator creates keyring named 'attestations' in each project"
      - name: GCP_KEY_VERSION_NFT_TWAP
        source: "Created via `gcloud kms keys create nft-twap --keyring attestations --location us-east1 --purpose asymmetric-signing --default-algorithm ec-sign-secp256k1-sha256 --protection-level software`"
      - name: GCP_KEY_VERSION_DEFILLAMA
        source: "Same gcloud command with key id 'defillama'"
      - name: GCP_KEY_VERSION_CEX
        source: "Same gcloud command with key id 'cex'"
      - name: GCP_KEY_VERSION_SNAPSHOT_TALLY
        source: "Same gcloud command with key id 'snapshot-tally'"
      - name: GCP_KEY_VERSION_OAUTH_PROOF
        source: "Same gcloud command with key id 'oauth-proof'"
    dashboard_config:
      - task: "Create 2 GCP projects: call-it-sepolia and call-it-mainnet (D-09)"
        location: "GCP Console → IAM & Admin → Manage Resources"
      - task: "Enable KMS API + Secret Manager API in each project"
        location: "GCP Console → APIs & Services → Library"
      - task: "Create service account for relayer with roles roles/cloudkms.signer + roles/secretmanager.secretAccessor"
        location: "GCP Console → IAM & Admin → Service Accounts"
  - service: gcp-secret-manager
    why: "Per D-08 — non-signing secrets (Privy app secret, Alchemy key, RPC URLs, Pinata JWT, Telegram bot token, Better Stack token, Upstash REST URL/token) live in GCP Secret Manager"
    env_vars: []
    dashboard_config:
      - task: "Create secrets PRIVY_APP_SECRET, ALCHEMY_API_KEY, RPC_URL_ARBITRUM_SEPOLIA, RPC_URL_ARBITRUM_MAINNET, PINATA_JWT, TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID_P0, TELEGRAM_CHAT_ID_P1, BETTERSTACK_SOURCE_TOKEN, UPSTASH_REDIS_REST_URL, UPSTASH_REDIS_REST_TOKEN, RELAYER_INTERNAL_HMAC in each GCP project"
        location: "GCP Console → Security → Secret Manager → Create Secret"
  - service: telegram
    why: "Per D-15 — 2-channel routing (P0 + P1) for OPS-07..14 alerts"
    env_vars: []
    dashboard_config:
      - task: "Create Telegram bot via @BotFather; disable group privacy so getUpdates returns channel messages (Pitfall D, Open Question 5)"
        location: "Telegram → @BotFather → /newbot → /setprivacy → Disable"
      - task: "Create 2 Telegram channels (P0-paged + P1-digest); add bot as admin with post + read permissions"
        location: "Telegram → New Channel → Manage → Administrators"
      - task: "Record both channel IDs in GCP Secret Manager as TELEGRAM_CHAT_ID_P0 and TELEGRAM_CHAT_ID_P1"
        location: "Use @userinfobot or @raw_data_bot to read channel ID; store in GCP Secret Manager"
  - service: upstash
    why: "Per D-03 — BullMQ backing + paymaster daily counter + DefiLlama cache"
    env_vars: []
    dashboard_config:
      - task: "Create Upstash Redis database (serverless tier); pick region us-east-1 to minimize Fly iad RTT"
        location: "Upstash Console → Redis → Create Database"
      - task: "Copy REST URL + REST token; store in GCP Secret Manager"
        location: "Upstash Console → Database → Details tab"
  - service: betterstack
    why: "Per D-14 — Pino-native log storage + 5 dashboards"
    env_vars: []
    dashboard_config:
      - task: "Create Better Stack account; create a Logtail source for the relayer"
        location: "Better Stack → Logs → Sources → Connect Source → Node.js (Pino)"
      - task: "Record source token in GCP Secret Manager as BETTERSTACK_SOURCE_TOKEN"
        location: "Better Stack → Source → Settings → Source token"
  - service: fly-io
    why: "Per D-01/D-02 — relayer host in iad with always-on machines per Pattern 4"
    env_vars: []
    dashboard_config:
      - task: "Create Fly app `call-it-relayer-sepolia` and `call-it-relayer-mainnet` (separate apps per network)"
        location: "`flyctl apps create call-it-relayer-sepolia` and `flyctl apps create call-it-relayer-mainnet`"
      - task: "Inject GCP secrets via `flyctl secrets set` at deploy time (Plan 00-05 wires the GH Actions workflow)"
        location: "Plan 00-05"

must_haves:
  truths:
    - "Operator can curl the relayer's `/health` endpoint deployed to Fly iad (or locally) and receive 200 OK with JSON `{ status: 'ok' }` (OPS-24, success criterion #3)."
    - "Relayer logs structured Pino events to Better Stack via `@logtail/pino` transport with NO private keys or full Telegram tokens ever appearing in log output (OPS-05, V7 ASVS)."
    - "Relayer signing keys live in GCP KMS — the relayer process never holds private key material; a KMS round-trip test on Sepolia produces a signature that ecrecovers to the KMS-derived address (OPS-19, Pitfall 7)."
    - "5 separate KMS keys exist (NFT-TWAP, DefiLlama, CEX, Snapshot/Tally, OAuth-proof); EIP-712 domain binds `attestationType` so one key compromise cannot be replayed across paths (D-07)."
    - "Telegram alert dispatcher routes 6 P0 events (pause, dispute_raised, force_settle, rep_fallback, settle_failed, stylus_reactivation) to TELEGRAM_CHAT_ID_P0 and 3 P1 events (paymaster_80, tvl_approach, settle_stuck_25m) to TELEGRAM_CHAT_ID_P1 (D-15, OPS-07..14)."
    - "Paymaster daily-cap counter increments in Upstash Redis on every sponsored-tx attempt; counter is keyed by `paymaster:YYYY-MM-DD` with TTL 25h; alert at 80% of $50/day cap fires to P1 (SAFETY-15, SAFETY-17, OPS-10)."
    - "Admin endpoint `PATCH /admin/paymaster-cap` requires a valid GCP IAM service-account token (rejects 401 without; 200 with) — operator-only cap raises per SAFETY-16."
    - "8 CEX scraper STUB modules (Binance, Coinbase, OKX, Bybit, Kraken, Bitget, KuCoin, Upbit) each emit a `cex_scraper_alive{exchange='...'}` Pino log every 60s; the unit test asserts each stub fires its heartbeat exactly once when invoked (OPS-17)."
    - "Synthetic-event handler at `POST /internal/test-alert` accepts an HMAC-signed payload with `event`, `nonce`, `timestamp` and dispatches to the alert pipeline so the daily CI cron (Plan 00-04) can verify end-to-end (D-16, Pitfall D)."
    - "`RepCalculatedFallback` alert message contains a Markdown link to `docs/runbooks/relayer-key-rotation.md#manual-rep-compensation` (OPS-25)."
    - "Sponsored-campaign allowlist admin endpoint stub `POST /admin/allowlist` exists (returns 501 NotImplemented in Phase 0; Phase 4 adds real logic) — IAM-gated like paymaster admin (OPS-26)."
    - "D-01: Relayer hosts on Fly.io with always-on machines (auto_stop_machines=false, min_machines_running=1) so 30-min Pyth retry loops + BullMQ workers + CEX scrapers never scale to zero."
    - "D-02: Single Fly region `iad` (US-East) — lowest hops to Alchemy + Pyth Hermes for Phase 0–6; multi-region deferred."
    - "D-03: BullMQ backing + paymaster counter + DefiLlama cache use Upstash serverless Redis (pay-per-request, ~$5/mo) via REST URL + token; no idle cost."
    - "D-06: All signing keys live in GCP KMS — no `.env.*` files contain private key material; env-file-key path is forbidden by V8 ASVS + spec §10.5 KMS mandate."
    - "D-09: GCP project structure is per-network (`call-it-sepolia` + `call-it-mainnet`); the relayer's GCP_PROJECT_ID env var routes to the right project at boot; no KMS key or secret is reachable from the wrong network."
  artifacts:
    - path: apps/relayer/src/index.ts
      provides: "Fastify app with /health, /internal/test-alert, /admin/paymaster-cap, /admin/allowlist routes; loads env from GCP Secret Manager at boot"
      exports: ["buildApp", "default"]
    - path: apps/relayer/src/lib/kms-signer.ts
      provides: "viem-compatible Account wrapper around @google-cloud/kms asymmetricSign per Pattern 3 (5 separate keys per attestation type per D-07)"
      exports: ["gcpKmsAccount", "AttestationType"]
    - path: apps/relayer/src/workers/alerts.ts
      provides: "9-event Telegram dispatcher with P0/P1 routing per Pattern 5 (D-15)"
      exports: ["sendAlert", "AlertEvent", "P0_EVENTS"]
    - path: apps/relayer/src/workers/paymaster-counter.ts
      provides: "Upstash Redis counter for paymaster daily spend; 80% threshold alert trigger"
      exports: ["incrementPaymasterSpend", "getPaymasterSpend", "checkPaymasterThreshold"]
    - path: apps/relayer/fly.toml
      provides: "Fly.io always-on machine config per Pattern 4 (auto_stop_machines=false, min_machines_running=1, iad region)"
      contains: "primary_region = \"iad\""
    - path: apps/relayer/Dockerfile
      provides: "Node 22 LTS multi-stage Docker build for Fly.io deploy"
    - path: apps/relayer/src/lib/logger.ts
      provides: "Pino instance with @logtail/pino transport + redaction config for sensitive fields"
      exports: ["logger"]
  key_links:
    - from: apps/relayer/src/index.ts
      to: apps/relayer/src/lib/secret-manager.ts
      via: "boot-time fetch of all non-signing secrets from GCP Secret Manager (D-08)"
      pattern: "loadSecrets\\(\\)|getSecret\\("
    - from: apps/relayer/src/lib/kms-signer.ts
      to: "@google-cloud/kms asymmetricSign API"
      via: "Per-attestation-type cryptoKeyVersionPath; DER signature decoded via der-to-viem-hex helper"
      pattern: "kms\\.asymmetricSign\\(|cryptoKeyVersionPath\\("
    - from: apps/relayer/src/workers/alerts.ts
      to: "node-telegram-bot-api sendMessage"
      via: "P0_EVENTS set membership decides which chat ID is used"
      pattern: "bot\\.sendMessage\\("
    - from: apps/relayer/src/workers/paymaster-counter.ts
      to: apps/relayer/src/workers/alerts.ts
      via: "When daily spend crosses 0.8 × cap, dispatch 'paymaster_80' alert event"
      pattern: "sendAlert\\(['\"]paymaster_80"
    - from: apps/relayer/src/routes/internal-test-alert.ts
      to: apps/relayer/src/workers/alerts.ts
      via: "HMAC-verified synthetic event invokes sendAlert with the event name + nonce in payload"
      pattern: "verifyHmac|sendAlert\\("
    - from: apps/relayer/fly.toml
      to: apps/relayer/src/routes/health.ts
      via: "Fly health check polls /health every 30s"
      pattern: "path = \"/health\""
---

## Phase Goal

**As an** operator running the relayer service, **I want to** post a single synthetic event to `/internal/test-alert` and observe (a) a structured Pino log line in Better Stack, (b) a P0 or P1 Telegram message in the correct channel within 60 seconds, and (c) a sign-on-Sepolia round-trip from a GCP-KMS-backed viem Account that ecrecovers to the expected address, **so that** every signing and alerting path Phase 4+ relies on is proven before any contract is deployed (Pitfall 7 closed; Pitfall D mitigation pre-wired for Plan 00-04's CI cron).

<objective>
Build the relayer skeleton on Fastify 5.6.1 + Pino 9 + BullMQ + Upstash Redis with GCP-KMS-backed viem signers (5 attestation keys) + a 9-event Telegram alert dispatcher (P0/P1 channels per D-15) + Dockerfile + fly.toml for iad always-on deploy. No business logic — only the substrate every later phase consumes.

Purpose: Closes OPS-05 (Pino structured logging), OPS-07..14 (Telegram alert dispatcher for all 9 events), OPS-17 (CEX heartbeat scaffolding), OPS-19 (KMS-backed signing), OPS-24 (Fastify + /health on Fly.io), OPS-25 (RepCalculatedFallback runbook link), OPS-26 (allowlist admin endpoint stub), SAFETY-15/16/17 (paymaster daily cap counter + admin endpoint + 80% alert).

Output: A relayer service that builds locally with `pnpm --filter @call-it/relayer build`, passes all Vitest tests including the KMS round-trip (Sepolia integration test gated by env presence), exposes /health on local port 8080, and has a Dockerfile + fly.toml ready for the Plan 00-05 deploy workflow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-foundation/00-CONTEXT.md
@.planning/phases/00-foundation/00-RESEARCH.md
@.planning/phases/00-foundation/00-VALIDATION.md
@CLAUDE.md
@CALL_IT_SPEC1.md
@.planning/phases/00-foundation/00-01-SUMMARY.md

<interfaces>
Consumes from Plan 00-01:
- `@call-it/shared` exports: `USDC_ARB_NATIVE`, `ARBITRUM_MAINNET_CHAIN_ID`, `ARBITRUM_SEPOLIA_CHAIN_ID`, `EnvConfigSchema` (zod), all Pyth feed IDs
- `@call-it/config/tsconfig/node.json` — TypeScript base for Node-runtime packages
- `@call-it/config/eslint/base.js` — ESLint flat config

Establishes (consumed by Plan 00-03 polled-events fallback worker and Plan 00-04 synthetic-alert CI):
- `apps/relayer/src/workers/alerts.ts` → `export async function sendAlert(event: AlertEvent, payload: Record<string, unknown>): Promise<void>` — single dispatcher
- `apps/relayer/src/workers/alerts.ts` → `export type AlertEvent = 'pause' | 'dispute_raised' | 'force_settle' | 'rep_fallback' | 'settle_failed' | 'stylus_reactivation' | 'paymaster_80' | 'tvl_approach' | 'settle_stuck_25m'` (9 events)
- `apps/relayer/src/workers/alerts.ts` → `export const P0_EVENTS: ReadonlySet<AlertEvent>` — used by tests + by the synthetic-event handler to pick the expected chat ID
- `apps/relayer/src/lib/kms-signer.ts` → `export function gcpKmsAccount(opts: { projectId, locationId, keyRingId, keyId, keyVersion, expectedAddress }): viem.Account` — 5 instances live per Plan 00-04 deploy
- `apps/relayer/src/lib/kms-signer.ts` → `export type AttestationType = 'nft-twap' | 'defillama' | 'cex' | 'snapshot-tally' | 'oauth-proof'`
- `apps/relayer/src/lib/redis.ts` → `export function getRedis(): Redis` — single connection pool; used by paymaster counter + BullMQ
- `apps/relayer/src/lib/logger.ts` → `export const logger: pino.Logger` — root Pino instance with @logtail/pino transport and redact config
- `apps/relayer/src/lib/secret-manager.ts` → `export async function loadSecrets(): Promise<EnvConfig>` — boot-time secrets fetch; D-08

Wired but stubbed (Phase 4 fills the real logic):
- `apps/relayer/src/workers/cex-heartbeat.ts` — 8 named exchange stubs, each emits Pino log `cex_scraper_alive{exchange='binance'}` (OPS-17)
- `apps/relayer/src/routes/admin-paymaster.ts` — PATCH endpoint (SAFETY-16); Phase 4 wires real contract setPaymasterDailyCap
- `apps/relayer/src/routes/admin-allowlist.ts` — POST endpoint (OPS-26); returns 501 in Phase 0

External (out of plan scope but referenced):
- `@google-cloud/kms` 5.x: `kms.asymmetricSign({ name, digest: { sha256 } })` — returns DER-encoded (r, s)
- `@google-cloud/secret-manager` 5.x: `client.accessSecretVersion({ name: 'projects/X/secrets/Y/versions/latest' })`
- `node-telegram-bot-api`: `new TelegramBot(token, { polling: false })` + `bot.sendMessage(chatId, text, { parse_mode: 'Markdown' })`
- `@logtail/pino`: Pino transport — `import logtailTransport from '@logtail/pino'`
- `bullmq` 5.x: `new Queue(name, { connection })` + `new Worker(name, processor, { connection })`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build env loader + GCP Secret Manager + Pino logger + Upstash Redis singletons</name>
  <files>apps/relayer/src/env.ts, apps/relayer/src/lib/logger.ts, apps/relayer/src/lib/redis.ts, apps/relayer/src/lib/secret-manager.ts, apps/relayer/src/lib/telegram.ts, apps/relayer/src/types.ts, apps/relayer/test/logger.test.ts</files>
  <read_first>
    - .planning/phases/00-foundation/00-RESEARCH.md §Pattern 5 (lines 681–713) — Telegram routing structure
    - .planning/phases/00-foundation/00-RESEARCH.md §Security Domain V8 + V7 (lines 1282–1285) — secret handling + Pino redaction
    - .planning/phases/00-foundation/00-CONTEXT.md D-08, D-09 (GCP Secret Manager per-network projects)
    - .planning/phases/00-foundation/00-RESEARCH.md Pitfall A (lines 984–991) — Upstash BullMQ compatibility verification (run in Wave 0 of THIS plan)
    - .planning/phases/00-foundation/00-RESEARCH.md Pitfall B (lines 993–1001) — KMS region pinning, software-backed keys
    - packages/shared/src/schemas/env-config.ts — the EnvConfigSchema zod object from Plan 00-01
  </read_first>
  <behavior>
    - Test 1 (`apps/relayer/test/logger.test.ts`): Pino logger emits a JSON line for `logger.info({ event: 'test' }, 'hello')` with shape `{ level: 30, time, event: 'test', msg: 'hello' }`; redacted fields (TELEGRAM_BOT_TOKEN, PRIVY_APP_SECRET, RELAYER_INTERNAL_HMAC, UPSTASH_REDIS_REST_TOKEN, any key matching `*_PRIVATE_KEY`) appear as `[Redacted]` (not the raw value); structured fields (e.g., `callId`, `user`, `event`) preserved end-to-end.
    - Test 2 (`apps/relayer/test/logger.test.ts`): Calling `logger.info({ TELEGRAM_BOT_TOKEN: '12345:abcdef' }, 'oops')` produces a log line where `TELEGRAM_BOT_TOKEN` is `[Redacted]` (Pino redaction config working).
  </behavior>
  <action>Write `apps/relayer/src/types.ts` exporting a `RelayerEnv` interface mirroring `EnvConfigSchema` from `@call-it/shared` plus runtime-only fields (`GCP_PROJECT_ID`, `GCP_LOCATION_ID`, `GCP_KEYRING_ID`, the 5 KMS key versions, the Telegram chat IDs, etc.). Write `apps/relayer/src/lib/secret-manager.ts` exporting `async function loadSecrets(): Promise<RelayerEnv>` — fetches each non-signing secret via `@google-cloud/secret-manager`'s `accessSecretVersion({ name: 'projects/${GCP_PROJECT_ID}/secrets/${name}/versions/latest' })`; falls back to `process.env[name]` ONLY in local dev (detected via `NODE_ENV !== 'production'`) to allow `.env.local` for offline development; validates the resulting object via `EnvConfigSchema.parse()`; throws (with redacted error) if any required secret is missing in production. Write `apps/relayer/src/env.ts` exporting `let env: RelayerEnv; export async function initEnv() { env = await loadSecrets(); return env; }` — called once at boot from `index.ts`. Write `apps/relayer/src/lib/logger.ts` exporting a Pino instance built via `pino({ level: env.LOG_LEVEL ?? 'info', redact: { paths: ['TELEGRAM_BOT_TOKEN', 'PRIVY_APP_SECRET', 'RELAYER_INTERNAL_HMAC', 'UPSTASH_REDIS_REST_TOKEN', 'PINATA_JWT', 'BETTERSTACK_SOURCE_TOKEN', '*.privateKey', '*.private_key', 'headers.authorization', 'req.headers.authorization'], censor: '[Redacted]' }, transport: env.NODE_ENV === 'production' ? { target: '@logtail/pino', options: { sourceToken: env.BETTERSTACK_SOURCE_TOKEN } } : undefined })`. The transport is omitted in dev so logs stream to stdout as JSON. Write `apps/relayer/src/lib/redis.ts` exporting `getRedis()` which lazily creates and memoizes a Redis client via `ioredis` from `env.UPSTASH_REDIS_REST_URL` + `env.UPSTASH_REDIS_REST_TOKEN` per Upstash docs. **Critical:** include a `pingWithBullMQCompat()` smoke function that runs once at boot and (a) `PING` succeeds, (b) `XADD test-stream * field value` + `XLEN test-stream >= 1` works (proves blocking-stream commands per Pitfall A), (c) `SUBSCRIBE test-channel` + `PUBLISH test-channel msg` round-trips work. Log result; do NOT crash on failure but emit a P1 alert via the alerts module (which lands in Task 4) — placeholder via a TODO comment if the alerts module is not yet in scope. Write `apps/relayer/src/lib/telegram.ts` exporting `getBot()` lazily-initializing a `TelegramBot(env.TELEGRAM_BOT_TOKEN, { polling: false })` instance; the actual `sendAlert` lives in `workers/alerts.ts` (Task 3). Write `apps/relayer/test/logger.test.ts` per behaviors above using vitest + a custom stream to capture Pino output.</action>
  <verify>
    <automated>pnpm --filter @call-it/relayer test -- test/logger.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @call-it/relayer test test/logger.test.ts` reports 2 passing tests
    - `apps/relayer/src/lib/logger.ts` includes the exact redaction paths listed above (verifiable with `grep -c "TELEGRAM_BOT_TOKEN" apps/relayer/src/lib/logger.ts` ≥ 1)
    - `apps/relayer/src/lib/secret-manager.ts` calls `accessSecretVersion` (verifiable with `grep -c "accessSecretVersion" apps/relayer/src/lib/secret-manager.ts` ≥ 1) — confirms GCP Secret Manager usage per D-08
    - `apps/relayer/src/lib/redis.ts` includes a `pingWithBullMQCompat` function (verifiable with `grep -c "pingWithBullMQCompat" apps/relayer/src/lib/redis.ts` ≥ 1) per Pitfall A Wave 0 validation
  </acceptance_criteria>
  <done>Env loader fetches from GCP Secret Manager with local-dev fallback; Pino logger redacts all sensitive fields; Upstash Redis client + BullMQ-compat smoke function in place; Telegram client lazy-init wired.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GCP-KMS-backed viem signer (5 attestation keys) + DER-to-viem-hex helper + Sepolia round-trip integration test</name>
  <files>apps/relayer/src/lib/kms-signer.ts, apps/relayer/src/lib/der-to-viem-hex.ts, apps/relayer/test/der-to-viem-hex.test.ts, apps/relayer/test/kms-roundtrip.test.ts</files>
  <read_first>
    - .planning/phases/00-foundation/00-RESEARCH.md §Pattern 3 (lines 560–631) — full kms-signer.ts canonical example
    - .planning/phases/00-foundation/00-CONTEXT.md D-06, D-07 (KMS mandate + 5 separate keys per attestation type per Pitfall 7)
    - .planning/phases/00-foundation/00-RESEARCH.md §Don't Hand-Roll table (lines 962–977) — KMS signing wrapper guidance
    - .planning/phases/00-foundation/00-RESEARCH.md Pitfall B (lines 993–1001) — region + cost considerations
    - .planning/phases/00-foundation/00-RESEARCH.md Assumption A1 (line 1098) — viem-gcp-kms-signer library landscape verification
  </read_first>
  <behavior>
    - Test 1 (`apps/relayer/test/der-to-viem-hex.test.ts`): Given a known DER-encoded secp256k1 signature for a known digest + known address, `derToViemHex(der, digest, address)` returns the viem-style `0x{r}{s}{v}` 65-byte hex string; ecrecover on that hex returns the expected address. Use 3 hard-coded test vectors (one for each recovery bit value v ∈ {27, 28}, plus one s-value that requires low-S normalization).
    - Test 2 (`apps/relayer/test/der-to-viem-hex.test.ts`): If `derToViemHex` receives a DER signature that does NOT recover to `expectedAddress` for either v=27 or v=28, it throws `KmsSignerError('No recovery bit matches expected address')`.
    - Test 3 (`apps/relayer/test/kms-roundtrip.test.ts`, GATED): Integration test — calls `gcpKmsAccount({ ..., expectedAddress: KMS_DERIVED_ADDRESS })`, then `account.signMessage({ message: 'hello' })`, then verifies the signature via viem `recoverMessageAddress({ message, signature })`. Test is skipped (vitest `it.skipIf`) when `GCP_PROJECT_ID` env is absent — runs only in CI when secrets are injected.
  </behavior>
  <action>Write `apps/relayer/src/lib/der-to-viem-hex.ts` exporting `function derToViemHex(der: Uint8Array, digest: Hex, expectedAddress: Hex): Hex`. Decode DER (start with `0x30 <len> 0x02 <rlen> <r> 0x02 <slen> <s>`); strip leading zeros from r/s; if `s > secp256k1.curve.n / 2` then normalize via `s = n - s` (low-S per BIP-0066); try recovery bit v=27, recover address via viem's `recoverMessageAddress`-equivalent under-the-hood (use viem's `secp256k1.recoverPublicKey` + `publicKeyToAddress`); if recovered === expectedAddress return `0x${r}${s}${v.toString(16)}`; else try v=28; else throw `KmsSignerError`. Reference the noble/secp256k1 + viem internals exactly per Pattern 3 lines 600–628 of RESEARCH.md.
  Write `apps/relayer/src/lib/kms-signer.ts` exporting `type AttestationType = 'nft-twap' | 'defillama' | 'cex' | 'snapshot-tally' | 'oauth-proof'` and `function gcpKmsAccount(opts: { projectId, locationId, keyRingId, keyId: AttestationType, keyVersion, expectedAddress })`. Internally instantiate `new KeyManagementServiceClient()`; build the `cryptoKeyVersionPath`; expose `signDigest(digest)` that calls `kms.asymmetricSign({ name: versionName, digest: { sha256: hexToBytes(digest) } })`, then `derToViemHex(resp.signature, digest, expectedAddress)`. Wrap into viem's `toAccount({ address, signMessage, signTypedData })` — signMessage hashes via `hashMessage`, signTypedData via `hashTypedData`. `signTransaction` throws "Use serializeTransaction + signDigest" (transactions sign at a lower level; Phase 4 uses this for settlement tx signing). Also export a `verifyKmsAddress(opts): Promise<Address>` helper that fetches the KMS public key via `kms.getPublicKey({ name: versionName })`, converts PEM to viem `Hex`, hashes via `keccak256` → address, and asserts equality with `expectedAddress` (boot-time sanity check).
  Write `apps/relayer/test/der-to-viem-hex.test.ts` with 3 test vectors generated via viem (`signMessage` with a known private key, capture the resulting signature, convert to DER format via `@noble/curves/secp256k1`, then test the reverse). Vitest unit test; no GCP dependency.
  Write `apps/relayer/test/kms-roundtrip.test.ts` gated by `it.skipIf(!process.env.GCP_PROJECT_ID)`. Creates one `gcpKmsAccount({ keyId: 'nft-twap', keyVersion: process.env.GCP_KEY_VERSION_NFT_TWAP, ... })`, signs `'hello'`, recovers address via `recoverMessageAddress`, asserts equals `account.address`.</action>
  <verify>
    <automated>pnpm --filter @call-it/relayer test -- test/der-to-viem-hex.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @call-it/relayer test test/der-to-viem-hex.test.ts` reports ≥3 passing tests (one per recovery bit + low-S normalization vector)
    - `apps/relayer/src/lib/kms-signer.ts` exports `gcpKmsAccount` and `AttestationType` with exactly 5 type members (`nft-twap`, `defillama`, `cex`, `snapshot-tally`, `oauth-proof`) per D-07
    - `grep -c "kms\\.asymmetricSign" apps/relayer/src/lib/kms-signer.ts` returns ≥ 1
    - `grep -c "keyId: AttestationType" apps/relayer/src/lib/kms-signer.ts` returns ≥ 1 (type safety enforces per-key separation)
    - `apps/relayer/test/kms-roundtrip.test.ts` exists and uses `it.skipIf` — does not fail in CI when GCP credentials are absent
    - When `GCP_PROJECT_ID` is set, `pnpm --filter @call-it/relayer test test/kms-roundtrip.test.ts` reports 1 passing test (manual operator verification step before mainnet)
  </acceptance_criteria>
  <done>5-keys-per-AttestationType KMS signer wrapper compiled + unit-tested; DER-to-viem-hex helper handles both recovery bits + low-S normalization; Sepolia round-trip test runs when GCP creds are present and is correctly skipped when absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 9-event Telegram alert dispatcher with P0/P1 routing + paymaster counter + paymaster admin endpoint + CEX heartbeat + allowlist admin stub</name>
  <files>apps/relayer/src/workers/alerts.ts, apps/relayer/src/workers/paymaster-counter.ts, apps/relayer/src/workers/cex-heartbeat.ts, apps/relayer/src/workers/synthetic-event-handler.ts, apps/relayer/src/routes/admin-paymaster.ts, apps/relayer/src/routes/internal-test-alert.ts, apps/relayer/src/routes/admin-allowlist.ts, apps/relayer/test/alerts.test.ts, apps/relayer/test/paymaster-counter.test.ts, apps/relayer/test/paymaster-alert.test.ts, apps/relayer/test/paymaster-admin.test.ts, apps/relayer/test/cex-heartbeat.test.ts, apps/relayer/test/allowlist-admin.test.ts</files>
  <read_first>
    - .planning/phases/00-foundation/00-RESEARCH.md §Pattern 5 (lines 681–713) — 9-event AlertEvent type + P0 set + Markdown formatting
    - .planning/phases/00-foundation/00-RESEARCH.md §Pattern 9 (lines 902–944) — synthetic-event handler with HMAC verification
    - .planning/phases/00-foundation/00-CONTEXT.md D-15 (2-channel routing), D-13 (Stylus reactivation belt-and-suspenders)
    - CALL_IT_SPEC1.md §10.7 (paymaster $50/day cap; alert at 80%; SAFETY-15/16/17)
    - .planning/REQUIREMENTS.md OPS-07..14, OPS-17, OPS-25, OPS-26 (alert event → severity mapping)
    - .planning/phases/00-foundation/00-RESEARCH.md Pitfall D (lines 1012–1020) — synthetic-alert verification correctness
  </read_first>
  <behavior>
    - Test 1 (`apps/relayer/test/alerts.test.ts`): Calling `sendAlert(event, payload)` for each of the 6 P0 events (`pause`, `dispute_raised`, `force_settle`, `rep_fallback`, `settle_failed`, `stylus_reactivation`) invokes `bot.sendMessage` with `chatId === env.TELEGRAM_CHAT_ID_P0` and a message containing `🚨 P0`; for each of the 3 P1 events (`paymaster_80`, `tvl_approach`, `settle_stuck_25m`) invokes with `chatId === env.TELEGRAM_CHAT_ID_P1` and `📊 P1`. Use vitest mock for `node-telegram-bot-api`.
    - Test 2 (`apps/relayer/test/alerts.test.ts`): `sendAlert('rep_fallback', { callId: 'abc' })` produces a Markdown message containing the substring `runbooks/relayer-key-rotation.md#manual-rep-compensation` (OPS-25 link requirement).
    - Test 3 (`apps/relayer/test/paymaster-counter.test.ts`): `incrementPaymasterSpend(amountUsdc6)` with `amountUsdc6 = 1_000_000n` (1 USDC) sets Redis key `paymaster:YYYY-MM-DD` to `1000000`; subsequent increment by `2_000_000n` makes it `3000000`; TTL is between 24h and 25h (`24 * 3600 < ttl ≤ 25 * 3600`).
    - Test 4 (`apps/relayer/test/paymaster-alert.test.ts`): When cumulative daily spend crosses `40_000_000` (40 USDC, 80% of $50 cap), `checkPaymasterThreshold()` dispatches one `paymaster_80` alert; crossing again on the next increment within the same day does NOT re-fire (idempotency via Redis `SET NX` lock keyed `paymaster:alert-fired:YYYY-MM-DD`).
    - Test 5 (`apps/relayer/test/paymaster-admin.test.ts`): `PATCH /admin/paymaster-cap` with body `{ newCapUsdc6: 100_000_000n }` and a missing `Authorization` header returns 401; with a valid `Authorization: Bearer ${GCP_IAM_TOKEN}` (mocked to verify via `google-auth-library`'s ID-token verifier) returns 200 and writes the new cap to Redis key `paymaster:cap` (Phase 4 promotes this to an on-chain `setPaymasterDailyCap` call).
    - Test 6 (`apps/relayer/test/cex-heartbeat.test.ts`): The 8 exchange stubs (`binance`, `coinbase`, `okx`, `bybit`, `kraken`, `bitget`, `kucoin`, `upbit`) are exported from `cex-heartbeat.ts` as an array of `CexScraperStub` objects; invoking `emitAllHeartbeats()` emits exactly 8 Pino log lines each containing `cex_scraper_alive` with the exchange name in the structured field `{ exchange: 'binance' }` etc.
    - Test 7 (`apps/relayer/test/allowlist-admin.test.ts`): `POST /admin/allowlist` with valid IAM auth returns 501 Not Implemented in Phase 0 (stub for Phase 4) AND logs the Pino event `allowlist_admin_invoked` so we know the gate works.
  </behavior>
  <action>Write `apps/relayer/src/workers/alerts.ts` per Pattern 5 — `AlertEvent` union of the 9 events; `P0_EVENTS` set of 6 events; `sendAlert(event, payload)` resolves chat ID via P0 membership; builds Markdown text per `${tier} *${event}*\\n\\n\`\`\`json\\n${JSON.stringify(payload, null, 2)}\\n\`\`\``; for `rep_fallback`, appends `\\n\\n[Compensation runbook](https://github.com/${GITHUB_REPO}/blob/main/docs/runbooks/relayer-key-rotation.md#manual-rep-compensation)` (OPS-25). Use Fastify-isomorphic mocking via vitest's `vi.mock('node-telegram-bot-api')`.
  Write `apps/relayer/src/workers/paymaster-counter.ts` exporting `async function incrementPaymasterSpend(amountUsdc6: bigint): Promise<bigint>` and `async function getPaymasterSpend(): Promise<bigint>` and `async function checkPaymasterThreshold(): Promise<{ crossed80: boolean; current: bigint; cap: bigint }>`. The increment uses Redis `INCRBY` (atomic) on key `paymaster:YYYY-MM-DD` (UTC date) + `EXPIRE` to 25h. The threshold check reads `paymaster:cap` (default 50_000_000n if missing) and current spend; if `current >= 0.8 * cap` AND `SET NX paymaster:alert-fired:YYYY-MM-DD EX 86400` returns OK (lock acquired), dispatch `sendAlert('paymaster_80', { current, cap, ratio })`. Idempotency: only first crossing per day fires.
  Write `apps/relayer/src/routes/admin-paymaster.ts` — Fastify route `PATCH /admin/paymaster-cap` with a `preHandler` that verifies the `Authorization: Bearer <token>` header via `google-auth-library`'s `OAuth2Client.verifyIdToken({ idToken, audience })`. Reject 401 on missing or invalid; accept and write `paymaster:cap` in Redis on success.
  Write `apps/relayer/src/routes/admin-allowlist.ts` — Fastify route `POST /admin/allowlist` with the same IAM preHandler; body shape via zod; returns 501 in Phase 0 (Phase 4 adds the real allowlist contract write).
  Write `apps/relayer/src/routes/internal-test-alert.ts` — Fastify route `POST /internal/test-alert` with HMAC verification per Pattern 9: body shape `{ event: AlertEvent, nonce: string, timestamp: number }`; compute `hmac = HMAC-SHA256(RELAYER_INTERNAL_HMAC_SECRET, JSON.stringify({event, nonce, timestamp}))` and assert header `X-Internal-HMAC` equals; reject 403 on mismatch; reject 400 if `Math.abs(Date.now() / 1000 - timestamp) > 300` (5-min skew); on success call `sendAlert(event, { synthetic: true, nonce, timestamp })`.
  Write `apps/relayer/src/workers/cex-heartbeat.ts` with the 8 named stubs and `emitAllHeartbeats()` per behavior.
  Write `apps/relayer/src/workers/synthetic-event-handler.ts` exporting the handler function that the `/internal/test-alert` route calls (cleaner separation for unit testing).
  Write all 6 test files per behaviors above. Mocks: `node-telegram-bot-api` (vi.mock), `google-auth-library` (vi.mock), Redis (use `ioredis-mock` library).</action>
  <verify>
    <automated>pnpm --filter @call-it/relayer test -- test/alerts.test.ts test/paymaster-counter.test.ts test/paymaster-alert.test.ts test/paymaster-admin.test.ts test/cex-heartbeat.test.ts test/allowlist-admin.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 6 referenced test files exist; `pnpm --filter @call-it/relayer test test/alerts.test.ts test/paymaster-counter.test.ts test/paymaster-alert.test.ts test/paymaster-admin.test.ts test/cex-heartbeat.test.ts test/allowlist-admin.test.ts` reports ≥9 passing tests, 0 failures
    - `grep -c "AlertEvent" apps/relayer/src/workers/alerts.ts` returns ≥ 2 (type definition + at least one usage)
    - `grep -E "pause|dispute_raised|force_settle|rep_fallback|settle_failed|stylus_reactivation|paymaster_80|tvl_approach|settle_stuck_25m" apps/relayer/src/workers/alerts.ts | wc -l` returns ≥ 9 (all 9 events named)
    - `grep -c "P0_EVENTS" apps/relayer/src/workers/alerts.ts` returns ≥ 1 (set exists)
    - `grep -c "runbooks/relayer-key-rotation.md" apps/relayer/src/workers/alerts.ts` returns ≥ 1 (OPS-25 link)
    - `grep -c "binance.*coinbase.*okx.*bybit.*kraken.*bitget.*kucoin.*upbit" apps/relayer/src/workers/cex-heartbeat.ts` returns ≥ 1 OR each of the 8 exchanges appears at least once (use 8 separate greps)
    - `grep -c "501" apps/relayer/src/routes/admin-allowlist.ts` returns ≥ 1 (Phase 0 stub returns 501)
  </acceptance_criteria>
  <done>9-event Telegram dispatcher routes P0 (6) + P1 (3) correctly; paymaster counter increments atomically in Redis with 25h TTL; 80% threshold alert fires once per day; PATCH /admin/paymaster-cap and POST /admin/allowlist both require GCP IAM auth and gate Phase 0 stubs at 501/200; 8 CEX heartbeat stubs each emit `cex_scraper_alive{exchange}` Pino log lines; synthetic-event handler verifies HMAC + replay-window before dispatching alerts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Fastify app bootstrap + /health endpoint + Dockerfile + fly.toml + health smoke test</name>
  <files>apps/relayer/src/index.ts, apps/relayer/src/routes/health.ts, apps/relayer/Dockerfile, apps/relayer/.dockerignore, apps/relayer/fly.toml, apps/relayer/test/health.test.ts</files>
  <read_first>
    - .planning/phases/00-foundation/00-RESEARCH.md §Pattern 4 (lines 633–679) — full fly.toml example with auto_stop_machines=false + iad + health-check
    - .planning/phases/00-foundation/00-RESEARCH.md §Standard Stack — Fastify 5.6.1 + Node 22 LTS
    - .planning/phases/00-foundation/00-CONTEXT.md D-01, D-02 (Fly.io iad), D-04 (Vercel for OG fallback, NOT relayer)
    - CALL_IT_SPEC1.md §10.7 (relayer responsibilities)
  </read_first>
  <behavior>
    - Test 1 (`apps/relayer/test/health.test.ts`): A Fastify instance built via `buildApp()` responds to `GET /health` with `200` and body `{ status: 'ok', timestamp: <isoString>, version: <packageJsonVersion> }`.
    - Test 2 (`apps/relayer/test/health.test.ts`): The `/health` endpoint does NOT require any authentication (anonymous request returns 200).
  </behavior>
  <action>Write `apps/relayer/src/routes/health.ts` exporting a Fastify plugin that registers `GET /health` returning `{ status: 'ok', timestamp: new Date().toISOString(), version: process.env.npm_package_version ?? 'dev' }`. Write `apps/relayer/src/index.ts` exporting `async function buildApp(): Promise<FastifyInstance>` that:
  1. Calls `await initEnv()` (loads secrets from GCP per Task 1)
  2. Constructs `fastify({ logger: logger, trustProxy: true, bodyLimit: 1_048_576 })` — uses the Pino instance from Task 1
  3. Registers the `/health` route
  4. Registers `/internal/test-alert` (Task 3)
  5. Registers `/admin/paymaster-cap` (Task 3)
  6. Registers `/admin/allowlist` (Task 3)
  7. On startup, runs `pingWithBullMQCompat()` (Task 1); if it fails, logs `bullmq_compat_warning` Pino event AND dispatches a P1 `tvl_approach` alert with payload `{ category: 'infra', subsystem: 'redis-bullmq', message }` (temporary repurpose; Phase 1+ adds a dedicated `redis_degraded` event if needed — comment to note this)
  8. Returns the app instance; `if (require.main === module) { buildApp().then(app => app.listen({ port: env.PORT ?? 8080, host: '0.0.0.0' })) }`
  Write `apps/relayer/Dockerfile` as a multi-stage Node 22 LTS build: builder stage runs `pnpm install --frozen-lockfile --prod=false` + `pnpm --filter @call-it/relayer build` (compiles TS to `dist/`); runner stage `FROM node:22-alpine` copies `node_modules` + `dist/` only; `USER node`; `EXPOSE 8080`; `CMD ["node", "dist/index.js"]`. Build context root is the monorepo root (so pnpm workspace symlinks work); set `WORKDIR /app`.
  Write `apps/relayer/.dockerignore` excluding `node_modules`, `.git`, `.env*`, `test/`, `*.test.ts`, `dist/` (rebuilt in image), `apps/web/`, `packages/contracts/`, `packages/subgraph/` (relayer only needs `packages/shared` + `packages/config`).
  Write `apps/relayer/fly.toml` per Pattern 4 (lines 635–679 of RESEARCH.md) verbatim, with these explicit values: `app = "call-it-relayer-${NETWORK}"` (operator runs separate apps per network per D-09; `flyctl secrets set NETWORK=sepolia` etc.); `primary_region = "iad"` per D-02; `auto_stop_machines = false`; `min_machines_running = 1`; `[[services.http_checks]] path = "/health" interval = "30s"`; `[[vm]] cpu_kind = "shared" cpus = 2 memory_mb = 1024`. Add a top comment: "Phase 4 may bump memory_mb to 2048 when Playwright CEX scrapers land."
  Write `apps/relayer/test/health.test.ts` per behaviors — use `fastify().inject({ method: 'GET', url: '/health' })` to avoid binding a real port.</action>
  <verify>
    <automated>pnpm --filter @call-it/relayer test -- test/health.test.ts && pnpm --filter @call-it/relayer build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @call-it/relayer test test/health.test.ts` reports 2 passing tests
    - `pnpm --filter @call-it/relayer build` exits 0 (TypeScript compiles)
    - `apps/relayer/fly.toml` contains exactly `primary_region = "iad"`, `auto_stop_machines = false`, `min_machines_running = 1` (3 separate grep checks)
    - `apps/relayer/fly.toml` contains `path = "/health"` for the http_checks section
    - `apps/relayer/Dockerfile` uses `FROM node:22-alpine` AND has a separate builder stage (verifiable via `grep -c "FROM node:22-alpine AS" apps/relayer/Dockerfile` ≥ 1)
    - `apps/relayer/Dockerfile` includes `USER node` (non-root runtime)
    - `apps/relayer/src/index.ts` exports `buildApp` and registers all 4 routes (`/health`, `/internal/test-alert`, `/admin/paymaster-cap`, `/admin/allowlist`) — verifiable via `grep -c "register" apps/relayer/src/index.ts` ≥ 4
  </acceptance_criteria>
  <done>Fastify app builds + serves /health locally + all 4 routes registered + Dockerfile is non-root multi-stage Node 22 alpine + fly.toml has always-on iad config + the boot-time Redis BullMQ-compat smoke runs once per startup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public Internet → Relayer `/health` + `/og/*` (no-auth) | Public probe surface; must be safe to expose |
| Public Internet → Relayer `/internal/test-alert` (HMAC-gated) | CI-only; secret-shared with GH Actions |
| Public Internet → Relayer `/admin/*` (IAM-gated) | Operator-only via GCP IAM service-account ID token |
| Relayer Process → GCP KMS asymmetric-sign API | Long-lived service-account credential; rotatable, revocable |
| Relayer Process → GCP Secret Manager | Boot-time secret fetch; same service-account; no static API keys |
| Relayer Process → Upstash Redis (Upstash REST API) | Token-authenticated; counter integrity matters |
| Relayer Process → Telegram Bot API | Outbound only; bot token in GCP Secret Manager |
| Relayer Logs → Better Stack (Logtail) | Outbound only; redaction config prevents secret leakage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-08 | Spoofing | `/internal/test-alert` endpoint | mitigate | HMAC-SHA256 over `{event, nonce, timestamp}` with `RELAYER_INTERNAL_HMAC_SECRET` from GCP Secret Manager; 5-min replay window; nonce uniqueness enforced via Redis `SET NX paymaster:internal-nonce:<nonce> EX 600` (synthetic-event handler). Severity: HIGH — unverified endpoint could be used to fake alerts. |
| T-00-09 | Spoofing / Elevation | `/admin/paymaster-cap` and `/admin/allowlist` | mitigate | `google-auth-library` `verifyIdToken` with audience claim bound to the operator service-account ID; preHandler rejects 401 without valid Bearer token; mandatory per SAFETY-16 + OPS-26. Severity: HIGH — unauthorized cap raises drain protocol funds. |
| T-00-10 | Spoofing / Elevation of Privilege | Signing-key material in process memory | mitigate | GCP KMS is the only signing path; private key bytes never leave KMS; per-attestation-type key separation (D-07); EIP-712 domain binds `attestationType` so cross-path replay is impossible (Pitfall 7). Severity: CRITICAL — compromise of one key bounds blast radius to that oracle path only. |
| T-00-11 | Information Disclosure | Pino log lines | mitigate | `redact` paths list includes TELEGRAM_BOT_TOKEN, PRIVY_APP_SECRET, RELAYER_INTERNAL_HMAC, UPSTASH_REDIS_REST_TOKEN, PINATA_JWT, BETTERSTACK_SOURCE_TOKEN, `*.privateKey`, `*.private_key`, `headers.authorization`; verified by unit test that secrets log as `[Redacted]`. Severity: HIGH if missed. |
| T-00-12 | Tampering | Paymaster daily counter in Redis | mitigate | Atomic `INCRBY`; TTL 25h to ensure cleanup; idempotency lock `paymaster:alert-fired:YYYY-MM-DD` via `SET NX` prevents double-alert; admin cap-raise requires IAM auth (T-00-09). Severity: MEDIUM — counter drift causes cap drift but not direct fund loss. |
| T-00-13 | Repudiation | Failed signing operations | mitigate | Every `kms.asymmetricSign` call logs `{ event: 'kms_sign', keyId, latencyMs, success }` to Better Stack via Pino; sign failures route to P0 alert pipeline. Severity: MEDIUM — audit trail for any disputed signature. |
| T-00-14 | Denial of Service | Long-running BullMQ workers | mitigate | `auto_stop_machines = false` + `min_machines_running = 1` in fly.toml (Pattern 4); BullMQ-compat smoke at boot proves Upstash supports blocking commands (Pitfall A). Severity: MEDIUM — if Upstash silently degrades, P1 alert fires + manual swap to Fly Redis. |
| T-00-15 | Spoofing | Telegram bot token compromise | mitigate | Token in GCP Secret Manager with IAM-bound access; token rotation procedure documented in `docs/runbooks/relayer-key-rotation.md`; redaction config prevents accidental log exposure. Severity: HIGH if leaked — attacker can spoof alerts. Detection: bot abuse monitored via Better Stack uptime monitor on `/health`. |
| T-00-16 | Tampering | Supply chain on `@google-cloud/kms`, `node-telegram-bot-api`, `ioredis` | mitigate | All deps pinned with exact versions in `package.json`; `pnpm install --frozen-lockfile` in CI; `pnpm audit` step in Plan 00-05 deploy workflow; Dependabot/Renovate review required for any version bump on these specific packages. Severity: HIGH — supply-chain attack on signing path is catastrophic. |
| T-00-17 | Spoofing | Boot-time KMS public-key vs `expectedAddress` mismatch | mitigate | `verifyKmsAddress(opts)` runs at boot for every attestation-type key; relayer process refuses to start if any address mismatch (configuration drift detector). Severity: HIGH — wrong key version configured silently signs with the wrong key. |

**No `high` severity threats remain unmitigated.** T-00-08 through T-00-17 each have a code-side mitigation enforced by a unit test or boot-time check. The defense-in-depth pattern: KMS-only signing + per-attestation-type keys + EIP-712 domain binding + IAM-gated admin endpoints + HMAC-gated internal endpoints + Pino redaction + Dependabot/Renovate review for high-risk deps.
</threat_model>

<verification>
After all tasks complete:
1. `pnpm --filter @call-it/relayer test` runs all unit tests; ≥18 passing tests, 0 failures (KMS round-trip test skipped without creds is OK)
2. `pnpm --filter @call-it/relayer build` exits 0
3. `docker build -t call-it-relayer:test apps/relayer/` exits 0 (Dockerfile valid)
4. With `GCP_PROJECT_ID`, `GCP_LOCATION_ID`, `GCP_KEYRING_ID`, and 5 `GCP_KEY_VERSION_*` env vars set against a Sepolia GCP project, the KMS round-trip test passes (manual operator verification gate before Plan 00-05 deploy)
5. With `TELEGRAM_BOT_TOKEN` + `TELEGRAM_CHAT_ID_P0` + `TELEGRAM_CHAT_ID_P1` set, manually fire `curl -X POST http://localhost:8080/internal/test-alert ...` with a valid HMAC and observe a P0 message arrive in Telegram within 5 seconds (manual smoke before Plan 00-04 CI cron)
</verification>

<success_criteria>
- All 28 files in `<files_modified>` exist and pass lint
- `pnpm --filter @call-it/relayer test` reports ≥18 passing tests (logger, der-to-viem-hex, alerts × 9 events tested individually, paymaster counter, paymaster alert, paymaster admin, cex heartbeat, allowlist admin, health)
- `pnpm --filter @call-it/relayer build` produces `dist/` directory
- `docker build` against `apps/relayer/Dockerfile` succeeds with non-root final stage
- Fastify app boots locally (no GCP creds → fallback to .env.local) and responds 200 to `curl http://localhost:8080/health`
- 5 KMS attestation types are enforced at the TypeScript type level — adding a 6th would not compile without updating the `AttestationType` union (D-07 type-safety)
- Pino redaction config covers all secrets listed in the `<threat_model>` T-00-11 entry
- `fly.toml` has `auto_stop_machines = false` AND `min_machines_running = 1` AND `primary_region = "iad"` (3 separate confirmations)
</success_criteria>

<output>
After completion, create `.planning/phases/00-foundation/00-02-SUMMARY.md` per template. Record:
- Outcome of the Pitfall A BullMQ-compat smoke (Upstash sufficient OR fallback to Fly Redis needed)
- Outcome of Assumption A1 verification (which viem-gcp-kms-signer library was current OR confirmation the 50-line custom wrapper was written)
- Measured p95 latency of `kms.asymmetricSign` from Fly iad → GCP us-east1 (Pitfall B verification; should be <30ms)
- Confirmation that all 5 KMS keys were provisioned in BOTH `call-it-sepolia` and `call-it-mainnet` GCP projects (D-09)
- Confirmation that 2 Telegram channels exist + bot has admin + post + read-messages permission (Open Question 5)
- The KMS round-trip test output showing the signature recovered to the expected address
</output>
