---
phase: 04-settlementmanager-7-oracle-paths-solidity-baseline-rep-delta
plan: 09
type: execute
wave: 6
depends_on:
  - 04-06
  - 04-08
files_modified:
  - .github/workflows/cex-synthetic-ci.yml
  - docs/adr/SAFETY-57-oauth-permission-scoping.md
  - packages/shared/src/constants/addresses.ts
autonomous: false
requirements:
  - SETTLE-23
  - SETTLE-24
  - SAFETY-57
  - OPS-15
  - OPS-16

must_haves:
  truths:
    - "Weekly CI cron tests all 8 CEX scrapers with static HTML fixtures; synthetic test asserts Innovation Zone exclusion"
    - "SAFETY-57 documented: per-auth-method permission scoping is a v1 limitation; follows.read OAuth scope documented"
    - "SETTLEMENT_MANAGER_ARBITRUM_SEPOLIA in addresses.ts updated to real deployed address (post-deploy)"
    - "Phase 4 end-to-end verification checkpoint: forge test + relayer test + web build all GREEN"
  artifacts:
    - path: .github/workflows/cex-synthetic-ci.yml
      provides: "Weekly cron CI: runs all 8 CEX scraper testWithFixture functions with static HTML fixtures; fails if Innovation Zone exclusion broken or detection fails"
    - path: docs/adr/SAFETY-57-oauth-permission-scoping.md
      provides: "SAFETY-57 documented: follows.read scope requirement for Twitter/Farcaster feed; v1 limitation note; X API Basic tier reference"
  key_links:
    - from: cex-synthetic-ci.yml
      to: cex-adapter.ts
      via: "pnpm --filter @call-it/relayer test --run cex-scraper-synthetic (runs all testWithFixture functions)"
      pattern: "testWithFixture\|cex-synthetic"
---

<objective>
Wave 6: Weekly CEX CI cron + SAFETY-57 doc + address finalization + end-to-end verification checkpoint.

Purpose: Closes the last Phase 4 requirements. The weekly CEX synthetic CI is mandatory per D-02/Pitfall 10. SAFETY-57 must be documented per requirements. The operator checkpoint verifies the full end-to-end settle flow on Sepolia.

Output: cex-synthetic-ci.yml, SAFETY-57 doc, address finalization, [OPERATOR] end-to-end verification checkpoint.
</objective>

<execution_context>
@/home/.claude/get-shit-done/workflows/execute-plan.md
@/home/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-settlementmanager-7-oracle-paths-solidity-baseline-rep-delta/04-CONTEXT.md
@.planning/phases/04-settlementmanager-7-oracle-paths-solidity-baseline-rep-delta/04-RESEARCH.md

<interfaces>
<!-- From .github/workflows/ — existing CI workflow pattern -->
From .github/workflows/ci.yml or similar (pattern for cron + pnpm test):
```yaml
name: CEX Scraper Synthetic CI
on:
  schedule:
    - cron: '0 9 * * 1'  # Every Monday 09:00 UTC
  workflow_dispatch:      # Manual trigger for testing
jobs:
  cex-synthetic:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v3
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter @call-it/relayer test --run --reporter=verbose cex-binance cex-coinbase cex-okx cex-bybit cex-kraken cex-bitget cex-kucoin cex-upbit
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CEX weekly CI cron + SAFETY-57 doc + addresses.ts finalization</name>
  <files>
    .github/workflows/cex-synthetic-ci.yml,
    docs/adr/SAFETY-57-oauth-permission-scoping.md,
    packages/shared/src/constants/addresses.ts
  </files>
  <read_first>
    - .github/workflows/ (list existing workflows to match pattern — phase-gate.yml, deploy-relayer.yml; follow same structure)
    - apps/relayer/src/workers/__tests__/cex-binance.test.ts (testWithFixture test — this is the test the CI cron must run)
    - apps/relayer/src/workers/oracle-adapters/cex/ (confirm all 8 scraper test files exist after Plan 04-06)
    - packages/shared/src/constants/addresses.ts (current state — update SETTLEMENT_MANAGER_ARBITRUM_SEPOLIA placeholder)
    - .planning/phases/04-settlementmanager-7-oracle-paths-solidity-baseline-rep-delta/04-RESEARCH.md (§Pitfall 10 weekly CI cron, §SAFETY-57 OAuth scope v1 limitation)
    - .planning/STATE.md (deployed SM address from Plan 04-03 operator step)
  </read_first>
  <action>
    .github/workflows/cex-synthetic-ci.yml:
    - name: CEX Scraper Synthetic CI
    - on.schedule: cron '0 9 * * 1' (Every Monday 09:00 UTC)
    - on.workflow_dispatch (manual trigger)
    - job cex-synthetic: ubuntu-latest; checkout + pnpm setup + pnpm install --frozen-lockfile
    - Run step: pnpm --filter @call-it/relayer test --run --reporter=verbose -- cex-binance cex-coinbase cex-okx cex-bybit cex-kraken cex-bitget cex-kucoin cex-upbit
    - The testWithFixture tests (Plan 04-01 scaffold + Plan 04-06 implementation) must all pass GREEN in this workflow
    - Failure comment: "If any exchange test fails, check: 1) Exchange changed announcement page structure (update selector) 2) Innovation Zone exclusion patterns stale (update INNOVATION_ZONE_EXCLUSION_PATTERNS)"
    - Add Slack/Telegram notify on failure (optional — use existing Telegram bot if available, or GitHub notification email)

    docs/adr/SAFETY-57-oauth-permission-scoping.md:
    - Title: SAFETY-57: Per-Auth-Method Permission Scoping (v1 Limitation)
    - Status: Accepted (v1 limitation; Phase 1.5 extension planned)
    - Context: The spec calls for follow.read OAuth scope for Twitter to power "From your X" feed. In v1, permission scoping per auth method is not fully isolated — an OAuth token obtained for timeline read could theoretically be used for other API calls.
    - v1 Limitation: follows.read scope required for X API Basic tier ($100-200/mo) to access follow graph. This is not provisioned in Phase 4. Phase 1.5 will wire the full social linking + follow graph. The Twitter/Farcaster profile links (VERIFIED badges) do NOT require follows.read — only the "From your X" feed section does.
    - Security implication: Until Phase 1.5 ships with explicit scope binding, each OAuth proof should be restricted to the minimum scope (read-only, no write, no DM access). Current Privy OAuth implementation uses 'tweet.read users.read' scopes; Phase 1.5 upgrade to include 'follows.read' must be done via Privy custom scopes.
    - v1 acceptance: "From your X" feed section is not part of Phase 4 scope (it's Phase 1.5). The v1 limitation is the absence of that feature, not a security hole in existing code.
    - Reference: CLAUDE.md OAuth scope note, CALL_IT_SPEC1.md §9.8/§9.9, X API Basic tier pricing

    addresses.ts update:
    - Replace SETTLEMENT_MANAGER_ARBITRUM_SEPOLIA placeholder '0x0000...' with the real address deployed in Plan 04-03 operator step (from STATE.md / deployment output)
    - Replace FOLLOW_FADE_MARKET_ARBITRUM_SEPOLIA with new FFM v2 address from Plan 04-03
    - Add post-deploy verification comment block per PATTERNS.md pattern (on-chain assertions: settlementManager() → address, FFM v2 address, authorizedRepWriters → true)
    - Do NOT change ARBITRUM_ONE addresses (still placeholders until mainnet Phase 7.5)
  </action>
  <verify>
    <automated>cd /c/Users/woshv/Desktop/Call\ it && cat .github/workflows/cex-synthetic-ci.yml | grep "cron\|testWithFixture\|cex-" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/cex-synthetic-ci.yml exists with `cron: '0 9 * * 1'` schedule
    - cex-synthetic-ci.yml runs all 8 CEX test files (grep: `grep -c "cex-" .github/workflows/cex-synthetic-ci.yml` >= 8)
    - docs/adr/SAFETY-57-oauth-permission-scoping.md exists with follows.read scope documented
    - SAFETY-57 doc references "X API Basic tier" and "Phase 1.5" (grep: `grep "Basic tier\|Phase 1.5" docs/adr/SAFETY-57-oauth-permission-scoping.md`)
    - addresses.ts SETTLEMENT_MANAGER_ARBITRUM_SEPOLIA is not '0x0000000000000000000000000000000000000000' (real address post-deploy)
    - addresses.ts FOLLOW_FADE_MARKET_ARBITRUM_SEPOLIA updated to v2 address (not old 0x12aafa5a... address)
    - pnpm --filter @call-it/shared build exits 0
  </acceptance_criteria>
  <done>Weekly CEX CI cron created; SAFETY-57 v1 limitation documented; addresses.ts updated with real deployed addresses.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>All Phase 4 plans complete: SettlementManager + FFM v2 deployed on Sepolia, 7 oracle paths implemented, dispute system, Settled Receipt, Settled + CallerExited OG cards, /disputes/ log, provenance modal, subgraph handlers, OPS runbooks, CEX weekly CI, SAFETY-57 doc.</what-built>
  <how-to-verify>
    Full Phase 4 end-to-end verification (run each step in sequence):

    **Contract layer:**
    1. `forge test --root packages/contracts -v` — all tests GREEN (SettlementManagerTest + FfmSettlementTest + SettlementDisputeTest)
    2. `forge test --root packages/contracts --match-contract SettlementManagerForkTest --fork-url $ARB_ONE_RPC_URL -v` — mainnet fork money-path tests GREEN (ADR-0001)
    3. `forge test --root packages/contracts --match-test invariantFeeSplit --fuzz-runs 1000` — fee split invariant GREEN
    4. Verify on Sepolia: `cast call $SETTLEMENT_MANAGER_SEPOLIA "owner()(address)"` returns deployer address; `cast call $CALL_REGISTRY_SEPOLIA "settlementManager()(address)"` returns SM address

    **Relayer layer:**
    5. `pnpm --filter @call-it/relayer test --run` — all tests GREEN (pyth-adapter + defillama-adapter + cex-binance + outcome-word)
    6. `pnpm --filter @call-it/relayer build` — TypeScript exits 0

    **Frontend layer:**
    7. `pnpm --filter @call-it/web build` — Next.js exits 0
    8. `grep -rn "display.*grid\|gridTemplate" apps/web/app/call/ apps/web/app/og/ apps/web/app/disputes/` — returns 0 matches (Pitfall 15)
    9. `grep -rn "0xaf88\|0xFF97" apps/web/ apps/relayer/ packages/contracts/src/ --include="*.ts" --include="*.tsx" --include="*.sol" | grep -v "USDC_ARB_NATIVE\|constants/usdc\|constants/USDC\|// "` — returns 0 (no inline USDC literals)
    10. `grep -rn "pragma solidity" packages/contracts/src/ | grep -v "=0.8.30"` — returns 0 (only =0.8.30 pins)

    **Subgraph:**
    11. `pnpm --filter @call-it/subgraph build` — graph codegen + build exits 0

    **End-to-end Pyth demo spine:**
    12. Seed a BTC/USD price-target call on Sepolia via the web app
    13. After expiry, confirm settlement watcher enqueues the job and calls settle()
    14. Navigate to /call/[id] — verify Settled Receipt renders with outcome word + FINAL POSITIONS + provenance line
    15. Click "view oracle proof ↗" — verify provenance modal shows oracle URL + tx hash + Pyth price+confidence + truncated EIP-712 signature

    **Dispute flow:**
    16. Click "Dispute this settlement" — verify modal opens with amber styling, file upload, $5 bond line
    17. Navigate to /disputes/ — verify public log shows the call

    **OG cards:**
    18. Visit /og/{callId}?v=1 — verify X-Variant: settled header; outcome word visible
    19. Visit /og/{callId}?v=1&as=fader (if applicable) — verify FADED CORRECTLY if viewer is winning fader
  </how-to-verify>
  <resume-signal>Type "verified" with any failing step numbers, or "verified all" if everything passes. If any step fails, describe the error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions → CEX fixtures | CI reads static HTML fixtures from repo; no external network calls in test mode |
| CI → addresses.ts | Phase 4 addresses are post-deploy values; CI grep guard prevents ^0.8.x pragma and inline USDC |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09-01 | Tampering | CEX scraper selector drift (silent false negative) | mitigate | Weekly CI cron: if any scraper returns false positive or misses fixture → CI fails → alert; per-exchange isolation means one broken scraper doesn't cascade (D-02, Pitfall 10) |
| T-04-09-02 | Information Disclosure | follows.read OAuth scope too broad | accept | v1 limitation documented in SAFETY-57; follows.read not provisioned in Phase 4; Phase 1.5 will add with proper scope binding; no write scopes ever granted |
| T-04-09-03 | Tampering | addresses.ts stale after mainnet deploy | mitigate | Phase 7.5 deploy plan will update ARBITRUM_ONE addresses; placeholder addresses prevent accidental mainnet calls before deploy |
</threat_model>

<verification>
cex-synthetic-ci.yml: cron schedule + 8 CEX test runs
SAFETY-57 doc: follows.read scope v1 limitation documented
addresses.ts: real deployed addresses (not placeholders)
[OPERATOR] end-to-end checkpoint: forge test + relayer test + web build + subgraph build all GREEN; Pyth demo spine verifiable on Sepolia
</verification>

<success_criteria>
- Weekly CEX CI cron tests all 8 exchanges with testWithFixture (D-02, Pitfall 10)
- SAFETY-57 v1 limitation documented per requirement
- addresses.ts updated with real Sepolia deployed addresses
- Full end-to-end Pyth demo spine verifiable: settle → subgraph → receipt → provenance modal
- All Phase 4 requirement IDs covered across plans 01-09
</success_criteria>

<output>
After completion, create `.planning/phases/04-settlementmanager-7-oracle-paths-solidity-baseline-rep-delta/04-09-SUMMARY.md`
</output>
