---
phase: 04-settlementmanager-7-oracle-paths-solidity-baseline-rep-delta
plan: 07
type: execute
wave: 4
depends_on:
  - 04-05
files_modified:
  - apps/web/app/call/[id]/page.tsx
  - apps/web/app/call/[id]/layout.tsx
  - apps/web/lib/outcome-word.ts
  - apps/web/app/og/[callId]/route.ts
  - apps/web/app/og/duel/[challengeId]/route.ts
  - packages/ui/src/primitives/Stamp.tsx
autonomous: true
requirements:
  - UI-14
  - UI-15
  - UI-16
  - UI-17
  - UI-18
  - UI-19
  - UI-20
  - UI-21
  - UI-22
  - UI-23
  - UI-44
  - UI-45
  - UI-52
  - UI-54
  - SHARE-05
  - SHARE-06
  - SHARE-08
  - SHARE-12

must_haves:
  truths:
    - "The Settled Receipt page renders the outcome word at Syne 96px in the correct locked color (per §14.1 hex, NOT the Stamp purple token)"
    - "Stamp animation: scale 1.2→1.0 overshoot 0.98→1.0 + shadow expansion 0→4px 4px 0 {outcomeColor} (~300-400ms)"
    - "Rep delta counts up 0→±X with green/red pulse; JetBrains Mono"
    - "FINAL POSITIONS block: two-column flex (not grid); cap 20/side; sorted P&L desc; 4px accent corner brackets"
    - "Settled OG card variant 2: 1200×630, flexbox-only, outcome word ≥64px, stats row (P&L/REP/FINAL/CONVICTION)"
    - "CallerExited OG card variant 4: amber #FB923C hero, dimmed caller avatar 40%, 3 stats (TIME/SLASH/REP)"
    - "Duel OG variant 3 settled stubs filled: winner-aware (not caller-hardcoded), WINS/#E8F542, real rep deltas"
    - "Per-viewer outcome-word: winning fader sees FADED CORRECTLY + FADER WIN lozenge; viewerIsWinningFader is explicitly false when wallet is disconnected (D-09)"
    - "All OG routes: export const runtime='nodejs' first, flexbox-only, renderFallback on error"
  artifacts:
    - path: apps/web/app/call/[id]/page.tsx
      provides: "Settled/Disputed/CallerExited branch added; outcome-word hero 96px; FINAL POSITIONS; provenance line; action row; viewerIsWinningFader=false when wallet disconnected (D-09)"
    - path: apps/web/lib/outcome-word.ts
      provides: "getOutcomeWord({ outcome, fadeRealShare, repDelta, viewerIsWinningFader }); D-08 thresholds: CONTRARIAN HIT=fadeShare>=0.5, COLD CALL=delta<=3"
    - path: apps/web/app/og/[callId]/route.ts
      provides: "Variants 2 (Settled) + 4 (CallerExited) added in same route; buildSettledCard + buildCallerExitedCard; ?as=fader support"
    - path: apps/web/app/og/duel/[challengeId]/route.ts
      provides: "Settled stubs filled: winner-aware column, WINS in #E8F542, real rep deltas from subgraph"
    - path: packages/ui/src/primitives/Stamp.tsx
      provides: "Shadow expansion animation added (boxShadow 0→4px 4px 0 {color}); explicit hex color prop for CONTRARIAN HIT + COLD CALL + FADED CORRECTLY"
  key_links:
    - from: apps/web/lib/outcome-word.ts
      to: apps/web/app/call/[id]/page.tsx
      via: "getOutcomeWord() called with callData fields; returns outcome word string"
      pattern: "getOutcomeWord.*fadeRealShare.*repDelta"
    - from: apps/web/app/og/[callId]/route.ts
      to: buildSettledCard
      via: "if callData.status == 'Settled': return buildSettledCard(props); elif 'CallerExited': return buildCallerExitedCard(props)"
      pattern: "buildSettledCard\|buildCallerExitedCard"
---

<objective>
Wave 4 (parallel with Plan 04-06): Settled Receipt page + all 3 OG card variants + Stamp extension.

Purpose: The visible end of the Pyth vertical slice. After this plan, a user can navigate to /call/[id] for a settled call and see the full Settled Receipt including the 96px outcome word, stamp animation, FINAL POSITIONS, and provenance line. The Settled and CallerExited OG cards enable sharing. The duel OG fills Phase 3's deferred settled-state UAT.

Output: Updated /call/[id] page, outcome-word helper, OG variants 2+4, duel variant 3 fill, Stamp extension.
</objective>

<execution_context>
@/home/.claude/get-shit-done/workflows/execute-plan.md
@/home/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-settlementmanager-7-oracle-paths-solidity-baseline-rep-delta/04-UI-SPEC.md
@.planning/phases/04-settlementmanager-7-oracle-paths-solidity-baseline-rep-delta/04-PATTERNS.md
@.planning/phases/04-settlementmanager-7-oracle-paths-solidity-baseline-rep-delta/04-CONTEXT.md

<interfaces>
<!-- From apps/web/app/call/[id]/page.tsx — self-extension baseline -->
Status type (current page.tsx, extract line range ~76-80):
```typescript
status: 'Live' | 'CallerExited' | 'Settled' | 'Disputed';
```
Imports (current page.tsx ~33-57):
```typescript
'use client';
import { useState, useEffect } from 'react';
import { usePrivy } from '@privy-io/react-auth';
import { useAccount } from 'wagmi';
// FLEXBOX ONLY — no CSS grid (Pitfall 15)
// caller: string; // wallet address — NEVER rendered (AUTH-44)
```

From apps/web/app/og/[callId]/route.ts — the Live card builder to clone:
```typescript
export const runtime = 'nodejs'; // FIRST EXPORT — CRITICAL. Never 'edge'.
// cornerBracket() helper at ~line 140 — copy verbatim for settled card
// buildLiveCard(props) at ~line 138 — clone as buildSettledCard
function buildSettledCard(props) → same 1200×630, #09090E, 3px #E8F542 border, all flex
```

From apps/web/app/og/duel/[challengeId]/route.ts — settled stubs (Phase 3 D-11):
```typescript
// STUB CONTRACT (D-11):
//   const settled = false  ← flip to: const settled = challenge.winner !== ZERO_ADDRESS
//   "VS" in #64748B  ← flip to "WINS" in #E8F542 when settled
//   winner column: full opacity; loser: opacity 0.4 — winner must be winner-aware (not caller-hardcoded)
//   "? REP" in #94A3B8  ← replace with real repDelta values
```

From packages/ui/src/primitives/Stamp.tsx (extension needed):
```typescript
// current: animates scale 1.2→1.0 overshoot, opacity 0→1; uses StampColor token map
// needed: (a) add boxShadow animation 0 0 0 transparent → 4px 4px 0 {color}
// (b) accept explicit hex color string via color?: string prop (for CONTRARIAN HIT=#E8F542, COLD CALL=#94A3B8)
// UI-SPEC conflict note: do NOT change existing outcome-* tokens — only ADD the hex override + shadow
```

Outcome colors (LOCKED §14.1 — explicit hex, NOT Stamp tokens):
```
CALLED IT       → #4ADE80
LOUD AND WRONG  → #F87171
CONTRARIAN HIT  → #E8F542
COLD CALL       → #94A3B8
FADED CORRECTLY → #E8F542
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: outcome-word.ts helper + Stamp extension + Settled Receipt page (Settled/Disputed/CallerExited branches)</name>
  <files>
    apps/web/lib/outcome-word.ts,
    packages/ui/src/primitives/Stamp.tsx,
    apps/web/app/call/[id]/page.tsx,
    apps/web/app/call/[id]/layout.tsx
  </files>
  <read_first>
    - apps/web/app/call/[id]/page.tsx (full current file — understand the Live state structure fully before extending; identify status type, callData shape, all existing sections; sticky header pattern, existing 4-stat-row)
    - apps/web/app/call/[id]/layout.tsx (if exists — page frame wrapper)
    - packages/ui/src/primitives/Stamp.tsx (current Stamp — understand the existing scale animation; StampColor token map; how to ADD shadow without breaking existing behavior)
    - apps/web/lib/ directory (existing lib utilities — check for existing outcome helpers)
    - .planning/phases/04-settlementmanager-7-oracle-paths-solidity-baseline-rep-delta/04-UI-SPEC.md (Surface 1 layout ASCII art, outcome hero spec, FINAL POSITIONS spec, Stamp animation spec, Caller-Exited variant, Disputed variant, per-viewer D-09, animation contract §)
    - .planning/phases/04-settlementmanager-7-oracle-paths-solidity-baseline-rep-delta/04-CONTEXT.md (D-08 thresholds, D-09 per-viewer, D-10 provenance line)
    - apps/web/app/components/ (CornerBrackets, ConvictionBar — understand existing primitives before using)
  </read_first>
  <behavior>
    outcome-word.ts:
    - export function getOutcomeWord({ outcome, fadeRealShare, repDelta, viewerIsWinningFader }: OutcomeWordInput): { word: string; color: string; lozenge: string | null }
    - D-08 thresholds (from outcome-word.test.ts scaffold):
      - if viewerIsWinningFader && outcome === 'CallerLost': return { word: 'FADED CORRECTLY', color: '#E8F542', lozenge: 'FADER WIN' }
      - if outcome === 'CallerLost': return { word: 'LOUD AND WRONG', color: '#F87171', lozenge: null }
      - if outcome === 'CallerWon' && fadeRealShare >= 0.5: return { word: 'CONTRARIAN HIT', color: '#E8F542', lozenge: 'CONTRARIAN' }
      - if outcome === 'CallerWon' && repDelta <= 3: return { word: 'COLD CALL', color: '#94A3B8', lozenge: null }
      - default (CallerWon): return { word: 'CALLED IT', color: '#4ADE80', lozenge: null }
    - All colors are EXPLICIT HEX (§14.1, per UI-SPEC conflict note — NOT Stamp token map)
    - outcome-word.test.ts from Plan 04-01 must pass GREEN after this

    Stamp.tsx extension (additive):
    - Add optional `hexColor?: string` prop to Stamp
    - If hexColor provided: use hexColor for the border/shadow color instead of the StampColor token
    - Add boxShadow animation to the framer-motion variant: initial { boxShadow: '0 0 0 transparent' }, animate { boxShadow: '4px 4px 0 {hexColor}' } — transition ~300-400ms ease-out (UI-45)
    - Ensure prefers-reduced-motion CSS fallback still works (instant, no transform)
    - Do NOT change existing StampColor token map or break existing Stamp usage

    /call/[id]/page.tsx extension:
    - Import getOutcomeWord from apps/web/lib/outcome-word.ts
    - After existing Live branch: add `const isSettled = callData?.status === 'Settled' || callData?.status === 'Disputed'`; `const isCallerExited = callData?.status === 'CallerExited' && !isSettled`
    - Determine viewerIsWinningFader (D-09 — CRITICAL): viewerIsWinningFader MUST be explicitly false when no wallet is connected. The guard: viewerIsWinningFader = authenticated && !!account.address && hasFadePosition(account.address, callId) && callData.outcome === 'CallerLost'. When authenticated=false OR account.address is undefined, viewerIsWinningFader=false always. Public viewers with no connected wallet see the caller-centric outcome word (e.g. 'LOUD AND WRONG'), never 'FADED CORRECTLY'. This prevents any unauthenticated viewer from accidentally seeing the fader victory frame.
    - Call getOutcomeWord for the outcomeWord + color + lozenge
    - Settled layout (branches off existing page structure; reuses sticky header, same max-width 1024px):
      (a) 3px #2E2E42 PAGE FRAME + 4px #E8F542 corner brackets via <CornerBrackets> (UI-14)
      (b) Sticky caller header (reuse existing pattern; NO wallet address AUTH-44; handle + rep + badges)
      (c) Call statement: Syne 48px #E8E8E8; target value substring highlighted #E8F542; subline "settled N ago · settles automatically from {oracleHost} ↗" links to provenance modal (UI-15)
      (d) OUTCOME HERO: <Stamp hexColor={outcomeColor}> wrapping Syne 96px outcome word (UI-16, UI-44, UI-45); subline "by {handle} · {±X} rep" with count-up using CSS counter or requestAnimationFrame (UI-45)
      (e) STAT ROW: 4 cells FINAL VALUE / TARGET / CONVICTION / P&L; reuse 4-stat-row pattern from Live page (UI-19)
      (f) ACTION ROW: "[ SHARE THE RECEIPT → ]" accent fill + "[ VIEW ALL CALLS BY {handle} ]" outline (UI-20)
      (g) FINAL POSITIONS: flex-direction:row (NOT grid); two columns FOLLOWERS / FADERS sorted P&L desc capped 20/side; <CornerBrackets> header (UI-21)
      (h) Provenance line: "SETTLED FROM {oracleHost} at {timestamp} UTC · view oracle proof ↗" accent link (SETTLE-52 / D-10 — modal in Plan 04-08)
    - Caller-Exited variant (UI-22): amber banner ⚠ CALLER EXITED; caller subline at 40% opacity
    - Disputed variant (UI-23): outcome hero REPLACED by PENDING DISPUTE block (amber border, heading "PENDING DISPUTE" Space Grotesk 28-32px #FB923C); counter-claim thread below; dispute CTA hidden (dispute modal in Plan 04-08)
    - FLEXBOX ONLY — no CSS grid anywhere in this file (Pitfall 15)
    - AUTH-44: wallet address never rendered; only handle
    - All data: fetch from relayer /api/settle/:callId + subgraph for FINAL POSITIONS
  </behavior>
  <verify>
    <automated>cd /c/Users/woshv/Desktop/Call\ it && pnpm --filter @call-it/web build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/lib/outcome-word.test.ts (Plan 04-01 scaffold) passes GREEN: `pnpm --filter @call-it/web test --run` (or equivalent)
    - Stamp.tsx has hexColor prop and boxShadow animation (grep: `grep "hexColor\|boxShadow" packages/ui/src/primitives/Stamp.tsx`)
    - page.tsx contains getOutcomeWord import (grep: `grep "getOutcomeWord" apps/web/app/call/\\[id\\]/page.tsx`)
    - page.tsx: no display:grid (grep: `grep -n "display.*grid\|gridTemplate" apps/web/app/call/\\[id\\]/page.tsx` returns 0)
    - page.tsx: no wallet address rendered — caller address is internal only (grep: `grep "caller.*render\|{caller}" apps/web/app/call/\\[id\\]/page.tsx` returns 0 for render references)
    - page.tsx: outcome word at Syne 96px (grep: `grep "96\|syne.*96\|fontSize.*96" apps/web/app/call/\\[id\\]/page.tsx` returns hit in outcome hero section)
    - page.tsx: "FINAL POSITIONS" string present (grep: `grep "FINAL POSITIONS" apps/web/app/call/\\[id\\]/page.tsx`)
    - pnpm --filter @call-it/web build exits 0
    - CONTRARIAN HIT color is #E8F542 in outcome-word.ts, NOT #A855F7 (purple) (grep: `grep "A855F7\|purple" apps/web/lib/outcome-word.ts` returns 0)
    - page.tsx: viewerIsWinningFader is explicitly false when wallet is disconnected (D-09 compliance — public viewers see caller-centric word, never 'FADED CORRECTLY'). The viewerIsWinningFader assignment must guard on authentication state: `grep "viewerIsWinningFader" apps/web/app/call/\\[id\\]/page.tsx` returns a line that includes authenticated or account.address in the condition (NOT a bare boolean assignment without auth check)
  </acceptance_criteria>
  <done>outcome-word.ts implements D-08 thresholds matching scaffold tests (GREEN); Stamp extended with shadow animation + hex override; Settled Receipt page renders all 3 variants (Settled/Disputed/CallerExited) with correct colors, Syne 96px, FINAL POSITIONS flex layout; viewerIsWinningFader explicitly false when wallet disconnected (D-09 — no public viewer sees FADED CORRECTLY).</done>
</task>

<task type="auto">
  <name>Task 2: Settled OG variants 2+4 + Duel OG variant 3 settled stub fill</name>
  <files>
    apps/web/app/og/[callId]/route.ts,
    apps/web/app/og/duel/[challengeId]/route.ts
  </files>
  <read_first>
    - apps/web/app/og/[callId]/route.ts (full current file — understand buildLiveCard, cornerBracket helper, renderFallback, NodeJS runtime, all current viem reads; clone buildLiveCard as buildSettledCard)
    - apps/web/app/og/duel/[challengeId]/route.ts (full current file — identify Phase 3 D-11 stub lines: `const settled = false`, the "VS"/"WINS" section, opacity settings, "? REP" stub; winner column is currently caller-hardcoded)
    - apps/web/lib/outcome-word.ts (Task 1 — import getOutcomeWord for settled card color selection)
    - .planning/phases/04-settlementmanager-7-oracle-paths-solidity-baseline-rep-delta/04-UI-SPEC.md (Surface 2 Settled OG layout ASCII art, Surface 3 CallerExited OG, Surface 7 Duel fill instructions, OG outcome-word size note: ≥64px one-line fit, fader ?as=fader card, 200px readability gate)
    - .planning/phases/04-settlementmanager-7-oracle-paths-solidity-baseline-rep-delta/04-PATTERNS.md (§OG route buildSettledCard, §renderFallback pattern, §cache-bust ?v= param, §duel variant-3 fill instructions)
  </read_first>
  <action>
    /og/[callId]/route.ts additions (same file, branches on callData.status):
    - Keep `export const runtime = 'nodejs'` as FIRST export (already present — DO NOT move)
    - Check if ?as=fader query param: isViewerFader = searchParams.get('as') === 'fader' (D-09)
    - After buildLiveCard: add buildSettledCard(props: SettledCardProps): ReactElement
      Layout (1200×630, all h('div', { style: { display: 'flex', ... } })):
      (a) 3px #E8F542 border, 4x cornerBracket() calls
      (b) Header row: "CALL IT" Syne 40px #E8F542 left; "[SETTLED]" badge + "⬢ ARB" right
      (c) Caller line: "@{handle} · {conviction}% conviction · current rep {rep}" Space Grotesk 20px #94A3B8
      (d) OUTCOME WORD: Syne ~88-96px (fit to one line; if LOUD AND WRONG overflows at 96px drop to fit); outcomeColor from getOutcomeWord; CONTRARIAN lozenge if applicable; NEVER shows FADED CORRECTLY on default card (D-09)
      (e) Call statement: Syne 28-32px #F1F5F9
      (f) Stats row (flex, 4 cells): P&L / REP CHANGE / FINAL / CONVICTION; P&L + rep green/red per sign; mono
      (g) Footer: caller avatar 96px + "@{handle} · {rep} rep" left; "callitapp.xyz · Be right in public." right
      (h) If isViewerFader && outcome === CallerLost: render FADED CORRECTLY variant (accent, FADER WIN lozenge, fader's P&L) instead
    - Add buildCallerExitedCard(props: CallerExitedCardProps): ReactElement
      (a) 3px #E8F542 border, corner brackets
      (b) "CALL IT" header, no SETTLED badge (just ⬢ ARB)
      (c) Hero: "CALLER EXITED" Syne ~88px #FB923C amber
      (d) Call statement Syne 28-32px #F1F5F9
      (e) Stats row 3 cells: TIME BEFORE EXIT / STAKE SLASHED / REPUTATION IMPACT; slash + rep in #F87171
      (f) Caller avatar at 40% opacity (per SHARE-08); "@{handle}" below dimmed
      (g) Note copy: "Call continues for followers and faders. Settles at {expiry}." Space Grotesk 16px #94A3B8
      (h) Footer brand
    - In GET handler: branch on callData.status — 'Settled' or 'Disputed' → buildSettledCard; 'CallerExited' → buildCallerExitedCard; default → existing buildLiveCard
    - X-Variant header: 'settled', 'caller-exited', 'live' per branch
    - renderFallback on any data error (never 500)
    - NO display:grid anywhere in new builders (grep guard)

    /og/duel/[challengeId]/route.ts fill (Phase 3 D-11 stubs):
    - Find `const settled = false` → change to `const settled = challenge.winner !== '0x0000000000000000000000000000000000000000'`
    - Make winner column winner-aware: the existing code may hardcode caller column as winner — fix: `const callerIsWinner = challenge.winner === challenge.caller`; if callerIsWinner → caller column full opacity + WINS text; else → challenger column full opacity + WINS text
    - Flip "VS" text to "WINS" when settled + color #E8F542; losing column opacity 0.4
    - Winner handle color → #E8F542 when settled
    - Replace "? REP" stub with real rep delta values: fetch from subgraph RepCalculated events for this challengeId (or from relayer /api/duels/:id/live-state); winner delta in #4ADE80, loser in #F87171
    - Set X-Variant: 'duel-settled' when challenge.winner !== ZERO_ADDRESS
    - Keep ALL existing flexbox-only layout unchanged (only data + conditional rendering changes)
  </action>
  <verify>
    <automated>cd /c/Users/woshv/Desktop/Call\ it && grep -n "display.*grid\|gridTemplate" apps/web/app/og/\\[callId\\]/route.ts apps/web/app/og/duel/\\[challengeId\\]/route.ts 2>/dev/null; echo "grid check (should be empty)"</automated>
  </verify>
  <acceptance_criteria>
    - export const runtime = 'nodejs' is FIRST export in /og/[callId]/route.ts (grep: `head -5 apps/web/app/og/\\[callId\\]/route.ts | grep "runtime"`)
    - buildSettledCard function exists in route.ts (grep: `grep "buildSettledCard" apps/web/app/og/\\[callId\\]/route.ts`)
    - buildCallerExitedCard function exists (grep: `grep "buildCallerExitedCard" apps/web/app/og/\\[callId\\]/route.ts`)
    - No display:grid in either OG route file (grep returns 0)
    - ?as=fader support in route.ts (grep: `grep "as.*fader\|isViewerFader" apps/web/app/og/\\[callId\\]/route.ts`)
    - /og/duel/[challengeId]/route.ts: settled=false stub removed; callerIsWinner variable present; "WINS" string in settled branch (grep: `grep "callerIsWinner\|WINS\|settled.*winner" apps/web/app/og/duel/\\[challengeId\\]/route.ts`)
    - duel route: "? REP" stub replaced (grep: `grep "? REP" apps/web/app/og/duel/\\[challengeId\\]/route.ts` returns 0)
    - pnpm --filter @call-it/web build exits 0
    - CALLER EXITED color is #FB923C amber in buildCallerExitedCard (grep: `grep "FB923C" apps/web/app/og/\\[callId\\]/route.ts`)
    - Caller avatar in buildCallerExitedCard: opacity 0.4 reference (grep: `grep "0.4\|opacity.*40" apps/web/app/og/\\[callId\\]/route.ts`)
  </acceptance_criteria>
  <done>Settled OG variant 2 (buildSettledCard) and CallerExited OG variant 4 (buildCallerExitedCard) added to existing route; ?as=fader fader-win card supported; duel variant 3 stubs filled (winner-aware, WINS, real rep deltas); no display:grid anywhere; renderFallback on error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Connected wallet → per-viewer outcome | Browser wallet address compared to call participants — client-side only; no server-side auth needed for this comparison |
| OG route → Arbiscan | tx hash link — user clicks out to Arbiscan; no auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07-01 | Information Disclosure | Wallet address rendered in OG card | mitigate | AUTH-44 mandate: caller address internal-only; OG card shows @handle only; grep guard applied |
| T-04-07-02 | Tampering | display:grid in Satori OG routes | mitigate | Pitfall 15: CI grep guard + explicit flexbox-only comment in each OG builder; misrender is silent (Satori falls back to unknown behavior) |
| T-04-07-03 | Tampering | CONTRARIAN HIT using wrong purple color | mitigate | outcome-word.ts uses explicit #E8F542 hex (§14.1); Stamp hexColor override bypasses the stale #A855F7 token; UI-SPEC conflict resolution documented |
| T-04-07-04 | Tampering | Duel OG winner-hardcoded as caller | mitigate | callerIsWinner boolean introduced; winner-aware opacity + WINS text prevents false winner display when challenger wins |
| T-04-07-05 | Tampering | Unauthenticated viewer sees FADED CORRECTLY | mitigate | viewerIsWinningFader=false when wallet disconnected (D-09); guard requires authenticated AND account.address AND actual fade position; acceptance criterion grep gate enforces the auth check is in the assignment |
</threat_model>

<verification>
outcome-word.test.ts (Plan 04-01 scaffold) passes GREEN
Settled Receipt page renders three branches (Settled/Disputed/CallerExited)
96px Syne outcome word with correct §14.1 color
FINAL POSITIONS flex-direction:row, capped 20/side
OG variants 2+4 exist in same route file
duel OG winner-aware (not caller-hardcoded)
No display:grid in any modified file
pnpm --filter @call-it/web build exits 0
viewerIsWinningFader=false when wallet disconnected (D-09 — public viewers see caller-centric word only)
</verification>

<success_criteria>
- Settled Receipt page: 3px page frame, outcome hero 96px Syne §14.1 colors, Stamp animation with shadow expansion, rep count-up, FINAL POSITIONS, provenance line
- OG variant 2 (Settled) + variant 4 (CallerExited): flexbox-only, correct colors, 200px readability self-check
- Duel OG variant 3: settled stubs filled, winner-aware, WINS text, real rep deltas
- outcome-word.ts: D-08 thresholds implemented (fadeShare>=0.5=CONTRARIAN HIT, delta<=3=COLD CALL)
- Stamp extended with shadow animation + explicit hex color override
- Per-viewer fader card: ?as=fader renders FADED CORRECTLY with #E8F542
- viewerIsWinningFader explicitly false when wallet disconnected — public viewers never see FADED CORRECTLY (D-09)
</success_criteria>

<output>
After completion, create `.planning/phases/04-settlementmanager-7-oracle-paths-solidity-baseline-rep-delta/04-07-SUMMARY.md`
</output>
