---
phase: 04-settlementmanager-7-oracle-paths-solidity-baseline-rep-delta
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/interfaces/ISettlementManager.sol
  - packages/contracts/src/FollowFadeMarket.sol
  - packages/contracts/src/interfaces/IFollowFadeMarket.sol
  - packages/contracts/src/SettlementManager.sol
  - packages/contracts/src/ProfileRegistry.sol
autonomous: true
requirements:
  - SETTLE-01
  - SETTLE-02
  - SETTLE-03
  - SETTLE-04
  - SETTLE-05
  - SETTLE-06
  - SETTLE-07
  - SETTLE-08
  - SETTLE-09
  - SETTLE-10
  - SETTLE-11
  - SETTLE-12
  - SETTLE-41
  - SETTLE-42
  - SETTLE-43
  - SETTLE-44
  - SETTLE-45
  - SETTLE-46
  - SETTLE-47
  - SETTLE-48
  - SETTLE-49
  - SETTLE-50
  - SETTLE-51
  - SETTLE-39
  - SETTLE-40
  - SETTLE-25
  - SETTLE-26
  - SETTLE-27
  - SETTLE-28
  - SETTLE-29
  - SETTLE-30
  - SETTLE-31
  - SETTLE-32
  - SETTLE-33
  - SETTLE-34
  - SETTLE-35
  - SETTLE-36
  - SETTLE-37
  - SETTLE-38
  - REP-03
  - REP-04
  - REP-05
  - REP-06
  - REP-07
  - REP-08
  - REP-09
  - REP-10
  - REP-11
  - REP-12
  - REP-13
  - REP-14
  - REP-15
  - REP-16
  - REP-22
  - REP-23
  - REP-25
  - REP-26
  - REP-27
  - SAFETY-57

must_haves:
  truths:
    - "settle(callId, pythUpdateData) is permissionless, idempotent, atomic across all 14 steps, and reverts on any step failure"
    - "FollowFadeMarket.claimPayout correctly computes pro-rata payouts after applySettlement; CEI order enforced (claimed=true BEFORE safeTransfer)"
    - "ProfileRegistry.updateAfterSettlement increments settledCalls/wins/losses (no longer a stub)"
    - "_solidityBaselineRepDelta is in-contract: linear conviction scale, no 2x asymmetry, contrarian=1.0 (REP-22 lower fidelity)"
    - "Stylus try/catch seam is in-contract: on revert, RepCalculatedFallback emitted, settlement still completes"
    - "Dispute system on-chain: $5 bond custody, MAX_COUNTER_CLAIMS=3, DisputeWindowClosed revert after 24h, resolveDispute reversal re-distributes USDC"
    - "forceSettle gated by FORCE_SETTLE_COOLDOWN=7 days, emits CallForceSettled + CallSettled"
    - "1.7% fee extraction in FFM.applySettlement: 1.0% protocol + 0.4% creator + 0.3% LP into winning reserve; empty-pool → treasury (CALL-41)"
  artifacts:
    - path: packages/contracts/src/interfaces/ISettlementManager.sol
      provides: "LOCKED interface: enums (OracleAdapter, DisputeStatus), events (CallSettled, DisputeRaised, DisputeResolved, CallForceSettled, SettlementDelayed, RepCalculated, RepCalculatedFallback), errors, function signatures"
      exports: [ISettlementManager]
    - path: packages/contracts/src/SettlementManager.sol
      provides: "14-step settle(), forceSettle, raiseDispute, resolveDispute, _solidityBaselineRepDelta, Pyth dispatch, attestation verify, owner-settable adapterMap, USDC bond custody, receive() for ETH"
    - path: packages/contracts/src/FollowFadeMarket.sol
      provides: "REDEPLOYED: adds setSettlementManager, applySettlement, real claimPayout, getFadeRealReserve; replaces ClaimRequiresSettlement stub"
    - path: packages/contracts/src/ProfileRegistry.sol
      provides: "updateAfterSettlement stub body replaced with real settledCalls++/wins/losses implementation"
  key_links:
    - from: SettlementManager.settle()
      to: FollowFadeMarket.applySettlement()
      via: "step 11 call — atomically extracts 1.7% fees in same tx"
      pattern: "ffm.applySettlement.*callId.*protocolFee.*creatorFee.*lpFee"
    - from: SettlementManager.settle()
      to: IStylusScoreEngine.compute_rep_change()
      via: "step 8 try/catch seam — Stylus call wraps baseline fallback"
      pattern: "try.*stylusScoreEngine.compute_rep_change.*catch.*_solidityBaselineRepDelta"
    - from: SettlementManager.settle()
      to: ChallengeEscrow.settleDuel()
      via: "step 9 duel loop — ALL accepted duels for callId (Phase 3 D-09)"
      pattern: "for.*acceptedChallenges.*challengeEscrow.settleDuel"
---

<objective>
Wave 1 (parallel with Plan 04-01): Implement the SettlementManager contract, the FFM redeploy with real claimPayout, and the ProfileRegistry updateAfterSettlement stub body.

Purpose: This is the Pyth-spine contract foundation. After this plan, the 14-step settle() exists on-chain (Sepolia), the FFM pull-pattern claimPayout works, and the Solidity baseline rep delta is in-contract so the 48h Stylus cutoff is mechanical. Directly enables Plan 04-03 (deploy + wire) and Plan 04-04 (Pyth relayer).

Output: ISettlementManager.sol, SettlementManager.sol, FollowFadeMarket.sol (redeploy), ProfileRegistry.sol (stub filled).
</objective>

<execution_context>
@/home/.claude/get-shit-done/workflows/execute-plan.md
@/home/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-settlementmanager-7-oracle-paths-solidity-baseline-rep-delta/04-CONTEXT.md
@.planning/phases/04-settlementmanager-7-oracle-paths-solidity-baseline-rep-delta/04-RESEARCH.md
@.planning/phases/04-settlementmanager-7-oracle-paths-solidity-baseline-rep-delta/04-PATTERNS.md

<interfaces>
<!-- From packages/contracts/src/ChallengeEscrow.sol — primary analog for imports, CEI, modifiers -->
Import pattern (ChallengeEscrow.sol lines 1-56):
```solidity
pragma solidity =0.8.30;
import { IERC20 } from "@openzeppelin/contracts/token/ERC20/IERC20.sol";
import { SafeERC20 } from "@openzeppelin/contracts/token/ERC20/utils/SafeERC20.sol";
import { ReentrancyGuard } from "@openzeppelin/contracts/utils/ReentrancyGuard.sol";
import { Ownable2Step, Ownable } from "@openzeppelin/contracts/access/Ownable2Step.sol";
import { Pausable } from "@openzeppelin/contracts/utils/Pausable.sol";
import { USDC_ARB_NATIVE } from "./constants/USDC.sol";
```

CEI pattern (ChallengeEscrow.sol lines 235-243):
```solidity
// ── EFFECTS: ALL state writes BEFORE safeTransfer (CEI) ──
claimed[callId][msg.sender] = true;
// ── INTERACTIONS ──
IERC20(USDC_ARB_NATIVE).safeTransfer(recipient, amount);
```

setSettlementManager pattern (ChallengeEscrow.sol lines 375-379):
```solidity
function setSettlementManager(address newManager) external onlyOwner {
    require(newManager != address(0), "invalid-manager");
    settlementManager = newManager;
    emit SettlementManagerSet(newManager);
}
```

From packages/contracts/src/CallRegistry.sol:
```solidity
function markSettled(uint256 callId, ICallRegistry.Outcome outcome) external onlySettlementManager { ... }
function getCall(uint256 callId) external view returns (Call memory) { ... }
// activeDuplicateHashes is public mapping — accessible for step 12 clear
```

From packages/contracts/src/interfaces/IChallengeEscrow.sol:
```solidity
interface IChallengeEscrow {
    function settleDuel(uint256 challengeId, address winner) external; // onlySettlementManager
    function setSettlementManager(address newManager) external;
}
```

From packages/contracts/src/FollowFadeMarket.sol (current stub — lines 480-482):
```solidity
function claimPayout(uint256 /*callId*/) external nonReentrant {
    revert ClaimRequiresSettlement();
}
```
FFM existing state: followReserve[callId], fadeReserve[callId], fadeSeedVirtual[callId],
followShares[callId][user], fadeShares[callId][user], followTotalShares[callId],
fadeTotalShares[callId], claimed[callId][user], callerVolumeAtExit[callId]

From packages/contracts/src/ProfileRegistry.sol (stub — updateAfterSettlement emits only):
```solidity
function updateAfterSettlement(address caller, uint8 category, bool isCaller, bool isWinner, int256 repDelta) external onlyAuthorized {
    emit ProfileUpdated(caller); // STUB — Phase 4 implements
}
```

From packages/subgraph/abis/ ChallengeEscrow.json (ABI export pattern — add SettlementManager.json analogously)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ISettlementManager interface + FFM redeploy (applySettlement + real claimPayout + getFadeRealReserve)</name>
  <files>
    packages/contracts/src/interfaces/ISettlementManager.sol,
    packages/contracts/src/FollowFadeMarket.sol,
    packages/contracts/src/interfaces/IFollowFadeMarket.sol
  </files>
  <read_first>
    - packages/contracts/src/interfaces/IChallengeEscrow.sol (full file — interface structure, enum+struct+events+errors+functions pattern with NatSpec)
    - packages/contracts/src/FollowFadeMarket.sol (full file — all existing state, claimPayout stub at line 480, callerVolumeAtExit, fadeSeedVirtual, the _checkTvlCap internal)
    - packages/contracts/src/interfaces/IFollowFadeMarket.sol (current — add applySettlement, setSettlementManager, getFadeRealReserve)
    - .planning/phases/04-settlementmanager-7-oracle-paths-solidity-baseline-rep-delta/04-RESEARCH.md (§Fee Extraction Design, §Pull-pattern claimPayout, §FFM Redeploy Decision REQUIRED but MINIMAL)
    - .planning/phases/04-settlementmanager-7-oracle-paths-solidity-baseline-rep-delta/04-PATTERNS.md (§ISettlementManager.sol, §FFM REDEPLOY additions)
  </read_first>
  <behavior>
    ISettlementManager.sol:
    - pragma =0.8.30 EXACT; LOCKED header banner (same as IChallengeEscrow pattern)
    - enum OracleAdapter { Pyth, NftTwap, DefiLlama, RpcMetrics, Snapshot, Tally, CexScraper }
    - enum DisputeStatus { Open, Resolved }
    - struct DisputeRecord { address disputer; bytes32 evidenceHash; uint256 bondAmount; uint256 windowCloseAt; DisputeStatus status; uint8 counterClaimCount; address[] counterClaimers; }
    - events: CallSettled(indexed uint256 callId, uint8 outcome, int256 priceDelta), DisputeRaised(indexed uint256 callId, indexed address disputer, bytes32 evidenceHash), DisputeResolved(indexed uint256 callId, uint8 finalOutcome), CallForceSettled(indexed uint256 callId, uint8 outcome), SettlementDelayed(indexed uint256 callId, string reason, uint256 retryAt), RepCalculated(indexed uint256 callId, indexed address caller, uint128 currentRep, uint8 conviction, uint8 consensusPct, bool isWinner, uint256 baseValue, int256 delta), RepCalculatedFallback(indexed uint256 callId, indexed address caller, int256 baselineDelta, bytes lowLevelError)
    - errors: AlreadySettled(), CallNotExpired(), CallNotLive(), DisputeWindowClosed(), DisputeAlreadyRaised(), MaxCounterClaimsReached(), ForceSettleCooldownActive(), InsufficientEthForPythFee(), InvalidAttestation(), NotSettlementManager(), DisputeBondInsufficient()
    - functions: settle(uint256 callId, bytes[] calldata pythUpdateData) external; forceSettle(uint256 callId) external; raiseDispute(uint256 callId, bytes32 evidenceHash) external; counterClaim(uint256 callId, bytes32 evidenceHash) external; resolveDispute(uint256 callId, uint8 finalOutcome) external; setAdapterMap(uint8 marketType, uint8 eventSubtype, OracleAdapter adapter) external; receive() external payable; // NatSpec on each

    IFollowFadeMarket.sol additions (additive — DO NOT remove existing functions):
    - function setSettlementManager(address newManager) external;
    - function applySettlement(uint256 callId, uint8 outcome, uint256 protocolFeeAmt, uint256 creatorFeeAmt, uint256 lpFeeAmt) external;
    - function getFadeRealReserve(uint256 callId) external view returns (uint256);
    - error SettlementAlreadyApplied() (add to errors section)

    FollowFadeMarket.sol modifications (REDEPLOY — additive changes only to existing contract):
    - Add state: address public settlementManager; mapping(uint256 => bool) public settlementApplied;
    - Add setSettlementManager per existing ChallengeEscrow pattern (owner-only, zero-guard)
    - Add modifier onlySettlementManager (same as ChallengeEscrow)
    - Add getFadeRealReserve(uint256 callId) external view returns (uint256):
      return fadeReserve[callId] > fadeSeedVirtual[callId] ? fadeReserve[callId] - fadeSeedVirtual[callId] : 0;
    - Add applySettlement(uint256 callId, uint8 outcome, uint256 protocolFeeAmt, uint256 creatorFeeAmt, uint256 lpFeeAmt) external onlySettlementManager nonReentrant:
      - Guard: if (settlementApplied[callId]) revert SettlementAlreadyApplied()
      - Compute totalPool = followReserve[callId] + getFadeRealReserve(callId)
      - If getFadeRealReserve(callId) == 0: CALL-41 path — route entire followReserve (minus fees) to treasury; fadeSeedVirtual[callId] = 0
      - Normal path: transfer (protocolFeeAmt + creatorFeeAmt) to treasury; if outcome == CallerWon: followReserve[callId] += lpFeeAmt; else: fadeReserve[callId] += lpFeeAmt; fadeSeedVirtual[callId] = 0
      - CEI: settlementApplied[callId] = true BEFORE all transfers
      - emit SettlementApplied(callId, outcome, totalPool)
    - Replace claimPayout stub with real implementation per RESEARCH.md pull-payout pattern:
      - No whenNotPaused (pause carve-out §10.3)
      - nonReentrant
      - Read callRegistry.getCall(callId): require status is Settled or Disputed
      - Determine winner side from outcome; look up userShares + totalShares + winningReserve
      - if (userShares == 0) revert NoPayoutAvailable()
      - CEI: claimed[callId][msg.sender] = true BEFORE safeTransfer
      - payout = Math.mulDiv(userShares, winningReserve, totalShares)
      - safeTransfer(msg.sender, payout); emit PayoutClaimed(callId, msg.sender, payout)

    USDC_ARB_NATIVE import from ./constants/USDC.sol in FFM — NEVER inline the literal address
  </behavior>
  <verify>
    <automated>cd /c/Users/woshv/Desktop/Call\ it && forge build --root packages/contracts 2>&1 | grep -E "^Error|compilation error" | grep -v "SettlementManager.sol" | head -20; echo "ISettlementManager + FFM compile check"</automated>
  </verify>
  <acceptance_criteria>
    - ISettlementManager.sol exists with pragma =0.8.30, all 8 events, all 10+ errors, 7 function signatures
    - forge build on contracts compiles ISettlementManager.sol and FollowFadeMarket.sol without errors
    - IFollowFadeMarket.sol includes setSettlementManager, applySettlement, getFadeRealReserve
    - FollowFadeMarket.sol: claimPayout no longer reverts ClaimRequiresSettlement (grep: `grep "ClaimRequiresSettlement" packages/contracts/src/FollowFadeMarket.sol` returns 0 in the function body)
    - FollowFadeMarket.sol: claimed[callId][msg.sender] = true BEFORE safeTransfer in claimPayout (CEI grep: line number of `claimed[callId][msg.sender] = true` < line number of `safeTransfer`)
    - FollowFadeMarket.sol: applySettlement has settlementApplied[callId]=true BEFORE any transfer (CEI)
    - No inline USDC address in FollowFadeMarket.sol: `grep -n "0xaf88\|0xFF97" packages/contracts/src/FollowFadeMarket.sol` returns 0
    - pragma =0.8.30 in all modified files: `grep "pragma solidity" packages/contracts/src/FollowFadeMarket.sol` shows =0.8.30
  </acceptance_criteria>
  <done>ISettlementManager interface LOCKED with all events/errors/functions; FFM redeploy adds applySettlement (CEI, idempotency guard) + real claimPayout (pull-pattern, CEI, Math.mulDiv) + getFadeRealReserve; IFollowFadeMarket updated; all compile without error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SettlementManager.sol (14-step settle + Solidity baseline rep + dispute + forceSettle) + ProfileRegistry stub fill</name>
  <files>
    packages/contracts/src/SettlementManager.sol,
    packages/contracts/src/ProfileRegistry.sol
  </files>
  <read_first>
    - packages/contracts/src/ChallengeEscrow.sol (full file — primary analog: imports, inheritance, modifiers, CEI, constructor, setSettlementManager, Pausable pattern, ReentrancyGuard, onlyOwner, all event emit patterns)
    - packages/contracts/src/FollowFadeMarket.sol (current state after Task 1 — applySettlement + getFadeRealReserve signatures)
    - packages/contracts/src/interfaces/ISettlementManager.sol (created in Task 1 — implement this interface)
    - packages/contracts/src/interfaces/ICallRegistry.sol (markSettled, getCall, CallStatus enum, Outcome enum)
    - packages/contracts/src/interfaces/IChallengeEscrow.sol (settleDuel signature)
    - packages/contracts/src/interfaces/IProfileRegistry.sol (updateAfterSettlement, applyRepDelta, authorizedRepWriters)
    - packages/contracts/src/ProfileRegistry.sol (current stub — updateAfterSettlement lines, applyRepDelta)
    - .planning/phases/04-settlementmanager-7-oracle-paths-solidity-baseline-rep-delta/04-RESEARCH.md (§14-step settle() sequence LOCKED, §Solidity Baseline Rep Delta, §Dispatch Table Design, §Pyth adapter Solidity side, §Fee Extraction Design, §Pitfall 4 ETH budget, §Security Domain STRIDE patterns)
    - .planning/phases/04-settlementmanager-7-oracle-paths-solidity-baseline-rep-delta/04-PATTERNS.md (§SettlementManager.sol full pattern excerpt, §ETH receive pattern)
  </read_first>
  <behavior>
    SettlementManager.sol:
    - pragma =0.8.30; contract SettlementManager is Ownable2Step, ReentrancyGuard, Pausable, ISettlementManager
    - Imports: SafeERC20, IERC20, ECDSA from OZ; IPyth from @pythnetwork/pyth-sdk-solidity; USDC_ARB_NATIVE; all 4 interfaces
    - Constants: uint256 constant FORCE_SETTLE_COOLDOWN = 7 days; uint8 constant MAX_COUNTER_CLAIMS = 3; uint256 constant DISPUTE_WINDOW = 24 hours; uint256 constant DISPUTE_BOND = 5e6 (5 USDC 6-decimal); uint256 constant DISPUTE_REWARD = 2e6; uint256 constant PROTOCOL_FEE_BPS = 100 (1.0%); uint256 constant CREATOR_FEE_BPS = 40 (0.4%); uint256 constant LP_FEE_BPS = 30 (0.3%);
    - Constructor: Ownable(msg.sender); validates all addresses; sets pyth address; initializes PYTH_ADDRESS as immutable; receive() external payable {} for ETH top-ups
    - adapterMap: mapping(uint8 => mapping(uint8 => OracleAdapter)) public adapterMap; setAdapterMap() onlyOwner
    - IStylusScoreEngine seam: address public stylusScoreEngine; setStylusScoreEngine(address) onlyOwner (starts at address(0))
    - settle(uint256 callId, bytes[] calldata pythUpdateData) external nonReentrant whenNotPaused:
      Step 1: if call.status != Live revert AlreadySettled
      Step 2: if block.timestamp < call.expiry revert CallNotExpired
      Step 3: implicit via whenNotPaused
      Step 4: adapter = adapterMap[call.marketType][call.eventSubtype]
      Step 5: ICallRegistry.Outcome outcome = _dispatchOracle(callId, call, adapter, pythUpdateData); if outcome == Pending → emit SettlementDelayed + return (NOT revert)
      Step 6: for Pyth: confidence gate already in _settlePyth (returns Pending if wide)
      Step 7: bool coldStart = (ffm.getFadeRealReserve(callId) == 0)
      Step 8: try/catch rep seam:
        (a) address stylusAddr = stylusScoreEngine; int256 repDelta;
        (b) if (stylusAddr != address(0)) { try IStylusScoreEngine(stylusAddr).compute_rep_change(callerCurrentRep, conviction, consensusPct, isWinner, 10) returns (int32 d) { repDelta = int256(d); } catch (bytes memory err) { repDelta = _solidityBaselineRepDelta(conviction, consensusPct, isWinner); emit RepCalculatedFallback(callId, callerAddr, repDelta, err); } }
        (c) else { repDelta = _solidityBaselineRepDelta(conviction, consensusPct, isWinner); }
        (d) if (coldStart && isWinner && !callerExited) repDelta = (repDelta * 25) / 100;
        (e) if (!callerExited) pr.applyRepDelta(callerAddr, repDelta);
        (f) emit RepCalculated(callId, callerAddr, currentRep, conviction, consensusPct, isWinner, 10, repDelta);
      Step 9: duel loop — cr.getAcceptedChallengeIds(callId) → for each: address winner = isWinner ? call.caller : challengerAddr; ce.settleDuel(challengeId, winner); plus REP-27 ~1.5× rep for both duelists via applyRepDelta
      Step 10: (cold-start scaling already applied in step 8)
      Step 11: (uint256 protocolFee, uint256 creatorFee, uint256 lpFee) = _computeFees(callId, outcome, call.callerExited, call.callerVolumeAtExit); ffm.applySettlement(callId, outcome, protocolFee, creatorFee, lpFee);
      Step 12: cr.clearDuplicateHash(call.duplicateHash) (calls callRegistry.activeDuplicateHashes[h] = 0 pattern)
      Step 13: cr.markSettled(callId, outcome)
      Step 14: pr.updateAfterSettlement(callerAddr, call.category, true, isWinner, repDelta)
      Step 15: emit CallSettled(callId, outcome, priceDelta)

    _solidityBaselineRepDelta(uint8 conviction, uint8 consensusPct, bool isWinner) internal pure returns (int256):
    - REP-22 lower fidelity: linear conviction scale only; NO high-conviction 2× asymmetry; contrarian multiplier fixed=1.0
    - uint256 BASE = 10; scaled = (BASE * uint256(conviction) * 2) / 100; if scaled < 1 scaled = 1;
    - if isWinner: return int256(scaled); else: return -int256(scaled);

    _settlePyth(uint256 callId, Call memory call, bytes[] calldata pythUpdateData) internal returns (Outcome):
    - uint256 fee = pyth.getUpdateFee(pythUpdateData); require(address(this).balance >= fee, "InsufficientEthForPythFee")
    - pyth.updatePriceFeeds{value: fee}(pythUpdateData)
    - PythStructs.Price memory p = pyth.getPriceNoOlderThan(call.assetA_feedId, 60)
    - Confidence gate: if uint256(int256(int64(p.conf))) * 200 > uint256(int256(p.price > 0 ? int256(p.price) : -int256(p.price))): emit SettlementDelayed; return Outcome.Pending
    - Compare price to call.targetValue; return CallerWon or CallerLost

    submitAttestation(uint256 callId, bytes calldata attestationData, bytes calldata signature) external nonReentrant:
    - For non-Pyth adapters: relayer calls this before settle() with the signed EIP-712 attestation
    - Verify ecrecover using ECDSA.recover(_hashTypedDataV4(attestationHash), signature) == expectedSignerForType
    - Store attestation result; settle() reads from storage in step 4-5

    forceSettle(uint256 callId) external onlyOwner:
    - require block.timestamp >= call.expiry + FORCE_SETTLE_COOLDOWN, "ForceSettleCooldownActive"
    - (force an outcome — owner specifies via resolveDispute pattern or reads oracle)
    - emit CallForceSettled(callId, outcome); emit CallSettled(callId, outcome, 0)

    raiseDispute(uint256 callId, bytes32 evidenceHash) external nonReentrant whenNotPaused:
    - Require status == Settled; require block.timestamp <= settledAt + DISPUTE_WINDOW
    - if disputes[callId].disputer != address(0) and not counter-claim path: revert DisputeAlreadyRaised
    - if counterClaimCount >= MAX_COUNTER_CLAIMS: revert MaxCounterClaimsReached
    - CEI: update disputes[callId]; then IERC20(USDC_ARB_NATIVE).safeTransferFrom(msg.sender, address(this), DISPUTE_BOND)
    - Flip call status to Disputed (via cr.markDisputed — add to ICallRegistry or handle locally)
    - emit DisputeRaised(callId, msg.sender, evidenceHash)

    resolveDispute(uint256 callId, uint8 finalOutcome) external onlyOwner nonReentrant:
    - If finalOutcome != current outcome: reverse rep deltas; redistribute unclaimed USDC from old-winner reserve to new-winner reserve
    - Refund winning disputer bond + DISPUTE_REWARD; forfeit losing disputer bond to treasury
    - cr.markSettled(callId, finalOutcome) (re-settles with new outcome)
    - emit DisputeResolved(callId, finalOutcome)

    _computeFees(uint256 callId, Outcome outcome, bool callerExited, uint256 callerVolumeAtExit):
    - totalPool = ffm.followReserve(callId) + ffm.getFadeRealReserve(callId)
    - protocolFee = totalPool * PROTOCOL_FEE_BPS / 10000
    - creatorBase = callerExited ? callerVolumeAtExit : totalPool
    - creatorFee = creatorBase * CREATOR_FEE_BPS / 10000
    - lpFee = totalPool * LP_FEE_BPS / 10000
    - return (protocolFee, creatorFee, lpFee)

    ProfileRegistry.sol updateAfterSettlement stub fill:
    - Replace emit-only stub with: settledCalls[caller]++; if (isWinner) wins[caller]++; else losses[caller]++;
    - Also update categoryRep[caller][category] via applyRepDelta if not already done in step 8 (check — step 8 calls applyRepDelta; updateAfterSettlement handles stats counters only)
    - Emit ProfileUpdated(caller) after updates

    USDC_ARB_NATIVE import — NEVER inline
    No delegatecall to any user-controlled address (SAFETY-57 note)
  </behavior>
  <verify>
    <automated>cd /c/Users/woshv/Desktop/Call\ it && forge test --root packages/contracts --match-contract SettlementManagerTest -vv 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - forge build --root packages/contracts exits 0 (all contracts compile)
    - forge test --match-contract SettlementManagerTest exits 0 with all tests GREEN (SETTLE-02/05/08/44/46 + REP-14/22/23 + SETTLE-39/40)
    - forge test --match-contract FfmSettlementTest exits 0 (claimPayout CEI + CALL-41 empty-pool)
    - forge test --match-contract SettlementDisputeTest exits 0 (SETTLE-25..36)
    - grep "function _solidityBaselineRepDelta" packages/contracts/src/SettlementManager.sol returns 1 line
    - grep "RepCalculatedFallback" packages/contracts/src/SettlementManager.sol returns >= 2 (definition + emit)
    - grep "FORCE_SETTLE_COOLDOWN" packages/contracts/src/SettlementManager.sol returns >= 2 (constant + use in forceSettle)
    - grep "0xaf88\|0xFF97" packages/contracts/src/SettlementManager.sol returns 0 (no inline USDC)
    - pragma =0.8.30 exact: `grep "pragma solidity" packages/contracts/src/SettlementManager.sol` = "pragma solidity =0.8.30;"
    - ProfileRegistry updateAfterSettlement no longer stub: `grep "settledCalls\[" packages/contracts/src/ProfileRegistry.sol` returns >= 1
    - Fee math: 1.0+0.4+0.3=1.7% — invariantFeeSplit fuzz passes with 1000 runs
  </acceptance_criteria>
  <done>SettlementManager.sol implements all 14 steps atomically with Pyth dispatch, Solidity baseline rep fallback (REP-22), try/catch Stylus seam, duel loop, 1.7% fee extraction, dispute custody, forceSettle 7d cooldown. All Foundry tests GREEN. ProfileRegistry stub filled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External → settle() | Permissionless — any caller can invoke; idempotency guard is the primary defense |
| Relayer → submitAttestation() | KMS-signed EIP-712; ecrecover verifies against expected signer per attestation type |
| FFM USDC → SettlementManager | applySettlement called only by onlySettlementManager modifier |
| USDC → claimPayout winners | Pull-pattern; CEI enforced; nonReentrant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | settle() double-invocation | mitigate | Step 1 idempotency guard: status != Live → revert AlreadySettled (SETTLE-02). Forge test testSettleIdempotency covers this. |
| T-04-02-02 | Tampering | claimPayout reentrancy | mitigate | nonReentrant + CEI: claimed[callId][msg.sender]=true BEFORE safeTransfer. Forge test testClaimPayoutCEI covers this (T-04-01-02). |
| T-04-02-03 | Spoofing | Fake relayer attestation | mitigate | submitAttestation uses ECDSA.recover; expected signer per attestation type registered onlyOwner; EIP-712 domain chainId=42161n + verifyingContract binding (Pitfall 7). |
| T-04-02-04 | Repudiation | forceSettle silent use | mitigate | 7-day cooldown (FORCE_SETTLE_COOLDOWN) + emits BOTH CallForceSettled AND CallSettled (SETTLE-40) for loud audit trail (Pitfall 6). |
| T-04-02-05 | Tampering | Empty-pool virtual seed payout | mitigate | CALL-41: applySettlement checks getFadeRealReserve==0 → routes entire followReserve to treasury; no payout from virtual seed (Pitfall 22). |
| T-04-02-06 | Elevation | delegatecall to user-controlled address | mitigate | SettlementManager is non-upgradeable (SAFETY-18); no delegatecall paths; all external calls are to known interfaces (SAFETY-57 note in contract). |
| T-04-02-07 | Tampering | Sybil fade CONTRARIAN HIT manufacture | mitigate | Cold-start 25% scaling (REP-14) applied when getFadeRealReserve==0; Sybil fader must commit real USDC (financial disincentive). |
| T-04-02-08 | Tampering | Fee extraction rounding underflow | mitigate | Math.mulDiv (OZ overflow-safe); invariantFeeSplit fuzz 1000 runs asserts equality within 2 wei dust. |
</threat_model>

<verification>
forge build --root packages/contracts exits 0
forge test --match-contract SettlementManagerTest exits 0 (all GREEN)
forge test --match-contract FfmSettlementTest exits 0
forge test --match-contract SettlementDisputeTest exits 0
No inline USDC address in SettlementManager.sol or FollowFadeMarket.sol
pragma =0.8.30 exact in all new files
_solidityBaselineRepDelta function exists in SettlementManager.sol
RepCalculatedFallback emitted in catch block
FORCE_SETTLE_COOLDOWN = 7 days constant used in forceSettle
</verification>

<success_criteria>
- All Foundry test suites created in Plan 04-01 (SettlementManagerTest, FfmSettlementTest, SettlementDisputeTest) pass GREEN after this plan
- SettlementManager.sol implements the 14-step settle() per §12.4 sequence (LOCKED)
- FollowFadeMarket.sol redeploy: applySettlement (CEI), real claimPayout (pull-pattern, CEI, Math.mulDiv), getFadeRealReserve
- Solidity baseline rep delta in-contract (REP-22 lower fidelity — linear conviction, contrarian=1.0, no 2× asymmetry)
- Stylus try/catch seam present (stylusScoreEngine address settable; catch fires RepCalculatedFallback)
- Dispute system on-chain: $5 bond, MAX_COUNTER_CLAIMS=3, 24h window, USDC reversal
- forceSettle 7d cooldown, dual event emission
- ProfileRegistry updateAfterSettlement fills settledCalls/wins/losses counters
</success_criteria>

<output>
After completion, create `.planning/phases/04-settlementmanager-7-oracle-paths-solidity-baseline-rep-delta/04-02-SUMMARY.md`
</output>
