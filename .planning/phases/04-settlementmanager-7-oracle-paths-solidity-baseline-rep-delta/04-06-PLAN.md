---
phase: 04-settlementmanager-7-oracle-paths-solidity-baseline-rep-delta
plan: 06
type: execute
wave: 4
depends_on:
  - 04-04
files_modified:
  - apps/relayer/src/workers/oracle-adapters/nft-twap-adapter.ts
  - apps/relayer/src/workers/oracle-adapters/rpc-metrics-adapter.ts
  - apps/relayer/src/workers/oracle-adapters/snapshot-adapter.ts
  - apps/relayer/src/workers/oracle-adapters/tally-adapter.ts
  - apps/relayer/src/workers/oracle-adapters/cex/binance-scraper.ts
  - apps/relayer/src/workers/oracle-adapters/cex/coinbase-scraper.ts
  - apps/relayer/src/workers/oracle-adapters/cex/okx-scraper.ts
  - apps/relayer/src/workers/oracle-adapters/cex/bybit-scraper.ts
  - apps/relayer/src/workers/oracle-adapters/cex/kraken-scraper.ts
  - apps/relayer/src/workers/oracle-adapters/cex/bitget-scraper.ts
  - apps/relayer/src/workers/oracle-adapters/cex/kucoin-scraper.ts
  - apps/relayer/src/workers/oracle-adapters/cex/upbit-scraper.ts
  - apps/relayer/src/workers/oracle-adapters/cex/cex-adapter.ts
  - apps/relayer/src/workers/settlement-watcher.ts
autonomous: true
requirements:
  - SETTLE-13
  - SETTLE-14
  - SETTLE-15
  - SETTLE-16
  - SETTLE-17
  - SETTLE-18
  - SETTLE-19
  - SETTLE-20
  - SETTLE-21
  - SETTLE-22
  - SETTLE-23
  - SETTLE-24
  - SETTLE-06

must_haves:
  truths:
    - "All 8 CEX scrapers use Playwright headless Chromium; each is isolated with its own per-exchange selectors and Innovation Zone exclusion fixture"
    - "NFT TWAP adapter verifies >= 12 observations; fewer → ambiguous"
    - "Snapshot and Tally adapters read governance proposal state and sign with snapshot-tally KMS key"
    - "settlement-watcher.ts dispatch table routes all 7 adapter types via non-Pyth KMS-attestation path"
    - "Each CEX scraper exports testWithFixture(staticHtml: string): boolean for weekly CI synthetic test"
    - "rpc-metrics-adapter intentionally shares the defillama KMS key (both are off-chain numeric attestations); documented in code; AAVE_V3_POOL address imported from @call-it/shared pinned constants, never from call parameters"
  artifacts:
    - path: apps/relayer/src/workers/oracle-adapters/nft-twap-adapter.ts
      provides: "Alchemy getNFTSales 24h TWAP; >=12 obs check; sign with nft-twap KMS key (name=CallIt-NftTwap)"
    - path: apps/relayer/src/workers/oracle-adapters/rpc-metrics-adapter.ts
      provides: "viem getLogs for Aave/Compound liquidation events; on-chain metric reads; AAVE_V3_POOL from @call-it/shared; signs with defillama KMS key (intentional shared key for numeric off-chain attestations)"
    - path: apps/relayer/src/workers/oracle-adapters/cex/cex-adapter.ts
      provides: "Orchestrates all 8 scrapers; Innovation Zone exclusion; multi-signal confirm (symbol+name); exports testWithFixture per exchange"
    - path: apps/relayer/src/workers/oracle-adapters/snapshot-adapter.ts
      provides: "snapshot.js proposal state read; sign with snapshot-tally KMS key"
    - path: apps/relayer/src/workers/oracle-adapters/tally-adapter.ts
      provides: "Tally GraphQL fetch; sign with snapshot-tally KMS key (same key, per-type domain name)"
    - path: "apps/relayer/src/workers/oracle-adapters/cex/[binance|coinbase|okx|bybit|kraken|bitget|kucoin|upbit]-scraper.ts"
      provides: "8 isolated Playwright scrapers; per-exchange selectors; Innovation Zone exclusion"
  key_links:
    - from: settlement-watcher.ts dispatch
      to: cex-adapter.ts
      via: "if adapter == CexScraper → cexAdapter.scrapeAndAttest(callId, tokenSymbol, tokenName)"
      pattern: "cexAdapter.*scrapeAndAttest"
    - from: nft-twap-adapter.ts
      to: kms-signer.ts
      via: "gcpKmsAccount({ keyId: 'nft-twap' }); domain.name='CallIt-NftTwap'"
      pattern: "keyId.*nft-twap"
    - from: rpc-metrics-adapter.ts
      to: kms-signer.ts
      via: "gcpKmsAccount({ keyId: 'defillama' }) — intentionally shares defillama key; both are off-chain numeric attestations; documented with comment in rpc-metrics-adapter.ts"
      pattern: "keyId.*defillama.*rpc"
---

<objective>
Wave 4: Remaining 5 oracle adapters + 8 CEX scrapers. After this plan all 7 oracle paths are implemented (D-02 — full fidelity). The settlement watcher dispatch table handles all adapter types.

Purpose: Orbits around the proven Pyth spine + DefiLlama KMS rail. Each adapter reuses the same EIP-712 attestation pattern from Plan 04-04; the CEX scrapers are isolated modules with per-exchange selectors and Innovation Zone exclusion fixtures.

Output: nft-twap, rpc-metrics, snapshot, tally adapters + 8 CEX scrapers + cex-adapter orchestrator + settlement-watcher dispatch update.
Task 2 is split into 2a (first 4 CEX scrapers + orchestrator) and 2b (remaining 4 scrapers + dispatch wiring) to keep each task within ~5-6 files.
</objective>

<execution_context>
@/home/.claude/get-shit-done/workflows/execute-plan.md
@/home/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-settlementmanager-7-oracle-paths-solidity-baseline-rep-delta/04-CONTEXT.md
@.planning/phases/04-settlementmanager-7-oracle-paths-solidity-baseline-rep-delta/04-RESEARCH.md
@.planning/phases/04-settlementmanager-7-oracle-paths-solidity-baseline-rep-delta/04-PATTERNS.md

<interfaces>
<!-- From kms-signer.ts — AttestationType keys for each adapter -->
```typescript
export type AttestationType = 'nft-twap' | 'defillama' | 'cex' | 'snapshot-tally' | 'oauth-proof';
// Per-type KMS key separation (D-05, Pitfall 7):
// nft-twap → keyId: 'nft-twap'; domain.name = 'CallIt-NftTwap'
// defillama → keyId: 'defillama'; domain.name = 'CallIt-DefiLlama' (Plan 04-04)
// rpc-metrics → keyId: 'defillama' (INTENTIONAL — shares defillama key; both are numeric off-chain attestations)
//               domain.name = 'CallIt-RpcMetrics' (different domain name prevents cross-type replay within same key)
// cex → keyId: 'cex'; domain.name = 'CallIt-Cex'
// snapshot-tally → keyId: 'snapshot-tally'; domain.name = 'CallIt-SnapshotTally' (both share the key)
// Blast radius if defillama key is compromised: can forge DefiLlama + RpcMetrics attestations only
//   → two numeric attestation types. NFT-TWAP, CEX, Snapshot/Tally are still protected by separate keys.
```

From packages/shared/src/constants/addresses.ts (AAVE_V3_POOL):
```typescript
// AAVE_V3_POOL must come from @call-it/shared pinned constants, NOT from call parameters.
// Import: import { AAVE_V3_POOL_ARBITRUM_ONE } from '@call-it/shared/constants/addresses';
// This prevents forgeable attestations (W11 — if from call params, attacker can point to any contract).
```

From cex-heartbeat.ts stub — per-exchange modular structure:
```typescript
export const CEX_EXCHANGES = ['binance','coinbase','okx','bybit','kraken','bitget','kucoin','upbit'] as const;
// Phase 4: each exchange in its own file with:
// export async function scrape(tokenSymbol: string, tokenName: string, expiryTimestamp: number): Promise<boolean>
// export function testWithFixture(staticHtml: string): boolean  ← for weekly CI synthetic test
```

INNOVATION_ZONE_EXCLUSION_PATTERNS (from RESEARCH.md Adapter 7):
```typescript
const INNOVATION_ZONE_EXCLUSION_PATTERNS: Record<string, string[]> = {
  binance: ['Innovation Zone', 'Seed Tag', 'Monitoring Tag'],
  okx: ['Innovation Zone'], bybit: ['Innovation Zone'],
  bitget: ['PoP Zone'], // other exchanges: per-exchange patterns
};
```

Alchemy getNFTSales (from RESEARCH.md Adapter 3):
```typescript
const alchemy = new Alchemy({ apiKey: ALCHEMY_API_KEY, network: Network.ETH_MAINNET });
const sales = await alchemy.nft.getNFTSales({ contractAddress, startTime: unix24hAgo, endTime: unixExpiry });
// must have >= 12 observations; if < 12 → ambiguous (SETTLE-16)
```

Snapshot (from RESEARCH.md Adapter 5):
```typescript
import snapshot from '@snapshot-labs/snapshot.js';
const client = new snapshot.Client712('https://hub.snapshot.org');
const proposal = await client.request('proposal', { id: proposalId });
// outcome: proposal.state === 'closed' && compare votes to win threshold
```

Tally (from RESEARCH.md Adapter 6):
```typescript
const res = await fetch('https://api.tally.xyz/query', {
  method: 'POST', headers: { 'Api-Key': process.env.TALLY_API_KEY, 'Content-Type': 'application/json' },
  body: JSON.stringify({ query: `{ proposal(id: "${proposalId}") { status, eta, ... } }` })
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NFT TWAP + RPC metrics + Snapshot + Tally adapters (KMS-attestation rail)</name>
  <files>
    apps/relayer/src/workers/oracle-adapters/nft-twap-adapter.ts,
    apps/relayer/src/workers/oracle-adapters/rpc-metrics-adapter.ts,
    apps/relayer/src/workers/oracle-adapters/snapshot-adapter.ts,
    apps/relayer/src/workers/oracle-adapters/tally-adapter.ts
  </files>
  <read_first>
    - apps/relayer/src/workers/oracle-adapters/defillama-adapter.ts (Plan 04-04 — exact KMS-attestation pattern to replicate)
    - apps/relayer/src/lib/kms-signer.ts (AttestationType, gcpKmsAccount, per-type key IDs: nft-twap, snapshot-tally; verify defillama key is the intended shared key for rpc-metrics)
    - packages/shared/src/constants/addresses.ts (AAVE_V3_POOL_ARBITRUM_ONE — must import from here, never from call parameters)
    - apps/relayer/src/workers/__tests__/defillama-adapter.test.ts (passes GREEN — use same pattern for new adapters)
    - .planning/phases/04-settlementmanager-7-oracle-paths-solidity-baseline-rep-delta/04-RESEARCH.md (§Adapter 3 NFT TWAP, §Adapter 4 RPC Metrics, §Adapter 5 Snapshot, §Adapter 6 Tally, §Don't Hand-Roll EIP-712)
    - .planning/phases/04-settlementmanager-7-oracle-paths-solidity-baseline-rep-delta/04-PATTERNS.md (§defillama-adapter analog, §KMS signing pattern)
  </read_first>
  <action>
    Each adapter follows the same 3-export pattern as defillama-adapter.ts:
    1. fetch[Oracle]Data(params) — queries the external source
    2. sign[Oracle]Attestation(callId, data) — EIP-712 sign with per-type KMS key
    3. submit[Oracle]Attestation(callId, ...) — writeContract to SettlementManager.submitAttestation

    nft-twap-adapter.ts:
    - fetchNftTwap(contractAddress: string, expiryTimestamp: number): Promise<{ twapPriceWei: bigint; observationCount: number; evidenceHash: `0x${string}` }>
      Uses alchemy.nft.getNFTSales({ contractAddress, startTime: expiryTimestamp - 86400, endTime: expiryTimestamp })
      If sales.length < 12: return { ambiguous: true } (SETTLE-16 — < 12 obs → ambiguous)
      Compute TWAP as sum of sale prices / sales.length; return observationCount + IPFS evidenceHash of raw data
    - signNftTwapAttestation: keyId='nft-twap'; domain.name='CallIt-NftTwap'; chainId=42161n
    - submitNftFloor(callId, twapPriceWei, observationCount, evidenceHash): calls SM.submitNftFloor (SETTLE-13)

    rpc-metrics-adapter.ts:
    - IMPORTANT — AAVE_V3_POOL ADDRESS: import { AAVE_V3_POOL_ARBITRUM_ONE } from '@call-it/shared/constants/addresses' (W11 fix). The pool contract address MUST come from the @call-it/shared pinned constants, NOT from call parameters or call.assetA. If the address came from call params, an attacker could forge an attestation by pointing to a malicious contract they control. The shared constant is the only acceptable source. Acceptance criterion enforces this with a grep gate.
    - fetchRpcMetric(callId, metricType: 'liquidation' | 'active_addresses' | 'gas'):
      For liquidation: publicClient.getLogs({ address: AAVE_V3_POOL_ARBITRUM_ONE, event: LIQUIDATION_CALL_ABI, fromBlock, toBlock })
      For gas/metric: publicClient.getBlock + computations
    - signRpcAttestation: keyId='defillama' — INTENTIONALLY shares the defillama KMS key (W7 fix). Both rpc-metrics and defillama are off-chain numeric data attestations with the same trust level. File-level comment: "// KMS key: 'defillama' — shared intentionally with defillama-adapter.ts. Both adapters produce numeric off-chain attestations with equivalent trust requirements. Blast radius: a compromised 'defillama' key can forge DefiLlama AND RpcMetrics attestations; NFT-TWAP, CEX, and Snapshot/Tally keys remain isolated."
    - domain.name='CallIt-RpcMetrics'; chainId=42161n (different domain name from DefiLlama prevents cross-type replay within the shared key)
    - Pino logging per pattern

    snapshot-adapter.ts:
    - fetchSnapshotProposal(proposalId: string): import snapshot.js; client.request('proposal', { id: proposalId })
    - Outcome: if proposal.state === 'closed': compare votes to threshold; return CallerWon/CallerLost/Ambiguous
    - signSnapshotAttestation: keyId='snapshot-tally'; domain.name='CallIt-SnapshotTally'; chainId=42161n

    tally-adapter.ts:
    - fetchTallyProposal(proposalId: string): fetch to https://api.tally.xyz/query with TALLY_API_KEY header
    - GraphQL query: `{ proposal(id: "${proposalId}") { status, eta, voteStats { ... } } }`
    - Outcome determination from proposal.status === 'EXECUTED' / 'DEFEATED'
    - signTallyAttestation: keyId='snapshot-tally' (same key as Snapshot — per AttestationType); domain.name='CallIt-SnapshotTally'
    - Note: check TALLY_API_KEY in process.env — log warning if absent (free tier key required; see RESEARCH.md open question)

    All adapters: Pino structured logging with event field; error: return { ambiguous: true } on any fetch failure (not throw — prevents settlement-watcher crash); TypeScript strict
  </action>
  <verify>
    <automated>cd /c/Users/woshv/Desktop/Call\ it && pnpm --filter @call-it/relayer build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - pnpm --filter @call-it/relayer build exits 0 (all 4 new adapters compile)
    - nft-twap-adapter.ts: keyId='nft-twap' + domain.name='CallIt-NftTwap' + observationCount<12 ambiguous path (grep: `grep "nft-twap\|CallIt-NftTwap\|observationCount" apps/relayer/src/workers/oracle-adapters/nft-twap-adapter.ts`)
    - rpc-metrics-adapter.ts: keyId='defillama' — intentional shared key, documented with blast-radius comment (grep: `grep "keyId.*defillama\|intentional\|blast radius" apps/relayer/src/workers/oracle-adapters/rpc-metrics-adapter.ts`)
    - rpc-metrics-adapter.ts: domain.name='CallIt-RpcMetrics' (different from 'CallIt-DefiLlama'; prevents cross-type replay): `grep "CallIt-RpcMetrics" apps/relayer/src/workers/oracle-adapters/rpc-metrics-adapter.ts`
    - rpc-metrics-adapter.ts: AAVE_V3_POOL imported from @call-it/shared, NOT from call parameters (grep gate: `grep "AAVE_V3_POOL" apps/relayer/src/workers/oracle-adapters/rpc-metrics-adapter.ts | grep -v "import\|shared"` returns 0 — any usage must come via the import)
    - snapshot-adapter.ts: imports @snapshot-labs/snapshot.js; keyId='snapshot-tally' (grep: `grep "snapshot-tally\|snapshot.js" apps/relayer/src/workers/oracle-adapters/snapshot-adapter.ts`)
    - tally-adapter.ts: TALLY_API_KEY header; api.tally.xyz/query endpoint (grep: `grep "tally.xyz\|TALLY_API_KEY" apps/relayer/src/workers/oracle-adapters/tally-adapter.ts`)
    - All 4 adapters: chainId=42161n in EIP-712 domain (grep each file for "42161n")
    - No local private key in any adapter — all KMS via gcpKmsAccount
  </acceptance_criteria>
  <done>NFT TWAP, RPC metrics, Snapshot, Tally adapters implemented following DefiLlama KMS-attestation pattern; rpc-metrics intentionally shares defillama KMS key (documented with blast-radius comment, different domain name prevents cross-type replay); AAVE_V3_POOL imported from @call-it/shared pinned constants; ambiguous-read paths return { ambiguous: true }.</done>
</task>

<task type="auto">
  <name>Task 2a: First 4 CEX scrapers (Binance/Coinbase/OKX/Bybit) + cex-adapter orchestrator</name>
  <files>
    apps/relayer/src/workers/oracle-adapters/cex/binance-scraper.ts,
    apps/relayer/src/workers/oracle-adapters/cex/coinbase-scraper.ts,
    apps/relayer/src/workers/oracle-adapters/cex/okx-scraper.ts,
    apps/relayer/src/workers/oracle-adapters/cex/bybit-scraper.ts,
    apps/relayer/src/workers/oracle-adapters/cex/cex-adapter.ts
  </files>
  <read_first>
    - apps/relayer/src/workers/cex-heartbeat.ts (existing stubs — per-exchange modular structure; the function signatures that Phase 4 implements; CEX_EXCHANGES list; per-exchange isolation note)
    - apps/relayer/src/workers/__tests__/cex-binance.test.ts (Plan 04-01 scaffold — implement against these; testWithFixture + Innovation Zone exclusion + multi-signal)
    - apps/relayer/src/workers/__tests__/fixtures/binance-listing.html (fixture file from Plan 04-01)
    - apps/relayer/src/workers/oracle-adapters/defillama-adapter.ts (KMS signing pattern — cex adapter uses keyId='cex')
    - .planning/phases/04-settlementmanager-7-oracle-paths-solidity-baseline-rep-delta/04-RESEARCH.md (§Adapter 7 CEX Scrapers, §INNOVATION_ZONE_EXCLUSION_PATTERNS, §Pitfall 10 weekly CI, §Pitfall 19 multi-signal confirm)
    - .planning/phases/04-settlementmanager-7-oracle-paths-solidity-baseline-rep-delta/04-PATTERNS.md (§cex/binance-scraper.ts stub-to-real pattern, §per-exchange modular isolation)
  </read_first>
  <action>
    Each [exchange]-scraper.ts file (4 files: binance, coinbase, okx, bybit):
    - ANNOUNCE_URL constant (announcement page URL for that exchange; embed as in-file constant, not dynamic):
      Binance: 'https://www.binance.com/support/announcement/new-listings'
      Coinbase: 'https://blog.coinbase.com/search?query=listing'
      OKX: 'https://www.okx.com/help/section/new-listings'
      Bybit: 'https://announcements.bybit.com/en-US/'
    - EXCLUSION_PATTERNS: string[] per-exchange Innovation Zone patterns embedded as in-file constants (Pitfall 19):
      Binance: ['Innovation Zone', 'Seed Tag', 'Monitoring Tag']
      Coinbase: [] (no Innovation Zone equivalent)
      OKX: ['Innovation Zone']
      Bybit: ['Innovation Zone']
    - Per-exchange CSS selectors: embed as in-file constants (Pitfall 10 — selector drift is per-exchange):
      Binance selector: '.css-1w8xkr1' or 'h3.css-*' (announcement title; verify at implementation time)
      Coinbase selector: '.blog-post-title' or equivalent
      OKX selector: '.article-title' or equivalent
      Bybit selector: '.announcement-title' or equivalent
    - export async function scrape(tokenSymbol: string, tokenName: string, expiryTimestamp: number): Promise<boolean>
      (a) Playwright chromium.launch({ headless: true }) → page.goto(ANNOUNCE_URL) → get page content
      (b) Parse post titles (per-exchange selector) published within 24h of expiryTimestamp
      (c) Multi-signal: match BOTH full tokenName AND tokenSymbol in title (Pitfall 19)
      (d) Exclusion: if any EXCLUSION_PATTERN found in matching post → return false (Innovation Zone exclusion)
      (e) Return true only if clean match found; false if not found or excluded
      (f) await browser.close() in finally block
    - export function testWithFixture(staticHtml: string): boolean
      Uses basic string search (no browser needed) on staticHtml string
      Applies same multi-signal + exclusion logic on the static HTML (D-02, Pitfall 10)
    - Pino logger: event: '[exchange]_scraper_attempt', event: '[exchange]_scraper_result'

    cex-adapter.ts:
    - Imports binance, coinbase, okx, bybit scrapers (all 4 from this task)
    - Leave kraken, bitget, kucoin, upbit as stubs (// TODO: added in Task 2b)
    - export async function scrapeAndAttest(callId: bigint, tokenSymbol: string, tokenName: string, expiryTimestamp: number): Promise<'found' | 'not_found' | 'ambiguous'>
      Runs available exchange scrapers in parallel (Promise.allSettled) → if any returns true → sign attestation with keyId='cex'; domain.name='CallIt-Cex'; chainId=42161n; submit to SM; return 'found'
      If none found → return 'not_found' (ambiguous — dispute window)
    - INNOVATION_ZONE_EXCLUSION_PATTERNS object exported for tests (binance/coinbase/okx/bybit entries in this task; kraken/bitget/kucoin/upbit added in Task 2b)
    - export function getCexTestFixture(exchange: keyof typeof scrapers): (html: string) => boolean — wraps each scraper's testWithFixture

    cex-binance.test.ts from Plan 04-01 must pass GREEN after this task.
  </action>
  <verify>
    <automated>cd /c/Users/woshv/Desktop/Call\ it && pnpm --filter @call-it/relayer test --run --reporter=verbose 2>&1 | grep -E "cex-binance|PASS|FAIL" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - pnpm --filter @call-it/relayer test --run: cex-binance.test.ts passes GREEN (testWithFixture detects listing, Innovation Zone exclusion works)
    - All 4 first-batch scraper files exist (binance, coinbase, okx, bybit)
    - cex-adapter.ts exists with scrapeAndAttest export
    - Each first-batch scraper exports both scrape() and testWithFixture() functions (grep: `grep -l "testWithFixture" apps/relayer/src/workers/oracle-adapters/cex/binance-scraper.ts apps/relayer/src/workers/oracle-adapters/cex/coinbase-scraper.ts apps/relayer/src/workers/oracle-adapters/cex/okx-scraper.ts apps/relayer/src/workers/oracle-adapters/cex/bybit-scraper.ts | wc -l` == 4)
    - cex-adapter.ts: keyId='cex' + domain.name='CallIt-Cex' + chainId=42161n (grep: `grep "keyId.*cex\|CallIt-Cex\|42161n" apps/relayer/src/workers/oracle-adapters/cex/cex-adapter.ts`)
    - ANNOUNCE_URL embedded as in-file constant in each scraper: `grep "ANNOUNCE_URL" apps/relayer/src/workers/oracle-adapters/cex/binance-scraper.ts`
    - EXCLUSION_PATTERNS embedded as in-file constants: `grep "Innovation Zone\|Seed Tag" apps/relayer/src/workers/oracle-adapters/cex/binance-scraper.ts`
    - pnpm --filter @call-it/relayer build exits 0
    - TypeScript strict mode: no `any` types in scraper exports
  </acceptance_criteria>
  <done>First 4 CEX scrapers (Binance, Coinbase, OKX, Bybit) implemented with Playwright + embedded ANNOUNCE_URL + embedded per-exchange EXCLUSION_PATTERNS + testWithFixture; cex-binance scaffold tests GREEN; cex-adapter orchestrator wired with first 4 scrapers.</done>
</task>

<task type="auto">
  <name>Task 2b: Remaining 4 CEX scrapers (Kraken/Bitget/KuCoin/Upbit) + settlement-watcher dispatch update</name>
  <files>
    apps/relayer/src/workers/oracle-adapters/cex/kraken-scraper.ts,
    apps/relayer/src/workers/oracle-adapters/cex/bitget-scraper.ts,
    apps/relayer/src/workers/oracle-adapters/cex/kucoin-scraper.ts,
    apps/relayer/src/workers/oracle-adapters/cex/upbit-scraper.ts,
    apps/relayer/src/workers/oracle-adapters/cex/cex-adapter.ts,
    apps/relayer/src/workers/settlement-watcher.ts
  </files>
  <read_first>
    - apps/relayer/src/workers/oracle-adapters/cex/binance-scraper.ts (Task 2a — same pattern to follow for remaining 4 scrapers)
    - apps/relayer/src/workers/oracle-adapters/cex/cex-adapter.ts (Task 2a — add remaining 4 scrapers; remove TODO stubs)
    - apps/relayer/src/workers/settlement-watcher.ts (Plan 04-04 — update dispatch table with all 7 adapter types; add full switch statement)
    - .planning/phases/04-settlementmanager-7-oracle-paths-solidity-baseline-rep-delta/04-RESEARCH.md (§Adapter 7 CEX Scrapers, §INNOVATION_ZONE_EXCLUSION_PATTERNS for Kraken/Bitget/KuCoin/Upbit)
    - .planning/phases/04-settlementmanager-7-oracle-paths-solidity-baseline-rep-delta/04-PATTERNS.md (§per-exchange modular isolation, §cex-adapter orchestrator)
  </read_first>
  <action>
    Remaining 4 scraper files (kraken, bitget, kucoin, upbit) — same pattern as Task 2a:
    - ANNOUNCE_URL constants (embed in file):
      Kraken: 'https://blog.kraken.com/new-listings'
      Bitget: 'https://www.bitget.com/blog'
      KuCoin: 'https://www.kucoin.com/news'
      Upbit: 'https://upbit.com/service_center/notice?code=notice'
    - EXCLUSION_PATTERNS per exchange:
      Kraken: [] (no Innovation Zone equivalent — only major listings announced)
      Bitget: ['PoP Zone', 'Margin Trading Only', 'Monitoring']
      KuCoin: ['Innovation Zone']
      Upbit: ['유의 종목', 'KRW Market Only'] (Korean exclusion terms)
    - Per-exchange CSS selectors embedded as in-file constants
    - Same scrape() + testWithFixture() pattern from Task 2a

    cex-adapter.ts updates (additive to Task 2a):
    - Add imports for kraken, bitget, kucoin, upbit scrapers
    - Add kraken/bitget/kucoin/upbit EXCLUSION_PATTERNS to the exported INNOVATION_ZONE_EXCLUSION_PATTERNS object
    - scrapeAndAttest runs all 8 scrapers in parallel (was 4 in Task 2a)
    - Remove the TODO stubs from Task 2a

    settlement-watcher.ts dispatch table completion:
    - Import all adapter orchestrators: pythAdapter, defiLlamaAdapter, nftTwapAdapter, rpcMetricsAdapter, snapshotAdapter, tallyAdapter, cexAdapter
    - In the BullMQ worker: complete switch on adapter type:
      case OracleAdapter.Pyth: → pythAdapter.settlePythCall(callId, updateData, acceptedChallengeIds)
      case OracleAdapter.NftTwap: → nftTwapAdapter.submitNftFloor(...)
      case OracleAdapter.DefiLlama: → defiLlamaAdapter.submitDefiLlamaAttestation(...)
      case OracleAdapter.RpcMetrics: → rpcMetricsAdapter logic
      case OracleAdapter.Snapshot: → snapshotAdapter logic
      case OracleAdapter.Tally: → tallyAdapter logic
      case OracleAdapter.CexScraper: → cexAdapter.scrapeAndAttest(...)
    - For ambiguous/not_found: emit SettlementDelayed event via relayer route + Telegram alert
    - All settle() calls pass acceptedChallengeIds as third arg (carried from Plan 04-04 pattern)
  </action>
  <verify>
    <automated>cd /c/Users/woshv/Desktop/Call\ it && pnpm --filter @call-it/relayer build 2>&1 | tail -5 && pnpm --filter @call-it/relayer test --run 2>&1 | grep -E "PASS|FAIL" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - All 8 exchange scraper files exist (ls apps/relayer/src/workers/oracle-adapters/cex/ shows 9 files including cex-adapter.ts)
    - Each scraper exports both scrape() and testWithFixture() functions (grep: `grep -l "testWithFixture" apps/relayer/src/workers/oracle-adapters/cex/*.ts | wc -l` == 9 files total including cex-adapter.ts, or 8 scraper files specifically)
    - INNOVATION_ZONE_EXCLUSION_PATTERNS in cex-adapter.ts has entries for all 8 exchanges (grep: `grep "Innovation Zone\|Seed Tag\|PoP Zone\|KRW Market" apps/relayer/src/workers/oracle-adapters/cex/cex-adapter.ts | wc -l` > 0)
    - settlement-watcher.ts: switch/dispatch on all 7 OracleAdapter enum values (grep: `grep -c "case OracleAdapter\|case.*Adapter" apps/relayer/src/workers/settlement-watcher.ts` >= 7)
    - pnpm --filter @call-it/relayer build exits 0
    - pnpm --filter @call-it/relayer test --run: cex-binance tests GREEN
    - TypeScript strict mode: no `any` types in scraper exports
  </acceptance_criteria>
  <done>All 8 CEX scrapers implemented with Playwright + embedded ANNOUNCE_URL + embedded EXCLUSION_PATTERNS + testWithFixture; cex-adapter orchestrates all 8 scrapers; settlement-watcher dispatches all 7 oracle paths; full oracle coverage achieved (D-02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Exchange HTML → scraper | Untrusted HTML from exchange announcement pages; only title text is processed |
| Snapshot/Tally API → relayer | Public APIs; signed attestation binds the data to chainId 42161 |
| NFT sales data (Alchemy) → relayer | Rate-limited Alchemy API; observation count >=12 required |
| RPC liquidation data → relayer | On-chain event logs; AAVE_V3_POOL address pinned in @call-it/shared — cannot be spoofed via call parameters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | Tampering | CEX scraper false positive (Innovation Zone) | mitigate | Per-exchange INNOVATION_ZONE_EXCLUSION_PATTERNS embedded as in-file constants; multi-signal confirm (symbol+name); weekly CI synthetic test with fixture (Pitfall 19) |
| T-04-06-02 | Tampering | NFT TWAP < 12 observations inflating price | mitigate | observationCount < 12 → return ambiguous; dispute window opens (SETTLE-16, Pitfall 7 magnitude cap) |
| T-04-06-03 | Spoofing | Compromised 'cex' KMS key forges CEX listings | mitigate | Per-type key isolation (D-05): 'cex' key can only forge CEX attestations; keyId='snapshot-tally' is a separate key preventing cross-path replay (Pitfall 7) |
| T-04-06-04 | DoS | Playwright Chromium unavailable in Railway container | mitigate | RESEARCH.md open question noted: pnpm playwright install chromium in Dockerfile; scraper returns ambiguous on browser launch failure → dispute window is backstop |
| T-04-06-05 | Tampering | Tally API key missing → silent miss | mitigate | tally-adapter.ts logs warning if TALLY_API_KEY absent; returns ambiguous (not false positive); RESEARCH.md open question noted |
| T-04-06-06 | Spoofing | Compromised 'defillama' key forges RpcMetrics attestations | accept | rpc-metrics intentionally shares defillama key (both numeric off-chain attestations). Blast radius: DefiLlama + RpcMetrics attestations. NFT-TWAP, CEX, Snapshot/Tally protected by separate keys. Different EIP-712 domain names (CallIt-RpcMetrics vs CallIt-DefiLlama) prevent cross-type replay within the shared key. Documented in rpc-metrics-adapter.ts. |
| T-04-06-07 | Tampering | Forgeable AAVE_V3_POOL address from call params | mitigate | AAVE_V3_POOL_ARBITRUM_ONE imported from @call-it/shared pinned constants; never accepted from call parameters or user input. Grep gate in acceptance criteria enforces this. |
</threat_model>

<verification>
pnpm --filter @call-it/relayer test --run: cex-binance passes GREEN
All 8 scraper files exist with testWithFixture exports
settlement-watcher.ts dispatches all 7 adapter types
EIP-712 domain per-type key separation confirmed in all new adapters
rpc-metrics shares defillama KMS key — documented with blast-radius comment + different domain name
AAVE_V3_POOL imported from @call-it/shared pinned constants (grep gate passes)
</verification>

<success_criteria>
- All 7 oracle paths implemented: Pyth (Plan 04-04), DefiLlama (Plan 04-04), NFT TWAP, RPC metrics, Snapshot, Tally, CEX (this plan)
- 8 CEX scrapers: Playwright headless, embedded ANNOUNCE_URL + EXCLUSION_PATTERNS per-exchange in-file, Innovation Zone exclusion, testWithFixture
- rpc-metrics-adapter uses defillama KMS key (intentional, documented, accepted blast radius)
- AAVE_V3_POOL_ARBITRUM_ONE from @call-it/shared — never from call parameters
- cex-adapter.ts orchestrates all 8 with multi-signal confirm and single KMS attestation
- settlement-watcher.ts dispatch table handles all 7 OracleAdapter enum values
- D-02 fully delivered: all 8 CEX scrapers at full roadmap fidelity
</success_criteria>

<output>
After completion, create `.planning/phases/04-settlementmanager-7-oracle-paths-solidity-baseline-rep-delta/04-06-SUMMARY.md`
</output>
