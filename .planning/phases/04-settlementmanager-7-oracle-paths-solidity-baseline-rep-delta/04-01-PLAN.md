---
phase: 04-settlementmanager-7-oracle-paths-solidity-baseline-rep-delta
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/contracts/test/helpers/SmTestHelper.sol
  - packages/contracts/test/SettlementManagerTest.sol
  - packages/contracts/test/FfmSettlementTest.sol
  - packages/contracts/test/SettlementDisputeTest.sol
  - packages/contracts/test/SettlementManagerForkTest.sol
  - apps/relayer/src/workers/__tests__/pyth-adapter.test.ts
  - apps/relayer/src/workers/__tests__/defillama-adapter.test.ts
  - apps/relayer/src/workers/__tests__/cex-binance.test.ts
  - apps/relayer/src/workers/__tests__/outcome-word.test.ts
autonomous: true
requirements:
  - SETTLE-02
  - SETTLE-03
  - SETTLE-05
  - SETTLE-08
  - SETTLE-43
  - SETTLE-44
  - SETTLE-46
  - SETTLE-25
  - SETTLE-26
  - SETTLE-27
  - SETTLE-28
  - SETTLE-29
  - SETTLE-30
  - SETTLE-34
  - SETTLE-39
  - SETTLE-40
  - SETTLE-16
  - SETTLE-23
  - REP-14
  - REP-22
  - REP-23
  - SAFETY-57

must_haves:
  truths:
    - "Every money-path invariant test (fee split, pool conservation, idempotency) has a RED-gate Foundry test that fails to compile until SettlementManager.sol exists"
    - "Every relayer adapter unit test has a RED-gate Vitest spec that fails until the adapter modules exist"
    - "The test helper SmTestHelper boots all 4 existing contracts + a placeholder SettlementManager"
    - "ADR-0001 mainnet-fork test file exists with the correct --fork-url annotation"
  artifacts:
    - path: packages/contracts/test/helpers/SmTestHelper.sol
      provides: "Abstract test helper extending CeTestHelper with SettlementManager deploy + 4-contract wiring"
      exports: [SmTestHelper]
    - path: packages/contracts/test/SettlementManagerTest.sol
      provides: "Core settle() invariants: AlreadySettled, CallNotExpired, atomic rollback, Pyth confidence gate, fee split fuzz, gas snapshot, duel invalid challengeId revert"
    - path: packages/contracts/test/FfmSettlementTest.sol
      provides: "FFM claimPayout CEI, applySettlement idempotency, cold-start CALL-41 empty-pool treasury path"
    - path: packages/contracts/test/SettlementDisputeTest.sol
      provides: "Dispute bond, 24h window close, MAX_COUNTER_CLAIMS=3, reversal USDC re-distribution"
    - path: packages/contracts/test/SettlementManagerForkTest.sol
      provides: "Mainnet-fork money-path tests (real USDC) per ADR-0001"
    - path: apps/relayer/src/workers/__tests__/pyth-adapter.test.ts
      provides: "Mocked Hermes retry loop: wide confidence → retry, 30 retries exhausted → dispute"
    - path: apps/relayer/src/workers/__tests__/defillama-adapter.test.ts
      provides: "DefiLlama EIP-712 attestation signing unit test"
    - path: apps/relayer/src/workers/__tests__/cex-binance.test.ts
      provides: "CEX static-HTML fixture test + Innovation Zone exclusion for Binance"
    - path: apps/relayer/src/workers/__tests__/outcome-word.test.ts
      provides: "CONTRARIAN HIT when fadeShare>=50%; COLD CALL when delta<=3; TS/Solidity parity; viewerIsWinningFader=false when wallet disconnected"
  key_links:
    - from: SmTestHelper
      to: CeTestHelper
      via: "extends CeTestHelper; super.setUp() boots CR+FFM+CE+PR with MockUSDC"
      pattern: "abstract contract SmTestHelper is CeTestHelper"
    - from: SettlementManagerForkTest.sol
      to: native USDC 0xaf88d065
      via: "forge test --fork-url $ARB_ONE_RPC_URL"
      pattern: "vm.createSelectFork.*ARB_ONE_RPC"
---

<objective>
Wave 0: Create all RED-gate test scaffolds before any implementation code exists.

Purpose: Establish the test surface that will turn GREEN in Plan 04-02. Every money-critical invariant (fee split, pool conservation, idempotency, CEI, dispute reversal) must be a failing test first, so no implementation can ship without proofs. This is the TDD gate per 04-VALIDATION.md "Wave 0 Requirements".

Output: 9 test scaffold files (Foundry + Vitest) that compile but fail until their production counterparts exist.
</objective>

<execution_context>
@/home/.claude/get-shit-done/workflows/execute-plan.md
@/home/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-settlementmanager-7-oracle-paths-solidity-baseline-rep-delta/04-CONTEXT.md
@.planning/phases/04-settlementmanager-7-oracle-paths-solidity-baseline-rep-delta/04-RESEARCH.md
@.planning/phases/04-settlementmanager-7-oracle-paths-solidity-baseline-rep-delta/04-VALIDATION.md
@.planning/phases/04-settlementmanager-7-oracle-paths-solidity-baseline-rep-delta/04-PATTERNS.md

<interfaces>
<!-- From packages/contracts/test/helpers/CeTestHelper.sol — extend this -->
From packages/contracts/test/helpers/CeTestHelper.sol (abstract base):
```solidity
abstract contract CeTestHelper is FfmTestHelper {
    ChallengeEscrow internal ce;
    function setUp() public virtual override { super.setUp(); ... ce = new ChallengeEscrow(...); ... }
}
```

From packages/contracts/test/helpers/FfmTestHelper.sol:
```solidity
abstract contract FfmTestHelper is Test {
    FollowFadeMarket internal ffm;
    CallRegistry internal cr;
    ProfileRegistry internal pr;
    MockUSDC internal usdc; // uses vm.etch for mainnet addr
    address internal owner; address internal alice; address internal bob; address internal treasury;
    function setUp() public virtual { ... }
}
```

From packages/contracts/src/ChallengeEscrow.sol (existing pattern for settleDuel mock):
```solidity
function _settleDuel(uint256 challengeId, address winner) internal {
    address sm = makeAddr("settlementManager");
    vm.prank(owner); ce.setSettlementManager(sm);
    vm.prank(sm); ce.settleDuel(challengeId, winner);
}
```

From packages/contracts/foundry.toml — fuzz profile:
```toml
[profile.ci]
fuzz = { runs = 1000 }
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Foundry test scaffolds — SmTestHelper + SettlementManagerTest + FfmSettlementTest + SettlementDisputeTest</name>
  <files>
    packages/contracts/test/helpers/SmTestHelper.sol,
    packages/contracts/test/SettlementManagerTest.sol,
    packages/contracts/test/FfmSettlementTest.sol,
    packages/contracts/test/SettlementDisputeTest.sol,
    packages/contracts/test/SettlementManagerForkTest.sol
  </files>
  <read_first>
    - packages/contracts/test/helpers/CeTestHelper.sol (extend pattern — SmTestHelper extends CeTestHelper)
    - packages/contracts/test/helpers/FfmTestHelper.sol (C3 linearization chain; super.setUp() pattern)
    - packages/contracts/test/ChallengeEscrow.t.sol (event assertion pattern, mock-settManager, fuzz invariant structure)
    - packages/contracts/test/FollowFadeMarket.t.sol (claimPayout setup, reserve state)
    - packages/contracts/src/constants/USDC.sol (USDC_ARB_NATIVE)
    - packages/contracts/foundry.toml (=0.8.30 pin, ci fuzz profile 1000 runs)
    - .planning/phases/04-settlementmanager-7-oracle-paths-solidity-baseline-rep-delta/04-RESEARCH.md (§ Validation Architecture, §Wave 0 Gaps, §Foundry Property-Fuzz Invariants, §14-step settle sequence, §ADR-0001 note)
    - .planning/decisions/0001-sepolia-staging-usdc.md (mainnet fork mandate)
  </read_first>
  <behavior>
    RED gate: All tests fail to compile (missing SettlementManager, missing FFM applySettlement/claimPayout, missing dispute functions).

    SmTestHelper.sol:
    - abstract contract SmTestHelper is CeTestHelper
    - Declares: SettlementManager internal sm;
    - setUp() calls super.setUp() then deploys sm with (cr, ffm, ce, pr, USDC_ARB_NATIVE, treasury, PYTH_ADDRESS) where PYTH_ADDRESS = makeAddr("pyth") for unit tests
    - Calls cr.setSettlementManager(address(sm)), ffm.setSettlementManager(address(sm)), ce.setSettlementManager(address(sm)), pr.setSettlementManager(address(sm)), pr.setAuthorizedRepWriter(address(sm), true) all vm.prank(owner)

    SettlementManagerTest.sol:
    - testSettleIdempotency: second settle reverts AlreadySettled (SETTLE-02)
    - testCallNotExpired: settle() before expiry reverts CallNotExpired (SETTLE-03)
    - testAtomicRollback: mock a step-N failure, assert whole tx reverts, balances unchanged (SETTLE-05)
    - testPythConfidenceGate: wide confidence (conf*200 > price) → outcome == Pending, SettlementDelayed emitted (SETTLE-08)
    - invariantFeeSplit: total fees == 1.0%+0.4%+0.3% of totalPool within 2 wei dust (SETTLE-46)
    - invariantPoolConservation: followReserve + fadeRealReserve + feesTransferred == preSettlePool (SETTLE-46)
    - testSettleGas: forge snapshot, gas O(1) unaffected by participant count (SETTLE-44)
    - testColdStartScale: fadeRealReserve==0 → winner repDelta == 25% of uncapped delta (REP-14)
    - testStylusFallback: deliberate-revert mock StylusEngine → _solidityBaselineRepDelta fires, RepCalculatedFallback emitted (REP-22/23)
    - testForceSettleCooldown: forceSettle before 7d from expiry reverts ForceSettleCooldownActive (SETTLE-39)
    - testForceSettleEvents: forceSettle emits BOTH CallForceSettled AND CallSettled (SETTLE-40)
    - testDuelInvalidChallengeId: settle(callId, [], [wrongChallengeId]) reverts when challenge.callId != callId (SETTLE-43 guard, Blocker 3 fix). Also test: settle with challenge.status != Accepted reverts. Validates that the off-chain-supplied acceptedChallengeIds array is verified on-chain per IChallengeEscrow.getChallenge().

    FfmSettlementTest.sol:
    - testClaimPayoutCEI: claimed[callId][user]=true BEFORE safeTransfer; second call reverts AlreadyClaimed (SOCIAL-47)
    - testApplySettlementIdempotency: second applySettlement reverts (settlementApplied guard)
    - testEmptyPoolToTreasury: fadeRealReserve==0 → entire followReserve (minus fees) to treasury (CALL-41, Pitfall 22)
    - testClaimPayoutProRata: payout == Math.mulDiv(userShares, winningReserve, totalShares)

    SettlementDisputeTest.sol:
    - testDisputeBondTaken: raiseDispute transfers $5 USDC bond (SETTLE-25/26)
    - testDisputeWindowClosed: raiseDispute after 24h window reverts DisputeWindowClosed (SETTLE-29)
    - testMaxCounterClaims: 4th counter-claim reverts MaxCounterClaimsReached (SETTLE-30)
    - testDisputeReversal: resolveDispute(callId, oppOutcome) re-distributes USDC from old-winner→new-winner and reverses rep (SETTLE-34)
    - testForceSettleAfterDispute: forceSettle callable only after FORCE_SETTLE_COOLDOWN=7 days (SETTLE-39)

    SettlementManagerForkTest.sol:
    - Header comment: "// ADR-0001: forge test --fork-url $ARB_ONE_RPC_URL"
    - Uses vm.createSelectFork(vm.envString("ARB_ONE_RPC_URL")) in setUp()
    - testRealUsdcSettle: full settle() with real USDC 0xaf88d065...e5831; verifies fee transfers land in treasury
    - testRealUsdcClaimPayout: claimPayout sends real USDC to winner

    All files: pragma solidity =0.8.30; exact pin + RED GATE comment
    All fuzz invariants annotated: forge test --match-test invariant* --fuzz-runs 1000
  </behavior>
  <verify>
    <automated>cd /c/Users/woshv/Desktop/Call\ it && forge build --root packages/contracts 2>&1 | grep -E "error|Error" | grep -v "SettlementManager\|applySettlement\|claimPayout" | head -20; echo "RED gate: compile errors for missing SM types are expected"</automated>
  </verify>
  <acceptance_criteria>
    - All 5 Solidity test files exist at the listed paths with pragma =0.8.30 exact pin
    - SmTestHelper.sol declares `abstract contract SmTestHelper is CeTestHelper`
    - SettlementManagerTest.sol contains at minimum: testSettleIdempotency, testCallNotExpired, testAtomicRollback, testPythConfidenceGate, invariantFeeSplit, invariantPoolConservation, testColdStartScale, testStylusFallback, testForceSettleCooldown, testForceSettleEvents, testDuelInvalidChallengeId (grep check: `grep -c "function test\|function invariant" packages/contracts/test/SettlementManagerTest.sol` >= 11)
    - SettlementManagerTest.sol: testDuelInvalidChallengeId asserts revert when challenge.callId != callId AND when challenge status != Accepted (SETTLE-43 on-chain guard)
    - FfmSettlementTest.sol contains testClaimPayoutCEI with `claimed[callId][msg.sender] = true` BEFORE safeTransfer comment
    - SettlementDisputeTest.sol contains testDisputeWindowClosed and testDisputeReversal
    - SettlementManagerForkTest.sol contains `ARB_ONE_RPC_URL` and `createSelectFork`
    - forge build fails with "file not found" / "undeclared identifier" for SettlementManager (expected RED gate — not a random error)
    - No USDC literal address in any test file (grep: `grep -r "0xaf88\|0xFF97" packages/contracts/test/Settlement` returns 0 for inline literals; USDC_ARB_NATIVE import is OK)
  </acceptance_criteria>
  <done>All Foundry test scaffolds exist, pragma =0.8.30 pinned, RED gate confirmed (forge build fails on missing SettlementManager types), SmTestHelper inheritance chain valid, fork test annotated with ADR-0001 mandate, testDuelInvalidChallengeId covers SETTLE-43 on-chain guard for the off-chain-supplied challenge array.</done>
</task>

<task type="tdd">
  <name>Task 2: Vitest adapter test scaffolds — Pyth retry, DefiLlama EIP-712, CEX Binance fixture, outcome-word parity</name>
  <files>
    apps/relayer/src/workers/__tests__/pyth-adapter.test.ts,
    apps/relayer/src/workers/__tests__/defillama-adapter.test.ts,
    apps/relayer/src/workers/__tests__/cex-binance.test.ts,
    apps/relayer/src/workers/__tests__/outcome-word.test.ts
  </files>
  <read_first>
    - apps/relayer/src/lib/kms-signer.ts (gcpKmsAccount, AttestationType, gcpKmsAccount signature pattern — 5 keys)
    - apps/relayer/src/workers/cex-heartbeat.ts (CEX_EXCHANGES list, scraper stub structure, testWithFixture export pattern)
    - apps/relayer/package.json (vitest config, existing test patterns)
    - .planning/phases/04-settlementmanager-7-oracle-paths-solidity-baseline-rep-delta/04-RESEARCH.md (§Adapter 1 Pyth, §Adapter 2 DefiLlama, §Adapter 7 CEX, §D-08 outcome-word thresholds, §INNOVATION_ZONE_EXCLUSION_PATTERNS)
    - .planning/phases/04-settlementmanager-7-oracle-paths-solidity-baseline-rep-delta/04-PATTERNS.md (§ViTest adapter patterns)
  </read_first>
  <behavior>
    RED gate: All Vitest tests fail with "Cannot find module" or "is not a function" until adapters are created in Plan 04-03.

    pyth-adapter.test.ts:
    - Mocks HermesClient.getLatestPriceUpdates with vi.mock
    - testWideConfidenceRetry: adapter returns SettlementDelayed when conf*200 > price (SETTLE-08/10)
    - testRetryExhausted: after 30 failed retries, adapter calls openDisputeWindow (SETTLE-11)
    - testSuccessfulPythFetch: valid price → returns updateData array + feeWei
    - All mock HermesClient responses use binary.data format (string[] hex)

    defillama-adapter.test.ts:
    - Mocks gcpKmsAccount signTypedData (vi.mock kms-signer.ts)
    - testAttestation: verifies EIP-712 domain has name="CallIt-DefiLlama", chainId=42161n, verifyingContract=SETTLEMENT_MANAGER_ADDRESS
    - testChainIdBinding: attestation with chainId=1 fails ecrecover check
    - testFetchTvl: mocked fetch to api.llama.fi returns tvl value → signed attestation

    cex-binance.test.ts:
    - Uses static HTML fixture file: apps/relayer/src/workers/__tests__/fixtures/binance-listing.html
    - Create the fixture: minimal valid Binance announcement HTML with "TokenX (TKX) listing" in a post title published within 24h
    - testDetectsListing: scraper detects TokenX in fixture → returns true
    - testInnovationZoneExclusion: fixture post with "Innovation Zone" in title → scraper returns false (Pitfall 19)
    - testMultiSignalConfirm: only symbol match without full name → returns false

    outcome-word.test.ts (D-08 Foundry↔Vitest parity gate):
    - Import getOutcomeWord() from apps/web/lib/outcome-word.ts (create stub with TODO: implement)
    - testContrarianHit: fadeRealShare=0.55, isWinner=true → "CONTRARIAN HIT"
    - testCalledIt: fadeRealShare=0.3, isWinner=true → "CALLED IT"
    - testColdCall: isWinner=true, repDelta=2 → "COLD CALL" (delta <= 3 threshold)
    - testLoudAndWrong: isWinner=false → "LOUD AND WRONG"
    - testFadedCorrectly: viewerIsWinningFader=true, isWinner=false → "FADED CORRECTLY"
    - testPublicViewer: viewerIsWinningFader=false (wallet disconnected / no position), isWinner=false → "LOUD AND WRONG" (NOT "FADED CORRECTLY"; D-09 — FADED CORRECTLY only for actual winning faders with connected wallet)
    - These tests define the D-08 thresholds: CONTRARIAN HIT when fadeRealShare >= 0.5; COLD CALL when delta <= 3

    Create fixture file: apps/relayer/src/workers/__tests__/fixtures/binance-listing.html (minimal static HTML)

    All test files: TypeScript strict mode, import from module path that will exist post-Plan 04-03
  </behavior>
  <verify>
    <automated>cd /c/Users/woshv/Desktop/Call\ it && pnpm --filter @call-it/relayer test --run 2>&1 | grep -E "FAIL|Cannot find module|not a function" | head -10; echo "RED gate: module-not-found errors expected"</automated>
  </verify>
  <acceptance_criteria>
    - All 4 Vitest test files exist at listed paths
    - cex-binance.test.ts fixture file exists at apps/relayer/src/workers/__tests__/fixtures/binance-listing.html
    - outcome-word.test.ts defines the CONTRARIAN HIT threshold as fadeRealShare >= 0.5 and COLD CALL as repDelta <= 3 (these become the spec-of-record for D-08)
    - outcome-word.test.ts: testPublicViewer asserts viewerIsWinningFader=false returns caller-centric word (LOUD AND WRONG when CallerLost), never "FADED CORRECTLY" (D-09 — public viewer sees caller-centric outcome, not fader outcome)
    - defillama-adapter.test.ts verifies EIP-712 domain chainId=42161n (Pitfall 7)
    - `pnpm --filter @call-it/relayer test --run` fails with module-not-found errors (expected RED gate)
    - No inline USDC or contract addresses in test files (use process.env or named imports)
  </acceptance_criteria>
  <done>All Vitest adapter scaffolds exist with RED gate confirmed (module-not-found errors), D-08 outcome-word thresholds codified as tests (CONTRARIAN HIT=fadeShare>=0.5, COLD CALL=delta<=3), fixture HTML exists for Binance Innovation Zone exclusion test, testPublicViewer asserts viewerIsWinningFader=false never returns "FADED CORRECTLY".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → contract | Unit tests interact with contract state via vm.prank / vm.deal — no external trust crossing in scaffolds |
| test → KMS mock | Vitest mocks gcpKmsAccount — no real KMS calls; mock isolation is essential |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | SettlementManagerTest invariantFeeSplit | mitigate | Fuzz invariant (1000 runs) asserts fee == 1.7% ± 2 wei dust; CI blocks on failure |
| T-04-01-02 | Tampering | FfmSettlementTest testClaimPayoutCEI | mitigate | Test asserts claimed=true BEFORE safeTransfer; will fail if CEI is violated in implementation |
| T-04-01-03 | Repudiation | SettlementDisputeTest testDisputeReversal | mitigate | Test asserts USDC re-distribution from old-winner to new-winner on resolveDispute reversal |
| T-04-01-04 | Tampering | defillama-adapter.test.ts chainId binding | mitigate | Test asserts EIP-712 domain chainId=42161n; wrong-chain attestation fails ecrecover |
| T-04-01-05 | Tampering | cex-binance.test.ts Innovation Zone | mitigate | Static fixture test asserts Innovation Zone exclusion returns false per T-04-01 (Pitfall 19) |
| T-04-01-06 | Tampering | testDuelInvalidChallengeId | mitigate | Test asserts settle() reverts when off-chain-supplied challengeId has wrong callId or non-Accepted status — prevents fake challenge injection (SETTLE-43) |
</threat_model>

<verification>
Foundry scaffolds compile to RED gate (missing SettlementManager types expected).
Vitest scaffolds import from not-yet-created adapter modules (expected module-not-found).
No inline addresses, no ^0.8.x pragma, no display:grid.
D-08 thresholds (fadeShare>=0.5, delta<=3) are test-codified.
testPublicViewer asserts viewerIsWinningFader=false yields caller-centric word.
testDuelInvalidChallengeId covers SETTLE-43 on-chain guard.
</verification>

<success_criteria>
- 5 Foundry test files exist with pragma =0.8.30, RED gate on missing SM types
- 4 Vitest test files exist, RED gate on missing adapter modules
- Binance fixture HTML at correct path
- D-08 outcome-word thresholds recorded as executable Vitest specs
- testPublicViewer asserts viewerIsWinningFader=false (no connected wallet) never returns FADED CORRECTLY
- testDuelInvalidChallengeId: invalid challenge array entry reverts settle()
- forge build and pnpm test both fail with the expected missing-module errors (not arbitrary errors)
</success_criteria>

<output>
After completion, create `.planning/phases/04-settlementmanager-7-oracle-paths-solidity-baseline-rep-delta/04-01-SUMMARY.md`
</output>
