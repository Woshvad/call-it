---
phase: 09-mobile-responsive-on-7-critical-pages
plan: 05
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - apps/web/app/page.tsx
  - apps/web/app/profile/[address]/ProfileClient.tsx
  - apps/web/components/ProfileTabs.tsx
  - apps/web/app/leaderboard/LeaderboardClient.tsx
autonomous: true
requirements: [UI-48, UI-49]
must_haves:
  truths:
    - "At 375px the Feed renders single-column with no horizontal scroll; DuelRowCard stacks (drops the two minWidth:120px cells) (UI-48)"
    - "At 375px the Profile clamps its 680px container and stacks the 3-card CATEGORY REPUTATION grid to a single column; ProfileTabs are >=44px tall (UI-48/UI-49)"
    - "At 375px the Leaderboard 4-column row fits 343px with NO column drop (it is 4-col in live code, not the 6-col the UI-SPEC assumed — RESEARCH divergence #1) and the viewer-row accent (#E8F542 left border + #1A1A24 bg) survives mobile"
    - "Tailwind typography classNames on ProfileClient.tsx and LeaderboardClient.tsx are NOT touched — only inline layout styles are swapped (RESEARCH divergence #4)"
    - "Filter chips / tabs / each leaderboard row meet the 44x44 touch-target minimum at mobile (D-03)"
  artifacts:
    - path: "apps/web/app/page.tsx"
      provides: "375px-responsive Feed: container clamp + DuelRowCard column-stack + >=44px chips"
      contains: "useIsMobile"
    - path: "apps/web/app/profile/[address]/ProfileClient.tsx"
      provides: "375px-responsive Profile: container clamp + CATEGORY REPUTATION column-stack (inline layout only)"
      contains: "useIsMobile"
    - path: "apps/web/app/leaderboard/LeaderboardClient.tsx"
      provides: "375px-responsive Leaderboard: 4-col row clamped to 343px, viewer-row accent preserved (no column drop, no grid)"
      contains: "useIsMobile"
  key_links:
    - from: "apps/web/app/page.tsx"
      to: "apps/web/app/hooks/useIsMobile.ts"
      via: "useIsMobile() drives Feed container clamp + DuelRowCard stack"
      pattern: "useIsMobile"
    - from: "apps/web/app/leaderboard/LeaderboardClient.tsx"
      to: "apps/web/app/hooks/useIsMobile.ts"
      via: "useIsMobile() clamps the 760px container; viewer-row accent preserved through mobile path"
      pattern: "useIsMobile"
---

<objective>
The Feed / Profile / Leaderboard cluster (UI-48/UI-49). These three are the lightest critical pages — mostly container-clamp + a single row-stack each — and live in separate files so they are conflict-free. After this plan, the three feed-adjacent pages a returning user navigates render single-column at 375px with no horizontal scroll.

Key reconciliations baked in (RESEARCH divergences): the Leaderboard is a **4-column** row in live code (not the 6-column table the UI-SPEC assumed) so there is NOTHING to drop — just clamp to fit and preserve the viewer-row accent; and `ProfileClient`/`LeaderboardClient` carry Tailwind **typography** classNames that must NOT be touched (swap inline layout only).

Purpose: UI-48/UI-49 for Feed + Profile + Leaderboard.
Output: `apps/web/app/page.tsx`, `apps/web/app/profile/[address]/ProfileClient.tsx`, `apps/web/components/ProfileTabs.tsx`, `apps/web/app/leaderboard/LeaderboardClient.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-mobile-responsive-on-7-critical-pages/09-CONTEXT.md
@.planning/phases/09-mobile-responsive-on-7-critical-pages/09-RESEARCH.md
@.planning/phases/09-mobile-responsive-on-7-critical-pages/09-UI-SPEC.md
@.planning/phases/09-mobile-responsive-on-7-critical-pages/09-PATTERNS.md
@apps/web/app/hooks/useIsMobile.ts
</context>

<artifacts_this_phase_produces>
- `apps/web/app/page.tsx` (Feed) — `isMobile`-conditional container clamp + `DuelRowCard` column-stack + ≥44px filter chips.
- `apps/web/app/profile/[address]/ProfileClient.tsx` — container clamp + CATEGORY REPUTATION 3-card→1-column stack (inline layout only).
- `apps/web/components/ProfileTabs.tsx` — tab row ≥44px tall at mobile (note: lives at `apps/web/components/`, NOT `app/components/`).
- `apps/web/app/leaderboard/LeaderboardClient.tsx` — 4-col row clamped to 343px, viewer-row accent preserved, no grid, no column drop.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Feed page.tsx — container clamp + DuelRowCard column-stack + >=44px chips</name>
  <read_first>
    - apps/web/app/page.tsx (container maxWidth:'680px' ~line 727; DuelRowCard ~lines 232-249 with two minWidth:'120px' cells ~lines 235/246/353; filter chips already flexWrap:'wrap'; 1 stray className= to confirm is not layout)
    - apps/web/app/hooks/useIsMobile.ts
    - .planning/phases/09-mobile-responsive-on-7-critical-pages/09-UI-SPEC.md (§"1. Feed")
    - .planning/phases/09-mobile-responsive-on-7-critical-pages/09-RESEARCH.md (Per-Page Inventory §1)
  </read_first>
  <action>
    Modify `apps/web/app/page.tsx`. Add `const isMobile = useIsMobile();`. Container (`maxWidth:'680px'` ~line 727) → at mobile `{ width:'100%', maxWidth:'100%', padding:'0 16px' }` (Pattern 2). `DuelRowCard` (~lines 232-249): the main `flexDirection:'row'` holding two `minWidth:'120px'` cells (caller ~246, challenger symmetric) overflows 343px → at mobile flip the card to `flexDirection:'column'`, DROP the `minWidth:'120px'` (cells `width:'100%'`). Filter chips: already `flexWrap:'wrap'` — ensure each chip is ≥44px tall at mobile (D-03). Confirm the 1 stray `className=` on this file is NOT a layout/responsive class (it isn't, per RESEARCH); leave it. Grep the file for any other `width: '` / `minWidth:` > 343px and clamp. No `display:grid`.
  </action>
  <verify>
    <automated>cd apps/web && pnpm build 2>&1 | tail -8 && pnpm exec playwright test tests/responsive.spec.ts -g "no horizontal scroll — /\b|touch target — /\b"</automated>
  </verify>
  <acceptance_criteria>
    - `page.tsx` imports + calls `useIsMobile`; container clamps to `width:'100%'`/`padding:'0 16px'` at mobile.
    - `DuelRowCard` flips to `flexDirection:'column'` and drops `minWidth:'120px'` at mobile.
    - `playwright test -g "no horizontal scroll"` + `-g "touch target"` pass for `/`.
    - No `display:grid` introduced; `cd apps/web && pnpm build` exits 0.
  </acceptance_criteria>
  <done>Feed is single-column at 375px, DuelRowCard stacks, chips ≥44px, no horizontal scroll.</done>
</task>

<task type="auto">
  <name>Task 2: Profile — container clamp + CATEGORY REPUTATION stack + ProfileTabs >=44px (inline layout only)</name>
  <read_first>
    - apps/web/app/profile/[address]/ProfileClient.tsx (container maxWidth:'680px' line 57; stat row ~line 114 already flexWrap:'wrap'; CATEGORY REPUTATION 3 cards flex:'1 1 0' minWidth:160px ~lines 128-133; Tailwind typography classNames lines 125-238 — DO NOT TOUCH)
    - apps/web/components/ProfileTabs.tsx (tab row flexDirection:'row' ~line 36 — NOTE this file is at apps/web/components/, NOT app/components/)
    - apps/web/app/hooks/useIsMobile.ts
    - .planning/phases/09-mobile-responsive-on-7-critical-pages/09-UI-SPEC.md (§"4. Profile")
    - .planning/phases/09-mobile-responsive-on-7-critical-pages/09-RESEARCH.md (Per-Page Inventory §4 + Divergence #4 — typography classNames stay, swap inline layout only)
  </read_first>
  <action>
    Modify `apps/web/app/profile/[address]/ProfileClient.tsx`: add `const isMobile = useIsMobile();`; container (`maxWidth:'680px'` line 57) → mobile `{ width:'100%', maxWidth:'100%', padding:'0 16px' }`. CATEGORY REPUTATION grid (~lines 128-133, 3 cards `flex:'1 1 0'` `minWidth:160px` → 3×160=480 > 343) → at mobile force the wrapper `flexDirection:'column'`, drop the `minWidth:160px`, each card `width:'100%'`. Stat row (~line 114) is already `flexWrap:'wrap'` — leave; ensure interactive elements ≥44px. CRITICAL: do NOT touch the Tailwind typography classNames (`font-mono`, `text-xs`, `text-brand-muted`, `uppercase`, `tracking-wide`, etc., lines 125-238) — they are typography, not layout (Divergence #4); swap ONLY inline `style={{}}` layout values. Then modify `apps/web/components/ProfileTabs.tsx` (consume `useIsMobile` via `../app/hooks/useIsMobile` — confirm relative path from `apps/web/components/`): the tab row (`flexDirection:'row'` ~line 36) — ensure each tab is ≥44px tall at mobile; allow `flexWrap:'wrap'` if the tabs exceed 343px width. No `display:grid`.
  </action>
  <verify>
    <automated>cd apps/web && pnpm build 2>&1 | tail -8 && pnpm exec playwright test tests/responsive.spec.ts -g "no horizontal scroll.*profile|touch target.*profile"</automated>
  </verify>
  <acceptance_criteria>
    - `ProfileClient.tsx` imports + calls `useIsMobile`; container clamps at mobile; CATEGORY REPUTATION stacks to `flexDirection:'column'` with `minWidth` dropped.
    - Tailwind typography classNames are byte-for-byte unchanged (a `git diff` on className lines shows no edits).
    - `ProfileTabs.tsx` tabs are ≥44px tall at mobile.
    - `playwright test -g "no horizontal scroll"` + `-g "touch target"` pass for the profile path.
    - No `display:grid`; `cd apps/web && pnpm build` exits 0.
  </acceptance_criteria>
  <done>Profile is single-column at 375px, category cards stack, tabs ≥44px, typography classNames untouched, no horizontal scroll.</done>
</task>

<task type="auto">
  <name>Task 3: Leaderboard — clamp 4-col row to 343px, preserve viewer-row accent (no column drop, no grid)</name>
  <read_first>
    - apps/web/app/leaderboard/LeaderboardClient.tsx (container maxWidth:'760px' line 68; LeaderboardTableRow lines ~247-282 with 4 cells # width:48px / Caller flex:1 / Rep width:80px / Calls width:72px, header lines ~220-223; viewer-row accent lines 257-258: borderLeft:'3px solid #E8F542' + backgroundColor:'#1A1A24'; each row is an <a href="/profile/..."> line ~249; Tailwind typography classNames — DO NOT TOUCH)
    - apps/web/app/hooks/useIsMobile.ts
    - .planning/phases/09-mobile-responsive-on-7-critical-pages/09-UI-SPEC.md (§"5. Leaderboard" — note the UI-SPEC describes a 6-col table)
    - .planning/phases/09-mobile-responsive-on-7-critical-pages/09-RESEARCH.md (Divergence #1 — CRITICAL: live code is 4-col, NOT 6-col; the "drop ACC/BEST/sparkline" instruction is MOOT; just clamp to fit 343px and preserve the viewer accent; treat the missing UI-13 columns as OUT OF SCOPE for this phase)
  </read_first>
  <action>
    Modify `apps/web/app/leaderboard/LeaderboardClient.tsx`: add `const isMobile = useIsMobile();`; container (`maxWidth:'760px'` line 68) → mobile `{ width:'100%', maxWidth:'100%', padding:'0 16px' }`. RECONCILIATION (Divergence #1): the live row is 4-column (`#` width:48px · `Caller` flex:1 · `Rep` width:80px · `Calls` width:72px ≈ 200px fixed + flex), which ALREADY fits 343px. So DO NOT drop any columns and DO NOT implement the UI-SPEC's described 6-col→3-col collapse (those ACC/BEST/sparkline columns do not exist in this surface — the UI-13 gap is OUT OF SCOPE for Phase 9, flagged separately to the user). Keep the row as `flexDirection:'row'`. The mobile work is: (a) clamp the container; (b) ensure each row (an `<a href="/profile/...">`, ~line 249) is ≥44px tall (D-03); (c) PRESERVE the viewer's-own-row accent through the mobile path — `borderLeft:'3px solid #E8F542'` + `backgroundColor:'#1A1A24'` (lines 257-258) must still apply when `isMobile`; (d) confirm the 48/80/72px fixed cells + the `Caller` flex cell do not force scroll at 343px (they shouldn't — verify with the no-horizontal-scroll test). Do NOT touch Tailwind typography classNames. Do NOT introduce `display:grid`.
  </action>
  <verify>
    <automated>cd apps/web && pnpm build 2>&1 | tail -8 && pnpm exec playwright test tests/responsive.spec.ts -g "no horizontal scroll.*leaderboard|touch target.*leaderboard" && pnpm exec playwright test tests/leaderboard.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `LeaderboardClient.tsx` imports + calls `useIsMobile`; container clamps at mobile.
    - Row stays `flexDirection:'row'` (no column drop); viewer-row accent (`borderLeft:'3px solid #E8F542'` + `#1A1A24` bg) still present under the mobile path (grep both tokens present).
    - Each row is ≥44px tall at mobile.
    - `playwright test -g "no horizontal scroll"` + `-g "touch target"` pass for `/leaderboard`; existing `leaderboard.spec.ts` still passes (no desktop regression).
    - Tailwind typography classNames unchanged; no `display:grid`; `cd apps/web && pnpm build` exits 0.
  </acceptance_criteria>
  <done>Leaderboard 4-col row fits 343px with no scroll, viewer-row accent preserved, rows ≥44px, no column drop, no grid.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → relayer/subgraph | Unchanged — these three pages keep their existing data reads; this plan only swaps inline layout styles. No new fetch, input, or auth surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05-01 | Information disclosure | Profile / Leaderboard mobile layout | mitigate | Layout-only swaps on the same JSX trees — no conditionally-rendered new content, no owner-only control surfaced. The viewer-row accent on Leaderboard reveals only the already-public "this is you" highlight (same as desktop). |
| T-09-05-02 | Tampering | Tailwind typography classNames | accept | Explicitly NOT edited (Divergence #4) — zero risk of a className-driven visual regression because they are untouched. |
| T-09-05-SC | Tampering | npm/pnpm installs | mitigate | No new packages. Supply-chain gate N/A. |
</threat_model>

<verification>
- `cd apps/web && pnpm build` exits 0.
- `pnpm exec playwright test tests/responsive.spec.ts -g "no horizontal scroll|touch target"` green for `/`, profile, `/leaderboard`.
- Existing `leaderboard.spec.ts`, `feed-shell.spec.ts`, `profile-shell.spec.ts`, `profile-overview.spec.ts` still pass.
- No `display:grid` added to any of the 4 files.
</verification>

<success_criteria>
- Feed, Profile, Leaderboard render single-column / clamped at 375px with no horizontal scroll and ≥44px targets.
- Leaderboard keeps 4 columns (no drop) and the viewer-row accent; Tailwind typography untouched.
- No new dependency; no grid.
</success_criteria>

<output>
Create `.planning/phases/09-mobile-responsive-on-7-critical-pages/09-05-SUMMARY.md` when done.
</output>
