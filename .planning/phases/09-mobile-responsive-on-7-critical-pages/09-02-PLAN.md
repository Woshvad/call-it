---
phase: 09-mobile-responsive-on-7-critical-pages
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - apps/web/app/components/MobileDrawer.tsx
  - apps/web/app/components/GlobalNav.tsx
autonomous: true
requirements: [UI-49]
must_haves:
  truths:
    - "At mobile width a hamburger button appears on the LEFT of GlobalNav and opens a left-anchored slide-over drawer (D-05)"
    - "The drawer lists auth-aware destinations: Feed + Leaderboard always; Profile + New Call gated on authenticated && ready; Sign in / Sign out toggled on authenticated (D-06)"
    - "An unauthenticated viewer NEVER sees New Call / Profile / Sign out actionable in the drawer (D-06 / Pitfall 5 auth-flicker guard)"
    - "The NotificationBell stays pinned in the top bar on mobile — NOT moved into the drawer (D-06)"
    - "The drawer closes on Esc, backdrop click, and any link tap (Claude's-discretion, mirrors NotificationInbox)"
    - "Desktop GlobalNav is unchanged (hamburger renders only when isMobile) (D-05)"
  artifacts:
    - path: "apps/web/app/components/MobileDrawer.tsx"
      provides: "Left-anchored slide-over nav drawer mirroring NotificationInbox; auth-aware destinations (D-05/D-06)"
      contains: "usePrivy"
    - path: "apps/web/app/components/GlobalNav.tsx"
      provides: "Hamburger trigger (mobile only) + drawer open-state, NotificationBell still pinned (D-05/D-06)"
      contains: "useIsMobile"
  key_links:
    - from: "apps/web/app/components/GlobalNav.tsx"
      to: "apps/web/app/components/MobileDrawer.tsx"
      via: "useState open-state + conditional render gated on useIsMobile"
      pattern: "MobileDrawer"
    - from: "apps/web/app/components/MobileDrawer.tsx"
      to: "@privy-io/react-auth"
      via: "usePrivy() authenticated/ready/logout for D-06 gating"
      pattern: "usePrivy"
---

<objective>
The mobile navigation slice (UI-49 "left sidebar collapsed behind a hamburger"). The app has no sidebar today — `GlobalNav` is just wordmark + NotificationBell (verified 56 lines) — so the hamburger drawer is additive and fills a real gap: there is currently no global mobile navigation. This slice also directly satisfies SC2's "banner does not block return navigation or sign-out" because the drawer is the exit path that stays active on the banner pages (D-07).

After this plan: a mobile user can open a left drawer from any page, navigate to Feed/Leaderboard/Profile/New Call (auth-gated), and Sign in / Sign out — including from the desktop-only-banner pages.

Purpose: UI-49 literal requirement + the only global mobile navigation surface.
Output: `apps/web/app/components/MobileDrawer.tsx` (new), `apps/web/app/components/GlobalNav.tsx` (modified).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-mobile-responsive-on-7-critical-pages/09-CONTEXT.md
@.planning/phases/09-mobile-responsive-on-7-critical-pages/09-UI-SPEC.md
@.planning/phases/09-mobile-responsive-on-7-critical-pages/09-PATTERNS.md
@apps/web/app/components/NotificationInbox.tsx
@apps/web/app/components/GlobalNav.tsx
@apps/web/app/hooks/useIsMobile.ts
</context>

<artifacts_this_phase_produces>
- **`apps/web/app/components/MobileDrawer.tsx`** — exports `MobileDrawer` (props: `{ open: boolean; onClose: () => void }`). Left-anchored slide-over; auth-aware destination list; closes on Esc / backdrop / link-tap.
- **`apps/web/app/components/GlobalNav.tsx`** (modified) — adds a hamburger `<button aria-label="Open menu">` (glyph `☰`, accent `#E8F542`, ≥44px, `isMobile`-only) to the LEFT of the wordmark; owns drawer open-state (`useState`); renders `<MobileDrawer>`. `NotificationBell` stays pinned.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: MobileDrawer.tsx — left-anchored auth-aware slide-over</name>
  <read_first>
    - apps/web/app/components/NotificationInbox.tsx (the slide-over analog — copy verbatim: Esc-close useEffect ~lines 168-176; backdrop-click handler ~lines 215-222; backdrop+panel JSX ~lines 236-266; usePrivy import line 28 + usage ~line 166)
    - .planning/phases/09-mobile-responsive-on-7-critical-pages/09-PATTERNS.md (MobileDrawer pattern block — exact panel deltas: left:0, width 'min(300px,85vw)', boxShadow '4px 0 0 0 #E8F542', borderRight accent)
    - .planning/phases/09-mobile-responsive-on-7-critical-pages/09-UI-SPEC.md (Mobile Navigation Contract table — panel/backdrop/destinations/close-behavior/link-styling values; Copywriting Contract for drawer labels)
    - .planning/phases/09-mobile-responsive-on-7-critical-pages/09-CONTEXT.md (D-05, D-06, D-07, Pitfall 5)
  </read_first>
  <action>
    Create `apps/web/app/components/MobileDrawer.tsx` as a `'use client'` structural mirror of `NotificationInbox.tsx`, anchored LEFT. Props `{ open: boolean; onClose: () => void }`; render `null` when `!open`. Reuse verbatim from NotificationInbox: the Esc-close `useEffect` (keydown → `e.key === 'Escape'` → `onClose()`), the `panelRef` + `handleBackdropClick` (close when click target is outside the panel), and the backdrop tokens `position:'fixed', inset:0, zIndex:50, background:'rgba(9,9,14,0.6)'`. Panel deltas vs NotificationInbox (per 09-PATTERNS exact values): `left: 0` (was `right: 0`), `width: 'min(300px, 85vw)'` (was 380), `borderRight: '3px solid #E8F542'` with `borderLeft/borderTop/borderBottom: 'none'` (was `borderRight:'none'`), `boxShadow: '4px 0 0 0 #E8F542'` (was `'-4px 0 0 0 #E8F542'`), `background:'#09090E'`, `display:'flex', flexDirection:'column', padding:'20px'`. Auth-aware destinations (D-06) via `usePrivy()` reading `authenticated, ready, logout`: render Feed (`/`) and Leaderboard (`/leaderboard`) ALWAYS; render Profile (`/profile/{address}`) and New Call (`/new`) ONLY when `authenticated && ready` (Pitfall 5 — never flash authenticated links to logged-out users); render `Sign in` when `!authenticated` and `Sign out` (calls `logout()`) when `authenticated`. Copy text per UI-SPEC Copywriting Contract: `Feed` · `Leaderboard` · `Profile` · `New Call` · `Sign in` / `Sign out`. Each destination is a `<Link onClick={onClose}>` (close on link-tap) or a button for Sign out, styled neobrutalist (monospace, uppercase, accent border / hard offset shadow per project hex tokens) and ≥44px tall (D-03). For the authenticated Profile href, use the connected wallet address from the same Privy/wagmi source GlobalNav/NotificationBell already use (read how NotificationBell resolves the address; do not invent a new fetch). Do NOT move the NotificationBell into the drawer (D-06 — bell stays in the top bar). Keep ALL responsive logic in `apps/web` (this file) — do not import `useIsMobile` here; visibility is controlled by the parent (GlobalNav) so the drawer is pure.
  </action>
  <verify>
    <automated>cd apps/web && pnpm exec tsc --noEmit -p tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/app/components/MobileDrawer.tsx` exists, `'use client'`, exports `MobileDrawer`.
    - Source imports `usePrivy` from `@privy-io/react-auth` and reads `authenticated, ready, logout`.
    - Panel is left-anchored (`left: 0`, `boxShadow: '4px 0 0 0 #E8F542'`); backdrop `rgba(9,9,14,0.6)` `zIndex:50` (grep both).
    - Profile + New Call links are inside an `authenticated && ready` conditional (Pitfall 5); `Sign out` calls `logout()`.
    - Each link/button carries a `minHeight`/height ≥44 and an `onClose` on tap.
    - tsc passes; web build still compiles.
  </acceptance_criteria>
  <done>Left-anchored drawer compiles, mirrors NotificationInbox, gates Profile/New Call/Sign-out on auth, closes on Esc/backdrop/link-tap.</done>
</task>

<task type="auto">
  <name>Task 2: GlobalNav.tsx — hamburger trigger (mobile only), bell stays pinned</name>
  <read_first>
    - apps/web/app/components/GlobalNav.tsx (self — 56 lines: wordmark left + NotificationBell right at height 52; this is the exact mount point)
    - apps/web/app/hooks/useIsMobile.ts (the hook from 09-01 — consume it here)
    - apps/web/app/components/MobileDrawer.tsx (the component from Task 1 — render it here)
    - .planning/phases/09-mobile-responsive-on-7-critical-pages/09-UI-SPEC.md (Mobile Navigation Contract — hamburger trigger row + accent reservation list)
  </read_first>
  <action>
    Modify `apps/web/app/components/GlobalNav.tsx`. Import `useIsMobile` from `../hooks/useIsMobile` and `MobileDrawer` from `./MobileDrawer`. Add `const isMobile = useIsMobile();` and `const [drawerOpen, setDrawerOpen] = useState(false);`. Render a hamburger `<button aria-label="Open menu" onClick={() => setDrawerOpen(true)}>` to the LEFT of the wordmark, ONLY when `isMobile` (glyph `☰` or inline 3-line SVG; color accent `#E8F542`; `minWidth:44, minHeight:44`; transparent background, no border or a thin accent border consistent with the register). Render `<MobileDrawer open={drawerOpen} onClose={() => setDrawerOpen(false)} />` (the drawer self-gates on `open`, so it is safe to always mount). Keep `NotificationBell` exactly where it is (pinned right). Desktop layout MUST be visually unchanged — the hamburger only appears at `isMobile`. Do not alter the existing `height: 52` bar or the wordmark. The hamburger is the only accent-colored addition (per UI-SPEC accent reservation list).
  </action>
  <verify>
    <automated>cd apps/web && pnpm build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `GlobalNav.tsx` imports `useIsMobile` and `MobileDrawer` (grep both).
    - Hamburger `<button aria-label="Open menu"` is gated on `isMobile` and is ≥44×44.
    - `<MobileDrawer` is rendered with `open`/`onClose` wired to local `useState`.
    - `NotificationBell` is still rendered in the bar (grep `NotificationBell` present, NOT removed).
    - `cd apps/web && pnpm build` exits 0.
    - `playwright test tests/responsive.spec.ts -g "touch target"` shows no regression from the new hamburger (it is ≥44px).
  </acceptance_criteria>
  <done>GlobalNav shows a ≥44px hamburger at mobile that opens the drawer; bell stays pinned; desktop unchanged; web build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| logged-out viewer → drawer destination list | The drawer must NOT surface authenticated-only destinations (New Call, Profile) or Sign-out as actionable to an unauthenticated viewer. This is the one access-control-adjacent surface in Phase 9 (the security_threat_model brief item (a)). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Elevation of privilege / Information disclosure | MobileDrawer destination gating | mitigate | Profile + New Call + Sign-out rendered ONLY inside `authenticated && ready` (Pitfall 5). This is a UX gate, NOT the security boundary — server-side route auth + the existing CR-01 forgeable-action fix (Phase 8) remain the real boundary and are UNCHANGED. The drawer adds no new privileged action: `logout()` is the existing Privy session teardown. |
| T-09-02-02 | Spoofing | Privy auth state | accept | Drawer reads existing `usePrivy()` state; introduces no new auth path, token, or proof. No new trust established. |
| T-09-02-SC | Tampering | npm/pnpm installs | mitigate | No new packages installed (Privy, React already present). Supply-chain gate N/A. |
</threat_model>

<verification>
- Logged-out: drawer shows only Feed, Leaderboard, Sign in (no Profile/New Call/Sign out) — verified by source-conditional + (operator) manual check.
- `cd apps/web && pnpm build` exits 0.
- Full apps/web Playwright suite shows no regression in `touch target` from the new control.
</verification>

<success_criteria>
- Hamburger appears only at mobile, ≥44×44, opens a left drawer; bell stays pinned; desktop unchanged.
- Drawer destinations are auth-aware (D-06); closes on Esc/backdrop/link-tap.
- No new runtime dependency.
</success_criteria>

<output>
Create `.planning/phases/09-mobile-responsive-on-7-critical-pages/09-02-SUMMARY.md` when done.
</output>
