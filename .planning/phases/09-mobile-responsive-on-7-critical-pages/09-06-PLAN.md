---
phase: 09-mobile-responsive-on-7-critical-pages
plan: 06
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - apps/web/app/signin/page.tsx
  - apps/web/app/onboarding/layout.tsx
  - apps/web/app/onboarding/handle/page.tsx
  - apps/web/app/onboarding/socials/page.tsx
  - apps/web/app/onboarding/follow-graph/page.tsx
  - apps/web/app/onboarding/fund/page.tsx
  - apps/web/app/onboarding/tagline/page.tsx
autonomous: true
requirements: [UI-48, UI-49]
must_haves:
  truths:
    - "At 375px the Sign-in centered column clamps to calc(100vw - 32px) so the 400px column (live value, NOT the unbuilt 480px UI-24 frame) does not overflow by ~25px (UI-48, RESEARCH divergence #3)"
    - "The 3 sign-in CTAs are full-width and >=44px tall at mobile (UI-49)"
    - "At 375px the Onboarding layout frame (maxWidth:480px) clamps to width:100% + padding:0 16px (UI-48)"
    - "All 5 onboarding subroutes (handle, socials, follow-graph, fund, tagline) render single-column with inputs + advance buttons >=44px tall and the tagline commitment line wraps cleanly (UI-48/UI-49)"
  artifacts:
    - path: "apps/web/app/signin/page.tsx"
      provides: "375px-responsive Sign-in: 400px column clamped to calc(100vw - 32px), CTAs >=44px (D-03)"
      contains: "useIsMobile"
    - path: "apps/web/app/onboarding/layout.tsx"
      provides: "375px-responsive onboarding frame: 480px clamp + 16px gutter"
      contains: "useIsMobile"
  key_links:
    - from: "apps/web/app/signin/page.tsx"
      to: "apps/web/app/hooks/useIsMobile.ts"
      via: "useIsMobile() clamps the 400px centered column at mobile"
      pattern: "useIsMobile"
    - from: "apps/web/app/onboarding/layout.tsx"
      to: "apps/web/app/hooks/useIsMobile.ts"
      via: "useIsMobile() clamps the 480px frame at mobile; subroutes inherit"
      pattern: "useIsMobile"
---

<objective>
The Sign-in + Onboarding cluster (UI-48/UI-49) — the lightest critical pages. Sign-in is already a centered column; the only mobile issue is that its `maxWidth:400px` overflows a 375px viewport by ~25px (RESEARCH divergence #3 — the 480px UI-24 corner-bracket frame is unbuilt, so plan against the live 400px). Onboarding is a 480px frame + 5 column-form subroutes that mostly already work — the job is to clamp the frame and verify each subroute's inputs/buttons hit ≥44px.

These two surfaces close the share-loop entry: a new visitor who taps "Sign in" on a mobile receipt lands on a usable sign-in, then a usable onboarding flow.

Purpose: UI-48/UI-49 for Sign-in + the multi-screen Onboarding flow.
Output: `apps/web/app/signin/page.tsx` + `apps/web/app/onboarding/layout.tsx` + 5 subroute pages.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-mobile-responsive-on-7-critical-pages/09-CONTEXT.md
@.planning/phases/09-mobile-responsive-on-7-critical-pages/09-RESEARCH.md
@.planning/phases/09-mobile-responsive-on-7-critical-pages/09-UI-SPEC.md
@.planning/phases/09-mobile-responsive-on-7-critical-pages/09-PATTERNS.md
@apps/web/app/hooks/useIsMobile.ts
</context>

<artifacts_this_phase_produces>
- `apps/web/app/signin/page.tsx` — `isMobile`-conditional clamp of the 400px column to `calc(100vw - 32px)`; CTAs ≥44px.
- `apps/web/app/onboarding/layout.tsx` — 480px frame clamped to `width:100%` + `padding:0 16px` at mobile.
- The 5 onboarding subroute pages (`handle`, `socials`, `follow-graph`, `fund`, `tagline`) — inputs + advance buttons ≥44px, tagline line wraps.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Sign-in — clamp 400px column to calc(100vw - 32px), CTAs >=44px</name>
  <read_first>
    - apps/web/app/signin/page.tsx (centered column maxWidth:'400px' line 168, width:'100%' line 167; the 3 sign-in CTAs already width:'100%')
    - apps/web/app/signin/SignInButtons.tsx (the 3 CTA buttons — confirm height; ensure ≥44px at mobile)
    - apps/web/app/hooks/useIsMobile.ts
    - .planning/phases/09-mobile-responsive-on-7-critical-pages/09-UI-SPEC.md (§"6. Sign-in")
    - .planning/phases/09-mobile-responsive-on-7-critical-pages/09-RESEARCH.md (Divergence #3 — live column is 400px not 480px; UI-24 corner-bracket frame is unbuilt, do not assume it)
    - apps/web/tests/signin.spec.ts (regression guard — must still pass)
  </read_first>
  <action>
    Modify `apps/web/app/signin/page.tsx`: add `const isMobile = useIsMobile();`. The centered column at line 168 (`maxWidth:'400px'`) overflows 375px by ~25px → at mobile set `maxWidth:'calc(100vw - 32px)'` (keep `width:'100%'` line 167). Do NOT add a 480px corner-bracket frame — UI-24 is unbuilt and out of scope (Divergence #3). Ensure the 3 sign-in CTAs (in `SignInButtons.tsx` if that is where they render, or inline) are ≥44px tall at mobile (D-03) — they are already `width:'100%'`. If the ≥44px change belongs in `SignInButtons.tsx`, edit it there (it is part of the sign-in surface and not touched by any other plan). No `display:grid`.
  </action>
  <verify>
    <automated>cd apps/web && pnpm build 2>&1 | tail -8 && pnpm exec playwright test tests/responsive.spec.ts -g "no horizontal scroll.*signin|touch target.*signin" && pnpm exec playwright test tests/signin.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `signin/page.tsx` imports + calls `useIsMobile`; the 400px column clamps to `calc(100vw - 32px)` at mobile (grep).
    - No 480px frame introduced.
    - The 3 CTAs are ≥44px tall at mobile.
    - `playwright test -g "no horizontal scroll"` + `-g "touch target"` pass for `/signin`; existing `signin.spec.ts` still passes.
    - `cd apps/web && pnpm build` exits 0.
  </acceptance_criteria>
  <done>Sign-in column fits 375px (no ~25px overflow), CTAs ≥44px, no horizontal scroll, signin.spec.ts green.</done>
</task>

<task type="auto">
  <name>Task 2: Onboarding layout frame clamp + 5 subroutes >=44px / tagline wrap</name>
  <read_first>
    - apps/web/app/onboarding/layout.tsx (inner frame maxWidth:'480px' line 110, width:'100%' line 109, padding:'1.5rem' line 114; outer padding '2rem 1rem' line 52)
    - apps/web/app/onboarding/handle/page.tsx (7 inline styles), socials/page.tsx (4), follow-graph/page.tsx (7), fund/page.tsx (23 — heaviest, audit hardest), tagline/page.tsx (5 — confirm commitment line wraps)
    - apps/web/app/hooks/useIsMobile.ts
    - .planning/phases/09-mobile-responsive-on-7-critical-pages/09-UI-SPEC.md (§"7. Onboarding")
    - .planning/phases/09-mobile-responsive-on-7-critical-pages/09-RESEARCH.md (Per-Page Inventory §7)
    - apps/web/tests/onboarding.spec.ts (regression guard)
  </read_first>
  <action>
    Modify `apps/web/app/onboarding/layout.tsx`: add `const isMobile = useIsMobile();`; the inner frame (`maxWidth:'480px'` line 110) → at mobile add `width:'100%'` (already line 109) + ensure side gutter resolves to 16px (`padding:'0 16px'` on the frame or keep the outer `1rem`/16px — confirm the 480px clamp + width:100% means at 375px the frame is constrained, no overflow). Then audit EACH of the 5 subroutes individually — they are already `flexDirection:'column'` forms with `width:'100%'` inputs; the work is verification + targeted fixes: confirm every input and advance button is ≥44px tall at mobile (D-03), and in `tagline/page.tsx` confirm the commitment line ("EVERY CALL IS PERMANENT...") wraps cleanly at 343px (no overflow). The `fund` subroute is the heaviest (23 inline styles) — grep it for any `width: '` / `minWidth:` > 343px and clamp. If a subroute needs the hook for a conditional, import `useIsMobile` per-subroute; if the layout-frame clamp alone suffices (forms are already full-width column), do NOT add the hook to a subroute that needs no conditional — keep edits minimal. No `display:grid`.
  </action>
  <verify>
    <automated>cd apps/web && pnpm build 2>&1 | tail -8 && pnpm exec playwright test tests/responsive.spec.ts -g "no horizontal scroll.*onboarding|touch target.*onboarding" && pnpm exec playwright test tests/onboarding.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `onboarding/layout.tsx` clamps the 480px frame so 375px has no overflow (frame `width:'100%'` + 16px gutter at mobile).
    - All 5 subroutes pass the `no horizontal scroll` + `touch target` tests at 375/390px (each subroute path is in 09-01's PAGES list).
    - `tagline` commitment line wraps (covered by the no-horizontal-scroll assertion on `/onboarding/tagline`).
    - Existing `onboarding.spec.ts` still passes (no desktop regression).
    - No `display:grid`; `cd apps/web && pnpm build` exits 0.
  </acceptance_criteria>
  <done>Onboarding frame + all 5 subroutes render single-column at 375px with ≥44px inputs/buttons and no horizontal scroll; onboarding.spec.ts green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated visitor → Sign-in / Onboarding | Public-by-default surfaces. A layout swap adds no new auth path — sign-in still goes through the existing Privy flow; onboarding still reads existing state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06-01 | Spoofing / EoP | Sign-in CTAs | accept | The 3 sign-in CTAs invoke the EXISTING Privy paths unchanged; this plan only resizes their containers. No new auth surface, token, or proof introduced. |
| T-09-06-02 | Information disclosure | onboarding mobile layout | accept | Layout-only swaps on existing forms; no new field, no new data read. |
| T-09-06-SC | Tampering | npm/pnpm installs | mitigate | No new packages. Supply-chain gate N/A. |
</threat_model>

<verification>
- `cd apps/web && pnpm build` exits 0.
- `pnpm exec playwright test tests/responsive.spec.ts -g "no horizontal scroll|touch target"` green for `/signin` + all 5 `/onboarding/*` paths.
- Existing `signin.spec.ts` + `onboarding.spec.ts` still pass.
- No `display:grid` added.
</verification>

<success_criteria>
- Sign-in column fits 375px; CTAs ≥44px; onboarding frame + 5 subroutes single-column, ≥44px inputs/buttons, no horizontal scroll.
- No 480px UI-24 frame assumed; no new dependency; no grid.
</success_criteria>

<output>
Create `.planning/phases/09-mobile-responsive-on-7-critical-pages/09-06-SUMMARY.md` when done.
</output>
