---
phase: 09-mobile-responsive-on-7-critical-pages
plan: 04
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - apps/web/app/components/DesktopOnlyBanner.tsx
  - apps/web/app/new/page.tsx
  - apps/web/app/duel/[challengeId]/page.tsx
autonomous: true
requirements: [UI-50]
must_haves:
  truths:
    - "A single shared <DesktopOnlyBanner> renders only when isMobile && !dismissed — absent at desktop and on the 7 critical pages (D-08/D-09, UI-50)"
    - "The banner mounts on exactly 2 files: /new (covers New Call AND the ?quote= quote composer — no standalone quote route) and /duel/[challengeId] (UI-50, RESEARCH divergence #5)"
    - "The banner pushes content DOWN (not an overlay) so the non-responsive page renders interactive below it — warn-but-allow (D-08)"
    - "The banner is dismissible for the session via an [x] button; the hamburger drawer (09-02) stays active on these pages so return navigation / sign-out is never blocked (D-07, SC2)"
    - "Banner copy is exactly 'Best viewed on desktop' + the locked subtext; dismiss control has aria-label='Dismiss' and a >=44px hit area (D-03/D-09)"
  artifacts:
    - path: "apps/web/app/components/DesktopOnlyBanner.tsx"
      provides: "Shared per-session-dismissible desktop-only banner, isMobile-gated (D-08/D-09, UI-50)"
      contains: "Best viewed on desktop"
    - path: "apps/web/app/new/page.tsx"
      provides: "DesktopOnlyBanner mounted at top of the returned tree (covers New Call + ?quote= composer)"
      contains: "DesktopOnlyBanner"
    - path: "apps/web/app/duel/[challengeId]/page.tsx"
      provides: "DesktopOnlyBanner mounted at top of the returned tree"
      contains: "DesktopOnlyBanner"
  key_links:
    - from: "apps/web/app/components/DesktopOnlyBanner.tsx"
      to: "apps/web/app/hooks/useIsMobile.ts"
      via: "useIsMobile() gates the banner render (absent at desktop)"
      pattern: "useIsMobile"
    - from: "apps/web/app/new/page.tsx"
      to: "apps/web/app/components/DesktopOnlyBanner.tsx"
      via: "mounted at top of returned JSX (pushes content down, not overlay)"
      pattern: "DesktopOnlyBanner"
---

<objective>
The desktop-only banner slice (UI-50). The 3 non-critical pages (Duel §15.5, Quote composer §15.10, New Call §15.2) were scope-cut from the responsive pass — they get a "Best viewed on desktop" banner instead. Critically, RESEARCH resolved that the Quote composer is NOT a standalone route: it is the `?quote=` mode of `app/new/page.tsx`. So the banner mounts on exactly **2 files**: `/new` (covers both New Call and the quote composer) and `/duel/[challengeId]`.

After this plan: a mobile user opening `/new`, `/new?quote=...`, or `/duel/[id]` sees an honest, dismissible banner at the top, with the (non-responsive) page fully interactive below it — and the hamburger drawer (from 09-02) still lets them navigate away or sign out (this is what satisfies SC2).

Purpose: UI-50 + SC2.
Output: `apps/web/app/components/DesktopOnlyBanner.tsx` (new), `apps/web/app/new/page.tsx` + `apps/web/app/duel/[challengeId]/page.tsx` (mount).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-mobile-responsive-on-7-critical-pages/09-CONTEXT.md
@.planning/phases/09-mobile-responsive-on-7-critical-pages/09-UI-SPEC.md
@.planning/phases/09-mobile-responsive-on-7-critical-pages/09-PATTERNS.md
@.planning/phases/09-mobile-responsive-on-7-critical-pages/09-RESEARCH.md
@apps/web/app/hooks/useIsMobile.ts
</context>

<artifacts_this_phase_produces>
- **`apps/web/app/components/DesktopOnlyBanner.tsx`** — exports `DesktopOnlyBanner` (no required props). Renders `null` unless `isMobile && !dismissed`. Copy: heading `Best viewed on desktop`, subtext, `[×]` dismiss (`aria-label="Dismiss"`, ≥44px hit area).
- **`apps/web/app/new/page.tsx`** (modified) — mounts `<DesktopOnlyBanner />` at the very top of the returned tree.
- **`apps/web/app/duel/[challengeId]/page.tsx`** (modified) — same.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: DesktopOnlyBanner.tsx — shared, isMobile-gated, per-session dismissible</name>
  <read_first>
    - .planning/phases/09-mobile-responsive-on-7-critical-pages/09-RESEARCH.md (Code Examples — Per-session dismissible banner, the verified skeleton lines 316-345)
    - .planning/phases/09-mobile-responsive-on-7-critical-pages/09-UI-SPEC.md (Desktop-Only Banner Contract table + Copywriting Contract — exact heading/subtext/aria-label and styling tokens)
    - .planning/phases/09-mobile-responsive-on-7-critical-pages/09-CONTEXT.md (D-08 warn-but-allow, D-09 dismissible-for-session)
    - apps/web/app/hooks/useIsMobile.ts
    - apps/web/app/components/NotificationInbox.tsx (neobrutalist card token reference: accent #E8F542, hard offset shadow)
  </read_first>
  <action>
    Create `apps/web/app/components/DesktopOnlyBanner.tsx` as a `'use client'` component per the verified skeleton in 09-RESEARCH.md Code Examples. `const isMobile = useIsMobile();` + `const [dismissed, setDismissed] = useState(false);`; `if (!isMobile || dismissed) return null;` (absent at desktop AND on the 7 critical pages because they never mount it; per-session dismiss — D-09). Styling tokens (UI-SPEC Desktop-Only Banner Contract): `border:'3px solid #E8F542'`, `boxShadow:'4px 4px 0 0 #E8F542'`, `background:'#13131D'`, `padding:'12px 14px'`, `margin:'12px 16px'`, `display:'flex', flexDirection:'row', justifyContent:'space-between', alignItems:'flex-start', gap:'12px'`. Copy EXACTLY (UI-SPEC Copywriting Contract): heading `Best viewed on desktop` (accent `#E8F542`, monospace, uppercase), subtext `This page isn't optimized for small screens yet. Use the menu to navigate away.` (muted `#94A3B8`, ~13px). Dismiss `<button onClick={() => setDismissed(true)} aria-label="Dismiss">×</button>` with `minWidth:44, minHeight:44` (≥44px hit area even though the glyph is small — D-03), transparent bg, accent color. The banner pushes content (it is normal flow, NOT `position:fixed`/overlay — D-08). Import `useIsMobile` from `../hooks/useIsMobile`.
  </action>
  <verify>
    <automated>cd apps/web && pnpm exec tsc --noEmit -p tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/app/components/DesktopOnlyBanner.tsx` exists, `'use client'`, exports `DesktopOnlyBanner`.
    - Source contains the literal heading `Best viewed on desktop` and the locked subtext.
    - `return null` guard is `!isMobile || dismissed`.
    - Dismiss button has `aria-label="Dismiss"` and `minWidth:44`/`minHeight:44`.
    - Banner is normal-flow (no `position: 'fixed'`/`absolute` overlay).
    - tsc passes.
  </acceptance_criteria>
  <done>Shared banner compiles, renders only at mobile until dismissed, carries the locked copy + neobrutalist styling, and pushes content (not overlay).</done>
</task>

<task type="auto">
  <name>Task 2: Mount DesktopOnlyBanner on /new (incl ?quote=) and /duel/[challengeId]</name>
  <read_first>
    - apps/web/app/new/page.tsx (find the top of the returned JSX tree — banner mounts here; this file covers BOTH New Call and the ?quote= quote composer per RESEARCH divergence #5)
    - apps/web/app/duel/[challengeId]/page.tsx (find the top of the returned JSX tree)
    - apps/web/app/components/DesktopOnlyBanner.tsx (Task 1)
    - .planning/phases/09-mobile-responsive-on-7-critical-pages/09-UI-SPEC.md (Desktop-Only Banner Contract — Mount sites + Placement rows: net 2 files, very top of tree)
    - apps/web/tests/quote-composer.spec.ts (regression guard — this file asserts no grid + quote-composer behavior; the banner mount must not break it)
  </read_first>
  <action>
    Mount `<DesktopOnlyBanner />` at the VERY TOP of the returned JSX tree in `apps/web/app/new/page.tsx` and `apps/web/app/duel/[challengeId]/page.tsx` (import from `../components/DesktopOnlyBanner` / `../../components/DesktopOnlyBanner` — confirm the relative depth per file location). Place it as the first child so the existing (non-responsive) page renders interactive BELOW it (D-08 — pushes content down, not an overlay). Do NOT make these pages responsive — they are intentionally scope-cut; the banner is the only change. Do NOT mount the banner on any of the 7 critical pages. The `/new` mount covers the `?quote=` quote-composer mode automatically (same file). Confirm `quote-composer.spec.ts` and `new-call-publish.spec.ts` still pass after the mount (the banner is `null` at desktop, so desktop-rendered specs are unaffected).
  </action>
  <verify>
    <automated>cd apps/web && pnpm build 2>&1 | tail -10 && pnpm exec playwright test tests/responsive.spec.ts -g "desktop-only banner" tests/quote-composer.spec.ts tests/new-call-publish.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `DesktopOnlyBanner` is imported and mounted as the first child of the returned tree in BOTH `apps/web/app/new/page.tsx` and `apps/web/app/duel/[challengeId]/page.tsx` (grep both).
    - The banner is NOT mounted in any of: `app/page.tsx`, `app/call/[id]/page.tsx`, `app/profile/[address]/ProfileClient.tsx`, `app/leaderboard/LeaderboardClient.tsx`, `app/signin/page.tsx`, `app/onboarding/**` (the `desktop-only banner` Tier-1 source test from 09-01 asserts this absence).
    - `playwright test -g "desktop-only banner"` passes (banner present on the 2 banner files, absent on the 7 critical files).
    - `quote-composer.spec.ts` + `new-call-publish.spec.ts` still pass (no desktop regression).
    - `cd apps/web && pnpm build` exits 0.
  </acceptance_criteria>
  <done>The banner renders at the top of /new (+ ?quote=) and /duel/[id] at mobile only, absent on all 7 critical pages and at desktop; regression specs green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mobile user → banner-page exit path | The banner must NOT block return navigation or sign-out (SC2). |
| client → server | Unchanged — banner is a static client render, no fetch, no input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-01 | Denial (UX) | banner blocking sign-out / navigation | mitigate | D-08 banner is normal-flow (pushes content), NOT a fixed overlay → never covers page controls. D-07 keeps the hamburger drawer (09-02) active on these pages → exit/sign-out always reachable. Banner is dismissible (D-09). |
| T-09-04-02 | Information disclosure | banner copy | accept | Static copy, no data, no PII. |
| T-09-04-SC | Tampering | npm/pnpm installs | mitigate | No new packages. Supply-chain gate N/A. |
</threat_model>

<verification>
- `pnpm exec playwright test tests/responsive.spec.ts -g "desktop-only banner"` green (present on 2 banner files, absent on 7 critical files).
- `quote-composer.spec.ts` + `new-call-publish.spec.ts` green (no desktop regression).
- `cd apps/web && pnpm build` exits 0.
</verification>

<success_criteria>
- Shared `<DesktopOnlyBanner>` mounts on exactly `/new` + `/duel/[challengeId]`, renders only at mobile until dismissed, absent on the 7 critical pages.
- Banner pushes content (not overlay); hamburger exit stays available (SC2).
- No new dependency.
</success_criteria>

<output>
Create `.planning/phases/09-mobile-responsive-on-7-critical-pages/09-04-SUMMARY.md` when done.
</output>
