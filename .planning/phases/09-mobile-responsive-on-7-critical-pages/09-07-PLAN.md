---
phase: 09-mobile-responsive-on-7-critical-pages
plan: 07
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - apps/web/app/components/ChallengeFormModal.tsx
  - packages/ui/src/compound/CallerExitModal.tsx
  - packages/ui/src/compound/PositionExitModal.tsx
  - packages/ui/src/compound/FollowFadeModal.tsx
autonomous: true
requirements: [UI-48, UI-49]
must_haves:
  truths:
    - "FollowFadeModal / CallerExitModal / PositionExitModal already carry the calc(100vw - 32px) clamp — these are VERIFY-ONLY (RESEARCH divergence #2); their internal flexDirection:'row' button rows stack to full-width column and buttons are >=44px at 375px"
    - "ChallengeFormModal (480px panel) gets overlay horizontal padding so the panel never touches the screen edge and its internal row button rows stack at mobile (D-04)"
    - "No responsive logic (no useIsMobile / no matchMedia) is added INSIDE packages/ui components — they feed the Satori/OG build (Pitfall 2); mobile styling is intrinsic (clamp values) or passed via props/page wrapper"
    - "Each in-scope modal at 375px has no horizontal overflow and >=44px touch targets (D-03/D-04)"
  artifacts:
    - path: "apps/web/app/components/ChallengeFormModal.tsx"
      provides: "375px-safe ChallengeFormModal: overlay h-padding + stacked internal button rows (D-04)"
      contains: "calc(100vw"
    - path: "packages/ui/src/compound/CallerExitModal.tsx"
      provides: "Verified 375px-safe (clamp already present); internal button rows stack"
      contains: "calc(100vw"
  key_links:
    - from: "apps/web/app/components/ChallengeFormModal.tsx"
      to: "375px viewport"
      via: "panel maxWidth clamp + overlay h-padding so the 480px panel never overflows"
      pattern: "calc\\(100vw"
---

<objective>
The in-scope modals slice (D-04). A 480–560px modal overflows a 375px phone, and the Follow/Fade flow on a shared receipt is core to the mobile experience — so the modals the critical pages render are in scope.

RESEARCH reconciled two things the UI-SPEC got wrong: (1) `FollowFadeModal`, `CallerExitModal`, and `PositionExitModal` ALREADY carry the `calc(100vw - 32px)` clamp (the UI-SPEC's "add the clamp" instruction is moot) — so these are VERIFY-ONLY for the clamp, plus a real check that their internal button rows stack and hit ≥44px. (2) The inline `DisputeModal`/`ProvenanceModal` live in `call/[id]/page.tsx` and are handled in plan 09-03 (not here, to keep file ownership clean). The genuinely-needs-work modal here is `ChallengeFormModal` (480px panel).

After this plan: every modal a critical page can open is usable at 375px — no horizontal overflow, stacked full-width buttons, ≥44px targets.

Purpose: D-04 modal reach for UI-48/UI-49.
Output: `apps/web/app/components/ChallengeFormModal.tsx` (real change) + verify-pass + button-stack on the 3 `packages/ui` modals.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-mobile-responsive-on-7-critical-pages/09-CONTEXT.md
@.planning/phases/09-mobile-responsive-on-7-critical-pages/09-RESEARCH.md
@.planning/phases/09-mobile-responsive-on-7-critical-pages/09-UI-SPEC.md
@.planning/phases/09-mobile-responsive-on-7-critical-pages/09-PATTERNS.md
</context>

<artifacts_this_phase_produces>
- `apps/web/app/components/ChallengeFormModal.tsx` — overlay horizontal padding + panel clamp so the 480px panel never touches the screen edge; internal `flexDirection:'row'` button rows stack full-width at mobile.
- `packages/ui/src/compound/{FollowFade,CallerExit,PositionExit}Modal.tsx` — verified clamp present; internal button rows stack full-width; ≥44px buttons. (Intrinsic mobile styling only — NO `useIsMobile`/`matchMedia` added inside packages/ui.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: ChallengeFormModal — overlay h-padding + panel clamp + stacked button rows</name>
  <read_first>
    - apps/web/app/components/ChallengeFormModal.tsx (panel maxWidth:'480px' ~line 337; internal flexDirection:'row' button/action rows)
    - packages/ui/src/compound/FollowFadeModal.tsx (the clamp pattern to mirror — line ~185 maxWidth:'calc(100vw - 32px)'; this is the in-repo analog per 09-PATTERNS)
    - .planning/phases/09-mobile-responsive-on-7-critical-pages/09-UI-SPEC.md (In-scope modals table — ChallengeFormModal row; Universal modal rule)
    - .planning/phases/09-mobile-responsive-on-7-critical-pages/09-RESEARCH.md (In-scope modals table; Anti-pattern: do not put responsive logic inside packages/ui)
  </read_first>
  <action>
    Modify `apps/web/app/components/ChallengeFormModal.tsx`. The panel (`maxWidth:'480px'` ~line 337) overflows when the viewport is < 480px → change to `maxWidth: 'min(480px, calc(100vw - 32px))'` (or equivalent so it never exceeds the viewport minus a 16px gutter each side) — this is intrinsic (no hook needed; the clamp is viewport-relative CSS). Ensure the overlay/backdrop has horizontal padding (e.g. `padding: '0 16px'` on the overlay flex container) so the panel never touches the screen edge. Stack any internal `flexDirection:'row'` button/action rows to `flexDirection:'column'` with each button `width:'100%'` at narrow widths — since this file is in `apps/web`, you MAY use `useIsMobile` here if a JS conditional is cleaner than a CSS clamp; prefer the intrinsic `calc()`/`min()` clamp for the panel width and reserve `useIsMobile` only for the button-row stack if needed. Buttons ≥44px tall. No `display:grid`.
  </action>
  <verify>
    <automated>cd apps/web && pnpm build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `ChallengeFormModal.tsx` panel `maxWidth` is viewport-clamped (`calc(100vw` or `min(480px, calc(100vw - 32px))`) — grep `calc(100vw`.
    - Overlay has horizontal padding so the panel never touches the edge.
    - Internal button rows stack full-width with ≥44px buttons at mobile.
    - `cd apps/web && pnpm build` exits 0 (the web build typechecks the modal + its packages/ui imports).
  </acceptance_criteria>
  <done>ChallengeFormModal panel fits within 375px with edge padding, buttons stack full-width ≥44px.</done>
</task>

<task type="auto">
  <name>Task 2: Verify 3 packages/ui modal clamps + stack their internal button rows (no hook inside packages/ui)</name>
  <read_first>
    - packages/ui/src/compound/FollowFadeModal.tsx (line ~185 — confirm maxWidth:'calc(100vw - 32px)' present; internal action/button row)
    - packages/ui/src/compound/CallerExitModal.tsx (line ~110 — confirm clamp present)
    - packages/ui/src/compound/PositionExitModal.tsx (line ~103 — confirm clamp present)
    - .planning/phases/09-mobile-responsive-on-7-critical-pages/09-RESEARCH.md (Divergence #2 — all three ALREADY clamped; UI-SPEC "add clamp" is moot; verify-only for the clamp + stack the internal rows)
    - .planning/phases/09-mobile-responsive-on-7-critical-pages/09-UI-SPEC.md (Universal modal rule; Pitfall 2 — keep responsive logic OUT of shared internals)
  </read_first>
  <action>
    For `FollowFadeModal.tsx` (line ~185), `CallerExitModal.tsx` (line ~110), `PositionExitModal.tsx` (line ~103): (1) VERIFY the `maxWidth: 'calc(100vw - 32px)'` clamp is present (Divergence #2 says it is — if any is missing, add it intrinsically). (2) For each modal's internal `flexDirection:'row'` button/action row, make the buttons stack full-width and ≥44px tall at narrow widths. CRITICAL (Pitfall 2): do NOT import `useIsMobile` or call `matchMedia` inside these `packages/ui` files — they feed the `@vercel/og`/Satori Node build, which has no `window`. Achieve the stack intrinsically: use `flexWrap:'wrap'` with each button `flex:'1 1 100%'` (so they stack when the panel is narrow) OR `minWidth` on buttons that forces wrap, OR accept a mobile-styling prop the calling page passes (the receipt page already knows `isMobile`). Pick the option that stacks at the clamped panel width without a JS viewport read. Keep the existing desktop appearance unchanged at wide widths. No `display:grid`.
  </action>
  <verify>
    <automated>cd "C:/Users/woshv/Desktop/Call it" && pnpm --filter @call-it/ui build 2>&1 | tail -10; cd apps/web && pnpm build 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - All three modals contain `maxWidth: 'calc(100vw - 32px)'` (grep each — present after verify/fix).
    - NO `useIsMobile` import and NO `matchMedia` call exists in any `packages/ui/src` file (`grep -rn "useIsMobile\|matchMedia" packages/ui/src` returns 0).
    - Each modal's internal button row stacks full-width / ≥44px at the clamped panel width (intrinsic flexWrap or prop, not a JS viewport read).
    - `pnpm --filter @call-it/ui build` and `cd apps/web && pnpm build` both exit 0.
  </acceptance_criteria>
  <done>The 3 packages/ui modals are confirmed clamped, their button rows stack full-width ≥44px intrinsically, and no browser-only API leaked into packages/ui.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| packages/ui shared components → Satori/OG Node build | The OG card render path imports packages/ui primitives and runs in a Node/serverless context with no `window`. A browser-only API (`matchMedia`) leaking in would break OG rendering. |
| client → relayer/contracts | Unchanged — modals keep their existing Follow/Fade/Challenge/Exit transaction paths; this plan only resizes/stacks their UI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07-01 | Denial of service (build break) | packages/ui OG/Satori path | mitigate | NO `useIsMobile`/`matchMedia` inside packages/ui (Pitfall 2). Mobile stacking is intrinsic CSS (`flexWrap`/`min()`/`calc()`) or a prop passed from the already-`isMobile`-aware page. Verified by `grep -rn matchMedia packages/ui/src` == 0. |
| T-09-07-02 | Tampering | modal transaction calldata | accept | UNCHANGED — the Follow/Fade/Challenge/Exit calldata + amount logic (incl. the Phase-8 CR-01 origin-locked / pinned-amount fixes) is untouched; this plan only restyles containers + button rows. |
| T-09-07-SC | Tampering | npm/pnpm installs | mitigate | No new packages. Supply-chain gate N/A. |
</threat_model>

<verification>
- `pnpm --filter @call-it/ui build` exits 0 (OG/Satori path intact).
- `cd apps/web && pnpm build` exits 0.
- `grep -rn "useIsMobile\|matchMedia" packages/ui/src` returns 0 (Pitfall 2).
- All three packages/ui modals + ChallengeFormModal contain a viewport clamp.
- Existing OG snapshot specs (`og-thumbnail-200px.spec.ts`, `og-fallback*.spec.ts`) still pass (no shared-component regression).
</verification>

<success_criteria>
- Every in-scope modal (FollowFade/CallerExit/PositionExit/ChallengeForm) fits within 375px with stacked full-width ≥44px buttons.
- No browser-only API leaked into packages/ui; OG build intact.
- No new dependency; no grid.
</success_criteria>

<output>
Create `.planning/phases/09-mobile-responsive-on-7-critical-pages/09-07-SUMMARY.md` when done.
</output>
