---
phase: 09-mobile-responsive-on-7-critical-pages
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - apps/web/app/call/[id]/page.tsx
autonomous: true
requirements: [UI-48, UI-49]
must_haves:
  truths:
    - "At 375px the Settled Receipt (#1 share-landing) renders single-column with no horizontal scroll (SC1/SC3, UI-48)"
    - "The outcome-hero word is legible at 375px — 52px target, >=36px hard floor, fits the viewport with no clip/wrap (SC3, D-03 spirit)"
    - "The settled 4-stat row stacks 2x2 via flexWrap (NOT display:grid), preserving the inter-cell dividers + outer border"
    - "The settled action row (Share / Share as Frame / View All) stacks full-width, each button >=44px tall (SC3, UI-49)"
    - "The Live Receipt activity-feed + quote-calls two-column block stacks to single column at mobile (UI-48); the quote-calls column is read-only display and gets NO desktop-only banner"
    - "The inline ProvenanceModal (560px) and DisputeModal (480px) clamp to calc(100vw - 32px) so they do not overflow 375px (D-04)"
  artifacts:
    - path: "apps/web/app/call/[id]/page.tsx"
      provides: "375px-responsive Settled + Live Receipt with data-outcome-word hook, 2x2 stat stack, full-width action stack, clamped inline modals"
      contains: "data-outcome-word"
  key_links:
    - from: "apps/web/app/call/[id]/page.tsx"
      to: "apps/web/app/hooks/useIsMobile.ts"
      via: "useIsMobile() drives every isMobile ? mobile : desktop layout swap on this page"
      pattern: "useIsMobile"
    - from: "apps/web/tests/responsive.spec.ts"
      to: "apps/web/app/call/[id]/page.tsx"
      via: "[data-outcome-word] legibility assertion targets the settled outcome hero"
      pattern: "data-outcome-word"
---

<objective>
The #1-priority vertical slice: take the Settled Receipt (and the Live Receipt branch it shares a file with) from desktop-only to 375px-verified-green. This is the page a Twitter/Farcaster share-visitor lands on, which is the entire reason D-02 (mobile-first first paint) exists.

After this plan: a share-visitor on a phone sees the outcome word legibly, the FINAL VALUE/TARGET/CONVICTION/P&L stats in a readable 2×2, and the Share / Share-as-Frame / View-All buttons stacked full-width and tappable — with no horizontal scroll. The Live branch (activity feed + quote-calls) stacks single-column. The inline ProvenanceModal and DisputeModal stop overflowing the screen.

`call/[id]/page.tsx` is 2675 lines, so this is a focused 2-task plan: Task 1 owns the Settled branch (the priority) + the `data-outcome-word` hook; Task 2 owns the Live branch + the two inline modals.

Purpose: SC1 + SC3 (the stated phase priority) + UI-48/UI-49 for the receipt surface.
Output: `apps/web/app/call/[id]/page.tsx` (modified — both branches + 2 inline modals + the test hook).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-mobile-responsive-on-7-critical-pages/09-CONTEXT.md
@.planning/phases/09-mobile-responsive-on-7-critical-pages/09-RESEARCH.md
@.planning/phases/09-mobile-responsive-on-7-critical-pages/09-UI-SPEC.md
@.planning/phases/09-mobile-responsive-on-7-critical-pages/09-PATTERNS.md
@apps/web/app/hooks/useIsMobile.ts
</context>

<artifacts_this_phase_produces>
- **`data-outcome-word`** attribute — added to the outcome-hero `<p>` in `call/[id]/page.tsx` (~line 1509-1518). This is the test hook that 09-01's `outcome word legible` Playwright assertion depends on.
- The settled + live branches of `call/[id]/page.tsx` gain `isMobile`-conditional inline-style swaps (container clamp, 2×2 stat wrap, full-width action stack, two-column→column, modal clamps).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Settled Receipt branch — container clamp, 52px outcome hero + data-outcome-word, 2x2 stat stack, full-width action row</name>
  <read_first>
    - apps/web/app/call/[id]/page.tsx (the settled branch: container maxWidth:'1024px' at line 1475; outcome hero fontSize:'96px' at line 1511; 4-stat row at lines ~1566-1593; action row at line ~1596; FINAL POSITIONS / provenance / Caller-Exited / Disputed variant blocks — grep every flexDirection: 'row' in the settled half and audit each)
    - apps/web/app/hooks/useIsMobile.ts (consume useIsMobile)
    - .planning/phases/09-mobile-responsive-on-7-critical-pages/09-UI-SPEC.md (§"3. Settled Receipt" — exact mobile contract: 52px hero, 2x2 via flexWrap, full-width action stack, preserve dividers/borders/Stamp; Typography table — outcome-hero floor 36px + the 5 LOCKED outcome colors)
    - .planning/phases/09-mobile-responsive-on-7-critical-pages/09-RESEARCH.md (Per-Page Inventory §2&3; Pitfall 4 outcome clipping; Anti-Pattern "no grid — use flexWrap")
  </read_first>
  <action>
    Modify the SETTLED branch of `apps/web/app/call/[id]/page.tsx`. Add `const isMobile = useIsMobile();` at the top of the component. (1) Container: the `maxWidth:'1024px', margin:'0 auto', padding:'32px 24px'` at line 1475 → at mobile use `{ width:'100%', maxWidth:'100%', margin:'0 auto', padding:'0 16px' }` (Pattern 2). (2) Outcome hero (line 1511): `fontSize:'96px'` → `'52px'` when `isMobile` (UI-SPEC target; Playwright floor 36px). Keep `lineHeight:1.0`, `letterSpacing:'-0.02em'`, the 3px border, hard offset shadow, the `<Stamp>` animation, and the 5 LOCKED outcome colors unchanged. Add a `data-outcome-word` attribute to the outcome `<p>` (~line 1509-1518) so the legibility assertion is stable. (3) 4-stat row (~lines 1566-1593): each cell is `flex:1` inside a `flexDirection:'row'` with `2px solid #2E2E42` outer border + `1px` inter-cell `borderRight`. At mobile, wrap to 2×2: set the row `flexWrap:'wrap'` and each cell `flex:'1 1 45%'` (two per row) — PRESERVE the inter-cell dividers and the 2px outer border (neobrutalist). DO NOT use `display:grid` (Pitfall 3/15 — `quote-composer.spec.ts` asserts no grid; Satori coupling). (4) Action row (~line 1596): `flexDirection:'row', gap:'12px'` with 3 buttons each `flex:1` (Share filled w/ `boxShadow:'4px 4px 0 0 #09090E'`, Share-as-Frame, View All) → at mobile `flexDirection:'column'`, each button `width:'100%'`, `gap:'12px'`, ≥44px tall; preserve the fill/outline treatments + the primary's hard offset shadow. (5) Audit the FINAL POSITIONS block + provenance line + Caller-Exited and Disputed variant blocks: every `flexDirection:'row'` holding ≥2 sizable cells flips to `'column'` at mobile; every interactive element ≥44px. Grep the settled half for `width: '` and `minWidth:` to catch any inner fixed width > 343px (Assumption A3) and clamp it.
  </action>
  <verify>
    <automated>cd apps/web && pnpm build 2>&1 | tail -10 && pnpm exec playwright test tests/responsive.spec.ts -g "outcome word legible|no horizontal scroll.*call"</automated>
  </verify>
  <acceptance_criteria>
    - `data-outcome-word` attribute present on the outcome hero `<p>` (grep).
    - Outcome hero uses `'52px'` under `isMobile`; `playwright test -g "outcome word legible"` passes (fontSize ≥36, box fits viewport, no wrap).
    - Settled 4-stat row uses `flexWrap:'wrap'` + `flex:'1 1 45%'` at mobile; NO `display:grid` introduced (grep `display: 'grid'` in the settled half = 0).
    - Action row stacks `flexDirection:'column'` with each button `width:'100%'` at mobile.
    - `playwright test -g "no horizontal scroll"` passes for the settled-receipt path.
    - `cd apps/web && pnpm build` exits 0.
  </acceptance_criteria>
  <done>Settled Receipt is single-column at 375px with a legible 52px outcome hero, 2×2 flexWrap stats, full-width stacked action buttons, no horizontal scroll, no grid.</done>
</task>

<task type="auto">
  <name>Task 2: Live Receipt branch two-column stack + inline ProvenanceModal/DisputeModal clamp</name>
  <read_first>
    - apps/web/app/call/[id]/page.tsx (live branch container maxWidth:'1024px' at line 1936; live two-column activity-feed + quote-calls block ~line 1656; the Follow/Fade/Challenge action row; inline DisputeModal at line 600 maxWidth:'480px'; inline ProvenanceModal at line 818 maxWidth:'560px')
    - apps/web/app/hooks/useIsMobile.ts
    - .planning/phases/09-mobile-responsive-on-7-critical-pages/09-UI-SPEC.md (§"2. Live Receipt" + In-scope modals table — ProvenanceModal MUST clamp; DisputeModal add clamp; quote-calls column is read-only display, NO banner)
    - .planning/phases/09-mobile-responsive-on-7-critical-pages/09-RESEARCH.md (Per-Page Inventory §2&3 — the long list of flexDirection:'row' lines 614/715/833/907/1449/1667/1691/1757/1859/1989/2309/2397/2531 to audit)
  </read_first>
  <action>
    Modify the LIVE branch + the two inline modals in `apps/web/app/call/[id]/page.tsx` (reuse the `const isMobile = useIsMobile();` added in Task 1 — do not declare twice). (1) Live container: `maxWidth:'1024px', margin:'0 auto', padding:'24px'` at line 1936 → mobile `{ width:'100%', maxWidth:'100%', padding:'0 16px' }`. (2) Live two-column block (~line 1656): `flexDirection:'row', alignItems:'flex-start'` (activity feed + quote-calls) → `'column'` at mobile with the activity feed FIRST, quote-calls below. The quote-calls column is a READ-ONLY display surface — it stays in scope as part of this responsive page and gets NO desktop-only banner (it is NOT the composer; the composer is `/new?quote=` which gets the banner in 09-04). (3) Follow / Fade / Challenge action row: stack `flexDirection:'column'`, each button `width:'100%'`, `gap:'12px'`, ≥44px tall; preserve filled / outline / orange-outline treatments. (4) Inline DisputeModal (line 600, `maxWidth:'480px'`): add `maxWidth: isMobile ? 'calc(100vw - 32px)' : '480px'` (or unconditional `calc(100vw - 32px)` if it never exceeds 480 on desktop — confirm); ensure the overlay has horizontal padding so the panel never touches the screen edge; stack any internal `flexDirection:'row'` button rows to `column` full-width at mobile. (5) Inline ProvenanceModal (line 818, `maxWidth:'560px'` — OVERFLOWS 375px, this is the one inline modal that genuinely must change): add `maxWidth: isMobile ? 'calc(100vw - 32px)' : '560px'`, overlay h-padding, internal `row`→`column` stacks, buttons ≥44px. (6) Audit the remaining live-branch `flexDirection:'row'` blocks (lines 614/715/833/907/1449/1667/1691/1757/1859/1989/2309/2397/2531 per RESEARCH) — flip the ones holding ≥2 sizable cells to `'column'`; ≥44px on interactive elements. Do NOT introduce `display:grid` anywhere.
  </action>
  <verify>
    <automated>cd apps/web && pnpm build 2>&1 | tail -10 && pnpm exec playwright test tests/responsive.spec.ts -g "no horizontal scroll.*call|touch target.*call"</automated>
  </verify>
  <acceptance_criteria>
    - Live two-column block flips to `flexDirection:'column'` at mobile (activity feed first).
    - ProvenanceModal `maxWidth` clamps to `calc(100vw - 32px)` at mobile (grep — line ~818); DisputeModal clamped similarly (line ~600).
    - Follow/Fade/Challenge buttons stack full-width ≥44px at mobile.
    - No `display: grid` introduced on this page (grep across full file = 0).
    - `playwright test -g "no horizontal scroll"` and `-g "touch target"` pass for the live-receipt path.
    - `cd apps/web && pnpm build` exits 0; full file still has exactly one `const isMobile = useIsMobile();`.
  </acceptance_criteria>
  <done>Live Receipt stacks single-column at 375px, action buttons full-width, and the inline ProvenanceModal/DisputeModal clamp inside the viewport — no horizontal scroll, no grid.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public unauthenticated viewer → Settled Receipt | The settled receipt is a PUBLIC share-landing surface (no auth). A pure layout swap must not leak any owner-only control or PII into the mobile layout (security_threat_model brief item (b)). |
| client → relayer/subgraph | Unchanged — this plan moves no data, adds no fetch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Information disclosure | Settled Receipt mobile layout | mitigate | The mobile path only changes inline `style` objects (container width, flexDirection, fontSize, maxWidth) on the SAME JSX tree — no conditionally-rendered new content, no owner-only control surfaced. AUTH-44 (no wallet address on receipts) is unchanged: the layout swap touches no handle/address rendering logic. Caller-only "Exit your call" controls remain behind their existing condition gates (unchanged). |
| T-09-03-02 | Tampering | data-outcome-word attribute | accept | A static test-only attribute on the outcome `<p>`; carries no data, no behavior; readable in DOM but reveals only the already-public outcome word. |
| T-09-03-SC | Tampering | npm/pnpm installs | mitigate | No new packages. Supply-chain gate N/A. |
</threat_model>

<verification>
- `cd apps/web && pnpm build` exits 0.
- `pnpm exec playwright test tests/responsive.spec.ts -g "outcome word legible|no horizontal scroll|touch target"` green for the receipt paths.
- `grep -c "display: 'grid'" apps/web/app/call/[id]/page.tsx` returns 0 (filter header comments per grep hygiene if needed).
- `quote-composer.spec.ts` no-grid assertion still passes.
</verification>

<success_criteria>
- Settled + Live Receipt are single-column at 375px, no horizontal scroll, legible 52px outcome hero, full-width stacked action buttons.
- Inline ProvenanceModal + DisputeModal clamp inside the viewport.
- `data-outcome-word` hook present; no `display:grid`; no new dependency.
</success_criteria>

<output>
Create `.planning/phases/09-mobile-responsive-on-7-critical-pages/09-03-SUMMARY.md` when done.
</output>
