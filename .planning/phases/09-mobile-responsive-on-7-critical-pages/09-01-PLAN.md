---
phase: 09-mobile-responsive-on-7-critical-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/hooks/useIsMobile.ts
  - apps/web/tests/responsive.spec.ts
autonomous: true
requirements: [UI-48, UI-49, UI-50]
must_haves:
  truths:
    - "useIsMobile() returns true on the server snapshot so SSR/first paint is mobile-first (D-02)"
    - "useIsMobile() converges to the real matchMedia('(max-width: 767px)') value after hydration with no hydration warning (D-01)"
    - "responsive.spec.ts runs at 375px AND 390px viewports (not the vacuous desktop default) (D-10)"
    - "The spec asserts no horizontal scroll, >=44x44 touch targets, banner presence/absence, and outcome-word legibility for the 7 critical pages + 2 banner pages"
  artifacts:
    - path: "apps/web/app/hooks/useIsMobile.ts"
      provides: "SSR-safe useIsMobile() hook (useSyncExternalStore, getServerSnapshot=>true) — D-01/D-02"
      contains: "getServerSnapshot"
    - path: "apps/web/tests/responsive.spec.ts"
      provides: "Wave-0 Playwright mechanical gate for UI-48/UI-49/UI-50/SC3 + Tier-1 source assertions"
      contains: "viewport"
  key_links:
    - from: "apps/web/tests/responsive.spec.ts"
      to: "apps/web/app/hooks/useIsMobile.ts"
      via: "readFileSync Tier-1 source assertion (getServerSnapshot returns true)"
      pattern: "getServerSnapshot"
---

<objective>
Build the shared foundation slice every other Phase 9 plan depends on: the SSR-safe `useIsMobile()` hook (D-01/D-02) and the `responsive.spec.ts` Playwright mechanical gate (D-10) that encodes the UI-48/UI-49/UI-50/SC3 acceptance criteria.

This is the Wave-0 scaffold. No page is retrofit here — but after this plan, every later plan has (a) the hook to swap layouts against and (b) a runnable test harness that initially fails for un-retrofit pages and goes green as each page is fixed.

Purpose: The entire frontend is inline `style={{}}` with zero CSS media queries (verified). Responsiveness can only be delivered by `isMobile ? mobileStyle : desktopStyle` swaps, which requires the hook. The Nyquist gate requires the spec to exist before page tasks can verify.
Output: `apps/web/app/hooks/useIsMobile.ts`, `apps/web/tests/responsive.spec.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-mobile-responsive-on-7-critical-pages/09-CONTEXT.md
@.planning/phases/09-mobile-responsive-on-7-critical-pages/09-RESEARCH.md
@.planning/phases/09-mobile-responsive-on-7-critical-pages/09-UI-SPEC.md
@.planning/phases/09-mobile-responsive-on-7-critical-pages/09-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This plan creates the symbols the rest of Phase 9 consumes:
- **`apps/web/app/hooks/useIsMobile.ts`** — exports `useIsMobile(): boolean`. Internal: `QUERY = '(max-width: 767px)'`, `subscribe`, `getSnapshot`, `getServerSnapshot` (returns `true`).
- **`apps/web/tests/responsive.spec.ts`** — Playwright spec with per-viewport (`375`, `390`) describes; named tests matching `-g` filters: `no horizontal scroll`, `touch target`, `single column` / `full-width`, `desktop-only banner`, `outcome word legible`, `server snapshot`.
- **`data-outcome-word`** test hook — NOTE: the attribute is ADDED to the outcome hero `<p>` in plan 09-03 (the Receipt slice that owns `call/[id]/page.tsx`). This plan's outcome-word test targets `[data-outcome-word]` and will go green once 09-03 lands.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SSR-safe useIsMobile() hook</name>
  <read_first>
    - apps/web/app/hooks/ (confirm dir; create if absent — no hook exists in apps/web today per 09-PATTERNS "No Analog Found")
    - .planning/phases/09-mobile-responsive-on-7-critical-pages/09-RESEARCH.md (Pattern 1 — full verified implementation, lines 147-181)
    - .planning/phases/09-mobile-responsive-on-7-critical-pages/09-CONTEXT.md (D-01, D-02, and Claude's-discretion breakpoint note)
    - apps/web/app/components/NotificationInbox.tsx (confirm 'use client' + React import register used in apps/web)
  </read_first>
  <behavior>
    - getServerSnapshot() returns true (D-02 mobile-first first paint) — server HTML and first client render both mobile, no hydration mismatch.
    - getSnapshot() returns window.matchMedia('(max-width: 767px)').matches on the client; returns true when window/matchMedia is undefined (SSR/test-node guard).
    - subscribe(cb) registers matchMedia 'change' via addEventListener (NEVER the deprecated addListener); returns a cleanup that removeEventListener's; is a no-op returning ()=>{} when window/matchMedia is undefined.
    - useIsMobile() === useSyncExternalStore(subscribe, getSnapshot, getServerSnapshot).
  </behavior>
  <action>
    Create `apps/web/app/hooks/useIsMobile.ts` as a `'use client'` module exactly per 09-RESEARCH.md Pattern 1 (the implementation is verified and copy-ready there). Concrete locks: `QUERY = '(max-width: 767px)'` (single breakpoint, `< 768px ⇒ mobile`, per Claude's-discretion in D-01 — tablet falls into desktop). Import `useSyncExternalStore` from `'react'`. `getServerSnapshot` MUST return `true` (this is the D-02 lock the spec asserts). Use `addEventListener('change', callback)` in `subscribe`, never `addListener`. The hook MUST live in `apps/web/app/hooks/` ONLY — NEVER `packages/ui` (Pitfall 2: pulling `matchMedia` into the Satori/OG Node build path breaks `@vercel/og`). Export a named `useIsMobile` (not default). Do not read `window` during render to seed any `useState` (Pitfall 1 — hydration mismatch under `force-dynamic`).
  </action>
  <verify>
    <automated>cd apps/web && pnpm exec tsc --noEmit -p tsconfig.json 2>&1 | head -20; pnpm exec playwright test tests/responsive.spec.ts -g "server snapshot"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/app/hooks/useIsMobile.ts` exists, begins with `'use client'`, imports `useSyncExternalStore` from `react`, exports `useIsMobile`.
    - Source contains `getServerSnapshot` returning `true` and `addEventListener('change'` (grep: `getServerSnapshot` present; `addListener` absent).
    - `matchMedia` appears ONLY in `apps/web` — `grep -r "matchMedia" packages/ui/src` returns 0 matches.
    - `pnpm exec playwright test tests/responsive.spec.ts -g "server snapshot"` passes (Tier-1 source assertion from Task 2).
  </acceptance_criteria>
  <done>Hook compiles, lives in apps/web only, server snapshot defaults true, and the Tier-1 source assertion is green.</done>
</task>

<task type="auto">
  <name>Task 2: responsive.spec.ts Playwright mechanical gate (Wave-0 scaffold)</name>
  <read_first>
    - apps/web/tests/signin.spec.ts (Tier-1 readFileSync source-assertion pattern + the real-Privy-app-id gating block at ~lines 38-83/46-52 — copy the gating idiom)
    - apps/web/playwright.config.ts (single chromium / Desktop Chrome project; testMatch ['**/*.spec.ts']; webServer pnpm start after build — Pitfall 6: viewport MUST be set in-spec or tests pass vacuously at desktop width)
    - apps/web/tests/quote-composer.spec.ts (existing "no grid" assertion style + readFileSync register to mirror)
    - .planning/phases/09-mobile-responsive-on-7-critical-pages/09-RESEARCH.md (Code Examples — responsive spec skeleton, lines 347-384; Validation Architecture; Open Question 1 seeded-id)
    - .planning/phases/09-mobile-responsive-on-7-critical-pages/09-VALIDATION.md (Requirement → Test Map; the exact -g filter names each later task verifies against)
  </read_first>
  <action>
    Create `apps/web/tests/responsive.spec.ts` per the verified skeleton in 09-RESEARCH.md Code Examples. It MUST set viewport in-spec via `test.use({ viewport: { width, height: 812 } })` inside per-width describe blocks for BOTH `375` and `390` (Pitfall 6 — the lone chromium project is Desktop Chrome; a viewport-less spec passes vacuously). Cover these named tests so each later plan's `-g` filter resolves (names are the contract the VALIDATION map and later plans cite): (1) `no horizontal scroll — {path}` asserting `document.documentElement.scrollWidth <= clientWidth` for the 7 critical paths (`/`, `/leaderboard`, `/signin`, `/onboarding/handle`, `/onboarding/socials`, `/onboarding/follow-graph`, `/onboarding/fund`, `/onboarding/tagline`, the settled-receipt path, the live-receipt path, a profile path); (2) `touch target >=44px — {path}` filtering `button, a[href], [role="button"]` with `getBoundingClientRect()` width/height `< 44` while `width > 0`, expecting 0; (3) `outcome word legible — settled receipt` locating `[data-outcome-word]`, asserting computed `fontSize >= 36` AND bounding box `x >= 0` and `x + width <= width` (no clip/wrap); (4) `desktop-only banner` — Tier-1 source assertions (readFileSync) that `apps/web/app/new/page.tsx` and `apps/web/app/duel/[challengeId]/page.tsx` import/mount `DesktopOnlyBanner`, AND that NONE of the 7 critical page files mount it; (5) `single column` / `full-width` markers for the receipt action row; (6) `server snapshot` — Tier-1 readFileSync on `apps/web/app/hooks/useIsMobile.ts` asserting it contains `getServerSnapshot` and that the server snapshot returns `true` (D-02 lock). For browser-rendering tests that need a real `NEXT_PUBLIC_PRIVY_APP_ID` + `pnpm build`, copy the conditional skip/gating idiom from `signin.spec.ts` so the suite never hard-fails on a missing app id in CI; the Tier-1 source assertions (banner presence, server snapshot) run with NO Privy id. For the seeded settled-receipt id, use `/call/14` (Open Question 1 / MEMORY Phase-7 CallerLost seed) but read the id from an overridable const `SEEDED_SETTLED_CALL = process.env.RESPONSIVE_SETTLED_CALL_ID ?? '14'` so the operator can repoint it without editing assertions. NEVER assert `display:grid` presence — the house style is flex-only (Pitfall 3 — must not break `quote-composer.spec.ts`). Do NOT add a new Playwright project to `playwright.config.ts`.
  </action>
  <verify>
    <automated>cd apps/web && pnpm exec playwright test tests/responsive.spec.ts -g "server snapshot|desktop-only banner" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/tests/responsive.spec.ts` exists; contains `test.use({ viewport:` with both `375` and `390`.
    - The `server snapshot` and `desktop-only banner` Tier-1 source tests are present and PASS against the just-created hook (banner tests assert mount-site source, which is allowed to be RED-pending until 09-04 — gate on the server-snapshot test being green now; banner source tests may be written as `.fixme`/conditional until 09-04 lands, or assert the negative-on-critical-pages half which is already true).
    - Spec sets viewport in-spec (NOT relying on the Desktop Chrome default — Pitfall 6).
    - `SEEDED_SETTLED_CALL` is overridable via env.
    - No `display: grid` assertion added; `quote-composer.spec.ts` still passes (`pnpm exec playwright test tests/quote-composer.spec.ts`).
  </acceptance_criteria>
  <done>responsive.spec.ts exists, sets 375/390 viewports in-spec, encodes the UI-48/49/50/SC3 named tests, and its Tier-1 source assertions (server snapshot) pass; the page-render tests are RED-pending the later page plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → existing relayer/subgraph | Unchanged. This plan adds NO data fetch, no input, no auth surface — it is a hook + a test file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Tampering | npm/pnpm installs | accept | Phase 9 installs ZERO new packages (verified: React 19 + Next 16 + @playwright/test all present). No legitimacy gate applies; no `[ASSUMED]`/`[SUS]` packages. |
| T-09-01-02 | Information disclosure | useIsMobile hook leaking into OG/Satori build | mitigate | Hook lives in `apps/web/app/hooks/` ONLY; `grep -r matchMedia packages/ui/src` must be 0 (Pitfall 2). No data crosses any boundary — `matchMedia` is a read-only viewport query. |
| T-09-01-SC | Tampering | supply-chain (installs) | mitigate | No installs in this plan; `T-09-01-SC` is a no-op marker — slopcheck N/A because no package manager is invoked. |
</threat_model>

<verification>
- `cd apps/web && pnpm exec playwright test tests/responsive.spec.ts -g "server snapshot"` is green.
- `grep -rn "matchMedia" packages/ui/src` returns nothing (Pitfall 2).
- `cd apps/web && pnpm exec playwright test tests/quote-composer.spec.ts` still passes (no-grid regression guard intact).
</verification>

<success_criteria>
- `useIsMobile.ts` exists in `apps/web/app/hooks/`, is `'use client'`, server snapshot returns `true`, uses `useSyncExternalStore` + `addEventListener('change')`.
- `responsive.spec.ts` exists, sets 375/390 viewports in-spec, encodes the named UI-48/49/50/SC3 tests, and its Tier-1 source assertions pass.
- No new runtime dependency added.
</success_criteria>

<output>
Create `.planning/phases/09-mobile-responsive-on-7-critical-pages/09-01-SUMMARY.md` when done.
</output>
