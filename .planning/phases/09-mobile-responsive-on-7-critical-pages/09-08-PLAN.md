---
phase: 09-mobile-responsive-on-7-critical-pages
plan: 08
type: execute
wave: 3
depends_on: ["09-01", "09-02", "09-03", "09-04", "09-05", "09-06", "09-07"]
files_modified:
  - apps/web/tests/responsive.spec.ts
autonomous: false
requirements: [UI-48, UI-49, UI-50]
must_haves:
  truths:
    - "The full apps/web Playwright suite is green — responsive.spec.ts AND every pre-existing regression guard (quote-composer no-grid, OG snapshots, signin/leaderboard/profile specs) (D-10a)"
    - "The settled-receipt outcome-word test runs against a confirmed-stable seeded settled call id (Open Question 1 resolved) (SC3)"
    - "The whole D-10a mechanical gate passes at BOTH 375px and 390px for all 7 critical pages + the 2 banner pages"
    - "An operator has signed off the SC3 share->receipt landing flow on a REAL iPhone (Safari) AND a REAL Android (Chrome) — outcome word legible, action buttons stack + are tappable, Sign-in CTA visible for unauthenticated viewers (D-10b)"
    - "D-11 BOTH-gates rule: the phase is NOT complete until the Playwright suite AND the operator real-device sign-off both pass — there is NO fallback for the device gate"
  artifacts:
    - path: "apps/web/tests/responsive.spec.ts"
      provides: "Finalized mechanical gate with the confirmed seeded settled-call id; full-suite-green"
      contains: "RESPONSIVE_SETTLED_CALL_ID"
  key_links:
    - from: "operator real device (iPhone Safari + Android Chrome)"
      to: "deployed mobile Settled Receipt share-landing"
      via: "checkpoint:human-verify hard gate (D-11, no fallback)"
      pattern: "human-verify"
---

<objective>
The Phase 9 closure + dual-gate slice (D-10/D-11). Phase 9 completion is intentionally coupled to BOTH a green automated suite AND a real-device operator sign-off — the user explicitly diverged from the deferrable-operator-check option (D-11), so the device gate is a HARD GATE with no fallback.

After this plan: the full `apps/web` Playwright suite is green at 375px + 390px (mechanical gate, D-10a), the outcome-word test is pinned to a confirmed seeded settled call, and an operator has personally verified the SC3 share→receipt landing on a real iPhone (Safari) + Android (Chrome) (D-10b). Only then is the phase complete.

Purpose: SC1 + SC2 + SC3 final verification; D-10/D-11 dual gate.
Output: finalized `apps/web/tests/responsive.spec.ts` (seeded id confirmed) + a passing full suite + the operator sign-off record.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-mobile-responsive-on-7-critical-pages/09-CONTEXT.md
@.planning/phases/09-mobile-responsive-on-7-critical-pages/09-RESEARCH.md
@.planning/phases/09-mobile-responsive-on-7-critical-pages/09-VALIDATION.md
@.planning/phases/09-mobile-responsive-on-7-critical-pages/09-UI-SPEC.md
@apps/web/tests/responsive.spec.ts
</context>

<artifacts_this_phase_produces>
- `apps/web/tests/responsive.spec.ts` — finalized: the outcome-word test reads `RESPONSIVE_SETTLED_CALL_ID` (env override) with a confirmed stable default; all named tests green.
- An operator sign-off record in `09-08-SUMMARY.md` (device, browser, pass/notes) — the D-11 hard-gate evidence.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Confirm seeded settled-call id + run the full apps/web Playwright suite green</name>
  <files>apps/web/tests/responsive.spec.ts</files>
  <read_first>
    - apps/web/tests/responsive.spec.ts (the SEEDED_SETTLED_CALL / RESPONSIVE_SETTLED_CALL_ID const from 09-01; the outcome-word test)
    - .planning/phases/09-mobile-responsive-on-7-critical-pages/09-RESEARCH.md (Open Question 1 + Assumption A1 — MEMORY says Phase-7 seeded a CallerLost call #14; confirm it is still settled+available on the test/staging target)
    - .planning/phases/09-mobile-responsive-on-7-critical-pages/09-VALIDATION.md (Sampling Rate — full suite command + regression-guard list)
    - apps/web/playwright.config.ts (webServer: pnpm start after a build — full suite needs pnpm build first)
    - apps/web/tests/quote-composer.spec.ts, leaderboard.spec.ts, signin.spec.ts, profile-*.spec.ts, og-thumbnail-200px.spec.ts, og-fallback*.spec.ts (the regression guards that must stay green)
  </read_first>
  <action>
    First, CONFIRM a stable seeded settled call id for the outcome-word assertion (resolves Open Question 1). Check whether call #14 (the Phase-7 CallerLost seed, per MEMORY) is still settled and renders on the test/staging target — query the live subgraph/relayer or the seed scripts (`apps/relayer/src/scripts/seed-loss-call.ts` / soak-seeder) for a known-settled id. Set the confirmed id as the default for `RESPONSIVE_SETTLED_CALL_ID` in `responsive.spec.ts` (keep the env override). If no single id reliably renders all needed states, point the outcome-word test at the most stable settled id and document the choice. Then run the FULL `apps/web` suite (`cd apps/web && pnpm build && pnpm exec playwright test`) and drive it to all-green at 375/390px: `responsive.spec.ts` (no horizontal scroll, touch targets ≥44, outcome word legible, banner present/absent, single-column/full-width, server snapshot) AND every pre-existing regression guard (`quote-composer.spec.ts` no-grid, `og-thumbnail-200px.spec.ts`, `og-fallback*.spec.ts`, `signin.spec.ts`, `leaderboard.spec.ts`, `profile-*.spec.ts`, `feed-shell.spec.ts`, `onboarding.spec.ts`, `new-call-publish.spec.ts`). If any responsive assertion is red, the corresponding earlier plan left a gap — fix the layout in that page file (within this plan's diff is acceptable for small fixes; large fixes route back to the owning plan). Do NOT weaken an assertion to make it pass. Do NOT add a Playwright project to the config (viewport stays in-spec).
  </action>
  <verify>
    <automated>cd apps/web && pnpm build && pnpm exec playwright test 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `RESPONSIVE_SETTLED_CALL_ID` default is a confirmed-settled call id (documented in the summary); env override retained.
    - `cd apps/web && pnpm build && pnpm exec playwright test` reports 0 failures (full suite green) — `responsive.spec.ts` + all regression guards.
    - The `outcome word legible` test passes against the confirmed id (fontSize ≥36, box within viewport, no wrap).
    - No assertion was weakened/skipped to pass; no `display:grid` anywhere (`quote-composer.spec.ts` no-grid still green).
  </acceptance_criteria>
  <done>Seeded id confirmed; the full apps/web Playwright suite is green at 375/390px with all regression guards intact (D-10a satisfied).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: [OPERATOR] Real-device share->receipt sign-off (D-11 HARD GATE — no fallback)</name>
  <files>.planning/phases/09-mobile-responsive-on-7-critical-pages/09-08-SUMMARY.md</files>
  <action>
    Pause for operator verification. This is a `checkpoint:human-verify` `blocking-human` gate — it is NEVER auto-approvable (`workflow.auto_advance` is ignored here per D-11). The operator personally runs the on-device steps in `<how-to-verify>` on a REAL iPhone (Safari) AND a REAL Android (Chrome) against the deployed staging build, then records device/browser/OS + pass/notes in `09-08-SUMMARY.md`. Do NOT mark Phase 9 complete until BOTH this gate AND Task 1's full Playwright suite are green (D-11 BOTH-gates rule, no fallback). If any on-device check fails, file the gap and route the fix back to the owning page/drawer/banner/modal plan rather than patching here.
  </action>
  <what-built>
    The 7 critical pages now render single-column at 375px with full-width buttons, a left hamburger drawer, ≥44px touch targets, and no horizontal scroll; the 2 non-critical pages (/new incl ?quote=, /duel/[id]) show a dismissible "Best viewed on desktop" banner; in-scope modals fit the viewport. The full Playwright mechanical gate (D-10a) is green at 375/390px. What automation CANNOT verify is the real-device share→receipt landing — real iOS Safari + Android Chrome rendering, touch, and Twitter/Farcaster card landing (D-10b). D-11 makes this a HARD GATE with NO fallback: Phase 9 cannot be marked complete without it.
  </what-built>
  <how-to-verify>
    Deploy the branch to the Sepolia/staging Vercel target (the existing `call-it-web-sepolia` web deploy flow — master push redeploys WEB via Vercel native; or run the web deploy per the Phase-7 runbook). Then, on a REAL iPhone (Safari) AND a REAL Android (Chrome):
    1. Open a Twitter/Farcaster share link (or paste the URL) to a SETTLED Receipt (the confirmed seeded settled call from Task 1). Confirm: the Twitter Card click lands on the mobile Settled Receipt; the outcome word ("CALLED IT" / "LOUD AND WRONG" / etc.) is LEGIBLE and not clipped/wrapped; the "[ SHARE THE RECEIPT → ]" + "[ VIEW ALL CALLS BY {handle} ]" buttons STACK vertically and are tappable; the Sign-in CTA is visible for an unauthenticated viewer.
    2. Open the hamburger drawer: confirm Feed + Leaderboard show for a logged-out viewer, Profile/New Call/Sign-out do NOT (auth-aware, D-06); confirm it closes on backdrop tap and link tap.
    3. Visit /new and /duel/[id]: confirm the "Best viewed on desktop" banner appears at the top, is dismissible, and the hamburger still lets you navigate away / sign out (SC2, D-07).
    4. Spot-check Feed, Profile, Leaderboard, Sign-in, Onboarding for no horizontal scroll + tappable controls.
    Record device + browser + OS version + pass/notes in 09-08-SUMMARY.md. If anything fails, file the gap and route the fix back to the owning plan — do NOT mark the phase complete.
  </how-to-verify>
  <verify>
    <human-check>Operator confirms on a real iPhone (Safari) AND a real Android (Chrome): settled-receipt outcome word legible + action buttons stack + tappable + Sign-in CTA visible for logged-out viewers; hamburger drawer auth-aware + closes correctly; banner present/dismissible on /new + /duel with exit still reachable; no horizontal scroll on the spot-checked critical pages. Device/browser/OS recorded in 09-08-SUMMARY.md.</human-check>
  </verify>
  <resume-signal>Type "approved" with the device/browser/OS recorded, or describe the issues found (which route back to the owning page plan as a gap).</resume-signal>
  <done>Operator has signed off the SC3 share→receipt landing on a real iPhone (Safari) AND a real Android (Chrome), with device/browser/OS + pass recorded in 09-08-SUMMARY.md (D-10b / D-11 hard gate satisfied).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public unauthenticated share-visitor → deployed mobile Settled Receipt | The operator gate verifies a PUBLIC surface on real devices — confirm no owner-only control or PII surfaced and the Sign-in CTA (not an authenticated action) is what a logged-out viewer sees. |
| operator device → staging deploy | Read-only verification; the operator does not transact (Sepolia not in Warpcast chainList — tap-to-transact is the Phase-10 D-01 gate, out of scope here). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-08-01 | Information disclosure | mobile Settled Receipt on real devices | mitigate | Operator visually confirms (step 1) no wallet address / owner-only control leaked into the mobile layout (AUTH-44 holds; layout-only changes). |
| T-09-08-02 | Elevation of privilege | hamburger drawer on real devices | mitigate | Operator confirms (step 2) logged-out viewers cannot reach New Call / Profile / Sign-out via the drawer (D-06 gate proven on-device, not just in source). |
| T-09-08-03 | Denial (UX) | banner blocking exit | mitigate | Operator confirms (step 3) the banner is dismissible and the hamburger exit stays reachable on banner pages (SC2/D-07). |
| T-09-08-SC | Tampering | npm/pnpm installs | mitigate | No new packages across the whole phase. Supply-chain legitimacy gate is N/A (verified: zero new deps). This checkpoint is NOT auto-approvable (it is a `blocking-human` gate independent of any package gate). |
</threat_model>

<verification>
- `cd apps/web && pnpm build && pnpm exec playwright test` reports 0 failures (D-10a).
- Operator sign-off recorded for iPhone Safari AND Android Chrome (D-10b / D-11 hard gate).
- Both gates pass before the phase is marked complete (D-11 BOTH-gates rule).
</verification>

<success_criteria>
- Full apps/web Playwright suite green at 375/390px with all regression guards intact.
- Outcome-word test pinned to a confirmed seeded settled call.
- Operator real-device sign-off (iPhone Safari + Android Chrome) recorded — the D-11 hard gate with no fallback is satisfied.
</success_criteria>

<output>
Create `.planning/phases/09-mobile-responsive-on-7-critical-pages/09-08-SUMMARY.md` when done (include the operator device/browser/OS sign-off record).
</output>
