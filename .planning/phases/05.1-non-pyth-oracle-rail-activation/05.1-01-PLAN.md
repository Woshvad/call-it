---
phase: 05.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/interfaces/ICallRegistry.sol
  - packages/contracts/test/AdapterMapTest.sol
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - SETTLE-06
  - SETTLE-DUAL-GOV

must_haves:
  truths:
    - "EventSubtype enum has Governance_Snapshot(6) and Governance_Tally(7) as distinct values, ProtocolMilestone(8)"
    - "adapterMap[Event][6] and adapterMap[Event][7] can each hold a different OracleAdapter without collision"
    - "AdapterMapTest suite passes 100% in Foundry (testAdapterMapGovernanceDualOracle + all-8-pairs)"
    - "No existing ICallRegistry consumers (CallRegistry.sol, SettlementManager.sol) require code changes from the enum expansion"
    - "SETTLE-DUAL-GOV requirement is documented in REQUIREMENTS.md"
  artifacts:
    - path: "packages/contracts/src/interfaces/ICallRegistry.sol"
      provides: "Updated EventSubtype enum with 9 values (0-8)"
      contains: "Governance_Snapshot"
    - path: "packages/contracts/test/AdapterMapTest.sol"
      provides: "Foundry test for dual-governance adapterMap configuration"
      exports: ["AdapterMapTest", "testAdapterMapGovernanceDualOracle", "testAllEightAdapterPairs"]
  key_links:
    - from: "ICallRegistry.sol EventSubtype"
      to: "SettlementManager.sol adapterMap[marketType][eventSubtype]"
      via: "uint8(call.eventSubtype) in _checkAdapterBinding at SettlementManager.sol:591"
      pattern: "Governance_Snapshot.*=.*6"
    - from: "AdapterMapTest.sol"
      to: "SmTestHelper.sol"
      via: "SmTestHelper base class super.setUp()"
      pattern: "is SmTestHelper"
---

<objective>
Apply the Option A EventSubtype enum split to ICallRegistry.sol: add Governance_Snapshot=6 and Governance_Tally=7, renumber ProtocolMilestone to 8. Add SETTLE-DUAL-GOV requirement to REQUIREMENTS.md. Write AdapterMapTest.sol to prove the dual-governance slot separation works and all 8 adapterMap pairs can be independently configured.

Purpose: The existing single Governance=6 value in EventSubtype creates an adapterMap collision — adapterMap[Event][6] can only hold one OracleAdapter (Snapshot=4 OR Tally=5, never both). This split resolves the collision. SettlementManager and CallRegistry source code do NOT change — only the interface enum body changes, and both contracts redeploy in Plan 02.

Output: ICallRegistry.sol with updated enum, AdapterMapTest.sol passing in Foundry, SETTLE-DUAL-GOV in REQUIREMENTS.md.

**Design decision (for user review):** ProtocolMilestone oracle-type assignment is set to `DefiLlama(2)` in the deploy script (Plan 02) rather than RpcMetrics, because §4.3.7 protocol/chain milestones are predominantly TVL-based metrics that DefiLlama already covers. If the user wants RpcMetrics for on-chain validator/supply-ratio milestones, override in Plan 02 before execution.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05.1-non-pyth-oracle-rail-activation/05.1-RESEARCH.md
@.planning/phases/05.1-non-pyth-oracle-rail-activation/05.1-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update EventSubtype enum in ICallRegistry.sol + add SETTLE-DUAL-GOV</name>
  <read_first>
    packages/contracts/src/interfaces/ICallRegistry.sol — read the entire file; the EventSubtype enum is at lines 23-32; verify pragma is =0.8.30 (CI guard); confirm CallRegistry.sol and SettlementManager.sol do NOT compare eventSubtype to integer literals (they use the interface enum via ABI encoding so no code change needed)
    .planning/phases/05.1-non-pyth-oracle-rail-activation/05.1-PATTERNS.md — ICallRegistry.sol section (exact Option A enum body)
    .planning/REQUIREMENTS.md — find the "SETTLE-52" entry at line 284 and the coverage table entry at line 815 (insertion point for SETTLE-DUAL-GOV); find the summary line at ~980 (update count)
  </read_first>

  <behavior>
    - BEFORE change: enum has 8 values (None=0 ... ProtocolMilestone=7)
    - AFTER change: enum has 9 values: None=0, TvlMilestone=1, VolumeFees=2, OnchainMetric=3, CexListing=4, TokenLaunch=5, Governance_Snapshot=6, Governance_Tally=7, ProtocolMilestone=8
    - The pragma line stays =0.8.30 exactly (CI grep guard must not fail)
    - SETTLE-DUAL-GOV appears in REQUIREMENTS.md as a new SETTLE-53 equivalent entry after SETTLE-52
  </behavior>

  <action>
    Modify packages/contracts/src/interfaces/ICallRegistry.sol:

    Replace the EventSubtype enum body (currently 8 values ending with Governance=6, ProtocolMilestone=7) with the 9-value Option A body:
      None=0, TvlMilestone=1, VolumeFees=2, OnchainMetric=3, CexListing=4, TokenLaunch=5, Governance_Snapshot=6, Governance_Tally=7, ProtocolMilestone=8.
    Add inline comment after each new value: "// NEW: Phase 05.1 Option A enum split (SETTLE-DUAL-GOV)" on the Governance_Snapshot and Governance_Tally lines.
    Add a comment block above Governance_Snapshot: "// Governance enum split: supports BOTH Snapshot(4) and Tally(5) oracle adapters via distinct adapterMap keys".
    Update comment next to ProtocolMilestone: "// 8 — renumbered from 7 (Phase 05.1 Option A)".
    Pragma must remain: pragma solidity =0.8.30;

    Modify .planning/REQUIREMENTS.md:
    After the SETTLE-52 line insert:
      - [ ] **SETTLE-DUAL-GOV**: SettlementManager adapterMap supports BOTH Snapshot(4) and Tally(5) governance oracle adapters via distinct EventSubtype keys (Governance_Snapshot=6 and Governance_Tally=7); adapterMap[Event][6] = Snapshot and adapterMap[Event][7] = Tally are independently configurable; _checkAdapterBinding preserves the oracleType binding for each (Phase 05.1 Option A, §13.5, §12.4)
    In the coverage table near line 815, after the SETTLE-52 row add:
      | SETTLE-DUAL-GOV | Phase 05.1 | Pending |
    Update the summary count line: "v1 requirements: 407 total" becomes 408; SETTLE count 52 becomes 53.
  </action>

  <verify>
    <automated>cd "packages/contracts" && forge build --silent 2>&1 | grep -i error || echo "BUILD OK"</automated>
  </verify>

  <done>forge build exits 0; grep "Governance_Snapshot" packages/contracts/src/interfaces/ICallRegistry.sol returns a match; grep "Governance_Tally" returns a match; grep "SETTLE-DUAL-GOV" .planning/REQUIREMENTS.md returns a match; grep "pragma solidity \^" packages/contracts/src/interfaces/ICallRegistry.sol returns empty (CI guard still passes)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write AdapterMapTest.sol — dual-governance + all-8-pairs</name>
  <read_first>
    packages/contracts/test/SettlementAttestationTest.sol — read the entire file; note the SmTestHelper base class usage, setUp() override pattern, vm.prank(owner) pattern, OracleAdapter enum references, and how the test contract declares constants
    packages/contracts/test/helpers/SmTestHelper.sol — read setUp() to understand what state is deployed (SM, CR, PR, FFM, CE) and what the `owner` address is (deployer key used in vm.startBroadcast inside the helper, likely `address(this)` or a vm.addr)
    packages/contracts/src/interfaces/ISettlementManager.sol — read lines 24-31 for OracleAdapter enum values (Pyth=0, NftTwap=1, DefiLlama=2, RpcMetrics=3, Snapshot=4, Tally=5, CexScraper=6)
    packages/contracts/src/interfaces/ICallRegistry.sol — read the updated EventSubtype enum (just modified in Task 1) and MarketType enum
  </read_first>

  <behavior>
    - testAdapterMapGovernanceDualOracle: sets adapterMap[Event=2][Governance_Snapshot=6]=Snapshot(4) AND adapterMap[Event=2][Governance_Tally=7]=Tally(5); asserts each slot independently returns the correct OracleAdapter; asserts neither overwrites the other
    - testAllEightAdapterPairs: sets all 8 pairs from the RESEARCH.md table; asserts each reads back correctly; covers SETTLE-06 + SETTLE-DUAL-GOV
    - testNonPythAttestationRejectedWithoutAdapterMap: submits a fake Snapshot attestation (oracleType=4) against adapterMap[Event][6]=Pyth(0) default; asserts the call reverts with InvalidAttestation (confirms the blast-radius invariant: unset slot=Pyth blocks non-Pyth attestations)
    - testPythPathUnaffected: sets adapterMap[PriceTarget=0][None=0]=Pyth(0); asserts it stays Pyth(0) after the governance slots are configured (Pyth spine unaffected invariant)
  </behavior>

  <action>
    Create packages/contracts/test/AdapterMapTest.sol.

    File header pattern from SettlementAttestationTest.sol:
      // SPDX-License-Identifier: MIT
      pragma solidity =0.8.30;
      // Spec: CALL_IT_SPEC1.md §12.4 — setAdapterMap + _checkAdapterBinding
      // Requirements: SETTLE-06, SETTLE-DUAL-GOV

    Imports: forge-std/Test.sol, helpers/SmTestHelper.sol, interfaces/ICallRegistry.sol, interfaces/ISettlementManager.sol.

    Contract: AdapterMapTest is SmTestHelper. Constants: ADAPTER_PYTH=0, ADAPTER_NftTwap=1, ADAPTER_DEFI_LLAMA=2, ADAPTER_RPC_METRICS=3, ADAPTER_SNAPSHOT=4, ADAPTER_TALLY=5, ADAPTER_CEX_SCRAPER=6. setUp() calls super.setUp().

    testAdapterMapGovernanceDualOracle: use vm.prank(owner) for each setAdapterMap call (owner is the SmTestHelper deployer). Call sm.setAdapterMap(2, 6, ISettlementManager.OracleAdapter.Snapshot) and sm.setAdapterMap(2, 7, ISettlementManager.OracleAdapter.Tally). Assert sm.adapterMap(2, 6) == ADAPTER_SNAPSHOT and sm.adapterMap(2, 7) == ADAPTER_TALLY. Assert sm.adapterMap(2, 6) != sm.adapterMap(2, 7) (they differ — the core SETTLE-DUAL-GOV invariant).

    testAllEightAdapterPairs: call setAdapterMap for all 8 pairs from RESEARCH.md table (Event/TvlMilestone=1→DefiLlama=2, Event/VolumeFees=2→DefiLlama=2, Event/OnchainMetric=3→RpcMetrics=3, Event/CexListing=4→CexScraper=6, Event/TokenLaunch=5→CexScraper=6, Event/Governance_Snapshot=6→Snapshot=4, Event/Governance_Tally=7→Tally=5, Event/ProtocolMilestone=8→DefiLlama=2). Assert all 8 read back correctly.

    testNonPythAttestationRejectedWithoutAdapterMap: leave adapterMap[2][6] at default (Pyth=0). Expect vm.expectRevert("InvalidAttestation") when calling sm.submitAttestation with oracleType=4 (Snapshot) for a call with marketType=2, eventSubtype=6. NOTE: if SmTestHelper does not expose a helper to create a live call with those params, create one via vm.prank(callerAddr) + cr.createCall(..., MarketType.Event, EventSubtype.Governance_Snapshot, ...) before the submitAttestation call. If setting up a full call is too complex within the test scope, use vm.expectRevert and call _checkAdapterBinding via a test-harness wrapper, OR document that the attestation rejection is already covered by SettlementAttestationTest.sol and use an assertEq on the raw adapterMap return value instead (adapterMap[2][6] == 0 means Pyth, so oracleType=4 != 0 is the failure condition).

    testPythPathUnaffected: set governance slots as in testAdapterMapGovernanceDualOracle. Then assert sm.adapterMap(0, 0) == ADAPTER_PYTH (default zero, which IS Pyth). This confirms configuring governance slots does not disturb the Pyth price-target path.
  </action>

  <verify>
    <automated>cd "packages/contracts" && forge test --match-contract AdapterMapTest -v 2>&1 | tail -20</automated>
  </verify>

  <done>forge test --match-contract AdapterMapTest exits 0 with 4 passing tests; output shows "[PASS] testAdapterMapGovernanceDualOracle", "[PASS] testAllEightAdapterPairs", "[PASS] testNonPythAttestationRejectedWithoutAdapterMap", "[PASS] testPythPathUnaffected"</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ICallRegistry.sol → deployed CallRegistry.sol | Enum change is compile-time only; no live state changes in this plan (both contracts redeploy in Plan 02) |
| AdapterMapTest → SmTestHelper | Test-only; no production attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-01-01 | Tampering | ICallRegistry.sol EventSubtype enum renumbering | mitigate | Pre-deploy audit required (Plan 02 Task 1): verify no live Sepolia calls have eventSubtype=7 (old ProtocolMilestone) before deploying new CallRegistry; if any exist, document them — they will mis-decode as Governance_Tally after the enum change (RESEARCH.md Pitfall 3) |
| T-05.1-01-02 | Tampering | Storage-layout drift from enum expansion | accept | Neither CallRegistry nor SettlementManager store enum values as packed literals that would shift; the Call struct stores eventSubtype as uint8; ABI encoding is by integer value not by name; renumbering ProtocolMilestone from 7 to 8 only affects new calls created after redeploy — low risk on Sepolia staging with fresh state |
| T-05.1-01-03 | Denial of Service | adapterMap[Event][6] defaulting to Pyth(0) after enum split | mitigate | This plan establishes the test (testNonPythAttestationRejectedWithoutAdapterMap) proving the default Pyth slot rejects non-Pyth attestations; Plan 02 must call setAdapterMap for all 8 pairs atomically to avoid any window where the default blocks settlement |
| T-05.1-01-04 | Spoofing | CI pragma guard bypass | accept | CI grep guard already enforced from Phase 0; ICallRegistry.sol pragma stays =0.8.30; test verifies with forge build |
</threat_model>

<verification>
forge build exits 0 (no compilation errors from enum expansion)
forge test --match-contract AdapterMapTest exits 0 with 4 passing tests
grep "Governance_Snapshot" packages/contracts/src/interfaces/ICallRegistry.sol returns non-empty
grep "Governance_Tally" packages/contracts/src/interfaces/ICallRegistry.sol returns non-empty
grep -v "^//" packages/contracts/src/interfaces/ICallRegistry.sol | grep "pragma solidity \^" returns empty (no float pragma)
grep "SETTLE-DUAL-GOV" .planning/REQUIREMENTS.md returns non-empty
</verification>

<success_criteria>
ICallRegistry.sol EventSubtype enum has 9 values (None=0 through ProtocolMilestone=8) with Governance_Snapshot=6 and Governance_Tally=7 as distinct values. AdapterMapTest passes 4/4 in Foundry. SETTLE-DUAL-GOV is documented in REQUIREMENTS.md. forge build exits 0. No existing tests broken: forge test exits 0 across the full suite.
</success_criteria>

<output>
After completion, create .planning/phases/05.1-non-pyth-oracle-rail-activation/05.1-01-SUMMARY.md
</output>
