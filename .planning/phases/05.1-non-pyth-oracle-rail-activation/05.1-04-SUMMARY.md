---
phase: 05.1-non-pyth-oracle-rail-activation
plan: 04
subsystem: relayer
tags: [criteria-store, drizzle, postgres, migration, off-chain-criteria, defillama, rpc-metrics, cex, defer-safe, live]

# Dependency graph
requires:
  - phase: 05.1-non-pyth-oracle-rail-activation
    provides: rewired value adapters from Plan 03 (signOracleAttestation + targetValue pass-through)
provides:
  - call_oracle_criteria store (resolveCriteria/insertCriteria) for the string identifiers the on-chain Call does not carry (protocolSlug, tokenSymbol)
  - Live Drizzle migration 0004 applied to call-it-pg-sepolia
  - Fail-safe criteria resolution: missing row -> ambiguous:true -> DEFER (never mis-settle)
affects: [phase-06-soak, relayer-settlement, defillama-adapter, rpc-metrics-adapter, cex-adapter]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Drizzle versioned migration: db:generate produces NNNN_*.sql (no DB connection); db:migrate applies via the node pg driver through a fly proxy tunnel."
    - "Off-chain criteria: governance/NFT use assetA bytes32 encoding (no store lookup); DefiLlama/RpcMetrics/CEX use call_oracle_criteria, populated by insertCriteria at call creation."

key-files:
  created:
    - apps/relayer/src/db/criteria-store.ts
    - apps/relayer/src/workers/__tests__/criteria-store.test.ts
    - apps/relayer/src/scripts/backfill-criteria.ts
    - apps/relayer/src/db/migrations/0004_brave_wind_dancer.sql
  modified:
    - apps/relayer/src/db/schema.ts
    - apps/relayer/src/workers/calls-preflight.ts
    - apps/relayer/src/workers/oracle-adapters/defillama-adapter.ts
    - apps/relayer/src/workers/oracle-adapters/rpc-metrics-adapter.ts
    - apps/relayer/src/workers/oracle-adapters/cex/cex-adapter.ts

key-decisions:
  - "resolveCriteria returns null when the row is missing; adapters return ambiguous:true -> the settlement DEFERs (Pending) rather than settling against a guessed value."
  - "insertCriteria wired into calls-preflight for DefiLlama/RpcMetrics/CEX calls only; Pyth/Snapshot/Tally/NFT excluded (they use assetA encoding)."
  - "targetValue flows from the 19-field Call ABI through the adapters with no 0n default (a 0n default would settle against zero, violating SC-4)."

patterns-established:
  - "call_oracle_criteria table modeled on the trendingDuels Drizzle table; one-shot backfill script for seeded test calls."

requirements-completed: [SETTLE-18, SETTLE-19, SETTLE-20, SETTLE-21, SETTLE-22, SETTLE-23, SETTLE-24]

# Metrics
duration: live-operator-session
completed: 2026-06-02
---

# Phase 05.1 Plan 04: Off-chain Criteria Store (Gap B.3)

**The `call_oracle_criteria` store is live on Fly Postgres**, resolving the string identifiers (protocolSlug, tokenSymbol) that the on-chain Call does not carry, with a hard fail-safe: any missing/ambiguous criteria DEFERs settlement rather than mis-settling.

## Live DB migration
Migration `0004_brave_wind_dancer.sql` applied to `call-it-pg-sepolia` via `flyctl proxy 5433:5432` + `drizzle-kit migrate`. Verified: `call_oracle_criteria(call_id, oracle_type, identifier, target_unit, created_at)` exists (0 rows; backfill awaits seeded non-Pyth calls in the Phase 6 soak).

## Tests
criteria-store 5/5; oracle-attestation 12/12 still green (Pyth path unaffected by the import-chain changes).

## Carried to Phase 6
Backfill `call_oracle_criteria` once non-Pyth test calls are seeded on the new CallRegistry.

---
*Phase: 05.1 · Plan 04 · Completed 2026-06-02*
