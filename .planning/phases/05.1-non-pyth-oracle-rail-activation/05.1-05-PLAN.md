---
phase: 05.1
plan: 05
type: execute
wave: 3
depends_on:
  - 05.1-02
  - 05.1-03
  - 05.1-04
files_modified:
  - packages/shared/src/constants/addresses.ts
  - packages/subgraph/subgraph.yaml
  - packages/subgraph/src/call-registry.ts
autonomous: false
requirements:
  - SETTLE-06
  - SETTLE-DUAL-GOV
  - SETTLE-13
  - SETTLE-14
  - SETTLE-15
  - SETTLE-16
  - SETTLE-18
  - SETTLE-19
  - SETTLE-20
  - SETTLE-21
  - SETTLE-22
  - SETTLE-23
  - SETTLE-24

must_haves:
  truths:
    - "addresses.ts CALL_REGISTRY_ARBITRUM_SEPOLIA and SETTLEMENT_MANAGER_ARBITRUM_SEPOLIA match the addresses deployed in Plan 02"
    - "subgraph.yaml CallRegistry and SettlementManager datasources point to new addresses with correct startBlocks"
    - "Subgraph redeployed to Studio and indexes events from new contract addresses"
    - "Relayer .env updated with new SM + CR addresses"
    - "Phase 4 Foundry test suite re-passes against the new deploy (13+12+5+4 = 34 tests green)"
    - "cast call NEW_SM adapterMap(2,6)(uint8) returns 4 on Sepolia"
    - "oracle-attestation.test.ts still passes 12/12"
    - "Pyth price-target settlement path is unaffected — a PriceTarget/None call settles normally"
  artifacts:
    - path: "packages/shared/src/constants/addresses.ts"
      provides: "Updated CALL_REGISTRY_ARBITRUM_SEPOLIA and SETTLEMENT_MANAGER_ARBITRUM_SEPOLIA constants"
      contains: "Phase 05.1 redeploy"
    - path: "packages/subgraph/subgraph.yaml"
      provides: "Updated CallRegistry + SettlementManager datasource addresses and startBlocks"
      contains: "Phase 05.1 REDEPLOY"
  key_links:
    - from: "packages/shared/src/constants/addresses.ts"
      to: "apps/relayer/src/workers/settlement-watcher.ts settlementManagerAddress"
      via: "import SETTLEMENT_MANAGER_ARBITRUM_SEPOLIA from @call-it/shared"
      pattern: "SETTLEMENT_MANAGER_ARBITRUM_SEPOLIA"
    - from: "packages/subgraph/subgraph.yaml"
      to: "The Graph Studio subgraph"
      via: "graph deploy --studio call-it-sepolia"
      pattern: "address:.*NEW_SM_ADDRESS"
---

<objective>
Complete the redeploy rollup: update addresses.ts with the new SM and CR addresses from Plan 02, update subgraph.yaml datasource addresses + startBlocks, redeploy the subgraph to Studio, update relayer env, and verify the full Phase 4 Foundry suite re-passes. Close the phase with a live smoke verification confirming the Pyth spine is unaffected and the non-Pyth adapterMap is correctly configured.

Purpose: Without this plan, the relayer still sends transactions to the old SM (now inert), the subgraph indexes old contract addresses (misses all new events), and frontend queries show stale data. This plan wires all consumers to the new deployment.

Output: addresses.ts updated; subgraph redeployed; Phase 4 Foundry suite green; relayer env updated; live cast call assertions confirming the correct adapterMap on Sepolia.

**Phase 05.1 safety invariant check:** Every non-Pyth failure path in the deployed system reverts or DEFERs. The Pyth settlement spine is unaffected. threats_open after this plan should be 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.1-non-pyth-oracle-rail-activation/05.1-RESEARCH.md
@.planning/phases/05.1-non-pyth-oracle-rail-activation/05.1-PATTERNS.md
@.planning/phases/05.1-non-pyth-oracle-rail-activation/05.1-02-SUMMARY.md
@.planning/phases/05.1-non-pyth-oracle-rail-activation/05.1-03-SUMMARY.md
@.planning/phases/05.1-non-pyth-oracle-rail-activation/05.1-04-SUMMARY.md
</context>

<interfaces>
From packages/shared/src/constants/addresses.ts (existing pattern):
  export const CALL_REGISTRY_ARBITRUM_SEPOLIA = '0x...' as const;
  export const SETTLEMENT_MANAGER_ARBITRUM_SEPOLIA = '0x...' as const;
  Records CALL_REGISTRY_ADDRESSES[421614] and SETTLEMENT_MANAGER_ADDRESSES[421614] reference these constants — they auto-update, no manual change needed.

From packages/subgraph/subgraph.yaml (existing pattern):
  datasources: [{ name: CallRegistry, source: { address: "...", startBlock: N } }, { name: SettlementManager, source: { address: "...", startBlock: N } }]
  Other datasources (ProfileRegistry, FollowFadeMarket, ChallengeEscrow) are NOT redeployed — addresses unchanged.

Subgraph deploy command:
  cd packages/subgraph && pnpm run build && pnpm run deploy:sepolia
  (or: graph deploy --studio call-it-sepolia)

Phase 4 test suites (per RESEARCH.md):
  forge test --match-contract SettlementManagerTest     (13 tests)
  forge test --match-contract SettlementAttestationTest (12 tests — CRITICAL, includes testUnattestedNonPythDefers)
  forge test --match-contract SettlementDisputeTest     (5 tests)
  forge test --match-contract FfmSettlementTest         (4 tests)
  forge test (full suite — must include all of the above)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update addresses.ts + subgraph.yaml with new contract addresses</name>
  <read_first>
    packages/shared/src/constants/addresses.ts — read the entire file; find CALL_REGISTRY_ARBITRUM_SEPOLIA and SETTLEMENT_MANAGER_ARBITRUM_SEPOLIA constants; note the JSDoc comment pattern and the CALL_REGISTRY_ADDRESSES / SETTLEMENT_MANAGER_ADDRESSES record objects that reference the named constants
    packages/subgraph/subgraph.yaml — read the full file; find the CallRegistry datasource (note current address and startBlock) and the SettlementManager datasource (note current address and startBlock); confirm other datasources are NOT changed
    packages/subgraph/src/call-registry.ts — scan for any eventSubtype value comparisons that would need updating for Option A; RESEARCH.md says no branching on specific subtype values exists currently, but verify
    .planning/phases/05.1-non-pyth-oracle-rail-activation/05.1-02-SUMMARY.md — READ THIS FIRST to get the new CR address, new SM address, new CR deploy block, new SM deploy block
  </read_first>

  <action>
    A. Modify packages/shared/src/constants/addresses.ts:
       Update CALL_REGISTRY_ARBITRUM_SEPOLIA with the new CR address from Plan 02 SUMMARY.
       Add/update the JSDoc comment:
         @Phase 05.1 REDEPLOY: Option A EventSubtype enum split (Governance_Snapshot=6, Governance_Tally=7).
         DEPLOYED [date] via DeployPhase5_1.s.sol. Deploy block: [new block].
         Supersedes Phase 2 v2 address (0x7DAd...234D).
       Update SETTLEMENT_MANAGER_ARBITRUM_SEPOLIA with the new SM address from Plan 02 SUMMARY.
       Add/update JSDoc:
         @Phase 05.1 REDEPLOY: setAdapterMap x8 + setAttestationSigner x6 configured.
         DEPLOYED [date] via DeployPhase5_1.s.sol. Deploy block: [new block].
         Supersedes Phase 4 address (0xAc37...4595).
       Do NOT change CALL_REGISTRY_ADDRESSES or SETTLEMENT_MANAGER_ADDRESSES record objects — they use the named constants and auto-update.

    B. Update SUBGRAPH_URL_SEPOLIA constant to the next version label (e.g., v0.5.0) — this will be set correctly after the subgraph redeploy checkpoint.

    C. Modify packages/subgraph/subgraph.yaml:
       Update the CallRegistry datasource:
         address: [new CR address from Plan 02]
         startBlock: [new CR deploy block from Plan 02]
         Comment: # Phase 05.1 REDEPLOY: Option A enum split. DEPLOYED [date] via DeployPhase5_1.s.sol. Supersedes Phase 2 v2 address (0x7DAd...234D, block 272458669).
       Update the SettlementManager datasource:
         address: [new SM address from Plan 02]
         startBlock: [new SM deploy block from Plan 02]
         Comment: # Phase 05.1 REDEPLOY: setAdapterMap + setAttestationSigner configured. DEPLOYED [date]. Supersedes Phase 4 address (0xAc37...4595, block 272912513).
       All other datasources (ProfileRegistry, FollowFadeMarket, ChallengeEscrow) are UNCHANGED.

    D. Check packages/subgraph/src/call-registry.ts:
       If any handler compares call.eventSubtype to a literal integer for subtype 7 (old ProtocolMilestone), update the comparison to 8. If no branching on specific subtype values exists (RESEARCH.md says none), make no code change and add a comment: // eventSubtype stored as raw Int; no subtype-specific branching in v1 handlers.
  </action>

  <verify>
    <automated>cd "packages/shared" && pnpm tsc --noEmit 2>&1 | grep "error TS" | head -10 && grep "CALL_REGISTRY_ARBITRUM_SEPOLIA" "packages/shared/src/constants/addresses.ts" | head -3 && pnpm --filter @call-it/relayer tsc --noEmit 2>&1 | grep "error TS" | head -10 && pnpm --filter @call-it/subgraph build 2>&1 | grep -i "error" | head -10 || echo "SUBGRAPH BUILD OK"</automated>
  </verify>

  <done>grep CALL_REGISTRY_ARBITRUM_SEPOLIA packages/shared/src/constants/addresses.ts shows the new address (NOT 0x7DAd...234D); grep SETTLEMENT_MANAGER_ARBITRUM_SEPOLIA shows the new address (NOT 0xAc37...4595); grep the new SM address in subgraph.yaml returns a match; pnpm tsc --noEmit exits 0</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>addresses.ts and subgraph.yaml updated with new contract addresses. Phase 4 Foundry tests must re-pass against the new deploy. Subgraph must be redeployed to Studio. Relayer env must be updated. Live on-chain smoke confirms adapterMap is correctly set and Pyth spine is unaffected.</what-built>
  <how-to-verify>
    Step 1 — Run Phase 4 Foundry suite (these tests run against in-memory fork; no live chain needed):
      cd packages/contracts
      forge test 2>&1 | tail -20
      Expected: all tests GREEN; specifically:
        SettlementManagerTest: 13/13
        SettlementAttestationTest: 12/12 (including testUnattestedNonPythDefers)
        SettlementDisputeTest: 5/5
        FfmSettlementTest: 4/4
        AdapterMapTest: 4/4 (new from Plan 01)

    Step 2 — Run relayer oracle-attestation test:
      cd apps/relayer
      pnpm --filter @call-it/relayer test --run oracle-attestation
      Expected: 12/12 passing

    Step 3 — Redeploy subgraph to Studio:
      cd packages/subgraph
      pnpm run build
      pnpm run deploy:sepolia
      (or: graph deploy --studio call-it-sepolia --version-label v0.5.0)
      Wait 5-30 minutes for the subgraph to sync to the latest block.

    Step 4 — Update relayer env:
      In apps/relayer/.env (and .env.production if used):
        SETTLEMENT_MANAGER_ARBITRUM_SEPOLIA=[new SM address]
        CALL_REGISTRY_ARBITRUM_SEPOLIA=[new CR address]
      Restart the relayer worker process (fly deploy or equivalent).

    Step 5 — Live Sepolia smoke:
      # adapterMap assertions (use the new SM address):
      cast call $NEW_SM "adapterMap(uint8,uint8)(uint8)" 2 6 --rpc-url $ARBITRUM_SEPOLIA_RPC_URL
      # Expected: 4 (Snapshot)

      cast call $NEW_SM "adapterMap(uint8,uint8)(uint8)" 2 7 --rpc-url $ARBITRUM_SEPOLIA_RPC_URL
      # Expected: 5 (Tally)

      cast call $NEW_SM "adapterMap(uint8,uint8)(uint8)" 2 1 --rpc-url $ARBITRUM_SEPOLIA_RPC_URL
      # Expected: 2 (DefiLlama), NOT 0

      cast call $NEW_SM "adapterMap(uint8,uint8)(uint8)" 0 0 --rpc-url $ARBITRUM_SEPOLIA_RPC_URL
      # Expected: 0 (Pyth) — Pyth spine UNAFFECTED

      # Governance dual-oracle assertion:
      cast call $NEW_SM "adapterMap(uint8,uint8)(uint8)" 2 6 --rpc-url $ARBITRUM_SEPOLIA_RPC_URL
      # Must be 4 (Snapshot), NOT equal to:
      cast call $NEW_SM "adapterMap(uint8,uint8)(uint8)" 2 7 --rpc-url $ARBITRUM_SEPOLIA_RPC_URL
      # Must be 5 (Tally) — two different values, proving SETTLE-DUAL-GOV

      # Attestation signer check:
      cast call $NEW_SM "attestationSigner(uint8)(address)" 4 --rpc-url $ARBITRUM_SEPOLIA_RPC_URL
      # Must return KMS_ADDRESS_SNAPSHOT_TALLY (non-zero)

      # Security check — non-Pyth attestation without configured adapter reverts:
      # (This is proven by AdapterMapTest.testNonPythAttestationRejectedWithoutAdapterMap in Foundry)
      # Confirm via forge test --match-test testNonPythAttestationRejectedWithoutAdapterMap

    Step 6 — Safety invariant check:
      grep -r "CallIt-SnapshotTally\|CallIt-DefiLlama\|42161n" apps/relayer/src/workers/oracle-adapters/
      # Must return empty — no hardcoded wrong domains or chainIds

    Step 7 — Phase 05.1 security pass:
      Confirm threats_open = 0 (all STRIDE threats mitigated per threat models in plans 01-05).
      Confirm: "every non-Pyth failure path reverts or DEFERs" — verified by:
        - AdapterMapTest.testNonPythAttestationRejectedWithoutAdapterMap (Foundry)
        - criteria-store.test.ts ambiguous return (Vitest)
        - oracle-attestation.test.ts 12/12 (Vitest)
  </how-to-verify>
  <resume-signal>Type "verified" with a summary of the cast call outputs and forge test results, or describe any failures</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| addresses.ts → relayer env | Shared constants must match the live relayer env vars; stale old address in env = transactions to inert contract |
| subgraph.yaml → The Graph Studio | New datasource addresses must match deployed contracts exactly; wrong startBlock misses events |
| forge test suite → in-memory fork | Tests run against a local deployed copy; must also pass against the live Sepolia fork for full confidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-05-01 | Tampering | Partial address update (addresses.ts updated but relayer env not updated) | mitigate | Checkpoint Step 4 explicitly requires relayer env update and restart; Plan 05 REQUIRED NEXT STEPS block in deploy script output also lists this |
| T-05.1-05-02 | Denial of Service | Subgraph reindex lag after redeploy (Pitfall 7 from RESEARCH.md) | accept | Subgraph reindex takes 5-30 min; polled-events fallback from Phase 0 stays live during gap; checkpoint asks to wait for sync before end-to-end test; documented in pitfalls |
| T-05.1-05-03 | Tampering | Wrong startBlock in subgraph.yaml misses initial SetAdapterMap events | accept | setAdapterMap events are configuration events, not indexer data events; the subgraph does not currently index setAdapterMap; startBlock only determines when to start indexing CallCreated/CallSettled etc. from the new CR/SM addresses; using the deploy block is correct |
| T-05.1-05-04 | Spoofing | Pyth spine disrupted by adapterMap changes | mitigate | Checkpoint Step 5 explicitly verifies adapterMap(0,0) returns 0 (Pyth unaffected); AdapterMapTest.testPythPathUnaffected in Foundry also covers this |
| T-05.1-05-05 | Elevation of Privilege | Old SM address accepting calls after redeploy (reactivation window) | accept | Old SM is non-upgradeable and has no code change; it still accepts settle() calls for old callIds; but new callIds are on new CR/CR; no privilege escalation path because old SM has no new funds or authority |
</threat_model>

<verification>
forge test exits 0 (38 tests across 5 test contracts including AdapterMapTest)
pnpm --filter @call-it/relayer test --run oracle-attestation shows 12/12
pnpm --filter @call-it/relayer test --run criteria-store shows 5/5
grep "CALL_REGISTRY_ARBITRUM_SEPOLIA" packages/shared/src/constants/addresses.ts shows new address (not 0x7DAd)
cast call $NEW_SM "adapterMap(uint8,uint8)(uint8)" 2 6 returns 4
cast call $NEW_SM "adapterMap(uint8,uint8)(uint8)" 2 7 returns 5
cast call $NEW_SM "adapterMap(uint8,uint8)(uint8)" 0 0 returns 0 (Pyth unaffected)
grep -r "CallIt-SnapshotTally" apps/relayer/src/workers/oracle-adapters/ returns empty
grep -r "42161n" apps/relayer/src/workers/oracle-adapters/ returns empty
</verification>

<success_criteria>
addresses.ts updated. Subgraph redeployed to Studio. Relayer env updated and relayer restarted. All 38+ Foundry tests GREEN. oracle-attestation.test.ts 12/12. criteria-store.test.ts 5/5. Live Sepolia cast calls confirm: adapterMap(2,6)=4 (Snapshot), adapterMap(2,7)=5 (Tally), adapterMap(0,0)=0 (Pyth unaffected). threats_open = 0.
</success_criteria>

<output>
After completion, create .planning/phases/05.1-non-pyth-oracle-rail-activation/05.1-05-SUMMARY.md
Include: new contract addresses, cast call outputs, test counts, subgraph version label.
</output>
