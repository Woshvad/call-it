---
phase: 05.1-non-pyth-oracle-rail-activation
plan: 05
subsystem: shared-subgraph
tags: [rollup, addresses, subgraph, studio, forge-test, redeploy, live]

# Dependency graph
requires:
  - phase: 05.1-non-pyth-oracle-rail-activation
    provides: deployed cluster addresses from Plan 02
provides:
  - addresses.ts + subgraph.yaml repointed to the new cluster; subgraph redeployed to Studio
  - Confirmation the cluster change did not regress the contract suite (forge 185/185)
affects: [frontend, relayer, subgraph-indexer, phase-06-soak]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Redeploy rollup: update packages/shared addresses.ts (4 addrs) + subgraph.yaml (4 datasources, address+startBlock) -> rebuild subgraph -> graph deploy --deploy-key to Studio with a version label."

key-files:
  created: []
  modified:
    - packages/shared/src/constants/addresses.ts
    - packages/subgraph/subgraph.yaml
    - packages/subgraph/abis/SettlementManager.json

key-decisions:
  - "Rolled all 4 cluster addresses (CR/FFM/CE/SM), not just CR/SM, since the whole cluster was redeployed."
  - "Relayer env / GCP Secret Manager updates + go-live deferred to Phase 6 (relayer is pending and operates the rail during the soak)."

patterns-established:
  - "Subgraph Studio redeploy via `graph deploy --node https://api.studio.thegraph.com/deploy/ --deploy-key <key> call-it-sepolia --version-label <label>`."

requirements-completed: [SETTLE-06, SETTLE-DUAL-GOV, SETTLE-13, SETTLE-14, SETTLE-15, SETTLE-16, SETTLE-18, SETTLE-19, SETTLE-20, SETTLE-21, SETTLE-22, SETTLE-23, SETTLE-24]

# Metrics
duration: live-operator-session
completed: 2026-06-02
---

# Phase 05.1 Plan 05: Redeploy Rollup

**The new cluster is wired into the app's source of truth and the indexer.** `addresses.ts` (4 addresses) and `subgraph.yaml` (4 datasources + startBlocks) point at the deployed cluster; the subgraph was redeployed to Studio.

## Done
- `packages/shared/src/constants/addresses.ts` -> CR/FFM/CE/SM (+ supersede notes)
- `packages/subgraph/subgraph.yaml` -> 4 datasource addresses + startBlocks; ABI synced
- Subgraph redeployed to Studio: `call-it-sepolia`, version `phase05-1-cluster`
- `forge test`: 185 passed (the 1 failure is the pre-existing `ARB_ONE_RPC_URL` fork test, env-blocked)
- shared tsc clean; relayer tsc clean except the 1 pre-existing stylus-deactivation error; subgraph build OK

## Carried to Phase 6
- Relayer config (fly secrets `KMS_ADDRESS_*` + `GCP_LOCATION_ID`; GCP Secret Manager `NEXT_PUBLIC_CALL_REGISTRY_ADDRESS`; SA IAM) + relayer deploy
- Frontend `NEXT_PUBLIC_SUBGRAPH_URL` -> synced Studio version + redeploy

---
*Phase: 05.1 · Plan 05 · Completed 2026-06-02*
