---
phase: 05.1
slug: non-pyth-oracle-rail-activation
status: passed
verified: 2026-06-02
method: on-chain cast verification + forge test (interactive live cutover; gsd-verifier agent intentionally skipped — on-chain evidence IS the verification)
---

# Phase 05.1 Verification — Non-Pyth Oracle Rail Activation

**Status: PASSED.** The non-Pyth oracle rail is deployed + verified on Arbitrum Sepolia. The relayer go-live (which *operates* the rail) is a documented Phase-6 carry-over — same pattern as Phase 5's deferred live-ops items.

## Success Criteria (6/6)

| # | Criterion | Verdict | Evidence |
|---|-----------|---------|----------|
| 1 | Schema change: distinct adapterMap keys for Snapshot(4) vs Tally(5) governance; oracleType↔adapterMap binding preserved | ✅ | `EventSubtype.Governance_Snapshot=6` / `Governance_Tally=7` (Plan 01, AdapterMapTest 4/4); on-chain `adapterMap(2,6)=4`, `(2,7)=5` |
| 2 | Deploy config: `setAdapterMap` for every pair; non-zero adapters; gov calls route distinctly | ✅ | DeployPhase5_1 broadcast; `cast adapterMap` returns 2/4/5 for non-Pyth, 0 for Pyth |
| 3 | Relayer rewire: 6 adapters + watcher on unified `oracle-attestation.ts`, real chainId + 19-field decode + on-chain adapterMap dispatch; legacy `CallIt-*`/`42161` removed | ✅ | Plan 03 (oracle-attestation 12/12; legacy-domain/`42161n` greps empty); CEX `submitAttestation` bug fixed |
| 4 | Off-chain criteria retrieval; per-market targetValue verified; mismatch DEFERs | ✅ | Plan 04 `call_oracle_criteria` live; `resolveCriteria` null→ambiguous→DEFER; targetValue pass-through (no 0n default); criteria-store 5/5 |
| 5 | Redeploy rollup: addresses.ts, subgraph redeploy, Phase-4 suites re-pass | ✅ | Plan 05: addresses.ts (4) + subgraph.yaml (4 datasources) repointed; subgraph redeployed to Studio (`phase05-1-cluster`); `forge test` 185/185 |
| 6 | Safety: every non-Pyth gap reverts/DEFERs; threats_open:0; Pyth spine unaffected | ✅ | `adapterMap(0,0)=0` (Pyth unchanged); fail-safe DEFER throughout; cluster wiring consistent (see below) |

## On-chain cluster consistency (the Plan-02 fix, verified)
`CR.followFadeMarket→FFM`, `FFM.callRegistry→CR`, `CE.callRegistry→CR`, `CE.followFadeMarket→FFM`, `SM.{callRegistry,followFadeMarket,challengeEscrow}→CR/FFM/CE`, `CR.settlementManager→SM`, all `attestationSigner(1-6)` = derived KMS addresses.

## Deployed (canonical)
CR `0x9E3E467e5D1F1266354444CEaC67651c7e9CACEc` · FFM `0x5Aa7bC9ee202AD9197CB109e7EcF3d7d99C72a48` · CE `0xf0D65BFd5dFa4e40c81d198DD7ED78423a26Fdea` · SM `0x765f6ecd85059CF8eF59286DF578AEC0B13230fC`.

## Carried to Phase 6 (not a gap — documented dependency)
Relayer go-live + criteria backfill + frontend subgraph-URL/redeploy. Full checklist in 05.1-OPERATOR-HANDOFF.md.
