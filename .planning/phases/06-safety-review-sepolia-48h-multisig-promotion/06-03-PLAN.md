---
phase: 06-safety-review-sepolia-48h-multisig-promotion
plan: 03
type: execute
wave: 3
depends_on:
  - 06-02-PLAN.md
files_modified:
  - packages/contracts/test/SettlementManagerForkTest.sol
  - packages/contracts/test/TvlAggregation.t.sol
  - packages/contracts/test/CallRegistrySafety.t.sol
  - packages/contracts/test/SettlementSafetyMatrix.t.sol
autonomous: true
requirements:
  - SAFETY-29
  - SAFETY-30
  - SAFETY-31
  - SAFETY-32
  - SAFETY-33
  - SAFETY-34
  - SAFETY-35
  - SAFETY-36
  - SAFETY-37
  - SAFETY-38
  - SAFETY-39
  - SAFETY-40
  - SAFETY-41
  - SAFETY-43

must_haves:
  truths:
    - "forge test --match-contract SettlementSafetyMatrix exits 0 with all SAFETY-34/35/36/37/38/39/40 items covered"
    - "TvlAggregation.t.sol test_tvlBoundary_includesChallengeEscrow passes: $5,001 across CR+FFM+CE reverts TvlCapReached"
    - "CallRegistrySafety.t.sol extends to cover FFM/CE/SM pause guards and all SAFETY-43 owner-only guards"
    - "SettlementManagerForkTest.sol full loop tests pass with deterministic vm.mockCall Pyth path"
    - "forge test --profile ci exits 0 on all safety matrix files"
  artifacts:
    - path: "packages/contracts/test/SettlementSafetyMatrix.t.sol"
      provides: "SAFETY-34/35/36/37/38/39/40 unit tests (pause carve-out, decay, cooldown, UTC dup, slippage, idempotency, self-challenge)"
    - path: "packages/contracts/test/TvlAggregation.t.sol"
      provides: "SAFETY-31 ChallengeEscrow TVL boundary test extension"
    - path: "packages/contracts/test/CallRegistrySafety.t.sol"
      provides: "SAFETY-30/43 extensions for FFM/CE/SM pause + all owner-only guards"
    - path: "packages/contracts/test/SettlementManagerForkTest.sol"
      provides: "Full loop fork tests (create→follow/fade→settle→exit→dispute) with deterministic Pyth mock"
  key_links:
    - from: "packages/contracts/test/SettlementSafetyMatrix.t.sol"
      to: "packages/contracts/src/SettlementManager.sol"
      via: "sm.settle(callId, ...)"
      pattern: "sm\\.settle\\|ffm\\.exitPosition\\|ffm\\.follow"
    - from: "packages/contracts/test/TvlAggregation.t.sol"
      to: "packages/contracts/src/ChallengeEscrow.sol"
      via: "proposeChallenge + acceptChallenge to seed CE balance"
      pattern: "ce\\.proposeChallenge\\|ce\\.acceptChallenge"
---

**Decisions covered (CONTEXT.md):** D-01: hybrid money-path — the Arbitrum One mainnet-fork money-path suite (option c) extending `SettlementManagerForkTest.sol`, run in CI alongside the SAFETY-29–43 matrix.

<objective>
Extend the existing SAFETY test suite with all SAFETY-29–43 matrix items (minus SAFETY-42, which is in Plan 04 as the live destruction drill). This covers every boundary test, pause carve-out, owner-only guard, idempotency, decay/cooldown math, UTC dup edge, slippage, reentrancy, and self-challenge gate — plus extending the mainnet-fork suite with a deterministic full-loop path. All tests run in CI with no live-network dependency.

Purpose: These tests prove the contract invariants hold before the 48h soak starts, and form the CI gate that must pass before mainnet promotion. Extending existing files (rather than greenfield) keeps coverage connected to existing patterns.

Output: 4 modified/new Solidity test files; all forge tests green on the safety matrix.
</objective>

<execution_context>
@C:\Users\woshv\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\woshv\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-safety-review-sepolia-48h-multisig-promotion/06-CONTEXT.md
@.planning/phases/06-safety-review-sepolia-48h-multisig-promotion/06-PATTERNS.md

<interfaces>
<!-- Key test patterns extracted from codebase. Executor reads these files before editing. -->

From packages/contracts/test/CallRegistrySafety.t.sol (existing owner-guard template):
  function test_only_owner_pause() public {
      vm.prank(alice);
      vm.expectRevert(abi.encodeWithSignature("OwnableUnauthorizedAccount(address)", alice));
      registry.pause();
  }

From packages/contracts/test/TvlAggregation.t.sol (existing boundary pattern):
  function test_tvlBoundary5001Reverts() public {
      uint256 callId = _seedPool(alice, 100e6);
      uint256 currentCombined = registry.currentTvl() + ffm.getTvl();
      vm.prank(owner);
      registry.setTvlCap(currentCombined);
      vm.prank(bob);
      vm.expectRevert(); // TvlCapReached
      ffm.follow(callId, 1e6, 0);
  }

From packages/contracts/test/SettlementManagerForkTest.sol (fork setUp):
  function setUp() public {
      vm.createSelectFork(vm.envString("ARB_ONE_RPC_URL"));
      // fund alice from USDC_WHALE = 0x489ee077994B6658eAfA855C308275EAd8097C4A
  }
  // Existing try/catch settle() — Phase 6 adds vm.mockCall path for deterministic completion.

From packages/contracts/test/mocks/ (MockUSDC for unit tests):
  // MockUSDC is already used in CallRegistrySafety.t.sol — reuse for SettlementSafetyMatrix
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create SettlementSafetyMatrix.t.sol and extend TvlAggregation + CallRegistrySafety</name>
  <files>
    packages/contracts/test/SettlementSafetyMatrix.t.sol,
    packages/contracts/test/TvlAggregation.t.sol,
    packages/contracts/test/CallRegistrySafety.t.sol
  </files>
  <read_first>
    packages/contracts/test/CallRegistrySafety.t.sol (FULL — owner-guard template, pause pattern, MaliciousReentrantUSDC setup, existing test names to avoid collision)
    packages/contracts/test/TvlAggregation.t.sol (FULL — _seedPool helper, test_tvlBoundary5001Reverts, existing ChallengeEscrow wiring or lack thereof)
    packages/contracts/test/helpers/ (list helpers used — FfmTestHelper or similar base contract)
    packages/contracts/test/mocks/ (list MockUSDC + any mock Pyth)
    packages/contracts/src/ChallengeEscrow.sol (proposeChallenge + acceptChallenge signatures for TVL seed)
    packages/contracts/src/FollowFadeMarket.sol (exitPosition, follow, fade signatures for carve-out tests)
    packages/contracts/src/SettlementManager.sol (settle, raiseDispute, resolveDispute, forceSettle signatures; pause carve-out logic)
    .planning/phases/06-safety-review-sepolia-48h-multisig-promotion/06-PATTERNS.md (SettlementSafetyMatrix section lines 336-428, TvlAggregation section lines 430-462, CallRegistrySafety section lines 464-490)
    .planning/phases/06-safety-review-sepolia-48h-multisig-promotion/06-CONTEXT.md (boundary values: $4,999/$5,001, $100/$101, $1/$0.99, 24h lock, 4h cooldown)
  </read_first>
  <behavior>
    SettlementSafetyMatrix.t.sol new tests:
    - SAFETY-34: test_withdrawWhilePaused_exitPosition_succeeds — seed follow position, pause SM/FFM, exitPosition still succeeds
    - SAFETY-34: test_claimPayoutWhilePaused_succeeds — seed settled call, pause SM, claimPayout does NOT revert
    - SAFETY-35: test_callerExit_before24h_reverts — caller cannot exit before 24h lock (CallerTooEarly or equivalent)
    - SAFETY-35: test_callerExit_after24h_succeeds — caller CAN exit at 24h+1 second (vm.warp)
    - SAFETY-35: test_callerExit_penalty_at50pct — penalty at time=24h = 50% (floor math: 15 + 35 * timeRemainingRatio); assert USDC refund equals expected amount
    - SAFETY-35: test_callerExit_penalty_at15pct_floor — penalty at time=expiry = 15% floor; assert refund
    - SAFETY-36: test_followerExit_before4h_reverts — follower cannot exit before 4h cooldown (CooldownNotMet or equivalent)
    - SAFETY-36: test_followerExit_after4h_90pctReturn — follower receives 90% of stake after 4h cooldown
    - SAFETY-37: test_duplicateHash_utcDayBoundary — two calls with same hash but different UTC days: second call after settlement of first succeeds
    - SAFETY-37: test_duplicateHash_sameDayReverts — two calls with same hash on same UTC day: second reverts DuplicateCall
    - SAFETY-38: test_slippage_minSharesOut_reverts — follow(callId, amount, minSharesOut=large) reverts SlippageExceeded when AMM moves between quote and execution (vm.prank different user to move AMM first)
    - SAFETY-39: test_settle_idempotency — settle a call, then settle again; second settle reverts (AlreadySettled or equivalent), does NOT corrupt state
    - SAFETY-40: test_selfChallenge_reverts — caller challenges their own call; reverts SelfChallenge
    - SAFETY-41: test_reentrancy_maliciousUSDC — extend from existing MaliciousReentrantUSDC pattern; the reentrancy guard fires

    TvlAggregation.t.sol extension:
    - SAFETY-31: test_tvlBoundary_includesChallengeEscrow — seed CR+FFM to $4,800 combined; then seed ChallengeEscrow to $200 via proposeChallenge+acceptChallenge; total = $5,000; next $1 follow reverts TvlCapReached
    - SAFETY-32: test_maxStake_100_succeeds — createCall with stake=100e6 succeeds
    - SAFETY-32: test_maxStake_101_reverts — createCall with stake=101e6 reverts StakeAboveMaximum
    - SAFETY-33: test_minPosition_1_succeeds — follow(callId, 1e6, 0) succeeds
    - SAFETY-33: test_minPosition_99cents_reverts — follow(callId, 990000, 0) reverts PositionBelowMinimum

    CallRegistrySafety.t.sol extension (SAFETY-30 + SAFETY-43):
    - test_only_owner_pause_FFM — non-owner cannot pause FollowFadeMarket
    - test_only_owner_pause_CE — non-owner cannot pause ChallengeEscrow
    - test_only_owner_pause_SM — non-owner cannot pause SettlementManager
    - test_only_owner_setTvlCap_CR — non-owner cannot setTvlCap on CR
    - test_only_owner_setTvlCap_FFM — non-owner cannot setTvlCap on FFM
    - test_only_owner_setRelayer_PR — non-owner cannot setRelayer on ProfileRegistry
    - test_only_owner_setSettlementManager_PR — non-owner cannot setSettlementManager on PR
    - test_only_owner_forceSettle — non-owner cannot forceSettle (after 7d)
    - test_only_owner_resolveDispute — non-owner cannot resolveDispute
  </behavior>
  <action>
    Create packages/contracts/test/SettlementSafetyMatrix.t.sol:
    - Use the exact header pattern from CallRegistrySafety.t.sol (pragma =0.8.30, EXACT pin comment, forge-std imports)
    - Use MockUSDC etched at USDC_ARB_NATIVE address (same pattern as CallRegistrySafety.t.sol setUp)
    - If a MockPyth is available in test/mocks, use it; otherwise vm.mockCall on PYTH_ARB_ONE for settle() calls
    - Import all 5 contracts + interfaces from src/
    - The SettlementSafetyMatrixTest contract deploys a full stack in setUp() — follow the FfmTestHelper or existing stack pattern from CallRegistrySafety.t.sol
    - Each test function follows the pattern: arrange (seed state), act (call function), assert (expectRevert or assertEq on USDC balances/state)
    - For SAFETY-35 decay tests: use the formula from FollowFadeMarket.sol — `15% + 35% × timeRemainingRatio` where timeRemainingRatio = (expiry - now) / (expiry - createdAt). vm.warp to appropriate timestamps.
    - For SAFETY-38 slippage test: two transactions in sequence — (1) vm.prank(carol) follow(callId, 50e6, 0) to move the AMM, (2) vm.prank(alice) follow(callId, 10e6, MIN_SHARES=very large) vm.expectRevert SlippageExceeded

    Extend TvlAggregation.t.sol:
    - Read the existing test carefully to understand how ChallengeEscrow is already set up (or not). If CE is not yet in setUp, add it following the FfmTestHelper or existing stack pattern.
    - For test_tvlBoundary_includesChallengeEscrow: seed CR+FFM to just under the cap via _seedPool; then call ce.proposeChallenge(callId, stakeAmt) from alice and ce.acceptChallenge(challengeId) from the caller to lock $200 in CE; then attempt ffm.follow(callId, 1e6, 0) and expect TvlCapReached.
    - Add test_maxStake and test_minPosition tests using registry.createCall() and ffm.follow() with boundary amounts (see values in CONTEXT.md).

    CONDITIONAL — TVL CE aggregation fix path (Pitfall 3 / SAFETY-31):
    If test_tvlBoundary_includesChallengeEscrow reveals that ChallengeEscrow USDC balance is NOT included in the TVL aggregate (i.e., the test fails because CE funds are invisible to the cap), do NOT mark SAFETY-31 done on a red test. Instead:
    (a) Leave the test in place as a failing red test that documents the gap.
    (b) Investigate whether the TVL aggregation function in SettlementManager or CR reads CE.currentTvl() — if CE has no getTvl() or currentTvl() hook, one must be added to ChallengeEscrow.sol and the aggregation function updated.
    (c) Add a follow-up task within this plan wave (or flag to the operator) to extend the TVL aggregation at the contract level before SAFETY-31 can be closed. The test must pass green before the plan's done criterion is met.

    Extend CallRegistrySafety.t.sol:
    - Add FFM/CE/SM pause guard tests following the exact template from existing test_only_owner_pause (vm.prank(alice), vm.expectRevert with OwnableUnauthorizedAccount, call .pause()).
    - Add the owner-only guard tests for forceSettle, resolveDispute, setTvlCap (FFM), setRelayer (PR), setSettlementManager (PR). Each follows the same vm.prank(non-owner) + vm.expectRevert template.
  </action>
  <verify>
    <automated>cd packages/contracts && forge test --match-contract "SettlementSafetyMatrix|TvlAggregation|CallRegistrySafety" -vvv 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - forge test --match-contract "SettlementSafetyMatrix|TvlAggregation|CallRegistrySafety" exits 0
    - SettlementSafetyMatrix tests count >= 13 (all SAFETY-34/35/36/37/38/39/40/41 behaviors covered)
    - TvlAggregation test_tvlBoundary_includesChallengeEscrow PASSES (green) — a red result here means CE TVL aggregation is broken and requires a contract-level fix before SAFETY-31 can be closed; do not mark done if this test is red
    - CallRegistrySafety tests include at least 8 new tests for FFM/CE/SM pause + owner guards (SAFETY-30/43 extended)
    - forge test (full suite) exits 0 — no regressions in existing tests
    - grep for "SAFETY-31" comment in TvlAggregation.t.sol returns >= 1 (traceability)
    - grep for "SAFETY-34" comment in SettlementSafetyMatrix.t.sol returns >= 1
  </acceptance_criteria>
  <done>All SAFETY-29–41 and SAFETY-43 matrix items have a corresponding Foundry test that passes (green); test_tvlBoundary_includesChallengeEscrow is green confirming CE is in the TVL aggregate; forge test suite exits 0</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend SettlementManagerForkTest with deterministic full-loop tests (create→follow→settle→exit→dispute)</name>
  <files>
    packages/contracts/test/SettlementManagerForkTest.sol
  </files>
  <read_first>
    packages/contracts/test/SettlementManagerForkTest.sol (FULL — existing setUp, existing try/catch test, USDC_WHALE address, existing test names)
    packages/contracts/src/SettlementManager.sol (settle() signature with pythUpdateData bytes[])
    packages/contracts/src/FollowFadeMarket.sol (claimPayout, exitPosition signatures)
    packages/contracts/src/ChallengeEscrow.sol (proposeChallenge, acceptChallenge, claimDuelPayout signatures)
    .planning/phases/06-safety-review-sepolia-48h-multisig-promotion/06-PATTERNS.md (SettlementManagerForkTest section lines 287-334)
    .planning/phases/06-safety-review-sepolia-48h-multisig-promotion/06-RESEARCH.md (Pattern 3: Mainnet-Fork Money-Path Suite Extension)
  </read_first>
  <behavior>
    New test methods in SettlementManagerForkTest.sol:
    - test_fullLoop_createFollowSettleClaimPayout: create call (real USDC from whale) → follow(alice) → vm.mockCall Pyth → settle() → claimPayout(alice); assert alice.balance > original (winner receives USDC); assert treasury.balance increased by 1.7% fee
    - test_fullLoop_createFadeSettleClaimPayout: same with fade side winning (outcome = False/Below)
    - test_fullLoop_callerExit: create → follow(alice) → vm.warp(24h+1s) → callerExit(caller) → assert penalty deducted; assert alice can still exitPosition after
    - test_fullLoop_duelSettleClaimPayout: create → proposeChallenge(alice, stake) → acceptChallenge(caller) → vm.mockCall Pyth → settle() → claimDuelPayout(winner); assert USDC received
    - test_fullLoop_disputeOwnerResolve: create → follow → settle → raiseDispute(alice, bond) → resolveDispute(owner, oppositeOutcome) → claimPayout(alice per new outcome)
  </behavior>
  <action>
    Extend SettlementManagerForkTest.sol — do NOT modify the existing setUp or existing test methods. Add new test methods after the existing ones.

    Deterministic Pyth mock path (per PATTERNS.md Pattern 3 option A):
    Before each settle() call in new test methods, add:
      vm.mockCall(PYTH_ARBITRUM_ONE, abi.encodeWithSelector(IPyth.getPriceNoOlderThan.selector, feedId, 60), abi.encode(PythStructs.Price({price: 3000e6, conf: 100, expo: -6, publishTime: block.timestamp})));
    This bypasses the stale-price revert that the existing try/catch gracefully handles. With the mock in place, settle() completes deterministically and the new tests can assert downstream state (fees, payouts, rep).

    For USDC funding in new tests: reuse the existing whale impersonation pattern from setUp — vm.prank(USDC_WHALE) + IERC20(USDC_ARB_NATIVE).transfer(testAddr, amount).

    For the dispute test: raiseDispute requires a $5 USDC bond from alice. Fund alice with 5e6 extra. Then resolveDispute is called by owner (vm.prank(owner)) with the opposite outcome. Verify: the old winner can no longer claim, the new winner can.

    All new test methods use the fork network (no additional vm.createSelectFork needed — it is set in setUp).
    Add an env guard at the top of each new test: if (vm.envOr("ARB_ONE_RPC_URL", bytes32(0)) == bytes32(0)) { vm.skip(true); } — so the fork tests are skipped gracefully in CI environments without the RPC variable, but run when it is set. (The existing tests may already have this guard — follow the same pattern.)
  </action>
  <verify>
    <automated>cd packages/contracts && ARB_ONE_RPC_URL="" forge test --match-contract SettlementManagerFork -vv 2>&1 | grep -E "PASS|SKIP|FAIL" | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - forge test --match-contract SettlementManagerFork exits 0 in both ARB_ONE_RPC_URL="" (skip mode) and with a real RPC (full run mode)
    - grep -c "test_fullLoop_" packages/contracts/test/SettlementManagerForkTest.sol returns >= 5 (all five full-loop tests present)
    - Each new test includes a vm.mockCall on IPyth.getPriceNoOlderThan — grep -c "getPriceNoOlderThan" returns >= 5
    - Existing test methods in the file are unchanged — the file has no modifications to setUp() or existing test lines
    - forge test (full suite) exits 0 — no regressions
  </acceptance_criteria>
  <done>SettlementManagerForkTest has 5 deterministic full-loop tests covering the complete create→follow/fade→settle→exit/dispute/claim lifecycle with real USDC</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test mocks → production contracts | vm.mockCall bypasses real Pyth in CI — tests prove contract logic, not Pyth availability |
| Unit tests → live behavior | Foundry unit tests run on local EVM — live Sepolia behavior is the Wave 4 soak's job |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Tampering | SAFETY-34 pause carve-out test | mitigate | Test explicitly verifies exitPosition and claimPayout succeed while SM is paused — the carve-out is wired via pauseExcluded modifier per spec §10.3 |
| T-06-03-02 | Tampering | SAFETY-41 reentrancy — USDC mock callback | mitigate | MaliciousReentrantUSDC (from CallRegistrySafety.t.sol) attempts reentrant call during transfer; nonReentrant modifier fires ReentrancyGuardReentrantCall — test asserts this revert |
| T-06-03-03 | Tampering | SAFETY-31 CE TVL exclusion | mitigate | test_tvlBoundary_includesChallengeEscrow will FAIL if ChallengeEscrow is not included in TVL aggregate — exposing Pitfall 3 if it's still unresolved; conditional fix path documented in action |
| T-06-03-04 | Spoofing | vm.mockCall Pyth in fork tests | accept | Mock is test-only; no production path uses vm.mockCall. Fork tests with real RPC + real VAA are the deeper validation (run in CI with ARB_ONE_RPC_URL set) |
| T-06-03-05 | Denial of Service | Fork test ARB_ONE_RPC_URL missing | accept | vm.skip(true) guard prevents CI failure when RPC is unavailable; the soak (Wave 4) uses live Sepolia for real oracle path validation |
</threat_model>

<verification>
After both tasks complete:
- forge test --match-contract "SettlementSafetyMatrix|TvlAggregation|CallRegistrySafety" exits 0
- forge test --match-contract SettlementManagerFork exits 0 (with mock path)
- forge test (full suite, no fork) exits 0 with no regressions
- grep -c "SAFETY-31" packages/contracts/test/TvlAggregation.t.sol returns >= 1
- grep -c "test_fullLoop_" packages/contracts/test/SettlementManagerForkTest.sol returns >= 5
</verification>

<success_criteria>
- SAFETY-29–41 and SAFETY-43 matrix items all have a corresponding passing Foundry test
- TVL cap boundary verified across all 3 contracts holding funds (Pitfall 3 closed)
- Full USDC money-path loop confirmed in fork tests with deterministic Pyth mock
- All tests green — no regressions in prior safety/interference/gate suites
</success_criteria>

<output>
After completion, create .planning/phases/06-safety-review-sepolia-48h-multisig-promotion/06-03-SUMMARY.md
</output>
