---
phase: 06-safety-review-sepolia-48h-multisig-promotion
plan: 04
type: execute
wave: 3
depends_on:
  - 06-02-PLAN.md
files_modified:
  - packages/contracts/test/RevertingStylusEngineDrill.t.sol
  - apps/relayer/src/scripts/soak-seeder.ts
autonomous: true
requirements:
  - SAFETY-42
  - SAFETY-19

must_haves:
  truths:
    - "RevertingStylusEngineDrill.t.sol test_stylus_fallback_fires_RepCalculatedFallback passes in Foundry (unit drill)"
    - "forge test --match-contract RevertingStylusEngineDrill exits 0 — proves the fallback catch branch fires in a local test environment"
    - "soak-seeder.ts script exists, compiles without TypeScript errors, and has EVIDENCE_LOG_PATH output for SAFETY-21–28 tx hashes"
    - "soak-seeder.ts has evidence log emission for every soak action: callCreated, followed, faded, settled, callerExited, challengeProposed, challengeAccepted, challengeSettled, disputeRaised, disputeResolved, settlementDelayed"
  artifacts:
    - path: "packages/contracts/test/RevertingStylusEngineDrill.t.sol"
      provides: "SAFETY-42 Foundry unit drill — proxy.upgradeAndCall to RevertingStylusEngine, settle, assert RepCalculatedFallback"
    - path: "apps/relayer/src/scripts/soak-seeder.ts"
      provides: "48h soak seeding bot — multi-wallet, Circle faucet rate-limit aware, evidence log emitter"
  key_links:
    - from: "packages/contracts/test/RevertingStylusEngineDrill.t.sol"
      to: "packages/contracts/src/RevertingStylusEngine.sol"
      via: "ProxyAdmin.upgradeAndCall(proxy, REVERTING_ENGINE, '')"
      pattern: "RevertingStylusEngine"
    - from: "apps/relayer/src/scripts/soak-seeder.ts"
      to: "evidence/phase-6-soak/"
      via: "appendFileSync JSONL evidence log"
      pattern: "evidence-\\d+\\.jsonl"
---

**Decisions covered (CONTEXT.md):** D-04: the scripted seeding bot (`soak-seeder.ts`) that drives the bulk on-chain soak counts. D-05: the evidence-log scaffold + Telegram alert wiring, reusing the `stylus-deactivation-watcher.ts` alert path.

<objective>
Build the SAFETY-42 Foundry unit drill (RevertingStylusEngineDrill.t.sol — proves the Solidity baseline catch branch fires under test conditions) and the soak-seeder.ts script (the seeding bot for the 48h live soak). These are parallel to Plan 03's safety matrix work and both depend on Plan 02's redeployed cluster addresses.

Purpose: The destruction drill must be proven in a unit test first (Plan 04) and on live Sepolia during the soak (Plan 05 checkpoint). The soak-seeder is the automated driver that produces the evidence log required by D-05 — without it the Wave 4 soak cannot satisfy SAFETY-21–28.

Output: RevertingStylusEngineDrill.t.sol (unit drill), soak-seeder.ts (bot + evidence log), evidence/ directory scaffold.
</objective>

<execution_context>
@C:\Users\woshv\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\woshv\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-safety-review-sepolia-48h-multisig-promotion/06-CONTEXT.md
@.planning/phases/06-safety-review-sepolia-48h-multisig-promotion/06-PATTERNS.md

<interfaces>
<!-- Key interfaces and patterns the executor needs. -->

From packages/contracts/src/RevertingStylusEngine.sol (already built, Phase 5):
  // Intentionally reverts on compute_rep_change — triggers the fallback catch in SM.settle() step 7
  // No storage, no state — pure revert fixture.

From packages/contracts/script/CutoffFallback.s.sol (canonical ProxyAdmin.upgradeAndCall pattern — OZ 5.x 3-arg API):
  ProxyAdmin(PROXY_ADMIN_ADDR).upgradeAndCall(
      ITransparentUpgradeableProxy(payable(PROXY_ADDR)),
      SOLIDITY_BASELINE_ADDR, ""
  );
  // This is the ONLY ProxyAdmin upgrade API in OZ 5.x.
  // The 2-arg upgrade(address,address) was removed in OZ 5.0.
  // EIP-1967 admin slot = 0xb53127684a568b3173ae13b9f8a6016e243e63b6e8ee1178d6a717850b5d6103

From packages/contracts/test/SettlementManagerForkTest.sol (setUp pattern for drill test):
  // Fork setup with vm.deal for ETH, USDC_WHALE impersonation, full stack deploy.
  // RevertingStylusEngineDrill extends this pattern but uses vm.prank(proxyAdminOwner)
  // for the ProxyAdmin.upgradeAndCall call.

From apps/relayer/src/scripts/backfill-criteria.ts (script bootstrap pattern):
  loadEnvIfNeeded();
  async function main(): Promise<void> {
    let inserted = 0, errors = 0;
    for (const item of ITEMS) { try { ... inserted++; } catch { errors++; } }
    if (errors > 0) process.exit(1);
    process.exit(0);
  }
  main().catch((err) => { console.error('fatal:', err); process.exit(1); });

From apps/relayer/src/workers/alerts.ts (sendAlertSafe for non-crashing loops):
  import { sendAlertSafe } from '../workers/alerts.js';
  await sendAlertSafe('settle_stuck_25m', { callId, elapsed });

EvidenceEntry interface for soak-seeder.ts:
  interface EvidenceEntry {
    action: 'callCreated'|'followed'|'faded'|'settled'|'callerExited'
           |'challengeProposed'|'challengeAccepted'|'challengeSettled'
           |'disputeRaised'|'disputeResolved'|'settlementDelayed';
    txHash: `0x${string}`;
    callId?: number;
    walletIndex?: number;
    timestamp: number;
    block?: number;
  }
  // Write to evidence/phase-6-soak/evidence-${Date.now()}.jsonl (append-only JSONL)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create RevertingStylusEngineDrill.t.sol — SAFETY-42 unit drill</name>
  <files>
    packages/contracts/test/RevertingStylusEngineDrill.t.sol
  </files>
  <read_first>
    packages/contracts/src/RevertingStylusEngine.sol (FULL — confirm it is a pure-revert fixture with no storage)
    packages/contracts/test/SettlementManagerForkTest.sol (setUp pattern lines 67-144 — fork setup to adapt)
    packages/contracts/script/CutoffFallback.s.sol (FULL — ProxyAdmin.upgradeAndCall 3-arg pattern + EIP-1967 slot constant — canonical API anchor)
    packages/contracts/script/DeployPhase5Stylus.s.sol (lines 128-138 — how to read EIP-1967 admin slot)
    packages/contracts/src/StatelessTransparentProxy.sol (NatDoc about auto-created ProxyAdmin — lines 23-25 per PATTERNS.md)
    .planning/phases/06-safety-review-sepolia-48h-multisig-promotion/06-PATTERNS.md (RevertingStylusEngineDrill section lines 492-534)
    .planning/phases/05-stylusscoreengine-48h-cutoff/05.1-OPERATOR-HANDOFF.md (Stylus proxy address and ProxyAdmin address)
  </read_first>
  <behavior>
    - test_stylus_fallback_fires_RepCalculatedFallback:
      1. setUp deploys full stack on fork (or anvil with MockUSDC); gets ProxyAdmin address from EIP-1967 admin slot
      2. vm.prank(proxyAdminOwner) + ProxyAdmin.upgradeAndCall(proxy, RevertingStylusEngine, "") upgrades to revert fixture
      3. Verify: EIP-1967 impl slot reads RevertingStylusEngine address (not the real Stylus engine)
      4. Create + settle a call; assert RepCalculatedFallback event is emitted with non-empty errorBytes
      5. Assert: Call.status == Settled (settlement COMPLETES despite Stylus revert — fallback succeeds)
      6. Assert: treasury received the 1.7% fee (money routing unaffected by fallback)
      7. Restore: vm.prank(proxyAdminOwner) + ProxyAdmin.upgradeAndCall(proxy, REAL_STYLUS_ENGINE, "") restores the real engine
      8. Verify restored: impl slot reads real Stylus engine address again
    - test_stylus_fallback_baseline_delta_is_nonzero:
      After the fallback fires, verify the rep delta applied is the Solidity baseline value (not zero), by checking the RepCalculatedFallback event's baselineDelta field is != 0
  </behavior>
  <action>
    Create packages/contracts/test/RevertingStylusEngineDrill.t.sol.

    Use the exact pragma =0.8.30 header with EXACT pin comment (per PATTERNS.md shared patterns).

    For the test environment: this drill should run against a fork (like SettlementManagerForkTest) OR a local anvil with all addresses hardcoded. Prefer the fork approach for maximum realism. Add the same ARB_ONE_RPC_URL env guard (vm.skip if not set) as used in SettlementManagerForkTest.

    ProxyAdmin address: read from EIP-1967 admin slot using:
      bytes32 adminSlot = 0xb53127684a568b3173ae13b9f8a6016e243e63b6e8ee1178d6a717850b5d6103;
      address proxyAdminAddr = address(uint160(uint256(vm.load(address(stylusProxy), adminSlot))));

    ProxyAdmin owner: ProxyAdmin(proxyAdminAddr).owner() — this is the Phase 5 deployer (not the Safe yet). Use vm.prank(this owner address) for the upgrade call.

    OZ 5.x ProxyAdmin API — CRITICAL: OpenZeppelin 5.6.1 ProxyAdmin exposes ONLY the 3-argument form:
      upgradeAndCall(ITransparentUpgradeableProxy proxy, address impl, bytes calldata data)
    The 2-argument upgrade(address,address) was REMOVED in OZ 5.0 and does NOT exist in this codebase (compile error if used).
    Always use the 3-arg form with empty bytes for data when no init is needed:
      ProxyAdmin(proxyAdminAddr).upgradeAndCall(ITransparentUpgradeableProxy(payable(stylusProxy)), revertingEngine, "")
    The canonical read_first anchor is packages/contracts/script/CutoffFallback.s.sol which already uses the correct 3-arg form.

    EIP-1967 impl slot for post-upgrade verification:
      bytes32 implSlot = 0x360894a13ba1a3210667c828492db98dca3e2076cc3735a920a3ca505d382bbc;
      address impl = address(uint160(uint256(vm.load(address(stylusProxy), implSlot))));

    For the RepCalculatedFallback event: the event signature is defined in SettlementManager.sol — import it. Use vm.expectEmit(true, true, false, false) before the settle() call.

    The test file should have exactly 2 test methods. No helper contracts needed — all logic is inline.
  </action>
  <verify>
    <automated>cd packages/contracts && ARB_ONE_RPC_URL="" forge test --match-contract RevertingStylusEngineDrill -vv 2>&1 | grep -E "PASS|SKIP|FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - forge test --match-contract RevertingStylusEngineDrill exits 0 (with ARB_ONE_RPC_URL="" it skips gracefully; with RPC it passes)
    - grep -c "RepCalculatedFallback" packages/contracts/test/RevertingStylusEngineDrill.t.sol returns >= 1
    - grep -c "upgradeAndCall" packages/contracts/test/RevertingStylusEngineDrill.t.sol returns >= 2 (upgrade + restore calls — must use 3-arg form)
    - grep -c '"upgrade(address,address)"\|\.upgrade(proxy\|\.upgrade(address' packages/contracts/test/RevertingStylusEngineDrill.t.sol returns 0 (2-arg OZ 4.x form must NOT be present)
    - grep -c "adminSlot" packages/contracts/test/RevertingStylusEngineDrill.t.sol returns >= 1
    - forge test (full suite) exits 0 — no regressions
  </acceptance_criteria>
  <done>RevertingStylusEngineDrill.t.sol proves the RepCalculatedFallback event fires and settlement completes when the Stylus proxy points at the revert fixture; upgrade calls use the OZ 5.x 3-arg upgradeAndCall API</done>
</task>

<task type="auto">
  <name>Task 2: Create soak-seeder.ts and evidence/ directory scaffold</name>
  <files>
    apps/relayer/src/scripts/soak-seeder.ts,
    evidence/phase-6-soak/.gitkeep,
    evidence/phase-6-soak/SCHEMA.md
  </files>
  <read_first>
    apps/relayer/src/scripts/backfill-criteria.ts (FULL — bootstrap pattern, env loading, main() + process.exit, error loop)
    apps/relayer/src/workers/stylus-deactivation-watcher.ts (lines 150-169 — sendAlertSafe pattern + Redis idempotency lock)
    apps/relayer/src/workers/alerts.ts (sendAlertSafe export signature)
    packages/shared/src/constants/addresses.ts (Phase-6 addresses after Plan 02 — CR, FFM, CE, SM, USDC_ARB_SEPOLIA)
    .planning/phases/06-safety-review-sepolia-48h-multisig-promotion/06-PATTERNS.md (soak-seeder.ts section lines 538-617)
    .planning/phases/06-safety-review-sepolia-48h-multisig-promotion/06-CONTEXT.md (D-04: seeding bot design — ≥10 calls/all types, ≥30 follow-fade, ≥3 settles-per-type, caller-exit, challenge cycle, owner-resolved dispute)
  </read_first>
  <action>
    Create apps/relayer/src/scripts/soak-seeder.ts.

    Use the loadEnvIfNeeded() bootstrap from backfill-criteria.ts verbatim.

    The script drives the ≥48h soak on the Phase-6 Sepolia cluster. Structure:

    CONSTANTS block:
    - SOAK_WALLETS: array of 10 test wallet private keys (from env: SOAK_WALLET_0..SOAK_WALLET_9)
    - CONTRACT addresses: read from CALL_REGISTRY_ADDRESS, FFM_ADDRESS, CE_ADDRESS, SM_ADDRESS env vars (same as relayer)
    - USDC address: USDC_ARB_SEPOLIA from env or constant
    - FAUCET_RATE_LIMIT_MS: 7200000 (2 hours between faucet requests per wallet per Circle limit)
    - EVIDENCE_LOG_PATH: evidence/phase-6-soak/evidence-${Date.now()}.jsonl

    PHASES of soak execution (sequential, async):
    Phase A — Create ≥10 calls covering all types:
      - Price target (ETH/USD), Spread/vs (ETH vs BTC), Event/TVL, Event/CEX, Event/Governance_Snapshot, Event/Governance_Tally, Event/ProtocolMilestone (7 subtypes, ≥1 of each; repeat 3 to reach 10)
      - Use viem writeContract with CallRegistry ABI createCall function
      - After each: emit evidence log entry {action: 'callCreated', txHash, callId, walletIndex, timestamp, block}

    Phase B — Create ≥30 follow/fade positions:
      - Distribute across 10 wallets; ≥15 follow, ≥15 fade (mix across callIds)
      - Use FollowFadeMarket ABI follow/fade functions
      - After each: emit {action: 'followed'|'faded', ...}

    Phase C — Settle ≥3 calls per type:
      - Wait for call expiry (or use very-short-deadline test calls)
      - Call sm.settle(callId, [], []) — non-Pyth calls use the attestation relayer worker
      - After each: emit {action: 'settled', ...}
      - If SettlementDelayed is emitted (Pyth confidence retry): emit {action: 'settlementDelayed', ...}

    Phase D — Caller-exit:
      - warp ≥24h (or use a real-time 24h wait for live Sepolia)
      - callerExit on one of the seeded calls
      - emit {action: 'callerExited', ...}

    Phase E — Challenge cycle:
      - proposeChallenge on an active call from wallet[1], acceptChallenge from the call's caller
      - Wait for settlement; record claimDuelPayout
      - emit {action: 'challengeProposed'|'challengeAccepted'|'challengeSettled', ...}

    Phase F — Dispute + owner resolution:
      - After a call settles, raiseDispute from wallet[2] (with $5 USDC bond)
      - resolveDispute from owner wallet with opposite outcome
      - emit {action: 'disputeRaised'|'disputeResolved', ...}

    Evidence log emission helper (appendEvidenceLog):
      Appends a single JSON line to EVIDENCE_LOG_PATH using appendFileSync.
      Format: one EvidenceEntry JSON object per line (JSONL).

    Alert emission: On any Phase C settle-stuck event (> 25 minutes without settlement), call sendAlertSafe('settle_stuck_25m', { callId }).

    Rate limit handling: Between faucet-required actions (when a wallet needs USDC top-up), wait FAUCET_RATE_LIMIT_MS or log a "wallet needs manual funding" message and continue with other wallets.

    Error handling: Wrap each phase in try/catch. On error, log + increment errors counter. After all phases, if errors > 0 exit 1, else exit 0. Never crash the whole script on a single phase failure.

    Create evidence/phase-6-soak/.gitkeep (empty file to create the directory).
    Create evidence/phase-6-soak/SCHEMA.md with a one-line description of the EvidenceEntry schema and the purpose (maps SAFETY-21–28 + PITFALLS checklist to tx hashes).

    TypeScript compilation check: from apps/relayer/ run npx tsc --noEmit to confirm no TS errors.
  </action>
  <verify>
    <automated>cd apps/relayer && npx tsc --noEmit 2>&1 | grep -E "error TS" | head -10; echo "TS check exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit exits 0 in apps/relayer (no TypeScript errors from soak-seeder.ts)
    - grep -c "appendEvidenceLog\|evidence-" apps/relayer/src/scripts/soak-seeder.ts returns >= 2
    - grep -c "'callCreated'\|'settled'\|'disputeRaised'" apps/relayer/src/scripts/soak-seeder.ts returns >= 3 (evidence log actions present)
    - grep -c "sendAlertSafe" apps/relayer/src/scripts/soak-seeder.ts returns >= 1
    - evidence/phase-6-soak/.gitkeep exists
    - evidence/phase-6-soak/SCHEMA.md exists and contains "EvidenceEntry"
    - grep -c "SOAK_WALLET" apps/relayer/src/scripts/soak-seeder.ts returns >= 1 (multi-wallet support)
  </acceptance_criteria>
  <done>soak-seeder.ts compiles, has all 7 soak phases implemented, emits JSONL evidence log entries per action, uses sendAlertSafe for settle-stuck alerts; evidence directory scaffolded</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RevertingStylusEngineDrill → production proxy | Test uses vm.prank(owner) to execute ProxyAdmin.upgradeAndCall — on Sepolia, this requires the actual ProxyAdmin owner key |
| soak-seeder.ts → live Sepolia contracts | Script sends real USDC transactions on Sepolia — wrong addresses cause failed txs but no mainnet risk |
| evidence log → soak pass verdict | Log is append-only JSONL; it is not verified onchain — trust is in the tx hashes being real Sepolia txs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Tampering | RevertingStylusEngine upgrade path | mitigate | ProxyAdmin.upgradeAndCall (3-arg OZ 5.x form) requires ProxyAdmin.owner() key — on Sepolia, this is the deployer key; the test uses vm.prank(proxyAdminOwner) which simulates this; live drill (Plan 05) requires the real key |
| T-06-04-02 | Repudiation | Evidence log JSONL tamperability | accept | Evidence is tx hash + block + timestamp; anyone can verify each hash on Arbiscan — the log itself is a pointer, not proof |
| T-06-04-03 | Denial of Service | Circle faucet rate limits in soak-seeder | mitigate | Script has FAUCET_RATE_LIMIT_MS guard and multi-wallet design (10 wallets); if one wallet hits limit, others continue |
| T-06-04-04 | Elevation of Privilege | soak-seeder.ts private keys in env | mitigate | Keys are test-only Sepolia wallets (zero mainnet value); never stored in repo — only in env vars. SOAK_WALLET_N pattern mirrors the existing DEPLOYER_PRIVATE_KEY pattern. |
| T-06-04-05 | Information Disclosure | Evidence log in repo | accept | Only contains Sepolia tx hashes (public data); no private keys or PII |
</threat_model>

<verification>
After both tasks:
- forge test --match-contract RevertingStylusEngineDrill exits 0 (skip or pass depending on ARB_ONE_RPC_URL)
- npx tsc --noEmit exits 0 in apps/relayer
- evidence/phase-6-soak/.gitkeep exists
- forge test (full suite) exits 0 — no regressions
</verification>

<success_criteria>
- SAFETY-42 unit drill proves RepCalculatedFallback fires and settlement completes when Stylus proxy points at revert fixture
- Soak-seeder.ts is ready to drive the 48h live soak (Wave 4) with evidence log output
- Live destruction drill (Plan 05) has the unit test as its CI analogue
</success_criteria>

<output>
After completion, create .planning/phases/06-safety-review-sepolia-48h-multisig-promotion/06-04-SUMMARY.md
</output>
