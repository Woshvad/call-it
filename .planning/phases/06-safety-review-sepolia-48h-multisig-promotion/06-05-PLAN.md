---
phase: 06-safety-review-sepolia-48h-multisig-promotion
plan: 05
type: execute
wave: 4
depends_on:
  - 06-02-PLAN.md
  - 06-03-PLAN.md
  - 06-04-PLAN.md
files_modified:
  - evidence/phase-6-soak/EVIDENCE-LOG.md
autonomous: false
requirements:
  - SAFETY-21
  - SAFETY-22
  - SAFETY-23
  - SAFETY-24
  - SAFETY-25
  - SAFETY-26
  - SAFETY-27
  - SAFETY-28
  - SAFETY-42

must_haves:
  truths:
    - "Soak runs ≥48 continuous hours on the Phase-6 Sepolia cluster with the live relayer active"
    - "Soak meets all SAFETY-21–28 seeding minimums: ≥10 calls/all types, ≥30 follow-fade, ≥3 settles/type, ≥1 caller-exit, ≥1 challenge cycle, ≥1 dispute+resolution, Pyth SettlementDelayed flow verified"
    - "Stylus destruction drill on live Sepolia: RevertingStylusEngine deployed behind proxy, settle() fires RepCalculatedFallback + Telegram alert, real engine restored"
    - "Evidence log EVIDENCE-LOG.md maps every SAFETY-21–28 item + every PITFALLS 38-item to tx hash / screenshot / passing test"
    - "5 deferred Phase-4 UAT items verified manually through the UI (live settlement E2E, dispute flow E2E, provenance modal D-10, OG 200px readability, live OG render for settled/exited)"
    - "Pre-deploy rituals pass: grep arbitrum-sepolia in dist == 0, chainId hardcoded in relayer EIP-712 domain == 42161, relayer ETH >= 0.5 ETH, Pyth bytes32 CI vs Hermes verified"
  artifacts:
    - path: "evidence/phase-6-soak/EVIDENCE-LOG.md"
      provides: "Full evidence log — every SAFETY-21–43 item + PITFALLS-38 checklist item mapped to tx hash / test / screenshot"
      contains: "SAFETY-21"
  key_links:
    - from: "evidence/phase-6-soak/evidence-*.jsonl"
      to: "evidence/phase-6-soak/EVIDENCE-LOG.md"
      via: "operator compiles JSONL entries into the final checklist document"
      pattern: "txHash"

user_setup:
  - service: arbitrum-sepolia-soak
    why: "Live 48h soak requires funded test wallets + live relayer + ProxyAdmin key for destruction drill"
    env_vars:
      - name: SOAK_WALLET_0..SOAK_WALLET_9
        source: "Generate 10 new test wallets (cast wallet new); fund each via Circle faucet at faucet.circle.com"
      - name: ARBITRUM_SEPOLIA_RPC_URL
        source: "Alchemy dashboard (already set for Phase 05.1)"
      - name: DEPLOYER_PRIVATE_KEY
        source: "Same key as Phase 05.1 deploy — needed for destruction drill ProxyAdmin.upgradeAndCall"
    dashboard_config:
      - task: "Fund each SOAK_WALLET with ≥20 USDC via Circle Sepolia faucet (faucet.circle.com — requests 20 USDC per address per 2h)"
        location: "faucet.circle.com"
      - task: "Confirm relayer is live and targeting Phase-6 addresses (from Plan 02 Step 5e)"
        location: "Railway/Fly.io relayer dashboard"
---

**Decisions covered (CONTEXT.md):** D-04: the ≥48h soak driven by the seeding bot + the 5 manually-verified Phase-4 UAT items. D-05: the checked evidence log mapping every SAFETY-29–43 + PITFALLS-38 item to a tx hash / screenshot / test.

<objective>
Execute the ≥48h Sepolia soak and compile the evidence log. This wave runs AFTER the safety matrix tests (Plan 03) and seeder/drill tooling (Plan 04) are ready, AND after the Phase-6 cluster is live (Plan 02 — the live cluster is a direct prerequisite for soak transactions). The soak-seeder.ts drives automated seeding; the destruction drill runs mid-soak to prove SAFETY-42; the 5 deferred Phase-4 UAT items are manually verified through the UI; the pre-deploy rituals are run as a final gate. The evidence log is the Phase 6 output that gates multisig promotion (Wave 5).

Purpose: 48h live proof is non-optional per the ROADMAP staging gate. The evidence log is the explicit deliverable mandated by D-05 — it maps every SAFETY item to observable reality on Sepolia, not just CI assertions.

Output: EVIDENCE-LOG.md with every SAFETY-21–43 item and every PITFALLS-38 item checked off with tx hash, screenshot, or test reference.
</objective>

<execution_context>
@C:\Users\woshv\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\woshv\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-safety-review-sepolia-48h-multisig-promotion/06-CONTEXT.md
@.planning/phases/04-settlementmanager-7-oracle-paths-solidity-baseline-rep-delta/04-HUMAN-UAT.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Key references for the soak and evidence log. -->

Soak minimum requirements (SAFETY-21–28):
  SAFETY-22: ≥10 seeded calls covering each call type (Price target, Spread/vs, ≥3 event subtypes)
  SAFETY-23: ≥30 follow/fade positions
  SAFETY-24: ≥3 settled calls per call type — verify outcome words, payouts, rep updates
  SAFETY-25: ≥1 caller-exit triggered with broadcast verified
  SAFETY-26: ≥1 challenge cycle proposed→accepted→settled
  SAFETY-27: ≥1 dispute raised and owner-resolved
  SAFETY-28: Pyth confidence retry exercised → SettlementDelayed event emitted + relayer waited 30×60s

5 deferred Phase-4 UAT items (04-HUMAN-UAT.md):
  UAT-1: Live settlement E2E — stake a call, wait for settlement, verify payout received in wallet
  UAT-2: Dispute flow E2E — raise dispute, wait for owner resolution, verify payout reversal
  UAT-3: Provenance modal D-10 — open Provenance modal on a settled call, verify oracle URL + tx + raw data + EIP-712 sig all present and correct per oracle type
  UAT-4: OG card 200px readability QA — open /og/[callId] at viewport 200px, verify all outcome words are legible (SHARE-12/UI-18)
  UAT-5: Live OG render for settled/exited calls — verify /og/[callId] returns X-Variant: settled (or exited) with correct card layout

Pre-deploy rituals (SAFETY-29 + ROADMAP Phase 6 SC5):
  1. grep -r "arbitrum-sepolia" in prod bundle dist/ → must return 0 matches
  2. grep hardcoded chainId "42161" in relayer EIP-712 domain separator → must return ≥1 match
  3. cast balance $RELAYER_ADDRESS --rpc-url $ARBITRUM_SEPOLIA_RPC_URL → must be ≥ 0.5 ETH
  4. Pyth bytes32 pre-deploy CI: for each spec'd asset feed ID in packages/shared/src/constants/addresses.ts, run `curl https://hermes.pyth.network/v2/price_feeds?query={symbol}` and assert the response bytes32 matches the hardcoded constant
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Execute ≥48h Sepolia soak with seeding bot and destruction drill</name>
  <read_first>
    apps/relayer/src/scripts/soak-seeder.ts (from Plan 04 — the seeding bot)
    packages/contracts/script/CutoffFallback.s.sol (ProxyAdmin.upgradeAndCall 3-arg pattern for destruction drill — canonical API anchor)
    .planning/phases/05-stylusscoreengine-48h-cutoff/05.1-OPERATOR-HANDOFF.md (StylusScoreEngine proxy + ProxyAdmin addresses)
    .planning/phases/06-safety-review-sepolia-48h-multisig-promotion/06-CONTEXT.md (D-04 seeding bot design, D-05 evidence log)
    .planning/phases/04-settlementmanager-7-oracle-paths-solidity-baseline-rep-delta/04-HUMAN-UAT.md (5 deferred UAT items to close)
    .planning/research/PITFALLS.md (38-item checklist — read all 38 items before the soak starts)
  </read_first>
  <what-built>
    soak-seeder.ts is ready (Plan 04). RevertingStylusEngineDrill.t.sol passes (Plan 04). Phase-6 cluster is live (Plan 02 — direct prerequisite). Safety matrix tests are green (Plan 03). The soak can now run.
  </what-built>
  <how-to-verify>
    Step 1 — Start the soak (record start timestamp for ≥48h gate):
    a. Fund 10 test wallets via faucet.circle.com (20 USDC per wallet).
    b. Set SOAK_WALLET_0..SOAK_WALLET_9 env vars.
    c. node apps/relayer/src/scripts/soak-seeder.ts
    Record: start timestamp + first evidence JSONL file path.

    Step 2 — Mid-soak Stylus destruction drill (SAFETY-42 live):
    OZ 5.x ProxyAdmin API note: use upgradeAndCall(address,address,bytes) — 3 arguments. The 2-arg upgrade(address,address) does NOT exist in OZ 5.6.1. The cast command must encode all 3 arguments.
    a. forge script packages/contracts/script/CutoffFallback.s.sol --rpc-url $ARBITRUM_SEPOLIA_RPC_URL --broadcast
       BUT with SOLIDITY_BASELINE_ADDR replaced by the RevertingStylusEngine address.
       (Or use cast: cast send $PROXY_ADMIN "upgradeAndCall(address,address,bytes)" $PROXY $REVERTING_ENGINE 0x --private-key $DEPLOYER_PRIVATE_KEY --rpc-url $ARBITRUM_SEPOLIA_RPC_URL)
    b. Trigger a settle() call on a live call → confirm RepCalculatedFallback event in Arbiscan logs.
    c. Confirm Telegram alert fired (check Telegram bot for "stylus_fallback" alert message).
    d. Restore real engine: cast send $PROXY_ADMIN "upgradeAndCall(address,address,bytes)" $PROXY $REAL_STYLUS_ENGINE 0x --private-key $DEPLOYER_PRIVATE_KEY --rpc-url $ARBITRUM_SEPOLIA_RPC_URL
    Record: upgrade tx hash, settle tx hash (with RepCalculatedFallback in logs), restore tx hash.

    Step 3 — Wait ≥48h from the start timestamp, monitoring:
    - Relayer /health stays 200 throughout
    - Settlement-watcher logs show ≥3 settles per call type
    - Telegram alerts fire for Pyth confidence retry → SettlementDelayed (manually trigger via a call with very tight confidence threshold or wait for a natural wide-confidence window)
    - Soak bot completes all phases A–F

    Step 4 — Manual UAT (5 deferred Phase-4 items) via browser against the Sepolia UI:

    [MANUAL VERIFICATION — human judgment required, no automated proxy for these items per D-04]

    UAT-1 [MANUAL]: Open a settled call page → verify payout reflected in wallet (check Arbiscan for claimPayout tx). Record: screenshot + Arbiscan claimPayout tx hash.
    UAT-2 [MANUAL]: Open a disputed call → follow the dispute UI → owner resolves → verify payout changed. Record: screenshot before/after resolution.
    UAT-3 [MANUAL]: Click "Provenance" on a settled Pyth call → verify oracle URL, tx hash, raw price data, and EIP-712 signature are all shown (D-10 oracle-type path aware). Record: screenshot of provenance modal with all 4 fields visible.
    UAT-4 [MANUAL]: Resize browser to 200px viewport → open /og/[callId] → verify "CALLED IT" / "LOUD AND WRONG" / etc. outcome words are readable at 200px. Record: screenshot at 200px width.
    UAT-5 [MANUAL]: Open /og/[callId] for a settled call → verify response headers include X-Variant: settled (curl -I), card shows settled layout not live layout. Record: curl -I output + screenshot.

    These 5 items require human eyes and cannot be automated. Each must be marked closed with a screenshot filename or observation note in the soak operator notes before "soak-complete" is signalled.

    Step 5 — Pre-deploy rituals:
    a. grep -r "arbitrum-sepolia" apps/relayer/dist/ → must be 0 matches (or run from build output)
       Shortcut: grep -r "arbitrum-sepolia" apps/relayer/src/ | grep -v "\.md\|\.json\|test" → 0 matches in production code paths
    b. grep -r "42161" apps/relayer/src/workers/ | grep -i "domain\|chainId\|eip712" → ≥1 match
    c. cast balance $RELAYER_ADDRESS --rpc-url $ARBITRUM_SEPOLIA_RPC_URL → ≥ 0.5 ETH
    d. Pyth bytes32 CI: for BTC/ETH/SOL/ARB/OP/POL feeds (first 6): curl https://hermes.pyth.network/v2/price_feeds?query={symbol} | jq '.[0].id' and compare to addresses.ts PYTH_FEED_* constants. Assert exact match.

    Step 6 — PITFALLS 38-item checklist: go through every item in .planning/research/PITFALLS.md and mark each as: ✅ PASS (with evidence — tx hash / test name / screenshot) or ❌ FAIL (blocks Phase 7).
  </how-to-verify>
  <acceptance_criteria>
    - Soak duration >= 48h (start and end timestamps recorded)
    - evidence/*.jsonl has ≥10 callCreated entries, ≥30 followed/faded entries, ≥9 settled entries (3 per type), ≥1 callerExited, ≥1 challengeSettled, ≥1 disputeResolved
    - Destruction drill: RepCalculatedFallback event present in Arbiscan for the drill settle tx (using 3-arg upgradeAndCall); Telegram received "stylus_fallback" alert; real engine restored (impl slot verified post-restore)
    - settlementDelayed action present in evidence JSONL (Pyth confidence retry exercised)
    - All 5 manual UAT items (UAT-1 through UAT-5) verified by human and noted as closed with screenshot/observation in soak operator notes — these require human judgment and have no automated backstop
    - Pre-deploy rituals: all 4 pass (0 arbitrum-sepolia in dist, chainId hardcoded, ETH balance ≥0.5, Pyth bytes32 match)
    - PITFALLS 38-item checklist: 0 failures (every item is ✅ PASS with evidence)
  </acceptance_criteria>
  <resume-signal>Type "soak-complete" with the start and end timestamps, destruction drill tx hashes, and PITFALLS checklist result (N/38 passed). Describe any failures for remediation before proceeding to Wave 5.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Compile and verify EVIDENCE-LOG.md — gate for multisig promotion</name>
  <read_first>
    evidence/phase-6-soak/evidence-*.jsonl (all JSONL files produced by soak-seeder)
    .planning/research/PITFALLS.md (the 38-item checklist — verify all items)
    .planning/phases/06-safety-review-sepolia-48h-multisig-promotion/06-CONTEXT.md (D-05: evidence log format)
    .planning/phases/06-safety-review-sepolia-48h-multisig-promotion/06-03-SUMMARY.md (safety matrix test results)
    .planning/phases/06-safety-review-sepolia-48h-multisig-promotion/06-04-SUMMARY.md (destruction drill unit test result)
  </read_first>
  <what-built>
    Soak completed (Task 1). Evidence JSONL files exist. PITFALLS checklist evaluated.
  </what-built>
  <how-to-verify>
    Compile evidence/phase-6-soak/EVIDENCE-LOG.md with:

    Section 1: SAFETY-21–28 Soak Minimums
    | Item | Requirement | Status | Evidence |
    |------|-------------|--------|----------|
    | SAFETY-21 | ≥48h soak | ✅ | Start: {ts}, End: {ts}, Duration: {N}h |
    | SAFETY-22 | ≥10 calls / all types | ✅ | callIds: {list}, tx hashes: {from JSONL} |
    | SAFETY-23 | ≥30 follow/fade positions | ✅ | tx hashes: {from JSONL} |
    | SAFETY-24 | ≥3 settled per type + outcome/payout verified | ✅ | tx hashes: {settle txs} |
    | SAFETY-25 | ≥1 caller-exit broadcast | ✅ | tx hash: {callerExited entry} |
    | SAFETY-26 | ≥1 challenge cycle | ✅ | propose: {tx}, accept: {tx}, settle: {tx} |
    | SAFETY-27 | ≥1 dispute + owner resolution | ✅ | raise: {tx}, resolve: {tx} |
    | SAFETY-28 | Pyth SettlementDelayed verified | ✅ | tx hash: {settlementDelayed entry} |

    Section 2: SAFETY-29–43 Safety Matrix
    | Item | Method | Evidence |
    Each SAFETY item mapped to: Foundry test name (from 06-03-SUMMARY.md) OR live Sepolia tx OR screenshot.
    SAFETY-42: destruction drill live Sepolia tx hashes (upgrade + settle + restore).

    Section 3: PITFALLS 38-item Checklist
    Copy all 38 items from PITFALLS.md. Mark each ✅/❌ with evidence reference.

    Section 4: Phase-4 Deferred UAT Closure
    UAT-1..5: mark each closed with screenshot filename or observation note. These items were verified manually (human judgment, no automated proxy per D-04) — record the human verification outcome here.

    Section 5: Pre-deploy Ritual Results
    All 4 rituals marked ✅/❌.

    The EVIDENCE-LOG.md is the Phase 6 pass/fail verdict document.
  </how-to-verify>
  <acceptance_criteria>
    - evidence/phase-6-soak/EVIDENCE-LOG.md exists and has all 5 sections
    - Section 1: all 8 SAFETY-21–28 items marked ✅
    - Section 2: all 15 SAFETY-29–43 items marked ✅ (test name or tx hash for each)
    - Section 3: all 38 PITFALLS items marked ✅ (0 failures)
    - Section 4: all 5 Phase-4 UAT items marked CLOSED (human-verified, no automated backstop per D-04)
    - Section 5: all 4 pre-deploy rituals marked ✅
    - grep -c "❌" evidence/phase-6-soak/EVIDENCE-LOG.md returns 0
  </acceptance_criteria>
  <resume-signal>Type "evidence-log-verified" when EVIDENCE-LOG.md is complete with all items checked off, or describe any remaining gaps that need remediation before multisig promotion proceeds.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live Sepolia → evidence log | Tx hashes are publicly verifiable on Arbiscan; log is a record, not a proof |
| Destruction drill deployer key → ProxyAdmin | The deployer key executes upgradeAndCall (3-arg OZ 5.x form) — must be the ProxyAdmin.owner(); same key used in production before multisig transfer |
| Manual UAT → human verification | Human judgment for 200px readability + provenance modal — no automated proxy; these items are explicitly marked [MANUAL] in Task 1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Tampering | Destruction drill — ProxyAdmin owner mismatch | mitigate | Before upgrade, verify cast call PROXY_ADMIN "owner()(address)" == DEPLOYER_ADDRESS; if mismatch, do NOT proceed — investigate ownership state first |
| T-06-05-02 | Repudiation | Evidence log completeness | mitigate | Evidence log includes tx hash for every soak action type; any missing action type is a gap that blocks Phase 7 |
| T-06-05-03 | Denial of Service | Circle faucet exhausted during soak | mitigate | 10 wallets × 20 USDC = 200 USDC total; Circle faucet rate-limit is 20 USDC per 2h per address — spread requests across wallets; soak-seeder handles rate limits |
| T-06-05-04 | Elevation of Privilege | Soak deployer key exposed in env during 48h | mitigate | Destruction drill (Step 2) uses deployer key for ≤30 minutes; key should be removed from env after the drill and before the 48h wait. Soak wallets are test-only with no mainnet value. |
| T-06-05-05 | Tampering | Pyth SettlementDelayed not naturally triggering | accept | If a natural wide-confidence window does not occur during 48h, manually trigger by calling settle() on a call immediately after submission (before 30s freshness window) — the 30-retry logic handles the wait; log the SettlementDelayed event tx as evidence |
</threat_model>

<verification>
After Task 2 evidence-log-verified signal:
- evidence/phase-6-soak/EVIDENCE-LOG.md exists
- grep -c "❌" evidence/phase-6-soak/EVIDENCE-LOG.md returns 0
- Soak duration verified ≥48h from timestamps
- Destruction drill tx hash verifiable on Arbiscan (RepCalculatedFallback in logs; used 3-arg upgradeAndCall)
- All PITFALLS 38 items ✅ — Phase 7 unblocked
</verification>

<success_criteria>
- ≥48h Sepolia soak completed with all SAFETY-21–28 minimums met
- SAFETY-42 live destruction drill completed: RepCalculatedFallback fired + Telegram alert + engine restored
- PITFALLS 38-item checklist 100% pass rate
- 5 deferred Phase-4 UAT items all CLOSED (human-verified)
- Pre-deploy rituals all pass
- EVIDENCE-LOG.md is the go/no-go document for multisig promotion (Wave 5)
</success_criteria>

<output>
After completion, create .planning/phases/06-safety-review-sepolia-48h-multisig-promotion/06-05-SUMMARY.md with the soak start/end timestamps, key evidence tx hashes, and PITFALLS checklist summary.
</output>
