---
phase: 06-safety-review-sepolia-48h-multisig-promotion
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01-PLAN.md
files_modified:
  - packages/contracts/script/DeployPhase6.s.sol
  - packages/shared/src/constants/addresses.ts
  - packages/subgraph/subgraph.yaml
autonomous: false
requirements:
  - SAFETY-02
  - SAFETY-03

must_haves:
  truths:
    - "DeployPhase6.s.sol deploys CR v4 + FFM v4 + CE v3 + SM v5 with resolveUsdc() in CE and SM constructors"
    - "ProfileRegistry keeps its Phase 05.1 address — only CR/FFM/CE/SM are redeployed"
    - "Post-deploy assertions confirm sm.usdc() == resolveUsdc() and ce.usdc() == resolveUsdc() on Sepolia"
    - "ProfileRegistry.setSettlementManager(newSM) is called to wire the new SM address"
    - "addresses.ts is updated with 4 new Phase-6 Sepolia contract addresses + USDC_ARB_SEPOLIA constant"
    - "subgraph.yaml is updated with new contract addresses and startBlocks for all 4 redeployed contracts"
    - "The relayer go-live 5-step checklist from 05.1-OPERATOR-HANDOFF.md is retargeted to Phase-6 addresses and executed"
  artifacts:
    - path: "packages/contracts/script/DeployPhase6.s.sol"
      provides: "Full-cluster redeploy script extending DeployPhase5_1 pattern with resolveUsdc()"
    - path: "packages/shared/src/constants/addresses.ts"
      provides: "Phase-6 Sepolia addresses + USDC_ARB_SEPOLIA"
      contains: "USDC_ARB_SEPOLIA"
    - path: "packages/subgraph/subgraph.yaml"
      provides: "Subgraph data sources updated to Phase-6 addresses"
  key_links:
    - from: "packages/contracts/script/DeployPhase6.s.sol"
      to: "packages/shared/src/constants/addresses.ts"
      via: "console.log DEPLOYMENT SUMMARY → operator copies new addresses"
      pattern: "CALL_REGISTRY_ARBITRUM_SEPOLIA"
    - from: "packages/shared/src/constants/addresses.ts"
      to: "packages/subgraph/subgraph.yaml"
      via: "subgraph address references Phase-6 CR/FFM/CE/SM addresses"
      pattern: "address:"

user_setup:
  - service: arbitrum-sepolia-rpc
    why: "Sepolia deploy requires an RPC endpoint and funded deployer key"
    env_vars:
      - name: ARBITRUM_SEPOLIA_RPC_URL
        source: "Alchemy dashboard → Apps → Arbitrum Sepolia → API key"
      - name: DEPLOYER_PRIVATE_KEY
        source: "Hardware wallet or KMS — same key used for Phase 05.1 deploy"
      - name: TREASURY_ADDRESS
        source: "Same treasury address as Phase 05.1"
      - name: KMS_ADDRESS_NFT_TWAP
        source: "Same KMS signer address as Phase 05.1"
      - name: KMS_ADDRESS_DEFILLAMA
        source: "Same KMS signer address as Phase 05.1"
      - name: KMS_ADDRESS_SNAPSHOT_TALLY
        source: "Same KMS signer address as Phase 05.1"
      - name: KMS_ADDRESS_CEX
        source: "Same KMS signer address as Phase 05.1"
    dashboard_config:
      - task: "Ensure deployer wallet has ≥0.1 ETH on Arbitrum Sepolia"
        location: "Alchemy faucet or bridge"
      - task: "Ensure relayer wallet has ≥0.5 ETH on Arbitrum Sepolia for Pyth VAA fees"
        location: "Alchemy faucet"
---

<objective>
Write DeployPhase6.s.sol (cloning DeployPhase5_1.s.sol with resolveUsdc() substitution), execute the Sepolia broadcast, update addresses.ts + subgraph.yaml with the new addresses, and run the retargeted Phase-05.1 relayer go-live 5-step checklist. This establishes the live Sepolia cluster with Circle USDC baked in — the foundation for the ≥48h soak.

Purpose: Without the redeployed cluster pointing at Circle Sepolia USDC, the soak bot cannot make any real USDC transfers. This plan completes the cluster + relayer go-live that unblocks the parallel Wave 3 safety matrix tests and the Wave 4 soak.

Output: Live Sepolia cluster (CR v4 / FFM v4 / CE v3 / SM v5) with Circle USDC; relayer retargeted; subgraph updated.
</objective>

<execution_context>
@C:\Users\woshv\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\woshv\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-safety-review-sepolia-48h-multisig-promotion/06-CONTEXT.md
@.planning/phases/06-safety-review-sepolia-48h-multisig-promotion/06-PATTERNS.md
@.planning/phases/05.1-non-pyth-oracle-rail-activation/05.1-OPERATOR-HANDOFF.md

<interfaces>
<!-- Key interfaces extracted from codebase for executor use. -->

From packages/contracts/script/DeployPhase5_1.s.sol (template to clone):
  - Header + imports pattern: lines 1-76 (import all 5 contracts + interfaces; import USDC constants)
  - Address constants block: lines 93-134 (PROFILE_REGISTRY, STYLUS_SCORE_ENGINE_PROXY, PROXY_ADMIN, PYTH_ARBITRUM_SEPOLIA, TVL_CAP, PYTH_ETH_BUDGET)
  - run() function: lines 136-364 (deployerKey from env, vm.startBroadcast, deploy order: CR → FFM → CE → SM, wire setSettlementManager + setAdapterMap + setAttestationSigner)
  - Post-deploy assertions: lines 366-595 (all require() checks on addresses/TVL/wiring)
  - Deployment summary console.log: lines 558-636

Phase-6 diff from Phase 5.1 (MINIMAL — only these changes):
  1. Import: add "import { resolveUsdc, USDC_ARB_SEPOLIA } from '../src/constants/USDC.sol';"
  2. ChallengeEscrow deploy arg: resolveUsdc() instead of USDC_ARB_NATIVE
  3. SettlementManager deploy arg: resolveUsdc() instead of USDC_ARB_NATIVE
  4. Post-deploy assertions: add "require(address(ce.usdc()) == resolveUsdc(), 'ce USDC gate failure')" and same for SM
  5. After vm.stopBroadcast(): call PR.setSettlementManager(address(sm)) — per resolved question in planning context
  6. Version comments updated to "Phase 6"

From packages/shared/src/constants/addresses.ts (constant format):
  export const CALL_REGISTRY_ARBITRUM_SEPOLIA = '0x...' as const;
  // Phase 05.1 cluster redeploy 2026-06-02 (block N); supersedes prior address.
  // Deploy tx: 0x... | forge-std log reference.

From packages/subgraph/subgraph.yaml (datasource pattern):
  - name: CallRegistry
    network: arbitrum-sepolia
    source:
      address: "0x..."  # Phase 6 v4 address
      startBlock: N     # Phase 6 deploy block

Phase 05.1 OPERATOR-HANDOFF 5-step checklist (retarget to Phase-6 addresses):
  Step 1: Update relayer env — CALL_REGISTRY_ADDRESS, FFM_ADDRESS, CE_ADDRESS, SM_ADDRESS to new Phase-6 values
  Step 2: Update relayer USDC env — USDC_ADDRESS to USDC_ARB_SEPOLIA (0x75faf114...)
  Step 3: Run backfill-criteria.ts against new SM address (oracle criteria for seeded calls)
  Step 4: Restart relayer workers — settlement-watcher, oracle adapters, stylus-deactivation-watcher
  Step 5: Verify relayer is live — hit /health endpoint and check settlement-watcher logs for new SM subscription
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write DeployPhase6.s.sol (clone of DeployPhase5_1 with resolveUsdc() + Phase-6 assertions)</name>
  <files>
    packages/contracts/script/DeployPhase6.s.sol
  </files>
  <read_first>
    packages/contracts/script/DeployPhase5_1.s.sol (FULL FILE — this is the exact template; read every line)
    packages/contracts/src/constants/USDC.sol (after Plan 01 — confirm resolveUsdc() is present)
    packages/contracts/src/ChallengeEscrow.sol (constructor signature)
    packages/contracts/src/SettlementManager.sol (constructor signature)
    .planning/phases/06-safety-review-sepolia-48h-multisig-promotion/06-PATTERNS.md (DeployPhase6.s.sol section, lines 90-185)
    .planning/phases/06-safety-review-sepolia-48h-multisig-promotion/06-CONTEXT.md (canonical_refs section — resolved question about ProfileRegistry.setSettlementManager)
  </read_first>
  <action>
    Create packages/contracts/script/DeployPhase6.s.sol by copying DeployPhase5_1.s.sol verbatim and applying the following minimal diff:

    1. File header: update comment "Phase 5.1" → "Phase 6"; update "Requirement:" to include SAFETY-02, SAFETY-21.
    2. Import line: add "import { resolveUsdc, USDC_ARB_SEPOLIA } from '../src/constants/USDC.sol';" alongside the USDC_ARB_NATIVE import (or replace USDC_ARB_NATIVE import if USDC.sol exports both from the same file — which it does).
    3. Contract name: DeployPhase5_1 → DeployPhase6. Constants block: keep PROFILE_REGISTRY, STYLUS_SCORE_ENGINE_PROXY, PROXY_ADMIN, PYTH_ARBITRUM_SEPOLIA using the EXACT Phase 05.1 values (see addresses.ts for current Phase 05.1 addresses). These are unchanged by Phase 6 — only CR/FFM/CE/SM are redeployed.
    4. In run(): replace the two `USDC_ARB_NATIVE` constructor arguments in ChallengeEscrow and SettlementManager with `resolveUsdc()`. All other constructor arguments stay identical to DeployPhase5_1.s.sol.
    5. After vm.stopBroadcast(), before the post-deploy assertions: call `ProfileRegistry(PROFILE_REGISTRY).setSettlementManager(address(sm))` — this re-wires the mutable cross-ref from PR to the new SM. This call is from the deployer (who is still owner of PR since ownership transfer hasn't happened yet).
    6. Post-deploy assertions block: copy all existing assertions from DeployPhase5_1.s.sol. Add TWO new assertions immediately after the existing USDC-related checks:
       - require(address(sm.usdc()) == resolveUsdc(), "DeployPhase6: sm.usdc() != resolveUsdc() -- USDC gate failure");
       - require(address(ce.usdc()) == resolveUsdc(), "DeployPhase6: ce.usdc() != resolveUsdc() -- USDC gate failure");
       Also add: require(address(ProfileRegistry(PROFILE_REGISTRY).settlementManager()) == address(sm), "DeployPhase6: PR settlementManager not updated");
    7. console.log DEPLOYMENT SUMMARY: update all version labels to "Phase 6"; include the new addresses of CR/FFM/CE/SM. REQUIRED NEXT STEPS section must instruct the operator to: (a) update addresses.ts, (b) update subgraph.yaml, (c) run relayer go-live 5-step checklist.

    Do NOT change: KMS address env vars, adapterMap wiring, attestation signer wiring, ProfileRegistry address — all identical to DeployPhase5_1.s.sol.
    Do NOT modify DeployPhase5_1.s.sol.

    Verify the script compiles: forge build --contracts packages/contracts/script/DeployPhase6.s.sol exits 0.
  </action>
  <verify>
    <!-- Compile check + dry-run to catch constructor-wiring regressions before broadcast -->
    <automated>cd packages/contracts && forge build --contracts script/DeployPhase6.s.sol 2>&1 | tail -5 && forge script script/DeployPhase6.s.sol --sender 0x0000000000000000000000000000000000000001 --rpc-url $ARBITRUM_SEPOLIA_RPC_URL 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - forge build exits 0 for DeployPhase6.s.sol
    - forge script dry-run (--sender, NO --broadcast) exits 0: constructor-wiring checks pass without broadcasting; any resolveUsdc() mismatch or PR.settlementManager assertion failure would surface here before broadcast
    - grep -c "resolveUsdc" packages/contracts/script/DeployPhase6.s.sol returns >= 2 (at least the two constructor call sites)
    - grep -c "USDC gate failure" packages/contracts/script/DeployPhase6.s.sol returns >= 2 (both post-deploy assertions)
    - grep -c "setSettlementManager" packages/contracts/script/DeployPhase6.s.sol returns >= 1 (PR rewire call)
    - grep -c "USDC_ARB_NATIVE" packages/contracts/script/DeployPhase6.s.sol returns 0 (replaced by resolveUsdc())
  </acceptance_criteria>
  <done>DeployPhase6.s.sol compiles and passes dry-run; contains resolveUsdc() in both CE and SM constructor calls; post-deploy assertions include the USDC gate check and PR.settlementManager check</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Execute Sepolia broadcast and retarget relayer to Phase-6 cluster</name>
  <read_first>
    packages/contracts/script/DeployPhase6.s.sol (after Task 1 — the script to broadcast)
    .planning/phases/05.1-non-pyth-oracle-rail-activation/05.1-OPERATOR-HANDOFF.md (5-step relayer go-live checklist — retarget all steps to Phase-6 addresses)
    packages/shared/src/constants/addresses.ts (current Phase 05.1 addresses to supersede)
    packages/subgraph/subgraph.yaml (current datasource block — 4 entries to update)
    .planning/phases/06-safety-review-sepolia-48h-multisig-promotion/06-PATTERNS.md (addresses.ts and subgraph.yaml sections)
  </read_first>
  <what-built>
    DeployPhase6.s.sol is ready and dry-run verified (Task 1). This task broadcasts it, captures the new addresses, updates downstream config, and runs the relayer go-live.
  </what-built>
  <how-to-verify>
    Step 1 — Check relayer ETH balance [BLOCKING]:
    cast balance $RELAYER_ADDRESS --rpc-url $ARBITRUM_SEPOLIA_RPC_URL
    Result must be >= 0.5 ETH. If less, top up via Alchemy Sepolia faucet before proceeding.

    Step 2 — Broadcast DeployPhase6.s.sol:
    forge script packages/contracts/script/DeployPhase6.s.sol --rpc-url $ARBITRUM_SEPOLIA_RPC_URL --broadcast --verify (or skip --verify and run forge verify-contract separately)
    Record: the 4 new contract addresses from the DEPLOYMENT SUMMARY console output.

    Step 3 — Update packages/shared/src/constants/addresses.ts:
    Add USDC_ARB_SEPOLIA = '0x75faf114eafb1BDbe2F0316DF893fd58CE46AA4d' near the USDC section.
    Update CALL_REGISTRY_ARBITRUM_SEPOLIA, FOLLOW_FADE_MARKET_ARBITRUM_SEPOLIA, CHALLENGE_ESCROW_ARBITRUM_SEPOLIA, SETTLEMENT_MANAGER_ARBITRUM_SEPOLIA to the Phase-6 values.
    Add a comment on each: "Phase 6 cluster redeploy 2026-06-XX (block N); supersedes Phase 05.1 address."

    Step 4 — Update packages/subgraph/subgraph.yaml:
    Change address + startBlock for CallRegistry, FollowFadeMarket, ChallengeEscrow, SettlementManager to Phase-6 values. ProfileRegistry and StylusScoreEngine entries are unchanged.
    Run: graph build (in packages/subgraph) to confirm no build errors.
    Run: graph deploy --studio call-it-sepolia (or equivalent Studio CLI command) to redeploy the subgraph.

    Step 5 — Retargeted 05.1 OPERATOR-HANDOFF 5-step checklist:
    Step 5a: Update relayer env (.env / Railway env vars) — CALL_REGISTRY_ADDRESS, FFM_ADDRESS, CE_ADDRESS, SM_ADDRESS to Phase-6 values; USDC_ADDRESS to USDC_ARB_SEPOLIA.
    Step 5b: Re-grant KMS signers on new SM: cast send newSM "setAttestationSigner(uint8,address)" for each of the 4 KMS signer addresses (same addresses, new SM contract).
    Step 5c: Run node apps/relayer/src/scripts/backfill-criteria.ts (retargeted criteria for any existing seeded calls).
    Step 5d: Restart relayer workers.
    Step 5e: Verify relayer is live — curl https://call-it-relayer-sepolia/health returns 200; logs show settlement-watcher subscribed to new SM.

    Verification assertions:
    cast call $NEW_SM "usdc()(address)" --rpc-url $ARBITRUM_SEPOLIA_RPC_URL == 0x75faf114eafb1BDbe2F0316DF893fd58CE46AA4d
    cast call $NEW_CE "usdc()(address)" --rpc-url $ARBITRUM_SEPOLIA_RPC_URL == 0x75faf114eafb1BDbe2F0316DF893fd58CE46AA4d
    cast call $PROFILE_REGISTRY "settlementManager()(address)" --rpc-url $ARBITRUM_SEPOLIA_RPC_URL == $NEW_SM
    cast call $NEW_CR "owner()(address)" --rpc-url $ARBITRUM_SEPOLIA_RPC_URL == $DEPLOYER (not yet transferred to Safe)
    curl https://call-it-relayer-sepolia/health returns {"status":"ok"}
  </how-to-verify>
  <acceptance_criteria>
    - cast call NEW_SM "usdc()(address)" returns 0x75faf114eafb1BDbe2F0316DF893fd58CE46AA4d
    - cast call NEW_CE "usdc()(address)" returns 0x75faf114eafb1BDbe2F0316DF893fd58CE46AA4d
    - cast call PROFILE_REGISTRY "settlementManager()(address)" returns NEW_SM address
    - addresses.ts updated: USDC_ARB_SEPOLIA constant present + 4 new contract constants
    - subgraph.yaml updated: all 4 datasource addresses + startBlocks reflect Phase-6 deploy
    - graph build exits 0 on updated subgraph.yaml
    - Subgraph redeployed to Studio: graph deploy exits 0
    - Relayer /health returns 200 with new cluster addresses in worker config
    - Relayer logs show settlement-watcher subscribed to new SM contract address
  </acceptance_criteria>
  <resume-signal>Type "cluster-live" with the 4 new contract addresses (CR, FFM, CE, SM) and the deploy block number, or describe any broadcast failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Deploy script → live Sepolia | Broadcast trusts the RPC and deployer key; malicious RPC could send a different tx |
| addresses.ts → downstream consumers | Stale addresses in addresses.ts mean the relayer + subgraph talk to wrong contracts |
| Relayer env → live workers | Workers cached with Phase 05.1 addresses until restarted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering | DeployPhase6.s.sol broadcast | mitigate | Post-deploy assertions in the script exit non-zero if any address/wiring is wrong — broadcast is atomic with the assert pass. Dry-run (--sender, no --broadcast) catches constructor-wiring regressions before the real broadcast. |
| T-06-02-02 | Tampering | addresses.ts stale update | mitigate | cast call verification step confirms sm.usdc() + ce.usdc() + PR.settlementManager before calling the cluster live |
| T-06-02-03 | Spoofing | Relayer targeting Phase 05.1 SM after Phase 6 redeploy | mitigate | Relayer go-live Step 5a explicitly updates all address env vars; relayer health check post-restart confirms new SM address in worker config |
| T-06-02-04 | Denial of Service | Subgraph indexing gap between redeploy and Studio redeploy | accept | Polled-events fallback from Phase 0 (viem.getLogs) stays live during Studio sync; known gap per ROADMAP Phase 7 |
| T-06-02-05 | Elevation of Privilege | KMS signers not re-granted on new SM | mitigate | Step 5b of the retargeted OPERATOR-HANDOFF checklist explicitly re-runs setAttestationSigner for all 4 KMS signer keys on the new SM |
</threat_model>

<verification>
After task 2 operator completes and types "cluster-live":
- cast call NEW_SM "usdc()(address)" == 0x75faf114eafb1BDbe2F0316DF893fd58CE46AA4d
- cast call NEW_CE "usdc()(address)" == 0x75faf114eafb1BDbe2F0316DF893fd58CE46AA4d
- cast call PROFILE_REGISTRY "settlementManager()(address)" == NEW_SM
- grep -c "USDC_ARB_SEPOLIA" packages/shared/src/constants/addresses.ts returns >= 1
- graph build exits 0 in packages/subgraph
- Relayer /health 200 OK
</verification>

<success_criteria>
- Phase-6 Sepolia cluster live with Circle USDC in CE + SM
- ProfileRegistry wired to new SM
- addresses.ts + subgraph.yaml updated and verified
- Relayer go-live 5-step checklist executed against new cluster
- Soak bot can now fund wallets and execute USDC transfers (Wave 4 unblocked)
</success_criteria>

<output>
After completion, create .planning/phases/06-safety-review-sepolia-48h-multisig-promotion/06-02-SUMMARY.md with the 4 new contract addresses, deploy block, and relayer health confirmation.
</output>
