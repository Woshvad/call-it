---
phase: 01-core-contracts-auth-frontend-skeleton
plan: 10
type: execute
wave: 5
depends_on: [01, 02, 03, 04, 05, 06, 07, 08, 09]
files_modified:
  - packages/subgraph/subgraph.yaml
  - packages/subgraph/schema.graphql
  - packages/subgraph/src/call-registry.ts
  - packages/subgraph/src/profile-registry.ts
  - packages/subgraph/package.json
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - apps/web/tests/visual-smoke.spec.ts
  - apps/web/tests/design-system-snap.spec.ts
  - apps/web/app/_dev/design-system/page.tsx
  - .github/workflows/phase-1-gates.yml
  - .planning/STATE.md
autonomous: true
requirements:
  - UI-38
  - UI-39
  - UI-40
  - UI-41
  - UI-42
  - UI-43
  - AUTH-27
  - AUTH-29
  - OPS-04
tags:
  - subgraph-extension
  - requirements-amendment
  - visual-smoke
  - design-system-snap
  - ci-gate-final
  - phase-closure
must_haves:
  truths:
    - "Sepolia Studio subgraph indexes CallRegistry events (CallCreated, CallQuoted, ConvictionCapped, AssetAllowlisted, NftCollectionAllowlisted, TvlCapSet, SettlementManagerSet) and ProfileRegistry events (ProfileUpdated, HandleSet, SettlementManagerSet, RelayerSet, SocialLinked, SocialUnlinked) within 30s of emission (OPS-04)"
    - "REQUIREMENTS.md AUTH-27 and AUTH-29 are amended verbatim to reflect the D-06 spec deviation: 6th tx onward pays gas in USDC via Circle Paymaster (no ETH required) instead of user-provided ETH"
    - "PROJECT.md Key Decisions table contains a new row capturing the D-06 deviation with rationale"
    - "Playwright visual-smoke.spec.ts captures and asserts visual snapshots of all 4 Phase 1 baseline pages with pixelmatch tolerance"
    - "Playwright design-system-snap.spec.ts captures snapshots of @call-it/ui primitives (Button x9 variants, Skeleton x6 variants, Toast x3 statuses, Stamp, CornerBrackets, ConvictionBar)"
    - "phase-1-gates.yml final job phase-1-complete-gate runs the full Playwright matrix + Foundry CI fuzz + parity-diff + grep guards in one gate; tag phase-1-complete triggers it"
    - "All 158 Phase 1 REQ-IDs from ROADMAP.md are covered by at least one plan (01-10); coverage audit verified in 01-10-SUMMARY"
  artifacts:
    - path: "packages/subgraph/subgraph.yaml"
      provides: "Extended mappings for CallRegistry + ProfileRegistry events with Phase 1 ABI signatures (Pitfall C closed)"
      contains: "CallRegistry"
    - path: "packages/subgraph/schema.graphql"
      provides: "Call + Profile entity GraphQL schema extended from Phase 0 stubs with graph-cli 0.98 @entity(immutable:false) markers"
      contains: "type Call"
    - path: "packages/subgraph/src/call-registry.ts"
      provides: "AssemblyScript mapping handlers for each CallRegistry event"
      contains: "handleCallCreated"
    - path: "packages/subgraph/src/profile-registry.ts"
      provides: "AssemblyScript mapping handlers for each ProfileRegistry event"
      contains: "handleProfileUpdated"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Amended REQ text for AUTH-27 + AUTH-29 reflecting D-06"
      contains: "Circle Paymaster"
    - path: "apps/web/tests/visual-smoke.spec.ts"
      provides: "Playwright visual snapshot tests for the 4 Phase 1 pages"
      contains: "toHaveScreenshot"
    - path: ".github/workflows/phase-1-gates.yml"
      provides: "Final tag-gated phase-1-complete-gate job wiring the entire CI matrix"
      contains: "phase-1-complete-gate"
    - path: ".planning/phases/01-core-contracts-auth-frontend-skeleton/01-10-SUMMARY.md"
      provides: "Phase 1 closure SUMMARY with full 158-REQ coverage audit + locked decisions closure + pitfall status + outstanding items + deploy procedure"
      contains: "Coverage Audit"
  key_links:
    - from: "packages/subgraph/subgraph.yaml"
      to: "packages/contracts/out/CallRegistry.sol/CallRegistry.json (ABI)"
      via: "Subgraph references the deployed contract ABI; redeploy on ABI change"
      pattern: "abi:.*CallRegistry"
    - from: ".planning/REQUIREMENTS.md AUTH-27/AUTH-29"
      to: ".planning/phases/01-core-contracts-auth-frontend-skeleton/01-CONTEXT.md D-06"
      via: "Spec deviation logged in CONTEXT.md, amended in REQUIREMENTS.md, recorded in PROJECT.md"
      pattern: "Circle Paymaster|D-06"
---

<objective>
Phase 1 closure. Three integration tasks land here that the slice plans intentionally deferred: (1) extend the Phase 0 stub subgraph mappings to index the Phase 1 contract events on Sepolia Studio (closing Pitfall C — schema drift between Phase 0 stub events and Phase 1 real events); (2) amend REQUIREMENTS.md AUTH-27 + AUTH-29 to reflect the D-06 Circle USDC Paymaster spec deviation, and append the deviation to PROJECT.md Key Decisions; (3) capture Playwright visual snapshots of the 4 baseline pages + the @call-it/ui design-system primitives, wire the final tag-gated phase-1-complete-gate CI job, and write the Phase 1 SUMMARY with the 158-REQ coverage audit + locked-decisions closure + pitfall closure status + STATE.md handoff to Phase 1.5.

Purpose: Plans 01-09 ship the contract + relayer + frontend surface. Without Plan 10 the subgraph is still a stub (Plan 09 feed will return zero data because subgraph mappings don't recognize Phase 1 events); REQUIREMENTS.md still says "6th tx requires user-provided ETH" which directly contradicts Plan 07's implementation; and there's no visual baseline to catch design drift in Phase 2-9. This plan closes those three gaps and gates the phase-tag.

Output: Subgraph deployed to Sepolia Studio with full Phase 1 ABI mappings indexing CallRegistry + ProfileRegistry events; REQUIREMENTS.md amended verbatim; PROJECT.md decision row added; visual + design-system snapshot tests in CI; phase-1-gates.yml wired as the canonical Phase 1 gate; Phase 1 SUMMARY with 158-REQ coverage audit + locked-decisions closure ready for /gsd-verify-work.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-contracts-auth-frontend-skeleton/01-CONTEXT.md
@.planning/phases/01-core-contracts-auth-frontend-skeleton/01-RESEARCH.md
@.planning/phases/00-foundation/00-03-SUMMARY.md
@.planning/phases/01-core-contracts-auth-frontend-skeleton/01-02-SUMMARY.md
@.planning/phases/01-core-contracts-auth-frontend-skeleton/01-04-SUMMARY.md
@.planning/phases/01-core-contracts-auth-frontend-skeleton/01-07-SUMMARY.md
@.planning/phases/01-core-contracts-auth-frontend-skeleton/01-09-SUMMARY.md

<interfaces>
<!-- Surfaces this closure plan composes. -->

From Plan 02:
- packages/contracts/out/CallRegistry.sol/CallRegistry.json — ABI
- packages/contracts/out/ProfileRegistry.sol/ProfileRegistry.json — ABI
- Deployed Arbitrum Sepolia addresses for CallRegistry + ProfileRegistry in packages/shared/src/constants/addresses.ts

From Phase 0:
- packages/subgraph/subgraph.yaml — existing stub mappings to extend (NOT replace)
- packages/subgraph/schema.graphql — entity schema baseline
- packages/subgraph/src/* — AssemblyScript handlers (empty stubs from Phase 0)
- @graphprotocol/graph-cli@0.98.1 + @graphprotocol/graph-ts@0.38.2 already installed (STATE.md notes graph-cli@0.98.1 requires explicit @entity(immutable:true|false))

From Plan 03:
- .github/workflows/phase-1-gates.yml — Wave-1 baseline; extend final job here

Subgraph build workflow:
- pnpm --filter @call-it/subgraph exec graph codegen after editing subgraph.yaml + schema.graphql
- pnpm --filter @call-it/subgraph exec graph build
- pnpm --filter @call-it/subgraph exec graph deploy --studio call-it-sepolia (deploy key from SUBGRAPH_DEPLOY_KEY env at deploy time; operator runs manually with the Studio dashboard's deploy key)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Subgraph mapping extension for CallRegistry + ProfileRegistry events; deploy to Sepolia Studio; close Pitfall C (ABI drift)</name>
  <read_first>
    - packages/subgraph/subgraph.yaml (Phase 0 baseline)
    - packages/subgraph/schema.graphql (Phase 0 entities)
    - packages/subgraph/src/ (Phase 0 stub handlers)
    - packages/subgraph/package.json (Phase 0 deps + scripts)
    - packages/contracts/out/CallRegistry.sol/CallRegistry.json (Plan 02 ABI)
    - packages/contracts/out/ProfileRegistry.sol/ProfileRegistry.json (Plan 02 ABI)
    - .planning/phases/00-foundation/00-03-SUMMARY.md (Phase 0 subgraph baseline)
    - .planning/phases/01-core-contracts-auth-frontend-skeleton/01-RESEARCH.md Pitfall C (subgraph schema drift) + Standard Stack subgraph row
    - .planning/phases/01-core-contracts-auth-frontend-skeleton/01-CONTEXT.md D-24, D-27
  </read_first>
  <files>packages/subgraph/subgraph.yaml, packages/subgraph/schema.graphql, packages/subgraph/src/call-registry.ts, packages/subgraph/src/profile-registry.ts, packages/subgraph/package.json, packages/shared/src/constants/addresses.ts</files>
  <behavior>
    - Behavior: After deploy, the subgraph indexes a published Call from Plan 08's flow within ~30s; querying { calls(orderBy: createdAt, orderDirection: desc, first: 5) { id, caller { id, displayHandle }, marketType, eventSubtype, stake, conviction, createdAt } } returns recent calls
    - Behavior: ProfileRegistry events update the Profile entity's settledCalls + totalCalls (write triggered by future SettlementManager in Phase 4 — Phase 1 only emits ProfileUpdated on first-touch lazy init; the handler still updates the entity for forward-compat)
    - Behavior: _meta { block { number }, hasIndexingErrors } is queryable so Plan 09's feed race can detect lag
    - Behavior: The deployed subgraph URL is recorded in packages/shared/src/constants/addresses.ts as SUBGRAPH_URL_SEPOLIA so Plan 09's relayer subgraph-client reads from the canonical source
  </behavior>
  <action>Extend packages/subgraph/subgraph.yaml. Add a new dataSources entry for CallRegistry pointing at the Arbitrum Sepolia deployed address from Plan 02 packages/shared/src/constants/addresses.ts. List event handlers: CallCreated(uint256,address,uint8,uint96) maps to handleCallCreated, CallQuoted(uint256,uint256) to handleCallQuoted, ConvictionCapped(address,uint8,uint8) to handleConvictionCapped, AssetAllowlisted(string,bytes32) to handleAssetAllowlisted, NftCollectionAllowlisted(address) to handleNftCollectionAllowlisted, TvlCapSet(uint256) to handleTvlCapSet, SettlementManagerSet(address) to handleSettlementManagerSet. Add a second dataSource for ProfileRegistry with: ProfileUpdated(address,uint32,uint16) to handleProfileUpdated, HandleSet(address,string) to handleHandleSet, SettlementManagerSet(address) to handleProfileRegistrySettlementManagerSet (distinct handler name from CallRegistry's same-named event), RelayerSet(address) to handleRelayerSet, SocialLinked(address,uint8,string,bytes32) to handleSocialLinked, SocialUnlinked(address,uint8) to handleSocialUnlinked. ABI references are local file paths to the JSON ABIs copied into packages/subgraph/abis/CallRegistry.json + ProfileRegistry.json. Add a prebuild script in packages/subgraph/package.json: "prebuild": "mkdir -p abis && cp ../contracts/out/CallRegistry.sol/CallRegistry.json abis/CallRegistry.json && cp ../contracts/out/ProfileRegistry.sol/ProfileRegistry.json abis/ProfileRegistry.json"; update build script to "pnpm prebuild && graph codegen && graph build"; add "deploy:sepolia": "graph deploy --studio call-it-sepolia". Per graph-cli 0.98+ requirement (per STATE.md), update packages/subgraph/schema.graphql to add @entity(immutable: false) markers on Call and Profile entities. Schema fields per RESEARCH validation map + Plan 02 contract surface: Call entity has id ID, caller Profile, marketType Int, eventSubtype Int, category Int, assetA BigInt, assetB BigInt, targetValue BigInt, expiry BigInt, stake BigInt, virtualFadeSeed BigInt, createdAt BigInt, conviction Int, status Int, openToChallenges Boolean, duplicateHash Bytes, criteriaHash Bytes, parentCallId BigInt, quoteOf Call (nullable). Profile entity has id ID, displayHandle String optional, twitterHandle String optional, farcasterHandle String optional, totalCalls Int, settledCalls Int, wins Int, losses Int, globalRep BigInt, lastActiveAt BigInt, and a derivedFrom calls list. Write packages/subgraph/src/call-registry.ts in AssemblyScript: import event types from ../generated/CallRegistry/CallRegistry and Call + Profile from ../generated/schema; export handleCallCreated that does: let call = new Call(event.params.id.toString()); ensureProfile(event.params.caller); call.caller = event.params.caller.toHexString(); call.marketType = event.params.marketType; call.stake = event.params.stake; call.createdAt = event.block.timestamp; call.status = 0; call.save(); then increment Profile.totalCalls. Similar handlers for the other events. The handler for CallQuoted updates the existing call's quoteOf reference using event.params.quoteId and event.params.parentId. Define a helper function ensureProfile(address: Address): Profile that lazy-creates the Profile entity if not exists, setting globalRep = 100 (REP-01 mirror) and initializing counts to 0. Write packages/subgraph/src/profile-registry.ts with: handleProfileUpdated updates Profile.totalCalls + settledCalls + lastActiveAt; handleHandleSet sets displayHandle; handleSocialLinked sets twitterHandle or farcasterHandle based on event.params.kind (0 = twitter, 1 = farcaster); handleSocialUnlinked clears the corresponding handle; handleProfileRegistrySettlementManagerSet + handleRelayerSet are bookkeeping no-ops (event indexed for forward-compat). After all files are written: run pnpm --filter @call-it/subgraph build to verify codegen + AS compilation. Then run pnpm --filter @call-it/subgraph deploy:sepolia (operator provides SUBGRAPH_DEPLOY_KEY via env at deploy time; the deploy is non-CI manual — flag in task summary if deploy step is gated behind operator). After deploy, record the deployed subgraph URL in packages/shared/src/constants/addresses.ts as SUBGRAPH_URL_SEPOLIA = the studio URL. Smoke test from the relayer dev shell: call relayerClient.getFeed() against a Sepolia-published call, verify the entity is indexed within 30s — record the latency in 01-10-SUMMARY.md.</action>
  <verify>
    <automated>pnpm --filter @call-it/subgraph build &amp;&amp; ls packages/subgraph/generated/ 2&gt;/dev/null | head</automated>
  </verify>
  <acceptance_criteria>
    - pnpm --filter @call-it/subgraph build exits 0 (codegen + AssemblyScript compile)
    - packages/subgraph/subgraph.yaml lists both CallRegistry and ProfileRegistry as dataSources with their event handlers
    - packages/subgraph/schema.graphql defines Call and Profile entities with @entity(immutable: false) markers (graph-cli 0.98+ requirement)
    - packages/subgraph/src/call-registry.ts exports handleCallCreated, handleCallQuoted, handleConvictionCapped + the 4 setter event handlers
    - packages/subgraph/src/profile-registry.ts exports handleProfileUpdated, handleHandleSet, handleSocialLinked, handleSocialUnlinked, handleProfileRegistrySettlementManagerSet, handleRelayerSet
    - packages/shared/src/constants/addresses.ts records SUBGRAPH_URL_SEPOLIA (after deploy)
    - GraphQL query against the deployed subgraph returns calls indexed within 30s of a Plan-08-published test call (operator-verified, recorded in SUMMARY)
    - Phase 0 polled-events fallback worker (OPS-04) still functional — Plan 09 feed race tested against it as deliberate fallback
  </acceptance_criteria>
  <done>Subgraph deployed to Sepolia Studio with full Phase 1 ABI mappings; Pitfall C closed; the relayer feed endpoint (Plan 09) has a live data source; 30s indexing latency confirmed in SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: REQUIREMENTS.md AUTH-27 + AUTH-29 amendment + PROJECT.md Key Decisions D-06 row + visual + design-system snapshot tests + final phase-1-complete-gate workflow</name>
  <read_first>
    - .planning/REQUIREMENTS.md (current AUTH-27 + AUTH-29 text — lines 53 + 55 per the file)
    - .planning/PROJECT.md (Key Decisions table — extension point)
    - .planning/phases/01-core-contracts-auth-frontend-skeleton/01-CONTEXT.md D-06 (spec deviation rationale)
    - .github/workflows/phase-1-gates.yml (Plan 03 baseline — final job goes here)
    - apps/web/tests/og-fallback.spec.ts (visual snapshot analog from Phase 0)
    - apps/web/playwright.config.ts (Playwright visual snapshot config)
    - @call-it/ui primitives (Plan 04 — snapshot target)
    - All Phase 1 pages: /, /new, /profile/[address], /signin (Plans 05-09)
  </read_first>
  <files>.planning/REQUIREMENTS.md, .planning/PROJECT.md, apps/web/tests/visual-smoke.spec.ts, apps/web/tests/design-system-snap.spec.ts, apps/web/app/_dev/design-system/page.tsx, .github/workflows/phase-1-gates.yml</files>
  <action>Edit .planning/REQUIREMENTS.md. Find the line for AUTH-27 (currently around line 53). Replace it verbatim with: "- [ ] AUTH-27: Paymaster sponsors exactly the first 5 transactions per user; 6th transaction onward pays gas in USDC via Circle USDC Paymaster on Arbitrum (EIP-2612 permit per tx) — no ETH required. (§10.7, App.A.1; amended by Phase 1 D-06 spec deviation)". Find AUTH-29 and replace verbatim with: "- [ ] AUTH-29: When the per-account 5-tx sponsorship cap is exhausted, the frontend automatically routes subsequent userOps through Circle USDC Paymaster (CIRCLE_PAYMASTER_ARBITRUM_ONE) with a per-tx EIP-2612 permit signed by the user's Privy embedded wallet; UI surfaces the USDC gas mode Tag (§10.7; amended by Phase 1 D-06 spec deviation)". Edit .planning/PROJECT.md. Find the Key Decisions table or section. Append a new row referencing D-06: "| D-06 (Phase 1) | 6th-tx-onward gas mechanic | Circle USDC Paymaster on Arbitrum with per-tx EIP-2612 permit replaces user-provided ETH; no ETH ever required. Rationale: eliminates user-friction onboarding step that contradicts the USDC-native value prop. | 2026-05-23 | Locked in Phase 1 — Plans 07/08 implementation". Create apps/web/app/_dev/design-system/page.tsx as a dev-only client component (the route is under _dev so it is ignored in production routing per Next.js convention or guarded by an env flag NEXT_PUBLIC_DEV_ROUTES=1). The page renders every @call-it/ui primitive variant in a horizontal flex layout (Receipt is NOT included on this page — the ESLint no-display-grid rule scoped in Plan 01 only blocks the Receipt subtree, but the dev page uses pure flexbox anyway to keep the Phase 7 OG-via-Satori invariant universal). The page renders: 9 Button variants in a Card row (3 intents × 3 sizes), 4 Tag variants, CornerBrackets inside a relative Card, 6 Skeleton variants stacked vertically, Stamp component with the word CALLED IT in 4 colors, ConvictionBar at values 1, 50, 100, and a button labeled "Trigger toasts" that fires useToast().show for each of success/info/error (so the design-system-snap test can capture the toast state). Build apps/web/tests/visual-smoke.spec.ts per RESEARCH validation map. Four Playwright tests, one per Phase 1 page (/, /new, /profile/[address], /signin). For each test navigate while mocking Privy session via page.route to a stable seeded user identity, mock relayer endpoints (/api/feed, /api/profile/{address}, /api/onboarding/state) with deterministic JSON; wait for network idle; call await expect(page).toHaveScreenshot('{page-name}.png', { maxDiffPixelRatio: 0.02 }). The first run generates the baseline goldens committed to apps/web/tests/__screenshots__/visual-smoke.spec.ts/. Build apps/web/tests/design-system-snap.spec.ts. Navigates to /_dev/design-system, waits for fonts to load (await page.evaluate(() => document.fonts.ready)), captures full-page screenshot via toHaveScreenshot. Also fires the toast trigger button and captures a second screenshot with all 3 toasts visible. For Stamp, before navigating set page.emulateMedia({ reducedMotion: 'reduce' }) so the framer-motion animation is disabled deterministically for the snapshot. Visual snapshots are committed as goldens. Edit .github/workflows/phase-1-gates.yml (Plan 03 baseline). Add a final job phase-1-complete-gate with: if: startsWith(github.ref, 'refs/tags/phase-1-complete'). Job runs all the following in series: (a) foundry-tests with FOUNDRY_PROFILE=ci forge test (1000-run fuzz), (b) parity-diff via pnpm run parity:diff, (c) AST test pnpm --filter @call-it/web vitest run tests/privy-provider-order.ast.test.ts, (d) full Playwright suite covering signin, onboarding, new-call-publish, utc-day-boundary, paymaster-cap-handoff, address-book-cooldown, feed-shell, profile-shell, visual-smoke, design-system-snap, plus the og-fallback regression from Phase 0, (e) ESLint via pnpm --filter @call-it/ui lint (no-display-grid on the Receipt component), (f) Phase 0 grep guards via pnpm run grep-guards (USDC paste, pragma pin, env-network). Job needs Foundry installer + pnpm + Node 22 + Playwright with --with-deps browser install. Document the tag-gating in 01-10-SUMMARY.md (Task 3) so the operator knows git tag phase-1-complete-YYYYMMDD followed by git push --tags triggers the gate.</action>
  <verify>
    <automated>pnpm --filter @call-it/web exec playwright test tests/visual-smoke.spec.ts tests/design-system-snap.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - .planning/REQUIREMENTS.md AUTH-27 text contains "Circle USDC Paymaster" (grep matches once); AUTH-29 text contains "Circle USDC Paymaster" + "EIP-2612"
    - .planning/PROJECT.md Key Decisions table contains a row referencing "D-06" + "Circle USDC Paymaster"
    - apps/web/tests/visual-smoke.spec.ts exists with 4 tests (one per Phase 1 page) using toHaveScreenshot
    - apps/web/tests/design-system-snap.spec.ts exists with primitives snapshots; goldens committed to apps/web/tests/__screenshots__/
    - apps/web/app/_dev/design-system/page.tsx exists and renders every primitive variant
    - Playwright visual + design-system tests pass against the baseline goldens
    - .github/workflows/phase-1-gates.yml contains a phase-1-complete-gate job gated by if: startsWith(github.ref, 'refs/tags/phase-1-complete')
    - The phase-1-complete-gate job runs: foundry CI fuzz, parity-diff, AST test, full Playwright matrix, ESLint, Phase 0 grep guards
    - All earlier-plan e2e tests still pass (regression check across the entire Phase 1 surface)
  </acceptance_criteria>
  <done>REQUIREMENTS.md + PROJECT.md amended for D-06; visual + design-system snapshots in CI; phase-1-complete-gate is the canonical gate that runs the entire Phase 1 invariant pile in one tag-triggered job.</done>
</task>

<task type="auto">
  <name>Task 3: Phase 1 SUMMARY + 158-REQ coverage audit + STATE.md handoff to Phase 1.5</name>
  <read_first>
    - All 9 prior plan SUMMARYs (01-01-SUMMARY.md through 01-09-SUMMARY.md)
    - .planning/ROADMAP.md Phase 1 requirements list (158 IDs)
    - .planning/STATE.md (current position + pending items)
    - .planning/phases/01-core-contracts-auth-frontend-skeleton/01-CONTEXT.md (locked decisions D-01..D-36)
    - .planning/phases/00-foundation/VERIFICATION.md (Phase 0 model for the verification doc)
  </read_first>
  <files>.planning/phases/01-core-contracts-auth-frontend-skeleton/01-10-SUMMARY.md, .planning/STATE.md</files>
  <action>Write .planning/phases/01-core-contracts-auth-frontend-skeleton/01-10-SUMMARY.md consolidating Phase 1 closure. Required sections in this order: (1) Coverage Audit — table listing every one of the 158 Phase 1 REQ-IDs from ROADMAP.md and the plan(s) that addressed each. Generate by greping every Plan's requirements: frontmatter via grep -E "^  - (AUTH|CALL|REP|SAFETY|UI|OPS)-[0-9]+$" .planning/phases/01-core-contracts-auth-frontend-skeleton/01-*-PLAN.md and asserting set equality with the ROADMAP list. Any REQ-ID not covered produces a CRITICAL FINDING that surfaces to the operator. (2) Locked Decisions Closure — table of D-01..D-36 with status per decision (implemented as specified / deviated with rationale). Note D-06 explicitly as deviation requiring REQUIREMENTS.md amendment (which Task 2 already did). (3) Pitfall Closure Status — table of Pitfalls 1, 12, 13, 14, 15, 16, 20 + new Phase 1 pitfalls A/B/C/D with the file + test that enforces each. (4) Outstanding Items for Phase 1.5 — anything deferred: social linking on-chain (linkTwitter / linkFarcaster relayer-side wiring), From your X / From your Farcaster feed sections, VERIFIED badges with on-chain proof verification. (5) Outstanding Items for Phase 7 — /[handle] sugar route, OG card variants 1-4 (Live, Settled, DuelSettled, CallerExited), subgraph publish from Studio to Decentralized Network on Arbitrum. (6) Performance Metrics — recorded gas cost of createCall from Plan 02's --gas-report, /api/feed endpoint P50 + P95 latency from Plan 09's observability, paymaster sponsorship throughput from Plan 07, total Playwright suite runtime. (7) External Surfaces Operator Must Maintain — Privy app config (Plan 05), Alchemy AA policy (Plans 05+07), Coinbase Onramp config (Plan 06), Circle paymaster monitoring (Plan 07 — daily caps the contract enforces independently of our relayer), Fly Postgres backup schedule (Plan 01), Subgraph Studio key rotation (Plan 10). (8) Deploy + Tag Procedure — exact command sequence for the operator: git checkout master; pnpm install; pnpm turbo run build; git tag phase-1-complete-YYYYMMDD; git push --tags. This triggers phase-1-complete-gate workflow defined in Task 2. After green run, operator opens .planning/STATE.md and updates the position to Phase 1.5. Update .planning/STATE.md frontmatter: set completed_phases to 2, last_updated to today's date, update current_focus to Phase 1.5 (parallel with Phase 2). Append two rows to the Accumulated Context Decisions section: a row for D-06 spec deviation with the rationale, and a row for Plan 01's exact dependency version locks (versions.lock.json). Append two rows to Blockers/Concerns: subgraph monthly query-budget concern ($4 per 100K queries past the 100K free tier), and Privy app secret rotation runbook (operator runbook needed before Phase 6 multisig promotion). If the coverage audit finds ANY REQ-ID uncovered, do NOT mark the phase complete — instead emit a "## ⚠ Source Audit: Unplanned Items Found" block in the SUMMARY listing the uncovered IDs and the operator must run /gsd-plan-phase --gaps to close them before tagging.</action>
  <verify>
    <automated>echo "Coverage audit start" &amp;&amp; grep -hE "^  - (AUTH|CALL|REP|SAFETY|UI|OPS)-[0-9]+$" .planning/phases/01-core-contracts-auth-frontend-skeleton/01-*-PLAN.md | sort -u | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/01-core-contracts-auth-frontend-skeleton/01-10-SUMMARY.md exists with all 8 required sections in order
    - Coverage Audit section lists every 158 Phase 1 REQ-ID from ROADMAP.md mapped to at least one plan
    - Locked Decisions Closure section covers D-01..D-36 with a status indicator per decision
    - Pitfall Closure Status section covers Pitfalls 1, 12, 13, 14, 15, 16, 20 + A, B, C, D each with file + test reference
    - Deploy + Tag Procedure section gives exact git commands to tag and push
    - .planning/STATE.md is updated: completed_phases set to 2 (Phase 0 + Phase 1); current_focus updated; D-06 and versions.lock.json added to Decisions; subgraph monthly cost + Privy webhook secret rotation added to Blockers/Concerns
    - If any REQ-ID uncovered, SUMMARY has a Source Audit Unplanned Items Found block — gates the operator before tagging
    - Output of the verify command shows the number of unique REQ-IDs covered across all plans (should be ≥ 158 — duplicates are OK, missing IDs are NOT)
  </acceptance_criteria>
  <done>Phase 1 SUMMARY captures full coverage, locked-decision status, pitfall closure, performance metrics, and the operator's deploy + tag procedure; STATE.md handed off to Phase 1.5; Phase 1 ready for /gsd-verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Subgraph deploy → Sepolia Studio | Studio deploy key held by operator; CI does NOT deploy (manual operator step) |
| Subgraph schema → frontend reads | Schema versioning happens via Studio version pins; Plan 09 relayer reads from the recorded URL |
| REQUIREMENTS.md edit → downstream phases | All future phases read REQUIREMENTS.md; D-06 amendment is the spec-of-record |
| Visual snapshots → CI baseline | Goldens committed to apps/web/tests/__screenshots__/; any drift trips the test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-67 | Tampering (Pitfall C) | Subgraph schema drift between Phase 0 stubs and Phase 1 real events | mitigate | Task 1 regenerates ABIs via prebuild copy from packages/contracts/out; AS compilation fails loudly on event signature mismatch |
| T-01-68 | Information disclosure | SUBGRAPH_DEPLOY_KEY leak | mitigate | Key held by operator only; CI does NOT have access; documented in Task 1 + Task 3 operator runbook |
| T-01-69 | Tampering | Visual snapshot baseline corrupted by future regression | mitigate | maxDiffPixelRatio 0.02 absorbs anti-aliasing noise; goldens committed to version control; any drift requires explicit update via --update-snapshots and code review |
| T-01-70 | Information disclosure | /_dev/design-system page accessible in production | mitigate | Next.js _dev convention typically ignored in production routing; defensive env guard NEXT_PUBLIC_DEV_ROUTES=1 required; documented in operator runbook |
| T-01-71 | DoS | Subgraph indexing failure not surfaced | mitigate | _meta.hasIndexingErrors queryable; Plan 09 feed race falls through to polled-events worker; Phase 0 telegram alert on indexing-error event (extend Phase 0 alert dispatcher) |
</threat_model>

<verification>
After all three tasks complete:

1. pnpm --filter @call-it/subgraph build exits 0
2. pnpm --filter @call-it/web exec playwright test tests/visual-smoke.spec.ts tests/design-system-snap.spec.ts exits 0
3. grep -c "Circle USDC Paymaster" .planning/REQUIREMENTS.md returns 2 (AUTH-27 + AUTH-29 amended)
4. grep -c "D-06" .planning/PROJECT.md returns at least 1 (Key Decisions row)
5. .github/workflows/phase-1-gates.yml contains the line phase-1-complete-gate (grep matches)
6. .planning/phases/01-core-contracts-auth-frontend-skeleton/01-10-SUMMARY.md exists with all 8 sections
7. .planning/STATE.md frontmatter completed_phases is 2
8. All Phase 1 Playwright + Vitest + Foundry tests still pass (regression)
9. pnpm run grep-guards still passes
10. pnpm run parity:diff still passes
</verification>

<success_criteria>
1. Subgraph indexes Phase 1 contract events on Sepolia Studio within ~30s (Pitfall C closed)
2. REQUIREMENTS.md AUTH-27 + AUTH-29 amended verbatim for D-06; PROJECT.md captures the decision
3. Visual smoke snapshots of 4 Phase 1 pages captured as goldens
4. Design-system snap of every @call-it/ui primitive captured as goldens (regression defense for Phase 2-9)
5. phase-1-complete-gate CI job is the single tag-triggered canonical Phase 1 gate
6. 01-10-SUMMARY.md provides 158-REQ coverage audit, locked-decisions closure, pitfall closure, performance metrics, external surface inventory, and deploy+tag procedure
7. STATE.md handed off to Phase 1.5 with D-06 and operational concerns recorded
</success_criteria>

<output>
After completion, .planning/phases/01-core-contracts-auth-frontend-skeleton/01-10-SUMMARY.md is the canonical Phase 1 closure document (Task 3 writes it). No additional summary file needed.
</output>
