---
phase: 01-core-contracts-auth-frontend-skeleton
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/ui/package.json
  - packages/ui/tsconfig.json
  - packages/ui/src/index.ts
  - packages/ui/tailwind.preset.ts
  - packages/ui/vitest.config.ts
  - packages/ui/.eslintrc.cjs
  - packages/shared/src/schemas/env-config.ts
  - packages/shared/src/index.ts
  - apps/web/package.json
  - apps/relayer/package.json
  - apps/relayer/drizzle.config.ts
  - apps/relayer/src/db/schema.ts
  - apps/relayer/src/db/client.ts
  - apps/relayer/src/db/index.ts
  - apps/relayer/src/db/migrations/0001_address_book.sql
  - apps/relayer/src/db/migrations/0002_auth_methods.sql
  - apps/relayer/src/db/migrations/0003_onboarding_state.sql
  - packages/config/versions.lock.json
  - .planning/phases/01-core-contracts-auth-frontend-skeleton/WAVE-0-VERIFICATION.md
autonomous: false
requirements:
  - AUTH-31
  - AUTH-32
  - SAFETY-18
tags:
  - workspace-bootstrap
  - dependencies
  - drizzle
  - postgres
  - ui-package
  - wave-0
user_setup:
  - service: fly-postgres
    why: "Per D-07 — Fly Postgres in iad is the canonical app DB for Phase 1+ (address_book, auth_methods, onboarding_state)"
    env_vars:
      - name: POSTGRES_URL
        source: "Output of `fly postgres create --region iad --name call-it-pg --vm-size shared-cpu-1x --volume-size 1 --initial-cluster-size 1`; the printed `DATABASE_URL` is stored in GCP Secret Manager"
      - name: ENS_MAINNET_RPC_URL
        source: "Alchemy Ethereum Mainnet free-tier RPC URL (separate from Arbitrum keys per Phase 0 D-09 per-network IAM isolation)"
    dashboard_config:
      - task: "Provision Fly Postgres in iad region attached to the existing call-it-relayer Fly app on the private network"
        location: "`fly postgres create --region iad --name call-it-pg` then `fly postgres attach call-it-pg --app call-it-relayer`"
      - task: "Store POSTGRES_URL in GCP Secret Manager for BOTH call-it-sepolia and call-it-mainnet projects per Phase 0 D-09"
        location: "GCP Console → Security → Secret Manager → Create Secret named POSTGRES_URL in each project"
      - task: "Store ENS_MAINNET_RPC_URL in GCP Secret Manager for both projects (D-13)"
        location: "GCP Console → Security → Secret Manager"
  - service: circle-paymaster
    why: "Per D-04 — Wave 0 must re-verify the Circle USDC Paymaster on Arbitrum One address (RESEARCH MEDIUM confidence on 0x6C973eBe80dCD8660841D4356bf15c32460271C9) and locate a Sepolia equivalent if one exists"
    env_vars:
      - name: NEXT_PUBLIC_CIRCLE_PAYMASTER_ADDRESS
        source: "Verified against current Arbitrum third-party docs and Circle's public paymaster announcement. If the verified address differs from RESEARCH's 0x6C97... value, the operator updates packages/shared/src/constants/addresses.ts and the env schema during this task and records the discrepancy in WAVE-0-VERIFICATION.md"
    dashboard_config:
      - task: "Open https://docs.arbitrum.io/for-devs/third-party-docs/Circle/usdc-paymaster-quickstart, copy current mainnet paymaster address verbatim, paste into WAVE-0-VERIFICATION.md and packages/shared/src/constants/addresses.ts"
        location: "Arbitrum third-party docs"
      - task: "Open https://www.circle.com/blog/how-to-integrate-circle-paymaster-to-enable-users-to-pay-gas-fees-with-their-usdc-balance, scan for a Sepolia paymaster address; if none documented, write 'no Sepolia paymaster — Sepolia staging will use Alchemy sponsorship for ALL tx and Circle USDC handoff is verified only on the §19.11 mainnet smoke test' into WAVE-0-VERIFICATION.md"
        location: "Circle blog"
must_haves:
  truths:
    - "`pnpm install` from monorepo root succeeds with @call-it/ui as a recognized workspace member"
    - "Fly Postgres is reachable from a relayer dev shell and the three Drizzle migrations are applied"
    - "Solid evidence that the runtime @privy-io/wagmi 4.0.8 API exposes createConfig + WagmiProvider with the same surface RESEARCH Pattern 3 assumes (or surfaced deviations documented for downstream plans)"
    - "Circle USDC Paymaster mainnet address is re-verified against current Arbitrum docs and recorded in addresses.ts + WAVE-0-VERIFICATION.md"
    - "Phase 1 envs (POSTGRES_URL, ENS_MAINNET_RPC_URL, NEXT_PUBLIC_PRIVY_APP_ID, NEXT_PUBLIC_ALCHEMY_AA_POLICY_ID, NEXT_PUBLIC_RELAYER_BASE_URL, NEXT_PUBLIC_CALL_REGISTRY_ADDRESS, NEXT_PUBLIC_PROFILE_REGISTRY_ADDRESS, NEXT_PUBLIC_CIRCLE_PAYMASTER_ADDRESS, ALCHEMY_PAYMASTER_ADDRESS, PRIVY_APP_SECRET, PRIVY_WEBHOOK_SECRET) are validated by the Phase-0-extended Zod env schema and the Phase 0 grep guards still pass"
  artifacts:
    - path: "packages/ui/package.json"
      provides: "@call-it/ui workspace manifest with @call-it/config + @call-it/shared workspace deps"
      contains: "@call-it/ui"
    - path: "packages/ui/tailwind.preset.ts"
      provides: "Shared Tailwind preset consumed by apps/web (extracted neobrutalist tokens from apps/web/tailwind.config.ts)"
      contains: "brand-accent"
    - path: "packages/ui/.eslintrc.cjs"
      provides: "Lints the @call-it/ui workspace via the shared @call-it/config base — extended to scope the existing no-display-grid rule onto packages/ui/src/compound/Receipt.tsx and children (Pitfall 15)"
      contains: "no-display-grid"
    - path: "apps/relayer/src/db/schema.ts"
      provides: "Drizzle schema for address_book, auth_methods, onboarding_state (D-07/D-08/D-10/D-32)"
      contains: "addressBook"
    - path: "apps/relayer/src/db/client.ts"
      provides: "getDb() singleton+memo factory mirroring apps/relayer/src/lib/redis.ts pattern"
      contains: "getDb"
    - path: "packages/shared/src/schemas/env-config.ts"
      provides: "Extended Zod env schema covering all Phase 1 new envs; rejects mismatched network/chain pairings per Phase 0 pattern"
      contains: "NEXT_PUBLIC_CIRCLE_PAYMASTER_ADDRESS"
    - path: "packages/config/versions.lock.json"
      provides: "Locked exact versions of @privy-io/wagmi (verified runtime value), @account-kit/infra, drizzle-orm, drizzle-kit, react-hook-form, @hookform/resolvers, class-variance-authority, ts-morph (RESEARCH Assumptions A3-A6 closed)"
      contains: "@privy-io/wagmi"
    - path: ".planning/phases/01-core-contracts-auth-frontend-skeleton/WAVE-0-VERIFICATION.md"
      provides: "Operator-facing record of all Wave 0 outputs the Phase 1 build depends on: Circle paymaster addresses, Privy wagmi v4 surface, Alchemy aa-sdk paymaster RPC choice, exact installed dependency versions, Fly Postgres connection string proof"
      contains: "Circle USDC Paymaster"
  key_links:
    - from: "apps/relayer/src/db/client.ts"
      to: "POSTGRES_URL secret in GCP Secret Manager"
      via: "process.env.POSTGRES_URL injected from Fly secrets (synced from GCP Secret Manager per Phase 0 D-09)"
      pattern: "process\\.env\\.POSTGRES_URL"
    - from: "packages/ui/tailwind.preset.ts"
      to: "apps/web/tailwind.config.ts"
      via: "presets: [uiPreset] reference in apps/web tailwind config"
      pattern: "presets:.*uiPreset|from .*packages/ui"
    - from: "packages/shared/src/schemas/env-config.ts"
      to: ".github/workflows/grep-guards.yml + apps/web/lib + apps/relayer/src/env.ts"
      via: "Phase 0 env grep guard still passes; new envs are consumed by Wave 1+ via the validated schema"
      pattern: "EnvConfigSchema"
---

<objective>
Wave 0 foundation slice. Install the Phase 1 NEW dependencies, create the `@call-it/ui` workspace shell (no components yet — that's Plan 04), provision Fly Postgres + wire Drizzle ORM with the 3-table schema, extend the Phase 0 env Zod schema with every Phase 1 environment variable, lock the runtime versions of the new dependencies into `packages/config/versions.lock.json`, and re-verify the two MEDIUM-confidence external facts from RESEARCH: the Circle USDC Paymaster Arbitrum address and the actual `@privy-io/wagmi@4.0.8` API surface. Every downstream Plan 02-10 task assumes this foundation is in place.

Purpose: Without `packages/ui` declared as a workspace, plans 04/05/06/08/09 cannot import design primitives. Without Drizzle + Postgres, plans 06/07 (onboarding, address book, 24h cooldown) cannot persist state. Without the env schema extended, the CI grep guard will reject the new envs every other plan introduces. Without the Wave 0 verification record, plans 05 and 07 risk wiring against a wrong Circle paymaster address or a phantom `@privy-io/wagmi` API.

Output: A buildable monorepo where `pnpm install && pnpm build` succeeds with the new `@call-it/ui` workspace present (empty), the relayer has a working Postgres connection backed by 3 applied migrations, every env required by Phase 1 is declared and validated, and the planner's open MEDIUM-confidence assumptions are closed in writing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-contracts-auth-frontend-skeleton/01-CONTEXT.md
@.planning/phases/01-core-contracts-auth-frontend-skeleton/01-RESEARCH.md
@.planning/phases/01-core-contracts-auth-frontend-skeleton/01-PATTERNS.md
@.planning/phases/01-core-contracts-auth-frontend-skeleton/01-VALIDATION.md
@.planning/phases/00-foundation/00-01-SUMMARY.md
@.planning/phases/00-foundation/00-02-SUMMARY.md

<interfaces>
<!-- Key Phase 0 surfaces the executor extends. Use these signatures directly. -->

From packages/shared/src/schemas/env-config.ts (Phase 0):
```
export const EnvConfigSchema = z.object({ NEXT_PUBLIC_NETWORK, NEXT_PUBLIC_CHAIN_ID, ... }).superRefine((data, ctx) => { /* cross-field network/chain coupling */ });
export type EnvConfig = z.infer<typeof EnvConfigSchema>;
```

From apps/relayer/src/lib/redis.ts (Phase 0):
```
export function getRedis(): Redis;          // singleton + memo
export async function _resetRedisForTesting(): Promise<void>;
```

From packages/shared/package.json (Phase 0 — manifest analog):
```
{ "name": "@call-it/shared", "main": "./src/index.ts", "types": "./src/index.ts", "exports": { ".": "./src/index.ts" } }
```

From packages/config/eslint/no-display-grid.js (Phase 0):
```
module.exports = { meta: { type: 'problem', messages: { noDisplayGrid: '...' } }, create(context) { /* JSXAttribute style={{ display: 'grid' }} detector */ } };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create @call-it/ui workspace shell + tailwind preset + ESLint base wiring</name>
  <read_first>
    - packages/shared/package.json (workspace manifest analog — PATTERNS § Pattern H)
    - packages/shared/tsconfig.json (workspace tsconfig analog)
    - apps/web/tailwind.config.ts (color tokens to extract — PATTERNS § file 9)
    - packages/config/eslint/base.js (ESLint flat config + no-display-grid rule scoping — PATTERNS § Pattern I, lines 42-55)
    - packages/config/eslint/no-display-grid.js (the rule body — DO NOT duplicate)
    - .planning/phases/01-core-contracts-auth-frontend-skeleton/01-PATTERNS.md § 9-12 (workspace + preset + ESLint scoping)
    - .planning/phases/01-core-contracts-auth-frontend-skeleton/01-CONTEXT.md § D-15, D-16, D-17, D-18, D-19
    - pnpm-workspace.yaml (verify packages/ui is matched by the existing workspace glob; if not, add `packages/ui`)
  </read_first>
  <files>packages/ui/package.json, packages/ui/tsconfig.json, packages/ui/src/index.ts, packages/ui/tailwind.preset.ts, packages/ui/vitest.config.ts, packages/ui/.eslintrc.cjs, packages/config/eslint/base.js, apps/web/tailwind.config.ts, apps/web/package.json, pnpm-workspace.yaml (if needed)</files>
  <action>Per D-15 create a new `@call-it/ui` workspace at `packages/ui/`. The package.json mirrors `packages/shared/package.json` verbatim (PATTERNS § Pattern H): name `@call-it/ui`, version `0.0.1`, private true, `main`/`types`/`exports` pointing at `./src/index.ts`, scripts `build` (`tsc --build`), `test` (`vitest run`), `lint` (`eslint src/`). Dependencies: `@call-it/shared: workspace:*`, `class-variance-authority` (verified version pinned exact from versions.lock.json — Task 3), `clsx` (exact), `tailwind-merge` (exact), `@radix-ui/react-dialog` `@radix-ui/react-popover` `@radix-ui/react-tooltip` `@radix-ui/react-slider` (exact versions), `framer-motion` (exact). Peer deps: `react` and `react-dom` (^19). devDependencies: `@call-it/config: workspace:*`, `typescript ^5.6.0`, `vitest ^3.0.0`, `tailwindcss ^3.4`, `postcss`, `autoprefixer`. Create `packages/ui/tsconfig.json` extending `@call-it/config/tsconfig/library.json` exactly as `packages/shared/tsconfig.json` does. Create `packages/ui/src/index.ts` with a header comment "Phase 1 P04 will populate this barrel" and an empty `export {};` so `tsc --build` succeeds. Create `packages/ui/vitest.config.ts` mirroring `apps/relayer/vitest.config.ts` shape. Create `packages/ui/tailwind.preset.ts` per PATTERNS § file 9 (lines 503-526): extract the `theme.extend.colors` (brand-bg `#09090E`, brand-accent `#E8F542`, brand-text `#FFFFFF`, brand-muted `#A1A1AA`, brand-border `#27272A`, brand-surface `#18181B`, outcome-win `#22C55E`, outcome-loss `#EF4444`, outcome-contrarian `#A855F7`), the `fontFamily` (display Syne, body Space Grotesk, mono JetBrains Mono), and the `borderWidth` (3px, 4px) from `apps/web/tailwind.config.ts` into the preset. Then reduce `apps/web/tailwind.config.ts` to `{ presets: [uiPreset], content: [...existing content globs..., '../../packages/ui/src/**/*.{ts,tsx}'] }`. Create `packages/ui/.eslintrc.cjs` that imports the shared base from `@call-it/config/eslint/base.js`. EXTEND `packages/config/eslint/base.js` per PATTERNS § Pattern I lines 42-55: add ONE new `files` block scoped to `['packages/ui/src/compound/Receipt.tsx', 'packages/ui/src/compound/Receipt/**/*.{ts,tsx}']` that activates the existing `no-display-grid` rule — do NOT duplicate the rule logic. Add `packages/ui` to the `apps/web/package.json` dependencies as `"@call-it/ui": "workspace:*"`. Verify `packages/ui` is matched by the existing `pnpm-workspace.yaml` globs; if not, add the line `- packages/ui`. Per D-15/D-16: DO NOT install `shadcn/ui`. Use `pnpm install` from monorepo root to apply.</action>
  <verify>
    <automated>pnpm install --frozen-lockfile=false &amp;&amp; pnpm --filter @call-it/ui build &amp;&amp; pnpm --filter @call-it/ui lint &amp;&amp; pnpm --filter @call-it/web build &amp;&amp; node -e "console.log(require('./packages/ui/package.json').name === '@call-it/ui' ? 'OK' : (() =&gt; { process.exit(1); })())"</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/ui/package.json` exists with `"name": "@call-it/ui"` and a `"@call-it/shared": "workspace:*"` dependency entry
    - `pnpm install` resolves `@call-it/ui` from the workspace (no warning about missing package)
    - `pnpm --filter @call-it/ui build` exits 0 (empty package, but tsc has a valid project)
    - `pnpm --filter @call-it/ui lint` exits 0
    - `apps/web/tailwind.config.ts` contains an import of `packages/ui/tailwind.preset` and lists it under `presets`
    - `pnpm --filter @call-it/web build` still exits 0 (regression check on the existing Phase 0 OG fallback build)
    - `packages/config/eslint/base.js` has exactly ONE new `files` array containing `packages/ui/src/compound/Receipt.tsx` AND `packages/ui/src/compound/Receipt/**/*.{ts,tsx}` with the `no-display-grid` rule attached; no duplicate of the rule body anywhere
    - `apps/web/package.json` lists `"@call-it/ui": "workspace:*"` in dependencies
    - Grep `pnpm-workspace.yaml` and confirm `packages/ui` matches a glob (either explicit line `- packages/ui` or via `- packages/*`)
  </acceptance_criteria>
  <done>The `@call-it/ui` workspace is installable, buildable, lintable, and consumed-as-workspace-dep by `apps/web`; the no-display-grid ESLint rule is scoped to the future `<Receipt>` file path without rule duplication; the Phase 0 web build still passes.</done>
</task>

<task type="auto">
  <name>Task 2: Provision Fly Postgres + Drizzle schema (3 tables) + db client singleton + applied migrations</name>
  <read_first>
    - apps/relayer/src/lib/redis.ts (singleton+memo pattern — PATTERNS § Pattern G, lines 17-56)
    - apps/relayer/package.json (current relayer deps + scripts to extend)
    - apps/relayer/src/env.ts (current env schema; Postgres URL must be loaded the same way)
    - apps/relayer/src/index.ts (registration point — DO NOT touch yet; routes land in Plans 06/07/08/09)
    - .planning/phases/01-core-contracts-auth-frontend-skeleton/01-RESEARCH.md § "Common Operation 2" (lines 1312-1342) — exact Drizzle schema shape
    - .planning/phases/01-core-contracts-auth-frontend-skeleton/01-PATTERNS.md § file 20 (db/client.ts singleton; db/schema.ts shape; drizzle.config.ts)
    - .planning/phases/01-core-contracts-auth-frontend-skeleton/01-CONTEXT.md § D-07, D-08, D-10, D-32
    - apps/relayer/test/redis.test.ts (closest test fixture for singleton resets — model the future db tests on it)
  </read_first>
  <files>apps/relayer/package.json, apps/relayer/drizzle.config.ts, apps/relayer/src/db/schema.ts, apps/relayer/src/db/client.ts, apps/relayer/src/db/index.ts, apps/relayer/src/db/migrations/0001_address_book.sql, apps/relayer/src/db/migrations/0002_auth_methods.sql, apps/relayer/src/db/migrations/0003_onboarding_state.sql, apps/relayer/src/env.ts</files>
  <action>Add to `apps/relayer/package.json` dependencies (exact versions from Task 3's versions.lock.json): `drizzle-orm`, `postgres` (the postgres-js driver per RESEARCH "Common Operation 2"; NOT `pg` — RESEARCH line 1025 uses `postgres-js` adapter), and devDependencies: `drizzle-kit`. Add scripts: `"db:generate": "drizzle-kit generate"`, `"db:push": "drizzle-kit push"`, `"db:migrate": "drizzle-kit migrate"`. Create `apps/relayer/drizzle.config.ts` per Drizzle docs: schema path `./src/db/schema.ts`, out `./src/db/migrations`, dialect `postgresql`, credentials reading from `process.env.POSTGRES_URL`. Create `apps/relayer/src/db/schema.ts` per RESEARCH "Common Operation 2" verbatim — three tables: `addressBook` with columns `id serial PK`, `privyUserId varchar(128) NOT NULL`, `address varchar(42) NOT NULL`, `label text`, `addedAt timestamp DEFAULT now() NOT NULL`, `removedAt timestamp` (soft delete per D-08); `authMethods` with `id serial PK`, `privyUserId varchar(128) NOT NULL`, `authType varchar(32) NOT NULL` (`'google'|'twitter'|'wallet'`), `linkedAt timestamp DEFAULT now() NOT NULL`; `onboardingState` with `privyUserId varchar(128) PK`, `currentStep integer DEFAULT 1 NOT NULL`, `handleSetAt timestamp`, `socialsStepCompletedAt timestamp`, `followgraphOptinAt timestamp`, `taglineCommittedAt timestamp`. Add indexes: `address_book_user_idx` on `(privyUserId)`, `auth_methods_user_idx` on `(privyUserId, linkedAt)`. Per D-08 "never delete, only soft-remove" — no DELETE statements ever; downstream Plan 07 uses `removedAt IS NULL` filter. Create `apps/relayer/src/db/client.ts` using the EXACT singleton+memo pattern from `apps/relayer/src/lib/redis.ts` (PATTERNS § Pattern G): `let _db: ReturnType<typeof drizzle> | undefined;`, `export function getDb()` that lazily constructs `drizzle(postgres(process.env.POSTGRES_URL!, { max: 10, idle_timeout: 20 }), { schema })`, an `_resetDbForTesting()` helper mirroring `_resetRedisForTesting`, and a `getLogger().info({ event: 'postgres_connection_established' }, ...)` log line on first connection. Create `apps/relayer/src/db/index.ts` barrel exporting `getDb`, `_resetDbForTesting`, and the three table objects. Extend `apps/relayer/src/env.ts` Zod schema to require `POSTGRES_URL` (z.string().url() — accept both `postgres://` and `postgresql://` prefixes) and `ENS_MAINNET_RPC_URL` (z.string().url()). Run `pnpm --filter @call-it/relayer exec drizzle-kit generate` to produce migration SQL files in `apps/relayer/src/db/migrations/` — rename the generated files to `0001_address_book.sql`, `0002_auth_methods.sql`, `0003_onboarding_state.sql` (or one combined `0001_phase_1_initial.sql` if drizzle-kit emits one file; either is acceptable as long as the SQL exists in version control). DO NOT register the db in `src/index.ts` — that wiring lands in Plan 06.</action>
  <verify>
    <automated>pnpm --filter @call-it/relayer install &amp;&amp; pnpm --filter @call-it/relayer typecheck &amp;&amp; pnpm --filter @call-it/relayer exec drizzle-kit generate --dry-run || echo 'OK if exits with valid schema check'</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/relayer/src/db/schema.ts` exports the three tables `addressBook`, `authMethods`, `onboardingState` with the column shapes above (grep `pgTable\\('address_book'`, `pgTable\\('auth_methods'`, `pgTable\\('onboarding_state'` each returns exactly one match)
    - File `apps/relayer/src/db/client.ts` exports `getDb` and `_resetDbForTesting`; `getDb` uses lazy memoization (grep `let _db:.*\\| undefined` matches)
    - `apps/relayer/src/env.ts` schema rejects a config missing `POSTGRES_URL` (Vitest can be added later, but the schema must reject in TS)
    - At least one SQL file in `apps/relayer/src/db/migrations/` contains `CREATE TABLE` statements for all three tables (combined or separate)
    - `apps/relayer/package.json` lists `drizzle-orm` + `postgres` in dependencies and `drizzle-kit` in devDependencies (with versions matching versions.lock.json from Task 3)
    - `pnpm --filter @call-it/relayer typecheck` exits 0
    - `pnpm --filter @call-it/relayer build` (or `tsc --noEmit` via the existing relayer build) exits 0
    - `apps/relayer/src/index.ts` is UNTOUCHED — no `getDb` import added yet (Plan 06 will add it)
  </acceptance_criteria>
  <done>Drizzle schema for the 3 Phase 1 tables compiles; `getDb()` follows the relayer's existing singleton pattern; migration SQL exists in version control; the relayer still builds; the env schema requires `POSTGRES_URL` and `ENS_MAINNET_RPC_URL`.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Operator — Verify Wave 0 external facts + lock dependency versions</name>
  <read_first>
    - .planning/phases/01-core-contracts-auth-frontend-skeleton/01-RESEARCH.md § Assumptions Log (A1-A12) + § Open Questions 1, 2, 5
    - .planning/phases/01-core-contracts-auth-frontend-skeleton/01-CONTEXT.md § D-04, D-05, D-06 (Circle paymaster) + D-36 (wagmi chain config)
    - CLAUDE.md § Technology Stack ("@privy-io/wagmi pinned to 4.0.8 in actual package.json" callout)
    - apps/web/package.json (verify @privy-io/wagmi installed version)
    - node_modules/@privy-io/wagmi/dist/index.d.ts (verify runtime API surface — does v4 still export createConfig + WagmiProvider?)
    - packages/shared/src/constants/addresses.ts (where the Circle paymaster address will land)
    - packages/shared/src/schemas/env-config.ts (Phase 0 baseline — extended here)
  </read_first>
  <action>Operator performs the 6-step verification sequence in <how-to-verify> below. Each step writes a dated entry into .planning/phases/01-core-contracts-auth-frontend-skeleton/WAVE-0-VERIFICATION.md recording the verified external fact (Circle paymaster address, Sepolia paymaster status, Privy wagmi v4 API surface, Alchemy paymaster RPC choice, locked dependency versions). The output of step 5 also writes packages/config/versions.lock.json. After all 6 steps the operator types approved as the resume signal. If any verification step surfaces a discrepancy with RESEARCH (different paymaster address, different Privy wagmi API), the operator pauses and surfaces the diff to the planner BEFORE Plan 02 starts so downstream slices (05, 07) get the corrected values.</action>
  <what-built>
    Wave 0 needs three verifications the planner could not do at planning time without committing the wrong values to source:
    1. The Circle USDC Paymaster Arbitrum One address (RESEARCH MEDIUM-confidence 0x6C973eBe80dCD8660841D4356bf15c32460271C9 — may have been redeployed)
    2. Sepolia equivalent of the Circle paymaster (RESEARCH says "Wave 0 task — verify with Arbitrum + Circle docs")
    3. The actual `@privy-io/wagmi@4.0.8` API surface vs RESEARCH Pattern 3 which was written against v1.32.5 docs
    4. Which Alchemy paymaster RPC the bundler invokes: standard ERC-7677 `pm_getPaymasterStubData` OR Alchemy custom `alchemy_requestGasAndPaymasterAndData` (RESEARCH Open Question 1)
    5. Exact installed versions of every Phase 1 NEW dependency (Assumptions A3-A7) locked into `packages/config/versions.lock.json`
  </what-built>
  <how-to-verify>
    1. Open `node_modules/@privy-io/wagmi/dist/index.d.ts` (or `*.d.cts`). Search for `export.*createConfig` and `export.*WagmiProvider`. If both exist with signatures compatible with `createConfig({ chains: [arbitrum, arbitrumSepolia], transports: { ... } })`, write to WAVE-0-VERIFICATION.md: "@privy-io/wagmi@4.0.8 exposes createConfig + WagmiProvider matching RESEARCH Pattern 3 — Plan 05 unblocked". If the v4 API has renamed or removed either, document the new signature and STOP — Plan 05 needs revision before proceeding.
    2. Open https://docs.arbitrum.io/for-devs/third-party-docs/Circle/usdc-paymaster-quickstart in a browser. Copy the documented Arbitrum One paymaster address. Compare to `0x6C973eBe80dCD8660841D4356bf15c32460271C9`. If match, write to WAVE-0-VERIFICATION.md: "Circle USDC Paymaster Arbitrum One mainnet address confirmed at <addr> as of <date>". If different, record the actual address, update `packages/shared/src/constants/addresses.ts` to add `CIRCLE_PAYMASTER_ARBITRUM_ONE = <addr>` and the env var `NEXT_PUBLIC_CIRCLE_PAYMASTER_ADDRESS` default in the env schema.
    3. On the same docs page, search for "Sepolia" or "testnet". If a Sepolia paymaster address is documented, record it; otherwise write "No Sepolia paymaster documented as of <date> — Plan 07 implements Circle USDC handoff mainnet-only and Sepolia staging will rely on Alchemy sponsorship for all tx (note for Phase 6 § 19.11 mainnet smoke test)".
    4. Open https://www.alchemy.com/docs/wallets/reference/aa-sdk/core/functions/erc7677Middleware and the gas-policy dashboard documentation. Determine whether enabling a gas policy with a callback URL routes the bundler call to standard ERC-7677 OR Alchemy custom RPC. Record which one in WAVE-0-VERIFICATION.md — Plan 07 paymaster-policy route shape depends on this.
    5. Run `node -e "const fs=require('fs');const w=require('./apps/web/package.json');const r=require('./apps/relayer/package.json');const u=require('./packages/ui/package.json');const out={'@privy-io/wagmi':w.dependencies['@privy-io/wagmi'],'@privy-io/react-auth':w.dependencies['@privy-io/react-auth'],wagmi:w.dependencies.wagmi,viem:w.dependencies.viem,'@tanstack/react-query':w.dependencies['@tanstack/react-query'],'@account-kit/infra':w.dependencies['@account-kit/infra'],'react-hook-form':w.dependencies['react-hook-form'],'@hookform/resolvers':w.dependencies['@hookform/resolvers'],zod:w.dependencies.zod||r.dependencies.zod,'drizzle-orm':r.dependencies['drizzle-orm'],'drizzle-kit':r.devDependencies['drizzle-kit'],postgres:r.dependencies.postgres,'class-variance-authority':u.dependencies['class-variance-authority'],'framer-motion':u.dependencies['framer-motion'],'ts-morph':w.devDependencies['ts-morph']};fs.writeFileSync('packages/config/versions.lock.json',JSON.stringify(out,null,2));console.log(JSON.stringify(out,null,2));"`. Inspect the printed JSON; if any value is `undefined` (dependency not yet installed), pause and install it via `pnpm --filter <package> add <dep>` BEFORE re-running.
    6. Write `.planning/phases/01-core-contracts-auth-frontend-skeleton/WAVE-0-VERIFICATION.md` containing: dated section per item 1-5 above, with the verified address strings, RPC choices, and a "Plan 05 unblocked", "Plan 07 unblocked" green-light statement at the bottom.
  </how-to-verify>
  <acceptance_criteria>
    - If the verified `@privy-io/wagmi@4.0.8` API surface diverges from RESEARCH.md Pattern 3 in a way that affects `createConfig` signature, `WagmiProvider` props, or hook names — halt execution and re-issue Plans 05/06/07 with revised imports BEFORE Wave 2 begins. WAVE-0-VERIFICATION.md MUST record a `## API DIVERGENCE` section in this case, naming each affected plan.
  </acceptance_criteria>
  <resume-signal>Type "approved" when WAVE-0-VERIFICATION.md has been written and the five items above are recorded. Then proceed to Plan 02. If item 1 or 2 surfaced a major discrepancy (different address or different API), respond with the diff so the planner can issue a revision to Plans 05/07 before Wave 2 starts.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → Source code | Wave 0 verification commits external facts (paymaster address, dependency versions) into source — wrong value persists into deploy if not double-checked |
| Postgres ← Relayer | Untrusted user-supplied addresses + handles flow into address_book and onboarding_state via Plans 06/07 routes — Wave 0 establishes schema-level length caps (varchar(42), varchar(128), varchar(32)) |
| Privy webhook → auth_methods | Plan 07 wires; this plan only declares the table — webhook spoof defense lands later |
| Fly secrets → Relayer | POSTGRES_URL and ENS_MAINNET_RPC_URL leave GCP Secret Manager → Fly env vars; cannot leak to frontend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `packages/shared/src/constants/addresses.ts` Circle paymaster entry | mitigate | Wave 0 Task 3 verifies against current Arbitrum + Circle docs and records source URL + verification date in WAVE-0-VERIFICATION.md (T8 — wrong paymaster address strands tx 6+ permits) |
| T-01-02 | Information disclosure | Postgres URL in env | mitigate | POSTGRES_URL fetched at runtime from GCP Secret Manager → Fly secret env injection per Phase 0 D-09; never in `.env` file or version control. Env schema rejects literal `localhost`/`127.0.0.1` connection strings in `mainnet` profile (extend the Phase 0 cross-field refine) |
| T-01-03 | Tampering | Drizzle schema column lengths | mitigate | `varchar(42)` on address column rejects oversize 0x literals; `varchar(128)` on `privy_user_id` matches Privy's documented DID format; varchar(32) on auth_type prevents arbitrary strings |
| T-01-04 | Spoofing | Privy webhook signature secret | accept (deferred to Plan 07) | Wave 0 declares `PRIVY_WEBHOOK_SECRET` env requirement; Plan 07 wires HMAC verification on the webhook route. Risk window during Wave 0 is zero — no webhook route exists yet |
| T-01-05 | Elevation of privilege | Soft-delete pattern (D-08) | mitigate | Schema enforces `removedAt timestamp` nullable; downstream Plan 07 reads filter `WHERE removed_at IS NULL`; no `DELETE` statement is ever issued from relayer code — code review gate in Plan 07 |
| T-01-06 | DoS | drizzle-kit migration on hot Fly Postgres | accept | Phase 1 schema is fresh tables only — no destructive ALTER on existing data. Operator runs `drizzle-kit push` against a freshly-provisioned Postgres in Task 3 with zero rows |
| T-01-07 | Information disclosure | Wave 0 verification doc | accept | WAVE-0-VERIFICATION.md is version-controlled; addresses + RPC choices are public information; no secrets recorded |
</threat_model>

<verification>
After all three tasks complete:

1. `pnpm install --frozen-lockfile=false` exits 0 (workspace recognizes `@call-it/ui`)
2. `pnpm --filter @call-it/ui build` exits 0 (empty TS project compiles)
3. `pnpm --filter @call-it/web build` exits 0 (apps/web still works with new tailwind preset import)
4. `pnpm --filter @call-it/relayer typecheck` exits 0 (env + db client + schema typecheck)
5. `pnpm --filter @call-it/relayer test` runs Phase 0 tests green (regression check — no new db tests yet, Plan 07 adds them)
6. `pnpm run grep-guards` (Phase 0 CI guard) exits 0 — no bridged USDC.e address, no `^0.8.x` pragma, no `arbitrum-sepolia` in mainnet env
7. `cat .planning/phases/01-core-contracts-auth-frontend-skeleton/WAVE-0-VERIFICATION.md` shows the dated record for items 1-5 in Task 3 with "Plan 05 unblocked" + "Plan 07 unblocked" lines
8. `cat packages/config/versions.lock.json` shows exact versions for all 15 dependencies listed in Task 3 step 5; no `undefined` values
9. `node -e "const s=require('./packages/shared/src/schemas/env-config').EnvConfigSchema || require('./packages/shared/src/schemas/env-config').default; console.log(typeof s)"` exits 0 (schema importable)
</verification>

<success_criteria>
1. `@call-it/ui` workspace exists, installable from monorepo root, lint-clean, and listed as a workspace dep on `apps/web`
2. Drizzle schema for `address_book`, `auth_methods`, `onboarding_state` lands in `apps/relayer/src/db/schema.ts` with the exact column shape from RESEARCH "Common Operation 2"; `getDb()` singleton follows the relayer's `getRedis()` pattern
3. Tailwind preset extracted from `apps/web/tailwind.config.ts` into `packages/ui/tailwind.preset.ts`; `apps/web/tailwind.config.ts` consumes via `presets: [uiPreset]`; visual parity with Phase 0 maintained
4. ESLint no-display-grid rule extended to scope `packages/ui/src/compound/Receipt.tsx` and children WITHOUT duplicating the rule body (Pitfall 15 anti-drift defense pre-staged for Plan 04)
5. Every Phase 1 environment variable is declared in `packages/shared/src/schemas/env-config.ts` and validated by the Phase 0 grep guard pipeline
6. `packages/config/versions.lock.json` records the exact runtime versions of all 15 Phase 1 NEW dependencies (closes RESEARCH Assumptions A3-A7)
7. WAVE-0-VERIFICATION.md records the Circle paymaster address verification, Sepolia equivalent status, Alchemy paymaster RPC choice, and Privy wagmi v4 API surface confirmation (closes RESEARCH Open Questions 1, 2, 5 and Assumption A1)
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-contracts-auth-frontend-skeleton/01-01-SUMMARY.md` recording:
- Final installed version of every Phase 1 dependency (from versions.lock.json)
- Confirmed Circle USDC Paymaster address(es) (mainnet + Sepolia status)
- Confirmed Alchemy paymaster RPC choice (ERC-7677 vs Alchemy custom)
- Privy wagmi v4 API surface match/divergence vs RESEARCH Pattern 3
- Any deltas downstream plans (05, 07) need to absorb
- Postgres connection string proof (e.g., `fly postgres connect` succeeded; migrations applied)
- Time + token cost
</output>
</output>
