---
phase: 01-core-contracts-auth-frontend-skeleton
plan: 08
type: execute
wave: 4
depends_on: [01, 02, 03, 04, 05, 06, 07]
files_modified:
  - apps/web/app/new/page.tsx
  - apps/web/app/new/layout.tsx
  - apps/web/app/new/components/MarketTypeSwitcher.tsx
  - apps/web/app/new/components/PriceTargetFields.tsx
  - apps/web/app/new/components/SpreadVsFields.tsx
  - apps/web/app/new/components/EventFields.tsx
  - apps/web/app/new/components/DeadlinePicker.tsx
  - apps/web/app/new/components/ConvictionSliderField.tsx
  - apps/web/app/new/components/CriteriaField.tsx
  - apps/web/app/new/components/AdvancedSettings.tsx
  - apps/web/app/new/components/PublishConfirmModal.tsx
  - apps/web/app/new/components/DuplicateWarning.tsx
  - apps/web/app/new/hooks/useDebouncedDupCheck.ts
  - apps/web/app/new/hooks/useSettledCalls.ts
  - apps/web/app/new/hooks/usePreflightValidation.ts
  - apps/web/app/new/hooks/usePublishCall.ts
  - apps/relayer/src/routes/calls-preflight.ts
  - apps/relayer/src/routes/calls-dup-check.ts
  - apps/relayer/src/index.ts
  - apps/relayer/__tests__/calls-preflight.test.ts
  - apps/relayer/__tests__/calls-dup-check.test.ts
  - apps/web/tests/new-call-publish.spec.ts
  - apps/web/tests/utc-day-boundary.spec.ts
autonomous: true
requirements:
  - CALL-37
  - CALL-38
  - CALL-39
  - CALL-40
  - CALL-42
  - CALL-43
  - CALL-44
  - CALL-45
  - CALL-46
  - CALL-47
  - CALL-48
  - CALL-49
  - CALL-50
  - CALL-51
  - CALL-52
  - CALL-53
  - CALL-54
  - CALL-55
  - CALL-56
  - CALL-57
  - CALL-58
  - CALL-59
  - CALL-60
  - CALL-61
  - CALL-64
  - CALL-65
  - CALL-66
  - UI-01
  - UI-02
  - UI-03
  - UI-51
  - UI-55
  - UI-56
tags:
  - new-call-form
  - rhf
  - zod-resolver
  - mode-conditional
  - duplicate-hash
  - preflight
  - receipt-preview
  - two-step-publish
  - slice-d
must_haves:
  truths:
    - "User on `/new` can publish a Price Target / Spread-vs / Event call; the userOp executes `CallRegistry.createCall` and the call appears via subgraph indexing within 30s (downstream Plan 09 feed displays it)"
    - "RHF + zodResolver consume `createCallSchema` from `@call-it/shared` (Plan 03) — same schema as the relayer preflight (D-29 parity guaranteed)"
    - "Mode switcher (`MarketTypeSwitcher`) toggles between 3 sub-forms (PriceTargetFields / SpreadVsFields / EventFields); EventFields branches further on `eventSubtype` (7 subtypes)"
    - "DeadlinePicker shows local-time entry AND UTC-day-bucket label below (CALL-46, Pitfall 12) — Playwright test covers a deadline near UTC midnight asserting the second label changes day appropriately"
    - "Duplicate-hash debounced 400ms check (D-22): typing slows form to 400ms idle → calls `/api/calls/dup-check` → if a match exists, renders amber inline warning above the conviction slider with link to quote-call instead (CALL-49)"
    - "ConvictionSliderField uses ConvictionBar (Plan 04, UI-51 muted-to-accent fill); shows the auto-cap warning Tag when user has `<10 settled calls` AND conviction `>=85` (read settledCalls via wagmi useReadContract — D-23 — cached per session)"
    - "Two-step publish modal: clicking [Publish] opens PublishConfirmModal showing the locked-in summary; user clicks [Confirm publish] which calls `POST /api/calls/preflight` first (server-side gate; D-28); on 200 fires the userOp via aa-sdk; on 422 surfaces field-level errors via RHF setError (D-31)"
    - "Live preview pane on right side renders `<Receipt mode='preview' data={formValues} />` — bound to RHF state via `form.watch()` (D-21)"
    - "Quote composer mode (`/new?quote=<parentCallId>`) loads the parent call data, pre-fills the form with parent context Card at top, sets `parentCallId` in the call payload — on success emits both CallCreated and CallQuoted events (CALL-57..61)"
    - "Advanced Settings panel hosts `openToChallenges` toggle (default ON per CALL-64) + Category select (Majors/DeFi/Other per CALL-62/63) + Auto-post to X (default ON per Phase 7 — disabled in Phase 1 UI but field present)"
  artifacts:
    - path: "apps/web/app/new/page.tsx"
      provides: "Main /new page composing form + preview + publish modal; uses RHF with zodResolver(createCallSchema)"
      contains: "useForm"
    - path: "apps/web/app/new/components/DeadlinePicker.tsx"
      provides: "Deadline input with local-time + UTC-day-bucket label (Pitfall 12 — CALL-46)"
      contains: "dayBucketUtc"
    - path: "apps/web/app/new/components/DuplicateWarning.tsx"
      provides: "Amber inline warning component shown when dup-check returns a match (CALL-25/26/49)"
      contains: "quote it instead"
    - path: "apps/web/app/new/hooks/useDebouncedDupCheck.ts"
      provides: "400ms debounced query firing /api/calls/dup-check (D-22)"
      contains: "400"
    - path: "apps/web/app/new/hooks/usePublishCall.ts"
      provides: "Orchestrates: preflight → if 200 build userOp via aa-config → aa-sdk sendUserOperation → wait for receipt → toast success → redirect to Plan 09's /call/[id] (currently /profile)"
      contains: "sendUserOperation"
    - path: "apps/relayer/src/routes/calls-preflight.ts"
      provides: "POST /api/calls/preflight running ALL gates server-side (D-28) — reads CallRegistry tvlCap + allowance + balance + settledCalls + activeDuplicateHashes via viem"
      contains: "preflight"
    - path: "apps/relayer/src/routes/calls-dup-check.ts"
      provides: "POST /api/calls/dup-check computing the hash + reading CallRegistry view + returning existing call ID if matched"
      contains: "computeDuplicateHash"
  key_links:
    - from: "apps/web/app/new/page.tsx"
      to: "@call-it/shared createCallSchema (Plan 03)"
      via: "RHF resolver: useForm({ resolver: zodResolver(createCallSchema) })"
      pattern: "zodResolver\\(createCallSchema\\)"
    - from: "apps/relayer/src/routes/calls-preflight.ts"
      to: "@call-it/shared createCallSchemaStrict + viem reads of CallRegistry"
      via: "Server-side schema parse + on-chain reads for tvl/allowance/balance/dup-hash"
      pattern: "createCallSchemaStrict"
    - from: "apps/web/app/new/components/DeadlinePicker.tsx"
      to: "@call-it/shared dayBucketUtc (Plan 03)"
      via: "Display UTC day label using the same TS function that mirrors Solidity"
      pattern: "dayBucketUtc"
    - from: "apps/web/app/new/hooks/usePublishCall.ts"
      to: "Plan 05 aa-config + Plan 07 useCirclePaymaster"
      via: "If sponsorship-cap-exceeded, route via Circle paymaster automatically"
      pattern: "useCirclePaymaster|erc7677Middleware"
---

<objective>
Vertical slice D: a user on `/new` composes a real call across the 3 market types + 7 event subtypes, sees the live `<Receipt mode='preview'>`, hits a 400ms-debounced duplicate-hash pre-check that surfaces an inline amber warning if a near-identical call already exists, gates through a two-step publish modal that calls the server-side preflight endpoint (D-28), then submits the userOp via the aa-sdk client built in Plan 05 — with automatic Circle USDC Paymaster handoff if the user is past their 5-tx cap (Plan 07 hook). CALL-37..70 land here at the UI + relayer-preflight layer.

Purpose: Critical-path steps 4 (Compose) and 5 (Publish) land here. This is the slice that proves the contract↔preflight parity (D-29) works in production: the same Zod schemas drive both RHF inline validation AND the relayer preflight gate AND the Foundry contract test fixture. Pitfall 12 (UTC-day boundary surprise) is closed in DeadlinePicker via the dual local+UTC label. CALL-49 inline dup warning is the form's UX defense against an avoidable contract revert.

Output: A fully functional `/new` page with all 3 market types, 7 event subtypes, dup-check, preflight, publish modal, receipt preview, and quote composer mode; relayer endpoints `/api/calls/preflight` + `/api/calls/dup-check`; Playwright e2e covering publish happy path + UTC-day boundary copy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-contracts-auth-frontend-skeleton/01-CONTEXT.md
@.planning/phases/01-core-contracts-auth-frontend-skeleton/01-RESEARCH.md
@.planning/phases/01-core-contracts-auth-frontend-skeleton/01-PATTERNS.md
@.planning/phases/01-core-contracts-auth-frontend-skeleton/01-VALIDATION.md
@.planning/phases/01-core-contracts-auth-frontend-skeleton/01-02-SUMMARY.md
@.planning/phases/01-core-contracts-auth-frontend-skeleton/01-03-SUMMARY.md
@.planning/phases/01-core-contracts-auth-frontend-skeleton/01-04-SUMMARY.md
@.planning/phases/01-core-contracts-auth-frontend-skeleton/01-05-SUMMARY.md
@.planning/phases/01-core-contracts-auth-frontend-skeleton/01-07-SUMMARY.md

<interfaces>
<!-- Surfaces this plan composes. -->

From @call-it/shared (Plan 03):
```ts
import { createCallSchema, createCallSchemaStrict, computeDuplicateHash, dayBucketUtc, MIN_STAKE, MAX_STAKE, HIGH_CONVICTION_THRESHOLD, CONVICTION_FLOOR_MIN_CALLS, MARKET_TYPE_TO_UINT, EVENT_SUBTYPE_TO_UINT, type CreateCallInput, type MarketType, type EventSubtype, type Category } from '@call-it/shared';
```

From @call-it/ui (Plan 04):
```ts
import { Button, Card, Tag, ConvictionBar, Receipt, useToast, Skeleton, CornerBrackets } from '@call-it/ui';
```

From apps/web/lib (Plans 05+07):
```ts
import { callRegistryAbi, profileRegistryAbi } from '@/lib/abis';
import { createAaClient } from '@/lib/aa-config';
import { useCirclePaymaster } from '@/hooks/useCirclePaymaster';
import { relayerClient } from '@/lib/relayer-client';
```

From wagmi v2:
```ts
import { useReadContract, useWriteContract } from 'wagmi';
```

From RESEARCH Pattern 10 (RHF + zodResolver — lines 1009-1046):
```tsx
const form = useForm<CreateCallInput>({ resolver: zodResolver(createCallSchema), mode: 'onChange' });
```

From Plan 07 paymaster handoff: when sponsorship-cap-exceeded error fires, route via `useCirclePaymaster().buildPaymasterAndData(...)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Relayer endpoints /api/calls/preflight + /api/calls/dup-check + Vitest tests</name>
  <read_first>
    - apps/relayer/src/routes/admin-paymaster.ts (Fastify route shape — Pattern C)
    - apps/relayer/src/lib/privy-auth.ts (Plan 06 — session pre-handler)
    - apps/relayer/src/lib/redis.ts (Phase 0 — caching dup-check)
    - apps/relayer/src/db/schema.ts (Plan 01)
    - packages/shared/src/validation/call-gates.ts (Plan 03 — createCallSchemaStrict)
    - packages/shared/src/hashing/duplicate-hash.ts (Plan 03 — computeDuplicateHash)
    - packages/shared/src/constants/addresses.ts (Plan 01/02 — CallRegistry deployed address)
    - .planning/phases/01-core-contracts-auth-frontend-skeleton/01-RESEARCH.md § D-28 + D-30 + D-31 + Pitfall 12
    - .planning/phases/01-core-contracts-auth-frontend-skeleton/01-PATTERNS.md § file 18 + Pattern C
    - apps/relayer/test/paymaster-admin.test.ts (Vitest pattern)
  </read_first>
  <files>apps/relayer/src/routes/calls-preflight.ts, apps/relayer/src/routes/calls-dup-check.ts, apps/relayer/src/index.ts, apps/relayer/__tests__/calls-preflight.test.ts, apps/relayer/__tests__/calls-dup-check.test.ts</files>
  <behavior>
    - Behavior: `POST /api/calls/dup-check` body `{ marketType, assetA, eventSubtype, targetValue, expiry }` returns `{ exists: true, existingCallId: <id> }` if a match exists in `activeDuplicateHashes` on CallRegistry; `{ exists: false }` otherwise
    - Behavior: Dup-check caches results in Redis `dup-check:{hash}` with 60s TTL (cheap; prevents hot-path RPC spam during debounced typing)
    - Behavior: `POST /api/calls/preflight` body matching `CreateCallInput` runs ALL gates server-side: parses via `createCallSchemaStrict.safeParse` (returns Zod field-level errors if invalid); reads `CallRegistry.currentTvl()` + `CallRegistry.tvlCap()` and asserts `currentTvl + stake + CREATION_FEE <= tvlCap` (CALL-34); reads `USDC.allowance(user, CallRegistry)` and `USDC.balanceOf(user)` and asserts >= incoming (CALL-35/36); reads `ProfileRegistry.settledCalls(user)` and informs (not blocks) about the high-conviction floor; reads `activeDuplicateHashes(computeDuplicateHash(input))` and returns 422 if `existingCallId != 0` (CALL-25/26); returns 200 with `{ ok: true, hash, settledCalls, suggestedConvictionCap }` on full pass
    - Behavior: 422 response shape: `{ errors: [{ field: 'criteriaText', code: 'criteria_required', message: '...' }, ...] }` — matches D-31 inline-error pattern
    - Behavior: User session is verified via privySessionPreHandler; the relayer reads the user's wallet address from the JWT (Privy session includes the embedded wallet address) to power USDC allowance/balance reads
  </behavior>
  <action>Create `apps/relayer/src/routes/calls-dup-check.ts` per PATTERNS § file 18 + RESEARCH D-22. Privy-session-gated. Body Zod schema mirrors Plan 03's `createCallSchema` partial (only the duplicate-hash inputs needed: marketType, assetA, eventSubtype, targetValue, expiry). Compute `hash = computeDuplicateHash({ marketType: MARKET_TYPE_TO_UINT[input.marketType], assetA: BigInt(input.assetA), metric: BigInt(EVENT_SUBTYPE_TO_UINT[input.eventSubtype] ?? 0), targetValue: input.targetValue, deadlineDay: dayBucketUtc(input.expiry) })`. Check Redis `dup-check:{hash}` (TTL 60s); on cache hit return. On miss, use viem `readContract` against the CallRegistry deployed address (read from `process.env.NEXT_PUBLIC_CALL_REGISTRY_ADDRESS` — Plan 02 wrote this) to call `activeDuplicateHashes(hash)`; if returns non-zero, response is `{ exists: true, existingCallId: <num> }`; else `{ exists: false }`. Cache the result in Redis with 60s TTL. Create `apps/relayer/src/routes/calls-preflight.ts`: privy-session-gated. Body Zod-validated via Plan 03's `createCallSchemaStrict`. On parse failure, return 422 with `{ errors: [...mapped per field...] }`. On parse success, perform parallel viem reads via `Promise.all`: `CallRegistry.currentTvl()`, `CallRegistry.tvlCap()`, `USDC.allowance(userAddress, CallRegistry)`, `USDC.balanceOf(userAddress)`, `ProfileRegistry.settledCalls(userAddress)`, `CallRegistry.activeDuplicateHashes(hash)`. Aggregate violations: TVL cap exceeded → push error; allowance insufficient → push; balance insufficient → push; duplicate hash exists → push. If any errors, return 422 with the structured error array. On full pass, return 200 with `{ ok: true, hash, settledCalls, suggestedConviction: settledCalls < CONVICTION_FLOOR_MIN_CALLS && input.conviction >= HIGH_CONVICTION_THRESHOLD ? CONVICTION_AUTOCAP : input.conviction }`. Log structured Pino events. Register both routes in `apps/relayer/src/index.ts`. Create `apps/relayer/__tests__/calls-dup-check.test.ts`: mock viem `readContract` to return 0 then a non-zero callId; assert response shape; assert second identical request hits Redis cache (no second viem call). Create `apps/relayer/__tests__/calls-preflight.test.ts`: mock viem reads with various seed values exercising each gate (TVL exceeded, allowance insufficient, balance insufficient, duplicate exists, full pass); assert each scenario produces the correct error shape; assert the conviction cap suggestion logic matches createCallSchemaStrict's behavior.</action>
  <verify>
    <automated>pnpm --filter @call-it/relayer typecheck &amp;&amp; pnpm --filter @call-it/relayer vitest run __tests__/calls-preflight.test.ts __tests__/calls-dup-check.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/relayer/src/routes/calls-preflight.ts` and `calls-dup-check.ts` exist and are registered
    - Preflight uses `createCallSchemaStrict` from `@call-it/shared` (grep matches)
    - Dup-check uses `computeDuplicateHash` AND `dayBucketUtc` from `@call-it/shared` (grep matches both)
    - Vitest tests demonstrate: 422 with structured errors for TVL/allowance/balance/duplicate violations; 200 with `{ ok, hash, settledCalls, suggestedConviction }` on full pass
    - Dup-check Redis cache test: second identical request returns cached result (mock viem call count assertion === 1)
    - All routes pass through privySessionPreHandler (401 test)
  </acceptance_criteria>
  <done>Server-side preflight + dup-check endpoints operational with the SAME Zod schema + hash function as the contract (D-29 parity preserved end-to-end); Redis caching defends the RPC budget during typed-in-form debounce storms.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /new page + 3 mode sub-forms + DeadlinePicker + ConvictionSliderField + DuplicateWarning + AdvancedSettings + PublishConfirmModal + Receipt preview + all hooks</name>
  <read_first>
    - apps/web/app/Providers.tsx (Plan 05)
    - apps/web/lib/wagmi.ts + privy-config.ts + aa-config.ts (Plan 05)
    - apps/web/lib/abis (Plan 05 — callRegistryAbi, profileRegistryAbi)
    - apps/web/lib/relayer-client.ts (Plan 05 — extend with postPreflight + postDupCheck)
    - apps/web/hooks/useCirclePaymaster.ts (Plan 07)
    - apps/web/hooks/useUsdcBalance.ts (Plan 06)
    - @call-it/ui Button, Card, Tag, ConvictionBar, Receipt, useToast, Skeleton (Plan 04)
    - @call-it/shared createCallSchema + dayBucketUtc + computeDuplicateHash + MARKET_TYPE_TO_UINT + CONVICTION_AUTOCAP + CONVICTION_FLOOR_MIN_CALLS + HIGH_CONVICTION_THRESHOLD + Type definitions (Plan 03)
    - .planning/phases/01-core-contracts-auth-frontend-skeleton/01-RESEARCH.md § Pattern 10 (RHF skeleton lines 1009-1046) + Pitfall 12 + Pitfall A
    - .planning/phases/01-core-contracts-auth-frontend-skeleton/01-CONTEXT.md § D-20, D-21, D-22, D-23, D-28, D-31
    - .planning/phases/01-core-contracts-auth-frontend-skeleton/01-PATTERNS.md § file 1 (CallRegistry storage), § files 13/14/15 (shared schemas)
    - CALL_IT_SPEC1.md § §15.2 (New Call form layout) — read if available locally; otherwise rely on CONTEXT/RESEARCH paraphrase + UI-01..56 acceptance text
    - apps/web/package.json (add react-hook-form + @hookform/resolvers — exact versions from Plan 01 lock)
  </read_first>
  <files>apps/web/app/new/page.tsx, apps/web/app/new/layout.tsx, apps/web/app/new/components/MarketTypeSwitcher.tsx, apps/web/app/new/components/PriceTargetFields.tsx, apps/web/app/new/components/SpreadVsFields.tsx, apps/web/app/new/components/EventFields.tsx, apps/web/app/new/components/DeadlinePicker.tsx, apps/web/app/new/components/ConvictionSliderField.tsx, apps/web/app/new/components/CriteriaField.tsx, apps/web/app/new/components/AdvancedSettings.tsx, apps/web/app/new/components/PublishConfirmModal.tsx, apps/web/app/new/components/DuplicateWarning.tsx, apps/web/app/new/hooks/useDebouncedDupCheck.ts, apps/web/app/new/hooks/useSettledCalls.ts, apps/web/app/new/hooks/usePreflightValidation.ts, apps/web/app/new/hooks/usePublishCall.ts, apps/web/lib/relayer-client.ts</files>
  <behavior>
    - Behavior: `/new` renders a 2-column layout: left (form) + right (`<Receipt mode='preview' data={formValues} />`). Layout is FLEXBOX (Pitfall 15 — Receipt's flexbox-only mandate is preserved at the parent level)
    - Behavior: MarketTypeSwitcher is a Tag-row toggle with 3 buttons (Price Target / Spread vs / Event); clicking switches the conditional sub-form below; switching does NOT clear unrelated fields except those that don't apply to the new mode
    - Behavior: PriceTargetFields shows asset (CoinPicker from CALL-06 allowlist + NftPicker from CALL-07 allowlist), targetValue, direction (≥ / ≤); SpreadVsFields shows assetA, assetB, metric (5 options per CALL-02); EventFields shows eventSubtype select (7 options) + subtype-conditional inputs (e.g., CexListing → exchange dropdown of 8; OnchainMetric → metric dropdown of 8)
    - Behavior: DeadlinePicker renders datetime-local input AND below it a line `Hash bucket: {dayBucketUtc(expiry)} UTC` per CALL-46 + Pitfall 12 — the bucket display updates live as the user types; if the local datetime crosses UTC midnight, the bucket label shows the next-day bucket (e.g., user in PST picks "today 11:32 PM" → bucket label "Hash bucket: 2026-05-23 00:00:00 UTC" surfaces)
    - Behavior: ConvictionSliderField wraps ConvictionBar with a hidden RHF Controller; below the slider renders the auto-cap warning Tag when `useSettledCalls() < CONVICTION_FLOOR_MIN_CALLS` AND current value `>= HIGH_CONVICTION_THRESHOLD` — Tag text per CALL-30/31: "New users are capped at 84 until you've settled 10+ calls"
    - Behavior: useDebouncedDupCheck takes formValues + 400ms debounce, calls `relayerClient.postDupCheck(...)` on idle, returns `{ match?: { existingCallId: number } }`; on `match` the form renders `<DuplicateWarning existingCallId={match.existingCallId} />` above the conviction slider (CALL-49 amber wording: "A nearly identical call is already live — quote it instead →" with link to `/new?quote=<existingCallId>`)
    - Behavior: AdvancedSettings renders `openToChallenges` toggle (default true — CALL-64), Category select (Majors/DeFi/Other — CALL-62/63), Auto-post-to-X toggle (default true; UI placeholder for Phase 7 — actual posting wires in Phase 7)
    - Behavior: PublishConfirmModal opens on [Publish] click; shows a locked summary card + final `<Receipt mode='preview'>` + two buttons `[Cancel]` `[Confirm publish]`. Confirm fires `usePublishCall().publish()` flow.
    - Behavior: `usePublishCall().publish()`: calls `relayerClient.postPreflight(input)`; on 422 sets RHF field errors via `form.setError(error.field, { message: error.message })` (D-31) AND closes the modal AND scrolls to the first invalid field; on 200 builds the userOp via aa-sdk (`createAaClient(...)` from Plan 05), calldata is `encodeFunctionData({ abi: callRegistryAbi, functionName: 'createCall', args: [...] })` with args properly mapped via MARKET_TYPE_TO_UINT etc.; submits via `sendUserOperation` with `erc7677Middleware` pointing at the relayer policy URL; if sponsorship-cap-exceeded error fires, calls `useCirclePaymaster().buildPaymasterAndData(...)` and retries; on receipt (waitForUserOperationReceipt) shows success toast and redirects to `/profile/{address}` (Plan 09 will route to `/call/[id]` once feed is live)
    - Behavior: Quote composer mode (`/new?quote=<parentCallId>`) — reads parent call via wagmi `useReadContract({ functionName: 'getCall', args: [BigInt(parentCallId)] })`; renders a parent context Card above the form; sets `parentCallId` field in the call payload so contract emits `CallQuoted(parentCallId, callId)` (CALL-57..61)
    - Behavior: Playwright `new-call-publish.spec.ts`: mock the whole stack (Privy session, Alchemy bundler, relayer endpoints); fill the form for a Price Target BTC ≥ $80k call; navigate to publish; assert preflight is called; on 200 assert sendUserOperation is invoked with the right calldata; on success assert redirect to /profile/{address}
    - Behavior: Playwright `utc-day-boundary.spec.ts`: fill a deadline near UTC midnight in a forced PST/EST timezone (use `playwright.config.ts` `webServer.env.TZ=America/Los_Angeles`); assert the "Hash bucket: ..." label shows the expected UTC day
  </behavior>
  <action>Add to `apps/web/package.json` dependencies (exact versions from Plan 01 lock): `react-hook-form`, `@hookform/resolvers`. Install via pnpm. Extend `apps/web/lib/relayer-client.ts` (Plan 05) with `postPreflight(input: CreateCallInput) → Promise<PreflightResult>` and `postDupCheck(input: DupCheckInput) → Promise<DupCheckResult>`; both type-safe via shared types. Build `apps/web/app/new/hooks/useSettledCalls.ts` per RESEARCH "Common Operation 1" lines 1294-1310: wagmi `useReadContract` against ProfileRegistry.settledCalls(address), cached per session (`staleTime: Infinity`). Build `apps/web/app/new/hooks/useDebouncedDupCheck.ts`: tanstack-query-driven; takes the relevant form fields as a dependency; debounces 400ms (use `use-debounce` lib OR a tiny custom hook); calls `relayerClient.postDupCheck(...)` only when all required fields are populated AND meaningfully changed; returns `{ match: { existingCallId } | undefined, isLoading }`. Build `apps/web/app/new/hooks/usePreflightValidation.ts`: tanstack-query manual `mutation` calling `relayerClient.postPreflight(...)`; surfaces structured errors. Build `apps/web/app/new/hooks/usePublishCall.ts` orchestrating: preflight → setError-on-422 → aa-sdk userOp → Circle paymaster fallback on -32000 → receipt-wait → toast + redirect. Build `apps/web/app/new/components/MarketTypeSwitcher.tsx`: 3 Tag buttons (Price Target / Spread vs / Event) styled as toggles; bound to RHF `setValue('marketType', ...)`. Build `PriceTargetFields`, `SpreadVsFields`, `EventFields` — each renders the inputs for its market type using @call-it/ui Card + RHF `Controller` for non-trivial widgets (Coin picker, NFT picker). The Coin/Nft picker components are sub-components within each fields file; both read allowlists from `packages/shared/src/constants/allowlists.ts` (Plan 02 may have shipped these per the CALL-06/07 requirements; if not, this Plan creates them in shared). Build `DeadlinePicker.tsx` per Pitfall 12 + CALL-46: datetime-local input + below it a `<div className="text-brand-muted text-sm font-mono">Hash bucket: {formatUtcDay(value)}</div>` where `formatUtcDay(expiry)` calls `dayBucketUtc(BigInt(expiry))` from @call-it/shared then formats as `YYYY-MM-DD HH:mm:ss UTC`. Build `ConvictionSliderField.tsx`: wraps Plan 04's ConvictionBar in an RHF Controller; reads `useSettledCalls()` to determine the auto-cap warning; renders the warning Tag inline below the slider per RESEARCH Pitfall A. Build `CriteriaField.tsx`: textarea + character counter + "VERIFIED CRITERIA" green Tag when length >= 50 (CALL-19); conditional-required indicator for event subtypes per CALL-15/16. Build `AdvancedSettings.tsx`: collapsible Card with the 3 settings. Build `DuplicateWarning.tsx`: amber Tag with the CALL-49 copy + Link to `/new?quote=${existingCallId}`. Build `PublishConfirmModal.tsx` using Radix Dialog (already available from @call-it/ui's underlying Radix dep — or directly import from `@radix-ui/react-dialog`); locked summary; the publish button is disabled while preflight is in flight. Build `apps/web/app/new/page.tsx` per RESEARCH Pattern 10 lines 1011-1046: `'use client'` first line; `const form = useForm<CreateCallInput>({ resolver: zodResolver(createCallSchema), mode: 'onChange', defaultValues: { ... } })`; layout `<div className="flex"><form className="flex-1 flex flex-col gap-4" onSubmit={form.handleSubmit(onPublish)}> { MarketTypeSwitcher, marketType-conditional sub-fields, DeadlinePicker, CriteriaField, ConvictionSliderField (with DuplicateWarning rendered above it when match), AdvancedSettings, [Publish] button } </form><aside className="flex-1"><Receipt mode='preview' data={form.watch()} /></aside></div>`. On Publish, open PublishConfirmModal; on Confirm, call `usePublishCall().publish(form.getValues())`. Also accept `searchParams?.quote` — if present, fetch the parent call via wagmi useReadContract and render a context Card at the top. Create `apps/web/app/new/layout.tsx` as a minimal nested layout (page title + breadcrumb). Create `apps/web/tests/new-call-publish.spec.ts` and `apps/web/tests/utc-day-boundary.spec.ts` per the Behavior block: mock relayer routes via `page.route`; mock Privy session via testkit; for the UTC test, set `TZ=America/Los_Angeles` in playwright config webServer env (or via test.use). Each test ends with a deterministic assertion (success toast text, redirect URL, label text).</action>
  <verify>
    <automated>pnpm --filter @call-it/web build &amp;&amp; pnpm --filter @call-it/web exec playwright test tests/new-call-publish.spec.ts tests/utc-day-boundary.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/app/new/page.tsx` uses `useForm` with `resolver: zodResolver(createCallSchema)` (grep matches `zodResolver\\(createCallSchema\\)`)
    - `pnpm run parity:diff` exits 0 after the new `createCallSchema` imports land in this plan — confirms Plan 03 did not drift when Plan 08 wired the form.
    - DeadlinePicker imports `dayBucketUtc` from `@call-it/shared`; the "Hash bucket: ..." label is rendered as a visible element (Playwright asserts via `page.getByText(/Hash bucket:/)`)
    - DuplicateWarning component renders the CALL-49 copy verbatim: "A nearly identical call is already live — quote it instead"
    - ConvictionSliderField uses Plan 04's `ConvictionBar`; the auto-cap Tag renders when settledCalls < 10 AND conviction >= 85
    - usePublishCall calls `relayerClient.postPreflight` BEFORE `sendUserOperation` (preflight gate); on 422 it calls `form.setError` (D-31)
    - On sponsorship-cap-exceeded error from the aa-sdk, the publish flow calls `useCirclePaymaster().buildPaymasterAndData(...)` and retries (handoff per Plan 07)
    - Playwright `new-call-publish.spec.ts` exits 0; publish happy path succeeds end-to-end against the mocked relayer + bundler stack
    - Playwright `utc-day-boundary.spec.ts` exits 0; user in `America/Los_Angeles` filling 11:32 PM PT shows the next UTC day's bucket label
    - Receipt preview pane updates as the user types (RHF `watch()`); no `display: grid` introduced (Pitfall 15)
    - Phase 0 OG fallback Playwright test still passes (regression check)
  </acceptance_criteria>
  <done>/new page renders 3 mode-conditional sub-forms with live Receipt preview; debounced dup-check surfaces CALL-49 inline warning; preflight gate runs server-side before any userOp; publish flow integrates Plan 07 Circle paymaster handoff; UTC-day boundary copy closes Pitfall 12 at the UX layer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend RHF state → Zod schema | Same schema as relayer preflight (D-29); type-level + runtime parity |
| Frontend → relayer dup-check | Privy-session-gated; rate-limited via Redis 60s cache (no per-IP throttle in v1 — defer) |
| Frontend → relayer preflight | Privy-session-gated; the ONLY pre-submit validation chokepoint that mirrors contract |
| Frontend → aa-sdk → bundler | userOp constructed client-side; the relayer policy endpoint is the chokepoint for sponsorship |
| Quote composer → CallRegistry view | useReadContract reads the parent call; no auth needed for view fns |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-51 | Tampering (D-29 anti-drift) | Frontend Zod schema diverges from contract | mitigate | createCallSchema is imported from @call-it/shared; same source as Plan 03 parity test; CI gate runs parity-diff on every PR |
| T-01-52 | Information disclosure (Pitfall 12) | UTC-day boundary surprise | mitigate | DeadlinePicker renders the UTC day label inline (CALL-46); Playwright utc-day-boundary.spec.ts asserts |
| T-01-53 | DoS | Debounced dup-check spams relayer during typing | mitigate | 400ms debounce client-side + 60s Redis cache server-side; tanstack-query dedupes in-flight requests |
| T-01-54 | Tampering | User bypasses preflight via direct RPC | accept | Contract enforces all gates onchain; preflight is the UX defense; tx will revert if user skips preflight — wasted gas but no security impact |
| T-01-55 | Information disclosure | settledCalls leak via useReadContract | accept | Settled calls count is a public on-chain view |
| T-01-56 | Tampering | Wrong MARKET_TYPE_TO_UINT mapping | mitigate | @call-it/shared types/call.ts (Plan 03) is the single source; Plan 03 parity test asserts vs Solidity enum order |
| T-01-57 | DoS | useDebouncedDupCheck firing on every keystroke without idle check | mitigate | 400ms debounce + only fires when REQUIRED fields are present (marketType, assetA, eventSubtype, targetValue, expiry) |
| T-01-58 | Replay | User submits the same call twice | mitigate | Contract's duplicate-hash gate reverts; preflight catches client-side; tx replay via mempool unlikely on Arbitrum |
</threat_model>

<verification>
After both tasks complete:

1. `pnpm --filter @call-it/relayer typecheck && pnpm --filter @call-it/relayer test` exits 0 (all Plan 06+07+08 Vitest tests green)
2. `pnpm --filter @call-it/web build && pnpm --filter @call-it/web exec playwright test` exits 0 (all 8+ Playwright spec files including new-call-publish + utc-day-boundary)
3. Grep `zodResolver\\(createCallSchema\\)` in apps/web matches in `app/new/page.tsx`
4. Grep `display:\\s*['\"]grid['\"]\\|grid-cols-` in `apps/web/app/new/` returns ZERO matches (Pitfall 15)
5. Grep `dayBucketUtc` in `apps/web/app/new/components/DeadlinePicker.tsx` matches
6. Grep `from '@call-it/shared'` in `apps/web/app/new/` matches at least 4 times (shared schemas + types + hashing + constants)
7. `pnpm run parity:diff` from monorepo root STILL exits 0 (the slice didn't break parity)
8. Phase 0 + Plans 05+06+07 e2e all still pass (regression check)
</verification>

<success_criteria>
1. User can publish all 3 call types (Price Target / Spread vs / 7 Event subtypes) end-to-end via /new
2. The same `createCallSchema` from `@call-it/shared` drives RHF validation, relayer preflight, AND Plan 02 contract parity (D-29 guarantee preserved across the full stack)
3. DeadlinePicker surfaces the UTC-day bucket label (Pitfall 12 + CALL-46)
4. Duplicate-hash 400ms debounce + amber inline warning + quote-call link (CALL-49)
5. Two-step publish modal calls preflight before sendUserOperation; field-level errors render inline (D-28/D-31)
6. Receipt preview pane uses `<Receipt mode='preview'>` (D-21) — the shared design lever Phases 2/4/7 reuse
7. Circle USDC Paymaster handoff (Plan 07) integrates automatically into the publish flow at tx 6+
8. Quote composer mode (`/new?quote=<id>`) supports CALL-57..61
9. Playwright covers happy path publish + UTC-day boundary
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-contracts-auth-frontend-skeleton/01-08-SUMMARY.md` recording:
- Wagmi + aa-sdk send-userOp signature actually used (since RESEARCH A2 left the integration uncertain)
- Bundle size delta of /new route
- Time + token cost
</output>
