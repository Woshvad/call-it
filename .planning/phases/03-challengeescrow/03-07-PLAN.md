---
phase: 03-challengeescrow
plan: 07
type: execute
wave: 6
depends_on: [03-05]
files_modified:
  - apps/web/app/og/duel/[challengeId]/route.ts
  - apps/web/app/page.tsx
autonomous: true
requirements:
  - SOCIAL-40
  - SOCIAL-41
  - SOCIAL-42
  - SOCIAL-51
  - SHARE-07

must_haves:
  truths:
    - "GET /og/duel/[challengeId] returns 200 with Content-Type image/png; X-Variant: duel-active when not settled"
    - "OG card uses export const runtime = 'nodejs' as first export; never 'edge'"
    - "OG card is entirely flexbox — zero display:grid or gridTemplateColumns in the JSX (Pitfall F/15)"
    - "Two-column layout (CALLER / VS / CHALLENGER) uses flex-direction:'row' with two flex:1 children"
    - "Settled fields are stubbed per D-11: both columns at full opacity, 'VS' not 'WINS', '? REP' deltas"
    - "On any error: renderFallback() is called and 200 returned (SHARE-10 — never 500)"
    - "Duels tab (fourth tab in feed) renders Active / Trending pinned / Recently settled sections with filter chips"
    - "⚔ OPEN badge renders on CallCard when call.openToChallenges == true"
    - "Duel King badge placeholder: does not render when duel_kings table has no row (correct Phase 3 behavior)"
    - "TRENDING DUEL label renders on pinned duel cards in feed and Duels tab"
  artifacts:
    - path: "apps/web/app/og/duel/[challengeId]/route.ts"
      provides: "OG card variant 3 per §16.4 — Node runtime, flexbox-only, renderFallback, stubbed settled fields"
      contains: "duel-active"
      min_lines: 100
    - path: "apps/web/app/page.tsx"
      provides: "Duels tab (fourth tab), filter chips (All/Active/Just settled/High-stakes/Trending), ⚔ OPEN badge on CallCards, TRENDING DUEL pin label, Duel King badge (placeholder)"
      contains: "TRENDING DUEL"
  key_links:
    - from: "apps/web/app/og/duel/[challengeId]/route.ts"
      to: "apps/web/app/og/[callId]/route.ts"
      via: "mirrors pattern exactly: runtime, renderFallback, viem client, ImageResponse, cache headers"
      pattern: "export const runtime = 'nodejs'"
    - from: "apps/web/app/page.tsx"
      to: "apps/relayer/src/routes/duels.ts"
      via: "fetch /api/duels for Duels tab data"
      pattern: "/api/duels"
---

<objective>
Build the Duel Settled OG card variant 3 and the Duels tab + distribution badges in the feed. These are the two distribution surfaces that drive sharing and discovery for Phase 3 — OG cards make duel links look great on X; Duels tab, ⚔ OPEN badges, and TRENDING DUEL pins drive in-product discovery.

Purpose: Without the OG card, duel links on X show the fallback card. Without the Duels tab and badges, the existing feed has no duel discoverability.

Output: /og/duel/[challengeId] route (Node runtime, flexbox, stubbed settled fields); feed Duels tab + filter chips; ⚔ OPEN badge + challenge filter; TRENDING DUEL pin; Duel King badge placeholder.
</objective>

<execution_context>
@C:\Users\woshv\Desktop\Call it\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\woshv\Desktop\Call it\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\woshv\Desktop\Call it\.planning\STATE.md
@C:\Users\woshv\Desktop\Call it\.planning\phases\03-challengeescrow\03-CONTEXT.md
@C:\Users\woshv\Desktop\Call it\.planning\phases\03-challengeescrow\03-UI-SPEC.md
@C:\Users\woshv\Desktop\Call it\.planning\phases\03-challengeescrow\03-05-SUMMARY.md

<interfaces>
From apps/web/app/og/[callId]/route.ts (exact analog — mirror every pattern):
- export const runtime = 'nodejs'; // FIRST LINE, mandatory
- import { ImageResponse } from '@vercel/og'
- import { NextRequest } from 'next/server'
- createPublicClient({ chain: arbitrumSepolia, transport: http(...) }) — server-side viem
- renderFallback() imported from lib — called on any error, always returns 200
- Cache-Control: public, max-age=60, stale-while-revalidate=300
- X-Variant: header set to describe card state
- ?v={statusVersion} cache-bust query param
- GET handler: await params (Promise<{challengeId: string}>); parse BigInt; try/catch → renderFallback
- ImageResponse: 1200×630; fonts array: Syne 700, SpaceGrotesk 400, JetBrains Mono 400
- h() from React: createElement helper (or JSX with '@jsxRuntime')
- cornerBracket() function from existing route.ts — copy verbatim

OG card §16.4 layout (UI-SPEC Surface 2, LOCKED):
  1200×630 outer div: background #09090E, border 3px #E8F542, display:flex, flexDirection:column
  Header row (flex-direction:row, justify:space-between): "CALL IT" Syne 64px 700 #E8F542 left; "ARB" text right
  Hero row (flex:1, flex-direction:row):
    CALLER column (flex:1, flex-direction:column, align:center): avatar 180px circle (border 3px #E8F542); handle Syne 32px 700; WINS/VS slot Syne 64px 700
    VS divider (width:24px, border-left 1px #2E2E42)
    CHALLENGER column (flex:1, flex-direction:column, align:center, opacity={settled?0.4:1.0}): avatar 180px (border 3px #FB923C); handle Syne 32px 700
  Pot row (Space Grotesk 16px 400 #F1F5F9): "Pot: $X,XXX · winner takes all"
  Meta row (Space Grotesk 12px 400 #64748B): "ARB / OP · ARB outperforms OP by >5%" left; "callitapp.xyz" Syne 12px #E8F542 right
  4x corner brackets (L-shaped, 4px stroke, #E8F542, 16px arm, all four corners)

STUB CONTRACT (D-11, active-duel Phase 3):
  winner highlight: both columns full opacity (1.0)
  WINS/VS text: "VS" in Syne 64px 700 #64748B (NOT "WINS" — Phase 4 sets "WINS")
  winner handle color: #F1F5F9 neutral (not #E8F542)
  rep deltas: "? REP" in #94A3B8
  X-Variant: 'duel-active'

CRITICAL: display:'flex' EVERYWHERE. NEVER display:'grid'. No gridTemplateColumns. No gridTemplateRows.

From apps/web/app/page.tsx (existing feed page — add Duels tab):
  Current tabs: Live calls / Settled / Following
  Add Duels as fourth tab
  Duels tab data from GET /api/duels (relayer Plan 05 route)
  Trending section: from /api/duels?status=trending
  Active section: from /api/duels?status=active (default)
  Recently settled: placeholder "No settled duels yet." (D-08)

Filter chips on Duels tab (UI-SPEC Surface 3):
  All / Active / Just settled / High-stakes (>$500) / Trending
  Default: All
  Chip style: 2px #2E2E42 border default; #E8F542 border+bg selected; 0px radius

⚔ OPEN badge (UI-SPEC Surface 6):
  Renders on CallCard where call.openToChallenges == true
  Style: "⚔ OPEN" — 2px #E8F542 border, #0D1A00 bg, #E8F542 text, 12px Space Grotesk 700 uppercase, 2px border-radius, 4px/2px padding

Duel King badge (UI-SPEC Surface 5):
  Renders where handle displayed (Profile, feed CallCard, Leaderboard, Receipt, Duel page)
  Badge: crown icon + "DUEL KING" — 2px #E8F542 border, #0D1A00 bg, #E8F542 text, 12px 700
  Placeholder: NO badge renders when duel_kings table empty (Phase 3 correct per D-08)
  Fetch from GET /api/duels?type=king or a separate /api/duel-king endpoint

TRENDING DUEL label (UI-SPEC Surface 3 + 4):
  Duels tab: pinned above Active section; "TRENDING DUEL" pill — 2px #E8F542 border, #0D1A00 bg, #E8F542 text, 12px 700 uppercase 0.12em
  Main feed: pinned trending duel card at top of global feed (3px #E8F542 border upgrade; hard shadow 4px 4px 0 #E8F542)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OG card variant 3 — /og/duel/[challengeId]/route.ts</name>
  <files>apps/web/app/og/duel/[challengeId]/route.ts</files>
  <read_first>
    apps/web/app/og/[callId]/route.ts — FULL FILE; every pattern must be mirrored exactly
    .planning/phases/03-challengeescrow/03-PATTERNS.md lines 857-965 — OG card route skeleton + two-column layout pattern
    .planning/phases/03-challengeescrow/03-UI-SPEC.md Surface 2 — §16.4 layout contract with STUB CONTRACT table
    .planning/phases/03-challengeescrow/03-RESEARCH.md Pitfall F — display:grid in OG card
    packages/shared/src/constants/addresses.ts — CHALLENGE_ESCROW_ARBITRUM_SEPOLIA
  </read_first>
  <action>
    Create apps/web/app/og/duel/[challengeId]/route.ts.

    FIRST LINE (mandatory, before any imports): export const runtime = 'nodejs';

    Import: ImageResponse from '@vercel/og'; NextRequest from 'next/server'; createPublicClient, http from 'viem'; arbitrumSepolia from 'viem/chains'; renderFallback from same lib path as og/[callId]/route.ts; CHALLENGE_ESCROW_ARBITRUM_SEPOLIA from '@call-it/shared'; h from react (or use createElement).

    Font loading: copy the exact font-loading pattern from og/[callId]/route.ts (syneBold, spaceGrotesk, jetBrainsMono — loaded once at module scope via fetch from the same font source).

    viem public client: copy exactly from og/[callId]/route.ts (arbitrumSepolia chain, ARBITRUM_SEPOLIA_RPC_URL env var).

    CHALLENGE_ESCROW_ABI: minimal ABI for getChallenge(uint256) → Challenge struct; same approach as Phase 2 inline ABI pattern.
    PROFILE_REGISTRY_ABI: minimal for getProfile(address) to get handle.

    cornerBracket() function: copy verbatim from og/[callId]/route.ts.

    buildDuelCard(props) function:
    - CRITICAL: ALL styles use display:'flex'. ZERO display:'grid'. TWO-COLUMN is flex-direction:'row' with two flex:1 children.
    - Outer div: width:1200, height:630, background:'#09090E', display:'flex', flexDirection:'column', border:'3px solid #E8F542', position:'relative'
    - Four cornerBracket() calls (all four corners, 4px stroke, #E8F542, 16px arm)
    - Header row: display:'flex', flexDirection:'row', justifyContent:'space-between'; "CALL IT" Syne 64px 700 #E8F542; "ARB" text right
    - Hero row: display:'flex', flexDirection:'row', flex:1
      - CALLER column: display:'flex', flexDirection:'column', flex:1, alignItems:'center', padding:'24px 32px'; avatar 180px circle 3px #E8F542 border; handle text Syne 32px 700 (color: settled ? '#E8F542' : '#F1F5F9' — stub uses #F1F5F9); WINS/VS: stub renders "VS" Syne 64px 700 #64748B
      - VS divider: width:24, display:'flex', borderLeft:'1px solid #2E2E42'
      - CHALLENGER column: display:'flex', flexDirection:'column', flex:1, alignItems:'center', opacity: settled ? 0.4 : 1.0 — stub uses 1.0; avatar 180px circle 3px #FB923C border; handle text Syne 32px 700 #F1F5F9 (stub)
    - Pot row: Space Grotesk 16px 400 #F1F5F9; "Pot: ${formatPot(pot)} · winner takes all"
    - Rep deltas row: "? REP" #94A3B8 (stub per D-11)
    - Meta row: flex-direction:'row', justifyContent:'space-between'; left: asset pair + call question Space Grotesk 12px #64748B; right: "callitapp.xyz" Syne 12px #E8F542

    GET handler:
    - Parse challengeId from await params — catch invalid BigInt → renderFallback with X-Variant:'duel-fallback'
    - Ignore ?v param (consumed for CDN cache-bust)
    - RPC reads: Promise.all([ChallengeEscrow.getChallenge(challengeId), ProfileRegistry.getProfile(challenge.caller), ProfileRegistry.getProfile(challenge.challenger)])
    - On RPC error: renderFallback with X-Variant:'duel-fallback', X-Reason:'rpc-error'
    - Build ImageResponse with buildDuelCard(props)
    - Headers: Cache-Control: public, max-age=60, stale-while-revalidate=300; X-Variant: 'duel-active' (Phase 3 stub; Phase 4 sets 'duel-settled' when winner populated)
    - renderFallback in all catch paths — never 500 (SHARE-10)

    formatPot(pot: bigint): string — format to "$X,XXX" with commas, no decimals unless < 10_000_000 (< $10).

    After creating file: grep -c "display.*grid\|gridTemplate" apps/web/app/og/duel/[challengeId]/route.ts MUST return 0. grep "export const runtime" MUST show 'nodejs'.
  </action>
  <verify>
    <automated>cd "C:\Users\woshv\Desktop\Call it" && grep -c "display.*grid\|gridTemplate" apps/web/app/og/duel/[challengeId]/route.ts 2>/dev/null || echo "0"</automated>
  </verify>
  <done>route.ts exists; `export const runtime = 'nodejs'` is first export; grep -c "display.*grid" returns 0; grep -c "duel-active" returns >= 1 (X-Variant header); `pnpm --filter @call-it/web build` exits 0 with the new route included; CHALLENGE_ESCROW_ARBITRUM_SEPOLIA imported (no inline 0x address)</done>
</task>

<task type="auto">
  <name>Task 2: Duels tab + ⚔ OPEN badge + Duel King badge + TRENDING DUEL pin in feed</name>
  <files>apps/web/app/page.tsx</files>
  <read_first>
    apps/web/app/page.tsx — full file; understand existing tab structure, CallCard composition, filter chip pattern before modifying
    .planning/phases/03-challengeescrow/03-UI-SPEC.md Surface 3 (Duels tab) + Surface 4 (TRENDING pin) + Surface 5 (Duel King) + Surface 6 (⚔ OPEN badge)
    .planning/phases/03-challengeescrow/03-CONTEXT.md D-06 (Riding = follow/fade, no new mechanic) + D-08 (recently-settled placeholder)
    packages/ui/src/ — Tag component API for badges
  </read_first>
  <action>
    Modify apps/web/app/page.tsx to add the Duels tab as the fourth tab alongside Live / Settled / Following:

    1. Duels tab: add "Duels" to the tab list. Active tab: 2px #E8F542 bottom border, #F1F5F9 text. Inactive: no border, #64748B text. Tab style consistent with existing tabs (flex-direction:row, 0px radius, neobrutalist).

    2. Duels tab content state: fetch /api/duels on tab activation. State: activeFilter ('All' | 'Active' | 'Just settled' | 'High-stakes' | 'Trending'). Filter chips above the list: All / Active / Just settled / High-stakes (>$500) / Trending. Chip style: 2px #2E2E42 default border, #111118 bg, #F1F5F9 text; selected: #E8F542 border+bg, #09090E text; 0px radius.

    3. Trending section: if /api/duels returns trending duels (isTrending: true): pin above Active section with "TRENDING DUEL" pill badge (2px #E8F542 border, #0D1A00 bg, #E8F542 text, 12px 700 uppercase 0.12em). Card row: CALLER avatar 32px + handle 16px 700 #E8F542 + rep 12px #64748B | market statement 16px #F1F5F9 + pot 12px JetBrains Mono #E8F542 + time remaining | CHALLENGER avatar 32px + handle 16px 700 #FB923C + rep. Below row: 4px consensus bar (#E8F542 left / #FB923C right). 2px #1E1E2E bottom border each row.

    4. Active duels section: "ACTIVE DUELS" 12px uppercase 700 tracked #64748B with count. Same row layout as trending. Sort: pot desc. Empty state: "No active duels yet. Challenge a caller to get started." + "⚔ OPEN" badge illustration.

    5. Recently settled section: "RECENTLY SETTLED (7D)" 12px uppercase 700 tracked #64748B. Placeholder: "No settled duels yet. Settle pending calls to see duel outcomes here." (D-08 — inert until Phase 4). 2px #1E1E2E border, #111118 bg.

    6. ⚔ OPEN badge: where CallCards are rendered on the Live tab — add ⚔ OPEN badge on each card where call.openToChallenges == true. Badge: "⚔ OPEN" — 2px #E8F542 border, #0D1A00 bg, #E8F542 text, 12px Space Grotesk 700 uppercase, 2px border-radius. Position: after category tag in card header line. "⚔ Challengeable" filter chip in the Live tab filter chips row — when active, shows only openToChallenges==true calls.

    7. Duel King badge: fetch /api/duels?type=king (or /api/duel-king endpoint from relayer). If a row exists in duel_kings: render "DUEL KING" badge (Lucide Crown 12px #E8F542 + "DUEL KING" text) next to the handle on CallCards where the caller's address matches duelKings.winnerAddress. Placeholder: if no king row returned, badge simply does not render (correct Phase 3 behavior — no loading state, no "TBD").

    8. TRENDING DUEL pin in main feed (Live tab): when /api/duels returns a trending duel, show it at the top of the main Live feed with upgraded styling: 3px #E8F542 card border (vs standard 2px), hard shadow 4px 4px 0 #E8F542, "TRENDING DUEL" label top-left.

    CRITICAL: All new JSX must use display:flex (never display:grid). Tab switching must not break existing Live/Settled/Following tab content. ⚔ OPEN and Duel King badges must not show wallet addresses.
  </action>
  <verify>
    <automated>cd "C:\Users\woshv\Desktop\Call it" && pnpm --filter @call-it/web build 2>&1 | tail -10</automated>
  </verify>
  <done>Duels tab present in apps/web/app/page.tsx; `pnpm --filter @call-it/web build` exits 0; grep confirms "TRENDING DUEL" in page.tsx; grep confirms "⚔ OPEN" or "openToChallenges" in page.tsx for badge render; grep confirms "DUEL KING" in page.tsx; grep -r "display.*grid" apps/web/app/page.tsx returns 0; existing Live/Settled/Following tabs not broken</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OG card server → RPC | Server-side viem reads in Node runtime; RPC key server-side only |
| OG card response | Image rendered from on-chain data; must never expose raw errors to public |
| Feed → /api/duels | Client fetches relayer endpoint; untrusted response shape |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-07-01 | Tampering | display:grid in OG card route | mitigate | Pitfall F/15 — grep guard: grep -c "display.*grid" og/duel/[challengeId]/route.ts = 0; ESLint rule catches at build time |
| T-3-07-02 | Information Disclosure | Stack trace in OG card error response | mitigate | Any error path calls renderFallback(); never returns 500; never includes error message in response body; X-Reason header for operator diagnostics only |
| T-3-07-03 | Tampering | Wrong runtime in OG route (edge instead of node) | mitigate | `export const runtime = 'nodejs'` is the FIRST export declaration; grep verification confirms |
| T-3-07-04 | Tampering | Duel King badge showing wrong address | mitigate | Badge renders handle from ProfileRegistry (not raw address); wallet address never in badge text (AUTH-44) |
| T-3-07-05 | Spoofing | Trending DUEL label on non-qualifying duel | mitigate | isTrending flag comes from relayer /api/duels which reads live trending_duels Postgres rows; client-side filter only hides non-trending — cannot inject trending status |
| T-3-07-06 | Tampering | RPC URL exposed in client bundle | mitigate | ARBITRUM_SEPOLIA_RPC_URL is server-side only (no NEXT_PUBLIC_ prefix) in OG route (Route Handler, not component); consistent with og/[callId]/route.ts pattern |
</threat_model>

<verification>
- OG route builds: `pnpm --filter @call-it/web build` exits 0
- runtime check: `grep "export const runtime" apps/web/app/og/duel/[challengeId]/route.ts` shows 'nodejs'
- No grid: `grep -c "display.*grid\|gridTemplate" apps/web/app/og/duel/[challengeId]/route.ts` = 0
- X-Variant header: `grep -c "duel-active" apps/web/app/og/duel/[challengeId]/route.ts` >= 1
- Duels tab: `grep -c "Duels\|TRENDING DUEL" apps/web/app/page.tsx` >= 2
- OPEN badge: `grep -c "openToChallenges\|OPEN" apps/web/app/page.tsx` >= 1
- Duel King badge: `grep -c "DUEL KING\|duelKing" apps/web/app/page.tsx` >= 1
- No new grid in feed: `grep -c "display.*grid" apps/web/app/page.tsx` = 0 (or same count as before — no new grid added)
</verification>

<success_criteria>
/og/duel/[challengeId] returns 1200×630 PNG with Node runtime and all-flexbox layout. Settled fields are stubbed per D-11 contract. The Duels tab, ⚔ OPEN badge, TRENDING DUEL pin, and Duel King badge (placeholder) are live in the feed. `pnpm --filter @call-it/web build` exits 0. Zero display:grid instances in any new duel surface.
</success_criteria>

<output>
After completion, create `.planning/phases/03-challengeescrow/03-07-SUMMARY.md`
</output>
