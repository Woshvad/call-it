---
phase: 03-challengeescrow
plan: 04
type: execute
wave: 4
depends_on: [03-03]
files_modified:
  - apps/relayer/src/db/schema.ts
  - packages/subgraph/src/challenge-escrow.ts
  - packages/subgraph/schema.graphql
  - packages/subgraph/subgraph.yaml
  - packages/subgraph/abis/ChallengeEscrow.json
autonomous: false
requirements:
  - SOCIAL-40
  - SOCIAL-41
  - SOCIAL-42
  - SOCIAL-48
  - SOCIAL-49
  - SOCIAL-50
  - SOCIAL-51

must_haves:
  truths:
    - "trending_duels and duel_kings tables exist in live Fly Postgres (verified via Drizzle migration)"
    - "challenge-escrow.ts exports real event handlers (not handleBlock stub) for all 7 ChallengeEscrow events"
    - "schema.graphql includes Challenge, ChallengePayout, UnclaimedOverage, TvlSnapshot entities with correct immutable flags"
    - "subgraph.yaml ChallengeEscrow data source uses real address + startBlock; no blockHandlers entry"
    - "graph build passes cleanly after changes"
    - "Sepolia Studio deploy completes (or operator checkpoint confirms deploy key set)"
  artifacts:
    - path: "apps/relayer/src/db/schema.ts"
      provides: "trending_duels + duel_kings Drizzle table definitions"
      contains: "trendingDuels"
    - path: "packages/subgraph/src/challenge-escrow.ts"
      provides: "Real AssemblyScript handlers for ChallengeProposed/Accepted/Rejected/Refunded/Settled/PayoutClaimed/UnclaimedOverageCreated"
      contains: "handleChallengeProposed"
    - path: "packages/subgraph/schema.graphql"
      provides: "Challenge, ChallengePayout, UnclaimedOverage, TvlSnapshot GraphQL entities"
      contains: "Challenge @entity"
    - path: "packages/subgraph/subgraph.yaml"
      provides: "ChallengeEscrow data source with event handlers, address, startBlock; Phase 0 blockHandlers removed"
      contains: "handleChallengeProposed"
  key_links:
    - from: "packages/subgraph/src/challenge-escrow.ts"
      to: "packages/subgraph/schema.graphql"
      via: "import Challenge, ChallengePayout, UnclaimedOverage, TvlSnapshot"
      pattern: "from '../generated/schema'"
    - from: "packages/subgraph/subgraph.yaml"
      to: "packages/subgraph/abis/ChallengeEscrow.json"
      via: "abis entry"
      pattern: "ChallengeEscrow.json"

user_setup:
  - service: fly-postgres-migration
    why: "Drizzle migration must be applied to live Fly Postgres via fly proxy tunnel"
    env_vars:
      - name: POSTGRES_URL
        source: "Existing .env — already set to 127.0.0.1:5433 from Phase 2 02-05 migration"
    dashboard_config:
      - task: "Open fly proxy tunnel before running db:migrate"
        location: "Terminal: fly proxy 5433:5432 -a call-it-pg-sepolia (keep running during migration)"
  - service: subgraph-studio-deploy
    why: "graph deploy:sepolia requires SUBGRAPH_STUDIO_DEPLOY_KEY (same key as Phase 2 plan 02-06)"
    env_vars:
      - name: SUBGRAPH_STUDIO_DEPLOY_KEY
        source: "Subgraph Studio dashboard → API keys (same key used in Phase 2)"
---

<objective>
Extend the Drizzle schema with trending_duels + duel_kings tables, apply the [BLOCKING] live migration, replace the Phase 0 challenge-escrow stub with real AssemblyScript handlers, update subgraph.yaml with the deployed ChallengeEscrow address + startBlock, and redeploy to Sepolia Studio.

Purpose: The subgraph is the single source of truth for Challenge entity state across the relayer and frontend. The Drizzle tables are required by the trending + Duel King workers (Plan 05). Both must exist before Plan 05's workers can run.

Output: Live Fly Postgres with 0003_* migration applied; Subgraph Studio updated with real ChallengeEscrow handlers; `graph build` clean.
</objective>

<execution_context>
@C:\Users\woshv\Desktop\Call it\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\woshv\Desktop\Call it\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\woshv\Desktop\Call it\.planning\STATE.md
@C:\Users\woshv\Desktop\Call it\.planning\phases\03-challengeescrow\03-CONTEXT.md
@C:\Users\woshv\Desktop\Call it\.planning\phases\03-challengeescrow\03-03-SUMMARY.md

<interfaces>
From apps/relayer/src/db/schema.ts (current — add tables after existing ones):
- Import pattern: pgTable, serial, varchar, text, timestamp, integer, index, uniqueIndex, jsonb from 'drizzle-orm/pg-core'
- boolean not currently imported — add if needed; jsonb and text already present
- notification-fanout.ts .onConflictDoNothing() pattern vs. .onConflictDoUpdate() for trending_duels

From packages/subgraph/src/follow-fade-market.ts (analog — follow exactly):
- AssemblyScript import style: import { BigInt } from '@graphprotocol/graph-ts'
- No closures, no null for value types
- ensureX() lazy-init pattern with zero defaults (BigInt.fromI32(0), '')
- @entity(immutable: false) for mutable entities, (immutable: true) for append-only

From packages/subgraph/subgraph.yaml (modification — ChallengeEscrow section lines 138-158):
- REPLACE the stub block handler section with real event handlers
- REMOVE blockHandlers entirely (Pitfall E)
- address: populated from Plan 03 deploy
- startBlock: deploy block number from Plan 03 output

From packages/subgraph/schema.graphql (existing — add entities):
- Existing entities: Call, Position, CallerExit, PoolInitialized, TvlSnapshot (may already exist), Profile
- Add: Challenge @entity(immutable: false), ChallengePayout @entity(immutable: true), UnclaimedOverage @entity(immutable: false), TvlSnapshot (may already exist with different fields — extend if present)

ChallengeEscrow ABI needed for subgraph.yaml:
- Build with `forge build` after Plan 02 — ABI is in out/ChallengeEscrow.sol/ChallengeEscrow.json
- Copy to packages/subgraph/abis/ChallengeEscrow.json

Drizzle migration workflow (Phase 2 02-05 exact pattern from STATE.md):
  1. Edit schema.ts (add trending_duels + duel_kings tables)
  2. Generate: `pnpm --filter @call-it/relayer run db:generate` → creates 0003_*.sql
  3. Start fly proxy: `fly proxy 5433:5432 -a call-it-pg-sepolia`
  4. Apply: `pnpm --filter @call-it/relayer run db:migrate`
  5. Verify tables exist in live Postgres
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drizzle schema + 0003_* migration generation</name>
  <files>apps/relayer/src/db/schema.ts</files>
  <read_first>
    apps/relayer/src/db/schema.ts — full file; understand existing table structure and imports before adding
    .planning/phases/03-challengeescrow/03-PATTERNS.md lines 565-617 — exact trending_duels + duel_kings table patterns
    apps/relayer/src/workers/notification-fanout.ts lines 272-282 — onConflictDoUpdate pattern for trending_duels
  </read_first>
  <action>
    Add to apps/relayer/src/db/schema.ts after the last existing table:

    trendingDuels table: id (serial PK), challengeId (integer notNull), trendingUntil (timestamp notNull), potUsdc (varchar 30 notNull default '0'), backerCount (integer notNull default 0), createdAt/updatedAt (timestamp defaultNow). Indexes: trendingDuelsChallengeIdx (uniqueIndex on challengeId — dedup by ON CONFLICT), trendingDuelsTrendingUntilIdx (index on trendingUntil — for delete-expired query).

    duelKings table: id (serial PK), winnerAddress (varchar 42 notNull), winStreak (integer notNull default 0), highestPotUsdc (varchar 30 notNull default '0'), lastWinAt (timestamp nullable), weekAnchor (timestamp notNull). Index: duelKingsWeekIdx (uniqueIndex on weekAnchor — one row per week). createdAt (timestamp defaultNow).

    Ensure boolean is NOT imported — both tables use integer/timestamp/varchar only. If jsonb is needed for future fields, leave it imported (already present).

    After adding tables: run `pnpm --filter @call-it/relayer run db:generate` to produce migration 0003_*.sql in the migrations folder. Verify it contains CREATE TABLE trending_duels and CREATE TABLE duel_kings.
  </action>
  <verify>
    <automated>cd "C:\Users\woshv\Desktop\Call it" && grep -c "trendingDuels\|duel_kings" apps/relayer/src/db/schema.ts</automated>
  </verify>
  <done>trending_duels + duel_kings table definitions in schema.ts; db:generate produces 0003_*.sql with both CREATE TABLE statements; `pnpm --filter @call-it/relayer build` exits 0 (TypeScript types from schema compile)</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Apply 0003_* Drizzle migration to live Fly Postgres</name>
  <what-built>0003_* migration generated with trending_duels + duel_kings CREATE TABLE statements</what-built>
  <how-to-verify>
    1. Open Fly proxy tunnel (keep running): `fly proxy 5433:5432 -a call-it-pg-sepolia`
    2. Apply migration: `pnpm --filter @call-it/relayer run db:migrate`
    3. Verify tables exist:
       `psql "postgresql://postgres@127.0.0.1:5433/call_it" -c "\dt"` — must show trending_duels and duel_kings in the table list
       `psql "postgresql://postgres@127.0.0.1:5433/call_it" -c "\d trending_duels"` — must show all columns
       `psql "postgresql://postgres@127.0.0.1:5433/call_it" -c "\d duel_kings"` — must show all columns
    4. Close proxy tunnel after verification.
  </how-to-verify>
  <resume-signal>Type "migration applied" after confirming both tables exist in live Postgres</resume-signal>
</task>

<task type="auto">
  <name>Task 3: challenge-escrow.ts real handlers + schema.graphql entities + subgraph.yaml + redeploy</name>
  <files>
    packages/subgraph/src/challenge-escrow.ts,
    packages/subgraph/schema.graphql,
    packages/subgraph/subgraph.yaml,
    packages/subgraph/abis/ChallengeEscrow.json
  </files>
  <read_first>
    packages/subgraph/src/challenge-escrow.ts — current Phase 0 stub (to be completely replaced)
    packages/subgraph/src/follow-fade-market.ts — exact analog for handler structure + AssemblyScript patterns
    packages/subgraph/schema.graphql — existing entities; find existing TvlSnapshot if present
    packages/subgraph/subgraph.yaml lines 138-158 — existing ChallengeEscrow stub section to replace
    .planning/phases/03-challengeescrow/03-PATTERNS.md lines 423-561 — all subgraph patterns including exact handler bodies, schema entities, yaml section
    .planning/phases/03-challengeescrow/03-RESEARCH.md lines 431-460 — Pattern 6 subgraph handlers
    packages/contracts out/ChallengeEscrow.sol/ChallengeEscrow.json — ABI (generated by forge build)
  </read_first>
  <action>
    Step 1: Copy the forge-generated ABI. After `forge build`: copy out/ChallengeEscrow.sol/ChallengeEscrow.json → packages/subgraph/abis/ChallengeEscrow.json. Filter to only include events and functions (drop bytecode/deployedBytecode sections if present — the subgraph only needs the ABI array).

    Step 2: Replace packages/subgraph/src/challenge-escrow.ts entirely. Remove handleBlock export. New file:
    - Header comment: "Phase 3 real handlers. Replaces Phase 0 stub. Pitfall E: handleBlock removed."
    - Imports: BigInt, Bytes from @graphprotocol/graph-ts; event types from '../generated/ChallengeEscrow/ChallengeEscrow'; entity types Challenge, ChallengePayout, UnclaimedOverage, TvlSnapshot from '../generated/schema'
    - ensureChallenge(challengeId: string) helper: lazy-init with zero defaults per AssemblyScript constraints (no null for value types). Fields: call (string, ''), challenger (Bytes, new Bytes(0)), caller (Bytes, new Bytes(0)), challengerStake (BigInt, 0), callerStake (BigInt, 0), status ('Proposed'), winner (nullable Bytes), proposedAt (BigInt, 0), acceptedAt (nullable BigInt, null), settledAt (nullable BigInt, null), overageClaimed (false)
    - handleChallengeProposed: sets call, challenger, caller, challengerStake, status='Proposed', proposedAt; challenge.save()
    - handleChallengeAccepted: updates callerStake, status='Accepted', acceptedAt; challenge.save()
    - handleChallengeRejected: updates status='Rejected'; challenge.save()
    - handleChallengeRefunded: updates status='Refunded'; challenge.save()
    - handleChallengeSettled: updates winner, status='Settled', settledAt; challenge.save()
    - handlePayoutClaimed: creates immutable ChallengePayout with id = txHash+logIndex; stores challengeId, winner, payout, protocolFee, timestamp
    - handleUnclaimedOverageCreated: creates UnclaimedOverage entity; stores challengeId, beneficiary, amount, timestamp

    Step 3: Update packages/subgraph/schema.graphql. Add entities:
    - Challenge @entity(immutable: false): id ID!, call String!, challenger Bytes!, caller Bytes!, challengerStake BigInt!, callerStake BigInt!, status String!, winner Bytes, proposedAt BigInt!, acceptedAt BigInt, settledAt BigInt, overageClaimed Boolean!
    - ChallengePayout @entity(immutable: true): id ID!, challengeId BigInt!, winner Bytes!, payout BigInt!, protocolFee BigInt!, timestamp BigInt!
    - UnclaimedOverage @entity(immutable: false): id ID!, challengeId BigInt!, beneficiary Bytes!, amount BigInt!, claimed Boolean!, timestamp BigInt!
    - TvlSnapshot: if already exists, add challengeEscrowTvl BigInt! field; if new, create with blockNumber, timestamp, challengeEscrowTvl, totalTvl

    Step 4: Update packages/subgraph/subgraph.yaml. Find the ChallengeEscrow stub section (lines ~138-158). Replace entirely with real event-handler data source per PATTERNS.md lines 522-561. Fill in: address from CHALLENGE_ESCROW_ARBITRUM_SEPOLIA (from addresses.ts / Plan 03); startBlock from Plan 03 deploy output. REMOVE blockHandlers section entirely (Pitfall E). eventHandlers must list all 7 events with exact signatures matching the Solidity events.

    Step 5: Build and deploy. `cd packages/subgraph && pnpm run codegen && pnpm run build`. Fix any AssemblyScript type errors. `graph auth $SUBGRAPH_STUDIO_DEPLOY_KEY && pnpm run deploy:sepolia`. If SUBGRAPH_STUDIO_DEPLOY_KEY not set: checkpoint for operator — see user_setup.

    CRITICAL CHECKS:
    - No handleBlock export anywhere in challenge-escrow.ts
    - No blockHandlers in the ChallengeEscrow yaml section
    - All AssemblyScript: no closures, no null for value types, BigInt.fromI32(0) not 0
    - @entity(immutable: false) on Challenge and UnclaimedOverage; @entity(immutable: true) on ChallengePayout
  </action>
  <verify>
    <automated>cd "C:\Users\woshv\Desktop\Call it\packages\subgraph" && pnpm run build 2>&1 | tail -10</automated>
  </verify>
  <done>packages/subgraph/src/challenge-escrow.ts has no handleBlock export; grep confirms "handleChallengeProposed" is exported; `pnpm run build` in packages/subgraph exits 0; subgraph.yaml ChallengeEscrow section has no blockHandlers key; schema.graphql contains "Challenge @entity"; Sepolia Studio deploy completes (or operator checkpoint confirmed deploy key)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live Postgres migration | db:migrate via fly proxy modifies production-class Sepolia DB; irreversible DDL |
| Subgraph event handler correctness | AssemblyScript handlers consume on-chain events; incorrect entity shapes cause silent data loss |
| startBlock accuracy | Wrong startBlock causes missed historical events — unchallengeables from before the block are invisible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-04-01 | Tampering | Wrong ChallengeEscrow address in subgraph.yaml | mitigate | Address copied from CHALLENGE_ESCROW_ARBITRUM_SEPOLIA in addresses.ts (Plan 03 output); grep check confirms address matches |
| T-3-04-02 | Denial of Service | Phase 0 blockHandlers block conflicting with event handlers | mitigate | blockHandlers section removed from subgraph.yaml entirely (Pitfall E); grep for "blockHandlers" in yaml returns 0 |
| T-3-04-03 | Tampering | AssemblyScript null for value type (BigInt) causes subgraph crash | mitigate | Use BigInt.fromI32(0) not 0 for unset BigInt fields; follow-fade-market.ts pattern enforced; graph build catches type errors |
| T-3-04-04 | Tampering | Drizzle migration schema drift (schema.ts vs DB) | mitigate | db:generate produces migration from schema.ts diff; operator applies and verifies tables via psql before resuming |
| T-3-04-05 | Elevation of Privilege | Subgraph deploy key exposure | mitigate | SUBGRAPH_STUDIO_DEPLOY_KEY read from env; never logged (graph auth reads from env or keystore); not committed |
</threat_model>

<verification>
- Schema tables defined: `grep -c "trendingDuels\|duelKings" apps/relayer/src/db/schema.ts` >= 2
- Migration generated: `ls apps/relayer/drizzle/migrations/0003_*.sql` exists
- Live tables: confirmed via operator psql check (Task 2 checkpoint)
- No stub handleBlock: `grep -c "handleBlock" packages/subgraph/src/challenge-escrow.ts` = 0
- Real handler exported: `grep -c "handleChallengeProposed" packages/subgraph/src/challenge-escrow.ts` = 1
- No blockHandlers in yaml: `grep -c "blockHandlers" packages/subgraph/subgraph.yaml` = 0
- Subgraph build: `cd packages/subgraph && pnpm run build` exits 0
- Schema entities: `grep -c "Challenge @entity" packages/subgraph/schema.graphql` >= 1
</verification>

<success_criteria>
Live Fly Postgres has trending_duels and duel_kings tables (0003_* migration applied). The challenge-escrow subgraph is deployed to Sepolia Studio with real event handlers. `graph build` exits 0. All 7 event handlers are in place. Plan 05's workers can now write to Postgres and query the subgraph.
</success_criteria>

<output>
After completion, create `.planning/phases/03-challengeescrow/03-04-SUMMARY.md`
</output>
