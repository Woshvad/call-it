---
phase: 03-challengeescrow
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/interfaces/IChallengeEscrow.sol
  - packages/contracts/test/helpers/CeTestHelper.sol
  - packages/contracts/test/ChallengeEscrow.t.sol
  - packages/contracts/test/ChallengeEscrowGates.t.sol
  - packages/contracts/test/ChallengeEscrowParity.t.sol
  - apps/web/src/__tests__/challenge-gates.test.ts
autonomous: true
requirements:
  - SOCIAL-29
  - SOCIAL-30
  - SOCIAL-31
  - SOCIAL-32
  - SOCIAL-33
  - SOCIAL-34
  - SOCIAL-35
  - SOCIAL-36
  - SOCIAL-38
  - SOCIAL-39
  - SOCIAL-46
  - SOCIAL-47
  - SOCIAL-48

must_haves:
  truths:
    - "IChallengeEscrow.sol defines the complete locked §12.3 interface: ChallengeStatus enum, Challenge struct, all events, all custom errors, all function signatures"
    - "CeTestHelper boots the 4-contract stack (adds ChallengeEscrow on top of FfmTestHelper's 3-contract stack)"
    - "All propose/accept/reject/refund/claim tests exist and fail (RED gate — ChallengeEscrow.sol does not yet exist)"
    - "Gate tests for SelfChallenge, stake bounds, acceptance window, TVL cap, openToChallenges flag exist and fail"
    - "Vitest parity tests for challenge gate logic exist and pass against the TS mirror"
    - "Fuzz invariant stubs for overage conservation, escrow accounting, payout ceiling exist"
  artifacts:
    - path: "packages/contracts/src/interfaces/IChallengeEscrow.sol"
      provides: "LOCKED §12.3 interface — enum/struct/events/errors/function signatures"
      contains: "ChallengeStatus"
    - path: "packages/contracts/test/helpers/CeTestHelper.sol"
      provides: "Abstract test base extending FfmTestHelper with ChallengeEscrow deploy"
      contains: "CeTestHelper"
    - path: "packages/contracts/test/ChallengeEscrow.t.sol"
      provides: "Full propose/accept/reject/refund/claim test matrix + fuzz invariants"
      contains: "testSelfChallengeBanned"
    - path: "packages/contracts/test/ChallengeEscrowGates.t.sol"
      provides: "Gate tests for all SOCIAL-29..39 reverts"
      contains: "testProposeRevertsNotOpen"
    - path: "apps/web/src/__tests__/challenge-gates.test.ts"
      provides: "Vitest parity for stake bounds / self-challenge / window / openToChallenges (D-29)"
  key_links:
    - from: "packages/contracts/test/helpers/CeTestHelper.sol"
      to: "packages/contracts/test/helpers/FfmTestHelper.sol"
      via: "abstract contract CeTestHelper is FfmTestHelper"
      pattern: "is FfmTestHelper"
    - from: "packages/contracts/test/ChallengeEscrow.t.sol"
      to: "packages/contracts/src/interfaces/IChallengeEscrow.sol"
      via: "import IChallengeEscrow for type assertions"
      pattern: "IChallengeEscrow"
---

<objective>
Define the ChallengeEscrow §12.3 LOCKED interface and write all failing tests before the contract exists (RED gate). This is the TDD foundation for Plan 02's GREEN implementation.

Purpose: Interface-first ordering — downstream plans (relayer, subgraph, frontend) import the IChallengeEscrow types. Test-first ordering — Plan 02 executor knows exactly which behaviors must pass. Both contracts and tests commit separately so CI proves RED before GREEN.

Output: IChallengeEscrow.sol (LOCKED, do not modify in Plan 02), CeTestHelper.sol, ChallengeEscrow.t.sol, ChallengeEscrowGates.t.sol, ChallengeEscrowParity.t.sol (Foundry all RED), challenge-gates.test.ts (Vitest GREEN on TS mirror).
</objective>

<execution_context>
@C:\Users\woshv\Desktop\Call it\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\woshv\Desktop\Call it\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\woshv\Desktop\Call it\.planning\PROJECT.md
@C:\Users\woshv\Desktop\Call it\.planning\ROADMAP.md
@C:\Users\woshv\Desktop\Call it\.planning\STATE.md
@C:\Users\woshv\Desktop\Call it\.planning\phases\03-challengeescrow\03-CONTEXT.md
@C:\Users\woshv\Desktop\Call it\.planning\phases\03-challengeescrow\03-RESEARCH.md
@C:\Users\woshv\Desktop\Call it\.planning\phases\03-challengeescrow\03-PATTERNS.md

<interfaces>
<!-- Key contracts and types the executor needs. Do not re-read these files in full. -->

From packages/contracts/src/interfaces/IFollowFadeMarket.sol (analog — mirror structure exactly):
- SPDX + pragma =0.8.30 header pattern
- One event per operation with indexed callId/user params
- One error per gate condition
- Function signatures with NatSpec

From packages/contracts/test/helpers/FfmTestHelper.sol (analog — extend, do not replace):
- abstract contract FfmTestHelper is TestBase, StdAssertions, StdChains, StdCheats, StdUtils
- setUp() boots ProfileRegistry + CallRegistry + FollowFadeMarket + MockUSDC etched at USDC_ARB_NATIVE
- alice, bob, owner, treasury addresses established; MockUSDC.mint() funded
- _seedPool(callId, aliceStake) helper creates a Live+openToChallenges call

From packages/contracts/src/CallRegistry.sol (settle seam analog):
- address public settlementManager;
- modifier onlySettlementManager()
- function setSettlementManager(address newManager) external onlyOwner

From packages/contracts/src/FollowFadeMarket.sol (TVL cap pattern, lines 254-260):
- uint256 combinedTvl = callRegistry.currentTvl() + getTvl();
- if (combinedTvl + amountIn > callRegistry.tvlCap()) revert TvlCapReached(...)
- ChallengeEscrow extends to 3-way: adds followFadeMarket.getTvl() + totalEscrow

ChallengeStatus enum (§12.3 LOCKED):
  Proposed = 0, Accepted = 1, Rejected = 2, Refunded = 3, Settled = 4

Challenge struct (§12.3 + D-09 RESEARCH.md Pattern 1):
  uint256 callId, address caller, address challenger,
  uint96 callerStake, uint96 challengerStake,
  uint64 proposedAt, address winner,
  ChallengeStatus status,
  bool callerClaimed, bool challengerClaimed, bool overageClaimed

Events (all indexed on challengeId):
  ChallengeProposed(uint256 indexed challengeId, uint256 indexed callId, address indexed challenger, uint96 challengerStake)
  ChallengeAccepted(uint256 indexed challengeId, address indexed caller, uint96 callerStake)
  ChallengeRejected(uint256 indexed challengeId, address indexed caller)
  ChallengeRefunded(uint256 indexed challengeId, address indexed challenger, uint96 amount)
  ChallengeSettled(uint256 indexed challengeId, address indexed winner)
  PayoutClaimed(uint256 indexed challengeId, address indexed winner, uint256 payout, uint256 protocolFee)
  OveragePushed(uint256 indexed challengeId, address indexed recipient, uint256 amount)
  UnclaimedOverageCreated(uint256 indexed challengeId, address indexed beneficiary, uint256 amount)
  SettlementManagerSet(address indexed newManager)

Errors:
  CallerNotOpenToChallenges(), SelfChallenge(), CallNotChallengeable()
  AcceptanceWindowExpired(), ClaimRefundNotAvailable()
  NotDuelWinner(), AlreadyClaimed()
  ChallengeNotAccepted(), ChallengeNotSettled()
  NotOverageRecipient(), StakeBelowMinimum(), StakeAboveMaximum()
  TvlCapReached(uint256 requested, uint256 available)
  NotSettlementManager(), NotAuthorized()

Constants (per §12.3 + §8.4):
  CHALLENGE_ACCEPTANCE_WINDOW = 24 hours
  MIN_STAKE = 5e6 ($5 USDC)
  MAX_STAKE = 100e6 ($100 USDC)
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: IChallengeEscrow.sol — LOCKED §12.3 interface + CeTestHelper + RED Foundry tests</name>
  <files>
    packages/contracts/src/interfaces/IChallengeEscrow.sol,
    packages/contracts/test/helpers/CeTestHelper.sol,
    packages/contracts/test/ChallengeEscrow.t.sol,
    packages/contracts/test/ChallengeEscrowGates.t.sol,
    packages/contracts/test/ChallengeEscrowParity.t.sol
  </files>
  <read_first>
    packages/contracts/src/interfaces/IFollowFadeMarket.sol — mirror structure (events, errors, function sigs)
    packages/contracts/test/helpers/FfmTestHelper.sol — base to extend for CeTestHelper
    packages/contracts/src/CallRegistry.sol lines 410-448 — setSettlementManager/onlySettlementManager exact pattern
    packages/contracts/src/FollowFadeMarket.sol lines 51-60 — contract declaration + SafeERC20 pattern
    packages/contracts/foundry.toml — pragma pin and fuzz profile
    packages/contracts/test/TvlAggregation.t.sol — test structure analog
    .planning/phases/03-challengeescrow/03-PATTERNS.md — IChallengeEscrow.sol pattern (lines 180-253), CeTestHelper pattern (lines 258-321)
    .planning/phases/03-challengeescrow/03-RESEARCH.md — Pattern 1 (struct), Pattern 4 (overage), Pattern 5 (CEI claim)
  </read_first>
  <behavior>
    - IChallengeEscrow.sol: pragma =0.8.30; SPDX MIT; full §12.3 interface with ChallengeStatus enum, Challenge struct, all 9 events, all 14 custom errors, all function signatures; NatSpec comment on claimDuelPayout + claimOverage marking §10.3 pause carve-out
    - CeTestHelper.sol: abstract contract extending FfmTestHelper; constructor deploys ChallengeEscrow with (registry, ffm, USDC_ARB_NATIVE, treasury, 5_000e6); challenger = makeAddr("challenger"); usdc.mint(challenger, 1000e6); approve ChallengeEscrow for alice/bob/challenger; exposes _proposeChallenge(from, callId, stake) helper
    - ChallengeEscrow.t.sol: imports ChallengeEscrow (will fail compile — RED gate intended); tests: testProposeAndAccept (happy path), testRejectRefundsImmediately, testClaimRefundAfterWindow (vm.warp 25h), testClaimDuelPayout_winner (mock settleDuel), testClaimDuelPayout_idempotent (AlreadyClaimed), testClaimDuelPayout_nonWinner (NotDuelWinner), testAsymmetricPot (pot = min*2), testOveragePushFail (mock token returning false), testClaimOverageLosing, fuzz_escrowConservation, fuzz_payoutCeiling, fuzz_overageConservation
    - ChallengeEscrowGates.t.sol: testProposeRevertsNotOpen (SOCIAL-29), testSelfChallengeBanned (SOCIAL-32), testChallengeNotLive (SOCIAL-33), testWindowExpired (SOCIAL-34), testTvlCap3Way (D-04 both propose+accept), testStakeBelowMinimum, testStakeAboveMaximum
    - ChallengeEscrowParity.t.sol: imports challenge gate parity Foundry side (mirrors TS test cases); tests identical gate conditions as Vitest file
    - All Foundry tests: compile fails because ChallengeEscrow.sol does not exist — this is the expected RED gate; commit with message "test(03-01): add failing ChallengeEscrow tests (RED)"
  </behavior>
  <action>
    Step 1 (RED): Create IChallengeEscrow.sol first — this compiles standalone (no contract dependency). Pragma =0.8.30 exact. Interface declares ChallengeStatus enum (Proposed/Accepted/Rejected/Refunded/Settled), Challenge struct with fields per RESEARCH.md Pattern 1, all events with indexed params per PATTERNS.md lines 220-233, all errors per PATTERNS.md lines 236-253, function signatures: proposeChallenge(uint256 callId, uint96 stake) returns (uint256 challengeId); acceptChallenge(uint256 challengeId); rejectChallenge(uint256 challengeId); claimRefund(uint256 challengeId); settleDuel(uint256 challengeId, address winner) — onlySettlementManager; claimDuelPayout(uint256 challengeId); claimOverage(uint256 challengeId); getTvl() returns (uint256); getChallenge(uint256 challengeId) returns (Challenge memory); setSettlementManager(address newManager); pause(); unpause().

    Step 2 (RED): Create CeTestHelper.sol extending FfmTestHelper per PATTERNS.md lines 258-321. Do NOT inherit StdInvariant (same C3 rule as FfmTestHelper). Challenger address + mint + max approve pattern. ChallengeEscrow constructor args: (address(registry), address(ffm), USDC_ARB_NATIVE, treasury, 5_000e6). _proposeChallenge(address from, uint256 callId, uint96 stake) helper uses vm.prank(from).

    Step 3 (RED): Create three test files. All import ChallengeEscrow from "../../src/ChallengeEscrow.sol" — file does not exist so compile fails. This is mandatory. Fuzz tests use bounded inputs: stake bounded to [5e6, 100e6]; callerStake/challengerStake bounded separately. Use vm.expectRevert with named error selectors (not strings). vm.warp for time-dependent tests.

    Step 4: Run `forge build --match-path "packages/contracts/test/ChallengeEscrow*"` — expect compile failure on ChallengeEscrow.sol missing. IChallengeEscrow.sol should compile standalone. Commit: "test(03-01): add failing ChallengeEscrow tests (RED gate — contract not yet created)".

    CLAUDE.md constraints: pragma =0.8.30 exact; USDC_ARB_NATIVE from ./constants/USDC.sol; no inline hex addresses; Ownable2Step pattern from CallRegistry analog; CEI comments on all claim functions.
  </action>
  <verify>
    <automated>cd "C:\Users\woshv\Desktop\Call it" && forge build --match-path "packages/contracts/src/interfaces/IChallengeEscrow.sol" 2>&1 | grep -c "Compiler run successful"</automated>
  </verify>
  <done>IChallengeEscrow.sol compiles standalone (forge build exits 0 for that file); all three test files exist and reference ChallengeEscrow.sol which does not yet exist; git commit "test(03-01)" present in log; grep confirms pragma =0.8.30 in IChallengeEscrow.sol</done>
</task>

<task type="tdd">
  <name>Task 2: Vitest challenge-gates parity (D-29) — GREEN gate</name>
  <files>
    apps/web/src/__tests__/challenge-gates.test.ts
  </files>
  <read_first>
    packages/shared/src/validation/ — existing Zod schema patterns from Phase 1/2
    apps/web/src/__tests__/ — existing test structure for call-gates parity
    .planning/phases/03-challengeescrow/03-RESEARCH.md — Vitest Parity section
    .planning/phases/03-challengeescrow/03-VALIDATION.md — parity requirements
  </read_first>
  <behavior>
    - TypeScript Zod schema mirror for challenge stake bounds (MIN_STAKE = 5_000_000n USDC micros, MAX_STAKE = 100_000_000n), openToChallenges flag check, self-challenge address equality check, acceptance window check (proposedAt + 86400 > now)
    - Tests: stakeBelowMinimum (4_999_999n rejects), stakeAtMinimum (5_000_000n accepts), stakeAboveMaximum (100_000_001n rejects), stakeAtMaximum (100_000_000n accepts), selfChallengeDetected (caller == challenger address), openToChallengesFlag (false = error), windowExpired (proposedAt more than 24h ago), windowValid (proposedAt 23h ago)
    - All tests GREEN (these are pure TS logic — no contract dependency)
    - `pnpm --filter @call-it/web test --run challenge-gates` exits 0
  </behavior>
  <action>
    Create apps/web/src/__tests__/challenge-gates.test.ts using vitest. Define challengeGates object with: validateStake(stake: bigint): { ok: boolean, error?: string }; isSelfChallenge(caller: string, challenger: string): boolean; isWindowExpired(proposedAt: number, nowTs: number): boolean; isOpenToChallenges(flag: boolean): boolean. Constants: CHALLENGE_MIN_STAKE = 5_000_000n; CHALLENGE_MAX_STAKE = 100_000_000n; CHALLENGE_ACCEPTANCE_WINDOW_SECS = 86400. Write all 8 unit tests using describe/it/expect from vitest. Import from the same file (or inline) — no contract imports. Matches the Foundry gate test names exactly (same gate logic, different language). Run `pnpm --filter @call-it/web test --run challenge-gates` — must exit 0.
  </action>
  <verify>
    <automated>cd "C:\Users\woshv\Desktop\Call it" && pnpm --filter @call-it/web test --run challenge-gates 2>&1 | tail -5</automated>
  </verify>
  <done>challenge-gates.test.ts exists; `pnpm --filter @call-it/web test --run challenge-gates` exits 0; 8 test cases all green; grep confirms CHALLENGE_MIN_STAKE = 5_000_000n in the file</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Interface definition | IChallengeEscrow.sol is consumed by Plan 02 (contract), Plan 03 (deploy), Plan 04 (subgraph ABI), Plan 05 (relayer ABI). Any drift here breaks all downstream. |
| Test suite | Foundry tests define the behavioral contract Plan 02 must satisfy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01-01 | Tampering | IChallengeEscrow.sol | mitigate | Interface is LOCKED per §12.3 — NatSpec comment states "LOCKED DO NOT MODIFY" at file header; any modification must be flagged; Plan 02 implements against this interface, not vice versa |
| T-3-01-02 | Spoofing | CeTestHelper actor setup | mitigate | challenger is a distinct makeAddr from alice/bob/owner — prevents test-actor confusion where self-challenge gate would not be exercised |
| T-3-01-03 | Tampering | Fuzz invariant seeds | accept | Fuzz seeds are non-cryptographic; invariant correctness matters, not seed security; risk is low |
| T-3-01-04 | Denial of Service | Compile RED gate | accept | Compile failure is intentional and documented; CI will be red until Plan 02 ships; acceptable — this is the TDD protocol |
</threat_model>

<verification>
- IChallengeEscrow.sol builds standalone: `forge build --match-path "*/interfaces/IChallengeEscrow.sol"` exits 0
- Test files reference non-existent ChallengeEscrow.sol (RED gate confirmed): `forge build --match-path "*/test/ChallengeEscrow*"` exits non-0 with "file not found" or similar for ChallengeEscrow.sol
- Vitest parity GREEN: `pnpm --filter @call-it/web test --run challenge-gates` exits 0
- Pragma pin: `grep -n "pragma solidity" packages/contracts/src/interfaces/IChallengeEscrow.sol` shows `=0.8.30`
- No USDC literal: `grep -n "0xaf88d065" packages/contracts/src/interfaces/IChallengeEscrow.sol` returns 0 results (interfaces should not have transfer paths)
- Commit exists: `git log --oneline | head -5` shows "test(03-01):" commit
</verification>

<success_criteria>
IChallengeEscrow.sol is the canonical §12.3 locked interface — all downstream plans import types from it. Foundry tests are FAILING (RED) — Plan 02 makes them GREEN. Vitest parity is PASSING — the TS mirror of challenge gate logic matches Foundry semantics. The test/interface split ensures interface stability before implementation.
</success_criteria>

<output>
After completion, create `.planning/phases/03-challengeescrow/03-01-SUMMARY.md`
</output>
