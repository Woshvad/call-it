---
phase: 03-challengeescrow
plan: 06
type: execute
wave: 6
depends_on: [03-05]
files_modified:
  - apps/web/app/duel/[challengeId]/page.tsx
  - apps/web/app/call/[id]/page.tsx
autonomous: true
requirements:
  - SOCIAL-30
  - SOCIAL-34
  - SOCIAL-35
  - SOCIAL-36
  - UI-11

must_haves:
  truths:
    - "Duel page at /duel/[challengeId] renders THE MARKET hero with asset pair, pot, settles-in countdown"
    - "Two-column duel card renders CALLER (yellow-green) / VS divider / CHALLENGER (orange) in flex-direction:row — never display:grid"
    - "MARKET CONSENSUS LIVE bar shows followReserve/fadeReserve ratio from /api/duels/:id/live-state; updates on ~5s poll"
    - "Riding sections display existing FollowFadeMarket follow/fade participant lists (D-06 — not a new mechanic)"
    - "Side with [caller] CTA triggers Follow modal on parent call; Side with [challenger] CTA triggers Fade modal"
    - "Challenge form modal: stake input pre-filled to caller's stake (SOCIAL-30); override allowed; zod bounds; USDC preflight; Send Challenge calls proposeChallenge"
    - "Live Receipt page shows pending challenge block when a challenge exists (challenge_proposed notification)"
    - "Wallet address never shown on Duel page (AUTH-44)"
    - "Best viewed on desktop mobile banner renders at viewport <= 768px"
  artifacts:
    - path: "apps/web/app/duel/[challengeId]/page.tsx"
      provides: "Full Duel page per §15.5 + UI-SPEC Surface 1: hero, two-column card, consensus bar, Riding, CTAs, Challenge form modal"
      contains: "duel_livestate"
      min_lines: 200
    - path: "apps/web/app/call/[id]/page.tsx"
      provides: "Live Receipt addition: pending challenge notification block (Surface 7 post-submit) + Challenge button with Challenge form inline"
      contains: "challenge_proposed"
  key_links:
    - from: "apps/web/app/duel/[challengeId]/page.tsx"
      to: "apps/relayer/src/routes/duel-live-state.ts"
      via: "fetch /api/duels/:id/live-state every 5s + window focus"
      pattern: "duelLiveState"
    - from: "apps/web/app/duel/[challengeId]/page.tsx"
      to: "packages/shared/src/constants/addresses.ts"
      via: "CHALLENGE_ESCROW_ARBITRUM_SEPOLIA import for wagmi writes"
      pattern: "CHALLENGE_ESCROW_ARBITRUM_SEPOLIA"
---

<objective>
Build the Duel page (/duel/[challengeId]) and extend the Live Receipt page with the challenge pending block and Challenge form. This plan delivers the full user-facing duel interaction surface: view a duel, see live stats, back a side, and propose a challenge.

Purpose: The Duel page is the distribution anchor for Phase 3 — shared OG cards, trending pins, and Duel King badges all point here. Without this page, Phase 3 has no end-to-end user story.

Output: /duel/[challengeId] renders all §15.5 sections with live data. /call/[id] shows the pending challenge block and Challenge form. Both pages build with zero TypeScript errors.
</objective>

<execution_context>
@C:\Users\woshv\Desktop\Call it\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\woshv\Desktop\Call it\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\woshv\Desktop\Call it\.planning\STATE.md
@C:\Users\woshv\Desktop\Call it\.planning\phases\03-challengeescrow\03-CONTEXT.md
@C:\Users\woshv\Desktop\Call it\.planning\phases\03-challengeescrow\03-UI-SPEC.md
@C:\Users\woshv\Desktop\Call it\.planning\phases\03-challengeescrow\03-05-SUMMARY.md

<interfaces>
From apps/web/app/call/[id]/page.tsx (analog — mirror data fetch + liveness pattern exactly):
- 'use client'; useState/useEffect/useCallback
- fetchCallData() pattern → mirror as fetchDuelLiveState(challengeId)
- 5s setInterval + window.addEventListener('focus') + cleanup on unmount
- usePrivy() + useAccount() for auth state
- useWriteContract + useWaitForTransactionReceipt for on-chain writes
- RELAYER_URL = process.env['NEXT_PUBLIC_RELAYER_URL']

From packages/ui/src/ (Phase 1 primitives — reuse, do not re-implement):
- CornerBrackets — wrap the two-column duel card (4px, #E8F542)
- Skeleton — duel-card-stub variant for loading state
- Card — wrap each duel column panel individually
- Tag — filter chips, ⚔ OPEN badge, TRENDING DUEL label
- Toast / ToastProvider — challenge sent / error feedback
- ConvictionBar (display mode, no slider) — MARKET CONSENSUS bar with dual colors

From packages/contracts/src/interfaces/IChallengeEscrow.sol (Plan 01):
- proposeChallenge(uint256 callId, uint96 stake) — wagmi useWriteContract call
- ChallengeStatus enum ordinals: 0=Proposed, 1=Accepted, 2=Rejected, 3=Refunded, 4=Settled

From apps/web/lib/abis/ (Phase 2 established pattern):
- Inline minimal ABI for proposeChallenge in the Duel page component (same pattern as og/[callId] inline ABI)

UI-SPEC color contracts (LOCKED):
  CALLER column: #E8F542 (left border 3px, header text, handle text)
  CHALLENGER column: #FB923C (right border 3px, header text, handle text)
  MUST NOT be swapped — carries meaning across all Phase 3 surfaces

UI-SPEC layout rules (LOCKED):
  Two-column duel card: display:flex; flex-direction:row (NEVER display:grid — Pitfall 15)
  All Duel page layouts: flexbox only
  Mobile banner at <=768px: "BEST VIEWED ON DESKTOP · Some features may not work on mobile."
  Wallet address never shown (AUTH-44) — handle + rep only

Challenge form (UI-SPEC Surface 7):
  Pre-fill stake to call.stake from live-state (SOCIAL-30)
  Override allowed within $5–$100 bounds (zod validation matches Foundry gates)
  Inline USDC allowance + balance preflight (Phase 1 pattern from /new page)
  Asymmetric duel notice when stakes differ
  "Send Challenge ▸" CTA → useWriteContract proposeChallenge
  Post-submit: close modal + Toast "Challenge sent — [handle] has 24h to accept."

Live Receipt pending block (UI-SPEC Surface 7 — added to /call/[id]):
  Appears when GET /api/notifications returns a challenge_proposed event for this call
  Style: 2px #FB923C left border; #111118 bg; text: "⚔ Challenge pending — [challenger] challenged this call · [caller] has [X]h to accept or reject"
  Caller-only: "You have [X]h to accept or reject this challenge." + accept/reject inline buttons
  Accept: USDC allowance/balance preflight first — callerMatchingStake = min(challenge.challengerStake, callerInputOrDefault). Read USDC.allowance(caller, CHALLENGE_ESCROW) via useReadContract; if allowance < callerMatchingStake, show "Approve USDC first" step (same pattern as /new page Phase 1 preflight). Only after allowance >= callerMatchingStake: useWriteContract approve(CHALLENGE_ESCROW, callerMatchingStake) → then useWriteContract acceptChallenge(challengeId)
  Reject: inline confirmation → useWriteContract rejectChallenge(challengeId)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Duel page /duel/[challengeId]/page.tsx — full §15.5 implementation</name>
  <files>apps/web/app/duel/[challengeId]/page.tsx</files>
  <read_first>
    apps/web/app/call/[id]/page.tsx — full data-fetch + liveness + wagmi pattern to mirror
    apps/web/app/call/[id]/layout.tsx — layout pattern for the route
    .planning/phases/03-challengeescrow/03-UI-SPEC.md Surface 1 — full layout + color contract
    .planning/phases/03-challengeescrow/03-PATTERNS.md lines 800-965 — duel page pattern (data fetch, 5s poll, two-column layout, challenge form)
    packages/ui/src/ — CornerBrackets, Skeleton, Card, Tag, Toast, ConvictionBar primitives API
    apps/web/app/components/GlobalNav.tsx — existing nav to understand page composition
  </read_first>
  <action>
    Create apps/web/app/duel/[challengeId]/page.tsx as a client component ('use client').

    State: duelLiveState (from /api/duels/:id/live-state), parentCallData (from existing /api/calls/:id/live-state for followReserve/fadeReserve/expiry), callerProfile, challengerProfile (from /api/profile/:address), ridingCaller (follow positions), ridingChallenger (fade positions), challengeFormOpen (boolean), challengeStake (string, pre-filled), loading/error states.

    Data fetch: fetchDuelLiveState(challengeId) fetches NEXT_PUBLIC_RELAYER_URL + /api/duels/:id/live-state. fetchParentLiveState(callId) fetches existing /api/calls/:id/live-state (same endpoint from Plan 02-07). Riding lists: fetch subgraph follow/fade positions on parent call via relayer (or inline GraphQL — use the relayer /api/duels endpoint's parentCall data if included). On 5s interval + window focus: re-fetch duelLiveState + parentLiveState.

    Layout (all flexbox — display:flex EVERYWHERE, display:grid FORBIDDEN):
    1. Back link + header bar (← Back · duel #d/{N} · arbitrum · LOCKED · {countdown})
    2. THE MARKET hero block (background #111118, 3px #2E2E42 border, CornerBrackets): section label "THE MARKET" in 12px Space Grotesk 700 uppercase 0.12em tracked #64748B; asset pair "{assetA} / {assetB}" in 64px Syne 700 with "/" in #E8F542; question line 16px #F1F5F9; 3-stat row (LIVE SPREAD / POT / SETTLES IN) with 2px #1E1E2E borders between cells; values in 16px JetBrains Mono
    3. Two-column duel card (flex-direction:row): wrapped in CornerBrackets 4px #E8F542; CALLER column (flex:1, 3px #E8F542 left border, #111118 bg): "CALLER" header in 12px #E8F542 uppercase, avatar 40px circle 2px #E8F542 border, handle in 16px Space Grotesk 700 #E8F542; POSITION box; stat rows (REP/ACCURACY/IN-CATEGORY/STREAK each separated by 1px #1E1E2E divider); VS divider (width:48px, 1px #2E2E42 line, "VS" in 12px #64748B 700 centered); CHALLENGER column (flex:1, 3px #FB923C right border): mirror CALLER with #FB923C accents
    4. MARKET CONSENSUS LIVE bar: label with 6px #4ADE80 pulse dot next to "· LIVE"; 8px bar with #E8F542 caller fill / #FB923C challenger fill; fill width = followReserve/(followReserve+fadeReserve); "X% FAVOR CALLER" left #E8F542, "Y% FAVOR CHALLENGER" right #FB923C; smooth 300ms width transition on update; "· updating" pulse when stale >15s
    5. Riding sections (D-06 — existing FollowFadeMarket data): flex-direction:row two-column; "Riding [callerHandle]" left (12px #E8F542 handle portion) / "Riding [challengerHandle]" right (12px #FB923C handle portion); each item: 24px avatar + 12px handle + 12px JetBrains Mono amount; max 5 visible + "+ N more" in 12px #E8F542; empty state "No one riding yet" in 12px #64748B
    6. Bottom CTAs (flex-direction:row): "Side with [callerHandle]" (filled #E8F542 bg, #09090E text, 3px black border, hard shadow 4px 4px 0 #09090E, hover shadow 2px 2px) → triggers existing Follow modal on parent callId; "Side with [challengerHandle]" (#FB923C outline, #FB923C border 3px, #FB923C text, hover fills) → triggers existing Fade modal
    7. Challenge form modal (when viewer is not caller and challengeFormOpen==true): overlay rgba(9,9,14,0.8); panel #111118 bg, 3px #E8F542 border, CornerBrackets 4px #E8F542; title "CHALLENGE [callerHandle]" 32px Space Grotesk 700; read-only parent call card; YOUR STAKE input pre-filled to callerStake (SOCIAL-30 — reads from duelLiveState.callerStake / parentCallData.stake); quick-stake buttons $5/$25/$50/$100; POT IF ACCEPTED computed live: min(input, callerStake)*2; zod bounds: MIN_STAKE=5_000_000n, MAX_STAKE=100_000_000n; USDC allowance + balance inline preflight; Send Challenge → useWriteContract({ address: CE_ADDR, abi: minimalABI, functionName: 'proposeChallenge', args: [callId, stake] }); on success: close modal + Toast "Challenge sent — [handle] has 24h to accept."
    8. Mobile banner: renders at CSS @media (max-width: 768px) or checked via windowWidth state; "BEST VIEWED ON DESKTOP · Some features may not work on mobile." in full-width 2px top+bottom #FB923C border, #111118 bg

    Caller view additions (when viewer == duelLiveState.caller and challenge is Proposed):
    - "You have [X]h to accept or reject this challenge." block (2px #FB923C left border)
    - Accept button: USDC allowance/balance preflight — callerMatchingStake = min(duelLiveState.challengerStake, duelLiveState.callerStake || duelLiveState.challengerStake). Read USDC allowance via useReadContract(USDC, allowance, [callerAddress, CHALLENGE_ESCROW_ARBITRUM_SEPOLIA]). If insufficient: show sub-step "Approve USDC" button → useWriteContract approve(CHALLENGE_ESCROW_ARBITRUM_SEPOLIA, callerMatchingStake); after approval confirmed: enable Accept. Accept CTA: useWriteContract acceptChallenge(challengeId). Mirror the /new page Phase 1 preflight pattern exactly.
    - Reject: inline "Reject challenge" → confirmation → useWriteContract rejectChallenge(challengeId)

    CRITICAL: No display:grid anywhere. Wallet address never shown — only handle (from ProfileRegistry/relayer /api/profile). AUTH-44 enforced.
    CRITICAL: CE_ADDR = CHALLENGE_ESCROW_ARBITRUM_SEPOLIA as `0x${string}` imported from @call-it/shared — never inline hex.
    CRITICAL: ChallengeStatus.Proposed = 0, Accepted = 1 — use ordinals in type checks against duelLiveState.status.
  </action>
  <verify>
    <automated>cd "C:\Users\woshv\Desktop\Call it" && pnpm --filter @call-it/web build 2>&1 | tail -15</automated>
  </verify>
  <done>/duel/[challengeId]/page.tsx exists; `pnpm --filter @call-it/web build` exits 0; grep -r "display.*grid" apps/web/app/duel/ returns 0 results; grep confirms CHALLENGE_ESCROW_ARBITRUM_SEPOLIA imported (no inline 0x address); grep confirms MIN_STAKE and MAX_STAKE used in stake validation; wallet address never rendered (grep -r "0x{8,}" apps/web/app/duel/ returns 0 functional renders)</done>
</task>

<task type="auto">
  <name>Task 2: Live Receipt pending challenge block + Challenge button integration</name>
  <files>apps/web/app/call/[id]/page.tsx</files>
  <read_first>
    apps/web/app/call/[id]/page.tsx — full file; understand existing structure before modifying
    .planning/phases/03-challengeescrow/03-UI-SPEC.md Surface 7 (pending block on Live Receipt) + Surface 6 (Challenge button on CallCard)
    apps/web/app/components/NotificationBell.tsx — notification polling pattern (30s) — understand before adding challenge poll
    .planning/phases/03-challengeescrow/03-PATTERNS.md lines 800-965 — Live Receipt integration points
  </read_first>
  <action>
    Modify apps/web/app/call/[id]/page.tsx to add:

    1. Challenge button in the action row (already shown as "Challenge" orange outline per Phase 2 CallCard): if not already rendered — add "Challenge" button with #FB923C outline (Lucide Swords 16px icon) next to Follow/Fade buttons; clicking opens ChallengeFormModal (same modal component from Task 1 or a shared component if extracted); if openToChallenges==false from live-state: show inline error toast "This caller isn't open to challenges right now."

    2. Pending challenge notification block: when the relayer /api/notifications?user={address}&type=challenge_proposed returns a pending challenge for this callId: render an amber block with 2px #FB923C left border, #111118 bg: text "⚔ Challenge pending — {challengerHandle} challenged this call · {callerHandle} has {X}h to accept or reject". X = Math.max(0, Math.floor((proposedAt + 86400 - Date.now()/1000) / 3600)).

    3. Caller-only accept/reject section: when viewer == call.caller and there's a pending challenge: show "You have [X]h to accept or reject this challenge." block; Accept button (filled #4ADE80) — USDC allowance/balance preflight before accept: callerMatchingStake = min(pendingChallenge.challengerStake, pendingChallenge.challengerStake) (caller matches exactly or less). Read USDC.allowance(caller, CHALLENGE_ESCROW) via useReadContract; if allowance < callerMatchingStake, render "Approve USDC" sub-step button → useWriteContract approve(CHALLENGE_ESCROW_ARBITRUM_SEPOLIA, callerMatchingStake); after approval receipt: render enabled Accept button → useWriteContract acceptChallenge(challengeId); Reject button (2px #F87171 border) → inline confirmation "This will immediately refund {challengerHandle}'s stake. Are you sure? [Keep call open] [Yes, reject]" → on confirm: useWriteContract rejectChallenge on ChallengeEscrow.

    4. Extract or inline the ChallengeFormModal: if the challenge form from Task 1 is complex, extract to a shared component in apps/web/app/components/ChallengeFormModal.tsx and import in both duel/[challengeId]/page.tsx and call/[id]/page.tsx.

    Keep all existing functionality intact — this is an additive change. Do NOT break the existing Follow/Fade/CallerExit flows. Run `pnpm --filter @call-it/web build` to confirm no regressions.
  </action>
  <verify>
    <automated>cd "C:\Users\woshv\Desktop\Call it" && pnpm --filter @call-it/web build 2>&1 | tail -10</automated>
  </verify>
  <done>apps/web/app/call/[id]/page.tsx modified with challenge block; build exits 0; grep confirms "challenge_proposed" OR "rejectChallenge" in call/[id]/page.tsx; existing Follow/Fade flow tests still pass; no display:grid added</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → ChallengeEscrow proposeChallenge | wagmi useWriteContract; user-provided stake value; must validate before tx |
| Client → ChallengeEscrow rejectChallenge/acceptChallenge | Only callable by the caller address; contract enforces but UI should also gate |
| User stake input | Free text input; must be validated against MIN_STAKE/MAX_STAKE and cast to uint96 before tx |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-06-01 | Tampering | Stake input — UI bypass of bounds | mitigate | Zod schema validates stake [$5, $100] before enabling Send Challenge; matches Foundry gates exactly (D-29 parity); contract enforces as final gate |
| T-3-06-02 | Spoofing | Self-challenge from UI | mitigate | UI pre-check: if viewer.address == call.caller, hide/disable Challenge form (show "You can't challenge your own call"); contract SelfChallenge revert as final gate |
| T-3-06-03 | Tampering | display:grid in Duel page JSX | mitigate | ESLint no-display-grid rule (Phase 0/1 config); post-task grep guard: grep -r "display.*grid" apps/web/app/duel/ returns 0 |
| T-3-06-04 | Information Disclosure | Wallet address rendered | mitigate | AUTH-44 enforced: only handle displayed; useAccount().address never placed in JSX visible text; grep guard before PR merge |
| T-3-06-05 | Tampering | CE_ADDR inline hex bypass (wrong contract) | mitigate | CE_ADDR imported from @call-it/shared CHALLENGE_ESCROW_ARBITRUM_SEPOLIA; CI grep guard blocks inline 0x literals |
| T-3-06-06 | Tampering | USDC allowance not checked before propose or accept | mitigate | Inline USDC allowance preflight on BOTH propose path (challenge form modal) and accept path (caller accept button on Duel page + Live Receipt): read USDC.allowance(caller/challenger, CHALLENGE_ESCROW) via useReadContract; callerMatchingStake = min(challenge.challengerStake, callerInput); show "Approve USDC" sub-step if insufficient; disables the action CTA until approved; contract safeTransferFrom is the final gate |
</threat_model>

<verification>
- Build clean: `pnpm --filter @call-it/web build` exits 0
- No grid: `grep -r "display.*grid\|gridTemplate" apps/web/app/duel/ apps/web/app/call/` returns 0 matches
- Stake bounds present: `grep -n "MIN_STAKE\|MAX_STAKE\|5_000_000\|100_000_000" apps/web/app/duel/[challengeId]/page.tsx | wc -l` >= 2
- No wallet address rendered: `grep -r "account.address\|address.slice\|address.substring" apps/web/app/duel/ | wc -l` = 0 (addresses used only for contract args, not display)
- CALLER color: `grep "#E8F542" apps/web/app/duel/[challengeId]/page.tsx | wc -l` >= 5 (multiple usages)
- CHALLENGER color: `grep "#FB923C" apps/web/app/duel/[challengeId]/page.tsx | wc -l` >= 3
- CE address imported: `grep "CHALLENGE_ESCROW_ARBITRUM_SEPOLIA" apps/web/app/duel/[challengeId]/page.tsx | wc -l` >= 1
- Mobile banner: `grep "BEST VIEWED ON DESKTOP\|768" apps/web/app/duel/[challengeId]/page.tsx | wc -l` >= 1
- challenge_proposed: `grep "challenge_proposed\|rejectChallenge\|acceptChallenge" apps/web/app/call/[id]/page.tsx | wc -l` >= 1
</verification>

<success_criteria>
/duel/[challengeId] renders all §15.5 sections with live data from the relayer. The challenge form proposes a challenge via proposeChallenge on ChallengeEscrow. The Live Receipt page shows pending challenge notifications and caller accept/reject controls. `pnpm --filter @call-it/web build` exits 0. No display:grid anywhere in the duel surface. Wallet addresses are never displayed.
</success_criteria>

<output>
After completion, create `.planning/phases/03-challengeescrow/03-06-SUMMARY.md`
</output>
