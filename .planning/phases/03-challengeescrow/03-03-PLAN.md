---
phase: 03-challengeescrow
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - packages/contracts/script/DeployPhase3.s.sol
  - packages/shared/src/constants/addresses.ts
autonomous: false
requirements:
  - SOCIAL-36
  - SOCIAL-48

must_haves:
  truths:
    - "ChallengeEscrow is deployed to Arbitrum Sepolia at a real non-zero address"
    - "CHALLENGE_ESCROW_ARBITRUM_SEPOLIA in addresses.ts matches the deployed address"
    - "Post-deploy assertions pass: getTvl()==0, settlementManager()==address(0), tvlCap()==5000000000"
    - "Deploy block is recorded in addresses.ts comment for subgraph.yaml startBlock (Plan 04)"
  artifacts:
    - path: "packages/contracts/script/DeployPhase3.s.sol"
      provides: "Deploy script with post-deploy assertions and NEXT STEPS output"
      contains: "CHALLENGE_ESCROW_ARBITRUM_SEPOLIA"
    - path: "packages/shared/src/constants/addresses.ts"
      provides: "CHALLENGE_ESCROW_ARBITRUM_SEPOLIA constant populated with real deploy address"
      contains: "CHALLENGE_ESCROW_ARBITRUM_SEPOLIA"
  key_links:
    - from: "packages/shared/src/constants/addresses.ts"
      to: "deployed ChallengeEscrow on Arbitrum Sepolia"
      via: "CHALLENGE_ESCROW_ARBITRUM_SEPOLIA constant"
      pattern: "CHALLENGE_ESCROW_ARBITRUM_SEPOLIA"

user_setup:
  - service: arbitrum-sepolia-deploy
    why: "Contract deployment to Arbitrum Sepolia requires private key + RPC access"
    env_vars:
      - name: DEPLOYER_PRIVATE_KEY
        source: "Existing .env — same deployer used for Phase 2 (0xDa8c5726f596E8dae99e6dDEBa8AEa1c8bE9A4a5)"
      - name: TREASURY_ADDRESS
        source: "Same treasury as Phase 2 deploy"
      - name: ARBITRUM_SEPOLIA_RPC_URL
        source: "Existing .env — Alchemy Arbitrum Sepolia RPC"
    dashboard_config:
      - task: "Note the deploy block number from forge script output — needed for subgraph.yaml startBlock in Plan 04"
        location: "Console output of forge script --broadcast"
---

<objective>
Write DeployPhase3.s.sol, then execute the Arbitrum Sepolia deploy with an operator checkpoint, then update addresses.ts with the real contract address. This plan cannot run autonomously because broadcasting to Sepolia requires the operator's private key.

Purpose: ChallengeEscrow is the new money contract — it must be deployed before the subgraph and relayer can be configured to watch it. The deploy block number is the critical output that flows into Plan 04 (subgraph.yaml startBlock).

Output: DeployPhase3.s.sol (dry-run verified), deployed ChallengeEscrow on Arbitrum Sepolia, CHALLENGE_ESCROW_ARBITRUM_SEPOLIA in addresses.ts with real address.
</objective>

<execution_context>
@C:\Users\woshv\Desktop\Call it\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\woshv\Desktop\Call it\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\woshv\Desktop\Call it\.planning\STATE.md
@C:\Users\woshv\Desktop\Call it\.planning\phases\03-challengeescrow\03-CONTEXT.md
@C:\Users\woshv\Desktop\Call it\.planning\phases\03-challengeescrow\03-02-SUMMARY.md

<interfaces>
From packages/contracts/script/DeployPhase2.s.sol (analog — copy structure exactly):
- Script + console imports
- CALL_REGISTRY + FOLLOW_FADE_MARKET hardcoded constants
- vm.startBroadcast(deployerKey) / vm.stopBroadcast()
- Post-deploy require assertions
- console.log("REQUIRED NEXT STEPS:") output block

Phase 2 deployed addresses (from addresses.ts + STATE.md):
  CALL_REGISTRY    = 0x7DAd732764abfC935aD5bf8e5CFF9BEA7B2C234D
  FOLLOW_FADE_MARKET = 0x12aafa5a70c3aD8Bd3a52252744f9F7Aa073E362
  USDC_ARB_NATIVE = 0xaf88d065e77c8cC2239327C5EDb3A432268e5831
  INITIAL_TVL_CAP = 5_000_000_000 ($5,000 USDC)

ChallengeEscrow constructor signature (from ChallengeEscrow.sol Plan 02):
  constructor(address _callRegistry, address _followFadeMarket, address _usdc, address _treasury, uint256 _tvlCap)

addresses.ts addition pattern (from PATTERNS.md lines 390-419):
  export const CHALLENGE_ESCROW_ARBITRUM_SEPOLIA = '0x...' as const
  export const CHALLENGE_ESCROW_ARBITRUM_ONE = '0x0000...' as const
  export const CHALLENGE_ESCROW_ADDRESSES: AddressRecord = { ... }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DeployPhase3.s.sol — deploy script with post-deploy assertions</name>
  <files>packages/contracts/script/DeployPhase3.s.sol</files>
  <read_first>
    packages/contracts/script/DeployPhase2.s.sol — exact analog (copy structure line by line)
    packages/contracts/src/ChallengeEscrow.sol — constructor args needed
    packages/contracts/src/interfaces/IChallengeEscrow.sol — function signatures for assertions
    .planning/phases/03-challengeescrow/03-PATTERNS.md lines 326-384 — DeployPhase3 pattern
  </read_first>
  <action>
    Create packages/contracts/script/DeployPhase3.s.sol. Copy DEPLOYMENT SAFETY CHECKLIST NatSpec header from DeployPhase2.s.sol updating for Phase 3.

    pragma =0.8.30 exact. Imports: Script, console from forge-std; ChallengeEscrow; ICallRegistry; IFollowFadeMarket.

    Constants: CALL_REGISTRY = 0x7DAd732764abfC935aD5bf8e5CFF9BEA7B2C234D; FOLLOW_FADE_MARKET = 0x12aafa5a70c3aD8Bd3a52252744f9F7Aa073E362; USDC_ARB_NATIVE = 0xaf88d065e77c8cC2239327C5EDb3A432268e5831 (hardcode per CLAUDE.md — ChallengeEscrow constructor asserts it anyway); INITIAL_TVL_CAP = 5_000_000_000.

    run(): reads DEPLOYER_PRIVATE_KEY + TREASURY_ADDRESS from env. startBroadcast. Deploys ChallengeEscrow(CALL_REGISTRY, FOLLOW_FADE_MARKET, USDC_ARB_NATIVE, treasuryAddress, INITIAL_TVL_CAP). stopBroadcast.

    Post-deploy assertions (outside broadcast): require(ce.getTvl() == 0); require(ce.settlementManager() == address(0)); require(ce.tvlCap() == INITIAL_TVL_CAP); require(address(ce.callRegistry()) == CALL_REGISTRY); require(address(ce.followFadeMarket()) == FOLLOW_FADE_MARKET).

    console.log output block:
      "ChallengeEscrow deployed at: {address}"
      "Deploy block: {block.number} — use for subgraph.yaml startBlock"
      "REQUIRED NEXT STEPS:"
      "1. Update packages/shared/src/constants/addresses.ts: CHALLENGE_ESCROW_ARBITRUM_SEPOLIA = {address}"
      "2. Update packages/subgraph/subgraph.yaml: ChallengeEscrow address + startBlock = {block}"
      "3. Remove blockHandlers entry from subgraph.yaml ChallengeEscrow section"
      "4. cd packages/subgraph && pnpm run build && pnpm run deploy:sepolia"

    Dry-run test: `forge script packages/contracts/script/DeployPhase3.s.sol --sig "run()" --fork-url $ARBITRUM_SEPOLIA_RPC_URL` (no --broadcast). Exits 0 and prints NEXT STEPS.
  </action>
  <verify>
    <automated>cd "C:\Users\woshv\Desktop\Call it" && forge build --match-path "packages/contracts/script/DeployPhase3.s.sol" 2>&1 | grep -c "Compiler run successful"</automated>
  </verify>
  <done>DeployPhase3.s.sol compiles; forge build exits 0; dry-run (--fork-url, no --broadcast) exits 0 and prints the REQUIRED NEXT STEPS block; no inline USDC.e address in the script (CI grep guard)</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [OPERATOR] Deploy ChallengeEscrow to Arbitrum Sepolia</name>
  <what-built>DeployPhase3.s.sol is ready and dry-run verified</what-built>
  <how-to-verify>
    Run the deploy:

    ```
    cd packages/contracts
    forge script script/DeployPhase3.s.sol \
      --rpc-url $ARBITRUM_SEPOLIA_RPC_URL \
      --broadcast \
      --verify \
      --sig "run()" \
      -vvvv
    ```

    Expected output:
    - "ChallengeEscrow deployed at: 0x..." — record this address
    - "Deploy block: NNNNNNNN" — record this block number
    - All 5 post-deploy require assertions pass (getTvl==0, settlementManager==0x0, tvlCap==5000000000, callRegistry==CR, followFadeMarket==FFM)
    - --verify may fail if no Arbiscan key (acceptable — can run forge verify-contract later)

    Then update addresses.ts (Task 3 below handles this — provide address + block to executor).
  </how-to-verify>
  <resume-signal>Provide the deployed address and deploy block number: "address=0x... block=NNNNNNNN"</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update addresses.ts with deployed ChallengeEscrow address</name>
  <files>packages/shared/src/constants/addresses.ts</files>
  <read_first>
    packages/shared/src/constants/addresses.ts — current file; find the CHALLENGE_ESCROW section or add after FOLLOW_FADE_MARKET section
    .planning/phases/03-challengeescrow/03-PATTERNS.md lines 389-419 — exact addition pattern
  </read_first>
  <action>
    Add to packages/shared/src/constants/addresses.ts after the FOLLOW_FADE_MARKET_ADDRESSES export:

    CHALLENGE_ESCROW_ARBITRUM_SEPOLIA constant with the actual deployed address from Task 2 checkpoint.
    CHALLENGE_ESCROW_ARBITRUM_ONE = placeholder '0x0000000000000000000000000000000000000000' (mainnet ships Phase 7.5).
    CHALLENGE_ESCROW_ADDRESSES record mapping chain IDs to both.
    JSDoc comment: deployed date, deploy block number (from Task 2 output), constructor args, and the 5 post-deploy verification lines (all green).

    Do NOT add a new AddressRecord type — reuse the existing one from the file. Match the FOLLOW_FADE_MARKET entry style exactly.

    After updating: `pnpm --filter @call-it/shared build` exits 0.
    Commit: "feat(03-03): deploy ChallengeEscrow Sepolia 0x...; update addresses.ts".
  </action>
  <verify>
    <automated>cd "C:\Users\woshv\Desktop\Call it" && grep "CHALLENGE_ESCROW_ARBITRUM_SEPOLIA" packages/shared/src/constants/addresses.ts | head -3</automated>
  </verify>
  <done>CHALLENGE_ESCROW_ARBITRUM_SEPOLIA in addresses.ts is a non-zero address; `pnpm --filter @call-it/shared build` exits 0; grep shows the constant; deploy block is in the JSDoc comment</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Deployer private key | DEPLOYER_PRIVATE_KEY in env — must not be logged or committed |
| Deployed contract address | Must match what's in addresses.ts — any mismatch breaks all downstream plans |
| USDC address in deploy script | Script hardcodes USDC_ARB_NATIVE; constructor asserts match |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-03-01 | Tampering | USDC address in deploy script | mitigate | USDC_ARB_NATIVE hardcoded in script + constructor assert `require(_usdc == USDC_ARB_NATIVE)`; CI grep guard prevents USDC.e |
| T-3-03-02 | Elevation of Privilege | Wrong constructor args (wrong CR/FFM address) | mitigate | Post-deploy assertions verify address(ce.callRegistry()) == CALL_REGISTRY and address(ce.followFadeMarket()) == FOLLOW_FADE_MARKET; forge reverts broadcast if assertions fail |
| T-3-03-03 | Information Disclosure | DEPLOYER_PRIVATE_KEY in logs | mitigate | Key read via vm.envUint (Foundry env), never logged; .env not committed (Phase 0 CI guard) |
| T-3-03-04 | Tampering | Wrong address in addresses.ts | mitigate | Operator copies address directly from forge script output; JSDoc comment includes block number cross-reference; `pnpm build` type-checks the constant is a valid `0x${string}` |
</threat_model>

<verification>
- Script compiles: `forge build --match-path "*/script/DeployPhase3.s.sol"` exits 0
- Address populated: `grep -v "0x0000000000000000000000000000000000000000" packages/shared/src/constants/addresses.ts | grep CHALLENGE_ESCROW_ARBITRUM_SEPOLIA` returns the real address
- Shared package builds: `pnpm --filter @call-it/shared build` exits 0
- Post-deploy on-chain verification: `cast call $CHALLENGE_ESCROW_ADDR "getTvl()(uint256)" --rpc-url $ARBITRUM_SEPOLIA_RPC_URL` returns 0
- `cast call $CHALLENGE_ESCROW_ADDR "settlementManager()(address)" --rpc-url $ARBITRUM_SEPOLIA_RPC_URL` returns 0x0000...
</verification>

<success_criteria>
ChallengeEscrow is live on Arbitrum Sepolia. The real address is in addresses.ts. The deploy block number is recorded for Plan 04's subgraph.yaml startBlock. The shared package builds with the new constant.
</success_criteria>

<output>
After completion, create `.planning/phases/03-challengeescrow/03-03-SUMMARY.md`
</output>
