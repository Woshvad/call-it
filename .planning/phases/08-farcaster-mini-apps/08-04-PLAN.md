---
phase: 08-farcaster-mini-apps
plan: 04
type: execute
wave: 2
depends_on: ["08-02"]
files_modified:
  - apps/web/app/call/[id]/page.tsx
  - apps/relayer/src/workers/__tests__/auto-post-worker.test.ts
  - packages/shared/src/share/share-text.ts
  - apps/web/tests/share-text.test.ts
autonomous: false
requirements: [SHARE-19]
user_setup: []

must_haves:
  truths:
    - "The Phase-7 auto-post-on-settle path lands the cast carrying the Mini App embed automatically — the embed rides the receipt URL already passed to warpcastComposeUrl; no cast-body/embeds payload change is needed (D-04, SHARE-19 SC3)"
    - "The receipt page gains an explicit 'SHARE AS FRAME →' outline control in the existing action row that opens warpcastComposeUrl(receiptUrl, buildShareText(...)) in a new tab; it is OMITTED (no dead button) if the receipt URL/handle cannot be constructed (D-04, UI-SPEC)"
    - "Farcaster receipt rendering matches the Twitter OG card variant (Settled/DuelSettled/CallerExited) for visual continuity because the embed imageUrl reuses the unchanged Phase-7 OG card (D-02 criterion 3)"
    - "The Warpcast compose-intent host in warpcastComposeUrl is verified against the current live spec at execute time; if migrated (warpcast.com -> farcaster.xyz), it is a one-line change to the pure builder + its test, preserving the purity contract (Open Q3)"
  artifacts:
    - path: "apps/web/app/call/[id]/page.tsx"
      provides: "SHARE AS FRAME -> outline control in the receipt action row, reusing warpcastComposeUrl + buildShareText"
      contains: "SHARE AS FRAME"
    - path: "apps/relayer/src/workers/__tests__/auto-post-worker.test.ts"
      provides: "assertion that the auto-post warpcastUrl carries the receipt URL (embed rides automatically)"
    - path: "packages/shared/src/share/share-text.ts"
      provides: "warpcastComposeUrl with the compose host verified/updated against the live spec (Open Q3)"
  key_links:
    - from: "apps/web/app/call/[id]/page.tsx (SHARE AS FRAME control)"
      to: "warpcastComposeUrl + buildShareText (@call-it/shared)"
      via: "reused pure builders, opened in a new tab"
      pattern: "warpcastComposeUrl"
    - from: "apps/relayer/src/workers/auto-post-worker.ts"
      to: "the receipt URL's fc:miniapp/fc:frame embed (Plan 02)"
      via: "embed rides receiptUrl in warpcastComposeUrl — no payload change"
      pattern: "warpcastComposeUrl\\(receiptUrl"
---

<objective>
Vertical slice C: close the distribution loop. After this slice, (1) the existing Phase-7 auto-post-on-settle worker lands a Farcaster cast whose Mini App embed renders automatically (the embed is a property of the receipt URL's HTML from Plan 02 — no worker payload change), and (2) the receipt page has a manual "SHARE AS FRAME →" control so a caller can post the actionable receipt on demand. This is the independently-demonstrable "auto-post + manual share both carry the Frame embed" deliverable (SC3), and it confirms the Open-Q3 compose-host against the live spec.

Purpose: D-04 wires the Frame embed into both the auto-post path and a manual affordance; SC3's "auto-post lands the cast and the Mini App is discoverable" is satisfied by reuse — the embed travels on the receipt URL Plan 02 already decorated. The only genuinely new app-owned visual surface this whole phase is the one outline control (per UI-SPEC). This slice depends on Plan 02 (the embed meta must exist on the receipt page for the embed to ride along).
Output: 1 receipt-page control, 1 auto-post test extension, a verified/possibly-1-line-updated compose builder + its test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-farcaster-mini-apps/08-CONTEXT.md
@.planning/phases/08-farcaster-mini-apps/08-RESEARCH.md
@.planning/phases/08-farcaster-mini-apps/08-PATTERNS.md
@.planning/phases/08-farcaster-mini-apps/08-UI-SPEC.md
@packages/shared/src/share/share-text.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify the auto-post embed rides automatically + extend the worker test; verify the compose host (Open Q3)</name>
  <files>apps/relayer/src/workers/__tests__/auto-post-worker.test.ts, packages/shared/src/share/share-text.ts, apps/web/tests/share-text.test.ts</files>
  <read_first>
    - apps/relayer/src/workers/auto-post-worker.ts (lines 248-258) — `makeProcessCall` builds `const receiptUrl = `${base}/call/${callId}``, `text = buildShareText(...)`, `warpcastUrl = warpcastComposeUrl(receiptUrl, text)`. PATTERNS: verify-only — the embed rides `receiptUrl` once the receipt HTML carries the Plan-02 meta; NO payload/embeds change. The cache-warm (`runCacheWarm`, lines 155-232) already bumps status_version + re-warms the OG card before posting (mitigates Pitfall 4).
    - apps/relayer/src/workers/__tests__/auto-post-worker.test.ts — the existing test that already asserts `warpcastUrl`; extend it (do not rewrite).
    - packages/shared/src/share/share-text.ts (lines 13-35) — the PURITY CONTRACT (no env, no network, no secrets) + the `[ASSUMED]` Warpcast compose-host note (lines 18-21) + `warpcastComposeUrl` (lines 33-35). 08-RESEARCH.md Open Q3 + State of the Art (warpcast.com -> farcaster.xyz migration underway) — verify the live host at execute time.
    - apps/web/tests/share-text.test.ts — the existing builder test to update if the host changes.
  </read_first>
  <action>
    Verify-only on the worker: confirm `auto-post-worker.ts` already builds `warpcastComposeUrl(receiptUrl, text)` with `receiptUrl = ${base}/call/${callId}` and makes NO structural change necessary for the embed to ride along (RESEARCH Pattern 4). Do NOT add an embeds-array payload — the embed is a property of the receipt URL's HTML (Plan 02). Extend `auto-post-worker.test.ts` with one assertion: the produced `warpcastUrl` contains the encoded receipt URL `${base}/call/${callId}` (so the cast carries the embed-bearing URL). Keep the existing assertions intact.

    Open Q3 — compose host: at execute time, verify the current canonical Warpcast compose-intent host/path against the live Farcaster docs (`warpcast.com/~/compose` vs a migrated `farcaster.xyz/~/compose`). If unchanged, make NO edit and record "verified current" in the SUMMARY. If migrated, make the ONE-LINE change to `warpcastComposeUrl` in `packages/shared/src/share/share-text.ts` (host only — preserve the `?text=…&embeds[]=…` shape and the function signature) and update the corresponding expectation in `apps/web/tests/share-text.test.ts`. Preserve the purity contract (no env/network/secret) and do NOT touch `buildShareText`/`twitterIntentUrl`. After any change, `pnpm --filter @call-it/shared build` so the web re-export + relayer import pick up the rebuilt dist (per the Phase-7 gotcha: shared dist is gitignored and must be rebuilt before web/relayer consume it).
  </action>
  <verify>
    <automated>cd "C:/Users/woshv/Desktop/Call it" && pnpm --filter @call-it/shared build 2>&1 | tail -3 && pnpm --filter @call-it/web test 2>&1 | tail -10 && pnpm --filter @call-it/relayer test 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `auto-post-worker.test.ts` asserts the produced `warpcastUrl` contains the URL-encoded receipt URL `${base}/call/${callId}`; all pre-existing assertions still pass.
    - `warpcastComposeUrl` either is unchanged (host verified current — note in SUMMARY) OR has a single host-string change with `share-text.test.ts` updated to match; in both cases the function signature is unchanged and the builder remains pure (no env/network/secret import).
    - `pnpm --filter @call-it/shared build` exits 0; `apps/web` + `apps/relayer` test suites are green.
  </acceptance_criteria>
  <done>The auto-post path provably carries the embed-bearing receipt URL, and the compose host is confirmed against the live spec.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: SHARE AS FRAME control on the receipt action row (visual verify)</name>
  <files>apps/web/app/call/[id]/page.tsx</files>
  <read_first>
    - apps/web/app/call/[id]/page.tsx (action row, lines 1548-1568) — the existing `SHARE THE RECEIPT →` primary (accent-filled) + `VIEW ALL CALLS BY {handle}` outline controls; the row's `gap:12px`, `padding:16px`, `marginBottom:32px`, and the mono/uppercase/`letter-spacing:0.08em`, `13px`/`700` label style (UI-SPEC Typography + Spacing). ANALOG = the sibling outline control (`page.tsx:1559-1566`).
    - 08-UI-SPEC.md "In-app control (receipt page)" + "Color" (the new control uses the SECONDARY/outline treatment: `border:2px solid #E8F542`, transparent fill, accent text — sits alongside, does not compete with, the accent-filled primary share button) + "Error state" (OMIT the control, no dead button, if the receipt URL/handle can't be constructed) + Copywriting (`SHARE AS FRAME →`, uppercase mono).
    - packages/shared/src/share/share-text.ts — `warpcastComposeUrl` + `buildShareText` (reuse; do not re-implement — UI-SPEC Inherited & Locked).
  </read_first>
  <action>
    What Claude automates first: add the `SHARE AS FRAME →` control to the receipt action row in `apps/web/app/call/[id]/page.tsx`, to the right of the existing `SHARE THE RECEIPT →` button. Reuse the exact action-row spacing/typography (`gap:12px`, `padding:16px`, `13px`/`700` mono uppercase, `letter-spacing:0.08em`) and the SECONDARY/outline treatment from UI-SPEC (`border:2px solid #E8F542`, transparent fill, `#E8F542` text — matching the `VIEW ALL CALLS BY {handle}` outline sibling). On click, open `warpcastComposeUrl(receiptUrl, buildShareText({ outcomeWord, handle, statement }))` in a new tab (`target="_blank"`, `rel="noopener noreferrer"`), where `receiptUrl = ${NEXT_PUBLIC_OG_BASE_URL}/call/${id}`. If the receipt URL or handle cannot be constructed, OMIT the control entirely (render nothing — no dead button), per UI-SPEC Error state. Introduce NO new colors/spacing/type sizes (this phase adds no design tokens). Reuse the shared builders — do not re-implement share text or the cast URL.
  </action>
  <what-built>A new `SHARE AS FRAME →` outline control in the receipt-page action row that opens the Warpcast compose intent (embed-bearing receipt URL) in a new tab; omitted when the URL/handle is missing.</what-built>
  <how-to-verify>
    1. Run the web app against the deployed Sepolia relayer (or a seeded local call): `pnpm --filter @call-it/web dev`, then open a settled receipt at `/call/{seededSettledId}` (use a known seeded settled callId from STATE/the soak cluster).
    2. Confirm the action row shows `SHARE THE RECEIPT →` (accent fill) and, to its right, `SHARE AS FRAME →` (outline: 2px `#E8F542` border, transparent fill, accent text) — same height/gap/typography as the sibling outline button; no layout shift, no new color.
    3. Click `SHARE AS FRAME →` — a new tab opens to the Warpcast compose intent URL with the receipt URL in `embeds[]=` and the `buildShareText` outcome word + handle in `text=`.
    4. (If you have a Farcaster account) paste/preview the composed cast and confirm the OG card + launch button render from the embed (this is the SC3 visual-continuity check; the live tap-to-transact remains the Phase-10 gate).
    5. Confirm that on a receipt where the handle/URL is unavailable, the control is absent (not a dead/disabled button).
  </how-to-verify>
  <verify>
    <automated>cd "apps/web" && node -e "const fs=require('fs');const f=fs.readFileSync('app/call/[id]/page.tsx','utf8');if(!/SHARE AS FRAME/.test(f))throw new Error('control missing');if(!/warpcastComposeUrl/.test(f))throw new Error('not reusing warpcastComposeUrl');console.log('OK')" && pnpm --filter @call-it/web build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/app/call/[id]/page.tsx` contains a `SHARE AS FRAME →` control that calls `warpcastComposeUrl` with `buildShareText(...)` and opens in a new tab (`rel="noopener noreferrer"`).
    - The control uses the outline treatment + the existing action-row spacing/typography (no new tokens); the border/accent style matches the sibling outline button pattern.
    - The control is omitted (renders nothing) when receiptUrl/handle is unavailable (no dead button).
    - `pnpm --filter @call-it/web build` exits 0.
    - Human visual verify per how-to-verify confirms placement, treatment, and the compose-intent behavior.
  </acceptance_criteria>
  <done>The SHARE AS FRAME outline control renders in the receipt action row with the correct outline treatment, opens the embed-bearing Warpcast compose intent, and is omitted when the URL/handle is missing.</done>
  <resume-signal>Type "approved" or describe the visual/behavioral issue to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| receipt page SHARE AS FRAME control -> Warpcast compose intent (new tab) | Opens an external compose URL built from the receipt URL + share text; new tab uses noopener/noreferrer |
| auto-post worker -> Farcaster cast | Reuses the existing Phase-7 cast-URL construction; embed rides the receipt URL (no new outbound payload) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | Tampering / reverse-tabnabbing | new-tab compose link | mitigate | The `SHARE AS FRAME →` link opens with `rel="noopener noreferrer"` (no `window.opener` handle). |
| T-08-04-02 | Injection | raw handle/statement in the cast text | mitigate | `buildShareText` + `warpcastComposeUrl` already URL-encode all args (purity contract); the control reuses them unchanged — no raw concatenation. |
| T-08-04-03 | Spoofing | compose URL points at a wrong host after migration | mitigate | Open Q3 verifies the live compose host at execute time; any change is a single reviewed host-string edit in the pure builder + its test. |
| T-08-04-04 | Information Disclosure | share text leaks non-public data | accept | The receipt is public-by-default (SHARE-21); the cast text is the same outcome word + handle + statement already on the public receipt. No new disclosure. |
| T-08-04-SC | Tampering | npm installs | mitigate | Zero new npm deps — reuses `@call-it/shared` builders; no `@farcaster/*` ([ASSUMED]) package introduced (the SPA path is Phase 10). |
</threat_model>

<verification>
- `pnpm --filter @call-it/shared build` then `pnpm --filter @call-it/web test` + `pnpm --filter @call-it/relayer test` all green.
- `pnpm --filter @call-it/web build` exits 0 with the new control.
- Human visual verify (Task 2 checkpoint) confirms placement/treatment + compose-intent behavior; the live in-Warpcast tap-to-transact is NOT a gate here (Phase 10, D-01).
</verification>

<success_criteria>
- Auto-post path provably carries the embed-bearing receipt URL (worker test extended); embed rides automatically — no payload change (D-04 / SC3).
- Manual `SHARE AS FRAME →` outline control added, reusing the shared builders, omitted on missing URL/handle (D-04 / UI-SPEC).
- Compose host verified against the live spec (Open Q3).
- Visual continuity (criterion 3) holds via the unchanged OG card embed.
</success_criteria>

<output>
Create `.planning/phases/08-farcaster-mini-apps/08-04-SUMMARY.md` when done.
</output>
