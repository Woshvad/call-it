---
phase: 08-farcaster-mini-apps
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/web/public/icon.png
  - apps/web/public/splash.png
  - apps/web/middleware.ts
  - apps/web/tests/farcaster-embed.test.ts
  - apps/web/tests/farcaster-manifest.test.ts
  - apps/web/tests/frame-tx.test.ts
  - apps/web/tests/middleware-public.test.ts
  - apps/web/lib/farcaster-fixtures.ts
autonomous: true
requirements: [SHARE-19]
user_setup: []

must_haves:
  truths:
    - "A logged-out Farcaster crawler can reach /.well-known/farcaster.json without being bounced to /signin (D-05, middleware public carve-out)"
    - "The manifest/embed static image URLs (icon.png 1024x1024, splash.png 200x200) resolve from apps/web/public, so the embed and manifest do not 404 (D-03/D-05)"
    - "RED test scaffolds exist for every SC1/SC2 testnet-validatable behavior so the later slices have a GREEN target (D-01 — testable-now scope, live tap deferred to Phase 10)"
  artifacts:
    - path: "apps/web/public/icon.png"
      provides: "1024x1024 PNG no-alpha manifest iconUrl asset"
    - path: "apps/web/public/splash.png"
      provides: "200x200 PNG manifest/embed splashImageUrl asset"
    - path: "apps/web/middleware.ts"
      provides: "PUBLIC_PREFIXES includes '/.well-known' (Pitfall 2)"
      contains: "/.well-known"
    - path: "apps/web/tests/frame-tx.test.ts"
      provides: "RED scaffold for the Frame tx wire format + status-aware buttons (SC2)"
    - path: "apps/web/tests/farcaster-embed.test.ts"
      provides: "RED scaffold for fc:miniapp + fc:frame embed meta shape (SC1a/SC3)"
    - path: "apps/web/tests/farcaster-manifest.test.ts"
      provides: "RED scaffold for /.well-known/farcaster.json schema (SC1b)"
    - path: "apps/web/lib/farcaster-fixtures.ts"
      provides: "shared seeded-callId fixtures (live/settled/caller-exited/duel statuses) for the Wave-1/2 slices"
  key_links:
    - from: "apps/web/middleware.ts"
      to: "isPublicRoute('/.well-known/farcaster.json')"
      via: "PUBLIC_PREFIXES startsWith match"
      pattern: "/\\.well-known"
---

<objective>
Wave 0 for Phase 8 (Farcaster Mini Apps). Lay the unblocking foundation for the two vertical slices that follow: (1) the two static image assets the manifest + embed reference; (2) the middleware public carve-out so the manifest is reachable by a logged-out Farcaster crawler; (3) the RED test scaffolds + shared fixtures that the Wave-1 embed/manifest slice and the Frame-tx slice turn GREEN.

Purpose: Phase 8 is code-complete-on-testnet (D-01); every SC1/SC2 behavior except the live in-Warpcast broadcast is unit-validatable. This plan installs the test surface so the later slices have a GREEN target, and removes the two RESEARCH gotchas (missing static assets, `/.well-known` not public) before any embed/manifest/tx code is written.
Output: 2 static assets, 1 middleware edit, 4 RED test files, 1 shared fixtures module.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-farcaster-mini-apps/08-CONTEXT.md
@.planning/phases/08-farcaster-mini-apps/08-RESEARCH.md
@.planning/phases/08-farcaster-mini-apps/08-PATTERNS.md
@.planning/phases/08-farcaster-mini-apps/08-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Static assets + middleware public carve-out</name>
  <files>apps/web/public/icon.png, apps/web/public/splash.png, apps/web/middleware.ts</files>
  <read_first>
    - apps/web/middleware.ts (lines 50-74) — the existing `PUBLIC_PREFIXES` array + `isPublicRoute` startsWith model (PATTERNS analog: middleware.ts self-extension, line-exact)
    - 08-RESEARCH.md Pitfall 2 (the matcher runs on `/.well-known/farcaster.json`; without the prefix a logged-out crawler 302s to /signin) and Runtime State Inventory "Build artifacts" row (icon.png 1024x1024 PNG no-alpha; splash.png 200x200)
    - 08-PATTERNS.md "No Analog Found" table (the two static assets have no code analog — Wave-0 task) and the manifest body excerpt showing `iconUrl: ${base}/icon.png` + `splashImageUrl: ${base}/splash.png`
  </read_first>
  <action>
    Generate `apps/web/public/icon.png` as a 1024x1024 PNG with NO alpha channel (manifest spec requires opaque), using the brand-bg `#09090E` background and a simple "Call It" wordmark/glyph (solid fill, no transparency). Generate `apps/web/public/splash.png` as a 200x200 PNG (alpha allowed) on the same `#09090E` background. Use ImageMagick (`magick`/`convert`) or a tiny node sharp/canvas one-off script run from `apps/web` to emit deterministic PNGs at exactly those pixel dimensions; do not commit a transparent icon (verify with a dimension/format check). These are the assets `/.well-known/farcaster.json` and the embed meta reference per D-03/D-05.

    In `apps/web/middleware.ts`, add the single string `'/.well-known'` to the `PUBLIC_PREFIXES` array (Pitfall 2). Do NOT add `'/api/frame'` — `/api/frame/*` is already covered by the existing `'/api'` prefix; confirm by reading the array, do not duplicate. Add a short comment noting the manifest must be reachable by an unauthenticated Farcaster crawler (mirrors the existing `/call`/`/duel` carve-out rationale).
  </action>
  <verify>
    <automated>cd "apps/web" && node -e "const fs=require('fs');for(const f of ['public/icon.png','public/splash.png']){if(!fs.existsSync(f))throw new Error('missing '+f)};const m=fs.readFileSync('middleware.ts','utf8');if(!m.includes('/.well-known'))throw new Error('middleware missing /.well-known');console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/public/icon.png` exists, is a valid PNG, dimensions exactly 1024x1024, NO alpha channel (opaque).
    - `apps/web/public/splash.png` exists, is a valid PNG, dimensions exactly 200x200.
    - `apps/web/middleware.ts` `PUBLIC_PREFIXES` array contains `'/.well-known'`; `/api/frame` is NOT separately added (covered by `'/api'`).
    - `grep -F "/.well-known" apps/web/middleware.ts` returns at least one match.
  </acceptance_criteria>
  <done>Both static assets resolve from public/ at the required dimensions; the manifest path is in the middleware public allowlist.</done>
</task>

<task type="auto">
  <name>Task 2: RED test scaffolds + shared status fixtures (Wave 0)</name>
  <files>apps/web/tests/farcaster-embed.test.ts, apps/web/tests/farcaster-manifest.test.ts, apps/web/tests/frame-tx.test.ts, apps/web/tests/middleware-public.test.ts, apps/web/lib/farcaster-fixtures.ts</files>
  <read_first>
    - apps/web/vitest.config.ts (lines 11-16) — `include: ['tests/**/*.test.ts']`, `globals: false` (import `describe`/`it`/`expect` from 'vitest'), `environment: 'node'` (NO jsdom/testing-library). Tests MUST live under `apps/web/tests/` and be `.test.ts` (matches the Phase 1.5 / Phase 7 convention in STATE.md).
    - apps/web/tests/share-text.test.ts + apps/web/tests/og-real-data-wiring.test.ts — existing test style (pure node assertions, explicit vitest imports) to mirror.
    - 08-RESEARCH.md Validation Architecture "Phase Requirements -> Test Map" rows (SC1a/SC1b/SC1c/SC2) for the exact behaviors each test must assert, and "Wave 0 Gaps" checklist.
    - 08-VALIDATION.md Per-Task Verification Map (the rows these scaffolds back) and the Nyquist note (SC2 live broadcast is Manual-Only/Phase-10, NOT a red test).
  </read_first>
  <action>
    Create `apps/web/lib/farcaster-fixtures.ts` exporting a small typed map of status fixtures used by every slice: for each status label the relayer `/api/calls/:id/live-state` can return (`'Live'`, `'Settled'`, `'Disputed'`, `'CallerExited'`), export the expected button set per D-02/D-06 — live -> `['Follow','Fade','Challenge']`, settled/disputed/caller-exited -> `['Follow','Challenge','Quote']` — plus a couple of representative seeded Sepolia callId strings (e.g. a live callId and a settled callId; use known seeded ids from STATE if available, else documented placeholders). Keep it pure (no env, no fetch) so both the web tests and the route code can import the button-selection table from one source.

    Create the four RED scaffolds under `apps/web/tests/`. They reference modules/routes that do not exist yet (Wave-1/2 builds them); each test MUST currently FAIL or be a `it.todo`/`describe.skip` that flips to a real assertion when the target lands — choose a form that runs under vitest without a hard import error (prefer importing lazily inside the test body or asserting a not-yet-true expectation, so the file is collected, not a collection-time crash). Each scaffold:
    - `farcaster-embed.test.ts` (SC1a/SC3): asserts that `generateMetadata` for `call/[id]/layout.tsx` returns an `other` object with both `'fc:miniapp'` and `'fc:frame'` keys; that each value `JSON.parse`s to `{version:'1', imageUrl, button:{title, action:{type, url, ...}}}`; that `fc:miniapp.action.type === 'launch_miniapp'` and `fc:frame.action.type === 'launch_frame'`; and that `imageUrl` equals `/og/{id}?v={statusVersion}` (same statusVersion as the existing og:image — Pitfall 4).
    - `farcaster-manifest.test.ts` (SC1b): asserts `GET` of the manifest route returns 200 + JSON whose `miniapp` object has `version === '1'`, non-empty `name`, `homeUrl`, `iconUrl`, and that the top-level response has NO `accountAssociation` key (D-05).
    - `frame-tx.test.ts` (SC2): asserts the Frame tx route, given a known live callId, returns `{chainId:'eip155:421614', method:'eth_sendTransaction', params:{abi,to,data}}`; that `to` equals `FOLLOW_FADE_MARKET_ARBITRUM_SEPOLIA` from `@call-it/shared`; that `viem.decodeFunctionData` on `params.data` yields `functionName:'follow'` with args `[callId, 1_000_000n, 0n]` (min $1, D-07); and that the selected button set matches the fixtures table for the given status (D-02/D-06).
    - `middleware-public.test.ts` (SC1c): asserts that the middleware public-route predicate treats `'/.well-known/farcaster.json'` and `'/api/frame/tx/1'` as public. If `isPublicRoute` is not exported, this test may re-declare the prefix list or import after a small refactor — keep the assertion behavioral (the manifest path is public).
    Do NOT add jsdom or testing-library. Do NOT use `display: grid` anywhere (N/A here but house rule).
  </action>
  <verify>
    <automated>cd "apps/web" && pnpm vitest run tests/farcaster-embed.test.ts tests/farcaster-manifest.test.ts tests/frame-tx.test.ts tests/middleware-public.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All four files exist under `apps/web/tests/` and are collected by vitest (no collection-time import crash).
    - `apps/web/lib/farcaster-fixtures.ts` exists and exports the per-status button-set table; importing it has no side effects (pure).
    - Running the four tests shows them RED or `todo`/`skip` (the targets are not built yet) — NOT green-by-accident. Document in the SUMMARY which form each uses.
    - No test imports jsdom/@testing-library; `grep -rL "vitest" apps/web/tests/farcaster-*.test.ts apps/web/tests/frame-tx.test.ts apps/web/tests/middleware-public.test.ts` returns nothing (every file imports from vitest, since globals:false).
  </acceptance_criteria>
  <done>The Wave-0 RED scaffolds + fixtures exist and run; Wave 1/2 slices have a concrete GREEN target for SC1a/SC1b/SC1c/SC2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| logged-out Farcaster crawler -> /.well-known/farcaster.json | Unauthenticated public read crosses the middleware boundary; must NOT be bounced to /signin |
| public static assets (icon/splash) -> manifest/embed URLs | Display-only PNGs referenced by URL; no user input, no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Denial of Service | `/.well-known/farcaster.json` reachability | mitigate | Add `'/.well-known'` to `PUBLIC_PREFIXES` so the crawler is not redirected (Pitfall 2); Task 1 + middleware-public.test.ts assert it. |
| T-08-01-02 | Information Disclosure | static assets in public/ | accept | icon.png/splash.png are display-only brand images; no PII, no secrets — public by design. |
| T-08-01-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new npm dependencies in this plan (and the whole testable Phase-8 scope per RESEARCH). No install task exists; `@farcaster/*` packages ([ASSUMED]) are NOT introduced here. |
</threat_model>

<verification>
- `pnpm --filter @call-it/web build` exits 0 (assets + middleware edit do not break the build).
- `curl`-equivalent unit: `middleware-public.test.ts` proves `/.well-known/farcaster.json` is treated public.
- The four RED scaffolds are collected and run under `pnpm --filter @call-it/web test`.
</verification>

<success_criteria>
- icon.png (1024x1024, no alpha) + splash.png (200x200) exist in apps/web/public.
- middleware PUBLIC_PREFIXES includes '/.well-known'.
- 4 RED test scaffolds + the shared fixtures module exist, run under vitest, and back the SC1a/SC1b/SC1c/SC2 rows in 08-VALIDATION.md.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces (Phase 8 — all plans)

Phase 8 adds ZERO new npm dependencies and ZERO new contract surface (RESEARCH recommendation). Every new symbol/file/route below:

### New files
| Artifact | Plan | Kind | Notes |
|----------|------|------|-------|
| `apps/web/public/icon.png` | 01 | static asset | 1024x1024 PNG, no alpha — manifest `iconUrl` |
| `apps/web/public/splash.png` | 01 | static asset | 200x200 PNG — manifest/embed `splashImageUrl` |
| `apps/web/lib/farcaster-fixtures.ts` | 01 | module | pure per-status button-set table + seeded callId fixtures (`live -> [Follow,Fade,Challenge]`, `settled -> [Follow,Challenge,Quote]`) |
| `apps/web/tests/farcaster-embed.test.ts` | 01 (scaffold) → 02 (GREEN) | test | SC1a/SC3 embed-meta shape |
| `apps/web/tests/farcaster-manifest.test.ts` | 01 (scaffold) → 02 (GREEN) | test | SC1b manifest schema |
| `apps/web/tests/frame-tx.test.ts` | 01 (scaffold) → 03 (GREEN) | test | SC2 tx wire + status triplet + decode round-trip |
| `apps/web/tests/middleware-public.test.ts` | 01 | test | SC1c `/.well-known` + `/api/frame` public |
| `apps/web/lib/farcaster-embed.ts` | 02 | module | exports `buildFarcasterEmbeds(...) -> {miniappEmbed, frameEmbed}` (pure) |
| `apps/web/app/.well-known/farcaster.json/route.ts` | 02 | route handler (GET) | public nodejs manifest, body-only, NO `accountAssociation` (D-05) |
| `apps/web/lib/abis/ChallengeEscrow.ts` | 03 | ABI | exports `challengeEscrowAbi` (`proposeChallenge(uint256,uint96)->uint256`) promoted from `ChallengeFormModal.tsx` CE_ABI |
| `apps/web/app/api/frame/tx/[callId]/route.ts` | 03 | route handler (POST) | public nodejs Frame tx wire endpoint; status-aware buttons; origin-locked `to`; `eth_sendTransaction` wire JSON |

### New exported symbols
- `buildFarcasterEmbeds` (apps/web/lib/farcaster-embed.ts) — Plan 02
- `challengeEscrowAbi` (apps/web/lib/abis/ChallengeEscrow.ts + barrel `apps/web/lib/abis/index.ts`) — Plan 03
- per-status button-set table + fixtures (apps/web/lib/farcaster-fixtures.ts) — Plan 01

### New `Metadata.other` keys (apps/web/app/call/[id]/layout.tsx, Plan 02)
- `fc:miniapp` (primary; `action.type: 'launch_miniapp'`)
- `fc:frame` (legacy compat; `action.type: 'launch_frame'`) — only `action.type` differs (D-03)

### Modified (no new symbol)
- `apps/web/middleware.ts` — `PUBLIC_PREFIXES += '/.well-known'` (Plan 01)
- `apps/web/lib/abis/index.ts` — re-export `challengeEscrowAbi` (Plan 03)
- `apps/web/app/call/[id]/page.tsx` — `SHARE AS FRAME →` outline control (Plan 04)
- `apps/relayer/src/workers/__tests__/auto-post-worker.test.ts` — embed-rides-receiptUrl assertion (Plan 04)
- `packages/shared/src/share/share-text.ts` — `warpcastComposeUrl` host verified/possibly 1-line updated (Plan 04, Open Q3)

### Reused UNCHANGED (locked Phase-7 assets — NOT modified)
- `apps/web/app/og/[callId]/route.ts`, `apps/web/app/og/duel/[challengeId]/route.ts` (embed `imageUrl`)
- `apps/web/lib/abis/{FollowFadeMarket,CallRegistry,ProfileRegistry}.ts`, `packages/shared/.../addresses.ts`

### Deferred to Phase 10 (NOT produced here — D-01/D-05/D-04)
- signed `accountAssociation` block in the manifest (mainnet domain)
- `@farcaster/miniapp-sdk` / `@farcaster/miniapp-wagmi-connector` modern in-frame SPA + its live tap-to-transact (`[ASSUMED]` packages, gated behind a checkpoint if ever introduced)
- Mini App catalog/directory submission
- live in-Warpcast broadcast on `eip155:42161`
</artifacts_this_phase_produces>

<source_coverage_audit>
## Multi-Source Coverage Audit (Phase 8)

All four source types audited. Every in-scope item is COVERED by a plan. No unplanned items.

### GOAL (ROADMAP Phase 8 SC1–SC3)
| Goal item | Covered by | Status |
|-----------|------------|--------|
| SC1: receipt server-renders fc:frame meta + Mini App manifest; Warpcast renders OG card + action buttons | 02 (embed meta + manifest) + 01 (assets + public carve-out) | COVERED |
| SC2: tapping Follow/Fade/Challenge initiates the tx flow via the Frame tx protocol (testnet: wire well-formed; live broadcast = Phase 10 per D-01) | 03 (Frame tx wire endpoint) | COVERED (live tap = Phase-10 gate, recorded Manual-Only) |
| SC3: Farcaster rendering matches the Twitter OG variant; auto-post lands the cast; Mini App discoverable | 04 (auto-post embed rides + share-as-Frame) + 02 (embed reuses unchanged OG card) | COVERED (catalog submission = Phase 10 per D-04) |

### REQ (REQUIREMENTS.md phase_req_ids)
| Req ID | Covered by | Status |
|--------|------------|--------|
| SHARE-19 | 01, 02, 03, 04 (all plans list `requirements: [SHARE-19]`) | COVERED |

### RESEARCH (08-RESEARCH.md features/constraints/pitfalls)
| Item | Covered by | Status |
|------|------------|--------|
| Pattern 1 embed meta (fc:miniapp + fc:frame) | 02 T1 | COVERED |
| Pattern 2 manifest body, no accountAssociation | 02 T2 | COVERED |
| Pattern 3 legacy Frame tx wire (testable path) | 03 T2 | COVERED |
| Pattern 4 auto-post embed rides receiptUrl | 04 T1 | COVERED |
| Pitfall 2 `/.well-known` middleware carve-out | 01 T1 | COVERED |
| Pitfall 3 USDC-allowance one-tap blocker | 03 T2 (assume-allowance + deep-link policy, stated in code) | COVERED |
| Pitfall 4 stale OG embed version | 02 T1 (reuse same statusVersion) | COVERED |
| Pitfall 5 dotted route segment | 02 T2 (build-verify + rewrite fallback) | COVERED |
| Open Q1 settled Follow (off-chain) | 03 T2 (deep-link, D-06a) | COVERED |
| Open Q2 Quote not one-tap | 03 T2 (deep-link, D-06a) | COVERED |
| Open Q3 compose host migration | 04 T1 (verify-at-execute, 1-line fix) | COVERED |
| Static assets icon/splash | 01 T1 | COVERED |
| `@farcaster/*` SPA packages (modern path) | out of scope (Phase 10) — RESEARCH "ship testable scope with zero new deps" | EXCLUDED (Phase-10) |

### CONTEXT (08-CONTEXT.md D-01..D-07 + D-06a)
| Decision | Covered by (must_haves/truths citing the ID) | Status |
|----------|----------------------------------------------|--------|
| D-01 build-now / Phase-10 live gate | 01, 03 truths | COVERED |
| D-02 context-aware buttons | 02, 03, 04 truths | COVERED |
| D-03 emit both fc:miniapp + fc:frame | 02 truth | COVERED |
| D-04 auto-post + share-as-Frame; defer catalog | 02, 04 truths | COVERED |
| D-05 manifest now, association deferred | 02 truths | COVERED |
| D-06 settled triplet Follow/Challenge/Quote | 03 truth | COVERED |
| D-06a Follow/Quote deep-link, Challenge one-tap | 03 truths | COVERED |
| D-07 one-tap min-stake; larger via deep-link | 03 truths | COVERED |

**Exclusions (not gaps):** Deferred Ideas in 08-CONTEXT.md (live tap, signed accountAssociation, catalog submission, Frame text-input, mobile-responsive) are scoped to Phase 10/Phase 9. RESEARCH `@farcaster/*` SPA packages are the Phase-10 production-UX path.
</source_coverage_audit>

<output>
Create `.planning/phases/08-farcaster-mini-apps/08-01-SUMMARY.md` when done.
</output>
