---
phase: 07-og-service-final-variants-subgraph-final-mappings
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/web/eslint-rules/no-display-grid-in-og.js
  - apps/web/eslint.config.js
  - apps/web/tests/og-thumbnail-200px.spec.ts
  - apps/web/lib/share-text.ts
  - apps/web/tests/share-text.test.ts
  - apps/relayer/src/workers/__tests__/auto-post-worker.test.ts
  - packages/subgraph/tests/call-statement.test.ts
autonomous: true
requirements: [SHARE-15, SHARE-18]
must_haves:
  truths:
    - "Wave-0 gate (D-08 verify): a planted display:'grid' in an OG source file fails `pnpm --filter @call-it/web exec eslint app/og` with non-zero exit"
    - "Wave-0 gate (per D-01..D-08 enforcement): every downstream Phase 7 task has an automated verify rooted in a scaffold created here"
    - "share-text builders produce a Twitter-intent URL and a Warpcast cast URL (SHARE-15/SHARE-18) shared by web Share button + relayer auto-post (D-02)"
  artifacts:
    - path: "apps/web/eslint-rules/no-display-grid-in-og.js"
      provides: "Custom flat-config eslint rule banning display:'grid'/grid* in OG sources (Pitfall 15)"
      contains: "context.report"
    - path: "apps/web/lib/share-text.ts"
      provides: "twitterIntentUrl + warpcastComposeUrl pure builders (SHARE-15/18)"
      exports: ["twitterIntentUrl", "warpcastComposeUrl", "buildShareText"]
    - path: "apps/web/tests/og-thumbnail-200px.spec.ts"
      provides: "200px visual-regression scaffold for 5 outcome words (SC1)"
    - path: "apps/relayer/src/workers/__tests__/auto-post-worker.test.ts"
      provides: "RED scaffold for cache-warm + key-gate + Pitfall-18 trigger (SC2)"
    - path: "packages/subgraph/tests/call-statement.test.ts"
      provides: "RED scaffold for Call.statement templated default (SC3/D-05)"
  key_links:
    - from: "apps/web/eslint.config.js"
      to: "apps/web/eslint-rules/no-display-grid-in-og.js"
      via: "plugins + rules wiring scoped to app/og + lib/og-*"
      pattern: "no-display-grid"
---

<objective>
Lay the Wave-0 test + enforcement scaffolds for Phase 7 so every downstream task has a deterministic `<automated>` verify. Builds the real custom eslint rule that bans `display:'grid'` in OG sources (replacing the fake string-match), the 200px visual-regression spec for the 5 outcome words, the RED scaffolds for the auto-post worker and the subgraph `Call.statement` mapping, and the shared `share-text.ts` Twitter-intent + Warpcast-cast builders consumed by both the web Share button (SHARE-15) and the relayer auto-post worker (SHARE-18, D-02).

Purpose: Nyquist compliance — Phase 7 verification cannot pass without these scaffolds existing first. The grid-lint rule and the share-text builders are also real deliverables (SC1, SHARE-15/18), not just scaffolds.
Output: One eslint rule + config wiring, three test scaffolds (200px / auto-post / subgraph), and the shared share-text builder with its unit test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-og-service-final-variants-subgraph-final-mappings/07-CONTEXT.md
@.planning/phases/07-og-service-final-variants-subgraph-final-mappings/07-RESEARCH.md
@.planning/phases/07-og-service-final-variants-subgraph-final-mappings/07-PATTERNS.md
@.planning/phases/07-og-service-final-variants-subgraph-final-mappings/07-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Custom no-display-grid eslint rule + 200px visual-regression scaffold (SC1)</name>
  <files>apps/web/eslint-rules/no-display-grid-in-og.js, apps/web/eslint.config.js, apps/web/tests/og-thumbnail-200px.spec.ts</files>
  <read_first>
    - apps/web/eslint.config.js (currently empty flat config — only an `ignores` block; PATTERNS §eslint-rules)
    - apps/web/tests/og-fallback.spec.ts (Playwright OG-route harness analog; the obsolete string-match "rule" at ~L76-104 to replace; PATTERNS §og-thumbnail-200px)
    - apps/web/playwright.config.ts (existing config, reuse — do not add a new project)
    - apps/web/app/og/[callId]/route.ts (the OG source the rule + 200px gate target; confirm `display: 'flex'` only, runtime='nodejs' line 1)
    - 07-RESEARCH.md §Code Examples (the flat-config rule body + the 200px toHaveScreenshot snippet)
  </read_first>
  <action>
    Create `apps/web/eslint-rules/no-display-grid-in-og.js` as a flat-config custom rule per 07-RESEARCH §Code Examples: a `Property` AST visitor that reports when `key === 'display' && value === 'grid'` (message "Satori does not support display:'grid' — use flexbox.") and when the property key matches `/^grid(Template|Column|Row|Area|Auto)/` (message "Satori does not support '<key>' — use flexbox."). `meta.type = 'problem'`. Extend `apps/web/eslint.config.js`: import the rule, register it as a local plugin (e.g. `plugins: { 'call-it-og': { rules: { 'no-display-grid': <rule> } } }`), and add a config object scoped to `files: ['app/og/**/*.{ts,tsx}', 'app/api/og/**/*.ts', 'lib/og-*.ts']` setting `'call-it-og/no-display-grid': 'error'`. Preserve the existing `ignores` block. Create `apps/web/tests/og-thumbnail-200px.spec.ts` reusing the og-fallback harness: a VARIANTS array for the 5 outcome words (CALLED IT / LOUD AND WRONG / CONTRARIAN HIT / COLD CALL / FADED CORRECTLY), each `page.setViewportSize({ width: 200, height: 105 })` then `expect(page).toHaveScreenshot('og-200-<word>.png', { maxDiffPixelRatio: 0.02 })` (Playwright built-in — NO new dep per D-research A4). Use placeholder/seeded callId URLs marked with a TODO that Plan 07-03 wires to real seeded settled-call IDs; the spec must be skippable (`test.fixme` or env-gated) until baselines exist so it does not block Wave 0, but the file + structure must exist. Replace the obsolete eslint string-match test in og-fallback.spec.ts with an assertion-comment pointing at the real rule (do not delete the file).
  </action>
  <verify>
    <automated>cd apps/web && node -e "const r=require('./eslint-rules/no-display-grid-in-og.js'); if(!(r.default??r).meta) process.exit(1)" && pnpm --filter @call-it/web exec eslint app/og --max-warnings=0</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/eslint-rules/no-display-grid-in-og.js` exports a rule object with `meta.type === 'problem'` and a `create(context)` returning a `Property` visitor.
    - Source assertion: `apps/web/eslint.config.js` references `no-display-grid` and scopes it to `app/og/**`, `app/api/og/**`, `lib/og-*.ts`.
    - Behavior: planting `display: 'grid'` in any `app/og/**` file makes `pnpm --filter @call-it/web exec eslint app/og` exit non-zero; with no grid present it exits 0 (current code is flexbox-only so it must pass clean).
    - `apps/web/tests/og-thumbnail-200px.spec.ts` exists with 5 outcome-word entries and uses `toHaveScreenshot` at a 200px-width viewport (no `pixelmatch`/`pngjs` import).
  </acceptance_criteria>
  <done>Grid-lint rule fires on planted grid + passes clean on the real flexbox OG sources; 200px spec file exists with all 5 outcome words scaffolded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: share-text builders (SHARE-15/18) + RED scaffolds for auto-post worker + subgraph statement</name>
  <files>apps/web/lib/share-text.ts, apps/web/tests/share-text.test.ts, apps/relayer/src/workers/__tests__/auto-post-worker.test.ts, packages/subgraph/tests/call-statement.test.ts</files>
  <read_first>
    - apps/web/lib/relayer-client.ts (lib module shape + NEXT_PUBLIC_* base-URL handling; PATTERNS §share-text analog)
    - apps/relayer/src/lib/x-api-client.ts (key-gate degrade pattern the worker test will assert against; L73-79, L110-118)
    - apps/relayer/src/workers/__tests__/criteria-store.test.ts AND .../oracle-attestation.test.ts (existing relayer vitest worker-test shape)
    - packages/subgraph/tests/schema.test.ts (existing subgraph vitest shape) + packages/subgraph/vitest.config.ts
    - 07-RESEARCH.md §Code Examples (twitterIntentUrl + warpcastComposeUrl shapes; A3 Warpcast URL caveat)
  </read_first>
  <behavior>
    - Test 1 (share-text): `twitterIntentUrl(receiptUrl, text)` returns `https://twitter.com/intent/tweet?text=<enc>&url=<enc>` with both args URL-encoded.
    - Test 2 (share-text): `warpcastComposeUrl(receiptUrl, text)` returns `https://warpcast.com/~/compose?text=<enc>&embeds[]=<enc>` with both args URL-encoded.
    - Test 3 (share-text): `buildShareText({ outcomeWord, handle, statement })` returns a non-empty string ≤ 240 chars containing the outcome word (post-text construction, D-02).
    - Test 4 (auto-post RED scaffold): a `describe.todo`/failing placeholder asserting the worker (Plan 07-04) will (a) no-op when `X_API_WRITE_TOKEN` absent, (b) HEAD-verify `X-Variant` before post, (c) gate the trigger per the Pitfall-18 decision.
    - Test 5 (subgraph RED scaffold): a `describe.todo`/failing placeholder asserting `Call.statement` gets a templated default when enrichment is absent (Plan 07-02).
  </behavior>
  <action>
    Create `apps/web/lib/share-text.ts` with pure, dependency-free exports `twitterIntentUrl(receiptUrl, text)`, `warpcastComposeUrl(receiptUrl, text)`, and `buildShareText({ outcomeWord, handle, statement })` per 07-RESEARCH §Code Examples. Keep them pure (no `process.env`, no fetch) so BOTH `apps/web` (Share button, Plan 07-05) and `apps/relayer` (auto-post worker, Plan 07-04) can import the same logic; add a header comment noting the A3 Warpcast-URL caveat (verify shape against current Farcaster docs when keys are budgeted). Create `apps/web/tests/share-text.test.ts` with the 3 builder tests above (GREEN — these are real deliverables). Create `apps/relayer/src/workers/__tests__/auto-post-worker.test.ts` as a RED scaffold (`describe.todo` / `it.todo` or a single failing-by-design placeholder import) enumerating the SC2 behaviors (cache-warm HEAD + X-Variant + ETag, ≤30s retry, key-gated no-op, Pitfall-18 trigger gate). Create `packages/subgraph/tests/call-statement.test.ts` as a RED scaffold enumerating the SC3/D-05 templated-default behavior. The two RED scaffolds MUST be authored so Plan 07-04 and 07-02 turn them GREEN; do not delete or weaken them.
  </action>
  <verify>
    <automated>pnpm --filter @call-it/web exec playwright test share-text.test.ts --reporter=line 2>/dev/null || pnpm --filter @call-it/web exec vitest run tests/share-text.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/lib/share-text.ts` exports `twitterIntentUrl`, `warpcastComposeUrl`, `buildShareText`; none of them reads `process.env` or calls `fetch` (purity assertion — grep shows no `process.env`/`fetch` in the file).
    - share-text unit tests pass: encoded Twitter intent URL, encoded Warpcast compose URL, share text ≤ 240 chars containing the outcome word.
    - `apps/relayer/src/workers/__tests__/auto-post-worker.test.ts` exists and references cache-warm / X-Variant / key-gate / Pitfall-18 (todo or failing-by-design) — it is the scaffold Plan 07-04 turns green.
    - `packages/subgraph/tests/call-statement.test.ts` exists referencing the templated-default behavior — the scaffold Plan 07-02 turns green.
  </acceptance_criteria>
  <done>share-text builders pass their unit tests; auto-post + subgraph-statement RED scaffolds exist and are wired to the Wave-0 verification map in 07-VALIDATION.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OG source → Satori renderer | A `display:grid` slipping into an OG file silently misrenders at 200px on X (the "CALLED IT" word vanishes). The eslint rule is the boundary guard. |
| share-text builders → public post text | Builders construct URLs/text embedded in a public tweet/cast; they must not leak secrets and must URL-encode untrusted statement strings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Tampering | OG card render (grid misrender) | mitigate | Real custom eslint rule bans `display:'grid'`/`grid*` in `app/og`, `app/api/og`, `lib/og-*` (Pitfall 15); CI fails on a planted grid. |
| T-07-01-02 | Information Disclosure | share-text builders | mitigate | Builders are pure (no env/secret access); statement string is URL-encoded; no token ever passes through this module (write token lives only in the relayer, Plan 07-04). |
| T-07-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this plan (Playwright built-in `toHaveScreenshot`, A4) — Package Legitimacy Audit confirms zero new deps; slopcheck N/A. |
</threat_model>

<verification>
- `pnpm --filter @call-it/web exec eslint app/og --max-warnings=0` exits 0 on the real (flexbox-only) OG sources, non-zero on a planted `display:'grid'`.
- share-text unit tests green.
- auto-post + subgraph RED scaffolds present (Wave-0 gaps in 07-VALIDATION.md checked off).
</verification>

<success_criteria>
- Custom grid-lint rule is real (replaces the fake string-match) and wired scoped to OG files.
- 200px visual-regression spec exists for all 5 outcome words.
- share-text builders (SHARE-15/18) pass unit tests and are importable by web + relayer.
- RED scaffolds for auto-post (SC2) and subgraph statement (SC3/D-05) exist.
</success_criteria>

<output>
Create `.planning/phases/07-og-service-final-variants-subgraph-final-mappings/07-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (Plan 07-01 contributions)
- `apps/web/eslint-rules/no-display-grid-in-og.js` — NEW custom eslint flat-config rule (Pitfall 15 enforcement gate).
- `apps/web/eslint.config.js` — EXTENDED to wire the rule scoped to OG files.
- `apps/web/tests/og-thumbnail-200px.spec.ts` — NEW 200px visual-regression spec (5 outcome words).
- `apps/web/lib/share-text.ts` — NEW `twitterIntentUrl` / `warpcastComposeUrl` / `buildShareText` builders.
- `apps/web/tests/share-text.test.ts` — NEW unit test for the builders.
- `apps/relayer/src/workers/__tests__/auto-post-worker.test.ts` — NEW RED scaffold (SC2).
- `packages/subgraph/tests/call-statement.test.ts` — NEW RED scaffold (SC3/D-05).
