---
phase: 07-og-service-final-variants-subgraph-final-mappings
plan: 04
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - apps/relayer/src/lib/x-write-client.ts
  - apps/relayer/src/workers/auto-post-worker.ts
  - apps/relayer/src/workers/__tests__/auto-post-worker.test.ts
  - apps/relayer/src/db/schema.ts
  - apps/relayer/src/index.ts
autonomous: true
requirements: [SHARE-16, SHARE-17, SHARE-18, SHARE-03]
must_haves:
  truths:
    - "D-02 (full mechanism, key-gated): the auto-post worker runs the Pitfall-8 cache-warm sequence (bump statusVersion → GET /og/{id}?v={sv} regen → HEAD 200 + X-Variant settled|caller-exited + stable ETag) before posting; on failure it delays ≤30s then retries (SHARE-03)"
    - "D-02/SHARE-17 (degrade-to-no-op): x-write-client returns { posted:false } (NO throw) when X_API_WRITE_TOKEN is absent — mechanism ships, activates with no code change when keys land; the write is the only X mutation (SHARE-17)"
    - "D-07 (Pitfall-18 reconciliation, BLOCKING — RESOLVED): the Phase-4 runbook (04-RESEARCH.md:652-654) implemented NO on-chain claim-delay (claims occur during the 24h window; reversal affects unclaimed funds only). Per D-07 'if the runbook is silent, default-ON fires after cache-warm succeeds' — the trigger is default-ON, fires after cache-warm, with a configurable post-settle delay (PITFALLS.md:789 auto-post mitigation) to absorb early disputes"
    - "SHARE-16 (default-ON): auto-post is default-ON per the Advanced Settings toggle; toggling OFF is non-destructive"
    - "SHARE-18: the Farcaster cast URL is constructed parallel to the Twitter post via the shared share-text builders"
    - "D-02 (security): X_API_WRITE_TOKEN is added to the pino redact list and never logged"
  artifacts:
    - path: "apps/relayer/src/lib/x-write-client.ts"
      provides: "POST /2/tweets thin fetch wrapper, key-gated degrade-to-no-op (mirrors x-api-client.ts)"
      exports: ["postTweet"]
    - path: "apps/relayer/src/workers/auto-post-worker.ts"
      provides: "settle-triggered cache-warm + post worker (never-throw, idempotent posted-receipt dedup)"
      exports: ["startAutoPostWorker"]
    - path: "apps/relayer/src/db/schema.ts"
      provides: "posted_receipts dedup table (call_id unique)"
      contains: "posted_receipts"
  key_links:
    - from: "apps/relayer/src/workers/auto-post-worker.ts"
      to: "OG /og/{id} HEAD probe + x-write-client postTweet + share-text builders"
      via: "cache-warm HEAD → key-gated post → Farcaster cast URL"
      pattern: "X-Variant|postTweet|warpcastComposeUrl"
---

<objective>
Build the auto-post share-loop driver in the relayer (D-02): a settle-triggered worker that runs the Pitfall-8 cache-warm verification before posting, a key-gated `x-write-client` that degrades to a no-op when X write keys are absent, idempotent posted-receipt dedup, and the Farcaster cast URL constructed in parallel via the shared `share-text` builders. The Pitfall-18 / claim-delay reconciliation (D-07, BLOCKING) is RESOLVED against the Phase-4 runbook: that runbook implemented no on-chain claim-delay (claims occur during the dispute window; reversal affects unclaimed funds only — 04-RESEARCH.md:652-654), so per D-07 the trigger is default-ON and fires after cache-warm succeeds, with a configurable post-settle delay (the PITFALLS.md:789 auto-post-specific mitigation) to absorb early disputes.

Purpose: This closes critical-path step 9 (Share to X/FC) at the automation layer. The mechanism ships fully built and activates with zero code change once X write keys are budgeted.
Output: x-write-client + auto-post-worker + posted-receipts dedup table + worker start wiring + redact-list update; the Plan 07-01 RED scaffold turns GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-og-service-final-variants-subgraph-final-mappings/07-CONTEXT.md
@.planning/phases/07-og-service-final-variants-subgraph-final-mappings/07-RESEARCH.md
@.planning/phases/07-og-service-final-variants-subgraph-final-mappings/07-PATTERNS.md
@.planning/research/PITFALLS.md
@apps/relayer/src/lib/x-api-client.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: x-write-client (key-gated no-op) + posted-receipts dedup table (D-02, SHARE-17)</name>
  <files>apps/relayer/src/lib/x-write-client.ts, apps/relayer/src/db/schema.ts, apps/relayer/src/index.ts</files>
  <read_first>
    - apps/relayer/src/lib/x-api-client.ts (the exact mirror: module header/no-SDK note L1-20; key-gate L73-79; 429/!ok handling L110-118 — change throw→return for the write path)
    - apps/relayer/src/db/schema.ts (notifications unique-index pattern ~L152-159; callOracleCriteria ~L261-281 — analog for a posted_receipts(call_id unique) table)
    - apps/relayer/src/index.ts (pino redact list ~L89-101 — add X_API_WRITE_TOKEN; CORS block L117-133 unchanged here)
    - 07-RESEARCH.md §Pattern 3 (key-gated degrade-to-no-op; A2 user-context credential caveat) + §Security Domain V2/V7
    - 07-PATTERNS.md §x-write-client.ts
  </read_first>
  <action>
    Create `apps/relayer/src/lib/x-write-client.ts` mirroring `x-api-client.ts`: a thin `fetch`-based `postTweet({ text, url })` that POSTs `/2/tweets` (NO npm SDK), reading `process.env.X_API_WRITE_TOKEN` (user-context OAuth — A2; document the credential caveat in the header). KEY-GATE: when the token is absent, log `{ event: 'x_write_no_key' }` (token value NEVER logged) and RETURN `{ posted: false, reason: 'no_key' }` — do NOT throw (write-path differs from x-api-client which throws). Handle 429 / `!res.ok` by logging + returning a degraded result (no throw). Add a `postedReceipts = pgTable('posted_receipts', { callId: integer('call_id').primaryKey().notNull(), postedAt: timestamp('posted_at').defaultNow().notNull() })` table to `schema.ts` (callId PK = the "posted-once" dedup, mirroring the notifications unique-index discipline). Add `X_API_WRITE_TOKEN` (and any user-context consumer secret) to the pino redact list in `index.ts` (V7). Generate the migration for posted_receipts (`db:generate`).
  </action>
  <verify>
    <automated>pnpm --filter @call-it/relayer db:generate && pnpm --filter @call-it/relayer build && pnpm --filter @call-it/relayer test x-write-client 2>/dev/null || pnpm --filter @call-it/relayer test</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `x-write-client.ts` exports `postTweet`; on missing `X_API_WRITE_TOKEN` it RETURNS `{ posted:false }` (no `throw`), logging `x_write_no_key` without the token value.
    - Source assertion: `schema.ts` declares `posted_receipts` with `call_id` PK; a migration is generated for it.
    - Source assertion: `index.ts` redact list includes `X_API_WRITE_TOKEN`.
    - Behavior: relayer build exits 0; a unit test (mirroring x-api-client tests) confirms the no-key no-op returns without throwing AND that a present key drives exactly one POST.
  </acceptance_criteria>
  <done>Key-gated write client degrades to a no-op without keys; posted-receipts dedup table + migration exist; write token is redacted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: auto-post-worker — cache-warm (Pitfall 8) + Pitfall-18 trigger + Farcaster cast (D-02/D-07, SHARE-16/18)</name>
  <files>apps/relayer/src/workers/auto-post-worker.ts, apps/relayer/src/workers/__tests__/auto-post-worker.test.ts, apps/relayer/src/index.ts</files>
  <read_first>
    - apps/relayer/src/workers/notification-fanout.ts (worker template: Config/Handle ~L92-110; statusVersion key + incr ~L118-122 + L303-318; never-throw catch ~L635-643; CallSettled/CallerExited getLogs chunking blockSpan=9n ~L485-491,L595-620; queryHolders fetch shape ~L145-200; onConflictDoNothing ~L280-288)
    - apps/relayer/src/workers/settlement-watcher.ts (error discipline ~L715-728 — never throw)
    - apps/relayer/src/lib/x-write-client.ts (postTweet from Task 1)
    - apps/web/lib/share-text.ts (twitterIntentUrl/warpcastComposeUrl/buildShareText from Plan 07-01 — import the pure builders for SHARE-18)
    - apps/relayer/src/workers/__tests__/auto-post-worker.test.ts (the Plan 07-01 RED scaffold — turn GREEN)
    - .planning/phases/04-settlementmanager-7-oracle-paths-solidity-baseline-rep-delta/04-RESEARCH.md (§"Pitfall 18: Claim-Delay Decision" L652-654 — the runbook decision to reconcile against)
    - .planning/research/PITFALLS.md (Pitfall 18 L550-574 mitigation options; L789 auto-post 30-min delay mitigation)
    - 07-RESEARCH.md §Pattern 2 (worker as share-loop driver) + §Pattern 4 (statusVersion cache-bust)
  </read_first>
  <action>
    Create `apps/relayer/src/workers/auto-post-worker.ts` mirroring `notification-fanout.ts`: poll `CallSettled` + `CallerExited` via `parseAbiItem` + chunked `getLogs` (reuse blockSpan=9n + initPromise head-seeding + `initialized` guard). For each newly-settled call, run the Pitfall-8 cache-warm sequence: (1) `redis.incr(status_version:{callId})`, (2) `GET ${OG_BASE}/og/{id}?v={sv}` to force regen, (3) `HEAD ${OG_BASE}/og/{id}?v={sv}` and assert `200` + `X-Variant` ∈ {settled, caller-exited} + a stable ETag; on failure `delay ≤30s` then retry (SHARE-03). PITFALL-18 TRIGGER (D-07, reconciled): the Phase-4 runbook implemented no on-chain claim-delay (04-RESEARCH.md:652-654 — claims occur during the dispute window, reversal affects unclaimed funds only), so per D-07 default-ON fires AFTER cache-warm succeeds, gated by a configurable `AUTO_POST_DELAY_MS` (default a short post-settle delay per PITFALLS.md:789 to absorb early disputes; document the value in the worker header + the operator runbook). Build the post text via `buildShareText(...)` and post via `postTweet(...)` (Task 1) ONLY when not already in `posted_receipts` (idempotent dedup, `onConflictDoNothing` after a successful/no-op post). Construct the Farcaster cast URL in parallel via `warpcastComposeUrl(...)` (SHARE-18 — constructed in Phase 7; landing the cast is Phase 8). Never-throw discipline on every per-call try/catch (`{ event: 'auto_post_error', callId, phase }` + errors++); the interval must survive any single failure. Wire `startAutoPostWorker(...)` into the relayer boot (`index.ts` onReady, alongside the other workers) behind a default-ON `AUTO_POST_ENABLED` flag (SHARE-16). Turn the Plan 07-01 RED scaffold `auto-post-worker.test.ts` GREEN: assert (a) key-absent → no-op no-throw, (b) HEAD asserts X-Variant before posting, (c) ≤30s retry on cache-warm failure (fake clock), (d) the trigger gate fires after cache-warm + delay (Pitfall-18 consistency), (e) re-processed CallSettled does not double-post (dedup).
  </action>
  <verify>
    <automated>pnpm --filter @call-it/relayer test auto-post-worker && pnpm --filter @call-it/relayer build</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `auto-post-worker.ts` exports `startAutoPostWorker`, performs `redis.incr(status_version:...)` → GET regen → HEAD asserting `X-Variant` + ETag before any post, with a ≤30s retry on failure.
    - Source assertion: the trigger is default-ON, gated by `AUTO_POST_DELAY_MS`, and a header comment cites the Phase-4 runbook decision (04-RESEARCH.md:652-654) + D-07 — proving the Pitfall-18 reconciliation was performed.
    - Source assertion: a successful/no-op post writes to `posted_receipts` with `onConflictDoNothing`; the worker imports `warpcastComposeUrl` for the parallel Farcaster cast (SHARE-18).
    - Source assertion: `index.ts` starts the worker on boot behind `AUTO_POST_ENABLED` (default-ON, SHARE-16).
    - Behavior: `auto-post-worker.test.ts` (Plan 07-01 scaffold) is GREEN for all 5 assertions: no-key no-op, X-Variant gate, ≤30s retry, post-settle-delay trigger, dedup.
  </acceptance_criteria>
  <done>Auto-post worker runs cache-warm → key-gated post → Farcaster cast URL, default-ON, reconciled with the Phase-4 Pitfall-18 decision, idempotent, never-throw; the worker test is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Relayer worker → X API (POST /2/tweets) | The only X account mutation; uses a write token held server-side only; must degrade to no-op when absent (SHARE-17). |
| Settle event → public auto-post | Auto-post drives an attention spike; a stale OG preview or a pre-dispute post is a public-claim integrity risk. |
| Worker → OG service (HEAD probe) | The cache-warm probe must confirm the regenerated settled card before the post fires (Pitfall 8). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Information Disclosure | X write token in logs/bundle | mitigate | Token relayer-only; added to pino redact list; never logged (mirror x-api-client `x_*` event logs without value); degrade-to-no-op when absent. |
| T-07-04-02 | Tampering (public claim) | auto-post forging a stale preview | mitigate | Pitfall-8 cache-warm gate: bump statusVersion → GET regen → HEAD 200 + `X-Variant: settled|caller-exited` + stable ETag before posting; ≤30s retry. |
| T-07-04-03 | Repudiation / financial | auto-post front-running disputes (instant claims) | mitigate | Pitfall-18 reconciled with Phase-4 runbook (04-RESEARCH.md:652-654): no on-chain claim-delay exists; default-ON fires after cache-warm + a configurable post-settle delay (PITFALLS.md:789) to absorb early disputes; runbook documents reversal affects unclaimed funds only. |
| T-07-04-04 | Tampering | double-post on re-processed event | mitigate | `posted_receipts(call_id)` unique + `onConflictDoNothing` idempotency; never-throw worker discipline. |
| T-07-04-SC | Tampering | npm installs | mitigate | No new packages — raw `fetch` write client (no X SDK), bullmq/ioredis/pino already installed; slopcheck N/A. |
</threat_model>

<verification>
- `auto-post-worker.test.ts` green: no-key no-op, X-Variant cache-warm gate, ≤30s retry, post-settle-delay trigger, dedup.
- relayer `build` exits 0.
- Pitfall-18 reconciliation is documented inline (header comment cites 04-RESEARCH.md:652-654 + D-07).
</verification>

<success_criteria>
- Full auto-post mechanism ships, key-gated to a no-op (D-02/SHARE-16/17), with the Farcaster cast URL constructed in parallel (SHARE-18).
- Cache-warm (Pitfall 8) + Pitfall-18 trigger reconciliation both enforced.
- X write token redacted; idempotent dedup; never-throw worker.
</success_criteria>

<output>
Create `.planning/phases/07-og-service-final-variants-subgraph-final-mappings/07-04-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (Plan 07-04 contributions)
- `apps/relayer/src/lib/x-write-client.ts` — NEW key-gated `postTweet` (degrade-to-no-op).
- `apps/relayer/src/workers/auto-post-worker.ts` — NEW settle-triggered cache-warm + post worker (`startAutoPostWorker`).
- `apps/relayer/src/workers/__tests__/auto-post-worker.test.ts` — TURNED GREEN (cache-warm + key-gate + retry + Pitfall-18 + dedup).
- `apps/relayer/src/db/schema.ts` — EXTENDED with `posted_receipts` dedup table (+ migration).
- `apps/relayer/src/index.ts` — WIRED worker boot (AUTO_POST_ENABLED default-ON) + X_API_WRITE_TOKEN redact.
