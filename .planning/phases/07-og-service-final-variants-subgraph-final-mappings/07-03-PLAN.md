---
phase: 07-og-service-final-variants-subgraph-final-mappings
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - apps/web/app/og/[callId]/route.ts
  - apps/web/app/og/duel/[challengeId]/route.ts
  - apps/web/lib/relayer-client.ts
  - apps/web/tests/og-thumbnail-200px.spec.ts
autonomous: true
requirements: [SHARE-01, SHARE-02, SHARE-03, SHARE-20]
must_haves:
  truths:
    - "D-03 (OG real-data wiring): og/[callId] reads the market statement from the relayer marketLine (authoritative, D-05) with the subgraph Call.statement templated value as fallback — the `Call #N` stub at route.ts:777 is replaced"
    - "D-03 (settled stats): P&L / REP CHANGE / FINAL / TARGET stubs (`—` at route.ts:811-814) are wired from subgraph Settlement.priceDelta/finalPrice + RepEvent.delta"
    - "D-03 (DuelSettled): the duel OG settled fields (Pot / WINS / paired rep deltas) are wired from subgraph reads, replacing the Phase-3 stubs"
    - "SC1: all 5 OG variants render 1200×630 PNG and the outcome word stays legible at a 200px social thumbnail (200px visual-regression gate passes)"
    - "D-05 (no IPFS on hot path): the OG route's RPC freshness read of status/outcome (route.ts:782-795) is preserved; statement/settled fields come from relayer + subgraph only"
    - "SHARE-20: receipts stay off-chain OG images referenced on-chain by hash — no NFT minting introduced"
  artifacts:
    - path: "apps/web/app/og/[callId]/route.ts"
      provides: "Live/Settled/CallerExited cards reading real statement + settled stats"
      contains: "marketLine"
    - path: "apps/web/app/og/duel/[challengeId]/route.ts"
      provides: "DuelSettled card with real settled fields"
      contains: "Settlement"
    - path: "apps/web/lib/relayer-client.ts"
      provides: "getCallStatement / settled-field fetch helpers (relayer marketLine + subgraph fallback)"
      exports: ["getMarketLine"]
    - path: "apps/web/tests/og-thumbnail-200px.spec.ts"
      provides: "200px baselines committed for the 5 outcome words"
  key_links:
    - from: "apps/web/app/og/[callId]/route.ts"
      to: "apps/relayer /api/calls/:id/live-state (marketLine) + subgraph Settlement/RepEvent"
      via: "fetch marketLine + GraphQL settled-field read"
      pattern: "marketLine|Settlement"
---

<objective>
Wire all OG card variants to real data (D-03), replacing the `Call #N` statement stub and the `—` settled-stat stubs. The statement comes from the relayer `marketLine` (authoritative, D-05) with the subgraph `Call.statement` templated value as the safe fallback; the settled stats (P&L / REP CHANGE / FINAL / TARGET) come from the subgraph `Settlement` + `RepEvent` entities; the DuelSettled card's settled fields are wired the same way. The existing RPC read of `status`/`outcome` (freshness, Pitfall 8) and the `getOutcomeWordResult()` color logic (§14.1 locked) are preserved untouched. Commit the 200px visual-regression baselines so the SC1 readability gate runs green.

Purpose: This closes critical-path steps 6 (Live receipt renders) + 8 (Settled receipt renders) at the OG layer — the share-loop's visual payload becomes real, not stubbed.
Output: Wired og/[callId] + og/duel routes + relayer-client helpers + committed 200px baselines.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-og-service-final-variants-subgraph-final-mappings/07-CONTEXT.md
@.planning/phases/07-og-service-final-variants-subgraph-final-mappings/07-RESEARCH.md
@.planning/phases/07-og-service-final-variants-subgraph-final-mappings/07-PATTERNS.md
@.planning/phases/07-og-service-final-variants-subgraph-final-mappings/07-UI-SPEC.md
@apps/web/app/og/[callId]/route.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire og/[callId] statement + settled stats from relayer marketLine + subgraph (D-03)</name>
  <files>apps/web/app/og/[callId]/route.ts, apps/web/lib/relayer-client.ts</files>
  <read_first>
    - apps/web/app/og/[callId]/route.ts (the stubs to replace: `callStatement = Call #${callIdStr}` at L777; `pnlStr/repDeltaStr/finalValue/targetValue = '—'` at L811-814; KEEP the RPC status/outcome read L782-795 and `getOutcomeWordResult()` L796-801)
    - apps/web/lib/relayer-client.ts (getProfile at ~L122 + NEXT_PUBLIC_* base-URL at ~L13 — analog for a getMarketLine helper)
    - apps/relayer/src/routes/live-state.ts (the marketLine field wired in Plan 07-02 — the authoritative source)
    - packages/subgraph/src/settlement-manager.ts (Settlement.priceDelta/finalPrice + RepEvent.delta populate — ~L46-57; the settled-field source)
    - apps/web/lib/outcome-word.ts (getOutcomeWordResult — §14.1 locked colors; DO NOT modify)
    - 07-RESEARCH.md §Pattern 1 (subgraph for display fields, RPC for freshness) + §Pattern 4 (?v=statusVersion already wired)
    - 07-PATTERNS.md §og/[callId]/route.ts WIRE real data
  </read_first>
  <action>
    Add a `getMarketLine(callId)` helper to `apps/web/lib/relayer-client.ts` that fetches `/api/calls/:id/live-state` and returns the `marketLine` (authoritative prose from Plan 07-02), and a settled-field reader that GraphQL-queries the subgraph for `Settlement { priceDelta finalPrice }` + `RepEvent { delta }` for the callId (subgraph read stays server-side per D-27; reuse the existing relayer-proxy/subgraph-client convention). In `apps/web/app/og/[callId]/route.ts`: replace the `Call #${callIdStr}` stub (L777) with `marketLine` from the relayer, falling back to the subgraph `Call.statement` templated value, falling back to a generic safe string (never crash — UI-SPEC error-state row). Replace the settled-stat `—` stubs (L811-814): `finalValue` ← `Settlement.finalPrice`, `targetValue` ← the call target, `pnlStr` ← `Settlement.priceDelta`-derived P&L, `repDeltaStr` ← `RepEvent.delta` (sign-formatted). Wire the same statement + exit data into `buildCallerExitedCard`. PRESERVE: the RPC read of `status`/`outcome` for freshness (L782-795), `getOutcomeWordResult()` and its §14.1 colors (L796-801), `export const runtime = 'nodejs'` line 1, the `Cache-Control: max-age=60, stale-while-revalidate=300` + `X-Variant` headers, and `renderFallback` on any throw (SHARE-10). Flexbox only — the grid-lint rule (Plan 07-01) gates this. Do NOT introduce NFT minting (SHARE-20 — verify no regression).
  </action>
  <verify>
    <automated>pnpm --filter @call-it/web exec eslint app/og --max-warnings=0 && pnpm --filter @call-it/web build && pnpm --filter @call-it/web exec playwright test og-fallback.spec.ts og-unit.test.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `og/[callId]/route.ts` no longer contains the literal `` `Call #${callIdStr}` `` as the statement source; `callStatement` resolves from `marketLine` → subgraph `Call.statement` → generic fallback.
    - Source assertion: the `'—'` literals at the former L811-814 are replaced by Settlement/RepEvent-derived values (pnl/repDelta/final/target).
    - Source assertion: the RPC status/outcome read and `getOutcomeWordResult()` block are unchanged; `export const runtime = 'nodejs'` is still line 1.
    - Behavior: `eslint app/og` exits 0 (flexbox-only, no grid); `@call-it/web build` exits 0; existing og specs pass.
    - SHARE-20: no NFT mint call introduced anywhere in the OG path (grep clean).
  </acceptance_criteria>
  <done>og/[callId] renders real statement + real settled stats with safe fallbacks; freshness + color logic preserved; grid-lint + build + og specs green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire DuelSettled OG settled fields + commit 200px baselines (D-03, SC1)</name>
  <files>apps/web/app/og/duel/[challengeId]/route.ts, apps/web/tests/og-thumbnail-200px.spec.ts</files>
  <read_first>
    - apps/web/app/og/duel/[challengeId]/route.ts (Phase-3 stubs: "VS"/"WINS" slot ~L13, Pot row ~L14, "? REP" rep-deltas ~L15, settled=false-always ~L159; flexbox-row layout — KEEP)
    - packages/subgraph/src/challenge-escrow.ts + settlement-manager.ts (ChallengeSettled / Settlement reads for winner + pot + paired rep deltas)
    - apps/web/tests/og-thumbnail-200px.spec.ts (the scaffold from Plan 07-01 — fill in real seeded settled-call URLs + generate baselines)
    - apps/web/tests/og-fallback.spec.ts (PNG-dimension + header assertion patterns to mirror)
    - 07-UI-SPEC.md §OG Card Variant Contract (DuelSettled: winner highlighted / loser dimmed 40% / WINS in Syne / pot line / paired rep deltas)
  </read_first>
  <action>
    In `apps/web/app/og/duel/[challengeId]/route.ts`, replace the Phase-3 settled-field stubs: compute `settled` from the real challenge state (winner !== ZERO_ADDRESS), render "WINS" (Syne 64px) next to the winner column when settled (else keep "VS"), wire the real Pot value ("Pot: $X,XXX · winner takes all"), and the paired rep deltas from the subgraph `RepEvent`/settlement reads (replacing "? REP"). Keep the two-column `flexDirection: 'row'` layout (Pitfall 15 — no grid), winner highlighted / loser dimmed to 40% opacity per UI-SPEC, runtime='nodejs', cache headers, X-Variant, renderFallback. In `apps/web/tests/og-thumbnail-200px.spec.ts`, replace the Plan-07-01 placeholder callId URLs with real seeded settled-call IDs (use the Sepolia recovery-cluster seeded calls; document which seed maps to which outcome word), then run Playwright with `--update-snapshots` to generate and COMMIT the 5 baseline PNGs (`og-200-*.png`). The spec must now run un-skipped and assert `maxDiffPixelRatio: 0.02` against the committed baselines for all 5 outcome words.
  </action>
  <verify>
    <automated>pnpm --filter @call-it/web exec eslint app/og --max-warnings=0 && pnpm --filter @call-it/web exec playwright test og-thumbnail-200px.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `og/duel/[challengeId]/route.ts` derives `settled` from real winner state and renders "WINS" + real pot + real paired rep deltas when settled (no "? REP" / hardcoded "VS"-always).
    - Source assertion: the duel layout remains `flexDirection: 'row'` (no `display:'grid'`); `eslint app/og` exits 0.
    - Behavior: `apps/web/tests/og-thumbnail-200px.spec.ts` runs (no longer skipped) and passes for all 5 outcome words against committed baselines `og-200-*.png`.
    - The 5 baseline PNGs exist in the snapshots dir and are committed.
  </acceptance_criteria>
  <done>DuelSettled card shows real winner/pot/rep-deltas; the 200px readability gate passes for all 5 outcome words with committed baselines.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Subgraph/relayer → OG render | Display fields (statement, settled stats) cross into a public PNG; untrusted, length-capped, text-only (Satori). |
| RPC vs subgraph freshness | status/outcome must read fresh from RPC (Pitfall 8) to avoid a stale "live" card on a settled call; statement is immutable so subgraph lag is irrelevant. |
| callId param → fetch targets | callId is numeric-validated; the route fetches only project relayer/subgraph, never a user-supplied URL (no SSRF). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Tampering | OG card grid misrender | mitigate | Custom eslint rule (Plan 07-01) gates `display:'grid'` in app/og; CI fails on a planted grid; layouts stay flexbox. |
| T-07-03-02 | Tampering | stale-cache wrong preview | mitigate | RPC status/outcome freshness read preserved (Pitfall 8); statement immutable so subgraph lag is safe. |
| T-07-03-03 | Tampering | untrusted statement string in render | mitigate | Length-cap (OG truncates 77/87); Satori renders text not HTML; subgraph templated fallback guarantees a safe default. |
| T-07-03-04 | Tampering (SSRF) | og:image via attacker callId | mitigate | callId numeric-validated; fetch targets are fixed project relayer/subgraph only. |
| T-07-03-SC | Tampering | npm installs | mitigate | No new packages — @vercel/og 0.11.1 + Playwright built-in `toHaveScreenshot` (A4); slopcheck N/A. |
</threat_model>

<verification>
- `eslint app/og` exits 0 (grid-lint clean).
- `@call-it/web build` exits 0.
- `og-fallback.spec.ts` + `og-unit.test.ts` pass; `og-thumbnail-200px.spec.ts` passes for all 5 outcome words with committed baselines.
</verification>

<success_criteria>
- All 5 OG variants render real statement + real settled stats (D-03); stubs gone.
- 200px outcome-word readability gate green (SC1).
- Freshness (Pitfall 8) + §14.1 colors preserved; no NFT minting (SHARE-20).
</success_criteria>

<output>
Create `.planning/phases/07-og-service-final-variants-subgraph-final-mappings/07-03-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (Plan 07-03 contributions)
- `apps/web/app/og/[callId]/route.ts` — WIRED real statement (marketLine + subgraph fallback) + settled stats (Settlement/RepEvent).
- `apps/web/app/og/duel/[challengeId]/route.ts` — WIRED DuelSettled real winner/pot/rep-deltas.
- `apps/web/lib/relayer-client.ts` — EXTENDED with `getMarketLine` + settled-field reader.
- `apps/web/tests/og-thumbnail-200px.spec.ts` — FILLED with real seeded URLs + committed 5 baseline PNGs.
