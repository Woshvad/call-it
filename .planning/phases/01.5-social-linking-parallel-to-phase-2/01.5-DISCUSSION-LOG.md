# Phase 1.5: Social linking (parallel to Phase 2) - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-29
**Phase:** 1.5-Social linking (parallel to Phase 2)
**Areas discussed:** Follow-graph source & cost scope, OAuth proof + sponsored linking flow, VERIFIED badge rollout scope, Follow-graph privacy & lifecycle

---

## Follow-graph source & cost scope

### Q: Build the 'From your X/FC' feed now, or defer behind a flag?

| Option | Description | Selected |
|--------|-------------|----------|
| Ship linking + badges; defer the feed | Linking/badges (free Privy OAuth) now; feed behind a flag until X API budget approved | |
| Build the full feed now | Commit X API Basic ($100-200/mo) + Farcaster Hub; full AUTH-14/15/18 this phase | ✓ |
| You decide | Claude's discretion | |

**User's choice:** Build the full feed now
**Notes:** User accepts the X API Basic spend — resolves the STATE.md budget blocker for this item.

### Q: Which Farcaster follow-graph data source?

| Option | Description | Selected |
|--------|-------------|----------|
| Neynar (managed Hub API) | Hosted, reliable, dev/free tier; satisfies AUTH-18 'Hub API' intent | ✓ |
| Direct Farcaster Hub | Public/self-hosted hub; more operational surface | |
| You decide | Claude's discretion | |

**User's choice:** Neynar (managed Hub API)

---

## OAuth proof + sponsored linking flow

### Q: What should the on-chain proofHash commit to?

| Option | Description | Selected |
|--------|-------------|----------|
| Structured hash of verified claims | keccak256 over (user, platform, handle, OAuth subject/id, issuedAt, nonce); no PII; auditable | ✓ |
| Hash of raw OAuth token/proof | keccak256 of opaque proof blob; harder to audit | |
| You decide | Claude's discretion | |

**User's choice:** Structured hash of verified claims

### Q: How to handle handle-uniqueness and re-linking?

| Option | Description | Selected |
|--------|-------------|----------|
| One active link per handle; reject conflicts | Off-chain uniqueness index; reject second account; re-link own different handle OK | ✓ |
| Last-proof-wins (transfer on re-verify) | Most recent proof gets it; clears prior binding; more complexity + griefing surface | |
| You decide | Claude's discretion | |

**User's choice:** One active link per handle; reject conflicts

---

## VERIFIED badge rollout scope

### Q: Which surfaces get the badge wired now?

| Option | Description | Selected |
|--------|-------------|----------|
| Existing surfaces + reusable primitive | Profile + Feed + Live Receipt now; Duel/Leaderboard get it free in Phase 3/7 | |
| All surfaces now (build stubs) | Integrate everywhere now, stubbing Duel + Leaderboard hosts | ✓ |
| You decide | Claude's discretion | |

**User's choice:** All surfaces now (build stubs)
**Notes:** Captured as minimal badge-host stubs for the not-yet-built Duel/Leaderboard pages; full page build remains Phase 3/7.

### Q: How should the frontend resolve VERIFIED status?

| Option | Description | Selected |
|--------|-------------|----------|
| Subgraph events + relayer profile enrichment | SocialLinked/SocialUnlinked indexed; single-profile reads via /api/profile; combined X·FC derived | ✓ |
| Direct contract reads | wagmi per user; N+1 in lists; more RPC | |
| You decide | Claude's discretion | |

**User's choice:** Subgraph events + relayer profile enrichment

---

## Follow-graph privacy & lifecycle

### Q: How should the follow-graph data be stored server-side?

| Option | Description | Selected |
|--------|-------------|----------|
| Redis cache only (1h TTL), no durable copy | Data-minimizing; naturally expires; re-fetch on cold | |
| Postgres durable + Redis 1h cache | Durable mapping + cache; fewer re-fetches; more privacy surface to purge | ✓ |
| You decide | Claude's discretion | |

**User's choice:** Postgres durable + Redis 1h cache
**Notes:** Durable storage raises privacy stakes — purge must clear Postgres + Redis; opt-in consent must disclose durable storage.

### Q: How should 'cleared on disconnect' be enforced?

| Option | Description | Selected |
|--------|-------------|----------|
| Settings action (immediate) + SocialUnlinked watcher (backstop) | Immediate purge endpoint + event-watch backstop | ✓ |
| SocialUnlinked watcher only | Event-only; brief window of stale cache after unlink | |
| You decide | Claude's discretion | |

**User's choice:** Settings action (immediate) + SocialUnlinked watcher (backstop)

---

## Claude's Discretion

- Handle normalization (case-folding, @-prefix) for matching.
- X API rate-limit / quota degradation (graceful empty/cached section; never block main feed).
- Subgraph schema additions for SocialLinked/SocialUnlinked (extend + redeploy alongside Phase 2).
- <VerifiedBadge> visual treatment (reuse Tag primitive).
- Relayer endpoint shapes (link, unlink-purge, from-your-x, from-your-farcaster).
- Attestation-nonce management for proofHash.

## Deferred Ideas

- Full Duel page (Phase 3) + Leaderboard page (Phase 7) — only badge-host stubs here.
- Settled Receipt badge wiring → Phase 4.
- X API Pro tier (beyond Basic scale) → v1.1.
- Notifications of new "From your X" calls → extend Phase 2 notification center later.
- Mutual-follow / 2nd-degree graph → v1.1.
