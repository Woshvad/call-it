---
phase: 01.5-social-linking-parallel-to-phase-2
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/src/primitives/VerifiedBadge.tsx
  - packages/ui/src/primitives/__tests__/VerifiedBadge.test.tsx
  - packages/ui/src/compound/ProfileHeader.tsx
  - packages/ui/src/compound/CallCard.tsx
  - packages/ui/src/compound/Receipt.tsx
  - packages/ui/src/index.ts
  - apps/relayer/test/zero-mechanical-effect.test.ts
autonomous: true
requirements: [AUTH-09, AUTH-10]
must_haves:
  truths:
    - "<VerifiedBadge> renders VERIFIED · X, VERIFIED · FC, or VERIFIED · X · FC from verifiedX/verifiedFc props, and renders nothing when both are false (AUTH-09)"
    - "The badge appears on the Profile header, Feed call card, and Live Receipt header; Duel/Leaderboard get minimal badge-host slots sufficient to render+test (D-07)"
    - "A verified user and an unverified user receive identical mechanical treatment — no rep/stake/fee/payout difference (AUTH-10, D-09) — proven by a parity test"
  artifacts:
    - path: packages/ui/src/primitives/VerifiedBadge.tsx
      provides: "Reusable verified badge primitive (reuses Tag)"
      contains: "VerifiedBadge"
    - path: packages/ui/src/primitives/__tests__/VerifiedBadge.test.tsx
      provides: "AUTH-09 variant render tests"
    - path: apps/relayer/test/zero-mechanical-effect.test.ts
      provides: "AUTH-10 verified-vs-unverified mechanical parity invariant"
  key_links:
    - from: "packages/ui/src/compound/ProfileHeader.tsx"
      to: "VerifiedBadge"
      via: "verified slot replaced with verifiedX/verifiedFc"
      pattern: "VerifiedBadge"
    - from: "packages/ui/src/compound/Receipt.tsx"
      to: "VerifiedBadge"
      via: "header badge host"
      pattern: "VerifiedBadge"
---

<objective>
Build the reusable `<VerifiedBadge>` primitive and wire it onto every AUTH-09 surface, then lock the AUTH-10 zero-mechanical-effect invariant with an explicit parity test. The verification data already flows from the subgraph (`Profile.twitterHandle`/`farcasterHandle`, indexed in Phase 1) and from `/api/profile` `verifiedX`/`verifiedFc` (enriched in 01.5-02) — this plan is the presentational layer plus the invariant guard.

Per D-07, wire the badge fully on Profile header, Feed call card, and the shared Live Receipt header (the latter directly benefits the parallel Phase 2 Live Receipt). Duel parties and Leaderboard rows are not built yet (Phase 3 / Phase 7) — give them minimal badge-host slots/props sufficient to render and test AUTH-09 now, without building the full pages (those are Deferred Ideas).

Purpose: Deliver AUTH-09 (badges on all surfaces) and AUTH-10 (zero mechanical effect, tested) — the visible identity payoff of the phase.
Output: `<VerifiedBadge>` primitive + exports, wiring on ProfileHeader/CallCard/Receipt, badge-host props for Duel/Leaderboard, and two test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01.5-social-linking-parallel-to-phase-2/01.5-CONTEXT.md
@.planning/phases/01.5-social-linking-parallel-to-phase-2/01.5-RESEARCH.md

<read_first>
- packages/ui/src/primitives/Tag.tsx — neobrutalist pill (cva, intents info/success/warning/danger, rounded-none, inline-flex). The VerifiedBadge reuses Tag (D-07 Claude's Discretion: reuse Tag, intent="warning" = brand-accent #E8F542). inline-flex (NOT grid) is load-bearing for Satori/OG (Pitfall 15 / RESEARCH anti-pattern).
- packages/ui/src/compound/ProfileHeader.tsx — `ProfileHeaderUser.verified?: boolean` slot (lines 17-18) + the inline VERIFIED span (lines 69-73). Replace with `verifiedX?`/`verifiedFc?` props rendering <VerifiedBadge>.
- packages/ui/src/compound/CallCard.tsx — feed row; `@{call.handle}` span (line 39). Add an optional `verifiedX?`/`verifiedFc?` to CallCardData and render <VerifiedBadge> next to the handle.
- packages/ui/src/compound/Receipt.tsx — shared Live Receipt; add a badge host in the header next to the caller handle (couples with Phase 2 Live Receipt, D-07).
- packages/ui/src/index.ts — barrel; export VerifiedBadge + type.
- packages/ui/src/primitives/__tests__ — does NOT exist yet; create it (Vitest + Testing Library, matching the ui package test config).
- CLAUDE.md — neobrutalist tokens (#09090E surface, #E8F542 brand-accent); design system is the source for visual treatment (no separate UI-SPEC).
</read_first>

<interfaces>
VerifiedBadge contract (create):
  export type VerifiedBadgeProps = { verifiedX?: boolean; verifiedFc?: boolean; className?: string };
  export function VerifiedBadge(props): JSX | null
    // both false → null
    // X only → "VERIFIED · X"; FC only → "VERIFIED · FC"; both → "VERIFIED · X · FC"
    // renders via <Tag intent="warning" data-testid="verified-badge">

Existing data source (no new fetch needed in UI):
  /api/profile → { verifiedX: boolean; verifiedFc: boolean }  (enriched in 01.5-02)
  subgraph Profile → { twitterHandle, farcasterHandle }       (indexed Phase 1)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: VerifiedBadge primitive + variant render tests + barrel export</name>
  <files>packages/ui/src/primitives/VerifiedBadge.tsx, packages/ui/src/primitives/__tests__/VerifiedBadge.test.tsx, packages/ui/src/index.ts</files>
  <behavior>
    - verifiedX && verifiedFc → renders text "VERIFIED · X · FC"
    - verifiedX only → "VERIFIED · X"
    - verifiedFc only → "VERIFIED · FC"
    - both false/undefined → renders null (component returns null; nothing in the DOM)
    - renders inside a Tag with data-testid="verified-badge" (inline-flex, never grid)
  </behavior>
  <action>
Create `packages/ui/src/primitives/VerifiedBadge.tsx` per the interfaces contract, importing `Tag` from './Tag'. Use `intent="warning"` (brand-accent). Build the label from the two flags. Return `null` when neither flag is set. Forward `className` and `data-testid="verified-badge"`.
Export `VerifiedBadge` + `VerifiedBadgeProps` from `packages/ui/src/index.ts` (alongside the other primitive exports; update the header comment listing primitives).
Create `packages/ui/src/primitives/__tests__/VerifiedBadge.test.tsx` covering the 4 cases above with Testing Library `render` + `screen.queryByTestId`/text assertions.
  </action>
  <verify>
    <automated>pnpm --filter @call-it/ui test VerifiedBadge</automated>
  </verify>
  <done>VerifiedBadge.test.tsx passes all 4 variant cases; VerifiedBadge exported from the @call-it/ui barrel.</done>
</task>

<task type="auto">
  <name>Task 2: Wire VerifiedBadge onto Profile, Feed card, Receipt; add badge-host props for Duel/Leaderboard</name>
  <files>packages/ui/src/compound/ProfileHeader.tsx, packages/ui/src/compound/CallCard.tsx, packages/ui/src/compound/Receipt.tsx</files>
  <action>
ProfileHeader: extend `ProfileHeaderUser` with `verifiedX?: boolean; verifiedFc?: boolean` (keep `verified?` for back-compat or map it to verifiedX). Replace the inline VERIFIED span (lines 69-73) with `<VerifiedBadge verifiedX={user.verifiedX} verifiedFc={user.verifiedFc} />`. Import VerifiedBadge from '../primitives/VerifiedBadge'.
CallCard: add `verifiedX?: boolean; verifiedFc?: boolean` to `CallCardData`; render `<VerifiedBadge ... />` next to the `@{call.handle}` span (line 39) in the top row. Keep flexbox layout.
Receipt: add an optional `verifiedX?`/`verifiedFc?` to the receipt's caller/header data and render `<VerifiedBadge ... />` in the header next to the caller handle (the badge host that Phase 2's Live Receipt and Phase 4's Settled Receipt will populate — D-07; Settled wiring itself is deferred to Phase 4 per the Deferred Ideas, the host is ready now).
Duel/Leaderboard badge-host stubs (D-07): the full pages are out of scope (Phase 3/7). Provide the prop seam only — if a lightweight DuelParty/LeaderboardRow presentational type or component already exists in packages/ui, add `verifiedX?`/`verifiedFc?` and render the badge; if not, leave a typed `verifiedX?`/`verifiedFc?` field on the relevant exported data type with a short comment "// AUTH-09 badge host — full page Phase 3/7". Do NOT build the Duel or Leaderboard page.
All edits must keep inline-flex/flex (no grid) for Satori/OG compatibility.
  </action>
  <verify>
    <automated>pnpm --filter @call-it/ui build && pnpm --filter @call-it/ui test</automated>
  </verify>
  <done>VerifiedBadge imported + rendered in ProfileHeader, CallCard, and Receipt; verifiedX/verifiedFc props exist on the relevant exported data types; ui package builds + tests green; no grid introduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AUTH-10 zero-mechanical-effect parity invariant test</name>
  <files>apps/relayer/test/zero-mechanical-effect.test.ts</files>
  <behavior>
    - Construct two otherwise-identical user contexts that differ ONLY in verification status (one verifiedX/verifiedFc true, one both false).
    - Assert the mechanical surfaces are identical: stake-limit inputs ($5 min / $100 max / $1 min follow-fade — fees.ts constants), fee computation (1.0%+0.4%+0.3% settlement), and any rep-delta input are byte-identical regardless of verification (D-09).
    - The verification flags must not appear anywhere in the mechanical code paths (grep-style or input-equality assertion).
  </behavior>
  <action>
Create `apps/relayer/test/zero-mechanical-effect.test.ts` (Vitest). Because verification is purely presentational (it lives in /api/profile + subgraph handles, never in CallRegistry/FollowFadeMarket/SettlementManager/fees), the parity test asserts that the mechanical inputs/outputs (import from packages/shared/src/constants/fees.ts + the relayer's calls-preflight gate inputs) are computed without reading any verifiedX/verifiedFc field. Implement as: build a `mechanicalInputsFor({ verifiedX, verifiedFc, ...sharedFields })` helper inline in the test that runs the actual preflight/fee constants and assert `mechanicalInputsFor({verifiedX:true,verifiedFc:true,...}) deepEquals mechanicalInputsFor({verifiedX:false,verifiedFc:false,...})`. Also assert (static guard) that the social-link route module and profile mechanical path contain no rep/stake/fee write keyed on verification. Document the invariant in the test header referencing AUTH-10 / D-09 (mirrors Phase 1's parity-gate discipline).
  </action>
  <verify>
    <automated>pnpm --filter @call-it/relayer test zero-mechanical-effect</automated>
  </verify>
  <done>zero-mechanical-effect.test.ts passes: verified and unverified users yield identical mechanical treatment; the invariant is documented and enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification flag → mechanical code | Verification must never cross into rep/stake/fee/payout logic (AUTH-10) |
| badge → OG/Satori render | Badge may flow into an OG card; must stay flexbox (Pitfall 15) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.5-03-01 | Tampering | Verification affecting mechanics | mitigate | AUTH-10 parity test asserts identical mechanical treatment (D-09) |
| T-01.5-03-02 | Tampering | Badge layout in OG | mitigate | VerifiedBadge uses Tag inline-flex; no grid (Pitfall 15) |
| T-01.5-03-03 | Tampering | npm installs | accept | No new packages in this plan (UI uses existing Tag; relayer test uses existing deps) |
</threat_model>

<verification>
- `pnpm --filter @call-it/ui test` and `pnpm --filter @call-it/ui build` green.
- `pnpm --filter @call-it/relayer test zero-mechanical-effect` green.
- No `display: grid` / `display:'grid'` introduced in the three compound components.
</verification>

<success_criteria>
- VerifiedBadge renders all 3 variants + null state; wired on Profile, Feed card, Receipt; badge-host seams present for Duel/Leaderboard.
- AUTH-10 invariant is an explicit passing test.
</success_criteria>

<output>
Create `.planning/phases/01.5-social-linking-parallel-to-phase-2/01.5-03-SUMMARY.md` when done.
</output>
