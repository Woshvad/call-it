---
phase: 01.5-social-linking-parallel-to-phase-2
verified: 2026-06-07T15:20:00Z
status: passed
score: 4/4 success criteria verified (11/11 requirements resolved — 8 PASS, 3 deferred-as-documented)
mode: mvp
overrides_applied: 0
re_verification: false
deferred:
  - truth: "AUTH-14 — live Twitter follow-graph data (X API cross-reference produces real items)"
    addressed_in: "v1 operator follow-up (key provisioning); activates with no code change once X_API_BEARER_TOKEN is set"
    evidence: "01.5-05 closed complete-with-documented-deferral; x-api-client.ts:73-79 degrades to empty on missing key; feed route returns { items: [], source: 'empty' }"
  - truth: "Live 'From your Farcaster' data (Neynar cross-reference produces real items)"
    addressed_in: "v1 operator follow-up; activates with no code change once NEYNAR_API_KEY is set"
    evidence: "neynar-client.ts degrades to empty on missing key; feed-from-your-fc.ts returns EMPTY on error"
  - truth: "Deployed Fly relayer secret RELAYER_OAUTH_PROOF_ADDRESS on call-it-relayer-sepolia"
    addressed_in: "Phase-6 soak operator window (deliberate non-gating follow-up); LOCAL .env.local is set"
    evidence: "01.5-01-SUMMARY Operator/Runtime Notes; on-chain relayer() already cleared so live linking is unblocked once the deployed secret is set"
notes:
  - "Phase mode is mvp but the ROADMAP goal is NOT in User-Story format (\"As a..., I want to..., so that...\"). Per references/verify-mvp-mode.md the verifier should flag this. Verification was performed goal-backward against the 4 Success Criteria + 11 AUTH requirements (the contract), which fully cover the user-facing outcomes. Recommend running /gsd mvp-phase 1.5 to reformat the goal into User-Story form for a future UAT pass, but this does not block the phase — the success criteria are observably delivered."
  - "follow-graph-optin durable relayer route (/api/social/follow-graph-optin) does NOT exist; the web client POSTs to it best-effort and the AUTH-16 render gate runs entirely off localStorage. This is a documented forward-compatible stub (01.5-04 key-decisions), not a phase blocker — the load-bearing render gate is fully functional and tested."
---

# Phase 1.5: Social linking (parallel to Phase 2) — Verification Report

**Phase Goal:** Optional Twitter/Farcaster linking enriches identity so VERIFIED · X / · FC / · X · FC badges land on every surface before FollowFadeMarket goes live; Twitter follow-graph powers "From your X", Farcaster mirrors as "From your Farcaster"; zero mechanical effect on rep, stake limits, fees, or payouts.
**Verified:** 2026-06-07T15:20:00Z
**Status:** passed (with 3 documented/accepted deferrals tracked under Accepted Gaps)
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| # | Truth (Success Criterion) | Status | Evidence |
|---|---------------------------|--------|----------|
| SC-1 | User can optionally link Twitter (Privy OAuth proof) and/or Farcaster (Auth Kit proof); relayer verifies server-side and submits `linkTwitter`/`linkFarcaster`; gas sponsored by protocol | ✓ VERIFIED | On-chain `relayer()` on canonical PR `0xF66C0AFE` == oauth-proof KMS `0xdFc80922…` (was `0x0`) — linking no longer reverts `NotRelayer`. Real submission path: `social-link.ts:160-205` extracts the Privy-verified handle, computes proofHash, submits `linkTwitter` from the KMS wallet (`oauth-proof-submitter.ts`). Farcaster via SIWF `verifyFarcasterProof` (`social-link.ts:208-249`). Gas paid by relayer KMS wallet (D-03; funded 0.05 ETH). Owner unchanged at treasury `0xDa8c5726`. |
| SC-2 | VERIFIED · X / · FC / · X · FC badge renders next to handle on Profile, Feed card, Live + Settled Receipt headers, Duel parties, Leaderboard rows; zero mechanical effect (cross-checked in test) | ✓ VERIFIED (Duel/Leaderboard via prop seam; Settled badge data Phase 4) | `VerifiedBadge.tsx:55-70` derives all 3 label variants, returns null when both flags false; wired on `ProfileHeader.tsx`, `CallCard.tsx`, `Receipt.tsx` (UI test 60/60 incl. 7 badge variant tests). Duel (Phase 3) / Leaderboard (Phase 7) full pages out of scope per D-07 — delivered as the typed `VerifiedBadgeHost` seam. AUTH-10 parity test (`zero-mechanical-effect.test.ts`, 7/7) runs real fee/stake/conviction/rep-delta math, asserts verified==unverified deep-equal, + static guard that mechanical modules contain no `verifiedX`/`verifiedFc`. |
| SC-3 | "From your X" shows ≤10 (active + active duels, excludes settled), recency order, refreshed on open with 1h cache; "From your Farcaster" mirrors with collapse toggles; explicit opt-in shown in onboarding and never bypassed | ✓ VERIFIED (live data deferred — see Accepted Gaps) | Query enforces rules at source: `subgraph-client.ts:184-201` `orderBy: createdAt, orderDirection: desc`, `status_not_in: EXCLUDED_FEED_STATUSES` (settled/draft), `first: $first`; cap `FROM_YOUR_NETWORK_MAX = 10` (`follow-graph.ts:281,362`). Duels NOT excluded (active duels included). 1h Redis cache `follow:{x\|fc}:{id}`. `FromYourNetworkSections.tsx` renders both sections simultaneously (AUTH-18) with independent collapse toggles (`xCollapsed`/`fcCollapsed`, `aria-expanded`), gated by `shouldRenderFollowGraphSection` (AUTH-16). Opt-in screen + D-14 consent wired in `onboarding/follow-graph/page.tsx`. Live X/Neynar data dormant (no keys, by operator decision) — degrades to empty. |
| SC-4 | User can unlink Twitter/Farcaster directly (no relayer) via Settings; badge + handle reference removed; onchain history retained; follow-graph data server-side only, viewer-only, cleared on disconnect | ✓ VERIFIED | On-chain user-callable `unlinkTwitter()`/`unlinkFarcaster()` (`ProfileRegistry.sol:168-182`, no relayer auth, emits `SocialUnlinked`). Settings wires wagmi `writeContractAsync(unlink*)` + relayer `/api/social/unlink-purge` (`SocialLinkControls.tsx`). Badge removal chain: unlink → subgraph drops handle → `/api/profile` `verifiedX/verifiedFc` flip false (`profile.ts:183-204`) → `VerifiedBadge` returns null. Dual-store purge (Postgres `follow_graph` + Redis) immediate + `SocialUnlinked` watcher backstop (`social-unlink-watcher.ts`). Follow-graph keyed by viewer `privyUserId`, no target-user param (AUTH-17). |

**Score:** 4/4 success criteria verified

### Per-Requirement Verdicts

| Req | Description | Verdict | Evidence |
|-----|-------------|---------|----------|
| AUTH-06 | Link Twitter via OAuth → relayer-mediated `linkTwitter` | ✓ PASS | `social-link.ts:153-206` (Privy-verified handle → KMS `linkTwitter`); contract `ProfileRegistry.sol:187-199` (`NotRelayer` gate); on-chain `relayer()` set. |
| AUTH-07 | Link Farcaster via Auth Kit → relayer-mediated `linkFarcaster` | ✓ PASS | `farcaster-proof.ts` SIWF verify (nonce+domain enforced); `social-link.ts:208-249`; `AuthKitProvider` below PrivyProvider (`Providers.tsx:91`, AST test 6/6). |
| AUTH-09 | VERIFIED badges render across surfaces | ✓ PASS | `VerifiedBadge.tsx`; wired ProfileHeader/CallCard/Receipt; Duel/Leaderboard prop seam (D-07, Phase 3/7). UI test 60/60. |
| AUTH-10 | Zero mechanical effect (no rep/stake/fee/payout) | ✓ PASS | `zero-mechanical-effect.test.ts` 7/7 — real-math parity deep-equal + static source guard on fees.ts/call-gates.ts/follow-fade-gates.ts. |
| AUTH-12 | Unlink via Settings; removes badge/handle; retains onchain history | ✓ PASS | `ProfileRegistry.sol:168-182` (delete handle/proofHash/linkedAt only — Profile/rep untouched); `SocialLinkControls.tsx` settings unlink + purge. |
| AUTH-13 | `SocialIdentity` stored, 50-byte handle cap | ✓ PASS | `ProfileRegistry.sol:73-80` struct (all 6 fields); `MAX_HANDLE_LENGTH=50` (line 37) enforced at link (193/208) + relayer (`social-link.ts:163`). |
| AUTH-14 | Twitter follow-graph queries X API + cross-references linked handles | ◑ DEFERRED (code complete, live data dormant) | `x-api-client.ts` + `follow-graph.ts crossReference` (D-11) built + tested with mocks; live path dormant until `X_API_BEARER_TOKEN` set. See Accepted Gaps. |
| AUTH-15 | "From your X" ≤10, recency, active + active duels, excludes settled, 1h cache | ✓ PASS | `subgraph-client.ts:184-201` + `follow-graph.ts:281,330-371`; `from-your-x.test.ts` 9 tests (cap, recency, active-duel, exclude-settled, degrade). |
| AUTH-16 | Onboarding opt-in (Yes/No); declined never shows | ✓ PASS | `shouldRenderFollowGraphSection` returns true ONLY on explicit `=== true` (`follow-graph-preference.ts:51-57`); `follow-graph-optin.test.ts` 11/11. |
| AUTH-17 | Follow-graph server-side only, viewer-only, cleared on disconnect | ✓ PASS | `getFollowGraph` keyed by `privyUserId` only (no target param); dual-store purge + `SocialUnlinked` watcher; routes session-gated. |
| AUTH-18 | Farcaster mirror via Hub API; both simultaneous with collapse | ◑ DEFERRED (UI/mirror complete, live Neynar data dormant) | `neynar-client.ts` + `feed-from-your-fc.ts`; `FromYourNetworkSections.tsx` both-simultaneous + independent collapse. Live data dormant until `NEYNAR_API_KEY` set. See Accepted Gaps. |

### Required Artifacts

| Artifact | Provides | Status | Details |
|----------|----------|--------|---------|
| `packages/contracts/src/ProfileRegistry.sol` | Social surface (link/unlink/setRelayer/SocialIdentity/events) | ✓ VERIFIED | All functions present + deployed; on-chain `relayer()` set. |
| `apps/relayer/src/routes/social-link.ts` | Link/farcaster-link/unlink-purge routes | ✓ VERIFIED | 359 lines; real KMS submission, no stubs. Registered `index.ts:187`. |
| `apps/relayer/src/lib/oauth-proof-submitter.ts` | KMS raw-tx submit | ✓ VERIFIED | 142 lines; serialize→signDigest→sendRaw. |
| `apps/relayer/src/lib/twitter-proof.ts` / `farcaster-proof.ts` | Server-side proof verify | ✓ VERIFIED | Privy-verified handle (never body); SIWF nonce+domain. |
| `apps/relayer/src/lib/social-link-index.ts` | D-06 one-active-link uniqueness | ✓ VERIFIED | 150 lines; 409 on conflict + reactivate-on-relink. |
| `apps/relayer/src/workers/social-unlink-watcher.ts` | AUTH-17 backstop purge | ✓ VERIFIED | 302 lines; chunked getLogs, dual-store purge. Started `index.ts:296`. |
| `apps/relayer/src/lib/x-api-client.ts` / `neynar-client.ts` | Follow-graph fetch (degrade-to-empty) | ✓ VERIFIED | Typed `QuotaError` → empty on missing key / 4xx / 429. |
| `apps/relayer/src/lib/follow-graph.ts` | Viewer-only fetch/cache/cross-ref/assembly | ✓ VERIFIED | 372 lines; every failure path → `{ items: [], source: 'empty' }`. |
| `apps/relayer/src/routes/feed-from-your-x.ts` / `feed-from-your-fc.ts` | Feed section routes | ✓ VERIFIED | Session-gated, viewer-only, EMPTY on error. Registered `index.ts:192-193`. |
| `packages/ui/src/primitives/VerifiedBadge.tsx` | AUTH-09 badge primitive | ✓ VERIFIED | Null when both false; reuses Tag inline-flex (Satori-safe). |
| `apps/web/app/components/SocialLinkControls.tsx` | Link/unlink UI (onboarding + settings) | ✓ VERIFIED | 380 lines; real Privy/Auth Kit flows + on-chain unlink + purge. |
| `apps/web/lib/follow-graph-preference.ts` | AUTH-16 render gate + D-14 consent | ✓ VERIFIED | Pure render gate (`=== true` only); consent copy discloses durable/viewer-only/cleared. |
| `apps/web/app/components/FromYourNetworkSections.tsx` | Collapsible feed sections | ✓ VERIFIED | 296 lines; both simultaneous + collapse + AUTH-16 gate. Mounted `page.tsx:835`. |
| `apps/relayer/test/zero-mechanical-effect.test.ts` | AUTH-10 parity invariant | ✓ VERIFIED | 181 lines; real-math deep-equal + static guard. Non-vacuous. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `SocialLinkControls.tsx` | `/api/social/link[/farcaster]`, `/unlink-purge` | fetch + Privy bearer | ✓ WIRED | Onboarding + settings POST flows. |
| `Providers.tsx` | `AuthKitProvider` (below PrivyProvider) | innermost wrapper | ✓ WIRED | AST provider-order test 6/6 green. |
| `social-link.ts` | `ProfileRegistry.linkTwitter/linkFarcaster` (on-chain) | KMS oauth-proof submitter | ✓ WIRED | `relayer()` on-chain == KMS wallet; no longer `NotRelayer`. |
| `follow-graph.ts` | subgraph `Profile.twitterHandle`/`farcasterHandle` | crossReference (D-11) | ✓ WIRED | `queryLinkedProfilesByHandles`; normalized handle match. |
| `FromYourNetworkSections.tsx` | `/api/feed/from-your-x`, `/from-your-farcaster` | fetch on open | ✓ WIRED | Mounted at top of Live tab. |
| `/api/profile` | subgraph `queryProfileSocials` | `verifiedX/verifiedFc` derivation | ✓ WIRED | Flips false on unlink; degrades false on subgraph failure. |
| web client | `/api/social/follow-graph-optin` (durable opt-in) | best-effort POST | ⚠ PARTIAL | Relayer route NOT implemented; localStorage render gate is load-bearing + tested. Documented forward-compatible stub (non-blocking). |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `VerifiedBadge` (on surfaces) | verifiedX/verifiedFc | subgraph handles via `/api/profile` + caller props | ✓ Yes (real on-chain links flow through subgraph) | ✓ FLOWING |
| `FromYourNetworkSections` | xData/fcData items | `/api/feed/from-your-x[fc]` → X API / Neynar | ✗ Dormant (no API keys by operator decision) | ⚠ STATIC-by-design — degrades to quiet empty; activates with keys, no code change |

The only non-flowing data path is the live follow-graph (AUTH-14 / live "From your Farcaster"), which is an explicit, accepted operator deferral — see Accepted Gaps. The degrade-to-empty path is verified safe (never throws into the main feed).

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| On-chain relayer gate cleared | `cast call 0xF66C0AFE… "relayer()(address)"` | `0xdFc80922FAbc51a08350c0b371917e6EaB8b550A` | ✓ PASS |
| On-chain owner unchanged | `cast call 0xF66C0AFE… "owner()(address)"` | `0xDa8c5726f596E8dae99e6dDEBa8AEa1c8bE9A4a5` (treasury) | ✓ PASS |
| Relayer suite | `pnpm --filter @call-it/relayer test` | 189 passed / 1 skipped (GCP-gated kms-roundtrip) | ✓ PASS |
| UI suite (incl. VerifiedBadge) | `pnpm --filter @call-it/ui test` | 60/60 passed | ✓ PASS |
| Web suite (AUTH-16 + provider order) | `pnpm --filter @call-it/web test` | 51/51 passed (incl. 11 opt-in, 6 provider-order) | ✓ PASS |
| Relayer build | `pnpm --filter @call-it/relayer build` | exit 0 | ✓ PASS |
| Web build | `pnpm --filter @call-it/web build` | exit 0 | ✓ PASS |

### Requirements Coverage

All 11 declared requirements (AUTH-06/07/09/10/12/13/14/15/16/17/18) are claimed across the 5 plans and accounted for. 8 fully PASS; AUTH-14 and AUTH-18's live-data path are deferred-as-documented (code complete + tested with mocks). No orphaned requirements (every AUTH-id maps to at least one plan's `requirements-completed`/`requirements-deferred`).

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (none) | — | No TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER/"coming soon" in any phase source file | — | Clean — prior `void optIn` / "Coming soon" Farcaster / "TODO Phase 1.5" stubs all removed as claimed. |

### Disconfirmation Pass (Confirmation Bias Counter)

1. **Partially-met requirement:** AUTH-14/AUTH-18 live data — code complete but live cross-reference produces no real items without provisioned keys. Correctly reported as DEFERRED (operator decision, documented in 01.5-05), with verified safe degrade-to-empty.
2. **Test that passes but may not test the stated behavior:** Examined `zero-mechanical-effect.test.ts` — it is NON-vacuous (asserts concrete non-zero fee values `500000`/`200000`/`150000`/`850000` and a real negative rep-delta before asserting parity), so the deep-equal is meaningful, not an empty `{}=={}`.
3. **Uncovered path:** The durable server-side opt-in route (`/api/social/follow-graph-optin`) does not exist; the web client POSTs best-effort to a non-existent endpoint. The AUTH-16 guarantee does NOT depend on it (the localStorage render gate is load-bearing and tested). Reported as a non-blocking documented stub.

### Accepted Gaps (documented operator deferrals — tracked, NOT phase failures)

| Gap | Type | Why accepted | Activation |
|-----|------|--------------|------------|
| **AUTH-14 live "From your X" data** | Deferred (operator decision) | `X_API_BEARER_TOKEN` intentionally NOT provisioned for v1 (01.5-05 complete-with-documented-deferral). X API pricing changed 2026-02-06. | Set `X_API_BEARER_TOKEN` (follows.read) → live, no code change. Verified: section degrades to quiet empty, never throws into main feed. |
| **Live "From your Farcaster" data** | Deferred (operator decision) | `NEYNAR_API_KEY` intentionally NOT provisioned for v1. | Set `NEYNAR_API_KEY` → live, no code change. Same safe degrade verified. |
| **Deployed Fly relayer secret `RELAYER_OAUTH_PROOF_ADDRESS`** | Deferred (deliberate, timed around Phase-6 soak) | Setting it restarts the relayer mid-soak. LOCAL `.env.local` is set; on-chain `relayer()` already cleared. | `fly secrets set RELAYER_OAUTH_PROOF_ADDRESS=0xdFc80922… -a call-it-relayer-sepolia`. |
| **`follow_graph` Postgres migration `0005_warm_ego.sql` applied to deployed relayer DB** | Operator runtime step | Local DB migrated (per session facts); deployed DB needs `db:migrate`. | Apply migration → cross-session follow-graph persistence (until then Redis-only, degrades to empty on miss). |
| **Durable `/api/social/follow-graph-optin` relayer route** | Forward-compatible stub | AUTH-16 render gate runs off localStorage (load-bearing + tested); the durable route was explicitly deferred as forward-compatible in 01.5-04. | Implement the route (best-effort POST already wired client-side; no breaking change). |

### Human Verification Required

Phase mode is `mvp` but the ROADMAP goal is NOT in User-Story format. No interactive UAT walk-through was generated for this verification (goal-backward was performed against the 4 Success Criteria + 11 requirements, which fully cover the user-facing outcomes). For a future end-to-end UAT, recommend `/gsd mvp-phase 1.5` to reformat the goal into "As a..., I want to..., so that..." form. This is a process recommendation, not a phase blocker — no FAIL.

### Gaps Summary

**No blocking gaps.** All 4 ROADMAP success criteria are observably delivered in code and on-chain. The single hard gate (`setRelayer` on the canonical ProfileRegistry) is CLEARED on-chain — `relayer()` returns the oauth-proof KMS wallet, unblocking real `linkTwitter`/`linkFarcaster` submissions. The VERIFIED badge primitive, AUTH-10 zero-mechanical-effect invariant, link/unlink flows, opt-in render gate, and feed sections are all built, wired, and tested (relayer 189/1-skip, UI 60/60, web 51/51; relayer + web + UI builds exit 0). The only items not live are the X API / Neynar follow-graph DATA paths and the deployed Fly secret — all explicit, accepted operator deferrals with verified safe degrade-to-empty behavior and zero-code-change activation. The phase goal is achieved.

---

_Verified: 2026-06-07T15:20:00Z_
_Verifier: Claude (gsd-verifier)_
