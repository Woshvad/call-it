---
phase: 01.5-social-linking-parallel-to-phase-2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/schemas/env-config.ts
  - apps/relayer/src/types.ts
  - apps/relayer/src/lib/secret-manager.ts
autonomous: false
requirements: [AUTH-06, AUTH-07, AUTH-13]
must_haves:
  truths:
    - "ProfileRegistry.relayer() on the canonical Sepolia PR (0xF66C0AFEf03b43338FC5aE282e45C0Cf6A3c4820) returns the relayer KMS oauth-proof address, not address(0) (per D-01/D-02)"
    - "A linkTwitter/linkFarcaster call from the relayer KMS address no longer reverts NotRelayer"
    - "RELAYER_OAUTH_PROOF_ADDRESS is a validated relayer env var consumed by the link service in 01.5-02"
  artifacts:
    - path: packages/shared/src/schemas/env-config.ts
      provides: "Validated social-linking env vars (RELAYER_OAUTH_PROOF_ADDRESS, X_API_*, NEYNAR_API_KEY, FARCASTER_RELAY_URL, FARCASTER_AUTH_DOMAIN)"
      contains: "RELAYER_OAUTH_PROOF_ADDRESS"
    - path: apps/relayer/src/types.ts
      provides: "RelayerEnv fields for the oauth-proof KMS address + social-feed secrets"
      contains: "RELAYER_OAUTH_PROOF_ADDRESS"
  key_links:
    - from: "ProfileRegistry.relayer"
      to: "relayer KMS oauth-proof address"
      via: "owner setRelayer tx from treasury 0xDa8c5726"
      pattern: "setRelayer"
---

<objective>
Authorize the relayer on the canonical deployed ProfileRegistry and add the social-linking environment surface. This is the single hard gate for the entire CORE wave: until `setRelayer(relayerKmsAddress)` is called on the canonical PR, every `linkTwitter`/`linkFarcaster` reverts `NotRelayer` (RESEARCH Runtime State Inventory + Pitfall 1).

Per D-01/D-02 there is NO new contract code — the social surface (`linkTwitter`/`linkFarcaster`/`unlink*`/`SocialIdentity`/`relayer`/`setRelayer`/events) is already deployed and verified in `packages/contracts/src/ProfileRegistry.sol`. This plan (a) extends the relayer/shared env schema with the oauth-proof KMS address + the FEED-wave secret placeholders, and (b) drives the owner-gated `setRelayer` tx as a blocking operator checkpoint (treasury key 0xDa8c5726 — not autonomous).

Purpose: Unblock all CORE linking (01.5-02/03/04) by making the deployed contract trust the relayer KMS wallet, and give the relayer a validated config surface for proofs and feeds.
Output: env schema + RelayerEnv + secret-manager mappings extended; `setRelayer` executed and verified on-chain on the canonical PR.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.5-social-linking-parallel-to-phase-2/01.5-CONTEXT.md
@.planning/phases/01.5-social-linking-parallel-to-phase-2/01.5-RESEARCH.md

<read_first>
- packages/contracts/src/ProfileRegistry.sol — `setRelayer(address)` (lines 125-128, onlyOwner) + `relayer` slot (line 102, `address(0)` at deploy) + `NotRelayer` gate on linkTwitter (line 192) / linkFarcaster (line 207). DO NOT modify this file (D-01).
- packages/shared/src/constants/addresses.ts — `PROFILE_REGISTRY_ARBITRUM_SEPOLIA` = `0xF66C0AFEf03b43338FC5aE282e45C0Cf6A3c4820` (canonical, owner = treasury `0xDa8c5726f596E8dae99e6dDEBa8AEa1c8bE9A4a5`). This is the address `setRelayer` must target (Pitfall 1 — never the pre-redeploy PR).
- packages/shared/src/schemas/env-config.ts — existing zod `EnvConfigSchema` (ends at ENS_MAINNET_RPC_URL); add new fields following the existing `.optional()` + regex patterns.
- apps/relayer/src/types.ts — `RelayerEnv` interface (the loadSecrets() return shape).
- apps/relayer/src/lib/secret-manager.ts — `loadSecrets()` return object (lines 207-254); `GCP_KEY_VERSION_OAUTH_PROOF` already mapped (line 217), `GCP_PROJECT_ID`/`GCP_LOCATION_ID`/`GCP_KEYRING_ID` present (lines 208-210).
- apps/relayer/src/lib/kms-signer.ts — `gcpKmsAccount({ keyId: 'oauth-proof', ... expectedAddress })` + `verifyKmsAddress()`; the `oauth-proof` AttestationType already exists (lines 43-48).
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend shared + relayer env schema for social linking + FEED secrets</name>
  <files>packages/shared/src/schemas/env-config.ts, apps/relayer/src/types.ts, apps/relayer/src/lib/secret-manager.ts</files>
  <action>
Add the Phase 1.5 social-linking env surface (Claude's Discretion on exact var names; use the names below for downstream consistency). In `packages/shared/src/schemas/env-config.ts`, append to `EnvConfigSchema.object({...})`, all `.optional()` (the relayer runs in CI/dev without them):
- `RELAYER_OAUTH_PROOF_ADDRESS` — `z.string().regex(/^0x[0-9a-fA-F]{40}$/)` — the expected Ethereum address derived from the GCP `oauth-proof` KMS key (passed as `expectedAddress` to `gcpKmsAccount` in 01.5-02; verified by `verifyKmsAddress` at boot, per RESEARCH Q2).
- `FARCASTER_RELAY_URL` — `z.string().url()` — default documented as `https://relay.farcaster.xyz` (used by `@farcaster/auth-client` in 01.5-02).
- `FARCASTER_AUTH_DOMAIN` — `z.string().min(1)` — the AuthKitProvider domain asserted in `verifySignInMessage` (Pitfall 3; must equal the frontend AuthKitProvider domain set in 01.5-04).
- FEED-wave (01.5-05) placeholders, ALL optional so CORE never depends on them: `X_API_BEARER_TOKEN` (`z.string().min(1).optional()`), `NEYNAR_API_KEY` (`z.string().min(1).optional()`).
Do NOT add a mainnet superRefine for these (they are network-agnostic). Mirror each new field into the `RelayerEnv` interface in `apps/relayer/src/types.ts`, and into the `loadSecrets()` return object in `apps/relayer/src/lib/secret-manager.ts` reading from `process.env` with the same names (the X_API_/NEYNAR keys are GCP-Secret-Manager-held in production per the Security Domain table; in dev they fall back to process.env). Reference the existing PRIVY_APP_SECRET / ENS_MAINNET_RPC_URL handling as the pattern.
  </action>
  <verify>
    <automated>pnpm --filter @call-it/shared build && pnpm --filter @call-it/relayer build</automated>
  </verify>
  <done>Both packages typecheck/build with the new optional fields; `RELAYER_OAUTH_PROOF_ADDRESS`, `FARCASTER_RELAY_URL`, `FARCASTER_AUTH_DOMAIN`, `X_API_BEARER_TOKEN`, `NEYNAR_API_KEY` exist in EnvConfigSchema, RelayerEnv, and the loadSecrets() return.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Task 1 added the relayer/shared env surface (including `RELAYER_OAUTH_PROOF_ADDRESS`). The ProfileRegistry social surface is already deployed; no contract code changed (D-01). What remains is the single owner-gated authorization that no autonomous agent can perform: calling `setRelayer(relayerKmsAddress)` on the canonical ProfileRegistry from the treasury owner key.
  </what-built>
  <how-to-verify>
This is an operator action gated on the owner key + the relayer's oauth-proof KMS address. Steps:

1. Confirm the relayer's `oauth-proof` GCP KMS key is provisioned and record its Ethereum address (RESEARCH Q2). Derive/verify it, e.g. with the relayer's `verifyKmsAddress({ keyId: 'oauth-proof', projectId: 'call-it-sepolia', locationId: 'us-east1', keyRingId: 'attestations', keyVersion: <GCP_KEY_VERSION_OAUTH_PROOF>, expectedAddress: <candidate> })`. Set `RELAYER_OAUTH_PROOF_ADDRESS` (GCP Secret Manager / Fly secret) to this address.
2. Ensure that KMS wallet is funded with Arbitrum Sepolia ETH (gas for linkTwitter/linkFarcaster — D-03 relayer-pays).
3. From the treasury owner key `0xDa8c5726f596E8dae99e6dDEBa8AEa1c8bE9A4a5`, call `setRelayer(<RELAYER_OAUTH_PROOF_ADDRESS>)` on the canonical ProfileRegistry `0xF66C0AFEf03b43338FC5aE282e45C0Cf6A3c4820` (Arbitrum Sepolia). Example:
   `cast send 0xF66C0AFEf03b43338FC5aE282e45C0Cf6A3c4820 "setRelayer(address)" <RELAYER_OAUTH_PROOF_ADDRESS> --rpc-url <sepolia> --account <treasury-key>`
4. Verify on-chain: `cast call 0xF66C0AFEf03b43338FC5aE282e45C0Cf6A3c4820 "relayer()(address)" --rpc-url <sepolia>` returns the relayer address (NOT 0x0000…0000).
5. Confirm the relayer env (`RELAYER_OAUTH_PROOF_ADDRESS`) on Fly matches the address you set on-chain (drift here = NotRelayer at link time, Pitfall 1).
  </how-to-verify>
  <resume-signal>Type "approved" with the relayer() readback address, or describe the blocker (KMS key missing / wallet unfunded / owner-key access).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → ProfileRegistry | Owner key 0xDa8c5726 authorizes the relayer; a wrong address here grants linking power to the wrong wallet |
| relayer env → KMS | RELAYER_OAUTH_PROOF_ADDRESS must equal the key's derived address; drift breaks signing or, worse, authorizes a stale wallet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.5-01-01 | Elevation of Privilege | setRelayer target | mitigate | setRelayer targets only the canonical PR 0xF66C0AFE…; checkpoint readback (step 4) proves relayer() == intended KMS address (Pitfall 1) |
| T-01.5-01-02 | Spoofing | RELAYER_OAUTH_PROOF_ADDRESS | mitigate | expectedAddress verified via verifyKmsAddress() at relayer boot (T-00-17); env must match on-chain value (step 5) |
| T-01.5-01-03 | Tampering | npm/pip/cargo installs | mitigate | No installs in this plan; FEED-wave installs gated in 01.5-05 |
</threat_model>

<verification>
- `pnpm --filter @call-it/shared build` and `pnpm --filter @call-it/relayer build` exit 0.
- On-chain: `relayer()` on `0xF66C0AFEf03b43338FC5aE282e45C0Cf6A3c4820` returns the relayer KMS address (operator checkpoint readback).
</verification>

<success_criteria>
- New optional env fields present in EnvConfigSchema + RelayerEnv + loadSecrets() and both packages build.
- ProfileRegistry `relayer` slot on the canonical PR is the relayer KMS address (operator-confirmed); CORE linking is unblocked.
</success_criteria>

<output>
Create `.planning/phases/01.5-social-linking-parallel-to-phase-2/01.5-01-SUMMARY.md` when done.
</output>
