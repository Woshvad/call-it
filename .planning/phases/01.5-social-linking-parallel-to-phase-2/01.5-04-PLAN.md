---
phase: 01.5-social-linking-parallel-to-phase-2
plan: 04
type: execute
wave: 3
depends_on: [01.5-02]
files_modified:
  - apps/web/app/Providers.tsx
  - apps/web/app/onboarding/socials/page.tsx
  - apps/web/app/onboarding/follow-graph/page.tsx
  - apps/web/app/profile/[address]/settings/page.tsx
  - apps/web/app/components/SocialLinkControls.tsx
  - apps/web/tests/follow-graph-optin.test.tsx
autonomous: true
requirements: [AUTH-07, AUTH-12, AUTH-16]
must_haves:
  truths:
    - "Onboarding 'Connect Socials' triggers a real Twitter link (Privy) and a real Farcaster link (Farcaster Auth Kit) that POST to the relayer link routes (AUTH-07)"
    - "Farcaster AuthKitProvider is wired below PrivyProvider per CLAUDE.md provider order"
    - "Settings has link AND unlink controls; unlink calls the on-chain unlink (user wallet) and the relayer unlink-purge (AUTH-12)"
    - "Onboarding follow-graph opt-in stores the per-platform Yes/No; consent copy discloses durable, viewer-only, cleared-on-disconnect server-side storage; declined never renders the feed section (AUTH-16, D-14)"
  artifacts:
    - path: apps/web/app/components/SocialLinkControls.tsx
      provides: "Reusable link/unlink controls (Twitter + Farcaster) used by onboarding + settings"
      contains: "unlink"
    - path: apps/web/app/Providers.tsx
      provides: "Farcaster AuthKitProvider below PrivyProvider"
      contains: "AuthKitProvider"
    - path: apps/web/tests/follow-graph-optin.test.tsx
      provides: "AUTH-16 declined-never-renders test"
  key_links:
    - from: "apps/web/app/components/SocialLinkControls.tsx"
      to: "POST /api/social/link, /api/social/link/farcaster, /api/social/unlink-purge"
      via: "fetch with Privy bearer token"
      pattern: "api/social"
    - from: "apps/web/app/Providers.tsx"
      to: "AuthKitProvider"
      via: "wraps below PrivyProvider"
      pattern: "AuthKitProvider"
---

<objective>
Wire the social-linking frontend: replace the onboarding "Connect Socials" stubs with real Twitter (Privy) + Farcaster (Farcaster Auth Kit) link flows that call the 01.5-02 relayer routes, add link/unlink controls to Settings (AUTH-12), wire the Farcaster `AuthKitProvider` below `PrivyProvider` (CLAUDE.md provider order), and finish the follow-graph opt-in consent (AUTH-16/D-14). Linking is purely additive and must never break sign-in (Pitfall 5 / Pitfall 16).

Per D-01 there is no contract work; the on-chain unlink is user-callable directly via wagmi (`ProfileRegistry.unlinkTwitter`/`unlinkFarcaster`), and the relayer purge is a side call (D-13). `@farcaster/auth-kit@0.8.2` is already installed but unused — this plan wires it.

Purpose: Deliver AUTH-07 (Farcaster link), AUTH-12 (unlink via Settings), and AUTH-16 (opt-in consent) at the UI level.
Output: AuthKitProvider wiring, SocialLinkControls component, updated onboarding socials + follow-graph pages, Settings link/unlink section, and the opt-in render test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01.5-social-linking-parallel-to-phase-2/01.5-CONTEXT.md
@.planning/phases/01.5-social-linking-parallel-to-phase-2/01.5-RESEARCH.md

<read_first>
- apps/web/app/Providers.tsx — LOAD-BEARING provider order (AST-test-locked by apps/web/tests/privy-provider-order.ast.test.ts). Order: PrivyProvider > QueryClientProvider > WagmiProvider > ToastProvider. Add `AuthKitProvider` (from @farcaster/auth-kit) BELOW PrivyProvider per CLAUDE.md ("Farcaster AuthKitProvider wraps below PrivyProvider"). Confirm the AST test still passes after the edit (do not reorder the existing four).
- apps/web/app/onboarding/socials/page.tsx — current Twitter stub (advances as-if-linked, TODO Plan 07/1.5) + Farcaster "Coming soon" disabled button. Replace both with real flows.
- apps/web/app/onboarding/follow-graph/page.tsx — current opt-in screen; the `void optIn` is discarded with a "TODO Phase 1.5: POST the opt-in preference" comment. Wire the per-platform persistence + add the D-14 durable-storage consent disclosure.
- apps/web/app/profile/[address]/settings/page.tsx — Settings page; section 4 is "Connect/Disconnect socials — Privy linkAccount stubs (Phase 1.5 wires onchain)". Wire link + unlink here using SocialLinkControls.
- apps/web/app/onboarding/socials/page.tsx uses usePrivy() + useOnboardingState().advance(); reuse those hooks.
- The relayer routes to call (built in 01.5-02): POST /api/social/link, POST /api/social/link/farcaster, POST /api/social/unlink-purge (all session-gated → send the Privy access token as Bearer). Base URL = NEXT_PUBLIC_RELAYER_BASE_URL.
- ProfileRegistry unlink ABI for the wagmi write: function unlinkTwitter(), function unlinkFarcaster() (user-callable, no args; see apps/web/lib/abis/ProfileRegistry — already used by settings for setDisplayHandle). NEXT_PUBLIC_PROFILE_REGISTRY_ADDRESS env.
- @farcaster/auth-kit@0.8.2 docs: AuthKitProvider needs { rpcUrl (OP mainnet), domain, siweUri }; SignInButton / useSignIn produce the SIWF message + signature to POST to /api/social/link/farcaster. The domain MUST equal the relayer FARCASTER_AUTH_DOMAIN (01.5-01) for verifySignInMessage to pass (Pitfall 3).
</read_first>

<interfaces>
Privy access token (for relayer Bearer): usePrivy().getAccessToken() → string (send as Authorization: Bearer <token>).
Privy Twitter link: useLinkAccount() / linkTwitter flow (Privy v3) → after link, user.linkedAccounts contains twitter_oauth.
Farcaster Auth Kit: import { AuthKitProvider, SignInButton, useSignIn } from '@farcaster/auth-kit'; '@farcaster/auth-kit/styles.css'.
wagmi unlink: useWriteContract().writeContract({ address: PROFILE_REGISTRY, abi, functionName: 'unlinkTwitter' | 'unlinkFarcaster' }).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Farcaster AuthKitProvider + build SocialLinkControls (link + unlink)</name>
  <files>apps/web/app/Providers.tsx, apps/web/app/components/SocialLinkControls.tsx</files>
  <action>
Providers.tsx: import `{ AuthKitProvider }` from '@farcaster/auth-kit' and `'@farcaster/auth-kit/styles.css'`. Wrap the existing tree BELOW PrivyProvider (e.g. inside PrivyProvider, around QueryClientProvider or innermost above ToastProvider — keep the existing four in their locked relative order; AuthKitProvider must be inside PrivyProvider per CLAUDE.md). Config: `{ rpcUrl: <OP mainnet RPC, from env e.g. NEXT_PUBLIC_OP_MAINNET_RPC_URL with a public fallback>, domain: <NEXT_PUBLIC_FARCASTER_AUTH_DOMAIN, must match relayer FARCASTER_AUTH_DOMAIN>, siweUri: <app origin> }`. Run/observe the existing AST provider-order test — it asserts the PrivyProvider>QueryClient>Wagmi>Toast order; ensure your insertion does not break it.
Create `apps/web/app/components/SocialLinkControls.tsx` (client component) exporting `<SocialLinkControls mode="onboarding" | "settings" />`. It renders:
- Twitter: if not linked, a "Link Twitter / X" button that runs the Privy link flow (useLinkAccount), then POSTs to `${NEXT_PUBLIC_RELAYER_BASE_URL}/api/social/link` with `Authorization: Bearer ${await getAccessToken()}`; on 409 show "That handle is already linked to another profile" (D-06). If linked, show a "Unlink Twitter" button (settings mode) that calls wagmi `writeContract(unlinkTwitter)` AND POSTs `/api/social/unlink-purge { platform:'twitter' }` (AUTH-12, D-13).
- Farcaster: a Farcaster Auth Kit `SignInButton`/`useSignIn` flow; on success POST the { nonce, domain, message, signature } to `/api/social/link/farcaster`. Unlink mirrors Twitter (`unlinkFarcaster` + purge { platform:'farcaster' }).
- All network calls are best-effort and never block: a failure shows an inline retry, never throws into the page (Pitfall 5/16). Use the neobrutalist Button/Tag from @call-it/ui.
  </action>
  <verify>
    <automated>pnpm --filter @call-it/web test privy-provider-order && pnpm --filter @call-it/web build</automated>
  </verify>
  <done>AuthKitProvider wired below PrivyProvider; the AST provider-order test still passes; SocialLinkControls exists with link + unlink for both platforms; web build exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Replace onboarding socials stub + finish follow-graph opt-in consent</name>
  <files>apps/web/app/onboarding/socials/page.tsx, apps/web/app/onboarding/follow-graph/page.tsx, apps/web/tests/follow-graph-optin.test.tsx</files>
  <action>
onboarding/socials/page.tsx: replace the as-if-linked Twitter stub and the Farcaster "Coming soon" disabled button with `<SocialLinkControls mode="onboarding" />`. Keep "Skip for now" (AUTH-08) and the existing advance('socials') + routing (linked → /onboarding/follow-graph; skipped → /onboarding/fund). Keep the existing data-testids where tests depend on them; add the new control's testids.
onboarding/follow-graph/page.tsx: wire the discarded `optIn` — POST the per-platform opt-in to the relayer onboarding endpoint (reuse the onboarding state route / advance('followgraph') already persists the step; add the opt-in boolean to that persisted state, or POST a dedicated preference). Add the D-14 consent disclosure copy to the screen: explicitly state the follow graph is stored server-side (durable), is viewer-only, and is cleared on disconnect. "No thanks" must persist a declined state such that the "From your X/Farcaster" feed section (01.5-05) never renders for this user. Add per-platform framing (X and, if Farcaster linked, Farcaster) consistent with D-14.
Create `apps/web/tests/follow-graph-optin.test.tsx` (Vitest + Testing Library): assert (a) the consent copy mentions durable server-side storage + cleared-on-disconnect; (b) clicking "No thanks" results in a declined preference and that a feed-section render helper returns null/empty for a declined user (AUTH-16 declined-never-renders). Mock the relayer fetch + router.
  </action>
  <verify>
    <automated>pnpm --filter @call-it/web test follow-graph-optin</automated>
  </verify>
  <done>Onboarding socials uses SocialLinkControls (no more as-if-linked / Coming-soon stubs); follow-graph page persists per-platform opt-in + shows the D-14 durable-storage consent; declined state never renders the section (test passes).</done>
</task>

<task type="auto">
  <name>Task 3: Settings link/unlink section</name>
  <files>apps/web/app/profile/[address]/settings/page.tsx</files>
  <action>
In the Settings page, implement section 4 ("Connect/Disconnect socials") using `<SocialLinkControls mode="settings" />` so the owner can link Twitter/Farcaster later (D-07: "opt in later from Settings") and unlink either (AUTH-12). Unlink in settings mode: wagmi `writeContract` of `unlinkTwitter`/`unlinkFarcaster` from the user's own wallet (user-callable on-chain — removes badge + handle reference, retains on-chain history) followed by the relayer `/api/social/unlink-purge` call (clears follow-graph data — AUTH-17/D-13). Keep the existing owner-guard (non-owner redirect) and the existing handle-edit/custody/wallet-export/address-book sections intact. Use NEXT_PUBLIC_PROFILE_REGISTRY_ADDRESS for the unlink write.
  </action>
  <verify>
    <automated>pnpm --filter @call-it/web build</automated>
  </verify>
  <done>Settings renders SocialLinkControls(settings); unlink fires the wagmi unlink + relayer purge; existing settings sections unchanged; web build exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → relayer link routes | Privy access token must be sent; relayer re-verifies (never trust client handle) |
| Farcaster Auth Kit domain → relayer | Frontend AuthKitProvider domain must equal relayer FARCASTER_AUTH_DOMAIN (Pitfall 3) |
| social link flow → auth path | Linking is additive; a link failure must never break sign-in (Pitfall 16) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.5-04-01 | Spoofing/Tampering | SIWF domain mismatch | mitigate | Frontend AuthKitProvider domain == relayer FARCASTER_AUTH_DOMAIN; relayer verifies (Pitfall 3) |
| T-01.5-04-02 | Denial of Service | Privy/relay outage | mitigate | Link flows best-effort + retry; never block sign-in or onboarding skip (Pitfall 5/16) |
| T-01.5-04-03 | Information Disclosure | opt-in consent | mitigate | D-14 consent discloses durable, viewer-only, cleared-on-disconnect storage; declined never renders section |
| T-01.5-04-04 | Tampering | npm installs | accept | No new deps — @farcaster/auth-kit@0.8.2 already installed |
</threat_model>

<verification>
- `pnpm --filter @call-it/web test privy-provider-order` (provider order intact after AuthKitProvider insertion).
- `pnpm --filter @call-it/web test follow-graph-optin` (AUTH-16 declined-never-renders).
- `pnpm --filter @call-it/web build` exits 0.
</verification>

<success_criteria>
- Real Twitter + Farcaster link flows POST to the relayer; AuthKitProvider below PrivyProvider; provider-order AST test green.
- Settings link + unlink (on-chain unlink + relayer purge).
- Follow-graph opt-in persisted per-platform with D-14 consent; declined never renders the feed section.
</success_criteria>

<output>
Create `.planning/phases/01.5-social-linking-parallel-to-phase-2/01.5-04-SUMMARY.md` when done.
</output>
