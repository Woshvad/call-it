---
phase: 05-stylusscoreengine-48h-cutoff
plan: 03
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - packages/contracts/src/SolidityScoreEngine.sol
  - packages/contracts/src/RevertingStylusEngine.sol
autonomous: true
requirements:
  - REP-24
must_haves:
  truths:
    - "SolidityScoreEngine.compute_rep_change output matches _solidityBaselineRepDelta byte-for-byte for all conviction × isWinner combos"
    - "forge test --match-contract SolidityScoreEngineTest exits 0 (turns Plan 01's RED forge tests GREEN)"
    - "RevertingStylusEngine.compute_rep_change reverts with expected string (Phase 6 drill pre-staged)"
    - "Both files use pragma solidity =0.8.30 (CI grep guard)"
    - "SolidityScoreEngine ignores consensusPct (baseline low-fidelity per D-3: contrarian fixed at 1.0)"
  artifacts:
    - path: "packages/contracts/src/SolidityScoreEngine.sol"
      provides: "IStylusScoreEngine implementation with _solidityBaselineRepDelta math (REP-24, D-3)"
      contains: "pragma solidity =0.8.30"
      exports: ["compute_rep_change"]
    - path: "packages/contracts/src/RevertingStylusEngine.sol"
      provides: "IStylusScoreEngine implementation that reverts on compute_rep_change (D-6, SAFETY-42 prep)"
      contains: "pragma solidity =0.8.30"
  key_links:
    - from: "packages/contracts/test/SolidityScoreEngine.t.sol"
      to: "packages/contracts/src/SolidityScoreEngine.sol"
      via: "forge import + setUp deploy"
      pattern: "new SolidityScoreEngine"
    - from: "packages/contracts/test/SolidityScoreEngine.t.sol"
      to: "packages/contracts/src/RevertingStylusEngine.sol"
      via: "vm.expectRevert + compute_rep_change call"
      pattern: "new RevertingStylusEngine"
    - from: "packages/contracts/src/SolidityScoreEngine.sol"
      to: "packages/contracts/src/interfaces/IStylusScoreEngine.sol"
      via: "is IStylusScoreEngine + override"
      pattern: "is IStylusScoreEngine"
---

<objective>
Wave 2 (parallel with 05-02): deliver the two Solidity IStylusScoreEngine implementations.

SolidityScoreEngine.sol is the 48h-cutoff fallback (D-3). Its math MUST be byte-for-byte
identical to SettlementManager._solidityBaselineRepDelta so that:
  a) The parity forge tests written in Plan 01 go GREEN
  b) The proxy upgrade command in Plan 06/07 produces identical rep deltas to the runtime fallback

RevertingStylusEngine.sol is the Phase 6 destruction-drill fixture (D-6). Pre-deployed to Sepolia
in this phase so SAFETY-42 is mechanical in Phase 6.

Purpose: Separate from the Rust engine (05-02) — pure Solidity, no WASM dependency.
Output:
- packages/contracts/src/SolidityScoreEngine.sol
- packages/contracts/src/RevertingStylusEngine.sol
</objective>

<execution_context>
@C:\Users\woshv\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\woshv\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\woshv\Desktop\Call it\.planning\phases\05-stylusscoreengine-48h-cutoff\05-PATTERNS.md
@C:\Users\woshv\Desktop\Call it\.planning\phases\05-stylusscoreengine-48h-cutoff\05-RESEARCH.md

<interfaces>
<!-- Solidity baseline math (from SettlementManager.sol lines 731-748) — COPY EXACTLY:
  uint256 BASE = 10;
  uint256 scaled = (BASE * uint256(conviction) * 2) / 100;
  if (scaled < 1) scaled = 1;
  if (isWinner) { delta = int256(scaled); } else { delta = -int256(scaled); }
  // Note: SolidityScoreEngine.compute_rep_change receives uint256 baseValue not hardcoded 10.
  // Replace BASE with baseValue for the standalone contract (makes it usable even if SM's
  // base changes): uint256 scaled = (baseValue * uint256(conviction) * 2) / 100;
  // The parity test always passes baseValue=10 so the outputs match identically.
-->

<!-- IStylusScoreEngine locked function signature:
  function compute_rep_change(
    uint128 currentRep,    // unused in baseline — comment with /*currentRep*/
    uint8   conviction,
    uint8   consensusPct,  // unused in baseline — comment with /*consensusPct*/
    bool    isWinner,
    uint256 baseValue      // replaces hardcoded BASE=10 — always 10 from SM
  ) external view override returns (int32 delta);
-->

<!-- Pragma pattern (every existing Solidity file uses this exactly):
  // SPDX-License-Identifier: MIT
  pragma solidity =0.8.30;
  //                ^^^^^^^ EXACT pin -- never ^0.8.x. CI grep guard fails build otherwise.
  // Source: CLAUDE.md "Recommended Stack -- Pinned Versions"
-->

<!-- Import pattern from PATTERNS.md:
  import { IStylusScoreEngine } from "./interfaces/IStylusScoreEngine.sol";
-->

<!-- D-3 note: SolidityScoreEngine is LOW fidelity by design:
  - ignores consensusPct (contrarian fixed 1.0)
  - no high-conviction 2x asymmetry
  - linear conviction scaling only
  This INTENTIONALLY differs from the Rust engine. The parity TEST checks
  SolidityScoreEngine == _solidityBaselineRepDelta, NOT SolidityScoreEngine == Rust engine.
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SolidityScoreEngine.sol — 48h-cutoff fallback baseline (D-3)</name>
  <files>
    packages/contracts/src/SolidityScoreEngine.sol
  </files>
  <read_first>
    - packages/contracts/src/interfaces/IStylusScoreEngine.sol (locked interface — must implement exactly)
    - packages/contracts/src/SettlementManager.sol lines 731-748 (_solidityBaselineRepDelta — source of truth for math)
    - packages/contracts/src/constants/USDC.sol (minimal stateless Solidity file shape to mirror)
    - .planning/phases/05-stylusscoreengine-48h-cutoff/05-PATTERNS.md "SolidityScoreEngine.sol" section (full skeleton)
  </read_first>

  <behavior>
    - compute_rep_change(0, 50, 0, true, 10) == _baseline(50, true) == +10
    - compute_rep_change(0, 100, 0, true, 10) == _baseline(100, true) == +20
    - compute_rep_change(0, 50, 0, false, 10) == _baseline(50, false) == -10
    - compute_rep_change(0, 1, 0, true, 10) == int32(1) [floor: (10*1*2)/100=0 → 1]
    - compute_rep_change(0, 90, 0, true, 10) == _baseline(90, true) == +18
    - consensusPct is ignored: compute_rep_change(0, 50, 0, true, 10) == compute_rep_change(0, 50, 100, true, 10)
    - currentRep is ignored (unused parameter)
  </behavior>

  <action>
    Create packages/contracts/src/SolidityScoreEngine.sol.

    Header block (copy pragma pin pattern from any existing contract):
    // SPDX-License-Identifier: MIT
    pragma solidity =0.8.30;
    // [comment explaining this is REP-22, REP-24, OPS-16 — 48h cutoff fallback]
    // Math MUST be identical to SettlementManager._solidityBaselineRepDelta (REP-24).
    // Parity verified in test/SolidityScoreEngine.t.sol.

    Import IStylusScoreEngine using the { } named import pattern.

    Contract SolidityScoreEngine is IStylusScoreEngine.

    Single function compute_rep_change with external view override.
    Parameters: uint128 /*currentRep*/ (unused, comment out), uint8 conviction, uint8 /*consensusPct*/ (unused), bool isWinner, uint256 baseValue.
    Returns int32 delta.

    Body — copy _solidityBaselineRepDelta math with baseValue replacing the local BASE=10:
      uint256 scaled = (baseValue * uint256(conviction) * 2) / 100;
      if (scaled < 1) scaled = 1;
      if (isWinner) {
        delta = int32(int256(scaled));
      } else {
        delta = -int32(int256(scaled));
      }
    Add NatSpec comment: // REP-02: floor at 0 is applied by ProfileRegistry.applyRepDelta, not here.

    After creation run: forge test --match-contract SolidityScoreEngineTest -vv
    Expected: all 8 parity tests GREEN (Plan 01 tests now compile and pass).
  </action>

  <acceptance_criteria>
    - forge test --match-contract SolidityScoreEngineTest exits 0 (verify: forge test --match-contract SolidityScoreEngineTest -vv | tail -5 shows "Suite result: ok")
    - SolidityScoreEngine.sol contains "pragma solidity =0.8.30" (verify: grep -c "pragma solidity =0.8.30" packages/contracts/src/SolidityScoreEngine.sol → 1)
    - SolidityScoreEngine.sol contains "consensusPct" commented out (baseline ignores it per D-3): grep -c "consensusPct" packages/contracts/src/SolidityScoreEngine.sol → ≥1 (the parameter exists but is commented unused)
    - SolidityScoreEngine.sol does NOT contain "85" or "contrarian" or "hi" (no high-conviction logic — low fidelity only): grep -c "85\|contrarian\|high.conv" packages/contracts/src/SolidityScoreEngine.sol → 0
    - forge build exits 0 with no compilation warnings for SolidityScoreEngine.sol
  </acceptance_criteria>

  <verify>
    <automated>cd /c/Users/woshv/Desktop/Call\ it/packages/contracts && forge test --match-contract SolidityScoreEngineTest -vv 2>&1 | tail -10</automated>
  </verify>
  <done>
    forge test --match-contract SolidityScoreEngineTest: "Suite result: ok. 8 passed; 0 failed"
    SolidityScoreEngine.sol exists with pragma =0.8.30, no high-conviction logic.
    All Plan 01 parity tests GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 2: RevertingStylusEngine.sol — Phase 6 SAFETY-42 drill fixture (D-6)</name>
  <files>
    packages/contracts/src/RevertingStylusEngine.sol
  </files>
  <read_first>
    - packages/contracts/src/SolidityScoreEngine.sol (just created — identical header pattern to mirror)
    - packages/contracts/src/interfaces/IStylusScoreEngine.sol (interface to implement)
    - .planning/phases/05-stylusscoreengine-48h-cutoff/05-RESEARCH.md "RevertingStylusEngine Fixture" section
    - .planning/phases/05-stylusscoreengine-48h-cutoff/05-PATTERNS.md "RevertingStylusEngine.sol" section
  </read_first>

  <action>
    Create packages/contracts/src/RevertingStylusEngine.sol.

    Use identical header pattern to SolidityScoreEngine.sol (SPDX + pragma =0.8.30 + comment block).
    Comment block explanation: Phase 5 test fixture for Phase 6 SAFETY-42 drill. Pre-deployed to
    Sepolia so the destruction drill is mechanical. Verifies SettlementManager try/catch fires
    RepCalculatedFallback.

    Import IStylusScoreEngine.

    Contract RevertingStylusEngine is IStylusScoreEngine.

    Single function compute_rep_change: all parameters unnamed (uint128, uint8, uint8, bool, uint256).
    external view override returns (int32).
    Body: revert("RevertingStylusEngine: intentional revert for Phase 6 drill");

    After creation run: forge build to confirm both new contracts compile.
    Also run forge test --match-contract SolidityScoreEngineTest to confirm the
    test_reverting_engine_reverts test now passes (vm.expectRevert + deploy + call).
  </action>

  <acceptance_criteria>
    - packages/contracts/src/RevertingStylusEngine.sol exists with pragma =0.8.30 (verify: grep -c "pragma solidity =0.8.30" packages/contracts/src/RevertingStylusEngine.sol → 1)
    - RevertingStylusEngine.sol contains the revert string "Phase 6 drill" (verify: grep -c "Phase 6 drill" packages/contracts/src/RevertingStylusEngine.sol → 1)
    - forge build exits 0 with no compilation errors (verify: forge build 2>&1 | grep -c "Error" → 0)
    - forge test --match-test test_reverting_engine_reverts exits 0 (verify: forge test --match-test test_reverting_engine_reverts -vv | grep "PASS\|ok")
    - RevertingStylusEngine.sol does NOT implement any actual computation (body is only revert): grep -c "scaled\|conviction\|delta" packages/contracts/src/RevertingStylusEngine.sol → 0
  </acceptance_criteria>

  <verify>
    <automated>cd /c/Users/woshv/Desktop/Call\ it/packages/contracts && forge build 2>&1 | tail -3 && forge test --match-test test_reverting_engine_reverts -vv 2>&1 | tail -5</automated>
  </verify>
  <done>
    forge build exits 0 (both new contracts compile).
    forge test --match-test test_reverting_engine_reverts: "PASS" or "Suite result: ok".
    RevertingStylusEngine.sol has pragma =0.8.30, revert string, no computation.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SolidityScoreEngine → SettlementManager try/catch | Proxy upgrade replaces Stylus impl with this contract; SM calls via proxy; must produce identical outputs to runtime fallback |
| RevertingStylusEngine → SettlementManager try/catch | Intentional revert triggers catch branch; must NOT accidentally succeed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Tampering | SolidityScoreEngine math divergence from _solidityBaselineRepDelta | mitigate | 8 parity forge tests assert exact equality; acceptance criteria verifies forge test exits 0. Any math change breaks tests immediately (REP-24). |
| T-05-03-02 | Tampering | pragma floating (^0.8.x instead of =0.8.30) | mitigate | Acceptance criteria grep for "pragma solidity =0.8.30" → 1; CI grep guard also catches this in existing contracts.yml. |
| T-05-03-03 | Tampering | RevertingStylusEngine accidentally succeeds | mitigate | Contract body contains ONLY revert(); acceptance criteria verifies grep for "scaled|conviction|delta" → 0. test_reverting_engine_reverts with vm.expectRevert catches any non-revert path. |
  T-05-03-04 | Elevation of Privilege | 48h-cutoff proxy upgrade to wrong contract | accept | SolidityScoreEngine must be deployed to a specific address recorded in addresses.ts (Plan 04). Operator verifies address before issuing upgradeTo(). Promoted to multisig in Phase 6 (SAFETY-20). |
</threat_model>

<verification>
```bash
cd packages/contracts

# All SolidityScoreEngine parity tests green
forge test --match-contract SolidityScoreEngineTest -vv | tail -5
# Expected: Suite result: ok. 8 passed; 0 failed

# Build clean
forge build 2>&1 | grep -c "Error"
# Expected: 0

# Pragma pins correct
grep -c "pragma solidity =0.8.30" src/SolidityScoreEngine.sol src/RevertingStylusEngine.sol
# Expected: 2 (one per file)

# SolidityScoreEngine has no high-conviction logic (D-3 low-fidelity confirmed)
grep -c "85\|contrarian" src/SolidityScoreEngine.sol
# Expected: 0
```
</verification>

<success_criteria>
- forge test --match-contract SolidityScoreEngineTest: 8 passed; 0 failed (Plan 01 RED tests now GREEN)
- SolidityScoreEngine.sol: pragma =0.8.30, IStylusScoreEngine implementation, no high-conv logic
- RevertingStylusEngine.sol: pragma =0.8.30, always-reverts body, "Phase 6 drill" in revert string
- forge build exits 0 for both new files
</success_criteria>

<output>
After completion, create .planning/phases/05-stylusscoreengine-48h-cutoff/05-03-SUMMARY.md
</output>
