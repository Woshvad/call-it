---
phase: 05-stylusscoreengine-48h-cutoff
plan: 05
type: execute
wave: 3
depends_on:
  - "05-01"
files_modified:
  - apps/relayer/src/workers/stylus-deactivation-watcher.ts
  - apps/relayer/test/stylus-deactivation-watcher.test.ts
autonomous: true
requirements:
  - REP-19
must_haves:
  truths:
    - "deactivation-watcher.ts StylusDeactivationWatcherOpts interface has demoCutoffTimestamp?: number field"
    - "tick() fires sendAlert('stylus_demo_cutoff') at T-72h/T-48h/T-24h using SAME SET NX EX 86400 pattern as reactivation alerts"
    - "key prefix is stylus:demo-cutoff:T-${h}h:${dateKey} (distinct from stylus:alert-fired:T-${N}d:)"
    - "vitest Test 5 passes: demoCutoffTimestamp set to nowPlusDays(0.5) → alert fires with sendAlert('stylus_demo_cutoff')"
    - "Existing Tests 1-4 still pass (no regressions from watcher extension)"
    - "OPS-16 runbook SOLIDITY_BASELINE_ADDR reference updated from '<phase-4-deployed-address>' to real variable name"
  artifacts:
    - path: "apps/relayer/src/workers/stylus-deactivation-watcher.ts"
      provides: "Extended watcher with demoCutoffTimestamp field + 72h/48h/24h demo alerts"
      exports: ["startStylusDeactivationWatcher", "StylusDeactivationWatcherOpts"]
    - path: "apps/relayer/test/stylus-deactivation-watcher.test.ts"
      provides: "Test 5: demoCutoffTimestamp triggers stylus_demo_cutoff alert via Redis NX lock"
  key_links:
    - from: "apps/relayer/src/workers/stylus-deactivation-watcher.ts"
      to: "apps/relayer/src/workers/alerts.ts"
      via: "sendAlert('stylus_demo_cutoff', {...})"
      pattern: "sendAlert.*demo_cutoff"
    - from: "apps/relayer/test/stylus-deactivation-watcher.test.ts"
      to: "apps/relayer/src/workers/stylus-deactivation-watcher.ts"
      via: "import startStylusDeactivationWatcher"
      pattern: "startStylusDeactivationWatcher.*demoCutoffTimestamp"
---

<objective>
Wave 3 (parallel with 05-04): extend the Phase 0 deactivation watcher with 72h/48h/24h
demo-cutoff alert capability, mirroring the existing reactivation alert pattern exactly (D-7).

Purpose: Per D-7 and Pitfall 17, the watcher must fire at T-72h/T-48h/T-24h before a
configured demo date — forcing a go/no-go decision before the 48h cutoff window. This is
NOT a new system. It is a minimal extension of the existing watcher using the same Redis NX
idempotency pattern already proven in Phase 0.

Output:
- stylus-deactivation-watcher.ts — StylusDeactivationWatcherOpts extended; demo alert block added to tick()
- stylus-deactivation-watcher.test.ts — Test 5 added for demo cutoff path
- docs/runbooks/OPS-16-stylus-reactivation.md — SOLIDITY_BASELINE_ADDR reference fixed
</objective>

<execution_context>
@C:\Users\woshv\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\woshv\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\woshv\Desktop\Call it\.planning\phases\05-stylusscoreengine-48h-cutoff\05-PATTERNS.md
@C:\Users\woshv\Desktop\Call it\.planning\phases\05-stylusscoreengine-48h-cutoff\05-RESEARCH.md

<interfaces>
<!-- Existing StylusDeactivationWatcherOpts (lines 57-70 in watcher.ts):
  export interface StylusDeactivationWatcherOpts {
    publicClient: { readContract(...): Promise<unknown> };
    stylusAddress: string | null;
    intervalMs?: number;
    redis: { set(key, value, ...args): Promise<string | null> };
  }
  ADD: demoCutoffTimestamp?: number;  // Unix seconds. If set, fires cutoff alerts at T-72h/T-48h/T-24h
-->

<!-- Existing Redis NX idempotency pattern (lines 147-151 in watcher.ts) to COPY for demo cutoff:
  const lockKey = `stylus:alert-fired:T-${thresholdDays}d:${dateKey}`;
  const acquired = await redis.set(lockKey, '1', 'EX', 86400, 'NX');
  if (acquired === 'OK') {
    await sendAlert('stylus_reactivation', { ... });
  }
  break; // only highest triggered threshold fires per tick
  
  Demo cutoff key prefix MUST differ: stylus:demo-cutoff:T-${h}h:${dateKey}
  (prevents collision with reactivation lock keys)
-->

<!-- Demo cutoff logic to insert into tick() AFTER the reactivation threshold loop (after line 175):
  if (opts.demoCutoffTimestamp) {
    const demoThresholds = [72, 48, 24] as const;
    const nowSeconds = Math.floor(Date.now() / 1000);
    const hoursUntilDemo = (opts.demoCutoffTimestamp - nowSeconds) / 3600;
    for (const h of demoThresholds) {
      if (hoursUntilDemo <= h && hoursUntilDemo > 0) {
        const dateKey = new Date().toISOString().slice(0, 10);
        const lockKey = `stylus:demo-cutoff:T-${h}h:${dateKey}`;
        const acquired = await redis.set(lockKey, '1', 'EX', 86400, 'NX');
        if (acquired === 'OK') {
          await sendAlert('stylus_demo_cutoff', {
            hoursRemaining: Math.floor(hoursUntilDemo),
            threshold: h,
            demoCutoffTimestamp: opts.demoCutoffTimestamp,
            stylusAddress,
            message: h <= 48
              ? 'DECISION REQUIRED: Run cargo stylus check. If not working, run CutoffFallback.s.sol NOW.'
              : 'Pre-demo check: Verify Stylus engine is activated and compute_rep_change works.',
          });
        }
        break; // only highest triggered threshold (mirrors line 174 pattern)
      }
    }
  }
  Note: stylusAddress variable is already in scope (defined at line 84 in opts destructure).
  Note: nowSeconds must be declared inside this block if not already in scope at that point in tick().
  Check the existing nowSeconds usage — if it's only declared inside the reactivation threshold section,
  redeclare it (or move it to the top of tick()).
-->

<!-- Test 5 structure (extends existing describe block; mirrors Test 2 pattern lines 87-143):
  it('Test 5: demo-cutoff alert fires when hoursUntilDemo <= 24', async () => {
    const mockRedis = makeMockRedis('OK');
    const sendAlertMock = vi.mocked(sendAlert);
    // Set demoCutoffTimestamp to nowPlusDays(0.5) = 12 hours from now (in seconds)
    const demoCutoffTs = Math.floor(Date.now() / 1000) + 12 * 3600;
    const expiryFarFuture = nowPlusDays(200); // far future — no reactivation alert
    const mockPublicClient = makeMockPublicClient(expiryFarFuture);

    const watcher = startStylusDeactivationWatcher({
      publicClient: mockPublicClient as any,
      stylusAddress: '0xStylusAddr0000000000000000000000000000',
      intervalMs: 50,
      redis: mockRedis as any,
      demoCutoffTimestamp: demoCutoffTs,
    });

    await new Promise((resolve) => setTimeout(resolve, 150));
    watcher.stop();

    expect(sendAlertMock).toHaveBeenCalledWith(
      'stylus_demo_cutoff',
      expect.objectContaining({
        hoursRemaining: expect.any(Number),
        threshold: 24, // 12 hours < 24h threshold → fires at T-24h
        demoCutoffTimestamp: demoCutoffTs,
      }),
    );
    // Verify Redis lock key prefix is demo-cutoff, NOT alert-fired
    expect(mockRedis.set).toHaveBeenCalledWith(
      expect.stringMatching(/stylus:demo-cutoff:T-24h:/),
      '1', 'EX', 86400, 'NX',
    );
  });
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend deactivation watcher with demo-cutoff alert block (D-7)</name>
  <files>
    apps/relayer/src/workers/stylus-deactivation-watcher.ts,
    apps/relayer/test/stylus-deactivation-watcher.test.ts
  </files>
  <read_first>
    - apps/relayer/src/workers/stylus-deactivation-watcher.ts (full file — 199 lines — read in one pass before editing)
    - apps/relayer/test/stylus-deactivation-watcher.test.ts (full file — 182 lines — understand existing test structure before adding Test 5)
    - .planning/phases/05-stylusscoreengine-48h-cutoff/05-PATTERNS.md "stylus-deactivation-watcher.ts (modify existing worker)" section
    - .planning/phases/05-stylusscoreengine-48h-cutoff/05-RESEARCH.md "Phase 5 integration tasks" section
  </read_first>

  <behavior>
    - Test 5: demoCutoffTimestamp set to 12 hours from now → sendAlert('stylus_demo_cutoff') called with threshold=24
    - Test 5: Redis SET called with key matching /stylus:demo-cutoff:T-24h:/ pattern (NOT stylus:alert-fired:)
    - Test 1 (regression): no alert fires when daysRemaining > 30 (unaffected by new code)
    - Test 2 (regression): reactivation alerts at T-30/15/7/1 thresholds still fire
    - Test 3 (regression): graceful skip on readContract throw
    - Test 4 (regression): null stylusAddress skips without crash
  </behavior>

  <action>
    1. Edit apps/relayer/src/workers/stylus-deactivation-watcher.ts:
    a. Add demoCutoffTimestamp?: number to StylusDeactivationWatcherOpts interface (after the redis field).
    b. Inside tick(): after the closing brace of the existing reactivation threshold loop (after the "break;" at line 173-174), add the demo-cutoff block from the interfaces section. Declare nowSeconds = Math.floor(Date.now() / 1000) if it is not already in scope at that point (check line 128 — nowSeconds is declared there inside tick). The demo block can use the same nowSeconds.
    c. The stylusAddress variable is already destructured from opts at line 84. The demo block references it directly.
    d. Do NOT modify any existing threshold logic, Redis key patterns, or sendAlert calls.

    2. Edit apps/relayer/test/stylus-deactivation-watcher.test.ts:
    Add Test 5 inside the existing describe('startStylusDeactivationWatcher', ...) block, after Test 4. Use the skeleton from the interfaces section. Ensure it uses makeMockRedis and makeMockPublicClient helpers already defined.

    3. Run vitest: pnpm --filter @call-it/relayer test apps/relayer/test/stylus-deactivation-watcher.test.ts
    Expected: 5 passed, 0 failed.
  </action>

  <acceptance_criteria>
    - vitest exits 0: pnpm --filter @call-it/relayer test 2>&1 | grep "Tests.*passed" shows ≥5 passed
    - StylusDeactivationWatcherOpts contains demoCutoffTimestamp?: number (verify: grep -c "demoCutoffTimestamp" apps/relayer/src/workers/stylus-deactivation-watcher.ts → ≥2, one in interface + one in tick)
    - Watcher contains "stylus_demo_cutoff" alert name (verify: grep -c "stylus_demo_cutoff" apps/relayer/src/workers/stylus-deactivation-watcher.ts → 1)
    - Key prefix "stylus:demo-cutoff:" present (verify: grep -c "demo-cutoff" apps/relayer/src/workers/stylus-deactivation-watcher.ts → 1)
    - Test file contains Test 5 (verify: grep -c "Test 5" apps/relayer/test/stylus-deactivation-watcher.test.ts → 1)
    - No changes to existing reactivation alert logic: grep -c "stylus_reactivation" apps/relayer/src/workers/stylus-deactivation-watcher.ts → 1 (unchanged from original)
  </acceptance_criteria>

  <verify>
    <automated>cd /c/Users/woshv/Desktop/Call\ it && pnpm --filter @call-it/relayer test apps/relayer/test/stylus-deactivation-watcher.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>
    vitest: "5 passed; 0 failed" (Tests 1-4 unchanged + Test 5 new).
    demoCutoffTimestamp in interface and tick block.
    stylus_demo_cutoff alert with stylus:demo-cutoff: key prefix in watcher.
  </done>
</task>

<task type="auto">
  <name>Task 2: OPS-16 runbook SOLIDITY_BASELINE_ADDR reference update</name>
  <files>
    docs/runbooks/OPS-16-stylus-reactivation.md
  </files>
  <read_first>
    - docs/runbooks/OPS-16-stylus-reactivation.md (full file — locate SOLIDITY_BASELINE_ADDRESS reference and the cut-off fallback section)
    - .planning/phases/05-stylusscoreengine-48h-cutoff/05-RESEARCH.md "Deploy sequence for 48h cutoff" section
  </read_first>

  <action>
    Read docs/runbooks/OPS-16-stylus-reactivation.md in full. Locate any line referencing SOLIDITY_BASELINE_ADDRESS or a placeholder like "<phase-4-deployed-address>". Update those references to reflect:

    1. The variable name: SOLIDITY_SCORE_ENGINE_ARBITRUM_SEPOLIA (from addresses.ts, Phase 5 Plan 04)
    2. The source: "packages/shared/src/constants/addresses.ts (populated after Phase 5 Plan 06 deploy)"
    3. The cutoff command: Replace any outdated proxy upgrade command with the exact CutoffFallback.s.sol invocation:
       forge script script/CutoffFallback.s.sol --rpc-url $ARBITRUM_SEPOLIA_RPC_URL --broadcast
       OR the cast send equivalent from CutoffFallback.s.sol comments.
    4. Update the "deploy-date" calendar step to reference the Phase 5 deploy output and repoint-calendar.ts command.

    Do NOT restructure the runbook or add new sections — only update the placeholder references.
    If the runbook already has a correctly-formatted SOLIDITY_BASELINE_ADDRESS reference with a real address, leave it unchanged and note "no update needed" in SUMMARY.
  </action>

  <acceptance_criteria>
    - docs/runbooks/OPS-16-stylus-reactivation.md does NOT contain "<phase-4-deployed-address>" placeholder (verify: grep -c "<phase-4-deployed-address>" docs/runbooks/OPS-16-stylus-reactivation.md → 0)
    - Runbook contains "CutoffFallback" OR "SOLIDITY_SCORE_ENGINE" reference (verify: grep -c -i "cutoff\|SOLIDITY_SCORE_ENGINE" docs/runbooks/OPS-16-stylus-reactivation.md → ≥1)
    - Runbook contains "repoint-calendar" reference (verify: grep -c "repoint-calendar" docs/runbooks/OPS-16-stylus-reactivation.md → ≥1)
  </acceptance_criteria>

  <verify>
    <automated>grep -c "phase-4-deployed-address" /c/Users/woshv/Desktop/Call\ it/docs/runbooks/OPS-16-stylus-reactivation.md 2>/dev/null || echo "file not found or no match"</automated>
  </verify>
  <done>
    OPS-16 runbook has no "<phase-4-deployed-address>" placeholder.
    CutoffFallback.s.sol or SOLIDITY_SCORE_ENGINE referenced.
    repoint-calendar.ts command referenced.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| watcher → Redis | SET NX key with 86400s TTL; different prefix per alert type prevents cross-alert key collision |
| watcher → Telegram (via sendAlert) | Demo cutoff alert goes to same Telegram bot as reactivation alerts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Denial of Service | demoCutoffTimestamp not set after deploy | mitigate | OPS-16 runbook NEXT STEPS (from DeployPhase5Stylus.s.sol console output) instructs operator to set DEMO_CUTOFF_TIMESTAMP env var after Phase 5 deploy. Watcher is no-op if field is null/undefined. |
| T-05-05-02 | Denial of Service | Redis key collision between reactivation and demo-cutoff alerts | mitigate | Different key prefixes: reactivation uses stylus:alert-fired:T-${N}d: and demo uses stylus:demo-cutoff:T-${h}h:. No collision possible. |
| T-05-05-03 | Spoofing | Demo alert fires with wrong threshold | mitigate | Test 5 asserts exact threshold=24 for 12h remaining. Logic uses array [72,48,24] in descending order with break on first match — same pattern as proven reactivation loop. |
</threat_model>

<verification>
```bash
# All 5 tests pass
cd apps/relayer && pnpm test test/stylus-deactivation-watcher.test.ts 2>&1 | tail -5
# Expected: 5 passed; 0 failed

# Interface extended
grep -c "demoCutoffTimestamp" src/workers/stylus-deactivation-watcher.ts
# Expected: ≥2

# Alert name correct
grep -c "stylus_demo_cutoff" src/workers/stylus-deactivation-watcher.ts
# Expected: 1

# Key prefix distinct from reactivation
grep -c "demo-cutoff" src/workers/stylus-deactivation-watcher.ts
# Expected: 1
```
</verification>

<success_criteria>
- vitest: 5 passed; 0 failed (Tests 1-4 regression + Test 5 new)
- deactivation-watcher.ts: demoCutoffTimestamp in interface, demo-cutoff alert block in tick()
- Key prefix stylus:demo-cutoff: distinct from stylus:alert-fired:
- OPS-16 runbook: no placeholder address, CutoffFallback.s.sol referenced
</success_criteria>

<output>
After completion, create .planning/phases/05-stylusscoreengine-48h-cutoff/05-05-SUMMARY.md
</output>
