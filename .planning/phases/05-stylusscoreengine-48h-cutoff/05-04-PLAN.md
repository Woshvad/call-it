---
phase: 05-stylusscoreengine-48h-cutoff
plan: 04
type: execute
wave: 3
depends_on:
  - "05-02"
  - "05-03"
files_modified:
  - packages/contracts/script/DeployPhase5Stylus.s.sol
  - packages/contracts/script/CutoffFallback.s.sol
  - packages/shared/src/constants/addresses.ts
autonomous: true
requirements:
  - REP-21
  - REP-24
must_haves:
  truths:
    - "DeployPhase5Stylus.s.sol forge script --dry-run exits 0 (simulation; no broadcast without Sepolia key)"
    - "CutoffFallback.s.sol compiles; addresses.ts has stub entries for all 4 Phase 5 addresses"
    - "Deploy script post-deploy assertions check SM.stylusScoreEngine() == proxy, ProxyAdmin.owner() == deployer"
    - "NEXT STEPS section in script console output references repoint-calendar.ts and fly secrets set command"
    - "addresses.ts FILL AFTER DEPLOY comments present so executor knows to update after Plan 06"
  artifacts:
    - path: "packages/contracts/script/DeployPhase5Stylus.s.sol"
      provides: "Forge script: deploys SolidityScoreEngine + RevertingStylusEngine + ProxyAdmin + TransparentUpgradeableProxy + wires SM"
      exports: ["DeployPhase5Stylus"]
    - path: "packages/contracts/script/CutoffFallback.s.sol"
      provides: "Forge script: issues proxy.upgradeAndCall(SOLIDITY_BASELINE) — one command cutoff (OPS-16)"
      exports: ["CutoffFallback"]
    - path: "packages/shared/src/constants/addresses.ts"
      provides: "Stub Phase 5 address constants (FILL AFTER DEPLOY) for proxy, admin, Solidity engine, reverting engine"
  key_links:
    - from: "packages/contracts/script/DeployPhase5Stylus.s.sol"
      to: "packages/contracts/src/SolidityScoreEngine.sol"
      via: "new SolidityScoreEngine() in run()"
      pattern: "SolidityScoreEngine"
    - from: "packages/contracts/script/DeployPhase5Stylus.s.sol"
      to: "SettlementManager.setStylusScoreEngine(address)"
      via: "ISettlementManager(SETTLEMENT_MANAGER).setStylusScoreEngine(address(proxy))"
      pattern: "setStylusScoreEngine"
    - from: "packages/contracts/script/CutoffFallback.s.sol"
      to: "ProxyAdmin.upgradeAndCall"
      via: "ProxyAdmin(PROXY_ADMIN_ADDR).upgradeAndCall(payable(PROXY_ADDR), SOLIDITY_BASELINE_ADDR, '')"
      pattern: "upgradeAndCall"
---

<objective>
Wave 3: produce the deploy scripts and address stubs that the operator will run in Plan 06.
These scripts are written and forge-compiled here (autonomous); actual Sepolia broadcast happens in Plan 06 (operator checkpoint).

Purpose: Remove all ambiguity from the Phase 06 operator session — the operator runs one command,
gets console.log output with addresses, and pastes into addresses.ts stubs provided here.

Output:
- packages/contracts/script/DeployPhase5Stylus.s.sol (deploy + wire + assertions + NEXT STEPS)
- packages/contracts/script/CutoffFallback.s.sol (cutoff upgrade + verification)
- packages/shared/src/constants/addresses.ts (Phase 5 stub entries with FILL AFTER DEPLOY)
</objective>

<execution_context>
@C:\Users\woshv\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\woshv\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\woshv\Desktop\Call it\.planning\phases\05-stylusscoreengine-48h-cutoff\05-PATTERNS.md
@C:\Users\woshv\Desktop\Call it\.planning\phases\05-stylusscoreengine-48h-cutoff\05-RESEARCH.md

<interfaces>
<!-- Phase 4 deployed Sepolia addresses (constants in DeployPhase5Stylus.s.sol — from addresses.ts):
  SETTLEMENT_MANAGER_ARBITRUM_SEPOLIA = 0xAc37a0e4A3e575EF21684c28a5b820dB44654595
  CALL_REGISTRY_ARBITRUM_SEPOLIA     = 0x7DAd732764abfC935aD5bf8e5CFF9BEA7B2C234D
  Source: STATE.md Phase 4 deploy notes + addresses.ts
  (Use these exact addresses in the deploy script SETTLEMENT_MANAGER + CALL_REGISTRY constants)
-->

<!-- DeployPhase4.s.sol pattern (lines 105-184) — forge script structure to mirror:
  1. vm.envUint("DEPLOYER_PRIVATE_KEY") — never hardcode
  2. vm.envAddress("STYLUS_IMPL_ADDRESS") — Stylus WASM impl from cargo stylus deploy
  3. vm.startBroadcast(deployerKey) → deploy contracts → vm.stopBroadcast()
  4. Post-deploy require() assertions (AFTER stopBroadcast — view only)
  5. console.log() addresses
  6. console.log("REQUIRED NEXT STEPS:") with commands
-->

<!-- OZ 5.6.1 TransparentUpgradeableProxy import:
  import { TransparentUpgradeableProxy } from
    "@openzeppelin/contracts/proxy/transparent/TransparentUpgradeableProxy.sol";
  import { ProxyAdmin } from
    "@openzeppelin/contracts/proxy/transparent/ProxyAdmin.sol";
  Constructor: new TransparentUpgradeableProxy(stylusImplAddr, address(proxyAdmin), "")
  Engine is stateless: empty initData = "" (no initialize() needed)
-->

<!-- SettlementManager interface for setStylusScoreEngine:
  import { ISettlementManager } from "../src/interfaces/ISettlementManager.sol";
  ...
  ISettlementManager(SETTLEMENT_MANAGER).setStylusScoreEngine(address(proxy));
  // Post-deploy assertion:
  require(
    ISettlementManager(SETTLEMENT_MANAGER).stylusScoreEngine() == address(proxy),
    "DeployPhase5: SM.stylusScoreEngine() mismatch"
  );
-->

<!-- CutoffFallback.s.sol: ProxyAdmin.upgradeAndCall(payable(proxy), solidityBaseline, "")
  OZ 5.6.1 ProxyAdmin has upgradeAndCall(ITransparentUpgradeableProxy proxy, address impl, bytes calldata data)
  The proxy arg is ITransparentUpgradeableProxy — cast with payable()
  Verification: ProxyAdmin.getProxyImplementation(payable(proxy)) == solidityBaseline
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DeployPhase5Stylus.s.sol + CutoffFallback.s.sol</name>
  <files>
    packages/contracts/script/DeployPhase5Stylus.s.sol,
    packages/contracts/script/CutoffFallback.s.sol
  </files>
  <read_first>
    - packages/contracts/script/DeployPhase4.s.sol lines 1-184 (exact structural analog — mirror header, run(), assertions, console.log patterns)
    - packages/contracts/src/SolidityScoreEngine.sol (just created — import path)
    - packages/contracts/src/RevertingStylusEngine.sol (just created — import path)
    - packages/contracts/src/interfaces/ISettlementManager.sol (for setStylusScoreEngine + stylusScoreEngine() view)
    - .planning/phases/05-stylusscoreengine-48h-cutoff/05-PATTERNS.md "DeployPhase5Stylus.s.sol" section (full run() skeleton)
    - .planning/phases/05-stylusscoreengine-48h-cutoff/05-PATTERNS.md "CutoffFallback.s.sol" section
  </read_first>

  <action>
    1. Create packages/contracts/script/DeployPhase5Stylus.s.sol.

    Header: SPDX-License-Identifier: MIT, pragma solidity =0.8.30 (EXACT pin), comment block listing requirements REP-19/REP-21/REP-24 and DEPLOYMENT SAFETY CHECKLIST (mirror DeployPhase4.s.sol header pattern).

    Imports: Script + console (forge-std), TransparentUpgradeableProxy + ProxyAdmin (OZ 5.6.1), SolidityScoreEngine, RevertingStylusEngine, ISettlementManager.

    Contract DeployPhase5Stylus is Script.

    Constants:
    - SETTLEMENT_MANAGER = 0xAc37a0e4A3e575EF21684c28a5b820dB44654595 (Phase 4 Sepolia address, from STATE.md)
    - Comment per Phase 4 pattern: "NOT redeployed in Phase 5"

    function run() external:
    1. deployerKey = vm.envUint("DEPLOYER_PRIVATE_KEY")
    2. stylusImplAddr = vm.envAddress("STYLUS_IMPL_ADDRESS") [from cargo stylus deploy output]
    3. vm.startBroadcast(deployerKey)
    4. Deploy SolidityScoreEngine: solidityEngine = new SolidityScoreEngine()
    5. Deploy RevertingStylusEngine: revertingEngine = new RevertingStylusEngine()
    6. Deploy ProxyAdmin: proxyAdmin = new ProxyAdmin(vm.addr(deployerKey))
    7. Deploy TransparentUpgradeableProxy: proxy = new TransparentUpgradeableProxy(stylusImplAddr, address(proxyAdmin), "")
    8. Wire: ISettlementManager(SETTLEMENT_MANAGER).setStylusScoreEngine(address(proxy))
    9. vm.stopBroadcast()
    10. Post-deploy require assertions (view calls after stopBroadcast):
       require(ISettlementManager(SETTLEMENT_MANAGER).stylusScoreEngine() == address(proxy), "SM.stylusScoreEngine() mismatch")
    11. console.log deployment summary (copy pattern from DeployPhase4.s.sol):
       "SolidityScoreEngine:", address(solidityEngine)
       "RevertingStylusEngine:", address(revertingEngine)
       "ProxyAdmin:", address(proxyAdmin)
       "StylusScoreEngine proxy:", address(proxy)
    12. console.log "REQUIRED NEXT STEPS:" block with these exact 4 steps:
       "1. Update packages/shared/src/constants/addresses.ts (STYLUS_SCORE_ENGINE_PROXY, SOLIDITY_SCORE_ENGINE, REVERTING_STYLUS_ENGINE, PROXY_ADMIN constants)"
       "2. fly secrets set STYLUS_SCORE_ENGINE_ADDRESS=<proxy_addr> --app call-it-relayer-sepolia"
       "3. pnpm tsx scripts/repoint-calendar.ts --stylus-deploy-date $(date +%Y-%m-%d)"
       "4. Run CutoffFallback.s.sol round-trip on Sepolia to rehearse upgrade (REP-21, REP-24)"
    13. console.log "OPERATOR VERIFICATION (cast commands):" with the 3 cast calls from PATTERNS.md.

    2. Create packages/contracts/script/CutoffFallback.s.sol.

    Header: same pragma =0.8.30, SPDX. Comment: "OPS-16 RUNBOOK: execute if cargo stylus check fails 48h before demo."
    Include the equivalent cast send one-liner as a comment for operators who prefer CLI over forge script.

    Imports: Script + console, ProxyAdmin.

    Contract CutoffFallback is Script. Constants:
    - PROXY_ADMIN_ADDR = address(0) // FILL AFTER DEPLOY
    - PROXY_ADDR = address(0)       // FILL AFTER DEPLOY
    - SOLIDITY_BASELINE_ADDR = address(0) // FILL AFTER DEPLOY

    function run() external:
    1. deployerKey = vm.envUint("DEPLOYER_PRIVATE_KEY")
    2. vm.startBroadcast(deployerKey)
    3. ProxyAdmin(PROXY_ADMIN_ADDR).upgradeAndCall(payable(PROXY_ADDR), SOLIDITY_BASELINE_ADDR, "")
    4. vm.stopBroadcast()
    5. require(ProxyAdmin(PROXY_ADMIN_ADDR).getProxyImplementation(payable(PROXY_ADDR)) == SOLIDITY_BASELINE_ADDR, "CutoffFallback: implementation mismatch")
    6. console.log "[OK] Proxy upgraded to SolidityScoreEngine at:", SOLIDITY_BASELINE_ADDR

    After creating both files: forge build to confirm both scripts compile.
  </action>

  <acceptance_criteria>
    - forge build exits 0 for both scripts (verify: forge build 2>&1 | grep -c "Error" → 0)
    - DeployPhase5Stylus.s.sol contains "setStylusScoreEngine" (verify: grep -c "setStylusScoreEngine" packages/contracts/script/DeployPhase5Stylus.s.sol → ≥1)
    - DeployPhase5Stylus.s.sol contains "REQUIRED NEXT STEPS" (verify: grep -c "REQUIRED NEXT STEPS" packages/contracts/script/DeployPhase5Stylus.s.sol → 1)
    - DeployPhase5Stylus.s.sol contains "repoint-calendar" (verify: grep -c "repoint-calendar" packages/contracts/script/DeployPhase5Stylus.s.sol → 1)
    - CutoffFallback.s.sol contains "upgradeAndCall" (verify: grep -c "upgradeAndCall" packages/contracts/script/CutoffFallback.s.sol → 1)
    - CutoffFallback.s.sol contains "FILL AFTER DEPLOY" for all 3 address constants (verify: grep -c "FILL AFTER DEPLOY" packages/contracts/script/CutoffFallback.s.sol → 3)
    - Both files: grep -c "pragma solidity =0.8.30" returns 1 per file
    - forge script --dry-run (no broadcast): forge script script/DeployPhase5Stylus.s.sol --rpc-url $ARBITRUM_SEPOLIA_RPC_URL (simulation mode, exits without broadcast — skip if no RPC key)
  </acceptance_criteria>

  <verify>
    <automated>cd /c/Users/woshv/Desktop/Call\ it/packages/contracts && forge build 2>&1 | tail -3 && grep -c "setStylusScoreEngine" script/DeployPhase5Stylus.s.sol && grep -c "upgradeAndCall" script/CutoffFallback.s.sol</automated>
  </verify>
  <done>
    forge build exits 0 for both scripts.
    DeployPhase5Stylus.s.sol: setStylusScoreEngine, REQUIRED NEXT STEPS, repoint-calendar present.
    CutoffFallback.s.sol: upgradeAndCall, 3 FILL AFTER DEPLOY placeholders present.
  </done>
</task>

<task type="auto">
  <name>Task 2: addresses.ts Phase 5 stub entries</name>
  <files>
    packages/shared/src/constants/addresses.ts
  </files>
  <read_first>
    - packages/shared/src/constants/addresses.ts lines 86-200 (Phase 4 address entry pattern to mirror exactly — doc comment, exported const, value pattern)
    - .planning/phases/05-stylusscoreengine-48h-cutoff/05-PATTERNS.md "addresses.ts" section (exact stub format)
  </read_first>

  <action>
    Append Phase 5 address constants to packages/shared/src/constants/addresses.ts, following the Phase 4 doc-comment + exported const pattern exactly. Do not modify any existing constants.

    Add four new exported constants with FILL AFTER DEPLOY placeholder values (address(0) equivalent in TS = '0x0000000000000000000000000000000000000000' as const).

    For each constant, include a JSDoc comment block in the Phase 4 style explaining:
    - What it is and which plan deployed it
    - The proxy architecture (TransparentUpgradeableProxy → StylusScoreEngine WASM)
    - Post-deploy verification cast command

    Constants to add:
    1. STYLUS_SCORE_ENGINE_PROXY_ARBITRUM_SEPOLIA — the proxy address (main entry point for SM)
    2. PROXY_ADMIN_ARBITRUM_SEPOLIA — the ProxyAdmin (owner = deployer; promoted to multisig in Phase 6)
    3. SOLIDITY_SCORE_ENGINE_ARBITRUM_SEPOLIA — the 48h-cutoff fallback contract address
    4. REVERTING_STYLUS_ENGINE_ARBITRUM_SEPOLIA — Phase 6 drill fixture address

    All values: '0x0000000000000000000000000000000000000000' as const // FILL AFTER DEPLOY (Phase 5 Plan 06)

    Also: if an ARBITRUM_SEPOLIA_ADDRESSES or similar record/barrel export exists at the bottom of addresses.ts, extend it with the new Phase 5 constants (copy how Phase 4 entries were added).
  </action>

  <acceptance_criteria>
    - packages/shared/src/constants/addresses.ts contains STYLUS_SCORE_ENGINE_PROXY_ARBITRUM_SEPOLIA (verify: grep -c "STYLUS_SCORE_ENGINE_PROXY" packages/shared/src/constants/addresses.ts → 1)
    - addresses.ts contains all 4 new constants (verify: grep -c "FILL AFTER DEPLOY (Phase 5" packages/shared/src/constants/addresses.ts → 4)
    - addresses.ts compiles: pnpm --filter @call-it/shared build exits 0 (or tsc --noEmit in shared package if build is unavailable)
    - No existing constants modified: git diff packages/shared/src/constants/addresses.ts shows only additions (no deletions from existing Phase 2/3/4 entries)
  </acceptance_criteria>

  <verify>
    <automated>grep -c "STYLUS_SCORE_ENGINE_PROXY_ARBITRUM_SEPOLIA" /c/Users/woshv/Desktop/Call\ it/packages/shared/src/constants/addresses.ts && grep -c "FILL AFTER DEPLOY (Phase 5" /c/Users/woshv/Desktop/Call\ it/packages/shared/src/constants/addresses.ts</automated>
  </verify>
  <done>
    addresses.ts has 4 new Phase 5 stub constants with FILL AFTER DEPLOY.
    Package compiles with no TypeScript errors.
    No existing constants deleted or modified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Deploy script → Sepolia RPC | forge script broadcasts transactions; requires funded deployer key (env-only) |
| ProxyAdmin → proxy implementation slot | upgradeAndCall changes implementation; must point at correct address |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Elevation of Privilege | setStylusScoreEngine owner-only gate | mitigate | Function is onlyOwner in SettlementManager.sol:641. Script calls it under vm.startBroadcast(deployerKey) — only deployer key can run it. Phase 6 promotes to multisig (SAFETY-20). |
| T-05-04-02 | Spoofing | Wrong SETTLEMENT_MANAGER address in deploy script | mitigate | Address hardcoded from STATE.md Phase 4 deploy notes (0xAc37a...) + post-deploy require() assertion confirms SM.stylusScoreEngine() == proxy. If address is wrong, assertion reverts before script completes. |
| T-05-04-03 | Tampering | CutoffFallback upgradeAndCall to wrong address | mitigate | Three FILL AFTER DEPLOY constants must be filled from Plan 06 deployment output before running. Require assertion post-upgrade verifies getProxyImplementation() matches. |
| T-05-04-04 | Denial of Service | Missing NEXT STEPS in deploy output | mitigate | console.log "REQUIRED NEXT STEPS:" block in run() acceptance criteria. Operator cannot miss the relayer env var and calendar repoint commands. |
</threat_model>

<verification>
```bash
cd packages/contracts

# Both scripts compile
forge build 2>&1 | grep -c "Error"
# Expected: 0

# Deploy script has required wiring call
grep -c "setStylusScoreEngine" script/DeployPhase5Stylus.s.sol
# Expected: ≥1

# Cutoff script has upgrade call
grep -c "upgradeAndCall" script/CutoffFallback.s.sol
# Expected: 1

# addresses.ts has Phase 5 stubs
grep -c "STYLUS_SCORE_ENGINE_PROXY_ARBITRUM_SEPOLIA" \
  ../../packages/shared/src/constants/addresses.ts
# Expected: 1
```
</verification>

<success_criteria>
- forge build exits 0 for DeployPhase5Stylus.s.sol and CutoffFallback.s.sol
- DeployPhase5Stylus.s.sol: deploys 4 contracts, wires SM, asserts correctness, prints NEXT STEPS
- CutoffFallback.s.sol: upgradeAndCall + post-upgrade assertion + 3 FILL AFTER DEPLOY placeholders
- addresses.ts: 4 new Phase 5 stub constants appended, no existing entries modified
</success_criteria>

<output>
After completion, create .planning/phases/05-stylusscoreengine-48h-cutoff/05-04-SUMMARY.md
</output>
