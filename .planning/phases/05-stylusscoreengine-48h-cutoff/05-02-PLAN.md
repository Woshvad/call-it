---
phase: 05-stylusscoreengine-48h-cutoff
plan: 02
type: tdd
wave: 2
depends_on:
  - "05-01"
files_modified:
  - packages/contracts/stylus/src/lib.rs
  - packages/contracts/stylus/src/math.rs
autonomous: true
requirements:
  - REP-19
  - REP-20
must_haves:
  truths:
    - "cargo test exits 0 — all 9 D-2 unit tests GREEN after math.rs implements full-fidelity formula"
    - "cargo stylus export-abi emits 'compute_rep_change' (not 'computeRepChange') in function signature"
    - "lib.rs uses #[selector(name = 'compute_rep_change')] — enforces selector 0xff540eb6"
    - "lib.rs uses &self (not &mut self) — engine is view (STATICCALL-compatible)"
    - "D-2 worked examples: (90,80,true)=68, (50,20,true)=10, (30,50,false)=-6, (90,50,false)=-36, (50,0,true)=7"
  artifacts:
    - path: "packages/contracts/stylus/src/math.rs"
      provides: "Full compute_rep_delta implementation with D-2 math: confScaled, contrarianMilli, magnitude, hi-conv 2x"
      min_lines: 50
    - path: "packages/contracts/stylus/src/lib.rs"
      provides: "#[storage] StylusScoreEngine + #[public] impl with #[selector] override calling math::compute_rep_delta"
      exports: ["compute_rep_change"]
  key_links:
    - from: "packages/contracts/stylus/src/lib.rs"
      to: "packages/contracts/stylus/src/math.rs"
      via: "crate::math::compute_rep_delta call with alloy type conversion"
      pattern: "crate::math::compute_rep_delta"
    - from: "packages/contracts/stylus/src/lib.rs"
      to: "packages/contracts/src/interfaces/IStylusScoreEngine.sol"
      via: "#[selector(name = 'compute_rep_change')] → selector 0xff540eb6"
      pattern: "selector.*compute_rep_change"
---

<objective>
Wave 2 GREEN implementation: replace the math.rs todo!() body with full D-2 integer arithmetic,
then wire lib.rs as the Stylus contract entrypoint with the mandatory selector override.

Purpose: Turn all 9 RED tests GREEN. The math module is pure Rust (no Stylus host calls)
so cargo test runs without a chain. lib.rs is the Stylus-SDK scaffolding layer — it validates
the ABI boundary via cargo stylus export-abi.

Output:
- packages/contracts/stylus/src/math.rs — complete compute_rep_delta (D-2 formula)
- packages/contracts/stylus/src/lib.rs — replaces Phase-0 stub; #[storage] + #[public] + #[selector]
</objective>

<execution_context>
@C:\Users\woshv\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\woshv\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\woshv\Desktop\Call it\.planning\phases\05-stylusscoreengine-48h-cutoff\05-RESEARCH.md
@C:\Users\woshv\Desktop\Call it\.planning\phases\05-stylusscoreengine-48h-cutoff\05-PATTERNS.md

<interfaces>
<!-- D-2 exact integer arithmetic (LOCKED — do not invent alternatives):

Step 1 confScaled: max(1, (baseValue * conviction * 2) / 100)
  baseValue is always 10; conviction is u8 0-100
  At conviction=50: confScaled = max(1, (10*50*2)/100) = max(1,10) = 10
  At conviction=90: confScaled = max(1, (10*90*2)/100) = max(1,18) = 18

Step 2 contrarianMilli (winners ONLY, 1000 = 1.0x):
  contrarianMilli = 700 + (min(consensusPct, 85) * 1300) / 85
  At consensusPct=0:  700 + 0 = 700 (0.7x)
  At consensusPct=20: 700 + (20*1300)/85 = 700 + 305 = 1005 (≈1.0x) [Note: integer division]
  At consensusPct=80: 700 + (80*1300)/85 = 700 + 1223 = 1923 (≈1.9x)
  At consensusPct=85+: 700 + (85*1300)/85 = 700 + 1300 = 2000 (2.0x)
  LOSERS: contrarianMilli fixed at 1000 (1.0x, no contrarian)

Step 3 magnitude:
  if isWinner: magnitude = (confScaled * contrarianMilli) / 1000
  if loser:    magnitude = confScaled   (no contrarian applied)

Step 4 high-conviction asymmetry (conviction >= 85, BOTH winners and losers):
  magnitude *= 2

Step 5 delta:
  if isWinner: +magnitude as i32
  if loser:    -magnitude as i32

Verification of D-2 worked examples:
  (conviction=90, consensusPct=80, isWinner=true):
    confScaled = max(1, (10*90*2)/100) = 18
    contrarianMilli = 700 + (80*1300)/85 = 700 + 1223 = 1923
    magnitude = (18 * 1923) / 1000 = 34614/1000 = 34 (integer division)
    hi-conv (90>=85): magnitude *= 2 → 68
    delta = +68 ✓

  (conviction=50, consensusPct=20, isWinner=true):
    confScaled = max(1, (10*50*2)/100) = 10
    contrarianMilli = 700 + (20*1300)/85 = 700 + 305 = 1005
    magnitude = (10 * 1005) / 1000 = 10050/1000 = 10 (integer division)
    hi-conv (50<85): no 2x
    delta = +10 ✓

  (conviction=30, consensusPct=50, isWinner=false):
    confScaled = max(1, (10*30*2)/100) = max(1,6) = 6
    loser: magnitude = confScaled = 6
    hi-conv (30<85): no 2x
    delta = -6 ✓

  (conviction=90, consensusPct=50, isWinner=false):
    confScaled = 18
    loser: magnitude = confScaled = 18
    hi-conv (90>=85): magnitude *= 2 → 36
    delta = -36 ✓

  (conviction=50, consensusPct=0, isWinner=true):
    confScaled = 10
    contrarianMilli = 700 + 0 = 700
    magnitude = (10 * 700) / 1000 = 7
    hi-conv: no 2x
    delta = +7 ✓

Use checked arithmetic throughout:
  u64::checked_mul, u32::checked_add, etc.
  On overflow: return i32::MAX (never panic; max delta ≈ 80 so overflow impossible in practice
  but checked ops are required per security requirement).

_current_rep parameter: bind as _current_rep (underscore prefix) — unused in Phase 5.
-->

<!-- lib.rs critical pattern (from RESEARCH.md "Full Engine Skeleton"):
  #![no_std]
  #![no_main]
  extern crate alloc;
  use stylus_sdk::prelude::*;
  use stylus_sdk::alloy_primitives::{U128, U256};
  #[storage]
  pub struct StylusScoreEngine;
  #[public]
  impl StylusScoreEngine {
    #[selector(name = "compute_rep_change")]   // MANDATORY — forces 0xff540eb6
    pub fn compute_rep_change(&self, current_rep: U128, conviction: u8,
      consensus_pct: u8, is_winner: bool, base_value: U256) -> i32 {
      crate::math::compute_rep_delta(
        current_rep.to::<u128>(), conviction, consensus_pct, is_winner, base_value.to::<u64>())
    }
  }
Type map: U128→u128, u8→u8, bool→bool, U256→u64 (.to::<u64>() base_value always 10), i32→i32
&self (not &mut self): view state mutability required for STATICCALL compatibility.
-->

<!-- Phase-0 lib.rs stub to FULLY REPLACE (current content uses #[no_mangle] — not Stylus pattern):
  The Phase-0 file uses: #[no_mangle] pub extern "C" fn stub_ping() -> u32 { 0 }
  Replace ALL content. Do not preserve any Phase-0 stub lines.
-->

<!-- mod math declaration in lib.rs: add pub mod math; so crate::math is accessible from tests -->
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Implement full D-2 math in math.rs (GREEN)</name>
  <files>
    packages/contracts/stylus/src/math.rs
  </files>
  <read_first>
    - packages/contracts/stylus/tests/test_math.rs (the test expectations — implement to make these pass)
    - packages/contracts/src/SettlementManager.sol lines 731-748 (_solidityBaselineRepDelta — the low-fidelity baseline to beat)
    - .planning/phases/05-stylusscoreengine-48h-cutoff/05-RESEARCH.md "D-2 locked worked examples" table
  </read_first>

  <behavior>
    - compute_rep_delta(100, 90, 80, true, 10) == 68
    - compute_rep_delta(100, 50, 20, true, 10) == 10
    - compute_rep_delta(100, 30, 50, false, 10) == -6
    - compute_rep_delta(100, 90, 50, false, 10) == -36
    - compute_rep_delta(100, 50, 0, true, 10) == 7
    - compute_rep_delta(100,50,10,false,10) == compute_rep_delta(100,50,90,false,10) (contrarian not on losses)
    - compute_rep_delta(100,85,50,true,10) > compute_rep_delta(100,84,50,true,10) (hi-conv fires at 85)
    - compute_rep_delta(0, 90, 0, false, 10) returns negative value (no floor in engine)
    - compute_rep_delta(u128::MAX, 100, 100, true, 100) does not panic (checked arithmetic)
  </behavior>

  <action>
    Replace the todo!() body in packages/contracts/stylus/src/math.rs with the full D-2 formula.
    The function signature remains: pub fn compute_rep_delta(_current_rep: u128, conviction: u8, consensus_pct: u8, is_winner: bool, base_value: u64) -> i32.

    Implement the 5-step formula from the D-2 locked decision exactly as specified in the interfaces block:
    Step 1 confScaled: use checked_mul and checked_div. base_value as u64, conviction as u32 for intermediate. confScaled = max(1, (base_value * conviction_u64 * 2) / 100). All values fit in u64 (base=10, conviction≤100, *2=2000, /100=20 max — no overflow possible but use checked ops).
    Step 2 contrarianMilli: winners only. capped_pct = consensus_pct.min(85) as u64. contrarianMilli = 700u64 + (capped_pct * 1300) / 85. For losers: contrarianMilli = 1000u64 (1.0x, no contrarian).
    Step 3 magnitude: if is_winner: (confScaled * contrarianMilli) / 1000. If loser: confScaled.
    Step 4 if conviction >= 85: magnitude *= 2.
    Step 5 delta: if is_winner: magnitude as i32. If loser: -(magnitude as i32).
    All intermediate values are u64; magnitude max ≈ 40 (hi-conv → 80); no overflow. Use checked_mul/checked_div at each step and unwrap_or(i32::MAX as u64) for safety.
    No floor at 0 in engine (D-2: "The engine does NOT floor at 0").
  </action>

  <acceptance_criteria>
    - cargo test exits 0 with all 9 tests passing: verify command: cd packages/contracts/stylus && cargo test 2>&1 | grep "test result"
    - Expected output contains "test result: ok. 9 passed; 0 failed"
    - Specific test assertions: cargo test test_d2_example_bold_correct passes (== 68); cargo test test_d2_example_cold_start_win passes (== 7)
    - No use of float arithmetic anywhere in math.rs (grep -c "f32\|f64" packages/contracts/stylus/src/math.rs → 0)
    - No floor-at-0 logic in math.rs (grep -c "max(0" packages/contracts/stylus/src/math.rs → 0)
  </acceptance_criteria>

  <verify>
    <automated>cd /c/Users/woshv/Desktop/Call\ it/packages/contracts/stylus && cargo test 2>&1 | tail -5</automated>
  </verify>
  <done>
    cargo test: "test result: ok. 9 passed; 0 failed; 0 ignored"
    All 5 D-2 worked examples return exact expected values.
    No float arithmetic, no floor-at-0 in engine.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace Phase-0 lib.rs stub with Stylus #[storage]+#[public]+#[selector] scaffold</name>
  <files>
    packages/contracts/stylus/src/lib.rs
  </files>
  <read_first>
    - packages/contracts/stylus/src/lib.rs (current Phase-0 stub — all content to be replaced)
    - packages/contracts/src/interfaces/IStylusScoreEngine.sol lines 48-54 (function signature to match)
    - .planning/phases/05-stylusscoreengine-48h-cutoff/05-RESEARCH.md "Full Engine Skeleton" section
    - .planning/phases/05-stylusscoreengine-48h-cutoff/05-PATTERNS.md "lib.rs" section with critical #[selector] note
  </read_first>

  <action>
    Replace ALL content of packages/contracts/stylus/src/lib.rs with the verified Stylus SDK 0.10.7 skeleton.

    File must begin with:
      #![cfg_attr(not(any(test, feature = "export-abi")), no_std)]
      #![cfg_attr(not(any(test, feature = "export-abi")), no_main)]
      extern crate alloc;
    (Note: cfg_attr pattern instead of unconditional #![no_std] so cargo test and export-abi both work.)

    Imports: use stylus_sdk::prelude::*; and use stylus_sdk::alloy_primitives::{U128, U256};

    Add: pub mod math; (exposes math.rs as crate::math to the test files)

    Struct: #[storage] pub struct StylusScoreEngine; (stateless — no fields)

    Impl block:
      #[public]
      impl StylusScoreEngine {
        /// LOCKED: selector 0xff540eb6 = keccak256("compute_rep_change(uint128,uint8,uint8,bool,uint256)")[0:4]
        /// #[selector(name = "compute_rep_change")] is MANDATORY (D-1).
        /// Without it, #[public] converts compute_rep_change → computeRepChange (selector 0xfe7606ba).
        #[selector(name = "compute_rep_change")]
        pub fn compute_rep_change(
          &self,               // &self = view (not &mut self which would be nonpayable)
          current_rep: U128,   // uint128 in Solidity
          conviction: u8,      // uint8
          consensus_pct: u8,   // uint8
          is_winner: bool,     // bool
          base_value: U256,    // uint256 (always 10 from SM)
        ) -> i32 {             // int32 in Solidity (alloy Int<32> roundtrips as i32)
          crate::math::compute_rep_delta(
            current_rep.to::<u128>(),
            conviction,
            consensus_pct,
            is_winner,
            base_value.to::<u64>(),
          )
        }
      }

    Run cargo stylus export-abi (requires cargo-stylus 0.6.3 and wasm32-unknown-unknown target;
    if these are not installed locally, skip and note in output — Sepolia deploy step will verify).
    If cargo-stylus is available, grep output for "compute_rep_change" (snake_case, NOT camelCase).
  </action>

  <acceptance_criteria>
    - packages/contracts/stylus/src/lib.rs contains '#[selector(name = "compute_rep_change")]' (verify: grep -c 'selector.*compute_rep_change' packages/contracts/stylus/src/lib.rs → 1)
    - packages/contracts/stylus/src/lib.rs contains '&self' NOT '&mut self' for compute_rep_change (verify: grep -c '&mut self' packages/contracts/stylus/src/lib.rs → 0)
    - packages/contracts/stylus/src/lib.rs contains 'pub mod math;' (verify: grep -c 'pub mod math' packages/contracts/stylus/src/lib.rs → 1)
    - packages/contracts/stylus/src/lib.rs does NOT contain 'stub_ping' or '#[no_mangle]' (Phase-0 stub fully replaced)
    - cargo check --target wasm32-unknown-unknown exits 0 (if Rust wasm32 target installed; skip if absent — record in SUMMARY)
    - If cargo-stylus available: cargo stylus export-abi | grep "compute_rep_change" returns a line matching "compute_rep_change" NOT "computeRepChange"
  </acceptance_criteria>

  <verify>
    <automated>grep -c 'selector.*compute_rep_change' /c/Users/woshv/Desktop/Call\ it/packages/contracts/stylus/src/lib.rs && grep -c 'pub mod math' /c/Users/woshv/Desktop/Call\ it/packages/contracts/stylus/src/lib.rs && grep -c 'stub_ping' /c/Users/woshv/Desktop/Call\ it/packages/contracts/stylus/src/lib.rs</automated>
  </verify>
  <done>
    lib.rs has #[selector(name = "compute_rep_change")], pub mod math, no stub_ping.
    &self (not &mut self) on compute_rep_change.
    Phase-0 #[no_mangle] stub fully replaced.
    cargo check passes (or skip noted if Rust wasm target unavailable).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| math.rs → Stylus SDK | lib.rs converts alloy types (U128, U256) to native Rust types before calling math::compute_rep_delta |
| lib.rs → SettlementManager | #[selector] forces 0xff540eb6; STATICCALL-compatible &self view |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Spoofing | #[public] camelCase default | mitigate | #[selector(name = "compute_rep_change")] in lib.rs. Acceptance criteria verify 'selector.*compute_rep_change' grep. |
| T-05-02-02 | Tampering | &mut self state mutability | mitigate | &self enforced. Acceptance criteria verify grep -c '&mut self' → 0. STATICCALL from SM try/catch would revert on nonpayable. |
| T-05-02-03 | Tampering | Integer overflow in reputation math | mitigate | checked_mul / checked_div throughout compute_rep_delta. test_no_overflow_extremes covers u128::MAX input. Overflow impossible (max magnitude ≈ 80) but checked ops are defense-in-depth. |
| T-05-02-04 | Tampering | Wrong D-2 math constants | mitigate | 9 locked unit tests with exact expected values. Any constant change breaks test_d2_example_* immediately. |
</threat_model>

<verification>
```bash
cd packages/contracts/stylus && cargo test 2>&1 | tail -3
# Expected: test result: ok. 9 passed; 0 failed

grep -c 'selector.*compute_rep_change' packages/contracts/stylus/src/lib.rs
# Expected: 1

grep -c '&mut self' packages/contracts/stylus/src/lib.rs
# Expected: 0

grep -c 'stub_ping' packages/contracts/stylus/src/lib.rs
# Expected: 0

# If cargo-stylus installed:
cargo stylus export-abi | grep "compute_rep_change"
# Expected: function compute_rep_change(uint128 ...) external view returns (int32);
# Must NOT contain "computeRepChange"
```
</verification>

<success_criteria>
- cargo test: "9 passed; 0 failed" — all D-2 examples green
- lib.rs: #[selector(name = "compute_rep_change")], &self, pub mod math, no Phase-0 stub
- math.rs: no float arithmetic, no floor-at-0, checked arithmetic throughout
- If cargo-stylus available: export-abi shows snake_case "compute_rep_change", not camelCase
</success_criteria>

<output>
After completion, create .planning/phases/05-stylusscoreengine-48h-cutoff/05-02-SUMMARY.md
</output>
