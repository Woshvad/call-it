---
phase: 02-followfademarket
plan: 09
type: execute
wave: 4
depends_on: [02-08]
files_modified:
  - apps/web/app/og/[callId]/route.ts
  - apps/web/app/components/NotificationBell.tsx
  - apps/web/app/components/NotificationInbox.tsx
  - apps/web/app/layout.tsx
autonomous: true
requirements:
  - SOCIAL-24
  - SOCIAL-25
  - SHARE-04

must_haves:
  truths:
    - "GET /og/[callId]?v={n} returns 1200x630 PNG with follow%/fade% bar + time-left + corner brackets (§16 variant 1)"
    - "export const runtime = 'nodejs' present in route.ts (NEVER 'edge' — resvg-wasm bundling)"
    - "No display:grid in OG card JSX — flexbox only (Satori constraint)"
    - "NotificationBell shows unread count badge; NotificationInbox lists notifications with mark-read"
    - "?v={statusVersion} in og:image meta tag changes on CallerExited transition (D-09)"
  artifacts:
    - path: apps/web/app/og/[callId]/route.ts
      provides: "Live OG card variant 1: follow%/fade% progress bar + time-left countdown + corner brackets"
      contains: "export const runtime = 'nodejs'"
    - path: apps/web/app/components/NotificationBell.tsx
      provides: "Bell icon + unread count badge + click-to-open inbox"
    - path: apps/web/app/components/NotificationInbox.tsx
      provides: "Notification list (polled 30s), mark-read action, caller-exit notification card"
  key_links:
    - from: apps/web/app/og/[callId]/route.ts
      to: apps/relayer/src/workers/notification-fanout.ts
      via: "statusVersion in Redis bumped by worker; OG route reads fresh state via RPC not subgraph"
      pattern: "statusVersion|?v="
    - from: apps/web/app/components/NotificationBell.tsx
      to: apps/relayer/src/routes/notifications.ts
      via: "GET /api/notifications?user={address} polled at 30s"
      pattern: "notifications"
---

<objective>
Live OG card (variant 1) + notification inbox UI. These are the final two deliverables of Phase 2: the shareable social card that shows the live Follow/Fade market state, and the in-app bell that tells followers their call has been exited. Together with Plan 08, this closes all 34 phase requirements.

Purpose: Social sharing (SHARE-04) + caller-exit notification inbox (SOCIAL-24/25). Phase 2 closure.
Output: /og/[callId]/route.ts + NotificationBell.tsx + NotificationInbox.tsx + layout.tsx update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-followfademarket/02-CONTEXT.md
@.planning/phases/02-followfademarket/02-RESEARCH.md
@CALL_IT_SPEC1.md (§16 variant 1 Live State OG card — BINDING DESIGN CONTRACT)

<interfaces>
<!-- Key patterns from existing OG route -->

From apps/web/app/api/og/[callId]/route.ts (exact analog):
- export const runtime = 'nodejs'; // CRITICAL: not 'edge'
- export async function GET(req: NextRequest, { params }: { params: Promise<{ callId: string }> }) { const { callId } = await params; }
- const url = new URL(req.url); const v = url.searchParams.get('v') ?? '0';
- imageResponse.headers.set('Cache-Control', 'public, max-age=60, stale-while-revalidate=300');
- imageResponse.headers.set('X-Variant', 'fallback'); → change to 'live'
- ImageResponse from @vercel/og — already installed Phase 0

From packages/ui/src/compound/Receipt.tsx (existing component with corner brackets):
- CornerBrackets component usage
- Syne/Inter font imports pattern

Design tokens per §16 variant 1: 1200×630; follow% bar accent (#E8F542 filled left); fade% remainder (#1A1A1F filled right); LIVE badge top-left; handle + rep top-center; Call It + ARB bottom-right; corner bracket motif; time-left countdown; conviction + stake footer line.

From Phase 1 relayer (live-state proxy):
- viem createPublicClient with Arbitrum Sepolia RPC (server-side only — not wagmi)
- readContracts for followReserve, fadeReserve from FFM; getCall from CallRegistry

Notification polling:
- GET /api/notifications?user={address}&cursor= from Plan 07
- 30s polling interval (D-13)
- Radix Dialog or slide-over for inbox panel
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live OG card variant 1 (/og/[callId]/route.ts)</name>
  <files>apps/web/app/og/[callId]/route.ts</files>
  <read_first>
    - apps/web/app/api/og/[callId]/route.ts (full file — runtime declaration, GET handler signature, await params pattern, Cache-Control header, ImageResponse usage, error fallback pattern)
    - CALL_IT_SPEC1.md §16 variant 1 (FULL SECTION — binding design contract for card layout, colors, typography, progress bar, corner brackets)
    - .planning/phases/02-followfademarket/02-RESEARCH.md section "OG Card Variant 1 (Live State)" (card layout diagram, route shape with statusVersion, cache strategy D-09, flexbox-only rule)
    - .planning/phases/02-followfademarket/02-PATTERNS.md section "OG card variant 1" (runtime declaration, handler signature, v query param, cache-control, renderFallback origin)
    - packages/shared/src/constants/addresses.ts (FOLLOW_FADE_MARKET_ARBITRUM_SEPOLIA, CALL_REGISTRY_ARBITRUM_SEPOLIA names)
  </read_first>
  <action>
    Create apps/web/app/og/[callId]/route.ts — IMPORTANT path clarification: this file lives at apps/web/app/og/[callId]/route.ts (the Live variant). Do NOT create it at apps/web/app/api/og/[callId]/route.ts — that is the Phase 0 fallback route (apps/web/app/api/og/[callId]/route.ts), used only as a read-first analog for the route structure. The two routes coexist: the fallback at /api/og/[callId] and the Live variant at /og/[callId]. This plan creates the /og/[callId] route only.

    Create apps/web/app/og/[callId]/route.ts following the analog file exactly for the route structure. The CRITICAL first line after imports: `export const runtime = 'nodejs';` — NEVER 'edge'.

    GET handler: (1) parse callId and v from params/URL; (2) read live state via viem createPublicClient (server-side, not frontend wagmi): parallel Promise.all for [callRegistry.getCall(callId), ffm.followReserve(callId), ffm.fadeReserve(callId)]; (3) compute followPct = Number(followReserve * 100n / (followReserve + fadeReserve)), fadePct = 100 - followPct; (4) formatTimeLeft(call.expiry - now) as "3d 14h" or "< 1h" string; (5) render ImageResponse with the Live card JSX.

    Live card JSX (1200×630, ALL flexbox — NO display:grid per Satori constraint, Pitfall 15):
    - Outer: flex column, full width+height, #09090E background, corner bracket motif (4 absolute-ish positioned brackets — use position absolute with flexbox parent relative).
    - Top row: [LIVE] badge (#E8F542 text on dark bg) left; handle · repScore center; "Call It · ARB" right.
    - Middle: call statement in Syne bold (large), centered or left-padded.
    - Progress bar row: left filled #E8F542 at followPct% width; right filled #2A2A30 at fadePct% width; "X% Following  Y% Fading" label row below.
    - Footer row: "$X staked · X% conviction · timeLeft left" in Inter smaller.

    Cache-Control: 'public, max-age=60, stale-while-revalidate=300'; X-Variant: 'live'.

    On any error (RPC failure, call not found): fall through to renderFallback from @/lib/og-fallback-render (existing Phase 0 pattern — implement the TODO mentioned in PATTERNS.md).

    Import Syne font via fetch from Google Fonts or use the font import pattern already in the fallback route analog.
  </action>
  <verify>
    <automated>pnpm --filter @call-it/web build 2>&1 | grep -E "og/\[callId\]|error" | head -10</automated>
  </verify>
  <done>apps/web/app/og/[callId]/route.ts exists; first non-comment code line is `export const runtime = 'nodejs'`; GET handler is async; no display:grid in JSX (grep passes); uses viem server-side reads (not wagmi); Cache-Control header set; error fallback present; apps/web builds without error for this route.</done>
</task>

<task type="auto">
  <name>Task 2: NotificationBell + NotificationInbox UI</name>
  <files>
    apps/web/app/components/NotificationBell.tsx,
    apps/web/app/components/NotificationInbox.tsx,
    apps/web/app/layout.tsx
  </files>
  <read_first>
    - apps/web/app/layout.tsx (full file — existing layout structure, navbar, where to add NotificationBell)
    - packages/ui/src/primitives/Button.tsx (button variants for mark-read action)
    - packages/ui/src/compound/Receipt.tsx (neobrutalist card pattern for notification items)
    - .planning/phases/02-followfademarket/02-RESEARCH.md section "Notification Center Architecture" (GET /api/notifications response shape, polling pattern)
    - .planning/phases/02-followfademarket/02-CONTEXT.md D-13 (polled at ~30s, bell + unread badge + inbox list + mark-read)
  </read_first>
  <action>
    Create NotificationBell.tsx ('use client'): displays a bell icon (or 🔔 emoji — use SVG to match neobrutalist treatment) with a yellow-green unread count badge when unreadCount > 0. On mount, polls GET /api/notifications?user={address} every 30s using useInterval or setInterval in useEffect. Clears interval on unmount. On click: opens NotificationInbox. Uses usePrivy + useAccount to get authenticated address (only render when authenticated). Badge style: #E8F542 small circle with dark number, hard 2px border.

    Create NotificationInbox.tsx: Radix Dialog/Sheet slide-over panel. Receives notifications array + onMarkRead callback. Each notification item is a card: "⚠ [callerHandle] exited their own call · [callStatement] · -$X slashed" (SOCIAL-23/24 format). Relative timestamp. Mark-read button (or auto-mark-read on open). Shows empty state "No notifications yet" when list is empty. On mark-all-read: POST /api/notifications/mark-read { ids: [...allIds] }. Neobrutalist treatment: 2-3px borders, #09090E bg, accent hover states.

    Update apps/web/app/layout.tsx: import NotificationBell; add to the navbar/header section. Only render when user is authenticated (usePrivy().authenticated). Position: top-right area of the navbar, consistent with existing nav elements.
  </action>
  <verify>
    <automated>pnpm --filter @call-it/web build 2>&1 | grep -c "error" | xargs -I{} test {} -eq 0</automated>
  </verify>
  <done>NotificationBell.tsx polls /api/notifications every 30s; shows unread count badge; NotificationInbox shows notification cards with caller-exit format; mark-read calls POST /api/notifications/mark-read; both components added to layout.tsx (authenticated only); apps/web build exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OG card generator→RPC | Server-side viem reads; RPC key server-side only |
| Notification fetch→Relayer | Authenticated user's own address only; handled in Plan 07 auth check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09-01 | Tampering | OG card served from stale CDN after CallerExited | mitigate | statusVersion bump in notification-fanout worker (Plan 07); ?v= in og:image URL forces CDN cache-miss on version bump (D-09); max-age=60 limits stale window |
| T-02-09-02 | Repudiation | OG card uses 'edge' runtime (breaks resvg-wasm) | mitigate | `export const runtime = 'nodejs'` enforced; Pitfall 15: no display:grid in JSX (grep-verifiable) |
| T-02-09-03 | Information Disclosure | Notification inbox shows notifications for wrong user | mitigate | NotificationBell only renders for authenticated user; relayer auth gate checked in Plan 07; address from useAccount not URL param |
</threat_model>

<verification>
After both tasks complete:
1. `grep "export const runtime = 'nodejs'" apps/web/app/og/[callId]/route.ts` — present
2. `grep "display.*grid" apps/web/app/og/[callId]/route.ts` — 0 matches (no grid in OG JSX)
3. `grep "refetchInterval\|setInterval.*30" apps/web/app/components/NotificationBell.tsx` — 30s polling present
4. `grep "mark-read" apps/web/app/components/NotificationInbox.tsx` — mark-read endpoint call present
5. `grep "NotificationBell" apps/web/app/layout.tsx` — added to layout
6. `pnpm --filter @call-it/web build` exits 0
7. Manual smoke: `curl /og/{testCallId}?v=1` from deployed Vercel URL returns image/* content-type (post-deploy)
</verification>

<success_criteria>
- /og/[callId]/route.ts uses Node runtime; renders 1200x630 with follow%/fade% bar + time-left + corner brackets
- No display:grid in OG card JSX
- NotificationBell polls 30s and shows unread count
- NotificationInbox renders caller-exit notification cards with mark-read
- apps/web build exits 0
- Phase 2 complete: all 34 requirement IDs covered across plans 01-09
</success_criteria>

<output>
After completion, create `.planning/phases/02-followfademarket/02-09-SUMMARY.md`
</output>
