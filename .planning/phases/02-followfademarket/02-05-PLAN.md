---
phase: 02-followfademarket
plan: 05
type: execute
wave: 2
depends_on: [02-04]
files_modified:
  - apps/relayer/src/db/schema.ts
  - apps/relayer/src/db/migrations/
autonomous: false
requirements:
  - SOCIAL-24

must_haves:
  truths:
    - "apps/relayer/src/db/schema.ts exports notifications table with user_address, event_type, call_id, payload, created_at, read_at columns (D-13/D-14)"
    - "apps/relayer/src/db/schema.ts exports quote_stance table with call_id, quote_call_id, stance columns (D-15)"
    - "Drizzle migration SQL exists under apps/relayer/src/db/migrations/ for both tables"
    - "pnpm --filter @call-it/relayer db:migrate succeeds against Fly Postgres (no interactive prompts)"
  artifacts:
    - path: apps/relayer/src/db/schema.ts
      provides: "notifications + quote_stance table DDL with indexes"
      contains: "export const notifications = pgTable|export const quoteStance = pgTable"
    - path: apps/relayer/src/db/migrations/
      provides: "Generated SQL migration file for notifications + quote_stance tables"
  key_links:
    - from: apps/relayer/src/db/schema.ts
      to: apps/relayer/src/workers/notification-fanout.ts
      via: "Drizzle db.insert(notifications).values(rows)"
      pattern: "notifications"
    - from: apps/relayer/src/db/schema.ts
      to: apps/relayer/src/routes/quote-stance.ts
      via: "Drizzle db.insert(quoteStance).values(row)"
      pattern: "quoteStance"
---

<objective>
[BLOCKING] Database schema migration. The notifications and quote_stance tables must exist in Fly Postgres before any relayer worker or route can write to them. This plan is a mandatory gate — relayer notification fan-out and quote-stance verification will silently fail (or crash with PG errors) if the migration has not run.

Purpose: DB schema foundation for caller-exit notifications (D-13/D-14) and quote-call stance (D-15). Generic event_type column is reusable by Phase 3 + 4.
Output: Updated schema.ts + generated migration SQL + confirmed migration run on Fly Postgres.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-followfademarket/02-CONTEXT.md
@.planning/phases/02-followfademarket/02-RESEARCH.md

<interfaces>
<!-- Existing schema pattern to replicate -->

From apps/relayer/src/db/schema.ts (current):
- import { pgTable, serial, varchar, text, timestamp, integer, index } from 'drizzle-orm/pg-core'
- export const addressBook = pgTable('address_book', { id: serial('id').primaryKey(), ... }, (table) => ({ myIdx: index('...').on(table.colA) }))
- Pattern: 3-arg pgTable(name, columns, indexes)
- Add: import { jsonb } from 'drizzle-orm/pg-core' for payload column

From apps/relayer/drizzle.config.ts:
- schema: './src/db/schema.ts'
- out: './src/db/migrations'
- driver / dialect: check current config for exact value

In package.json scripts:
- "db:generate": "drizzle-kit generate"  (or similar)
- "db:migrate": "drizzle-kit migrate"   (or similar)
- Check apps/relayer/package.json for exact script names
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add notifications + quote_stance tables to schema.ts</name>
  <files>apps/relayer/src/db/schema.ts</files>
  <read_first>
    - apps/relayer/src/db/schema.ts (full file — existing imports, addressBook table structure, index naming convention)
    - apps/relayer/drizzle.config.ts (schema path, out path, dialect config)
    - apps/relayer/package.json (db:generate and db:migrate script names — confirm exact commands)
    - .planning/phases/02-followfademarket/02-PATTERNS.md section "apps/relayer/src/db/schema.ts" (exact table DDL with all columns, indexes, comments)
  </read_first>
  <action>
    Edit apps/relayer/src/db/schema.ts to add jsonb to the import line from drizzle-orm/pg-core. Then add two new table exports after the existing tables:

    notifications table (D-13/D-14): columns: id (serial PK), userAddress (varchar 42), eventType (varchar 50 — 'caller_exited' | 'settlement_ready' | 'challenge_proposed' | 'payout_available'), callId (integer), payload (jsonb default {}), createdAt (timestamp defaultNow), readAt (timestamp nullable). Indexes: notificationsUserReadIdx on (userAddress, readAt) for inbox queries; notificationsUserTimeIdx on (userAddress, createdAt) for pagination.

    quoteStance table (D-15): columns: id (serial PK), callId (integer), quoteCallId (integer), stance (varchar 10 — 'following' | 'fading'), createdAt (timestamp defaultNow). Index: quoteStanceQuoteIdx on (quoteCallId).

    Follow the exact pgTable 3-arg pattern from addressBook. Use JSDoc comments explaining generic extensibility (Phase 3/4) per D-14.
  </action>
  <verify>
    <automated>pnpm --filter @call-it/relayer build 2>&1 | grep -c "error" | xargs -I{} test {} -eq 0</automated>
  </verify>
  <done>apps/relayer/src/db/schema.ts exports both notifications and quoteStance tables; TypeScript build passes; jsonb import added; both tables have correct indexes; JSDoc comments reference D-13/D-14/D-15.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Generate + run Drizzle migration against Fly Postgres</name>
  <what-built>
    The notifications and quote_stance tables are defined in schema.ts. Drizzle must generate the SQL migration and apply it to Fly Postgres. This is a BLOCKING gate — all subsequent relayer worker + route verification depends on these tables existing.
  </what-built>
  <how-to-verify>
    1. Generate migration SQL: `pnpm --filter @call-it/relayer db:generate`
       Expected: new migration file appears under apps/relayer/src/db/migrations/ containing CREATE TABLE notifications ... CREATE TABLE quote_stance ...
    2. Inspect the generated SQL: confirm notifications has user_address, event_type, call_id, payload (jsonb), created_at, read_at columns; confirm quote_stance has call_id, quote_call_id, stance, created_at columns.
    3. Apply migration: `pnpm --filter @call-it/relayer db:migrate`
       Expected: migration applies without error; no interactive prompts (if prompted, the migration file must be edited to remove destructive statements and re-run)
    4. Verify tables exist on Fly Postgres: `fly postgres connect -a <fly-postgres-app-name>` then `\dt` — should list notifications and quote_stance
    5. Commit the generated migration SQL file: `git add apps/relayer/src/db/migrations/ && git commit -m "feat(02-05): add notifications + quote_stance tables migration"`
  </how-to-verify>
  <resume-signal>Type "migrated" once the migration has applied successfully and the generated SQL file is committed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Relayer→Fly Postgres | Notification writes from worker; stance writes from route |
| Frontend→Relayer API | Reads notifications via GET /api/notifications?user= |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Information Disclosure | notifications.payload JSONB | accept | Payload contains call metadata (no private keys, no PII beyond public wallet address); user_address is the viewer's own address; inbox is auth-gated in relayer (Privy session check) |
| T-02-05-02 | Tampering | eventType column injection | mitigate | eventType is application-controlled (written by relayer worker, not user input); varchar(50) cap; relayer validates before insert |
| T-02-05-03 | Tampering | Migration applies destructive changes in CI | mitigate | db:migrate (not db:push) uses generated migration files; operator reviews SQL before applying; checkpoint is human-verify |
</threat_model>

<verification>
After both tasks complete:
1. `grep "export const notifications = pgTable" apps/relayer/src/db/schema.ts` — present
2. `grep "export const quoteStance = pgTable" apps/relayer/src/db/schema.ts` — present
3. `ls apps/relayer/src/db/migrations/*.sql` — migration SQL file exists
4. `pnpm --filter @call-it/relayer build` exits 0
5. Migration successfully applied to Fly Postgres (operator confirms)
</verification>

<success_criteria>
- schema.ts compiles with both new tables
- Generated migration SQL file committed to git
- Migration applied to Fly Postgres without errors
- notifications table has user_address, event_type, call_id, payload, created_at, read_at
- quote_stance table has call_id, quote_call_id, stance, created_at
</success_criteria>

<output>
After completion, create `.planning/phases/02-followfademarket/02-05-SUMMARY.md`
</output>
