---
phase: 02-followfademarket
plan: 06
type: execute
wave: 2
depends_on: [02-04]
files_modified:
  - packages/subgraph/subgraph.yaml
  - packages/subgraph/src/follow-fade-market.ts
  - packages/subgraph/schema.graphql
autonomous: true
requirements:
  - SOCIAL-23
  - SOCIAL-44
  - SOCIAL-45

must_haves:
  truths:
    - "subgraph.yaml has FollowFadeMarket data source with 5 event handlers (Followed, Faded, PositionExited, CallerExited, PoolInitialized)"
    - "follow-fade-market.ts implements all 5 handlers with correct Position/CallerExit entity updates"
    - "graph build succeeds; graph deploy to Sepolia Studio succeeds"
    - "subgraph.yaml CallRegistry + ProfileRegistry addresses updated to v2 addresses from Plan 04"
  artifacts:
    - path: packages/subgraph/subgraph.yaml
      provides: "3 data sources (CallRegistry v2, ProfileRegistry v2, FollowFadeMarket) with event handlers"
    - path: packages/subgraph/src/follow-fade-market.ts
      provides: "5 AssemblyScript event handlers for FollowFadeMarket events"
    - path: packages/subgraph/schema.graphql
      provides: "Position, PositionExit, CallerExit entities extended for Phase 2"
  key_links:
    - from: packages/subgraph/src/follow-fade-market.ts
      to: packages/subgraph/schema.graphql
      via: "Position.load(id) + Position.save()"
      pattern: "Position.load|CallerExit.load"
---

<objective>
Subgraph extension for FollowFadeMarket events. The Live Receipt activity feed (D-08) and notification fan-out worker (D-13) both consume subgraph data. This plan deploys the extended subgraph to Sepolia Studio so the activity feed and CallerExited notifications have an indexed data source.

Purpose: Subgraph is the activity-feed and notification-discovery data source. Runs in Wave 2 parallel with Plan 05 (schema migration) since both depend only on Plan 04 (deploy).
Output: Updated subgraph.yaml + follow-fade-market.ts handlers + redeployed Sepolia Studio subgraph.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-followfademarket/02-CONTEXT.md
@.planning/phases/02-followfademarket/02-RESEARCH.md

<interfaces>
<!-- Subgraph patterns to replicate -->

From packages/subgraph/src/call-registry.ts (analog):
- AssemblyScript imports: import { BigInt, Bytes } from '@graphprotocol/graph-ts';
- import { Followed, Faded, PositionExited, CallerExited, PoolInitialized } from '../generated/FollowFadeMarket/FollowFadeMarket'
- import { Position, PositionExit, CallerExit, TvlSnapshot } from '../generated/schema'
- Handler pattern: load-or-create + set fields + save(); NO closures; NO null for value types; BigInt for uint256 via .plus() / .minus(); event.params.user.toHexString() for addresses
- ensureXxx() helper pattern from call-registry.ts lines 27-46

From packages/subgraph/subgraph.yaml (current):
- FollowFadeMarket placeholder block already present (address 0x000..., blockHandlers)
- Replace blockHandlers with eventHandlers; update address + startBlock from Plan 04 deploy
- Also update CallRegistry + ProfileRegistry address + startBlock to v2

From packages/subgraph/schema.graphql (current):
- Position entity may already have stub; extend with: callId String!, user String!, side String!, usdcDeposited BigInt!, sharesHeld BigInt!, entryTime BigInt!, exitedAt BigInt (nullable)
- CallerExit entity: extend with penaltyPaid BigInt!, stakeReturned BigInt!, reputationDelta BigInt!, callerVolumeAtExit BigInt!
- PositionExit entity: callId String!, user String!, side String!, usdcReturned BigInt!, slashAmount BigInt!, timestamp BigInt!
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: schema.graphql extensions + follow-fade-market.ts handlers</name>
  <files>
    packages/subgraph/schema.graphql,
    packages/subgraph/src/follow-fade-market.ts
  </files>
  <read_first>
    - packages/subgraph/schema.graphql (full file — existing entity definitions; check Position/CallerExit/PositionExit stubs)
    - packages/subgraph/src/call-registry.ts (full file — all patterns: imports, ensureProfile helper, handler functions, AssemblyScript constraints)
    - .planning/phases/02-followfademarket/02-RESEARCH.md section "Subgraph Extension" (event handler signatures, extended entities list)
    - .planning/phases/02-followfademarket/02-PATTERNS.md section "follow-fade-market.ts" (exact handler code pattern with IDs, entity field assignments)
    - packages/subgraph/abis/FollowFadeMarket.json (event signatures to validate parameter names)
  </read_first>
  <action>
    Update schema.graphql: extend or add Position entity (fields: id String! PK as "callId-userHex-side", callId String!, user String!, side String! "follow"|"fade", usdcDeposited BigInt!, sharesHeld BigInt!, entryTime BigInt!, exitedAt BigInt nullable); extend CallerExit entity (fields: id = callId, penaltyPaid BigInt!, stakeReturned BigInt!, reputationDelta BigInt!, callerVolumeAtExit BigInt!, timestamp BigInt!); add PositionExit entity (fields: id = callId+"-"+userHex+"-"+timestamp, callId String!, user String!, side String!, usdcReturned BigInt!, slashAmount BigInt!, timestamp BigInt!). Add @entity(immutable:false) on Position and CallerExit (they are updated). Add @entity(immutable:true) on PoolInitialized and PositionExit.

    Create follow-fade-market.ts with 5 handlers:
    - handleFollowed: load/create Position at id = callId+"-"+userHex+"-follow"; update usdcDeposited (add), sharesHeld (add), entryTime; save.
    - handleFaded: same pattern for fade side.
    - handlePositionExited: load Position by id; set exitedAt = event.block.timestamp; save; create PositionExit record.
    - handleCallerExited: load or create CallerExit at id = callId; set all fields; save.
    - handlePoolInitialized: create pool init record (TvlSnapshot entity if present in schema).

    AssemblyScript constraints: no closures, no null for value types, use BigInt.fromI32(0) for zero BigInt, use event.params.callId.toString() for string IDs, use event.params.user.toHexString() for address strings.
  </action>
  <verify>
    <automated>cd packages/subgraph && pnpm graph codegen 2>&1 | tail -10 && pnpm graph build 2>&1 | tail -10</automated>
  </verify>
  <done>graph codegen succeeds (generated/ types updated); graph build succeeds; follow-fade-market.ts has 5 exported handler functions; schema.graphql has Position entity with callId/user/side/usdcDeposited/sharesHeld/entryTime/exitedAt fields; no use of closures or null for value types.</done>
</task>

<task type="auto">
  <name>Task 2: subgraph.yaml update + Sepolia Studio redeploy</name>
  <files>packages/subgraph/subgraph.yaml</files>
  <read_first>
    - packages/subgraph/subgraph.yaml (full file — current CallRegistry/ProfileRegistry data sources; FollowFadeMarket placeholder block)
    - .planning/phases/02-followfademarket/02-PATTERNS.md section "subgraph.yaml" (exact YAML block to replace placeholder blockHandlers)
    - .planning/phases/02-followfademarket/02-04-SUMMARY.md (deployed addresses + startBlocks for all 3 contracts — available after Plan 04 completes)
  </read_first>
  <action>
    Update subgraph.yaml:
    1. Update CallRegistry data source: address → CALL_REGISTRY_ARBITRUM_SEPOLIA_V2 from addresses.ts; startBlock → v2 deploy block from Plan 04.
    2. Update ProfileRegistry data source: same for v2 addresses.
    3. Update FollowFadeMarket data source: replace placeholder address with real Sepolia address; replace blockHandlers with the 5 eventHandlers block from PATTERNS.md; set startBlock to FFM deploy block.

    After updating subgraph.yaml, run `graph codegen && graph build` to validate, then deploy to Sepolia Studio: `pnpm graph deploy --studio call-it-sepolia`. The deploy key comes from GRAPH_STUDIO_DEPLOY_KEY env var (already configured in Phase 1).

    Note: subgraph.yaml changes require all 3 data source addresses to come from Plan 04. If 02-04-SUMMARY.md is not yet committed, use the addresses.ts constants as the canonical source.
  </action>
  <verify>
    <automated>cd packages/subgraph && pnpm graph build 2>&1 | grep -E "Build completed|error" | head -5</automated>
  </verify>
  <done>subgraph.yaml has FollowFadeMarket event handlers (not blockHandlers); CallRegistry + ProfileRegistry addresses match v2 from addresses.ts; graph build succeeds; subgraph deployed to Sepolia Studio (deploy hash logged).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Contract events→Subgraph | AssemblyScript handlers index on-chain events; no user input |
| Subgraph→Relayer | Relayer queries subgraph for notification fan-out; subgraph data is append-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06-01 | Repudiation | OG card stale after CallerExited if subgraph lags | mitigate | Notification fan-out worker (Plan 07) bumps statusVersion in Redis on CallerExited event detection; OG card uses direct RPC reads not subgraph for freshness (D-09) |
| T-02-06-02 | Tampering | Wrong subgraph addresses after redeploy | mitigate | Addresses come from packages/shared/src/constants/addresses.ts (single source of truth); CI grep guard enforces no inline addresses in subgraph.yaml |
| T-02-06-03 | Denial of Service | Subgraph indexer lag during high activity | accept | Phase 1 D-24 polled-events fallback covers indexer lag; 800ms race timeout already wired |
</threat_model>

<verification>
After both tasks complete:
1. `pnpm graph build` exits 0 (from packages/subgraph/)
2. `grep -A 20 "name: FollowFadeMarket" packages/subgraph/subgraph.yaml` shows eventHandlers (not blockHandlers)
3. `grep "handleFollowed\|handleFaded\|handlePositionExited\|handleCallerExited\|handlePoolInitialized" packages/subgraph/src/follow-fade-market.ts` — all 5 handlers present
4. Sepolia Studio deployment link printed in console
5. `grep "callId.*String" packages/subgraph/schema.graphql` — Position entity has callId field
</verification>

<success_criteria>
- graph build exits 0
- subgraph.yaml has real Sepolia FFM address (not placeholder) with 5 event handlers
- follow-fade-market.ts has 5 exported functions; no closures; no null for value types
- schema.graphql has Position entity with required fields
- Subgraph deployed to Sepolia Studio
</success_criteria>

<output>
After completion, create `.planning/phases/02-followfademarket/02-06-SUMMARY.md`
</output>
