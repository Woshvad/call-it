---
phase: 02-followfademarket
plan: 04
type: execute
wave: 1
depends_on: [02-03]
files_modified:
  - packages/contracts/script/DeployPhase2.s.sol
  - packages/shared/src/constants/addresses.ts
  - packages/subgraph/abis/FollowFadeMarket.json
autonomous: false
requirements:
  - SOCIAL-09
  - SOCIAL-21

user_setup:
  - service: arbitrum-sepolia-rpc
    why: "Deploy 3 contracts to Arbitrum Sepolia via DeployPhase2.s.sol"
    env_vars:
      - name: DEPLOYER_PRIVATE_KEY
        source: "Hardcoded deployer key from Phase 1 deploy (already in GCP Secret Manager as DEPLOYER_PRIVATE_KEY)"
    dashboard_config:
      - task: "Verify contracts on Arbiscan Sepolia post-deploy"
        location: "Arbiscan Sepolia → forge verify-contract after deploy"

must_haves:
  truths:
    - "DeployPhase2.s.sol deploys 3 contracts in correct order and wires all setters"
    - "addresses.ts has FOLLOW_FADE_MARKET_ARBITRUM_SEPOLIA and updated v2 addresses for CallRegistry + ProfileRegistry"
    - "subgraph/abis/FollowFadeMarket.json ABI exported from forge build output"
    - "Post-deploy assertions pass: callRegistry.followFadeMarket() == ffm address; profileRegistry.authorizedRepWriters(ffm) == true"
    - "A follow() transaction on the deployed Sepolia FollowFadeMarket emits a Followed event and mints shares (verifiable via: cast send  'follow(uint256,uint256,uint256)' 1 1000000 0 --rpc-url  --private-key , then cast logs confirming Followed event)"
  artifacts:
    - path: packages/contracts/script/DeployPhase2.s.sol
      provides: "9-step deploy script: ProfileRegistry v2 → CallRegistry v2 → FollowFadeMarket → setFollowFadeMarket → setTvlCap → addAsset×25 + addNFTCollection×6 → setAuthorizedRepWriter(ffm,true) → assertions"
    - path: packages/shared/src/constants/addresses.ts
      provides: "FOLLOW_FADE_MARKET_ARBITRUM_SEPOLIA constant + updated CALL_REGISTRY_ARBITRUM_SEPOLIA_V2 + PROFILE_REGISTRY_ARBITRUM_SEPOLIA_V2"
    - path: packages/subgraph/abis/FollowFadeMarket.json
      provides: "Compiled ABI from forge build output"
  key_links:
    - from: packages/contracts/script/DeployPhase2.s.sol
      to: packages/shared/src/constants/addresses.ts
      via: "Deploy script console.log instructs operator to update addresses.ts"
      pattern: "FOLLOW_FADE_MARKET"
---

<objective>
Deploy script + ABI export + address constants. This is the vertical slice that turns 3 compiled contracts into a running Sepolia deployment. The checkpoint task verifies the 3 deployed contract addresses, asset allowlist re-population, and TVL cap before any frontend reads them.

Purpose: Ship the contracts to Sepolia Testnet. Single-operator deploy gate.
Output: DeployPhase2.s.sol + addresses.ts update + FollowFadeMarket.json ABI + Sepolia deployment checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-followfademarket/02-CONTEXT.md
@.planning/phases/02-followfademarket/02-RESEARCH.md
@.planning/phases/02-followfademarket/02-PATTERNS.md

<interfaces>
<!-- Key patterns to replicate -->

From packages/contracts/script/DeployPhase1.s.sol (analog):
- Full SPDX + pragma + comment block header
- vm.envUint("DEPLOYER_PRIVATE_KEY") pattern
- vm.startBroadcast / vm.stopBroadcast
- Post-deploy assertions: require(callRegistry.tvlCap() == INITIAL_TVL_CAP, "tvlCap mismatch")
- console.log summary block

From packages/shared/src/constants/addresses.ts (current):
- export const FOLLOW_FADE_MARKET_ARBITRUM_SEPOLIA = '0x...' as const; pattern
- DEPLOYED comment block with date, tx hash, deploy block, post-deploy smoke test results

Asset allowlist to re-populate: all 25 coins + 6 NFT collections from CallRegistry Phase 1
(read from packages/contracts/script/DeployPhase1.s.sol to get the exact feed IDs and addresses)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DeployPhase2.s.sol + ABI export</name>
  <files>
    packages/contracts/script/DeployPhase2.s.sol,
    packages/subgraph/abis/FollowFadeMarket.json
  </files>
  <read_first>
    - packages/contracts/script/DeployPhase1.s.sol (full file — script structure, asset allowlist loop, post-deploy assertions, console.log pattern)
    - packages/contracts/src/CallRegistry.sol (constants: initial tvlCap = 5_000_000_000 = 5000e6; INITIAL_TVL_CAP)
    - .planning/phases/02-followfademarket/02-RESEARCH.md section "Deploy Script Shape (DeployPhase2.s.sol)"
    - .planning/phases/02-followfademarket/02-PATTERNS.md section "DeployPhase2.s.sol"
  </read_first>
  <action>
    Create DeployPhase2.s.sol following DeployPhase1.s.sol exactly. Deploy order per D-01/D-04:
    1. Deploy ProfileRegistry v2 (constructor: Ownable(msg.sender))
    2. Deploy CallRegistry v2 (constructor: IProfileRegistry(address(profileRegistry)), 5_000e6)
    3. Deploy FollowFadeMarket (constructor: ICallRegistry(address(callRegistry)), IProfileRegistry(address(profileRegistry)), TREASURY_ADDRESS, 5_000e6)
    4. callRegistry.setFollowFadeMarket(address(followFadeMarket))
    5. callRegistry.setTvlCap(5_000e6) — redundant but explicit
    6. Re-populate asset allowlist: all 25 coins from Phase 1 (copy the exact addAsset calls from DeployPhase1.s.sol)
    7. Re-populate NFT collections: 6 collections (copy from DeployPhase1.s.sol)
    8. profileRegistry.setAuthorizedRepWriter(address(followFadeMarket), true)
    9. Post-deploy assertions: require(callRegistry.followFadeMarket() == address(followFadeMarket)); require(profileRegistry.authorizedRepWriters(address(followFadeMarket)) == true); require(callRegistry.tvlCap() == 5_000e6); require(callRegistry.currentTvl() == 0)

    TREASURY_ADDRESS: use vm.envAddress("TREASURY_ADDRESS") — operator must set in .env.

    Console.log summary: print all 3 addresses with deployment notes + "Update packages/shared/src/constants/addresses.ts and packages/subgraph/subgraph.yaml with these addresses."

    ABI export: after `forge build`, copy packages/contracts/out/FollowFadeMarket.sol/FollowFadeMarket.json to packages/subgraph/abis/FollowFadeMarket.json. Run `forge build` then copy. This can be done as a shell step in the task (not a separate Solidity script).
  </action>
  <verify>
    <automated>forge build --match-path packages/contracts/script/DeployPhase2.s.sol 2>&1 | grep -c "error" | xargs -I{} test {} -eq 0 && ls packages/subgraph/abis/FollowFadeMarket.json</automated>
  </verify>
  <done>DeployPhase2.s.sol compiles; FollowFadeMarket.json ABI file exists under packages/subgraph/abis/; script contains all 9 deploy steps with post-deploy assertions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [OPERATOR] Deploy to Arbitrum Sepolia + update addresses.ts</name>
  <what-built>
    DeployPhase2.s.sol is ready to deploy 3 contracts to Arbitrum Sepolia. Once deployed, the 3 Sepolia addresses must be recorded in packages/shared/src/constants/addresses.ts and packages/subgraph/subgraph.yaml before Phase 2 execution can continue.
  </what-built>
  <how-to-verify>
    1. Run deploy: `forge script packages/contracts/script/DeployPhase2.s.sol --rpc-url $ARBITRUM_SEPOLIA_RPC --broadcast --verify 2>&1 | tee deploy-phase2.log`
    2. Copy the 3 contract addresses from deploy-phase2.log output
    3. Update packages/shared/src/constants/addresses.ts: set FOLLOW_FADE_MARKET_ARBITRUM_SEPOLIA, CALL_REGISTRY_ARBITRUM_SEPOLIA (v2), PROFILE_REGISTRY_ARBITRUM_SEPOLIA (v2) to real deployed addresses
    4. Update packages/subgraph/subgraph.yaml: set CallRegistry address + startBlock, ProfileRegistry address + startBlock, FollowFadeMarket address + startBlock (all to Sepolia values from the deploy log)
    5. Verify on Sepolia: `cast call $CALL_REGISTRY_V2 "followFadeMarket()(address)" --rpc-url $ARBITRUM_SEPOLIA_RPC` returns FFM address; `cast call $PROFILE_REGISTRY_V2 "authorizedRepWriters(address)(bool)" $FFM_ADDR --rpc-url $ARBITRUM_SEPOLIA_RPC` returns true; `cast call $CALL_REGISTRY_V2 "tvlCap()(uint256)" --rpc-url $ARBITRUM_SEPOLIA_RPC` returns 5000000000 (5000e6)
    6. Commit: addresses.ts + subgraph.yaml updates committed to git
  </how-to-verify>
  <resume-signal>Type "deployed" with the 3 contract addresses once addresses.ts and subgraph.yaml are updated and committed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator→Sepolia RPC | Deployer key signs 3 contract deployments + N setter txs |
| addresses.ts→Frontend/Subgraph | Wrong address = all reads/writes to wrong contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Tampering | Wrong contract address in addresses.ts | mitigate | Post-deploy cast call verification in checkpoint task; CI grep guard enforces single source (no inline addresses) |
| T-02-04-02 | Tampering | Asset allowlist not re-populated after redeploy | mitigate | DeployPhase2.s.sol includes addAsset loops + post-deploy assertion on assetAllowlist count; operator smoke test in checkpoint |
| T-02-04-03 | Repudiation | Deploy log not preserved | accept | tee deploy-phase2.log captures broadcast output; addresses recorded in addresses.ts with DEPLOYED comment |
</threat_model>

<verification>
After both tasks complete (including human checkpoint):
1. `cast call $CALL_REGISTRY_V2 "followFadeMarket()(address)" --rpc-url $ARBITRUM_SEPOLIA_RPC` returns the FFM address
2. `cast call $PROFILE_REGISTRY_V2 "authorizedRepWriters(address)(bool)" $FFM_ADDR --rpc-url $ARBITRUM_SEPOLIA_RPC` returns true
3. `grep "FOLLOW_FADE_MARKET_ARBITRUM_SEPOLIA" packages/shared/src/constants/addresses.ts` returns a non-zero address (not 0x000...)
4. `grep "FollowFadeMarket" packages/subgraph/subgraph.yaml` shows correct address entry (not placeholder 0x000...)
5. `ls packages/subgraph/abis/FollowFadeMarket.json` exists
</verification>

<success_criteria>
- DeployPhase2.s.sol compiles and deploys to Sepolia successfully
- 3 real contract addresses in addresses.ts + subgraph.yaml
- cast call verifications pass on Sepolia
- FollowFadeMarket.json ABI present in subgraph/abis/
- Commit with addresses recorded
</success_criteria>

<output>
After completion, create `.planning/phases/02-followfademarket/02-04-SUMMARY.md`
</output>
