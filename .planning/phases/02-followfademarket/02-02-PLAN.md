---
phase: 02-followfademarket
plan: 02
type: execute
wave: 1
depends_on: [02-01]
files_modified:
  - packages/contracts/src/interfaces/IFollowFadeMarket.sol
  - packages/contracts/src/FollowFadeMarket.sol
autonomous: true
requirements:
  - SOCIAL-01
  - SOCIAL-02
  - SOCIAL-03
  - SOCIAL-04
  - SOCIAL-05
  - SOCIAL-06
  - SOCIAL-07
  - SOCIAL-08
  - SOCIAL-09
  - SOCIAL-10
  - SOCIAL-11
  - SOCIAL-12
  - SOCIAL-13
  - SOCIAL-14
  - SOCIAL-15
  - SOCIAL-16
  - SOCIAL-17
  - SOCIAL-18
  - SOCIAL-19
  - SOCIAL-20
  - SOCIAL-21
  - SOCIAL-22
  - SOCIAL-26
  - SOCIAL-27
  - SOCIAL-28

must_haves:
  truths:
    - "forge test --match-contract FollowFadeMarket -v exits 0 (Wave 0 RED tests turn GREEN)"
    - "FollowFadeMarket.sol compiles with pragma =0.8.30 and all OZ imports"
    - "AMM follow/fade mints shares via totalShares * amountIn / (reserve + amountIn)"
    - "Penalty injection adds USDC to reserve without minting shares (k grows)"
    - "callerExit applies rep slash via profileRegistry.applyRepDelta in same tx"
    - "exitPosition is NOT guarded by whenNotPaused (pause carve-out per §10.3)"
  artifacts:
    - path: packages/contracts/src/interfaces/IFollowFadeMarket.sol
      provides: "§12.2 interface: follow/fade/exitPosition/callerExit/initPool/claimPayout/getTvl signatures + errors + events"
      exports: ["IFollowFadeMarket"]
    - path: packages/contracts/src/FollowFadeMarket.sol
      provides: "Single-contract AMM with per-callId sub-state (§11.2)"
      min_lines: 350
  key_links:
    - from: packages/contracts/src/FollowFadeMarket.sol
      to: packages/contracts/src/interfaces/ICallRegistry.sol
      via: "ICallRegistry(callRegistry).markCallerExited(callId)"
      pattern: "markCallerExited"
    - from: packages/contracts/src/FollowFadeMarket.sol
      to: packages/contracts/src/interfaces/IProfileRegistry.sol
      via: "IProfileRegistry(profileRegistry).applyRepDelta(caller, delta)"
      pattern: "applyRepDelta"
---

<objective>
The FollowFadeMarket AMM contract — the financial heart of Phase 2. Ships the constant-product AMM with per-callId sub-state, all follow/fade/exit mechanics, penalty injection, TVL aggregation, caller-exit rep slash, and the complete IFollowFadeMarket interface. After this plan, all Wave 0 RED tests turn GREEN.

Purpose: Core money contract. Every dollar of follow/fade USDC lives here.
Output: IFollowFadeMarket.sol (interface) + FollowFadeMarket.sol (implementation). Tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-followfademarket/02-CONTEXT.md
@.planning/phases/02-followfademarket/02-RESEARCH.md
@.planning/phases/02-followfademarket/02-PATTERNS.md

<interfaces>
<!-- Patterns to replicate from codebase -->

From packages/contracts/src/CallRegistry.sol:
pragma solidity =0.8.30 (EXACT)
import { IERC20 } from "@openzeppelin/contracts/token/ERC20/IERC20.sol";
import { SafeERC20 } from "@openzeppelin/contracts/token/ERC20/utils/SafeERC20.sol";
import { ReentrancyGuard } from "@openzeppelin/contracts/utils/ReentrancyGuard.sol";
import { Ownable2Step, Ownable } from "@openzeppelin/contracts/access/Ownable2Step.sol";
import { Pausable } from "@openzeppelin/contracts/utils/Pausable.sol";
import { USDC_ARB_NATIVE } from "./constants/USDC.sol";
contract CallRegistry is Ownable2Step, ReentrancyGuard, Pausable, ICallRegistry { using SafeERC20 for IERC20; }

CEI pattern (from CallRegistry.sol lines 246-278):
// EFFECTS: ALL state writes FIRST
// INTERACTIONS: safeTransferFrom LAST

From packages/contracts/src/interfaces/ICallRegistry.sol (lines 14-47):
enum Side { Follow, Fade }
/// @notice Emitted on ... SOCIAL-XX.
event Followed(uint256 indexed callId, address indexed user, uint256 amountIn, uint256 sharesOut);
error SlippageExceeded(uint256 minOut, uint256 actualOut);

From packages/contracts/src/ProfileRegistry.sol:
New applyRepDelta + setAuthorizedRepWriter will be created in Plan 03;
IFollowFadeMarket.sol must declare initPool so CallRegistry can call it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: IFollowFadeMarket interface</name>
  <files>packages/contracts/src/interfaces/IFollowFadeMarket.sol</files>
  <read_first>
    - packages/contracts/src/interfaces/ICallRegistry.sol (full file — error NatSpec, event indexed placement, enum pattern, function signatures)
    - CALL_IT_SPEC1.md §12.2 (IFollowFadeMarket function signatures, error names, event shapes)
    - .planning/phases/02-followfademarket/02-PATTERNS.md section "IFollowFadeMarket.sol" (file header, enum Side, event signatures, error declarations)
    - .planning/phases/02-followfademarket/02-RESEARCH.md Pattern 8 (storage shape — flat mappings)
  </read_first>
  <behavior>
    - pragma solidity =0.8.30 (EXACT pin comment required)
    - enum Side { Follow, Fade }
    - Events (all indexed per pattern): Followed(uint256 indexed callId, address indexed user, uint256 amountIn, uint256 sharesOut), Faded(uint256 indexed callId, address indexed user, uint256 amountIn, uint256 sharesOut), PositionExited(uint256 indexed callId, address indexed user, Side side, uint256 usdcReturned, uint256 slashAmount), CallerExited(uint256 indexed callId, address indexed caller, uint64 timeElapsed, uint256 penaltyPaid, uint256 stakeReturned, int256 reputationDelta), PoolInitialized(uint256 indexed callId, uint256 stakeAmount, uint256 virtualFadeSeed)
    - Errors (with NatSpec): SlippageExceeded(uint256 minOut, uint256 actualOut), CallPastExpiry(), ExitCooldownActive(uint64 unlocksAt), CallerExitLocked(uint64 unlocksAt), TvlCapReached(uint256 requested, uint256 available), PositionBelowMinimum(), PositionAboveMaximum(), NotAuthorized(), CallNotLive(), NotCallerOfCall()
    - Functions: initPool(uint256 callId, uint256 stakeAmount, uint256 virtualFadeSeed) external, follow(uint256 callId, uint256 amountIn, uint256 minSharesOut) external, fade(uint256 callId, uint256 amountIn, uint256 minSharesOut) external, exitPosition(uint256 callId, Side side) external, callerExit(uint256 callId) external, claimPayout(uint256 callId) external, getTvl() external view returns (uint256), followReserve(uint256 callId) external view returns (uint256), fadeReserve(uint256 callId) external view returns (uint256), followTotalShares(uint256 callId) external view returns (uint256), fadeTotalShares(uint256 callId) external view returns (uint256), followShares(uint256 callId, address user) external view returns (uint256), fadeShares(uint256 callId, address user) external view returns (uint256)
  </behavior>
  <action>
    Create IFollowFadeMarket.sol. Copy the file header comment block from ICallRegistry.sol lines 1-7 verbatim; update spec references to §11.2, §12.2. Declare all errors, events, and function signatures per the behavior list above. Add NatSpec /// @notice on each item. No implementation code — interface only.
  </action>
  <verify>
    <automated>forge build --match-path packages/contracts/src/interfaces/IFollowFadeMarket.sol 2>&1 | grep -c "error" | xargs -I{} test {} -eq 0</automated>
  </verify>
  <done>IFollowFadeMarket.sol compiles without errors; contains enum Side, 5 events, 10 errors, 13 function signatures; uses pragma =0.8.30 EXACT.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FollowFadeMarket contract (GREEN)</name>
  <files>packages/contracts/src/FollowFadeMarket.sol</files>
  <read_first>
    - packages/contracts/src/CallRegistry.sol (full file — CEI pattern, constructor, ReentrancyGuard usage, SafeERC20, Pausable, constants block, onlyOwner setters, error pattern)
    - packages/contracts/src/interfaces/IFollowFadeMarket.sol (just created — all function signatures to implement)
    - packages/contracts/src/interfaces/ICallRegistry.sol (ICallRegistry.getCall() return type, CallStatus enum values: Live, CallerExited, Settled)
    - packages/contracts/src/interfaces/IProfileRegistry.sol (applyRepDelta signature — will be added in Plan 03; plan ahead)
    - .planning/phases/02-followfademarket/02-RESEARCH.md (ALL AMM patterns 1-8; pitfalls 1-7; all formulas with precise Solidity code)
    - .planning/phases/02-followfademarket/02-PATTERNS.md section "FollowFadeMarket.sol" (imports block, contract declaration, auth guard, CEI, pause carve-out)
  </read_first>
  <behavior>
    - pragma solidity =0.8.30 EXACT; import Math from OZ for mulDiv; USDC_ARB_NATIVE from ./constants/USDC.sol; add import { Math } from "@openzeppelin/contracts/utils/math/Math.sol"
    - Constants: SHARE_PRECISION = 1e18, MIN_POSITION = 1e6, MAX_POSITION = 100e6, POSITION_EXIT_PENALTY_PCT = 10, POSITION_EXIT_COOLDOWN = 4 hours, CALLER_EXIT_LOCK_DURATION = 24 hours, CALLER_EXIT_BASE_PCT = 15, CALLER_EXIT_VARIABLE_PCT = 35, INITIAL_SHARE_PRICE = 1e12
    - Storage mappings per Research Pattern 8: followReserve[callId], fadeReserve[callId], fadeSeedVirtual[callId] (uint256 — stores the virtual seed amount, NOT a bool), followTotalShares[callId], fadeTotalShares[callId], callerVolumeAtExit[callId], callerExitedAt[callId] (uint64), followShares[callId][user], fadeShares[callId][user], followPosition[callId][user], fadePosition[callId][user], followEntryTime[callId][user], fadeEntryTime[callId][user], claimed[callId][user] (bool)
    - initPool(callId, stakeAmount, virtualSeed): callable only by callRegistry address (NotAuthorized guard); sets followReserve[callId]=stakeAmount, fadeReserve[callId]=virtualSeed, fadeSeedVirtual[callId]=virtualSeed, followTotalShares[callId]=stakeAmount*SHARE_PRECISION/INITIAL_SHARE_PRICE, fadeTotalShares[callId]=virtualSeed*SHARE_PRECISION/INITIAL_SHARE_PRICE; emits PoolInitialized. DOES NOT transfer USDC (stake already transferred by CallRegistry).
    - follow/fade: nonReentrant + whenNotPaused; gate: callRegistry.getCall(callId).status must be Live or CallerExited (SOCIAL-08); gate: block.timestamp < call.expiry (STRICT <, Pitfall 10), revert CallPastExpiry; gate: amountIn >= MIN_POSITION, revert PositionBelowMinimum; gate: positionSize[callId][user] + amountIn <= MAX_POSITION, revert PositionAboveMaximum; TVL cap check: callRegistry.currentTvl() + getTvl() + amountIn > tvlCap → revert TvlCapReached; compute sharesOut via Math.mulDiv(totalShares, amountIn, reserve + amountIn); if sharesOut < minSharesOut revert SlippageExceeded; EFFECTS: update reserve, totalShares, userShares, positionSize, entryTime (SOCIAL-10: reset on additive); INTERACTIONS: IERC20(USDC_ARB_NATIVE).safeTransferFrom(msg.sender, address(this), amountIn); emit Followed/Faded.
    - exitPosition(callId, side): nonReentrant — NOT whenNotPaused (pause carve-out §10.3); gate: call status must be Live or CallerExited, revert CallNotLive; gate: entryTime[callId][user] + POSITION_EXIT_COOLDOWN <= block.timestamp, revert ExitCooldownActive; compute positionUsdcValue from shares * reserve / totalShares; slash = positionUsdcValue * POSITION_EXIT_PENALTY_PCT / 100; userReceives = positionUsdcValue - slash; toOpposite = slash * 50 / 100; toSameSide = slash * 40 / 100; toTreasury = slash - toOpposite - toSameSide (subtraction avoids dust, Pitfall 3); EFFECTS: burn shares, inject toOpposite into opposite reserve, inject toSameSide into same reserve (NO new shares — k grows per SOCIAL-11); INTERACTIONS: safeTransfer(msg.sender, userReceives); safeTransfer(treasury, toTreasury); emit PositionExited.
    - callerExit(callId): nonReentrant + whenNotPaused; gate: msg.sender == callRegistry.getCall(callId).caller, revert NotCallerOfCall; gate: block.timestamp >= call.createdAt + CALLER_EXIT_LOCK_DURATION, revert CallerExitLocked; compute penaltyPct via _callerExitPenaltyPct (Research Pattern 5: 15 + 35*remaining/total, floor 15%); slash = callerStakeAmount * penaltyPct / 100; callerStakeAmount = followShares[callId][caller] * followReserve[callId] / followTotalShares[callId]; snapshot callerVolumeAtExit[callId] = followReserve[callId] + (fadeReserve[callId] - fadeSeedVirtual[callId]) (SOCIAL-20; excludes virtual seed per Pitfall 7); split: toFollow = slash * 50 / 100, toFade = slash * 40 / 100, toTreasury = slash - toFollow - toFade; EFFECTS: inject into reserves, burn caller shares, set callerExitedAt[callId]; compute repDelta via _callerExitRepDelta (Research Pattern 6: -(45 - 35*elapsed/duration), floor -10); INTERACTIONS: callRegistry.markCallerExited(callId); profileRegistry.applyRepDelta(caller, repDelta); safeTransfer(msg.sender, userReceives); safeTransfer(treasury, toTreasury); emit CallerExited.
    - claimPayout(callId): nonReentrant — NOT whenNotPaused; interface stub only (full logic in Phase 4 with SettlementManager); for now: revert with a descriptive error "ClaimRequiresSettlement" if called. SOCIAL-46 will be wired by SettlementManager in Phase 4.
    - getTvl(): returns IERC20(USDC_ARB_NATIVE).balanceOf(address(this)) — real USDC only; virtual seed never transferred so not counted.
    - Constructor: Ownable(msg.sender), takes ICallRegistry _callRegistry, IProfileRegistry _profileRegistry, address _treasury, uint256 _tvlCap; validate _tvlCap <= MAX_ALLOWED_CAP = 100_000e6.
    - Owner setters: setTreasury(address), setTvlCap(uint256), setCallRegistry(address) (for post-deploy wiring flexibility).
  </behavior>
  <action>
    Create FollowFadeMarket.sol implementing IFollowFadeMarket. Follow the CEI pattern from CallRegistry.sol exactly — ALL state writes BEFORE any safeTransfer calls. Apply ReentrancyGuard on every USDC-touching function. Apply whenNotPaused on follow/fade/callerExit/initPool but NOT on exitPosition/claimPayout (pause carve-outs per §10.3).

    IProfileRegistry currently lacks applyRepDelta — the callerExit function must call it via the interface. Plan 03 adds this method to the deployed ProfileRegistry; use a forward declaration `IProfileRegistry(profileRegistry).applyRepDelta(caller, repDelta)`. If the interface doesn't yet have it, add it to IProfileRegistry.sol as part of this task (it's needed for this contract to compile).

    Never use `block.timestamp <= call.expiry` — always strict `<` (Pitfall 10).
    Never mint shares on penalty injection (Pitfall 9 §11.2 violation).
    Treasury must be a separate address — NEVER address(this) (Pitfall re TVL accounting).
  </action>
  <verify>
    <automated>forge test --match-contract "FollowFadeMarket$" -v 2>&1 | tail -30</automated>
  </verify>
  <done>FollowFadeMarket.sol compiles; forge test --match-contract "FollowFadeMarket$" exits 0 with all unit tests GREEN; invariant tests in FollowFadeMarketGates also compile; callerExit calls markCallerExited + applyRepDelta; exitPosition has no whenNotPaused modifier; getTvl() returns balanceOf(this).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User→FollowFadeMarket | Untrusted USDC deposits (follow/fade) and exit calls |
| CallRegistry→FollowFadeMarket | initPool called only by authorized CallRegistry |
| FollowFadeMarket→CallRegistry | markCallerExited — must be rejected if not FFM |
| FollowFadeMarket→ProfileRegistry | applyRepDelta — must be rejected if not in authorizedRepWriters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Tampering | Post-expiry free-roll deposit (Pitfall 10) | mitigate | `block.timestamp < call.expiry` strict `<` in follow/fade; test_expiryGateStrictLessThan in Wave 0 confirms off-by-one |
| T-02-02-02 | Tampering | Reentrancy via USDC callback | mitigate | `nonReentrant` on all USDC-touching functions (follow, fade, exitPosition, callerExit, initPool); CEI ordering with all state writes before safeTransfer |
| T-02-02-03 | Tampering | TVL cap bypass via split deposits | mitigate | TVL cap check inside `nonReentrant`; combinedTvl = callRegistry.currentTvl() + getTvl() read atomically in single tx |
| T-02-02-04 | Spoofing | Unauthorized markCallerExited (Pitfall 2) | mitigate | `require(msg.sender == callRegistry)` in initPool + callerExit path; markCallerExited on CallRegistry has `require(msg.sender == followFadeMarket)` guard |
| T-02-02-05 | Elevation | Malicious applyRepDelta caller | mitigate | ProfileRegistry authorizedRepWriters mapping (Plan 03); only FFM authorized in Phase 2; SettlementManager joins in Phase 4 |
| T-02-02-06 | Tampering | Phantom shares on penalty injection (§11.2 violation) | mitigate | Penalty injection adds to reserve only; `totalShares` NOT incremented; invariant_kNeverShrinks catches any violation |
| T-02-02-07 | Tampering | callerVolumeAtExit inflated by virtual seed (Pitfall 7) | mitigate | `callerVolumeAtExit = followReserve + (fadeReserve - fadeSeedVirtual)` — virtual seed subtracted |
| T-02-02-08 | Tampering | Dust from 50/40/10 integer division | mitigate | `toTreasury = slash - toOpposite - toSameSide` (subtraction absorbs rounding dust) |
</threat_model>

<verification>
After both tasks complete:
1. `forge build --match-path packages/contracts/src/FollowFadeMarket.sol` exits 0
2. `forge test --match-contract "FollowFadeMarket$" -v` exits 0 (all Wave 0 unit tests GREEN)
3. `forge test --match-contract FollowFadeMarketGates -v` exits 0
4. `grep -c "whenNotPaused" packages/contracts/src/FollowFadeMarket.sol` — exitPosition and claimPayout do NOT appear in that grep
5. `grep "balanceOf(address(this))" packages/contracts/src/FollowFadeMarket.sol` — getTvl uses balanceOf
6. `grep "CALLER_EXIT_BASE_PCT\|15.*35.*time" packages/contracts/src/FollowFadeMarket.sol` — penalty formula present
7. `grep "applyRepDelta\|markCallerExited" packages/contracts/src/FollowFadeMarket.sol` — both cross-contract calls present
</verification>

<success_criteria>
- IFollowFadeMarket.sol compiles with all errors, events, and function signatures
- FollowFadeMarket.sol compiles with pragma =0.8.30
- forge test --match-contract "FollowFadeMarket$" exits 0
- forge test --match-contract "FollowFadeMarketGates" exits 0
- exitPosition has no whenNotPaused modifier (pause carve-out verified)
- getTvl() uses USDC.balanceOf(address(this)) not a counter
- Penalty injection uses subtraction for last split term (no dust)
</success_criteria>

<output>
After completion, create `.planning/phases/02-followfademarket/02-02-SUMMARY.md`
</output>
