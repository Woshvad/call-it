---
phase: 02-followfademarket
plan: 03
type: execute
wave: 1
depends_on: [02-02]
files_modified:
  - packages/contracts/src/CallRegistry.sol
  - packages/contracts/src/interfaces/ICallRegistry.sol
  - packages/contracts/src/ProfileRegistry.sol
  - packages/contracts/src/interfaces/IProfileRegistry.sol
autonomous: true
requirements:
  - SOCIAL-09
  - SOCIAL-21
  - SOCIAL-23
  - SOCIAL-24
  - SOCIAL-25
  - SOCIAL-26
  - SOCIAL-27

must_haves:
  truths:
    - "createCall forwards caller stake to FollowFadeMarket (USDC.balanceOf(callRegistry) == 0 after createCall)"
    - "markCallerExited(callId) callable only by followFadeMarket address (NotAuthorized revert otherwise)"
    - "markSettled(callId, outcome) callable only by settlementManager address"
    - "ProfileRegistry has authorizedRepWriters mapping; applyRepDelta reverts NotAuthorizedWriter if caller not in set"
    - "forge test --match-contract TvlAggregation exits 0"
  artifacts:
    - path: packages/contracts/src/CallRegistry.sol
      provides: "Redeployed with stake-forward to FFM (D-01), markCallerExited + markSettled guards (D-02), setFollowFadeMarket setter"
      contains: "setFollowFadeMarket|markCallerExited|markSettled"
    - path: packages/contracts/src/interfaces/ICallRegistry.sol
      provides: "Updated interface with CallerExited status, callerExitedAt field, new function signatures"
    - path: packages/contracts/src/ProfileRegistry.sol
      provides: "Redeployed with authorizedRepWriters mapping, applyRepDelta, setAuthorizedRepWriter (D-04/D-05)"
      contains: "authorizedRepWriters|applyRepDelta"
    - path: packages/contracts/src/interfaces/IProfileRegistry.sol
      provides: "Updated with applyRepDelta + setAuthorizedRepWriter signatures"
  key_links:
    - from: packages/contracts/src/CallRegistry.sol
      to: packages/contracts/src/interfaces/IFollowFadeMarket.sol
      via: "IFollowFadeMarket(followFadeMarket).initPool(callId, stake, virtualFadeSeed)"
      pattern: "initPool"
    - from: packages/contracts/src/FollowFadeMarket.sol
      to: packages/contracts/src/CallRegistry.sol
      via: "callRegistry.markCallerExited(callId)"
      pattern: "markCallerExited"
---

<objective>
Redeploy diffs for CallRegistry (stake-forward, authorization surface) and ProfileRegistry (authorizedRepWriters + applyRepDelta). These are the single-custodian and authorized-rep-writer pivots that make Phase 4 SettlementManager a pure plug-in. After this plan, the full 3-contract stack can be deployed via DeployPhase2.s.sol (Plan 04).

Purpose: Phase-4-ready authorization surface; D-01 single-custodian; D-02 guarded status transitions; D-04 generic rep-writer set.
Output: Modified CallRegistry.sol + ProfileRegistry.sol + updated interfaces.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-followfademarket/02-CONTEXT.md
@.planning/phases/02-followfademarket/02-RESEARCH.md
@.planning/phases/02-followfademarket/02-PATTERNS.md

<interfaces>
<!-- Key patterns to replicate — executor MUST read these files -->

From packages/contracts/src/CallRegistry.sol lines 240-285 (createCall interaction block):
// INTERACTIONS: token pull LAST (CEI, SAFETY-05, SAFETY-14)
IERC20(USDC_ARB_NATIVE).safeTransferFrom(msg.sender, address(this), incoming);

From packages/contracts/src/CallRegistry.sol lines 350-385 (setSettlementManager pattern):
function setSettlementManager(address newManager) external onlyOwner {
    settlementManager = newManager;
    emit SettlementManagerSet(newManager);
}

From packages/contracts/src/ProfileRegistry.sol lines 102-130 (storage + setters):
address public relayer;
function setRelayer(address newRelayer) external onlyOwner { relayer = newRelayer; }

CallStatus enum in ICallRegistry.sol: Live, Settled — need to ADD CallerExited
Call struct needs: uint64 callerExitedAt field
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CallRegistry redeploy diff — stake-forward + authorization surface</name>
  <files>
    packages/contracts/src/CallRegistry.sol,
    packages/contracts/src/interfaces/ICallRegistry.sol
  </files>
  <read_first>
    - packages/contracts/src/CallRegistry.sol (full file — all constants, createCall function lines 174-285, setSettlementManager lines 350-374, existing Call struct)
    - packages/contracts/src/interfaces/ICallRegistry.sol (full file — CallStatus enum, Call struct layout with slot comments, existing events, errors)
    - .planning/phases/02-followfademarket/02-RESEARCH.md section "What Changes in Redeployed CallRegistry" (exact diff: new state, Call struct addition, createCall interaction block, new guarded status transitions)
    - .planning/phases/02-followfademarket/02-PATTERNS.md section "CallRegistry.sol (redeploy diff)" (line-precise diff instructions)
  </read_first>
  <behavior>
    - ICallRegistry.sol changes: Add CallerExited to CallStatus enum (between Live and Settled, or append — use append to avoid renumbering existing values); add uint64 callerExitedAt field to Call struct (slot 2, after openToChallenges, document slot packing); add markCallerExited(uint256 callId) and markSettled(uint256 callId, Outcome outcome) to interface; add event FollowFadeMarketSet(address indexed market); add error NotAuthorized().
    - CallRegistry.sol state additions: address public followFadeMarket; (add after settlementManager, with NatSpec D-02 reference)
    - CallRegistry.sol createCall modification: after the safeTransferFrom that pulls stake+fee, ADD: (1) safeTransfer(treasury, TREASURY_PORTION) — $5 to treasury; (2) uint256 virtualFadeSeed = BASE_VIRTUAL_FADE + VIRTUAL_FADE_PORTION; (i.e. 2e6 + 5e6 = 7e6); (3) safeTransfer(followFadeMarket, stake); (4) IFollowFadeMarket(followFadeMarket).initPool(callId, stake, virtualFadeSeed). Also update the currentTvl += stake line to remain as a counter (NOT removed — it tracks forwarded stakes for cap math per D-03).
    - New functions on CallRegistry.sol: markCallerExited(callId) — requires msg.sender == followFadeMarket else revert NotAuthorized(); sets _calls[callId].status = CallStatus.CallerExited; sets _calls[callId].callerExitedAt = uint64(block.timestamp); markSettled(callId, outcome) — requires msg.sender == settlementManager else revert NotSettlementManager(); sets _calls[callId].status = CallStatus.Settled; _calls[callId].outcome = outcome; setFollowFadeMarket(address) — onlyOwner, emits FollowFadeMarketSet.
    - computeCallerExitPenalty(callId) stub: replace return 0 with real formula from _callerExitPenaltyPct (Research Pattern 5).
    - Add import for IFollowFadeMarket: import { IFollowFadeMarket } from "./interfaces/IFollowFadeMarket.sol";
  </behavior>
  <action>
    Apply diffs to both files. The createCall function must import IFollowFadeMarket. The stake-forward happens LAST in the CEI chain — after all state effects, after the safeTransferFrom pulls funds, then safeTransfer to treasury, then safeTransfer to followFadeMarket, then initPool. Treasury safeTransfer replaces the old $10 fee split (treasury $5 + virtual accounting $5).

    The `currentTvl` counter remains but semantically it now tracks "total USDC forwarded to FFM" (sum of caller stakes), used by FFM's TVL cap check via callRegistry.currentTvl().

    Ensure CallerExited is appended to CallStatus enum after Settled to avoid renumbering.
  </action>
  <verify>
    <automated>forge test --match-contract TvlAggregation -v 2>&1 | tail -20</automated>
  </verify>
  <done>CallRegistry.sol compiles; ICallRegistry.sol has CallerExited in enum and callerExitedAt in struct; createCall body contains "initPool" and "safeTransfer.*followFadeMarket"; markCallerExited and markSettled exist with correct auth guards; forge test --match-contract TvlAggregation exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ProfileRegistry redeploy diff — authorizedRepWriters + applyRepDelta</name>
  <files>
    packages/contracts/src/ProfileRegistry.sol,
    packages/contracts/src/interfaces/IProfileRegistry.sol
  </files>
  <read_first>
    - packages/contracts/src/ProfileRegistry.sol (full file — storage layout, _initIfNeeded, updateAfterSettlement auth pattern, setRelayer pattern)
    - packages/contracts/src/interfaces/IProfileRegistry.sol (full file — existing function signatures)
    - .planning/phases/02-followfademarket/02-RESEARCH.md section "What Changes in Redeployed ProfileRegistry" (exact new state, new functions, auth migration on updateAfterSettlement)
    - .planning/phases/02-followfademarket/02-PATTERNS.md section "ProfileRegistry.sol (redeploy diff)" (line-precise diff)
  </read_first>
  <behavior>
    - IProfileRegistry.sol additions: add applyRepDelta(address user, int256 delta) external; add setAuthorizedRepWriter(address writer, bool authorized) external; add authorizedRepWriters(address writer) external view returns (bool); add event RepDeltaApplied(address indexed user, int256 delta, uint128 newRep); add event RepWriterSet(address indexed writer, bool authorized); add error NotAuthorizedWriter().
    - ProfileRegistry.sol state additions: mapping(address => bool) public authorizedRepWriters; (with NatSpec D-04: "Generic authorized rep-writers set. FollowFadeMarket authorized at Phase 2 deploy. SettlementManager joins in Phase 4.")
    - New function setAuthorizedRepWriter(address writer, bool authorized) external onlyOwner: sets authorizedRepWriters[writer] = authorized; emits RepWriterSet.
    - New function applyRepDelta(address user, int256 delta) external: if (!authorizedRepWriters[msg.sender]) revert NotAuthorizedWriter(); _initIfNeeded(user); int256 current = int256(uint256(_profiles[user].globalRep)); int256 newRep = current + delta; _profiles[user].globalRep = uint128(newRep < 0 ? 0 : uint256(newRep)); (REP-02 floor at 0); emit RepDeltaApplied(user, delta, _profiles[user].globalRep).
    - updateAfterSettlement auth migration: replace `if (msg.sender != settlementManager)` with `if (!authorizedRepWriters[msg.sender]) revert NotAuthorizedWriter();` — compatible with Phase 4 registration via setAuthorizedRepWriter.
    - SLOT STABILITY: do NOT reorder Profile struct fields; only append if needed.
  </behavior>
  <action>
    Apply diffs. The authorizedRepWriters mapping allows FollowFadeMarket (authorized in DeployPhase2.s.sol) to call applyRepDelta for caller-exit rep slashes, and will allow SettlementManager (authorized in Phase 4) to call updateAfterSettlement — no third redeploy needed.

    The globalRep floor-at-0 is critical for REP-02: `newRep = current + delta; uint128(newRep < 0 ? 0 : uint256(newRep))`. The signed arithmetic handles negative deltas cleanly.
  </action>
  <verify>
    <automated>forge test --match-contract FollowFadeMarket -v 2>&1 | grep -E "PASS|FAIL|error" | tail -20</automated>
  </verify>
  <done>ProfileRegistry.sol compiles; authorizedRepWriters mapping present; applyRepDelta function exists with NotAuthorizedWriter guard and REP-02 floor-at-0; IProfileRegistry.sol includes applyRepDelta + setAuthorizedRepWriter + RepDeltaApplied event; forge build exits 0; forge test --match-contract FollowFadeMarket exits 0 (repSlash test GREEN).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anyone→markCallerExited | Must be rejected unless msg.sender == followFadeMarket |
| Anyone→applyRepDelta | Must be rejected unless msg.sender in authorizedRepWriters |
| CallRegistry→FollowFadeMarket | initPool call happens in createCall CEI tail |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Spoofing | Unauthorized markCallerExited | mitigate | `if (msg.sender != followFadeMarket) revert NotAuthorized()` — hard address check; no msg.sender override possible |
| T-02-03-02 | Elevation | Unauthorized applyRepDelta | mitigate | `if (!authorizedRepWriters[msg.sender]) revert NotAuthorizedWriter()` — owner-managed allowlist; only FFM authorized in Phase 2 |
| T-02-03-03 | Tampering | Virtual fade seed transferred as real USDC (Pitfall 1) | mitigate | initPool receives virtualFadeSeed as pure accounting parameter (uint256); NO additional safeTransfer for the seed; only stake is forwarded |
| T-02-03-04 | Tampering | currentTvl counter drift | accept | currentTvl tracks forwarded stakes (counter, not balanceOf); documented discrepancy; FFM.getTvl() = real USDC; combined cap = callRegistry.currentTvl() + ffm.getTvl() where callRegistry.currentTvl ≈ 0 after stakes forward (only transient) |
</threat_model>

<verification>
After both tasks complete:
1. `forge build` exits 0 for all contracts
2. `forge test --match-contract FollowFadeMarket -v` exits 0
3. `forge test --match-contract TvlAggregation -v` exits 0
4. `grep "markCallerExited\|markSettled" packages/contracts/src/CallRegistry.sol` — both functions present
5. `grep "authorizedRepWriters" packages/contracts/src/ProfileRegistry.sol` — mapping present
6. `grep "CallerExited" packages/contracts/src/interfaces/ICallRegistry.sol` — enum value present
7. `grep "applyRepDelta" packages/contracts/src/interfaces/IProfileRegistry.sol` — function signature present
</verification>

<success_criteria>
- forge build exits 0 for all 4 modified files
- forge test --match-contract "FollowFadeMarket|TvlAggregation" exits 0
- createCall in CallRegistry.sol contains "initPool" call to FollowFadeMarket
- markCallerExited requires msg.sender == followFadeMarket
- applyRepDelta has REP-02 floor-at-0 and NotAuthorizedWriter guard
- updateAfterSettlement uses authorizedRepWriters check (not single-address settlementManager)
</success_criteria>

<output>
After completion, create `.planning/phases/02-followfademarket/02-03-SUMMARY.md`
</output>
