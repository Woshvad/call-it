---
phase: 02-followfademarket
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - packages/contracts/test/helpers/FfmTestHelper.sol
  - packages/contracts/test/FollowFadeMarket.t.sol
  - packages/contracts/test/FollowFadeMarketGates.t.sol
  - packages/contracts/test/FollowFadeMarketInterference.t.sol
  - packages/contracts/test/TvlAggregation.t.sol
  - packages/shared/src/validation/follow-fade-gates.ts
  - packages/shared/src/validation/follow-fade-gates.test.ts
autonomous: true
requirements:
  - SOCIAL-01
  - SOCIAL-02
  - SOCIAL-03
  - SOCIAL-04
  - SOCIAL-05
  - SOCIAL-06
  - SOCIAL-07
  - SOCIAL-08
  - SOCIAL-09
  - SOCIAL-10
  - SOCIAL-11
  - SOCIAL-12
  - SOCIAL-13
  - SOCIAL-14
  - SOCIAL-15
  - SOCIAL-16
  - SOCIAL-17
  - SOCIAL-18
  - SOCIAL-19
  - SOCIAL-20
  - SOCIAL-21
  - SOCIAL-22
  - SOCIAL-26

must_haves:
  truths:
    - "forge test --match-contract FollowFadeMarket exits 0 (all tests RED initially, GREEN after Plan 02)"
    - "Wave 0 test files exist with all required test function stubs covering SOCIAL-01..28"
    - "AMM k-invariant fuzz contract has invariant_kNeverShrinks and invariant_usdcBalanceMatchesReserves"
    - "TVL aggregation boundary tests cover $4999 OK / $5001 revert"
    - "Shared follow-fade-gates.ts Vitest parity stubs exist for frontend slippage math"
  artifacts:
    - path: packages/contracts/test/helpers/FfmTestHelper.sol
      provides: "Shared deploy + pool-seed helper for all FFM test contracts"
    - path: packages/contracts/test/FollowFadeMarket.t.sol
      provides: "Unit tests for SOCIAL-01..28 (initially failing)"
    - path: packages/contracts/test/FollowFadeMarketGates.t.sol
      provides: "Invariant fuzz: k-invariant, balance=reserves, penalty injection"
    - path: packages/contracts/test/FollowFadeMarketInterference.t.sol
      provides: "Multi-call interference fixtures (Pitfall 9)"
    - path: packages/contracts/test/TvlAggregation.t.sol
      provides: "TVL boundary tests (Pitfall 3)"
    - path: packages/shared/src/validation/follow-fade-gates.ts
      provides: "TypeScript AMM share math mirroring contract formula (D-29 parity)"
  key_links:
    - from: packages/contracts/test/FollowFadeMarket.t.sol
      to: packages/contracts/src/FollowFadeMarket.sol
      via: "import + setUp() wiring"
      pattern: "import.*FollowFadeMarket"
    - from: packages/shared/src/validation/follow-fade-gates.ts
      to: packages/contracts/test/FollowFadeMarket.t.sol
      via: "D-29 parity: Vitest gate matrix must match Foundry gate matrix"
      pattern: "computeMinSharesOut|computeCallerExitPenaltyPct"
---

## Phase Goal

**As a** prediction market user, **I want to** Follow or Fade any live call and exit positions with predictable penalties, **so that** I can participate in the social prediction market with transparent accountability.

<objective>
Wave 0 establishes the test scaffold that RED gates every subsequent plan. No production code is written here — only test files + shared TS parity stubs. When Wave 1 ships FollowFadeMarket.sol, all unit tests turn GREEN without modifying the test files.

Purpose: RED→GREEN TDD cycle. Tests define the contract before the contract exists.
Output: 5 Foundry test files + 1 Vitest parity module that collectively cover all SOCIAL-01..28 behaviors, k-invariant, TVL boundaries, multi-call interference, and empty-pool LP-fee routing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-followfademarket/02-CONTEXT.md
@.planning/phases/02-followfademarket/02-RESEARCH.md
@.planning/phases/02-followfademarket/02-VALIDATION.md

<interfaces>
<!-- Key patterns extracted from codebase — executor must read these files first -->

From packages/contracts/test/CallRegistry.t.sol (analog):
- setUp() pattern: makeAddr, vm.startPrank(owner), deploy contracts, addAsset, vm.stopPrank, vm.etch(USDC_ARB_NATIVE, ...), usdc.mint(alice, 1000e6), usdc.approve(...)
- Helper: _createEthCall(caller, stake, conviction, assetKey) returns callId
- vm.expectRevert(ICallRegistry.StakeBelowMinimum.selector) pattern
- Test naming: test_<behaviorName>_<expectation>

From packages/contracts/test/CallRegistryGates.t.sol (analog for fuzz):
- bound(value, min, max) for fuzz inputs
- invariant_ prefix for invariant tests
- Foundry fuzz profile: [profile.ci] fuzz.runs = 1000 in foundry.toml

From packages/contracts/src/CallRegistry.sol:
- USDC_ARB_NATIVE constant from ./constants/USDC.sol
- pragma solidity =0.8.30 (EXACT pin)
- MockUSDC at vm.etch(0xaf88d065e77c8cC2239327C5EDb3A432268e5831, ...)

From packages/shared/src/validation/call-gates.ts (analog for parity stubs):
- export function computeXxx(params): result
- Vitest test file at *.test.ts with same-matrix fixture
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: FfmTestHelper + FollowFadeMarket unit test scaffold (RED)</name>
  <files>
    packages/contracts/test/helpers/FfmTestHelper.sol,
    packages/contracts/test/FollowFadeMarket.t.sol
  </files>
  <read_first>
    - packages/contracts/test/CallRegistry.t.sol (full setUp pattern, helper pattern, test naming)
    - packages/contracts/test/CallRegistryGates.t.sol (fuzz test structure)
    - packages/contracts/src/CallRegistry.sol lines 1-80 (constants: MIN_STAKE=5e6, MAX_STAKE=100e6, CREATION_FEE=10e6, TREASURY_PORTION=5e6, VIRTUAL_FADE_PORTION=5e6, BASE_VIRTUAL_FADE=2e6)
    - .planning/phases/02-followfademarket/02-RESEARCH.md (AMM formulas Patterns 1-8; error names; storage shape Pattern 8)
    - .planning/phases/02-followfademarket/02-VALIDATION.md (Wave 0 test list + req→test map)
  </read_first>
  <behavior>
    - FfmTestHelper.sol: abstract contract that deploys ProfileRegistry v2, CallRegistry v2, FollowFadeMarket in setUp(); wires setFollowFadeMarket; vm.etch USDC at 0xaf88d065e77c8cC2239327C5EDb3A432268e5831; mints 1000e6 USDC to alice + bob; approves FFM + CallRegistry; adds one asset (ETH feed) and one NFT collection; exposes _seedPool(callId, caller, stake) helper.
    - FollowFadeMarket.t.sol: imports FfmTestHelper; MUST initially fail to compile (FollowFadeMarket.sol does not exist yet) — that is expected and correct Wave 0 behavior.
    - Cover all test stubs (can have empty bodies with // TODO: implement in Wave 1) for: testFollowSharesMinted, testFadeSharesMinted, testPositionBounds (min $1 / max $100 cumulative), testSlippage (SlippageExceeded fires when minSharesOut not met), testPostExpiryGate (CallPastExpiry strict <), testPositionExit (4h cooldown ExitCooldownActive + 10% slash + 50/40/10 split), testCallerExitLocked (24h lock), testCallerExitPenalty (fuzz over time: penalty = 15 + 35*remaining/total, floor 15%), testCallerExitSplit (50/40/10 to follow/fade/treasury), testCallerExitStatus (call.status == CallerExited after exit), testRepSlash (applyRepDelta called in same tx, globalRep decremented), testCreateCallForwards (USDC.balanceOf(callRegistry) == 0 after createCall), testPauseCarveout (exitPosition works while paused, follow reverts Paused), testCallNotLive (exitPosition on settled call reverts CallNotLive), testAcceptDepositCallerExited (follow/fade on CallerExited status succeeds), testEmptyPoolLpFee (when fadePool=virtual-only, LP fee routes to treasury not divide-by-zero).
    - Error names to use (these MUST match IFollowFadeMarket.sol that Plan 02 creates): SlippageExceeded(uint256 minOut, uint256 actualOut), CallPastExpiry(), ExitCooldownActive(uint64 unlocksAt), CallerExitLocked(uint64 unlocksAt), TvlCapReached(uint256 requested, uint256 available), PositionBelowMinimum(), PositionAboveMaximum(), NotAuthorized(), CallNotLive().
    - Use pragma solidity =0.8.30 (EXACT — never ^). Import from forge-std/Test.sol.
  </behavior>
  <action>
    Create FfmTestHelper.sol as an abstract contract in packages/contracts/test/helpers/ following the CallRegistry.t.sol setUp() pattern exactly. It deploys a 3-contract stack, vm.etch USDC, and exposes _seedPool(). 

    Create FollowFadeMarket.t.sol importing FfmTestHelper. All test functions have empty bodies or a single vm.skip() so they compile once the contract exists. The file MUST declare all error selectors for vm.expectRevert use.

    The file will not compile until Plan 02 creates FollowFadeMarket.sol — that compile failure is the RED gate confirming this is Wave 0. Executor MUST document this compile failure as expected in the commit message.

    Do NOT create FollowFadeMarket.sol — that belongs to Plan 02.

    Constants to use in test: MIN_POSITION = 1e6 ($1), MAX_POSITION = 100e6 ($100), POSITION_EXIT_PENALTY_PCT = 10, POSITION_EXIT_COOLDOWN = 4 hours (14400 seconds), CALLER_EXIT_LOCK_DURATION = 24 hours (86400 seconds), CALLER_EXIT_BASE_PCT = 15, CALLER_EXIT_VARIABLE_PCT = 35, INITIAL_SHARE_PRICE = 1e12.
  </action>
  <verify>
    <automated>
      forge build --match-path packages/contracts/test/helpers/FfmTestHelper.sol 2>&1 | grep -E "error|warning|compiled" || true
      # Expected: compile error on FollowFadeMarket.t.sol because FollowFadeMarket.sol does not exist (Wave 0 RED gate)
      # FfmTestHelper.sol itself should compile if CallRegistry/ProfileRegistry v1 stubs exist
      # After Plan 02: forge test --match-contract "FollowFadeMarket$" -v exits 0
    </automated>
  </verify>
  <done>FfmTestHelper.sol exists and compiles independently; FollowFadeMarket.t.sol exists with all test stubs; both use pragma =0.8.30; commit message notes RED gate expected until Plan 02.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Invariant fuzz tests + TVL boundary tests (RED)</name>
  <files>
    packages/contracts/test/FollowFadeMarketGates.t.sol,
    packages/contracts/test/FollowFadeMarketInterference.t.sol,
    packages/contracts/test/TvlAggregation.t.sol
  </files>
  <read_first>
    - packages/contracts/test/CallRegistryGates.t.sol (invariant test structure, bound() usage, setUp pattern)
    - .planning/phases/02-followfademarket/02-RESEARCH.md lines 969-1009 (invariant test shape + Wave 0 gaps list)
    - packages/contracts/foundry.toml ([profile.ci] fuzz.runs = 1000 — do NOT change this)
  </read_first>
  <behavior>
    - FollowFadeMarketGates.t.sol: Foundry invariant test contract with invariant_kNeverShrinks() (for every callId with shares > 0, followReserve * fadeReserve >= lastK[callId]), invariant_usdcBalanceMatchesReserves() (USDC.balanceOf(ffm) == sum of followReserve[i] + (fadeReserve[i] - fadeSeedVirtual amount[i]) for all callIds), invariant_noOverClaim() (no user's share value > their contributed USDC). Also includes test_penaltyInjectionGrowsK (after exitPosition, followReserve*fadeReserve > k_before), test_noPhantomSharesOnPenalty (totalShares unchanged after penalty injection), test_expiryGateStrictLessThan (at exact expiry timestamp, follow reverts; at expiry-1 succeeds).
    - FollowFadeMarketInterference.t.sol: multi-call tests: test_multiCallIndependentK (follow on callId=1 does not affect reserves of callId=2), test_multiCallTvlAggregates (combined getTvl() = sum of individual call pool values), test_crossCallExitNoLeakage (exiting position on callId=1 does not move shares on callId=2).
    - TvlAggregation.t.sol: test_tvlBoundary4999Succeeds ($4999 USDC across CR+FFM OK), test_tvlBoundary5001Reverts ($5001 reverts TvlCapReached), test_tvlAggregation (CallRegistry.currentTvl + FFM.getTvl() combined cap enforcement).
    - All use pragma =0.8.30 EXACT.
  </behavior>
  <action>
    Create the three test files. FollowFadeMarketGates.t.sol uses Foundry's invariant testing pattern (target selectors via targetContract + targetSelector arrays). FollowFadeMarketInterference.t.sol uses multiple callIds seeded via _seedPool(). TvlAggregation.t.sol uses vm.deal + USDC minting to push TVL to boundaries.

    All three will fail to compile until FollowFadeMarket.sol exists (Plan 02 RED gate). Commit message: "test(02-01): add Wave 0 invariant + TVL fuzz test stubs [RED gate]".
  </action>
  <verify>
    <automated>
      ls packages/contracts/test/FollowFadeMarketGates.t.sol packages/contracts/test/FollowFadeMarketInterference.t.sol packages/contracts/test/TvlAggregation.t.sol
      # After Plan 02: forge test --match-contract "FollowFadeMarketGates|FollowFadeMarketInterference|TvlAggregation" --profile ci -v exits 0
    </automated>
  </verify>
  <done>Three test files exist; FollowFadeMarketGates.t.sol contains invariant_kNeverShrinks and invariant_usdcBalanceMatchesReserves; TvlAggregation.t.sol contains test_tvlBoundary4999Succeeds and test_tvlBoundary5001Reverts; all use pragma =0.8.30.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TypeScript AMM parity stubs (D-29)</name>
  <files>
    packages/shared/src/validation/follow-fade-gates.ts,
    packages/shared/src/validation/follow-fade-gates.test.ts
  </files>
  <read_first>
    - packages/shared/src/validation/call-gates.ts (parity pattern: exported functions + Vitest fixture matrix)
    - packages/shared/src/validation/call-gates.test.ts (fixture matrix structure)
    - .planning/phases/02-followfademarket/02-RESEARCH.md Pattern 3 (frontend computeMinSharesOut), Pattern 5 (caller exit penalty math), Pattern 6 (rep slash decay)
    - .planning/phases/01-core-contracts-auth-frontend-skeleton/01-CONTEXT.md D-29 (anti-drift parity pattern)
  </read_first>
  <behavior>
    - follow-fade-gates.ts exports: computeMinSharesOut(totalShares: bigint, reserve: bigint, amountIn: bigint): bigint — mirrors Solidity sharesOut = totalShares * amountIn / (reserve + amountIn) using BigInt; computeCallerExitPenaltyPct(createdAt: bigint, expiry: bigint, now: bigint): number — returns integer pct [15,50]; computeCallerExitRepDelta(createdAt: bigint, expiry: bigint, now: bigint): number — returns integer rep delta [-45,-10]; computePositionSlashSplit(slash: bigint): { toOpposite: bigint, toSameSide: bigint, toTreasury: bigint } — 50/40/10 using subtraction for last term.
    - follow-fade-gates.test.ts: Vitest fixture matrix — at least 5 input/output cases per function covering edge cases (zero reserve, exact expiry, exactly 24h after creation, exactly at expiry). These must pass with the exported functions.
    - Exports added to packages/shared/src/index.ts if an index file exists.
  </behavior>
  <action>
    Create follow-fade-gates.ts with pure BigInt math functions. No external dependencies. Each function is a direct TypeScript translation of the Research Patterns from 02-RESEARCH.md. Use BigInt arithmetic throughout to match Solidity precision.

    Create follow-fade-gates.test.ts with Vitest describe/it blocks. The fixture matrix must match the gate-matrix pattern from call-gates.test.ts. Run pnpm --filter @call-it/shared test to confirm GREEN.
  </action>
  <verify>
    <automated>pnpm --filter @call-it/shared test -- --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>follow-fade-gates.ts exports computeMinSharesOut, computeCallerExitPenaltyPct, computeCallerExitRepDelta, computePositionSlashSplit; all Vitest tests in follow-fade-gates.test.ts pass green; `pnpm --filter @call-it/shared test` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test→Contract | Test files call contract functions; inputs are fuzz-generated (untrusted) |
| Shared TS→Contract | TypeScript parity math must match Solidity exactly; divergence = silent drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-W0-01 | Tampering | invariant_kNeverShrinks | mitigate | Foundry invariant runner with 1000 fuzz runs; any k shrinkage fails the suite |
| T-02-W0-02 | Tampering | computeMinSharesOut TS parity | mitigate | Vitest fixture matrix cross-checks TS vs expected values from contract formula; D-29 parity CI gate |
| T-02-W0-03 | Tampering | Penalty split dust (50/40/10) | mitigate | `toTreasury = slash - toOpposite - toSameSide` (subtraction not multiplication) in TS parity function; verified in test fixture |
</threat_model>

<verification>
After all three tasks complete:
1. `ls packages/contracts/test/FollowFadeMarket.t.sol packages/contracts/test/FollowFadeMarketGates.t.sol packages/contracts/test/FollowFadeMarketInterference.t.sol packages/contracts/test/TvlAggregation.t.sol packages/contracts/test/helpers/FfmTestHelper.sol` — all 5 files exist
2. `pnpm --filter @call-it/shared test` exits 0 (TS parity stubs GREEN)
3. Foundry test files reference error selectors: SlippageExceeded, CallPastExpiry, ExitCooldownActive, CallerExitLocked, TvlCapReached, PositionBelowMinimum, PositionAboveMaximum
4. FollowFadeMarketGates.t.sol contains `invariant_kNeverShrinks` and `invariant_usdcBalanceMatchesReserves` function names
5. TvlAggregation.t.sol contains `testTvlBoundary` functions
</verification>

<success_criteria>
Wave 0 test infrastructure complete when:
- All 5 Foundry test files exist with correct pragma =0.8.30
- All test stubs reference correct error selector names matching IFollowFadeMarket (to be created in Plan 02)
- Vitest parity stubs GREEN: `pnpm --filter @call-it/shared test` exits 0
- Commit message clearly notes RED compile gate (FollowFadeMarket.sol pending Plan 02)
- No test file uses watch mode flags or `--no-exit`
</success_criteria>

<output>
After completion, create `.planning/phases/02-followfademarket/02-01-SUMMARY.md`
</output>
