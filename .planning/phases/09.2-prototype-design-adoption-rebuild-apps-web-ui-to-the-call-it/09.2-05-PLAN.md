---
phase: 09.2-prototype-design-adoption
plan: 05
type: execute
wave: 3
depends_on: ["09.2-01", "09.2-02"]
files_modified:
  - apps/web/app/profile/[address]/ProfileClient.tsx
  - packages/ui/src/compound/ProfileHeader.tsx
  - apps/web/tests/profile-overview.spec.ts
  - apps/web/tests/profile-shell.spec.ts
autonomous: true
requirements: [AUTH-44, UI-48]
must_haves:
  truths:
    - "Profile data with no live source (rep-history sparkline, calibration/ROI/contrarian-hits, 8-category reputation bars, RECENT CALLS, MOST FOLLOWED BY, NOTABLE RECEIPTS) is HIDDEN — including the currently-FAKE hardcoded 60% category bars which are removed (D-07)"
    - "Identity renders handle + verified badges only — never a wallet address (AUTH-44 cited under D-14 invariants)"
    - "Profile specs asserting @call-it/ui imports are kept satisfied or updated in the same task (D-15)"
    - "ProfileClient stays a dumb props renderer; the RSC page is untouched (D-05)"
  artifacts:
    - path: "apps/web/app/profile/[address]/ProfileClient.tsx"
      provides: "Prototype profile markup: identity header, hero rep numeral, real stat blocks (accuracy/streak/W-L), degraded sections hidden"
      min_lines: 120
    - path: "packages/ui/src/compound/ProfileHeader.tsx"
      provides: "Rethemed flexbox-only profile header (square avatar, pill badges)"
  key_links:
    - from: "apps/web/app/profile/[address]/ProfileClient.tsx"
      to: "packages/ui"
      via: "@call-it/ui imports (spec-asserted)"
      pattern: "@call-it/ui"
---

<objective>
Port the prototype profile markup into ProfileClient.tsx and retheme ProfileHeader — rendering only what has a real source and deleting the fake 60% category bars.

Purpose: D-16 step 1, second pure-props screen; removes the one place the current app actively fakes data.
Output: Rethemed profile with honest degradation; lockstep-updated profile specs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-CONTEXT.md
@.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-UI-SPEC.md
@.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-PATTERNS.md (§6)
</context>

## Artifacts this phase produces (this plan)

- No new files — markup rewrites inside `ProfileClient.tsx` and `ProfileHeader.tsx`
- New inline display constant: profile hero rep numeral `clamp(64px, 18vw, 132px)`

<tasks>

<task type="auto">
  <name>Task 1: Port profile markup with honest degradation (remove fake 60% bars; hide no-source sections)</name>
  <files>apps/web/app/profile/[address]/ProfileClient.tsx</files>
  <read_first>
    - apps/web/app/profile/[address]/ProfileClient.tsx (current 262 lines: ProfileResponse props contract, '—' stubs at ~108-110, hardcoded 60% category bars at ~163, recentCalls [] at ~113 — logic/props stay, fakes go)
    - apps/web/app/profile/[address]/page.tsx (READ-ONLY — RSC fetch + props hand-off; DO NOT MODIFY)
    - "call it frontend/screens/profile.jsx" (markup donor: identity header, hero rep, stat rows, section dividers; note its line 36 consumes the BROKEN data.jsx OUTCOMES — never copy that import pattern)
    - apps/web/tests/profile-overview.spec.ts + profile-shell.spec.ts (the @call-it/ui import asserts + any markup asserts — lockstep contract for Task 2)
  </read_first>
  <action>
    Rewrite ProfileClient's render to the prototype profile markup keeping all props/hooks: identity header (square .avatar grad from handle, @handle Archivo display, VerifiedBadge x/fc from existing verifiedX/verifiedFarcaster fields), hero rep numeral Archivo 900 clamp(64px, 18vw, 132px), real stat blocks ONLY — Accuracy (wins/(wins+losses) or the existing computed field), Streak, W/L record, total calls (.stat-block recipes). REMOVE entirely (D-07 hidden, never faked): the hardcoded 60% category reputation bars (currently FAKE data — this is the one active D-07 violation in the app), the Calibration/ROI/Contrarian '—' stub blocks (hide the blocks, don't render dashes), the RECENT CALLS table (recentCalls is always [] — render the prototype-voice empty state "No calls on record yet." in .label-overline style instead of an empty table, or hide the section entirely), MOST FOLLOWED BY, NOTABLE RECEIPTS, and the 30d rep-history sparkline. Keep section structure with .section-divider so the page doesn't feel truncated. Keep @call-it/ui imports (profile specs assert them — D-15). Keep useIsMobile/375px handling (UI-48): single column, stat blocks 2-up wrap. AUTH-44: the address from the URL param is a lookup key only — never rendered.
  </action>
  <verify>
    <automated>pnpm --filter @call-it/web test</automated>
  </verify>
  <done>Profile renders prototype identity + real stats; all fake/no-source sections gone; vitest green.</done>
  <acceptance_criteria>
    - ProfileClient.tsx contains `clamp(64px, 18vw, 132px)` and imports from `@call-it/ui`
    - grep -c "60" apps/web/app/profile/[address]/ProfileClient.tsx — no hardcoded 60% bar values remain in render code (the fake category bars are gone)
    - grep -ci "calibration\|roi\|contrarian hits\|sparkline\|notable" apps/web/app/profile/[address]/ProfileClient.tsx returns 0
    - grep -c "address.slice\|formatAddress" apps/web/app/profile/[address]/ProfileClient.tsx returns 0 in rendered output paths
    - apps/web/app/profile/[address]/page.tsx unchanged: git diff --stat is empty for it
    - `pnpm --filter @call-it/web test` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Retheme ProfileHeader (flexbox-only) and lockstep profile specs</name>
  <files>packages/ui/src/compound/ProfileHeader.tsx, apps/web/tests/profile-overview.spec.ts, apps/web/tests/profile-shell.spec.ts</files>
  <read_first>
    - packages/ui/src/compound/ProfileHeader.tsx (current props + layout — props API stays; chrome changes)
    - apps/web/tests/profile-overview.spec.ts + profile-shell.spec.ts (FULL FILES — every source assert must be reconciled with the new markup)
    - "call it frontend/screens/profile.jsx" (header treatment recipe)
  </read_first>
  <action>
    ProfileHeader.tsx: retheme to the token layer — square avatar (radius 0, grad palette), Archivo 800 handle, JBM overline metadata with interpunct separators, pill badges; FLEXBOX ONLY (packages/ui Satori constraint, D-14); props API unchanged. Then reconcile profile-overview.spec.ts and profile-shell.spec.ts: keep the @call-it/ui import asserts satisfied; update any literal asserting removed sections (category bars, '—' stubs) to assert their ABSENCE with a D-07 comment; never delete tests (D-15).
  </action>
  <verify>
    <automated>pnpm --filter @call-it/ui test && pnpm --filter @call-it/web test && pnpm --filter @call-it/ui build && pnpm --filter @call-it/web build && pnpm --filter @call-it/web lint</automated>
  </verify>
  <done>ProfileHeader rethemed flexbox-only; specs reconciled; full gate green.</done>
  <acceptance_criteria>
    - ProfileHeader.tsx contains no `display: 'grid'`/`grid-cols` (grep returns 0) and no `border-radius: '50%'`/`rounded-full` (avatars square)
    - profile specs still assert `@call-it/ui` imports; test count not reduced
    - Full chain `pnpm --filter @call-it/ui test && pnpm --filter @call-it/web test && pnpm --filter @call-it/ui build && pnpm --filter @call-it/web build && pnpm --filter @call-it/web lint` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public viewer → /profile/[address] | public route rendering identity + reputation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.2-12 | Information Disclosure | wallet address leakage in new identity markup (AUTH-44) | mitigate | address-render grep gate = 0; AUTH-44 invariant test in vitest gate |
| T-09.2-13 | Spoofing | fake category-reputation bars implying false skill | mitigate | the hardcoded 60% bars are REMOVED (D-07); grep gate = 0 for no-source sections |
| T-09.2-SC | Tampering | package installs | accept | zero new packages this phase |
</threat_model>

<verification>
- Web + ui vitest green per task; full build/lint gate at plan end
</verification>

<success_criteria>
- Profile shows only real data in the prototype skin; the app's one active fake-data surface is gone
- ProfileHeader flexbox-only; specs lockstep-updated
</success_criteria>

<output>
Create `.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-05-SUMMARY.md` when done.
</output>
