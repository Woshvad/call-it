---
phase: 09.2-prototype-design-adoption
plan: 09
subsystem: ui
tags: [live-receipt, odds-bar, follow-fade, challenge, activity-feed, quote-calls, neobrutalism, d-06, d-07, d-08, d-14]

# Dependency graph
requires:
  - phase: 09.2 plan 01
    provides: globals.css token layer + brutal-* primitives (.brutal-card hero/bracketed, .btn cream/fade/duel/ghost/outline-white/big, .stat-block, .section-divider, .pill win/loss/duel/neutral, .label-overline, .h-statement, .brutal-bar.split, .activity-row + stampIn, .live-dot, avatar grads)
  - phase: 09.2 plan 02
    provides: rethemed @call-it/ui primitives (Stamp stays for the settled branch)
  - phase: 09.2 plan 07
    provides: settled-branch precedent in the SAME file — replace-render-keep-handlers seam, avatarGradClass/shortHash/formatUtc module helpers, module-scope-additions sanction
  - phase: 09.2 plan 08
    provides: cream-rethemed FollowFadeModal/CallerExitModal/PositionExitModal/ChallengeFormModal that the live CTAs open
provides:
  - Live render branch of /call/[id] in the prototype ReceiptLiveScreen skin (bracketed hero, identity row, .h-statement, JBM countdown stat row, .brutal-bar.split odds over FFM-derived followPct/fadePct, pool stat row, cream/fade/duel CTA row, prototype activity rows + quote cards, .brutal-card.heavy pending-challenge block)
  - LiveCountdown in-file display component (D HH:MM:SS JBM, 1s tick) over the existing expiry field
affects: [09.2-15 visual goldens + live-page manual checkpoint]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Replace-render-keep-handlers seam (PATTERNS §7) round 2: only the live branch JSX replaced; data/handler block byte-identical except the sanctioned import-line trim + module-scope display-helper insert"
    - "Odds bars consume existing FFM-derived percentages only (followPct/fadePct) — never fabricated (T-09.2-26)"
    - "Inline guard logic on money CTAs kept verbatim even when a render gate is added around the control"

key-files:
  created: []
  modified:
    - apps/web/app/call/[id]/page.tsx

key-decisions:
  - "Live header Share button CUT alongside WATCH (D-08): the old live-branch 'Share ↗' button had NO onClick (dead control) and no live-call share wiring exists (buildShareText is outcome-word-based) — settled receipts carry the real share intents (D-09)"
  - "Receipt preview card (<Receipt mode='live'>) CUT: not in the prototype ReceiptLiveScreen — the page IS the receipt now; Receipt + MarketPositioningBar removed from the @call-it/ui import (only sanctioned data-block-range diff)"
  - "CHALLENGE CTA render-gated on callData.openToChallenges per the plan, with the inline self-challenge/openToChallenges guard logic kept verbatim inside the onClick (belt + suspenders, zero handler drift)"
  - "Old sticky caller header + page-content wrapper dropped — AppShell (09.2-03) provides chrome/max-width; live branch now mirrors the settled branch's top-meta + hero structure"
  - "Position-exit control moved from a standalone bottom card into the hero footer next to the caller-exit affordance (prototype placement), conditional rendering + setters unchanged"
  - "Activity actions render as pills: FOLLOWED=win / FADED=loss / EXITED + CALLER EXITED=neutral (the live data has no quote/duel activity kinds; mapping covers all existing values)"

patterns-established:
  - "Cut-dead-controls comments must avoid the banned literal themselves (WATCH/64% greps) — same literal-grep lesson as 09.2-02"

requirements-completed: [AUTH-44, UI-48]

# Metrics
duration: 26min
completed: 2026-06-10
---

# Phase 09.2 Plan 09: Live Receipt Restyle Summary

**The live `/call/[id]` branch now renders the prototype ReceiptLiveScreen — bracketed hero with grad-avatar identity row, Archivo .h-statement, ticking JBM countdown, .brutal-bar.split odds over the real FFM reserves, pool stats, and cream FOLLOW / fade FADE / duel CHALLENGE CTAs opening the 09.2-08 cream modals — plus prototype .activity-row feed, quote-call minis, and a .brutal-card.heavy pending-challenge block, all over the byte-identical data/handler block**

## Performance

- **Duration:** ~26 min
- **Started:** 2026-06-10T14:40:17Z
- **Completed:** 2026-06-10T15:06:14Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments

- **Upper region replaced wholesale** (Task 1): old sticky header + dark hero + 4-stat row + MarketPositioningBar + 3 inline-styled buttons + Receipt preview → prototype structure: `← Back to the tape` ghost link + real posted-time mono overline (hidden when createdAt absent, D-07); `.brutal-card.hero.bracketed` with the CALLER EXITED warning strip (real `callerExitedAt`/`callerExitedPenalty` segments only); square grad avatar + `{handle}` + `{repScore} rep` identity row — caller accuracy/streak header stats have NO source on this page and are HIDDEN (D-07, no profile fetch added); LIVE (live-dot) + VERIFIED CRITERIA pills; `THE CALL · {CATEGORY}` overline; `.h-statement` market line (28px desktop / 22px mobile)
- **Honest stat row (D-07):** TIME LEFT (new `LiveCountdown` — D HH:MM:SS JetBrains Mono, 1s tick over the existing `expiry`), STAKE ("caller's commit"), CONVICTION ("permanent on settle"); the prototype's "Current spread" oracle stat and its category-average sub have no source → CUT
- **Odds split (key_link/T-09.2-26):** `.brutal-bar.split` consuming the existing `followPct`/`fadePct` derivations (win/loss fills, 2px black gap, 0.4s fill transition from the class recipe), headed by the 28px Archivo FOLLOW/FADE percentages; pool stat row from the live 5s-polled reserves — FOLLOW POOL / FADE POOL / TOTAL POT / YOUR POSITION (the last rendered only when the viewer holds a position; positions COUNT has no source → not rendered)
- **CTA row (D-06):** `↗ FOLLOW THIS CALL` (.btn cream big), `FADE · BET AGAINST` (.btn fade big), `⚔ CHALLENGE {handle}` (.btn duel big, render-gated on `openToChallenges` with the T-3-06-02 self-challenge guard kept verbatim) — each opens the EXISTING amount-based modal via the existing setter; mobile stacks full-width column, ≥44px targets (UI-48); `data-receipt-action-row` hook carried
- **Reasoning/criteria:** prototype quote treatment (`.label-overline` "Reasoning · from the caller") + chartreuse-tinted VERIFIED RESOLUTION CRITERIA block keeping the existing WORKING collapse toggle (criteria text in mono)
- **Exit affordances in the hero footer:** caller exit (penalty % link or lock countdown via `formatTimeLeft`) + position exit (10% penalty link or 4h-cooldown countdown) — existing conditional rendering and `setIsCallerExitModalOpen`/`setIsPositionExitModalOpen` wiring to the 09.2-08 cream modals, 44px touch targets
- **Activity feed (Task 2):** `.section-divider` with live-dot "ACTIVITY · UPDATING" + real action count → `.activity-row` recipe (page-level grid OK, stampIn entry animation): grad avatar, handle only (AUTH-44), action pill (FOLLOWED=win / FADED=loss / EXITED·CALLER EXITED=neutral), amount + relative time in JBM; empty state "No activity yet." mono microcopy
- **Quote calls:** "QUOTE CALLS · {n}" divider + `.brutal-card` minis with FOLLOWING/FADING stance pills (SOCIAL-45), handle + marketLine + `view quote call ↗`; the whole section HIDDEN when empty (D-07)
- **Pending-challenge block:** `.brutal-card.heavy` with the duel-purple `⚔ 1V1 DUEL · PENDING` pill; cream APPROVE/ACCEPT and outline REJECT (ghost keep-open / fade confirm) — `handleChallengeApprove`/`handleChallengeAccept`/`handleChallengeReject`, the USDC approve preflight (T-3-06-06), SOCIAL-31 matching-stake copy, disabled states, and the isZeroCE note all byte-identical
- **Toast:** wiring untouched; chrome moved to the token layer (error accent stays `#F87171`; success literal `#4ADE80` → `var(--accent-win)`); 5s polling + `MiniAppReady enabled={!!callData}` untouched (count 5 in file)

## Task Commits

Each task was committed atomically:

1. **Task 1: Replace the live branch header/market/odds/CTA region** - `9c59c6e` (feat)
2. **Task 2: Replace the live branch activity/quotes/challenge/exited regions** - `289b3aa` (feat)

## Files Created/Modified

- `apps/web/app/call/[id]/page.tsx` — live render branch rewritten in the prototype skin; `LiveCountdown` display component added at module scope (same sanctioned slot as 09.2-07's helpers); data/handler block (fetches, useReadContracts 5s poll, write handlers, modal state, derived values) untouched except the @call-it/ui import line trimmed (`MarketPositioningBar`, `Receipt` removed — both consumed only by the replaced JSX)

## Decisions Made

- **Live Share button cut with WATCH (D-08):** the outgoing live-branch "Share ↗" button had no onClick — a dead control — and there is no live-call share wiring (share text builders are outcome-word-based); settled receipts carry the real intents (D-09)
- **Receipt preview card cut:** the `<Receipt mode="live">` duplicate-preview is not part of the prototype ReceiptLiveScreen; the full page is the receipt
- **CHALLENGE CTA render-gated on the existing `openToChallenges` flag** (plan instruction) while keeping the inline guard logic verbatim inside onClick — no handler drift, no dead button
- **Old sticky header + page-content wrapper removed** — AppShell provides chrome and max-width; live branch now structurally mirrors the settled branch
- **Position-exit affordance relocated to the hero footer** beside the caller-exit affordance (prototype placement), conditionals + setters unchanged
- **Activity side→pill mapping** covers the actual data domain (followed/faded/exited/caller_exited); the prototype's quote/duel activity kinds have no source in `/api/feed` and are not fabricated

## Deviations from Plan

None — plan executed exactly as written. Seam notes for the record (mirroring 09.2-07's precedent): the kept data/handler block's only diffs are (a) the @call-it/ui import line trimmed of the two components whose JSX was replaced, and (b) the pure module-scope `LiveCountdown` display component inserted between the formatting helpers and the page component — zero handler/fetch/state/derived-value changes (verified by identifier-deletion grep over `git diff 06e7e3a..HEAD`: the only deleted identifier lines are the import members and re-indented onClick lines re-added verbatim).

## Verification

- Task 1 gate: `pnpm --filter @call-it/web test` 97/97; `tsc --noEmit` shows only the 3 pre-existing test-file errors (documented since 09.2-03)
- Task 2 full gate: web vitest 97/97 → shared `tsc --build` 0 → `next build` 0 (all routes) → web lint 0 (project lint script is a stub — pre-existing)
- Acceptance greps: `← Back to the tape` present (2 — live + settled); `.brutal-bar split` consuming `followPct`/`fadePct` (6 refs); case-insensitive WATCH grep = 0 and `64%` grep = 0 across the whole file (banned literals also scrubbed from comments — 09.2-02 lesson); modal-setter count 5, challenge-handler refs 6, handler-identifier diff additions mirror deletions exactly; `MiniAppReady` count 5 (≥2); AUTH-44 grep over the activity/quotes region = 0 address-rendering helpers; `data-receipt-action-row` + `data-outcome-word` hooks present
- Live-page manual checks (hydration, modal flows, 375px) roll into the plan 15 checkpoint per the plan

## Known Stubs

None — every rendered value has a live source (relayer `/live-state` + `/api/feed` + quote-stance + FFM `useReadContracts`); everything without one is hidden (D-07), and the two dead controls found (unwired live Share button, WATCH) were cut (D-08).

## Threat Flags

None — no new endpoints, auth paths, or trust-boundary changes. T-09.2-24 (dead-CTA money flow) mitigated by the handler-identifier diff gate + per-task vitest; T-09.2-25 (AUTH-44) grep = 0 in activity/positions markup; T-09.2-26 (fabricated odds) — the split bar consumes only the existing FFM-derived `followPct`/`fadePct`.

## Issues Encountered

None beyond two literal-grep comment trippers (WATCH / 64% appearing in my own explanatory comments), reworded before commit — the documented 09.2-02 pattern.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- The full `/call/[id]` money surface (live + settled) is now in the prototype skin over untouched handlers — D-16 step 2 complete for this route
- Plan 15's visual-golden regeneration + live-page manual checkpoint can now exercise both branches
- `LiveCountdown` is reusable if later screens (feed cards, duel) want a ticking JBM countdown

---
*Phase: 09.2-prototype-design-adoption*
*Completed: 2026-06-10*

## Self-Check: PASSED

- `apps/web/app/call/[id]/page.tsx` — FOUND
- Commit `9c59c6e` — FOUND
- Commit `289b3aa` — FOUND
- No unintended file deletions in either commit
