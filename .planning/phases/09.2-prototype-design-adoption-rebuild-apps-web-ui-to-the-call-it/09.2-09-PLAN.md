---
phase: 09.2-prototype-design-adoption
plan: 09
type: execute
wave: 5
depends_on: ["09.2-07", "09.2-08"]
files_modified:
  - apps/web/app/call/[id]/page.tsx
autonomous: true
requirements: [AUTH-44, UI-48]
must_haves:
  truths:
    - "Follow/fade/challenge/exit CTAs on the live receipt open the existing amount-based modals — never one-click stakes (D-06)"
    - "Live-receipt data with no source (WATCH button, caller accuracy/streak header stats, 'vs 64% cat avg') is CUT or HIDDEN — no dead buttons ship (D-07, D-08)"
    - "The data/handler block (~95-1390) stays verbatim; only the live render branch (was 1865-2751) is replaced; MiniAppReady stays on every branch (D-14)"
    - "Identity renders handles only throughout the live receipt — activity feed, quote calls, positions (D-14 AUTH-44)"
  artifacts:
    - path: "apps/web/app/call/[id]/page.tsx"
      provides: "Replaced live render branch: prototype ReceiptLiveScreen skin over existing FFM reserves, activity, quotes, challenge flows"
      contains: "MiniAppReady"
  key_links:
    - from: "live branch OddsBar"
      to: "FFM reserves (useReadContracts)"
      via: "existing derived followPct/fadePct"
      pattern: "followPct|fadePct"
    - from: "live branch CTAs"
      to: "FollowFadeModal/ChallengeFormModal (rethemed in 09.2-08)"
      via: "existing modal open-state setters"
      pattern: "FollowFadeModal"
---

<objective>
Replace the live render branch of `/call/[id]` (lines ~1865-2751) with the prototype ReceiptLiveScreen design — header, odds split bar, pool stats, activity feed, quote calls, CTA row, caller-exited banner, pending-challenge block — re-consuming every existing derived value and handler.

Purpose: Completes the core value surface (D-16 step 2); the live receipt is where money actions happen.
Output: Live branch in the new skin over untouched data/handlers.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-CONTEXT.md
@.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-UI-SPEC.md
@.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-PATTERNS.md (§7)
@.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-07-SUMMARY.md
</context>

## Artifacts this phase produces (this plan)

- No new files or exported symbols — the live-branch rewrite lives inside `apps/web/app/call/[id]/page.tsx`, consuming primitives from 09.2-01/02 and the modals rethemed in 09.2-08

<tasks>

<task type="auto">
  <name>Task 1: Replace the live branch header/market/odds/CTA region</name>
  <files>apps/web/app/call/[id]/page.tsx</files>
  <read_first>
    - apps/web/app/call/[id]/page.tsx (the live branch as left by 09.2-07; identify every derived value the new markup re-consumes: live-state fields — marketLine, category, stake, conviction, expiry, reasoning, criteriaText, status, callerExited fields, openToChallenges; FFM-derived followPct/fadePct/reserves; modal open-state setters; follow/fade/exit/challenge handlers)
    - "call it frontend/screens/receipt.jsx" (ReceiptLiveScreen markup donor: header identity row, statement, OddsBar, pool split, positions/pot stats, CTA row)
    - "call it frontend/components.jsx" (OddsBar/Countdown/Pill recipes)
  </read_first>
  <action>
    Replace the upper half of the live render branch with the prototype structure, re-consuming existing variables: back link "← Back to the tape" (ghost btn); header identity row (square avatar grad, @handle, VerifiedBadges, posted-time mono overline) — caller accuracy/streak header stats have NO source on this page and are HIDDEN (D-07; no extra profile fetch added); market statement .h-statement Archivo 28px (clamp 22px mobile); category + status pills; Countdown D HH:MM:SS JBM from expiry; conviction display + "permanent on settle" microcopy; reasoning/criteria sections (.label-overline headers, criteria in mono); odds split → .brutal-bar.split using existing followPct/fadePct (follow var(--accent-win) / fade var(--accent-loss), 2px black gap, 0.4s fill transition); pool stats (.stat-block row): follow pool $, fade pool $, total pot, your position — from existing FFM reserve/position derivations, hiding any null field (D-07). CTA row: FOLLOW (Button intent cream), FADE (intent fade), CHALLENGE (intent duel, rendered only when openToChallenges — existing gate) — each opens the EXISTING modal via the existing setter (D-06); exit affordances (caller exit / position exit) keep their existing conditional rendering wired to the rethemed exit modals. The prototype's WATCH button has no backing feature — CUT (D-08). "vs 64% cat avg" — no source, CUT (D-07). Keep every data-testid; keep MiniAppReady mounted. 375px: single column, full-width CTAs, 44px targets (UI-48).
  </action>
  <verify>
    <automated>pnpm --filter @call-it/web test</automated>
  </verify>
  <done>Live receipt upper region renders the prototype skin from existing derived data; CTAs open existing modals; vitest green.</done>
  <acceptance_criteria>
    - Live branch contains `← Back to the tape` and a .brutal-bar split treatment consuming the existing followPct/fadePct identifiers
    - grep -ci "WATCH" in the live-branch JSX returns 0 (D-08 cut); grep -c "64%" returns 0 (D-07 cut)
    - CTA handlers unchanged: git diff shows no modal-setter/handler identifier changes
    - grep -c "MiniAppReady" apps/web/app/call/[id]/page.tsx returns >= 2
    - `pnpm --filter @call-it/web test` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Replace the live branch activity/quotes/challenge/exited regions</name>
  <files>apps/web/app/call/[id]/page.tsx</files>
  <read_first>
    - apps/web/app/call/[id]/page.tsx (the remaining live-branch JSX: activity feed mapping, quote-calls section, pending-challenge accept/reject block, caller-exited banner — handlers stay)
    - "call it frontend/screens/receipt.jsx" (ACTIVITY rows + QUOTES section recipes; riders/sides treatment)
    - "call it frontend/styles.css" (.activity-row + stampIn recipe)
  </read_first>
  <action>
    Replace the remaining live-branch regions: activity feed → .activity-row recipe (grid allowed at page level) with stampIn entry animation, mapping the existing activity items (side follow/fade/quote/duel → pill color: win/loss/neutral/duel), handles + amounts + relative time in JBM — handles only (AUTH-44). Quote calls section → section-divider "QUOTE CALLS" + mini cards with FOLLOWING/FADING stance pills from existing quote-stance data. Pending-challenge block (when a challenge awaits the caller): keep the existing accept/reject handlers with their USDC approve preflight; restyle as a .brutal-card.heavy with duel-purple identity pill and cream ACCEPT / outline REJECT buttons. Caller-exited banner: warning #FB923C treatment, real exit data from existing fields. Empty states: activity "No activity yet." / quotes hidden when empty (D-07) — prototype-voice mono microcopy. Error toast pathway untouched (already #F87171). Keep all data-testids and the 5s polling wiring.
  </action>
  <verify>
    <automated>pnpm --filter @call-it/web test && pnpm --filter @call-it/shared build && pnpm --filter @call-it/web build && pnpm --filter @call-it/web lint</automated>
  </verify>
  <done>Full live branch in the prototype skin; challenge/exit flows untouched; full gate green.</done>
  <acceptance_criteria>
    - Live branch contains an .activity-row (or equivalent class) mapping over the existing activity data identifier
    - Accept/reject challenge handler identifiers unchanged (git diff gate)
    - grep in activity/quotes JSX for address-rendering helpers returns 0 (AUTH-44)
    - Full chain `pnpm --filter @call-it/web test && pnpm --filter @call-it/shared build && pnpm --filter @call-it/web build && pnpm --filter @call-it/web lint` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → FFM/CE contracts | follow/fade/exit/challenge writes with USDC approve preflights |
| public viewer → live receipt | public route rendering positions + activity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.2-24 | Tampering | restyled CTA silently detached from its tx handler (dead-CTA money flow) | mitigate | handler-identifier git-diff gates on both tasks; web vitest per task |
| T-09.2-25 | Information Disclosure | addresses in activity/positions markup (AUTH-44) | mitigate | grep gate = 0; AUTH-44 invariant test in vitest gate |
| T-09.2-26 | Spoofing | odds bar rendering fabricated percentages | mitigate | bar consumes existing FFM-derived followPct/fadePct only (key_link pattern gate) |
| T-09.2-SC | Tampering | package installs | accept | zero new packages this phase |
</threat_model>

<verification>
- Web vitest per task; full build/lint gate at plan end
- Live-page manual checks (hydration, modal flows) roll into the plan 15 checkpoint
</verification>

<success_criteria>
- Live receipt fully in the prototype skin; every money/social flow behaviorally identical
- No dead controls; no fabricated data; handles only
</success_criteria>

<output>
Create `.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-09-SUMMARY.md` when done.
</output>
