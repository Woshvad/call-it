---
phase: 09.2-prototype-design-adoption
plan: 10
type: execute
wave: 6
depends_on: ["09.2-09"]
files_modified:
  - apps/web/app/new/page.tsx
  - apps/web/app/new/components/MarketTypeSwitcher.tsx
  - apps/web/app/new/components/EventFields.tsx
  - apps/web/app/new/components/PriceTargetFields.tsx
  - apps/web/app/new/components/SpreadVsFields.tsx
  - apps/web/app/new/components/DeadlinePicker.tsx
  - apps/web/app/new/components/ConvictionSliderField.tsx
  - apps/web/app/new/components/CriteriaField.tsx
  - apps/web/app/new/components/AdvancedSettings.tsx
  - apps/web/app/new/components/DuplicateWarning.tsx
  - apps/web/app/new/components/PublishConfirmModal.tsx
  - apps/web/app/new/components/QuoteParentCard.tsx
  - apps/web/app/new/components/QuoteSuccess.tsx
  - packages/ui/src/compound/Receipt.tsx
  - apps/web/tests/new-call-publish.spec.ts
  - apps/web/tests/quote-composer.spec.ts
autonomous: true
requirements: [CALL-49, UI-51]
must_haves:
  truths:
    - "The /new restyle keeps RHF + zod + useDebouncedDupCheck + usePublishCall + the conviction gate intact — root visuals over existing app logic per the D-16 step-3 sequence; v1-clean is UX reference only (D-01)"
    - "Conviction-zone words (Hesitant ≤35 · Confident ≤65 · Bold ≤84 · On record ≤100) and $5/$25/$50/$100 stake quick-picks ship per the D-16 adoption sequence"
    - "new-call-publish.spec.ts source asserts (ConvictionBar import, CONVICTION_AUTOCAP, CALL-49 verbatim copy) are kept satisfied or updated in the same task (D-15)"
    - "Prototype create screens donate markup only — no prototype state, drafts, or mock preview math is ported (D-05)"
    - "The live Receipt preview stays flexbox-only (Satori parity contract, D-14)"
  artifacts:
    - path: "apps/web/app/new/components/ConvictionSliderField.tsx"
      provides: "Zone words + retained ConvictionBar import + CONVICTION_AUTOCAP warning"
      contains: "ConvictionBar"
    - path: "apps/web/app/new/components/PublishConfirmModal.tsx"
      provides: "Cream FINAL·CONFIRM panel with permanence copy over existing publish wiring"
      contains: "This is permanent"
    - path: "packages/ui/src/compound/Receipt.tsx"
      provides: "Rethemed flexbox-only live-preview card (DRAFT · LIVE PREVIEW header, conviction bar, if-correct/if-wrong rows)"
  key_links:
    - from: "apps/web/app/new/page.tsx"
      to: "packages/ui/src/compound/Receipt.tsx"
      via: "sticky live preview mode='preview'"
      pattern: "Receipt"
    - from: "ConvictionSliderField"
      to: "@call-it/ui ConvictionBar"
      via: "spec-asserted import"
      pattern: "@call-it/ui"
---

<objective>
Restyle the create flow (`/new`, including quote-compose mode) to the ROOT brutalist skin over the existing RHF/zod/hooks machinery, adding the two designed niceties with real wiring: conviction-zone words and stake quick-picks. Retheme the Receipt preview card.

Purpose: D-16 step 3 — "root visuals over existing app logic"; the app already implements most of v1-clean's create logic in production form.
Output: Rethemed create flow + preview; lockstep-updated publish/quote specs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-CONTEXT.md
@.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-UI-SPEC.md (Copywriting Contract: create-flow voice + conviction words)
@.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-PATTERNS.md
</context>

## Artifacts this phase produces (this plan)

- New UI constants in ConvictionSliderField: `CONVICTION_ZONES` — Hesitant (≤35), Confident (≤65), Bold (≤84), On record (≤100)
- New stake quick-pick control: `[5, 25, 50, 100]` chip row writing into the existing RHF stake field
- No new files — markup rewrites inside existing /new components + Receipt.tsx

Claude's-discretion choices documented (per CONTEXT): v1-clean drafts/autosave, shorthand parsing, and similar-calls modal are NOT included (backlog by default — not trivially cheap); the ≥50-char VERIFIED-CRITERIA badge ships only if the existing criteria-length validation already exposes the threshold (wire trivially or skip).

<tasks>

<task type="auto">
  <name>Task 1: Restyle field components in place (.brutal-input/.toggle-pill recipes)</name>
  <files>apps/web/app/new/components/MarketTypeSwitcher.tsx, apps/web/app/new/components/EventFields.tsx, apps/web/app/new/components/PriceTargetFields.tsx, apps/web/app/new/components/SpreadVsFields.tsx, apps/web/app/new/components/DeadlinePicker.tsx, apps/web/app/new/components/CriteriaField.tsx, apps/web/app/new/components/AdvancedSettings.tsx, apps/web/app/new/components/DuplicateWarning.tsx</files>
  <read_first>
    - apps/web/app/new/components/*.tsx for each file listed (FULL FILES — every RHF register/Controller binding, zod-driven error display, and data-testid stays)
    - "call it frontend/screens/create.jsx" (ROOT visual shell — the skin donor; its price/spread sections lack fields, so field STRUCTURE comes from the existing app components)
    - "call it frontend/v1-clean/screens/create.jsx" + create-shared.jsx (UX reference ONLY for field grouping/flow details — NEVER its rounded/Space Grotesk skin, D-01)
    - apps/web/tests/new-call-publish.spec.ts (the lockstep contract: CALL-49 verbatim copy in DuplicateWarning must survive)
  </read_first>
  <action>
    Restyle each field component's chrome to the token recipes keeping ALL form logic: text/number inputs → .brutal-input (bg var(--bg-secondary), 2px var(--border-subtle) border → white on focus, radius 0, italic muted placeholders); selects → .brutal-select; textareas → .brutal-textarea; MarketTypeSwitcher → three .brutal-card.interactive type cards with the selected card showing a chartreuse corner dot (screenshot canon) — same three market types, same RHF binding; DeadlinePicker → .chip row (24h/3d/7d/14d/30d/90d/Custom — existing options; active chip = cream inverse); AdvancedSettings → .toggle-pill for openToChallenges (the only real toggle — do NOT add the prototype's "Auto-post to socials"/"Allow followers" toggles, which have no backing, D-07/D-08); DuplicateWarning keeps its CALL-49 verbatim copy string "A nearly identical call is already live — quote it instead" restyled as a warning block (#FB923C border); CriteriaField in mono voice with "Why now? What's the read?" placeholder if a placeholder exists. Labels → .label-overline JBM. Every data-testid and RHF binding preserved.
  </action>
  <verify>
    <automated>pnpm --filter @call-it/web test</automated>
  </verify>
  <done>All field components carry the brutal recipes with untouched form logic; vitest green.</done>
  <acceptance_criteria>
    - DuplicateWarning.tsx still contains the exact string `A nearly identical call is already live — quote it instead` (CALL-49)
    - No new toggles without backing fields: grep -ci "auto-post\|allow followers" apps/web/app/new/components returns 0
    - RHF bindings unchanged: git diff shows no register/Controller/name= changes
    - `pnpm --filter @call-it/web test` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Conviction zone words + stake quick-picks + cream PublishConfirmModal; lockstep publish spec</name>
  <files>apps/web/app/new/components/ConvictionSliderField.tsx, apps/web/app/new/components/PublishConfirmModal.tsx, apps/web/app/new/page.tsx, apps/web/tests/new-call-publish.spec.ts</files>
  <read_first>
    - apps/web/app/new/components/ConvictionSliderField.tsx (ConvictionBar import from @call-it/ui + CONVICTION_AUTOCAP warning + useSettledCalls gate — ALL stay; spec line 103 asserts them)
    - apps/web/app/new/components/PublishConfirmModal.tsx (existing publish confirmation wiring — handlers stay)
    - apps/web/app/new/page.tsx (form layout + sticky Receipt preview mount + stake field binding for the quick-picks)
    - apps/web/tests/new-call-publish.spec.ts (FULL FILE — every source assert reconciled in this task)
    - "call it frontend/data.jsx" CONVICTION_WORDS reference (Hesitant≤35/Confident≤65/Bold≤84/On record≤100 — port the THRESHOLD VALUES as a local constant; never import data.jsx, D-05)
  </read_first>
  <action>
    ConvictionSliderField.tsx: add a CONVICTION_ZONES constant (Hesitant ≤35, Confident ≤65, Bold ≤84, On record ≤100) and render the active zone word next to the live conviction number in Archivo display voice; KEEP the ConvictionBar import from @call-it/ui, the muted→accent fill (UI-51), the CONVICTION_AUTOCAP warning copy, and the useSettledCalls gate (85%/10-settled floor) exactly. page.tsx: add the $5/$25/$50/$100 quick-pick chip row (.chip recipe) above/beside the stake input, writing through the existing RHF setValue for the stake field (respecting the zod min/max — $5 min, $100 max already enforced); restyle the page layout to the ROOT create shell (page-header "Go on record." voice, two-column desktop with sticky preview, single column <768px). PublishConfirmModal.tsx: cream .modal-panel with "FINAL · CONFIRM" JBM header and the permanence line "This is permanent. There's no edit after publish." — existing confirm/cancel wiring and any preflight untouched. new-call-publish.spec.ts: reconcile every source assert with the new markup (ConvictionBar/CONVICTION_AUTOCAP/CALL-49 should already hold; update any layout-literal asserts); never delete tests (D-15).
  </action>
  <verify>
    <automated>pnpm --filter @call-it/web test</automated>
  </verify>
  <done>Zone words + quick-picks live with real RHF wiring; cream confirm modal; spec reconciled; vitest green.</done>
  <acceptance_criteria>
    - ConvictionSliderField.tsx contains `Hesitant`, `Confident`, `Bold`, `On record`, AND still contains `ConvictionBar`, `@call-it/ui`, `CONVICTION_AUTOCAP`
    - page.tsx (or the stake field component) contains the quick-pick values 5/25/50/100 wired via the existing form setValue (identifier present)
    - PublishConfirmModal.tsx contains `FINAL` and `This is permanent. There's no edit after publish.`
    - new-call-publish.spec.ts test count not reduced
    - `pnpm --filter @call-it/web test` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Retheme the Receipt preview card (flexbox-only) + quote-compose surfaces; full gate</name>
  <files>packages/ui/src/compound/Receipt.tsx, apps/web/app/new/components/QuoteParentCard.tsx, apps/web/app/new/components/QuoteSuccess.tsx, apps/web/tests/quote-composer.spec.ts</files>
  <read_first>
    - packages/ui/src/compound/Receipt.tsx (FULL FILE — mode='preview' contract + any OG-fallback parity coupling with lib/og-fallback-render.ts; props stay; FLEXBOX ONLY)
    - apps/web/app/new/components/QuoteParentCard.tsx + QuoteSuccess.tsx (quote-compose mode chrome; wiring stays)
    - apps/web/tests/quote-composer.spec.ts (lockstep contract)
    - "call it frontend/screenshots" create frames description in RESEARCH (live-preview canon: "● Call It" + "DRAFT · LIVE PREVIEW" mono header, fill-in-the-blank headline dim-italic→bold-white, conviction bar, if-correct/if-wrong rows, "chain · arbitrum" footer — copy the FLOW/STATES, not the v1-clean rounded skin)
  </read_first>
  <action>
    Receipt.tsx: retheme the preview card to the canon — JBM mono header row "● Call It" + "DRAFT · LIVE PREVIEW", identity row (square avatar + handle), call headline where unfilled tokens render dim italic var(--text-muted) and filled tokens bold white (keep the existing token-fill logic), CONVICTION/STAKE stat pair, conviction bar fill, if-correct (+$ +rep, win color) vs if-wrong (−$ −rep, loss color) rows USING ONLY the existing computed payout/rep props (never the prototype's stake*1.8 mock math, D-05), mono footer "chain · arbitrum". STRICT FLEXBOX (Satori parity contract with lib/og-fallback-render.ts — do NOT touch og-fallback-render.ts this phase; UI/OG visual divergence is accepted per D-04). QuoteParentCard: .brutal-card with quoted-call summary + FOLLOWING/FADING stance pill. QuoteSuccess: cream success block with share affordances (existing wiring). quote-composer.spec.ts reconciled (D-15). Then run the full wave gate.
  </action>
  <verify>
    <automated>pnpm --filter @call-it/ui test && pnpm --filter @call-it/web test && pnpm --filter @call-it/shared build && pnpm --filter @call-it/ui build && pnpm --filter @call-it/web build && pnpm --filter @call-it/web lint</automated>
  </verify>
  <done>Preview card + quote surfaces rethemed; Receipt stays flexbox-only; full gate green.</done>
  <acceptance_criteria>
    - Receipt.tsx contains `DRAFT` and `LIVE PREVIEW` and contains no `display: 'grid'`/`grid-cols` (grep 0) and no `* 1.8` mock payout math (grep 0)
    - lib/og-fallback-render.ts unchanged: git diff --stat empty for it
    - quote-composer.spec.ts test count not reduced
    - Full chain exits 0 (shared → ui → web builds, ui+web vitest, lint)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| publish confirm → chain | createCall is a real $5-100 stake + $10 fee commitment |
| preview render → user expectation | preview must reflect real payout math, not mock |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.2-27 | Tampering | quick-picks bypassing zod stake bounds | mitigate | quick-picks write through existing RHF setValue + zod schema (no new validation path); values 5-100 inside existing bounds |
| T-09.2-28 | Spoofing | preview showing prototype mock payout math | mitigate | grep gate = 0 for mock math; preview consumes existing computed props only (D-05) |
| T-09.2-29 | Repudiation | permanence warning lost in restyle | mitigate | exact permanence string is an acceptance criterion |
| T-09.2-SC | Tampering | package installs | accept | zero new packages this phase |
</threat_model>

<verification>
- Web vitest per task; full shared→ui→web build + lint gate at Task 3
</verification>

<success_criteria>
- Create flow in ROOT skin over untouched RHF/zod/hooks; zone words + quick-picks really wired
- CALL-49/UI-51 asserts hold; Receipt preview flexbox-only with real math
</success_criteria>

<output>
Create `.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-10-SUMMARY.md` when done.
</output>
