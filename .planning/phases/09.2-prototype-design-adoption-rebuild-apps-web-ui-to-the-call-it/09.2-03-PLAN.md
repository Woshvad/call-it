---
phase: 09.2-prototype-design-adoption
plan: 03
type: execute
wave: 2
depends_on: ["09.2-01"]
files_modified:
  - apps/web/app/components/Icon.tsx
  - apps/web/app/components/AppShell.tsx
  - apps/web/app/components/Sidebar.tsx
  - apps/web/app/components/SystemTicker.tsx
  - apps/web/app/layout.tsx
  - apps/web/app/components/GlobalNav.tsx
  - apps/web/app/components/MobileDrawer.tsx
  - apps/web/app/components/NotificationBell.tsx
  - apps/web/tests/feed-shell.spec.ts
  - apps/web/tests/profile-shell.spec.ts
autonomous: true
requirements: [AUTH-44]
must_haves:
  truths:
    - "A 64px sticky header (brand + tagline, ⌘K search placeholder, wallet USDC pill from useUsdcBalance, existing NotificationBell) plus a 240px sidebar with //TAPE //YOU sections and a cream YOUR REP card replaces the 52px GlobalNav on desktop; MobileDrawer remains the <768px nav (D-10)"
    - "The shell is built AROUND ClientProviders — Providers.tsx is never edited and the provider-order AST test stays green (D-10, D-14)"
    - "SystemTicker ships behind a feature flag, default OFF, and the layout does not hardcode the 32px ticker offset when the flag is off (D-11)"
    - "The wallet pill renders handle + balance only — never a wallet address (AUTH-44, D-14)"
    - "Sidebar footer shows REAL deploy values (ARBITRUM SEPOLIA), never the prototype's fake block number (D-05)"
  artifacts:
    - path: "apps/web/app/components/AppShell.tsx"
      provides: "Client shell: optional ticker + 64px header + 240px sidebar grid + max-width 1180 main; pathname-based chrome gating"
      min_lines: 80
    - path: "apps/web/app/components/Sidebar.tsx"
      provides: "//TAPE //YOU nav sections with Next Links + cream .sidebar-rep card + .sidebar-meta footer"
      min_lines: 60
    - path: "apps/web/app/components/SystemTicker.tsx"
      provides: "32px price tape markup behind NEXT_PUBLIC_SYSTEM_TICKER flag, default OFF"
    - path: "apps/web/app/components/Icon.tsx"
      provides: "Inline SVG icon set ported from prototype components.jsx (square caps, miter joins)"
  key_links:
    - from: "apps/web/app/layout.tsx"
      to: "apps/web/app/components/AppShell.tsx"
      via: "AppShell mounted INSIDE ClientProviders, wrapping {children}"
      pattern: "<AppShell>"
    - from: "apps/web/app/components/AppShell.tsx"
      to: "apps/web/hooks/useUsdcBalance.ts"
      via: "wallet pill balance"
      pattern: "useUsdcBalance"
    - from: "apps/web/app/components/AppShell.tsx"
      to: "apps/web/app/components/MobileDrawer.tsx"
      via: "hamburger trigger below 768px (useIsMobile)"
      pattern: "MobileDrawer"
---

<objective>
Build the new app shell: ticker-flag + 64px header + 240px sidebar around `ClientProviders`, replacing GlobalNav on desktop while keeping MobileDrawer as the <768px nav.

Purpose: D-10/D-11 — the chrome every routed screen lives inside for the rest of the phase.
Output: AppShell/Sidebar/SystemTicker/Icon components, layout.tsx mount swap, shell-spec lockstep updates.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-CONTEXT.md
@.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-UI-SPEC.md (Shell Contract section)
@.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-PATTERNS.md (§4 App shell)
</context>

## Artifacts this phase produces (this plan)

- New components: `AppShell` (apps/web/app/components/AppShell.tsx), `Sidebar` (apps/web/app/components/Sidebar.tsx), `SystemTicker` (apps/web/app/components/SystemTicker.tsx), `Icon` (apps/web/app/components/Icon.tsx — 45-name inline SVG set)
- New env flag: `NEXT_PUBLIC_SYSTEM_TICKER` (ticker renders only when === '1'; default absent/off)
- New shell CSS consumption: `.app-header`, `.layout` page-level grid classes (grid allowed — page-level code, not packages/ui), `.brutal-search`, `.wallet-pill`, `.icon-btn`, `.nav-item`/`.nav-badge`/`.nav-section-label`, `.sidebar-rep`, `.sidebar-meta` from the 09.2-01 token layer
- Removed from layout: `GlobalNav` desktop mount (file kept only if MobileDrawer still imports from it; otherwise deleted)

<tasks>

<task type="auto">
  <name>Task 1: Create Icon, SystemTicker, Sidebar, and AppShell components</name>
  <files>apps/web/app/components/Icon.tsx, apps/web/app/components/SystemTicker.tsx, apps/web/app/components/Sidebar.tsx, apps/web/app/components/AppShell.tsx</files>
  <read_first>
    - "call it frontend/components.jsx" (Icon set lines 4-61: 1.5px stroke, strokeLinecap square, strokeLinejoin miter; SystemTicker at ~226; CreamCTA recipes)
    - "call it frontend/app.jsx" (shell structure lines 167-180: ticker → 64px header top:32px → 240px sidebar grid max-width 1180; NAV_TAPE/NAV_YOU registry lines 3-13)
    - apps/web/app/components/GlobalNav.tsx (the existing client-nav pattern being superseded: 'use client', useIsMobile ternaries, NotificationBell mount, MobileDrawer trigger)
    - apps/web/app/components/MobileDrawer.tsx (the <768px nav that STAYS — AppShell must trigger it)
    - apps/web/hooks/useUsdcBalance.ts (wallet pill data source)
    - apps/web/lib/relayer-client.ts (existing viewer-profile fetch helpers for the YOUR REP card — reuse, never invent a new endpoint)
    - apps/web/app/hooks/useIsMobile.ts (useSyncExternalStore pattern, server snapshot = mobile-first)
  </read_first>
  <action>
    All four files are 'use client' components in apps/web/app/components/ (NOT packages/ui — they use grid + window APIs; the ui package is Satori-constrained).
    Icon.tsx: port the prototype's inline SVG icon component (the 45-name registry) — props name/size; every path uses strokeWidth 1.5, strokeLinecap="square", strokeLinejoin="miter". Port only the icon names the shell + later screens actually reference (tape, you, duel, leaderboard, profile, create, bell, search, settings, arrow/caret names plus the obvious chart/clock/check/x set); add names lazily — do not blind-copy all 45 if unused.
    SystemTicker.tsx: 32px sticky top z-60 bg var(--bg-ticker) JBM 11px marquee per .system-ticker recipe — markup only, no data fetch (no price endpoint exists, D-11). Render ONLY when process.env.NEXT_PUBLIC_SYSTEM_TICKER === '1'; export a helper or accept an `enabled` prop AppShell computes; default OFF.
    Sidebar.tsx: 240px sticky aside — section labels "// TAPE" and "// YOU" (.nav-section-label JBM 10px tracking 0.14em), Next.js Link nav items (.nav-item; active route = bg var(--bg-secondary) + 3px var(--accent-win) left border, derive active from usePathname). TAPE section: The Tape (/), Leaderboard (/leaderboard). YOU section: Make a call (/new), Your profile (/profile/<viewer-address> via existing viewer identity), Settings (/profile/<address>/settings), Disputes (/disputes). Duel nav item gets the orange .nav-dot flag (prototype canon). Cream .sidebar-rep YOUR REP card pinned to the bottom: 44px Archivo 900 rep number + accuracy line fed from the EXISTING viewer profile fetch in lib/relayer-client.ts; NO sparkline v1 (D-10); if the viewer has no profile data, hide the card (D-07 degrade-to-hidden, never fake). .sidebar-meta mono footer with REAL values: app version from package.json + "ARBITRUM SEPOLIA" (current deploy) — never the prototype's fake "BLOCK 8,402,1??" (D-05).
    AppShell.tsx: composes ticker (flag-gated) + 64px sticky header + grid 240px/1fr layout + main (max-width 1180, padding 0 32px 80px). Header (page-level grid allowed): brand block "CALL IT" Archivo 900 22px + "be right in public" JBM 10px tagline linking to /; center .brutal-search visual placeholder with ⌘K kbd hint (allowed as visual-only per D-10, placeholder text "Search callers, calls, assets — ARB, ETH, @veda"); right side: existing NotificationBell + .wallet-pill showing formatted USDC balance from useUsdcBalance + viewer handle ("2,840.21 USDC · @handle" format) — HANDLE only, never an address; if no handle, balance only (AUTH-44). Sticky offsets are DERIVED: header top = ticker enabled ? 32px : 0; sidebar top = header bottom — never hardcode the prototype's top:96px (D-11). Pathname gating: on /signin and /onboarding/* render children full-bleed with NO sidebar (landing/onboarding own their chrome); everywhere else full shell. Below 768px (useIsMobile): hide sidebar, render hamburger in header that opens the existing MobileDrawer (follow GlobalNav's current hand-off pattern). The shell renders its static chrome unconditionally and gates only auth-dependent slots (wallet pill, rep card) so the Privy ssr:false late-hydration flash shows stable chrome (RESEARCH wiring-risk #4).
  </action>
  <verify>
    <automated>pnpm --filter @call-it/web test</automated>
  </verify>
  <done>All four components exist, compile, and follow the shell contract; ticker is flag-gated; wallet pill is handle-only.</done>
  <acceptance_criteria>
    - AppShell.tsx contains `NEXT_PUBLIC_SYSTEM_TICKER` gating and contains NO hardcoded `top: '96px'`/`top:96px` (grep -c "96px" apps/web/app/components/AppShell.tsx returns 0)
    - AppShell.tsx contains `useUsdcBalance` and `NotificationBell` and `MobileDrawer`
    - AppShell.tsx + Sidebar.tsx contain NO raw address rendering: grep -c "formatAddress\|slice(0, 6)\|slice(2" apps/web/app/components/AppShell.tsx apps/web/app/components/Sidebar.tsx returns 0
    - Sidebar.tsx contains `// TAPE` and `// YOU` and `sidebar-rep` and `ARBITRUM SEPOLIA`, and does NOT contain `8,402` (no fake block number)
    - Icon.tsx contains `strokeLinejoin="miter"` and `strokeLinecap="square"`
    - `pnpm --filter @call-it/web test` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Mount AppShell in layout.tsx, retire GlobalNav desktop chrome, restyle NotificationBell trigger, lockstep shell specs</name>
  <files>apps/web/app/layout.tsx, apps/web/app/components/GlobalNav.tsx, apps/web/app/components/MobileDrawer.tsx, apps/web/app/components/NotificationBell.tsx, apps/web/tests/feed-shell.spec.ts, apps/web/tests/profile-shell.spec.ts</files>
  <read_first>
    - apps/web/app/layout.tsx (post-09.2-01 state: fonts mounted, ClientProviders > GlobalNav > children)
    - apps/web/app/components/GlobalNav.tsx + MobileDrawer.tsx (what the drawer needs from its parent: open state, trigger, nav registry)
    - apps/web/app/components/NotificationBell.tsx (existing poll wiring — logic untouched, trigger chrome only)
    - apps/web/tests/feed-shell.spec.ts + apps/web/tests/profile-shell.spec.ts (READ FULLY — any assert on GlobalNav markup/52px header must be updated in THIS task per D-15)
    - apps/web/tests/privy-provider-order.ast.test.ts (the lock you must not trip — confirms why Providers.tsx is untouchable)
  </read_first>
  <action>
    layout.tsx: replace the <GlobalNav/> mount with <AppShell>{children}</AppShell> INSIDE <ClientProviders> (AppShell needs useUsdcBalance/NotificationBell context — it must be inside the providers; Providers.tsx itself is NEVER edited, D-10). Keep import './globals.css', force-dynamic, metadata, and font variables exactly as 09.2-01 left them.
    GlobalNav.tsx: desktop chrome is superseded. If MobileDrawer or its trigger logic lives here, extract what MobileDrawer needs into AppShell's hamburger handling; then delete GlobalNav.tsx if nothing imports it anymore, or reduce it to a re-export shim if tests reference it. Retire its `#2A2A30` border gray (D-02).
    MobileDrawer.tsx: retheme chrome to the token layer (bg var(--bg-secondary), 2px borders, .nav-item recipes, radius 0); nav logic, focus handling, and any data-testid hooks stay (D-14).
    NotificationBell.tsx: restyle the trigger as .icon-btn (square, 2px border, radius 0); poll wiring and inbox-open logic untouched (inbox panel itself is plan 09.2-13).
    Specs: update feed-shell.spec.ts / profile-shell.spec.ts in lockstep — any literal asserting the old 52px GlobalNav, old grays, or old header text updates to the new shell reality (64px header, AppShell). Never delete a test to make it pass; update the assert to the new contract (D-15).
  </action>
  <verify>
    <automated>pnpm --filter @call-it/web test && pnpm --filter @call-it/shared build && pnpm --filter @call-it/web build && pnpm --filter @call-it/web lint</automated>
  </verify>
  <done>AppShell is the mounted chrome; provider AST test green; no orphaned GlobalNav import; shell specs updated; build + lint green.</done>
  <acceptance_criteria>
    - layout.tsx contains `<AppShell>` inside `<ClientProviders>` and no longer mounts `<GlobalNav`
    - apps/web/app/Providers.tsx is BIT-IDENTICAL to its pre-plan state: git diff --stat apps/web/app/Providers.tsx returns empty
    - privy-provider-order.ast.test.ts passes (included in web vitest)
    - grep -rn "#2A2A30" apps/web/app/components | wc -l returns 0 (third legacy gray retired from nav, D-02)
    - feed-shell.spec.ts and profile-shell.spec.ts contain no asserts referencing GlobalNav markup that no longer exists (read + update; tests not deleted)
    - `pnpm --filter @call-it/web test && pnpm --filter @call-it/shared build && pnpm --filter @call-it/web build && pnpm --filter @call-it/web lint` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → relayer API | sidebar rep card + wallet pill render viewer identity data |
| public viewer → shell chrome | shell renders on public routes (/call, /leaderboard, /profile) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.2-07 | Information Disclosure | wallet pill / rep card leaking wallet addresses (AUTH-44) | mitigate | handle-only render pinned in acceptance criteria (grep gate for address-formatting helpers); AUTH-44 invariant test in per-task vitest gate |
| T-09.2-08 | Elevation of Privilege | shell mount accidentally altering provider order or middleware behavior | mitigate | Providers.tsx bit-identical gate (git diff empty); middleware.ts not in files_modified; AST test in vitest gate |
| T-09.2-09 | Spoofing | fake "live" data in ticker/footer (prototype mock values) | mitigate | ticker flag default OFF with no data; footer real-values-only acceptance criterion (no fake block number) |
| T-09.2-SC | Tampering | package installs | accept | zero new packages this phase |
</threat_model>

<verification>
- Web vitest green after each task (AST provider lock + AUTH-44 are the tripwires)
- Wave gate: shared build → web build → lint exit 0
- Manual (deferred to plan 15 checkpoint): late-hydration flash stability, 767/768px drawer fold
</verification>

<success_criteria>
- Desktop shell = optional ticker + 64px header + 240px sidebar + 1180px main; mobile keeps MobileDrawer
- Providers.tsx untouched; AST test green; AUTH-44 holds in all new chrome
- Ticker flag default OFF with no hardcoded offsets
</success_criteria>

<output>
Create `.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-03-SUMMARY.md` when done.
</output>
