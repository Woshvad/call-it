---
phase: 09.2
slug: prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it
status: draft
shadcn_initialized: false
preset: none
created: 2026-06-10
---

# Phase 09.2 — UI Design Contract

> Visual and interaction contract for the prototype-design-adoption phase. This phase adopts an EXISTING, COMPLETE design prototype (`call it frontend/`, ROOT variant) as the design of record. Nothing here is invented — every value below is EXTRACTED from the prototype and formalized as the contract. Where the prototype is silent (mobile, modals beyond the template, uncovered surfaces), this contract specifies how its primitives extend (CONTEXT D-13) and marks executor discretion explicitly.
>
> Sources: 09.2-CONTEXT.md D-01..D-16 (all locked), 09.2-RESEARCH.md (compatibility audit), `call it frontend/styles.css` (918 lines, tokens at lines 3-41), `call it frontend/components.jsx` (16 components), `call it frontend/app.jsx` (shell), `call it frontend/screens/*.jsx`, screenshots `home.png` + `settled.png` (ROOT ground truth).

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — manual design system extracted from the prototype (no shadcn; `components.json` absent and NOT to be added) |
| Preset | not applicable |
| Component library | existing `@call-it/ui` (CVA + Radix slider/toast) RETHEMED to prototype primitive recipes — no new component library |
| Icon library | prototype inline SVG `Icon` set (45 names, 1.5px stroke, `strokeLinecap="square"`, `strokeLinejoin="miter"` — `components.jsx:4-61`); NO external icon package |
| Fonts | **Archivo** 800/900 (display), **Inter** 400/500/600 (body), **JetBrains Mono** 500/600/700 (all data/labels/microcopy) — loaded via `next/font` (D-04). OG images KEEP Syne/SpaceGrotesk TTFs in `apps/web/app/fonts/` this phase; OG Archivo refresh is optional final wave |
| CSS pipeline | Tailwind **v3.4 classic** with `postcss.config.js` (NO v4 `@theme` port — D-14). Prototype primitives land as plain CSS classes in `apps/web/app/globals.css`; Tailwind utilities and ported classes are both acceptable per component (Claude's discretion) |

---

## Token Layer (single source of truth)

Prototype `:root` (`styles.css:3-41`) becomes the ONLY token layer, pasted into `apps/web/app/globals.css` as CSS custom properties (D-02). `packages/ui/tailwind.preset.ts` color names remap to `var(--*)` **keeping the existing `brand-*` alias names** so ~30 `@call-it/ui` consumers keep compiling.

```css
/* Backgrounds */
--bg-primary: #09090E;    --bg-secondary: #111118;  --bg-tertiary: #1A1A24;
--bg-quaternary: #13131D; --bg-ticker: #0F0F18;     --bg-inverse: #F5F1E8;   /* cream — signature */
/* Borders */
--border-subtle: #1E1E2E; --border-active: #2E2E42; --border-strong: #FFFFFF; --border-accent: #E8F542;
/* Accents (semantic — each has ONE job) */
--accent-win: #E8F542;     /* wins, success, accent highlights */
--accent-loss: #F87171;    /* losses, fade, error */
--accent-neutral: #3B82F6; /* pending, info, live indicators */
--accent-warning: #FB923C; /* warnings, high-conviction, exits */
--accent-duel: #A855F7;    /* 1v1 challenges ONLY */
/* Text */
--text-primary: #F1F5F9; --text-secondary: #94A3B8; --text-tertiary: #64748B;
--text-muted: #334155;   --text-inverse: #09090E;
/* Shadows — hard offset, zero blur, always black */
--shadow-brutal: 4px 4px 0 0 #000; --shadow-brutal-lg: 8px 8px 0 0 #000; --shadow-brutal-sm: 2px 2px 0 0 #000;
/* Fonts */
--font-display: "Archivo", "Inter", system-ui, sans-serif;
--font-sans: "Inter", system-ui, sans-serif;
--font-mono: "JetBrains Mono", ui-monospace, monospace;
```

**Retired by end of phase (D-02):** legacy grays `#18181B`, `#27272A`, `#A1A1AA`, `#2A2A30`.
**Replaced (D-03):** app green/red `#22C55E`/`#EF4444` → prototype `#E8F542`/`#F87171` everywhere money/outcome color appears. CONTRARIAN HIT stamp = `--accent-win` (fixes the documented stale `outcome-contrarian #A855F7` token in `Stamp.tsx:13-15`). `#A855F7` remains duel/challenger identity ONLY.
**Global base:** `border-radius: 0` everywhere (avatars become SQUARE). Borders 2-3px. `::selection { background: var(--accent-win); color: #000 }`. Placeholders italic `--text-muted`. Body: 14px Inter, line-height 1.45, antialiased, on `--bg-primary`. Delete `layout.tsx` inline body styles.

---

## Spacing Scale

Base unit 4px. For NET-NEW surfaces (uncovered by the prototype — D-13) use this scale:

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Compact element spacing, chip gaps |
| md | 16px | Default element spacing, stat-block padding |
| lg | 24px | Card padding (`.brutal-card`), header gaps |
| xl | 32px | Hero card padding, main horizontal padding |
| 2xl | 48px | Settled-receipt hero padding, section breaks |
| 3xl | 64px | Page-level spacing |

**Exceptions (prototype-verbatim — copy EXACTLY when porting prototype markup, never round to the scale):** 2px (nav col gap, bar gap), 6px (live dot, rep-num margins), 10px (nav-item padding, bar height, brand gap), 12px (row gap, nav icon gap), 14px (header padding, sidebar padding, activity-row gap, section-divider gap), 18px (mini-card padding), 20px (btn padding-x, tab padding-x), 28px (ticker gap, settled spread margins), 36px (modal-panel padding), 40px (page-header padding-top, modal overlay padding), 80px (main padding-bottom). Mobile: ≥44px touch targets on interactive controls (Phase 9 carry-over).

---

## Typography

Strict two-voice system (screenshot ground truth): **ultra-heavy Archivo (800/900), usually uppercase, negatively tracked** for headlines/call statements/big numerals; **tiny letter-spaced UPPERCASE JetBrains Mono** for every label/metadata/microcopy. Inter is the quiet body voice in between.

| Role | Font | Size | Weight | Line height | Tracking | Class |
|------|------|------|--------|-------------|----------|-------|
| Micro label / overline | JBM | 10-11px | 600 | 1.4 | 0.08-0.14em, UPPERCASE | `.label-overline`, `.nav-section-label`, `.stat-label` |
| Mono data | JBM | 12-13px | 500-600 | 1.4 | 0.02-0.06em | `.mono`, `.wallet-pill`, `Countdown` |
| Body | Inter | 14px | 400-500 | 1.45 | normal | base |
| Body large / page sub | Inter | 15-22px | 400 | 1.4 | normal | `.page-header .sub` |
| H3 | Archivo | 22px | 700 | 1.15 | -0.015em | `.h-3` |
| Statement | Archivo | 28px | 800 | 1.1 | -0.02em | `.h-statement` |
| H2 | Archivo | 32px | 800 | 1.05 | -0.025em | `.h-2` |
| H1 | Archivo | 48px | 800 | 0.95 | -0.035em, UPPERCASE | `.h-1` |
| Page title | Archivo | 56px | 900 | 0.95 | -0.04em, UPPERCASE | `.page-header h1` |
| Display / stamp | Archivo | 96-132px (stamp 120px on settled receipt; landing `clamp(64px, 10vw, 132px)`) | 900 | 0.85 | -0.04 to -0.055em, UPPERCASE | `.outcome-stamp`, `.lp-hero-headline` |

Weight contract: **Archivo only 700/800/900. Inter only 400/500/600. JBM only 500/600/700.** No other weights load via `next/font`.

Mobile clamps for 56-132px display type and 4-5-col stat rows: **executor discretion** (prototype has zero `@media`), preserving Phase 9's 375px requirements (single column, full-width actions, drawer nav).

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#09090E` + surfaces `#111118`/`#1A1A24`/`#13131D`/`#0F0F18` | Canvas, cards, ticker, table headers |
| Secondary (30%) | Cream `#F5F1E8` inverse blocks + white/gray borders + `#F1F5F9`/`#94A3B8`/`#64748B` text | Primary CTAs, modal panels, sidebar rep card, active chips/toggles — the signature inverse treatment (black text `#000` on cream, 2-3px black border, brutal shadow) |
| Accent (10%) | `#E8F542` chartreuse | Reserved-for list below |
| Destructive / loss | `#F87171` | LOUD AND WRONG stamp/tint, fade side (OddsBar right, `.btn.fade`), error toasts/states, slash amounts, down-arrows |
| Neutral info | `#3B82F6` | Pending/info states only |
| Warning | `#FB923C` | Warnings, high-conviction flags, caller-exit, duel nav dot |
| Duel identity | `#A855F7` | 1v1 challenge UI only (`.btn.duel`, OPEN/DUEL KING pills, challenger bar side) |

**Accent `#E8F542` reserved EXCLUSIVELY for:** win outcome stamps/text (CALLED IT, CONTRARIAN HIT, FADED CORRECTLY) + win tints `rgba(232,245,66,0.04-0.05)`; OddsBar/split-bar follow side; conviction-bar high fill; active tab underline (`.tab.active`); active nav-item left border + `.nav-badge`; live dot + ticker up-arrows + positive deltas/trends; `.your-row-tint` left border; `.pill.solid-win`/`.pill.win`; selection highlight; sidebar rep sparkline context; TOP 1% GLOBAL badge bg; `--border-accent`. **Accent is NEVER a generic button/interactive color — the primary CTA is CREAM, not chartreuse.**

Outcome color map (canonical — from `receipt.jsx:284-290` SETTLED_OUTCOMES; words come from app `lib/outcome-word.ts`, NEVER from `data.jsx` OUTCOMES which references broken CSS vars):

| Outcome word | Color | Hero tint | Sub-line |
|---|---|---|---|
| CALLED IT | `var(--accent-win)` | `rgba(232,245,66,0.04)` | "Right." |
| LOUD AND WRONG | `var(--accent-loss)` | `rgba(248,113,113,0.04)` | "The market remembers." |
| COLD CALL | `var(--text-tertiary)` | `rgba(100,116,139,0.04)` | "Didn't hit." |
| CONTRARIAN HIT | `var(--accent-win)` | `rgba(232,245,66,0.05)` | "Faded the consensus. Right." |
| FADED CORRECTLY | `var(--accent-win)` | `rgba(232,245,66,0.04)` | "Right to doubt." (per-viewer only — D-09 fader guard) |

Avatar grad palette (square, radius 0): a `#E8F542` · b `#FB923C` · c `#A855F7` (white text) · d `#22D3EE` · e `#F472B6` · f `#FBBF24`; black initials (except c), 2px white border (1px sm / 3px xl).

---

## Primitive Class Inventory (port verbatim into globals.css)

From `styles.css` — these are the design vocabulary for ALL surfaces including ones the prototype doesn't cover (D-13):

| Primitive | Recipe (key values) |
|---|---|
| `.brutal-card` (+`.heavy`/`.hero`/`.cream`/`.interactive`/`.bracketed`) | bg-secondary, 2px border-subtle, padding 24 (hero 32, settled hero 48), radius 0; heavy/hero = 3px white border + `--shadow-brutal`; cream = `#F5F1E8` bg, black text, 3px black border, brutal shadow; interactive hover = white border + shadow + `translate(-2px,-2px)` |
| `.btn` (+`.cream`/`.big`/`.full`/`.outline-white`/`.fade`/`.duel`/`.ghost`) | Archivo 700 13px UPPERCASE 0.04em, padding 12/20, 2px border-active; **cream = signature primary CTA**: cream bg, 800, black 2px border, brutal shadow, hover `translate(2px,2px)`+`--shadow-brutal-sm`, active `translate(4px,4px)`+no shadow, 0.08s linear; big = 18/28, 15px, 3px border |
| `.brutal-input`/`.brutal-textarea`/`.brutal-select`/`.brutal-slider` | bg-secondary, 2px border-subtle → white on focus, padding 14/16, radius 0; slider thumb 26px cream square, 2px black border, `2px 2px 0 #000` |
| `.pill` (+`solid-win`/`solid-duel`/`solid-warn`/`win`/`loss`/`duel`/`warn`/`neutral`/`muted`/`tag`) | JBM 11px 600 UPPERCASE 0.06em, padding 3/8, 1px currentColor border |
| `.chip` (+`.active`) | JBM 11px 600 UPPERCASE, padding 6/12, 1.5px border-active; active = cream bg, black text/border |
| `.toggle-pill` (+`.on`) | as chip at padding 10/16, 2px border; on = cream inverse |
| `.tabs`/`.tab` (+`.count`) | Archivo 13px 700 UPPERCASE, padding 14/20, 3px transparent bottom border; active = white text + `--accent-win` underline |
| `.brutal-table` | th: JBM 10px 600 0.1em UPPERCASE tertiary on bg-quaternary, 2px bottom border; td padding 16, 1px bottom border; row hover bg-tertiary |
| `.stat-block` (+`.bracketed` with `.br-bl`/`.br-br` spans) | padding 16, 1px border-subtle, bg-quaternary; label JBM 10px; value JBM 22px 600 (or `.display` Archivo 28px 900) |
| `.label-overline` | JBM 11px 600 0.08em UPPERCASE tertiary |
| `.section-divider` | 1px line + JBM 11px 0.14em UPPERCASE title |
| `.page-header` | h1 Archivo 56px 900; sub 15px secondary max 64ch, `.em` = accent 600 |
| `.outcome-stamp` | Archivo 900, UPPERCASE, line-height 0.85, `rotate(-1deg)`, `text-shadow: 4px 4px 0 #000`, `stampReveal` 0.4s `cubic-bezier(0.34,1.56,0.64,1)` (scale 1.3→0.96→1 overshoot) |
| `.modal-overlay`/`.modal-panel` | overlay `rgba(0,0,0,0.82)` + `blur(4px)`, z-200; **panel = cream, black text, 3px black border, `--shadow-brutal-lg`, max-width 620, padding 36, max-height 90vh** — the TEMPLATE for all 5 app modals (D-13) |
| `.bracketed` | 14px L-shaped 1px `--border-active` corner brackets (::before/::after top + `.br-bl`/`.br-br` child spans bottom) |
| `.brutal-bar` (+`.split` follow/gap/fade, caller/challenger) | 10px bar, 1px border; split gap = 2px black; fill transitions 0.4s |
| `.activity-row` | grid auto/1fr/auto, padding 14/0, 1px bottom border, `stampIn` 0.18s — template for notification inbox |
| `.avatar` (+sm/lg/xl, grad a-f) | 40px (28/56/80) square, Archivo 900 initials |
| `.your-row-tint` | `linear-gradient(90deg, rgba(232,245,66,0.06), transparent)` + 3px accent left border |
| `.system-ticker` | 32px sticky z-60, bg-ticker, JBM 11px, 80s marquee, pause on hover |
| `.live-dot` | 6px accent square, 1.4s opacity pulse |
| `.stagger` | children `hardCut` 0.16s fade with 30ms per-child delay |
| Utilities | `.row`/`.col`/`.spread`/`.mono`/`.muted`/`.text-win`/`.text-loss`/`.text-warn`/`.text-duel`/`.text-neutral`/`.bg-cream`/`.uppercase`/`.slot-num`, `.corner-stamp-tl/tr/bl/br` |

`.brutal-search` + `.wallet-pill` + `.icon-btn` + `.nav-item`/`.nav-badge`/`.nav-dot` + `.sidebar-rep` + `.sidebar-meta` port with the shell.

**Grid caveat:** prototype classes using `display: grid` (`.app-header`, `.layout`, `.activity-row`) are fine in PAGE-level code; any primitive promoted into `packages/ui` or used in OG paths MUST be re-laid-out flexbox-only (Satori ban, `no-display-grid-in-og` lint, error-level — D-14).

---

## Component Mapping (`@call-it/ui` retheme + prototype components)

| Prototype component | App target |
|---|---|
| `CreamCTA` / `OutlineCTA` | `Button` (CVA) — primary intent → `.btn.cream` recipe + press physics; secondary → `.btn.outline-white`; ghost → `.btn.ghost`. Accent shadow on Card DISAPPEARS (all shadows black) |
| `OutcomeStamp` | `Stamp` (framer-motion) — restyle to 120px Archivo 900, rotate(-1deg), `text-shadow 4px 4px 0 #000`, stampReveal overshoot |
| `OddsBar` / `ConfidenceBar` | `MarketPositioningBar` / `ConvictionBar` (Radix slider keeps logic) → `.brutal-bar` recipes |
| `Pill` / `VerifiedBadge` (x/fc/arb/criteria/open/king/topx) | `Tag` / existing `VerifiedBadge` → `.pill` recipes (inline-flex preserved for Satori parity) |
| `StatBlock` (bracketed, mono num) | net-new shared pattern (flexbox if in packages/ui) |
| `Avatar` (grad a-f, square) | replaces all round avatars (`border-radius: 50%` → 0) |
| `Tabs` | feed/page tab rows |
| `Icon` (45 names) | net-new icon component (apps/web); replaces ad-hoc icons |
| `Sparkline` / `RepBadge` / `Countdown` / `Bracketed` / `SystemTicker` | net-new; Sparkline hard angles `strokeLinejoin="miter"`; Countdown `D HH:MM:SS` JBM; SystemTicker behind flag default OFF (D-11) |
| `.modal-panel` cream template | FollowFadeModal, CallerExitModal, PositionExitModal, ChallengeFormModal, PublishConfirmModal, DisputeModal, ProvenanceModal — all keep their existing amount-based logic/wiring (D-06); restyle only |
| CornerBrackets (existing) | `.bracketed` recipe |
| Skeleton / Toast | retheme: radius 0, hard edges; error toast `#F87171` |

---

## Shell Contract (D-10, D-11)

- **SystemTicker:** 32px sticky top, z-60 — behind a feature flag, **default OFF** (no price endpoint). Layout MUST NOT hardcode the 32px offset when flag is off (header `top` and sidebar `top:96px` become conditional).
- **Header:** 64px sticky, 3-col grid (page-level grid OK): brand "CALL IT · be right in public" (Archivo 900 22px + JBM 10px tagline) | center `.brutal-search` with `⌘K` kbd (visual placeholder allowed) | right: existing NotificationBell as `.icon-btn` + wallet `.wallet-pill` "2,840.21 USDC · @handle" fed by existing `useUsdcBalance` (handle, never address — AUTH-44).
- **Sidebar:** 240px sticky, `// TAPE` and `// YOU` section labels (JBM 10px 0.14em), Next.js `Link` nav items (active = bg-secondary + 3px accent left border), cream `.sidebar-rep` YOUR REP card pinned bottom (44px Archivo 900 rep number from existing viewer profile data; NO sparkline v1), `.sidebar-meta` mono footer with REAL values (e.g. "V0.4.1 · ARBITRUM SEPOLIA" — never the prototype's fake block number).
- **Main:** max-width 1180px, padding `0 32px 80px`.
- **Mobile <768px:** sidebar collapses into the existing MobileDrawer; header persists; ≥44px touch targets.
- Shell is built AROUND `ClientProviders` — never inside `Providers.tsx` (provider order AST-locked). Must tolerate Privy `ssr:false` late-hydration flash.
- **Landing (logged-out):** adopt `home.jsx` — full-bleed, no sidebar; hero "BE RIGHT / IN PUBLIC." (`.lp-hero-headline`, second line accent); status strip "■ · LIVE NOW · {real callers} CALLERS ON RECORD · {real pot} POT ·" (live dot + JBM) — platform totals have no source → degrade per D-07 (hide the numbers, keep the LIVE strip working parts); implementation route (public `/` carve-out vs restyled `/signin`) is executor discretion within D-12 constraints (middleware public-prefixes, privy-token cookie flow, onboarding redirect chain, signin Playwright testids all keep working).

---

## Interaction & Motion Contract

| Interaction | Spec |
|---|---|
| Cream CTA press physics | hover: `translate(2px,2px)` + shadow 4px→2px; active: `translate(4px,4px)` + shadow→0; 0.08s linear |
| Interactive card hover | white border + brutal shadow + `translate(-2px,-2px)`, 0.12s linear |
| Standard transitions | 0.12s linear (color/border/bg) — never eased, never longer |
| Outcome stamp reveal | `stampReveal` 0.4s `cubic-bezier(0.34,1.56,0.64,1)`: scale 1.3 rotate(-2deg) opacity 0 → 0.96/-1deg → 1/-1deg |
| Feed/list entry | `.stagger` hardCut 0.16s, 30ms/child; activity rows `stampIn` 0.18s translateY(-8px) |
| Bars | fill width/flex-basis 0.4s `cubic-bezier(0.16,1,0.3,1)` — slide, never jump |
| Live dot | 1.4s opacity pulse |
| Ticker | 80s linear marquee, pause on hover (flag-gated) |
| Modals | `.modal-overlay` scrim 0.82 + blur(4px); no fade-to-black page transitions |
| Focus | inputs: border-subtle → border-strong; keyboard focus must stay visible (radius-0 outlines acceptable) |

---

## State & Degradation Contract (D-07, D-08)

- **Missing data → HIDDEN, never faked.** Applies to: feed pool $ / follow-fade-challenge counts / caller rep-accuracy-streak, leaderboard sparklines / 7d deltas / best-category / badges / hero StatBlocks, profile rep-history / calibration / ROI / category bars / followers / notable receipts, platform totals, duel live spread, settled-timeline middle milestones (ship 2-node created→settled), block/tx footers (real provenance data only).
- **No dead controls ship.** Cut (do not render) unless trivially wired: SORT, non-filtering category chips, 7D/30D toggles without refetch, NEXT-10 pagination, leaderboard row click, collapse toggles. Existing working controls (share intents, SHARE AS FRAME) supersede dead prototype equivalents (D-09).
- **Empty states:** prototype-voice mono microcopy, e.g. inbox "No notifications yet", feed "Nothing on the tape yet. Go on record." — label-overline style, never a bare blank.
- **Loading:** rethemed Skeletons (radius 0, hard edges) in card slots.
- **Errors:** existing toast pathway restyled — `#F87171` accent, problem + next step (e.g. "Couldn't load the tape. Retry."), never silent.
- **Money flows stay amount-based:** follow/fade/challenge/exit CTAs open the existing modals (D-06) — prototype one-click toggles are NOT adopted.

---

## Copywriting Contract

Voice: declarative second person; tiny letter-spaced UPPERCASE MONOSPACE for every label/microcopy; interpunct "·" as the universal separator.

| Element | Copy |
|---------|------|
| Brand tagline | "CALL IT · be right in public" |
| Landing hero | "BE RIGHT / IN PUBLIC." + "A reputation market for crypto calls. Stake on what you believe. Get a receipt that lasts forever." |
| Landing primary CTA | "Make your first call →" (cream, big); secondary "Sign in →" (cream) |
| Create flow voice | "Go on record." / "MARK IT." / "permanent on settle" / "Why now? What's the read?" |
| Conviction zone words | Hesitant (≤35) · Confident (≤65) · Bold (≤84) · On record (≤100); stake quick-picks $5/$25/$50/$100 |
| Publish confirmation (destructive — permanent) | "FINAL · CONFIRM" mono header + "This is permanent. There's no edit after publish." — cream `.modal-panel`, existing PublishConfirmModal wiring |
| Caller exit (destructive — rep slash) | existing CallerExitModal copy + real repDelta, restyled cream panel; exit framed as a slash (warning `#FB923C`) |
| Dispute raise (destructive — $5 bond) | existing DisputeModal copy, restyled; bond amount explicit before confirm |
| Empty state (inbox) | "No notifications yet" |
| Empty state (feed/tape) | heading "NOTHING ON THE TAPE" (mono overline) + body "Be the first to go on record." + cream CTA to /new |
| Error state | problem + action: "Couldn't load {thing}. Retry." — `#F87171`, retry affordance |
| Settled receipt subs | per outcome map above ("Right." / "The market remembers." / "Didn't hit." / "Faded the consensus. Right." / "Right to doubt.") |
| Back link | "← Back to the tape" (ghost btn) |
| Metadata footers | mono lowercase/UPPER mixed, "·"-separated, REAL values only (e.g. "settled · 0xa3f4…91d2 · block N · {UTC time}") |
| Identity | handles ONLY, never wallet addresses (AUTH-44) — "@handle" / "you.eth" style |

Outcome WORDS always come from `apps/web/lib/outcome-word.ts` (incl. D-09 per-viewer FADED CORRECTLY guard); this contract supplies colors/tints/subs only.

---

## Hard Invariants (every new markup — D-14)

1. AUTH-44: handles only, never wallet addresses, in any rendered identity.
2. Outcome words from `lib/outcome-word.ts`; colors from SETTLED_OUTCOMES (`receipt.jsx:284-290`); NEVER `data.jsx` OUTCOMES (broken vars).
3. Contract addresses only from `@call-it/shared`.
4. `MiniAppReady` mounted on every `/call/[id]` branch.
5. Flexbox-only in `packages/ui` and ALL OG paths (lint-enforced); CSS grid allowed in page-level code only.
6. Tailwind v3.4 classic + `postcss.config.js`; no v4 `@theme`.
7. `@farcaster/auth-kit/styles.css` stays in the cascade.
8. Existing Playwright `data-testid` hooks preserved (signed-in, disclaimer, onboarding dots).
9. Provider order untouched (`privy-provider-order.ast.test.ts`); shell wraps around `ClientProviders`.
10. Prototype = markup + token donor ONLY — no prototype state, mock data, hash routing, or `tweaks-panel.jsx` (D-05).
11. Never fight inline styles with `!important` — per-file markup rewrites route-by-route (wiring-risk #1).
12. Tests-of-record updated deliberately, never deleted; visual goldens regenerated only in the final wave after operator approval (D-15).

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none — shadcn not initialized, not used | not applicable |
| third-party | none | not applicable |

No new UI dependencies enter in this phase (icon set is inline SVG; fonts via `next/font`).

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
