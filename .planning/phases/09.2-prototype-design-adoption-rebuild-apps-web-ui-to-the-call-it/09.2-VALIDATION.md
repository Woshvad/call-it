---
phase: 09.2
slug: prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-10
---

# Phase 09.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest (unit/invariant) + Playwright (E2E/visual) + next build |
| **Config file** | `apps/web/vitest.config.ts`, `apps/web/playwright.config.ts` |
| **Quick run command** | `pnpm --filter @call-it/web test` |
| **Full suite command** | `pnpm --filter @call-it/shared build && pnpm --filter @call-it/ui test && pnpm --filter @call-it/web test && pnpm --filter @call-it/web build && pnpm --filter @call-it/web lint` |
| **Estimated runtime** | ~90s quick / ~5 min full |

---

## Sampling Rate

- **After every task commit:** Run `pnpm --filter @call-it/web test`
- **After every plan wave:** Run the full suite command (shared build → ui test → web test → web build → lint)
- **Before `/gsd-verify-work`:** Full suite must be green AND visual goldens regenerated + operator-approved (final wave only)
- **Max feedback latency:** 90 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| (populated by gsd-planner — every task must carry an automated verify or an explicit manual-only row below) | | | | | | | | | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements — no framework install needed. Wave-0 note for the planner:

- [ ] Visual-golden suites (visual-smoke, design-system-snap, responsive, og-thumbnail-200px) encode the OLD design and WILL fail mid-phase — they are excluded from per-task gates and regenerated deliberately in the final wave after operator approval.
- [ ] Source-assert tests (leaderboard `#1A1A24`, ConvictionBar import, `@call-it/ui` import asserts) must be updated in the same task that changes the asserted source.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Visual fidelity vs prototype | D-01 | Ground truth is screenshots + running prototype | Side-by-side `screenshots/home.png` + `settled.png` vs rendered routes; open `call it frontend/Call It.html` for interactive reference |
| Cream CTA press physics + stampReveal | D-01/specifics | Animation feel not snapshot-testable | Hover/press cream CTAs (2px/4px translate, shadow shrink); settle a receipt view and watch the stamp overshoot |
| Late-hydration flash with new shell | D-10 | Privy mounts ssr:false; timing-dependent | Hard-reload `/` and `/call/[id]`; shell must render stable chrome before Privy hydrates |
| Mobile drawer fold at 767px | D-10 | JS-driven breakpoint behavior | Resize across 767/768px; sidebar collapses into MobileDrawer; no dead nav |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 90s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
