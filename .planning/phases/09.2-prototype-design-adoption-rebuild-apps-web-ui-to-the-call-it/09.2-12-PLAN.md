---
phase: 09.2-prototype-design-adoption
plan: 12
type: execute
wave: 7
depends_on: ["09.2-10"]
files_modified:
  - apps/web/app/signin/page.tsx
  - apps/web/app/signin/SignInButtons.tsx
  - apps/web/tests/signin.spec.ts
autonomous: true
requirements: [AUTH-44, UI-48]
must_haves:
  truths:
    - "The logged-out experience adopts the prototype home.jsx landing design; implementation route chosen: the RESTYLED /signin carries the hero (the D-12 lower-risk option) — middleware public-prefix behavior, the privy-token cookie flow, and the onboarding redirect chain are untouched, and the existing signin Playwright hooks survive (D-12)"
    - "Landing platform totals (callers on record, total pot) have no source and are HIDDEN — the LIVE status strip keeps only its working parts (D-07)"
    - "middleware.ts is NOT modified — the public-prefix carve-out alternative was rejected to keep the auth surface frozen (D-12)"
  artifacts:
    - path: "apps/web/app/signin/page.tsx"
      provides: "Full-bleed landing: BE RIGHT / IN PUBLIC. hero, status strip, sign-in CTAs, disclaimer with /terms link"
      contains: "IN PUBLIC."
    - path: "apps/web/app/signin/SignInButtons.tsx"
      provides: "Rethemed Privy auth buttons (cream primary) with preserved testids"
      contains: "btn-connect-wallet"
  key_links:
    - from: "apps/web/app/signin/page.tsx"
      to: "apps/web/app/signin/SignInButtons.tsx"
      via: "existing Privy login wiring"
      pattern: "SignInButtons"
---

<objective>
Adopt the prototype `home.jsx` landing as the restyled `/signin` — full-bleed "BE RIGHT / IN PUBLIC." hero with the existing Privy auth flow as the CTAs — without touching middleware, cookies, or redirects.

Purpose: D-12 — the logged-out experience becomes the marketing surface; choosing the restyled-signin route keeps the entire auth perimeter frozen.
Output: Landing-grade signin page; lockstep-updated signin spec.

Decision of record (D-12 discretion): restyled `/signin` carries the hero. The public-`/` middleware carve-out was REJECTED because it touches `middleware.ts` PUBLIC_PREFIXES, the privy-token cookie check, and the onboarding redirect chain — three locked constraint surfaces — for zero functional gain this phase.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-CONTEXT.md
@.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-UI-SPEC.md (Shell Contract: Landing + Copywriting Contract)
@.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-PATTERNS.md (No Analog Found: Landing)
</context>

## Artifacts this phase produces (this plan)

- New CSS consumption: `.lp-hero-headline` with the prototype-verbatim clamp `clamp(64px, 10vw, 132px)` (ported into globals.css in 09.2-01's primitive set or added locally here if missing)
- No new files — landing markup lives in the restyled signin page

<tasks>

<task type="auto">
  <name>Task 1: Rebuild signin/page.tsx as the full-bleed prototype landing</name>
  <files>apps/web/app/signin/page.tsx</files>
  <read_first>
    - apps/web/app/signin/page.tsx (current 209 lines: privy-error-fallback testid at ~47, disclaimer testid at ~198 with the terms link, SignInButtons mount, custody tooltip — ALL behavioral elements survive)
    - "call it frontend/screens/home.jsx" (451-line markup donor: hero, status strip, section structure, voice; its MiniCallCards/leaderboard live bits and go('feed') CTAs are prototype wiring — CTAs become sign-in affordances)
    - apps/web/middleware.ts (READ-ONLY — confirms /signin is public; DO NOT MODIFY)
    - apps/web/tests/signin.spec.ts (lockstep contract: signed-in/disclaimer hooks)
  </read_first>
  <action>
    Rebuild the page as the full-bleed landing (no sidebar — AppShell already gates /signin chrome per plan 03): hero "BE RIGHT" white / "IN PUBLIC." chartreuse (.lp-hero-headline, prototype-verbatim clamp(64px, 10vw, 132px), Archivo 900, uppercase, line-height 0.85); sub copy "A reputation market for crypto calls. Stake on what you believe. Get a receipt that lasts forever."; status strip "■ · LIVE NOW ·" with .live-dot — the "8,442 CALLERS ON RECORD · $284K POT" numbers have NO source and are NOT rendered (D-07; keep the strip structurally with only the live dot + LIVE NOW text); primary CTA block = the EXISTING SignInButtons component (Privy flows untouched) framed in the landing voice — the hero CTA is the solid white/cream "Sign in →" treatment per screenshot canon; keep the custody tooltip and the existing disclaimer block with data-testid="disclaimer" and its /terms link verbatim in behavior (copy may restyle to mono microcopy); keep data-testid="privy-error-fallback". Marketing sections from home.jsx (how-it-works rows, receipt showcase) port as STATIC copy only where they need no data; any section requiring live feed/leaderboard data is included only if it reuses existing public fetch helpers trivially, otherwise HIDDEN (D-07) — do not add new endpoints. Mobile: single column, hero clamp handles scale, 44px targets (UI-48).
  </action>
  <verify>
    <automated>pnpm --filter @call-it/web test</automated>
  </verify>
  <done>Signin renders the landing hero with all auth behavior and testids intact; middleware untouched; vitest green.</done>
  <acceptance_criteria>
    - page.tsx contains `BE RIGHT` and `IN PUBLIC.` and `clamp(64px, 10vw, 132px)`
    - page.tsx contains `data-testid="disclaimer"` and `data-testid="privy-error-fallback"` and a link to `/terms`
    - page.tsx does NOT contain `8,442` or `$284K` (no fake platform totals, D-07)
    - apps/web/middleware.ts unchanged: git diff --stat empty for it
    - `pnpm --filter @call-it/web test` exits 0 (middleware-public.test.ts + signin-related vitest stay green)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Retheme SignInButtons + lockstep signin spec; full gate</name>
  <files>apps/web/app/signin/SignInButtons.tsx, apps/web/tests/signin.spec.ts</files>
  <read_first>
    - apps/web/app/signin/SignInButtons.tsx (Privy login handlers + testids btn-connect-wallet/btn-google/btn-twitter — wiring stays)
    - apps/web/tests/signin.spec.ts (FULL FILE — reconcile every assert with the new landing markup)
  </read_first>
  <action>
    SignInButtons.tsx: restyle the three auth buttons to the token recipes — primary sign-in treatment cream/solid-white per the landing canon, secondary options outline-white; ALL three data-testids and every Privy handler preserved; loading/disabled states restyled not removed. signin.spec.ts: reconcile asserts with the landing markup (old heading/copy asserts update to the new hero copy; testid-based asserts should pass unchanged); never delete tests (D-15). Run the full wave gate.
  </action>
  <verify>
    <automated>pnpm --filter @call-it/web test && pnpm --filter @call-it/shared build && pnpm --filter @call-it/web build && pnpm --filter @call-it/web lint</automated>
  </verify>
  <done>Auth buttons rethemed with intact wiring; spec reconciled; full gate green.</done>
  <acceptance_criteria>
    - SignInButtons.tsx still contains `btn-connect-wallet`, `btn-google`, `btn-twitter` testids and unchanged Privy handler identifiers (git diff gate)
    - signin.spec.ts test count not reduced
    - Full chain exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated browser → /signin | THE auth entry point; cookie + redirect chain must stay intact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.2-33 | Elevation of Privilege | middleware/public-prefix changes exposing or locking routes (D-12 risk) | mitigate | middleware.ts NOT in files_modified; git-diff-empty acceptance criterion; middleware-public.test.ts in vitest gate |
| T-09.2-34 | Spoofing | fake platform totals on the landing implying traction | mitigate | D-07 grep gates = 0 for the prototype's mock numbers |
| T-09.2-35 | Denial of Service | restyle breaking the Privy login flow (lost testids/handlers) | mitigate | testid + handler git-diff gates; signin spec lockstep |
| T-09.2-SC | Tampering | package installs | accept | zero new packages this phase |
</threat_model>

<verification>
- Web vitest per task; full build/lint gate at plan end
- Live sign-in click-through rolls into the plan 15 operator checkpoint
</verification>

<success_criteria>
- /signin is the prototype landing; auth perimeter byte-identical in behavior
- No fake numbers; testids intact; middleware untouched
</success_criteria>

<output>
Create `.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-12-SUMMARY.md` when done.
</output>
