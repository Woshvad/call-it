---
phase: 09.2-prototype-design-adoption
plan: 04
type: execute
wave: 3
depends_on: ["09.2-01", "09.2-02"]
files_modified:
  - apps/web/app/leaderboard/LeaderboardClient.tsx
  - apps/web/tests/leaderboard.spec.ts
autonomous: true
requirements: [UI-12, UI-13]
must_haves:
  truths:
    - "Leaderboard data with no live source (sparklines, 7d deltas, best category, badges, hero Calibration/ROI/Trajectory StatBlocks) is HIDDEN, never faked (D-07)"
    - "Dead prototype controls (period toggles that don't refetch, non-filtering category chips, NEXT-10 pagination, leaderboard row click) are CUT — no dead buttons ship (D-08)"
    - "Source-assert tests (leaderboard.spec.ts #1A1A24 literal, Card accent, 01 watermark) are updated in the SAME task as the source change — never deleted (D-15)"
    - "LeaderboardClient stays a dumb props renderer — page.tsx RSC fetch untouched (D-05)"
  artifacts:
    - path: "apps/web/app/leaderboard/LeaderboardClient.tsx"
      provides: "Prototype leaderboard markup: page-header, #1 hero card with 01 watermark + rep numeral, .brutal-table rows, your-row-tint"
      contains: "#1A1A24"
    - path: "apps/web/tests/leaderboard.spec.ts"
      provides: "Updated source asserts matching the new markup"
  key_links:
    - from: "apps/web/app/leaderboard/LeaderboardClient.tsx"
      to: "packages/ui (Card accent prop)"
      via: "hero card accent border"
      pattern: "<Card accent"
---

<objective>
Port the prototype leaderboard markup into LeaderboardClient.tsx — the easiest screen (pure props renderer) — hiding everything without a data source and cutting dead controls.

Purpose: First high-visibility screen on the new design; validates the token + primitive foundation end-to-end (D-16 step 1).
Output: Rethemed leaderboard with hero card, brutal table, viewer-row tint; lockstep-updated spec.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-CONTEXT.md
@.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-UI-SPEC.md
@.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-PATTERNS.md (§6)
</context>

## Artifacts this phase produces (this plan)

- No new files or exported symbols — markup-only rewrite inside `LeaderboardClient.tsx` using primitives from 09.2-01/02 (`.page-header`, `.brutal-table`, `.your-row-tint`, `.stat-block`, `Card accent`, `.avatar` grads)
- New inline display constant: hero rep numeral clamp `clamp(64px, 18vw, 132px)`; hero "01" watermark clamp `clamp(160px, 48vw, 360px)`

<tasks>

<task type="auto">
  <name>Task 1: Port hero + table markup from screens/leaderboard.jsx with D-07 degradation and D-08 cuts</name>
  <files>apps/web/app/leaderboard/LeaderboardClient.tsx</files>
  <read_first>
    - apps/web/app/leaderboard/LeaderboardClient.tsx (current 296 lines: useIsMobile + useAccount viewer-row logic, ACCENT/ROW_HIGHLIGHT_BG literals, the props contract from page.tsx — ALL logic stays)
    - apps/web/app/leaderboard/page.tsx (READ-ONLY — the RSC fetch + props hand-off; DO NOT MODIFY)
    - "call it frontend/screens/leaderboard.jsx" (the markup donor: page-header, rank-01 hero card with 360px watermark + 132px rep numeral + StatBlocks, table rows with chips/sparklines/badges)
    - apps/web/tests/leaderboard.spec.ts (Tests 1-5 source asserts — the lockstep contract for Task 2)
  </read_first>
  <action>
    Rewrite LeaderboardClient's render to the prototype leaderboard markup, keeping every hook, prop, and derived value: .page-header with Archivo 56px title (clamp(34px, 9vw, 56px) on mobile) + sub copy; #1 hero card as <Card accent> (keep the literal '<Card accent' — spec Test 4) with low-opacity "01" watermark (keep '01' + an opacity style — Test 4) sized clamp(160px, 48vw, 360px), hero rep numeral Archivo 900 clamp(64px, 18vw, 132px), and ONLY the StatBlocks with real sources: Accuracy (wins/settledCalls) and Calls (totalCalls) — Calibration/ROI/Trajectory have no source and are HIDDEN (D-07). Table → .brutal-table recipe: rank (.slot-num mono), CALLER (square .avatar grad from handle + handle + call count), REP (JBM 600), ACC% (computed wins/settledCalls). Columns with no source — BEST category, Δ7D sparkline, badges — are NOT rendered (D-07). Viewer row keeps the existing isViewer logic and renders .your-row-tint: keep the literal '#1A1A24' background (it IS --bg-tertiary — happy coincidence that keeps spec Test 3 green) + ACCENT left border (keep a 'borderLeft' + 'ACCENT' pairing — Test 3). CUT per D-08 (do not render): 7D/30D period toggles (data is all-time only — keep the existing D-06 v1-limitation note text for Test 5), category filter chips if they do not filter the rendered rows (wire trivially client-side over the existing rows ONLY if the row objects already carry a category field — they don't per RESEARCH, so cut), NEXT-10 pagination, row-level click navigation (no target), SORT. Keep useIsMobile ternaries for 375px (UI-48): single-column hero, horizontal-scroll or stacked table rows, 44px touch targets. AUTH-44: handles only — never render the address field from LeaderboardRow.
  </action>
  <verify>
    <automated>pnpm --filter @call-it/web test</automated>
  </verify>
  <done>Leaderboard renders prototype hero + table from existing props; no fake data; no dead controls; vitest green.</done>
  <acceptance_criteria>
    - LeaderboardClient.tsx contains `#1A1A24`, `<Card accent`, `'01'` (or "01" string), an `opacity` style, and a `borderLeft`/`ACCENT` pairing (spec Tests 3-4 stay green without edits)
    - LeaderboardClient.tsx contains `clamp(64px, 18vw, 132px)` (hero rep numeral) and `clamp(160px, 48vw, 360px)` (watermark)
    - LeaderboardClient.tsx contains NO sparkline, badge, bestCategory, Calibration, ROI, or Trajectory rendering (grep -ci "sparkline\|calibration\|trajectory" apps/web/app/leaderboard/LeaderboardClient.tsx returns 0)
    - LeaderboardClient.tsx renders no wallet address (grep -c "address.slice\|formatAddress" returns 0 in render paths; the address prop may exist for isViewer comparison only)
    - apps/web/app/leaderboard/page.tsx unchanged: git diff --stat apps/web/app/leaderboard/page.tsx is empty
    - `pnpm --filter @call-it/web test` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Lockstep-update leaderboard.spec.ts source asserts to the new markup</name>
  <files>apps/web/tests/leaderboard.spec.ts</files>
  <read_first>
    - apps/web/tests/leaderboard.spec.ts (FULL FILE — Tests 1-5 are source asserts; later tests may be browser asserts on old markup)
    - apps/web/app/leaderboard/LeaderboardClient.tsx (post-Task-1 state — the new source of truth the asserts must match)
  </read_first>
  <action>
    Walk every test in leaderboard.spec.ts against the post-Task-1 source. Tests 1-5 should already pass if Task 1 kept its pinned literals — verify by reading, not assuming. Any assert referencing markup that legitimately changed (old header copy, removed period toggle, old column set) is UPDATED to assert the new contract (e.g., assert the hidden columns are ABSENT, assert .brutal-table or the new header copy). Never delete a test to silence it (D-15); if a test's subject was cut by D-08 (e.g., a period-toggle test), rewrite it to assert the control is NOT rendered and reference D-08 in a comment. Browser-level tests that need a running app are left structurally intact (they run in plan 15's full Playwright pass).
  </action>
  <verify>
    <automated>pnpm --filter @call-it/web test && pnpm --filter @call-it/web lint</automated>
  </verify>
  <done>Every source assert in leaderboard.spec.ts matches the new markup; no test deleted; vitest + lint green.</done>
  <acceptance_criteria>
    - leaderboard.spec.ts still contains `toContain('#1A1A24')` (or an updated equivalent documented in a comment citing D-15)
    - Test count in leaderboard.spec.ts is >= the pre-task count (git diff shows no `test(` removals without replacements)
    - node -e on the spec is not required; verify via: grep -c "test(" apps/web/tests/leaderboard.spec.ts >= pre-change count
    - `pnpm --filter @call-it/web test && pnpm --filter @call-it/web lint` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public viewer → /leaderboard | public route rendering caller identities |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.2-10 | Information Disclosure | wallet addresses rendered in new table markup (AUTH-44) | mitigate | handle-only acceptance criterion; address prop used for isViewer comparison only; AUTH-44 invariant test in vitest gate |
| T-09.2-11 | Spoofing | fabricated stats (sparklines/badges) implying false reputation | mitigate | D-07 hidden-never-faked acceptance criterion (grep = 0 for no-source columns) |
| T-09.2-SC | Tampering | package installs | accept | zero new packages this phase |
</threat_model>

<verification>
- Web vitest green after each task; lint green at plan end
- leaderboard.spec.ts source asserts internally consistent with new source (grep gates)
</verification>

<success_criteria>
- Leaderboard renders the prototype design from existing props with zero fake data and zero dead controls
- Spec updated in lockstep; page.tsx RSC untouched
</success_criteria>

<output>
Create `.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-04-SUMMARY.md` when done.
</output>
