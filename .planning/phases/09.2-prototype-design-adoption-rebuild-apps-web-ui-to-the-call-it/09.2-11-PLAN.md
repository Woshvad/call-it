---
phase: 09.2-prototype-design-adoption
plan: 11
type: execute
wave: 7
depends_on: ["09.2-10"]
files_modified:
  - apps/web/app/duel/[challengeId]/page.tsx
autonomous: true
requirements: [AUTH-44]
must_haves:
  truths:
    - "The duel page keeps its 5s-poll data layer and the working caller-accept path; only render markup changes (D-05)"
    - "The non-transacting 'Side with X/Y' stubs are CUT from the rendered CTAs — no dead money buttons ship; wiring is deferred 09.1+ work per CONTEXT (D-08)"
    - "Duel data with no source (live spread hero value, ±20 rep payload) is HIDDEN, never faked (D-07)"
    - "Duel identity color #A855F7 is confined to challenge UI; identity renders handles only (D-14 AUTH-44)"
  artifacts:
    - path: "apps/web/app/duel/[challengeId]/page.tsx"
      provides: "Prototype duel skin: VS hero, per-side stats, riders lists, consensus bar — over existing DuelLiveState"
      contains: "A855F7"
  key_links:
    - from: "duel render"
      to: "DuelLiveState (page lines ~100-129)"
      via: "existing 5s-poll live-state fields"
      pattern: "DuelLiveState"
---

<objective>
Restyle `/duel/[challengeId]` to the prototype duel design over the existing live-state poll — VS hero, per-side stat columns, riders, consensus bar — cutting the non-transacting Side-with stubs.

Purpose: D-16 step 4; the duel page is the #A855F7 identity showcase.
Output: Rethemed duel page with honest CTAs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-CONTEXT.md
@.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-UI-SPEC.md
@.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-PATTERNS.md (§7 seam pattern applies)
</context>

## Artifacts this phase produces (this plan)

- No new files or exported symbols — markup rewrite inside `apps/web/app/duel/[challengeId]/page.tsx`

<tasks>

<task type="auto">
  <name>Task 1: Replace duel render markup with the prototype VS layout</name>
  <files>apps/web/app/duel/[challengeId]/page.tsx</files>
  <read_first>
    - apps/web/app/duel/[challengeId]/page.tsx (FULL FILE, 1030 lines — DuelLiveState fields at ~100-129: per-side handle/stake/rep/accuracy/categoryAccuracy/streak/position, assets, expiry; riders via followers/faders; consensus from FFM reserves; caller-accept handler WORKS and stays; the Side-with FollowFadeModal stubs at ~1002/1014 are no-ops to CUT)
    - "call it frontend/screens/duel.jsx" (markup donor: VS hero, side columns, riders {caller, amt}, consensus bar)
    - apps/web/tests/challenge-gates.test.ts (behavior tripwire — must stay green)
  </read_first>
  <action>
    Keep the entire data/handler layer (poll, derived values, accept/reject flow) and replace the render: page-header duel framing with #A855F7 identity accents (accent-duel pills, .btn.duel for challenge-context actions); VS hero — two side columns (.brutal-card) each with square avatar grad, @handle + VerifiedBadge, stake JBM, rep/accuracy/streak stats WHERE THE FIELDS EXIST in DuelLiveState (hide null fields, D-07); the live-spread hero StatBlock has NO source — HIDDEN (D-07); countdown from expiry (D HH:MM:SS JBM); riders lists per side (handle + amount rows, .activity-row style — handles only, AUTH-44); consensus bar → .brutal-bar.split with caller side var(--accent-win)-family vs challenger side #A855F7, 2px black gap (duel variant per UI-SPEC Color table). CTAs: the caller accept/reject pending-challenge flow keeps its existing handlers (cream ACCEPT / outline REJECT); the "Side with X" / "Side with Y" buttons are CUT — they are documented no-op stubs (page ~1002/1014) and D-08 forbids dead money buttons; leave the riders/consensus as the spectator surface and add a code comment citing D-08 + the 09.1 wiring deferral. Keep every data-testid; 375px single-column stacking (UI-48); error/loading states restyled to the token recipes.
  </action>
  <verify>
    <automated>pnpm --filter @call-it/web test</automated>
  </verify>
  <done>Duel page renders the prototype VS design from existing live-state; no dead Side-with CTAs; vitest green.</done>
  <acceptance_criteria>
    - page.tsx contains `#A855F7` (or accent-duel alias) and a two-column VS structure consuming DuelLiveState identifiers
    - grep -ci "Side with" apps/web/app/duel/[challengeId]/page.tsx returns 0 in rendered JSX (a D-08 comment may mention the cut)
    - Caller accept/reject handler identifiers unchanged (git diff gate)
    - grep for live-spread placeholder values returns 0 (no fabricated spread)
    - `pnpm --filter @call-it/web test` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Duel page polish pass + full gate</name>
  <files>apps/web/app/duel/[challengeId]/page.tsx</files>
  <read_first>
    - apps/web/app/duel/[challengeId]/page.tsx (post-Task-1 — audit remaining inline legacy colors/grays and any leftover old-design fragments)
  </read_first>
  <action>
    Sweep the file for leftovers: replace remaining legacy gray literals (#18181B/#27272A/#A1A1AA/#2A2A30) and old green/red (#22C55E/#EF4444) with token references (D-02/D-03); confirm settled-duel terminal states (if rendered here) use resolveSettledWord-style word sourcing and SETTLED_OUTCOMES colors consistent with plan 07's map; verify mobile stacking and 44px targets; then run the full wave gate.
  </action>
  <verify>
    <automated>pnpm --filter @call-it/web test && pnpm --filter @call-it/shared build && pnpm --filter @call-it/web build && pnpm --filter @call-it/web lint</automated>
  </verify>
  <done>No legacy palette remnants in the duel page; full gate green.</done>
  <acceptance_criteria>
    - grep -c "#18181B\|#27272A\|#A1A1AA\|#2A2A30\|#22C55E\|#EF4444" apps/web/app/duel/[challengeId]/page.tsx returns 0
    - Full chain `pnpm --filter @call-it/web test && pnpm --filter @call-it/shared build && pnpm --filter @call-it/web build && pnpm --filter @call-it/web lint` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → CE contract | challenge accept/reject with USDC preflight |
| public viewer → duel page | public route rendering both parties' identities |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.2-30 | Tampering | dead Side-with CTAs implying a working money flow | mitigate | CUT per D-08 (grep gate = 0); deferral documented in code comment |
| T-09.2-31 | Information Disclosure | addresses in rider lists (AUTH-44) | mitigate | handles-only render; AUTH-44 invariant in vitest gate |
| T-09.2-32 | Spoofing | fabricated live-spread value | mitigate | hidden per D-07 (grep gate) |
| T-09.2-SC | Tampering | package installs | accept | zero new packages this phase |
</threat_model>

<verification>
- Web vitest per task (challenge-gates tripwire); full build/lint gate at plan end
</verification>

<success_criteria>
- Duel page in prototype skin; accept flow intact; zero dead money CTAs; zero legacy palette
</success_criteria>

<output>
Create `.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-11-SUMMARY.md` when done.
</output>
