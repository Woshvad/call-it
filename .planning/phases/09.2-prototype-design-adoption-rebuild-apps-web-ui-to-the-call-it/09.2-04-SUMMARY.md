---
phase: 09.2-prototype-design-adoption
plan: 04
subsystem: ui
tags: [leaderboard, brutal-table, hero-card, neobrutalism, d07-degradation, d08-cuts, lockstep-tests]

# Dependency graph
requires:
  - phase: 09.2 plan 01
    provides: globals.css token layer + primitive classes (.page-header, .brutal-table, .your-row-tint, .stat-block, .avatar grads, .slot-num, .label-overline, .brutal-card)
  - phase: 09.2 plan 02
    provides: rethemed Card primitive with functional `accent` prop (3px var(--border-accent) border)
provides:
  - Prototype leaderboard markup in LeaderboardClient.tsx (page-header, #1 hero <Card accent> with 01 watermark + giant rep numeral, .brutal-table rows, viewer .your-row-tint)
  - Lockstep-updated leaderboard.spec.ts source asserts (Tests 1-9 + strict-mode-safe Tier-2)
  - Hero display clamps: rep numeral clamp(64px, 18vw, 132px); 01 watermark clamp(160px, 48vw, 360px)
affects: [09.2-05 profile port (same dumb-renderer pattern), 09.2-15 golden regeneration + full Playwright pass]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "D-07 degradation: stats without a live source (hero extra StatBlocks, table delta/category columns) are NOT rendered — conditional render on real fields only"
    - "D-08 cuts: dead prototype controls removed entirely (no useState for toggles/chips; no pagination button; rows are plain <tr> with no navigation)"
    - "Deterministic avatar grad: charCode sum of handle % 6 → avatar-grad-{a..f}"
    - "Lockstep spec: cut-control tests rewritten to assert the control is ABSENT, citing D-08/D-15 in comments"

key-files:
  created: []
  modified:
    - apps/web/app/leaderboard/LeaderboardClient.tsx
    - apps/web/tests/leaderboard.spec.ts

key-decisions:
  - "Hero overline = 'Rank 01 · Top of book' (real rank only) — the prototype's 'MAJORS SPECIALIST · 9-CALL STREAK' has no data source (D-07)"
  - "Tier-2 browser assert retargeted to getByRole('heading') — 'Top of book' now appears in both h1 and hero overline; getByText would strict-mode collide in plan 15's live pass"
  - "Acc% renders an em-dash when settledCalls === 0 (absence marker, not fake data); accuracy hero StatBlock hidden entirely when nothing settled"
  - "D-06 note kept as ALWAYS-rendered static microcopy (toggles that conditioned it are cut); text adapted, 'v1 limitation' literal preserved for spec Test 5"
  - "Table wrapped in .brutal-card padding-0 with overflowX auto + minWidth 480px at mobile (UI-48 horizontal-scroll path)"

patterns-established:
  - "Screen-port template: keep every hook/prop/derived value, swap only render JSX to prototype classes, conditionally render on real fields (D-07), cut dead controls (D-08)"

requirements-completed: [UI-12, UI-13]

# Metrics
duration: 9min
completed: 2026-06-10
---

# Phase 09.2 Plan 04: Leaderboard Port Summary

**Leaderboard rebuilt on the prototype design — .page-header Archivo title, #1 hero `<Card accent>` with a clamp(160px,48vw,360px) low-opacity "01" watermark and clamp(64px,18vw,132px) Archivo 900 rep numeral, .brutal-table rows with square avatar grads, and the UI-13 viewer-row tint — with every no-source stat hidden (D-07), every dead control cut (D-08), and the spec updated in the same plan (D-15)**

## Performance

- **Duration:** ~9 min
- **Started:** 2026-06-10T12:32:32Z
- **Completed:** 2026-06-10T12:41:30Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- **LeaderboardClient.tsx** rewritten to the prototype `screens/leaderboard.jsx` markup while staying a dumb props renderer: every hook (`useIsMobile`, `useAccount`), the props contract from page.tsx, and the `ACCENT`/`ROW_HIGHLIGHT_BG` literals survive; page.tsx RSC fetch untouched (`git diff --stat` empty — D-05)
- **Header**: `.page-header` with the Archivo 900 clamp(34px,9vw,56px) h1 "The Tape · Top of book" + em-accented sub copy ("accuracy × volume" — the prototype's third factor word omitted because that stat has no source and its name is a banned grep literal)
- **#1 hero**: `<Card accent>` (3px accent border from plan 02) carrying the giant low-opacity (0.06) "01" watermark at clamp(160px, 48vw, 360px), square `.avatar xl` grad from the handle, Archivo 900 uppercase handle, and the clamp(64px, 18vw, 132px) GLOBAL REP numeral; ONLY real-source StatBlocks render — Accuracy (wins ÷ settledCalls, hidden when nothing settled) and Calls (totalCalls); the prototype's other three hero StatBlocks, delta line, quote, and badges are NOT rendered (D-07)
- **Table**: `.brutal-card` p-0 wrapper + `.brutal-table` with 4 real-source columns — `.slot-num` mono zero-padded rank, CALLER (square `.avatar sm` grad + handle + call count secondary line), REP (mono 600, toLocaleString), ACC% (computed, win-colored at ≥70%, em-dash when unsettled); BEST/Δ7D/badge columns not rendered (D-07)
- **Viewer row (UI-13)**: existing isViewer logic intact; renders `.your-row-tint` class + the literal `#1A1A24` backgroundColor + `borderLeft` with `ACCENT` + mono "· YOU" marker — spec Tests 3/4 stayed green without edits
- **D-08 cuts** (zero dead controls): 7D/30D/ALL-TIME period toggles (never refetched), category filter chips (rows carry no category field), NEXT-10 pagination, row-level click navigation, SORT; the D-06 "v1 limitation" note survives as always-rendered static microcopy
- **Mobile (UI-48)**: single-column hero + stacked stat blocks, horizontal-scroll table (minWidth 480px), 44px row height, all via the existing `useIsMobile` ternary pattern
- **leaderboard.spec.ts lockstep (D-15)**: Test 6 rewritten to assert the new header AND that cut controls are absent (cites D-08/D-15 in comments); new Test 9 locks the prototype markup contract (brutal-table/your-row-tint/stat-block, both clamps, D-07 no-source absences, AUTH-44 no address formatting); Tier-2 retargeted to the heading role (strict-mode safe); zero tests deleted — count 9→10

## Task Commits

Each task was committed atomically:

1. **Task 1: Port hero + table markup with D-07 degradation and D-08 cuts** - `7fdf767` (feat)
2. **Task 2: Lockstep-update leaderboard.spec.ts to the new markup** - `d698348` (test)

## Files Created/Modified

- `apps/web/app/leaderboard/LeaderboardClient.tsx` — prototype markup over the unchanged props contract; hooks/literals/derived values preserved
- `apps/web/tests/leaderboard.spec.ts` — Tests 6 rewritten + Test 9 added + Tier-2 strict-mode fix (D-15)

## Decisions Made

- Hero overline shows only real data ("Rank 01 · Top of book") — specialist/streak copy had no source
- Acc% em-dash for zero settled calls is an absence marker, not fabricated data; the hero Accuracy block hides entirely in that case
- The D-06 limitation note now renders unconditionally (the toggles that gated it are cut); its copy was adapted to the new reality while preserving the `v1 limitation` literal for spec Test 5
- Tier-2 browser test updated (not left byte-identical) because the old `getByText('Top of book')` would strict-mode fail against the new markup in plan 15's live pass — a legitimate lockstep update under Task 2's mandate
- React row keys switched from `row.address` to `row.rank` — keeps the address field strictly comparison-only

## Deviations from Plan

None - plan executed exactly as written.

## Verification

- Task 1 gate: `pnpm --filter @call-it/web test` 97/97; acceptance greps all pass (`#1A1A24` ×4, `<Card accent` present, `01` + opacity style, borderLeft/ACCENT pairing, both clamps present, `sparkline|calibration|trajectory` case-insensitive = 0, `address.slice|formatAddress` = 0, page.tsx diff empty)
- Task 2 gate: web vitest 97/97 + lint 0 (project lint script is a stub — pre-existing); `playwright test tests/leaderboard.spec.ts` = 9 passed, 1 skipped (Tier-2, no PLAYWRIGHT_BASE_URL — runs in plan 15)
- Test count 10 ≥ pre-change 9; no `test(` removed without replacement
- `tsc --noEmit`: zero leaderboard errors; only the documented pre-existing farcaster-embed/farcaster-manifest test errors remain (out of scope, noted since 09.2-03)
- Threat gates: T-09.2-10 (AUTH-44 handle-only, grep = 0) and T-09.2-11 (D-07 grep = 0) both locked by spec Test 9 going forward

## Known Stubs

None — markup-only rewrite; no data flows touched. (Note: the `handle` field from `lib/leaderboard-client.ts` falls back to a truncated address for profiles with no handle — pre-existing lib behavior outside this plan's files; Test 2 asserts the lib unchanged.)

## Threat Flags

None — no new endpoints, auth paths, or trust-boundary changes.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- The D-16 step-1 validation is done: tokens + primitives + Card accent prop render a full screen end-to-end
- Plan 05 (profile) can copy this screen-port template (keep hooks, conditional-render on real fields, cut dead controls, lockstep the spec)
- Plan 15's full Playwright pass will exercise the updated Tier-2 heading assert against the live app

---
*Phase: 09.2-prototype-design-adoption*
*Completed: 2026-06-10*

## Self-Check: PASSED

- `apps/web/app/leaderboard/LeaderboardClient.tsx` — FOUND
- `apps/web/tests/leaderboard.spec.ts` — FOUND
- Commit `7fdf767` — FOUND
- Commit `d698348` — FOUND
