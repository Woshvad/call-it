---
phase: 09.2-prototype-design-adoption
plan: 12
subsystem: ui
tags: [landing, signin, privy, neobrutalism, archivo, d-12, d-07]

# Dependency graph
requires:
  - phase: 09.2 plan 01
    provides: globals.css :root tokens + brutal-* primitives (.brand, .live-dot, .label-overline, .h-1, .brutal-card cream, .brutal-table, .row/.col/.mono utilities) + next/font variables
  - phase: 09.2 plan 02
    provides: rethemed Button CVA (primary=cream press physics, outline-white, big size)
  - phase: 09.2 plan 03
    provides: AppShell /signin full-bleed carve-out (landing owns its chrome)
provides:
  - /signin as the prototype home.jsx landing — full-bleed BE RIGHT / IN PUBLIC. hero with the existing Privy auth flow as the CTAs (D-12 restyled-signin route; auth perimeter frozen)
  - Rethemed SignInButtons (cream big primary, outline-white secondaries) with all testids + handlers byte-identical
  - Reconciled signin.spec.ts (13 tests, was 12 — stale asserts updated, none deleted)
affects: [09.2-15 visual goldens + operator checkpoint, phase-09.1 demo hardening]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Landing route choice (D-12): logged-out marketing surface = restyled /signin; middleware public-prefix carve-out REJECTED (three locked constraint surfaces for zero gain)"
    - "Local recipe constant (LP_HERO_HEADLINE) when the donor class is missing from globals.css and globals.css is not in files_modified"

key-files:
  created: []
  modified:
    - apps/web/app/signin/page.tsx
    - apps/web/app/signin/SignInButtons.tsx
    - apps/web/tests/signin.spec.ts

key-decisions:
  - "D-12 decision of record: restyled /signin carries the hero — middleware.ts, privy-token cookie flow, onboarding redirect chain all untouched (git diff empty)"
  - "D-07: prototype platform totals (callers on record / pot) NOT rendered — LIVE strip ships only live-dot + 'LIVE NOW'; mini-feed + leaderboard preview sections hidden (need live data, no new endpoints)"
  - ".lp-hero-headline recipe applied as a local inline-style constant in page.tsx (globals.css not in files_modified); clamp(64px, 10vw, 132px) prototype-verbatim"
  - "Risk-callout 'Read the rules →' wired to the real /terms page (D-08: no dead controls); footer carries only real values (no fake deploy block/contract hash)"
  - "Connect Wallet label gains the prototype '→' suffix; spec's toContain('Connect Wallet') unchanged"

patterns-established:
  - "Logged-out surface = /signin; any future public-/ work must re-open D-12 explicitly"

requirements-completed: [AUTH-44, UI-48]

# Metrics
duration: 11min
completed: 2026-06-10
---

# Phase 09.2 Plan 12: Landing (Restyled /signin) Summary

**/signin is now the prototype home.jsx landing — full-bleed BE RIGHT / IN PUBLIC. hero (clamp(64px, 10vw, 132px)) with cream-CTA Privy auth, sourceless platform totals hidden per D-07, and the entire auth perimeter (middleware, cookie flow, redirects, testids) byte-identical in behavior**

## Performance

- **Duration:** ~11 min
- **Started:** 2026-06-10T15:59:29Z
- **Completed:** 2026-06-10T16:10:00Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- **page.tsx rebuilt as the full-bleed landing** (AppShell already renders /signin chrome-free per 09.2-03): brand top bar ("CALL IT · be right in public" via `.brand`); status strip `.live-dot` + "· LIVE NOW ·" with the prototype's fake "8,442 CALLERS / $284K POT" numbers NOT rendered (D-07); hero h1 "BE RIGHT" white / "IN PUBLIC." chartreuse on the prototype-verbatim `.lp-hero-headline` recipe (Archivo 900, `clamp(64px, 10vw, 132px)`, line-height 0.85, -0.055em, uppercase) applied as a local style constant; sub copy "A reputation market for crypto calls. Stake on what you believe. Get a receipt that lasts forever."
- **Auth flow untouched:** existing SignInButtons dynamic mount (ssr:false), PrivyErrorBoundary (`data-testid="privy-error-fallback"`), CustodyTooltip (role="tooltip", AUTH-38 copy verbatim, chrome retokenized), disclaimer (`data-testid="disclaimer"`) with the /terms link — all survive; D-33 order preserved
- **Static marketing sections ported from home.jsx:** how-it-works 3 cream blocks (GO ON RECORD / FOLLOW OR FADE / GET YOUR RECEIPT), differentiator strikethrough rows, fees table (`.brutal-table`, 1.7% total), risk callout (cream card; "Read the rules →" wired to the real /terms — D-08 no dead controls). Mini live-feed + leaderboard preview HIDDEN (need feed/leaderboard data; no new endpoints). Footer = real values only (brand + "Built on Arbitrum" + /terms link)
- **Mobile (UI-48):** single column throughout (isMobile grid switches), hero clamp handles scale, auth column clamps to calc(100vw - 32px), CTAs ≥44px (big/lg sizes)
- **SignInButtons rethemed:** Connect Wallet = solid cream big CTA (rethemed `primary` intent = prototype `.btn.cream` press physics, `big` size, "Connect Wallet →"); Google/X = `outline-white` intent; legacy chartreuse-shadow tailwind overrides deleted; ALL handlers (cookie-write effect, split-brain recovery, Pitfall-16 timeout, wagmi injected fallback) and all 3 testids byte-identical
- **signin.spec.ts reconciled (D-15, never delete):** stale asserts fixed — 'Sign in with Twitter' → 'Sign in with X' (Tier 1 + Tier 2), AUTH-37 'permanent public record'/'No edits.' → current disclaimer copy + href="/terms" assert (the 2026-06-09 copy change had left these stale); Tier-2 disclaimer assert → 'Terms & Conditions'; NEW Tier-1 test locks the landing (BE RIGHT / IN PUBLIC. / clamp / LIVE NOW / no fake totals / privy-error-fallback). 12 → 13 tests

## Task Commits

Each task was committed atomically:

1. **Task 1: Rebuild signin/page.tsx as the full-bleed prototype landing** - `ae9ee6a` (feat)
2. **Task 2: Retheme SignInButtons + lockstep signin spec; full gate** - `97abe74` (feat)

## Files Created/Modified

- `apps/web/app/signin/page.tsx` — full-bleed landing (hero, status strip, static marketing sections, preserved auth elements + disclaimer)
- `apps/web/app/signin/SignInButtons.tsx` — cream-primary/outline-white retheme; wiring untouched
- `apps/web/tests/signin.spec.ts` — reconciled asserts + new landing-hero Tier-1 test (13 tests)

## Decisions Made

- D-12 decision of record executed: restyled /signin carries the hero; public-`/` middleware carve-out rejected (PUBLIC_PREFIXES, privy-token cookie check, onboarding redirect chain are locked surfaces)
- D-07: platform totals hidden, not faked — strip keeps only working parts; data-needing home.jsx sections (The Tape preview, leaderboard preview) hidden rather than wired (no trivially-reusable public fetch path from a client landing without new risk)
- `.lp-hero-headline` applied locally in page.tsx (the class was never ported into globals.css by 09.2-01 and globals.css is not in this plan's files_modified) — satisfies the plan's "added locally here if missing" branch
- Risk callout CTA + footer link both target /terms (the only real public legal page) — no dead anchors ship

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] D-07 doc comment tripped the no-fake-totals acceptance grep**
- **Found during:** Task 1 verification
- **Issue:** The page header comment quoted the prototype's "8,442 CALLERS ON RECORD · $284K POT" literally — the acceptance criterion is a plain grep over page.tsx
- **Fix:** Reworded the comment to "callers-on-record count, total pot" (prose only, zero code change)
- **Files modified:** apps/web/app/signin/page.tsx
- **Commit:** ae9ee6a

**2. [Rule 1 - Bug] signin.spec.ts carried asserts stale against code shipped before this plan**
- **Found during:** Task 2 read_first reconciliation
- **Issue:** Tier-1 asserts expected 'Sign in with Twitter' (button has said "Sign in with X" since the X-brand rename) and 'permanent public record'/'No edits.' (disclaimer copy changed 2026-06-09 to the Terms & Conditions link) — those source-level tests would fail on any Playwright run
- **Fix:** Updated asserts to the live markup (sanctioned by the plan's "old heading/copy asserts update" instruction); no tests deleted
- **Files modified:** apps/web/tests/signin.spec.ts
- **Commit:** 97abe74

## Verification

- Task 1 gate: `pnpm --filter @call-it/web test` 97/97 (middleware-public.test.ts green); `git diff --stat apps/web/middleware.ts` empty
- Task 2 full wave gate: web vitest 97/97 + shared build 0 + `next build` 0 + web lint 0 (project lint script is a pre-existing stub)
- Acceptance greps all pass: page.tsx contains BE RIGHT / IN PUBLIC. / clamp(64px, 10vw, 132px) / both testids / /terms link; zero `8,442`/`$284K`; SignInButtons keeps all 3 `btn-*` testids with handler identifiers unchanged (diff shows JSX-styling-only changes); spec test count 13 ≥ 12
- Every Tier-1 assert string grep-verified against the live sources (Playwright run itself needs the prod webServer — rolls into the plan 15 operator checkpoint with the live sign-in click-through)

## Known Stubs

None — no data flows added; sections without data sources are hidden (D-07), not stubbed with placeholders.

## Threat Flags

None — no new endpoints, no new packages, no auth-surface changes (T-09.2-33/34/35 mitigations all hold: middleware diff empty, fake-totals greps 0, testid/handler gates pass).

## Issues Encountered

None beyond the documented deviations.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- The logged-out experience is now the marketing surface; plan 15's operator checkpoint covers the live sign-in click-through on the new landing
- Phase 09.1 demo hardening will polish THIS landing (the strategic-pivot intent)
- If platform totals ever get a real source (subgraph aggregate), the status strip has the structural slot ready

---
*Phase: 09.2-prototype-design-adoption*
*Completed: 2026-06-10*

## Self-Check: PASSED

- `apps/web/app/signin/page.tsx` — FOUND
- `apps/web/app/signin/SignInButtons.tsx` — FOUND
- `apps/web/tests/signin.spec.ts` — FOUND
- Commit `ae9ee6a` — FOUND
- Commit `97abe74` — FOUND
