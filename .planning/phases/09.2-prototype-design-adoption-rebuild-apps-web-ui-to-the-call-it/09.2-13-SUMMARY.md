---
phase: 09.2-prototype-design-adoption
plan: 13
subsystem: ui
tags: [onboarding, disputes, settings, notifications, terms, banners, brutal-input, toggle-pill, activity-row, neobrutalism, d-13]

# Dependency graph
requires:
  - phase: 09.2 plan 01
    provides: globals.css token layer + primitive classes (.brutal-input/.brutal-textarea/.brutal-select/.toggle-pill/.label-overline/.page-header/.section-divider/.stat-block/.activity-row/.brutal-table/.pill/.btn)
  - phase: 09.2 plan 02
    provides: rethemed @call-it/ui primitives (Button cream press physics, Card brutal-card, Tag pill, Toast)
  - phase: 09.2 plan 03
    provides: AppShell chrome (these pages render inside it; signin/onboarding carve-out)
  - phase: 09.2 plan 08
    provides: cream-surface accent rule reference (accents never raw text on cream)
provides:
  - Every surface the prototype doesn't cover in the new vocabulary (D-13 full coverage) — 5-step onboarding + layout, disputes, settings, terms, notification inbox, system banners
  - .activity-row inbox rows with the exact "No notifications yet" empty state (UI-SPEC copy contract)
  - Settings + AddressBookManager on brutal recipes over untouched wallet-export/cooldown logic
affects: [09.2-14, 09.2-15 visual goldens]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Onboarding chrome: Archivo brand + .label-overline JBM step counter + square accent progress dots — testids/aria/redirects byte-identical"
    - "Inbox rows: .activity-row (pill | content | JBM time) with stampIn entry; unread = accent left-rail + faint accent tint"
    - "Dark-surface empty states: .label-overline mono microcopy, never bare blanks (D-07)"

key-files:
  created: []
  modified:
    - apps/web/app/onboarding/layout.tsx
    - apps/web/app/onboarding/handle/page.tsx
    - apps/web/app/onboarding/socials/page.tsx
    - apps/web/app/onboarding/follow-graph/page.tsx
    - apps/web/app/onboarding/fund/page.tsx
    - apps/web/app/onboarding/tagline/page.tsx
    - apps/web/app/components/SocialLinkControls.tsx
    - apps/web/app/disputes/page.tsx
    - apps/web/app/components/NotificationInbox.tsx
    - apps/web/app/terms/page.tsx
    - apps/web/app/profile/[address]/settings/page.tsx
    - apps/web/components/AddressBookManager.tsx
    - apps/web/components/CustodyDisclosureCard.tsx
    - apps/web/components/PaymasterCapBanner.tsx
    - apps/web/app/components/DesktopOnlyBanner.tsx

key-decisions:
  - "Tagline step gets NO .brutal-textarea — the AUTH-21 commitment line is spec-locked display copy with no user input; adding a textarea would be a behavior change (restyle-only invariant wins over the plan's class suggestion)"
  - "follow-graph opt-out CTA = native .toggle-pill button (same onClick/disabled/testid); opt-in stays the cream primary Button — bracket framing '[ Yes, show me ]' dropped for prototype voice"
  - "SocialLinkControls linked-state indicators went CREAM (var(--bg-inverse) panel, black text/border, brutal-sm shadow) per the plan's cream linked-state direction; useSignIn url/timeout flow byte-identical"
  - "Disputes OPEN pill = .pill neutral (#3B82F6 pending), upheld = .pill win, overturned = .pill loss; bondStatusPill returns pill classes instead of hexes"
  - "Disputes '← Back to feed' link cut — the page now renders inside the AppShell sidebar (The Tape link); a duplicate back-link is redundant chrome (D-08 precedent)"
  - "WalletExportPrompt + PrivyFundButton needed ZERO edits — the prompt renders null (its toast chrome IS the 09.2-02 rethemed Toast primitive) and the fund button is a rethemed @call-it/ui Button with no hex literals; forcing edits would add risk for no visual gain"
  - "onboarding.spec.ts needed NO lockstep change — every assertion (testids, locked copy, structural greps) survives the restyle; test count unchanged"

patterns-established:
  - "Settings section voice: JBM '// SECTION NAME' .label-overline titles inside .brutal-card groups; warning border-left + warning outline button for destructive actions"

requirements-completed: [AUTH-44, UI-48]

# Metrics
duration: ~21min
completed: 2026-06-10
---

# Phase 09.2 Plan 13: Uncovered-Surface Restyle Summary

**Every surface the prototype doesn't cover — 5-step onboarding (+layout/dots), disputes, settings, terms, notification inbox, and the system banners — now wears the ported primitive vocabulary (.brutal-input/.toggle-pill/.activity-row/.brutal-table/.pill/.stat-block/cream CTAs) with zero behavior change: all gates, redirects, testids, poll/mark-read wiring, cooldown logic, and trigger thresholds byte-identical**

## Performance

- **Duration:** ~21 min
- **Started:** 2026-06-10T16:10:00Z
- **Completed:** 2026-06-10T16:31:47Z
- **Tasks:** 3
- **Files modified:** 15

## Accomplishments

- **Onboarding (Task 1):** layout gets the Archivo brand mark + a JBM `.label-overline` step counter (`STEP n / 5 // LABEL`) and square token-layer progress dots (active = accent 24px, completed = tertiary, future = bg-tertiary) — `data-testid="onboarding-progress"` + every `progress-dot-{slug}` + aria progressbar preserved; handle step rides `.brutal-input mono` with the loss-color error border (validation/advance untouched); socials/follow-graph/fund/tagline headings move to the Archivo display voice; follow-graph's opt-out is a `.toggle-pill`; fund's balance is a `.stat-block` (label/value) with `.label-overline` section labels and a `.section-divider` "or"; tagline's AUTH-21 line renders in accent Archivo over the cream COMMIT CTA. The privy-token/onboarding redirect chain, `useOnboardingState` POSTs, and middleware are untouched (middleware not even in files_modified)
- **SocialLinkControls:** linked-state indicators are now cream panels (full-width `flex:1`, black border/text, brutal-sm shadow); QR/redirect panel + status lines token-swapped (#22C55E/#ef4444/#888 → accent-win/accent-loss/text-tertiary); the quick-task-260610-f6s `useSignIn` url/timeout/connect-then-signIn flow is byte-identical
- **Disputes (Task 2):** `.page-header` voice, `.section-divider` section labels, `.brutal-card` rows (open = warning left rail), `.pill neutral` OPEN pill / `.pill win|loss` resolved pills / pill-class bond badges, `.brutal-select`/`.brutal-textarea` owner-admin inputs, `.btn cream|fade|outline-white` action buttons. 5s poll, resolve state machine, REQUIRED reversal preview ("Preview unavailable — cannot resolve safely." verbatim), and two-step destructive confirm all untouched. Real data only; empty states are mono overlines ("No disputes. Every settled call currently stands." / "No resolved disputes yet.")
- **NotificationInbox:** rows are `.activity-row` (type pill | handle-only content | JBM relative time) entering with stampIn; unread rows get an accent left rail + faint tint; empty state is exactly **"No notifications yet"** as a `.label-overline`; mark-read POST (Bearer token), auto-mark-on-open, Escape/backdrop close, and the 30s poll (lives in NotificationBell) untouched
- **Terms:** typographic restyle only — Archivo heading, Inter body, mono overline; the permanent-public-record copy verbatim; signin disclaimer link target intact
- **Settings (Task 3):** JBM `// SECTION` titles in `.brutal-card` groups, `.brutal-input mono` handle form + `.btn cream` save, wallet export framed as the destructive section (warning border-left + warning outline button); owner redirect guard, `setDisplayHandle` write, and `exportWallet()` untouched; no NEW address rendering added (the deposit/address-book displays are the user's own functional wallet surface)
- **AddressBookManager:** `.brutal-table` entries, `.pill warn` cooldown badge (`data-testid="cooldown-badge"`, "remaining" text), `.brutal-input` add form + `.btn cream`; `isInCooldown`/`formatCooldownRemaining`/`/api/addressbook`/optimistic-rollback logic byte-identical
- **Banners:** PaymasterCapBanner = warning-color mono strip on the token layer (`isCapped`/`return null` trigger intact); DesktopOnlyBanner drops the chartreuse shadow for the all-black `var(--shadow-brutal)` (design-rule compliance) with copy/dismiss/44px target intact; CustodyDisclosureCard token-swapped around the AUTH-22 locked copy

## Task Commits

Each task was committed atomically:

1. **Task 1: Restyle the 5-step onboarding flow with preserved dots/testids/redirect chain** - `1aa7d4b` (feat)
2. **Task 2: Restyle disputes page, notification inbox, and terms** - `fc31005` (feat)
3. **Task 3: Restyle settings + system banners; full gate** - `91a8282` (feat)

## Files Created/Modified

- 6 onboarding files (layout + 5 steps) — new-vocabulary chrome, logic verbatim
- `apps/web/app/components/SocialLinkControls.tsx` — cream linked indicators + token swaps
- `apps/web/app/disputes/page.tsx` — page-header/brutal-card/pill/admin-input retheme
- `apps/web/app/components/NotificationInbox.tsx` — .activity-row rows + locked empty state
- `apps/web/app/terms/page.tsx` — typographic restyle, content verbatim
- `apps/web/app/profile/[address]/settings/page.tsx` — section groups + warning export framing
- `apps/web/components/AddressBookManager.tsx` — brutal-table/pill/input chrome
- `apps/web/components/CustodyDisclosureCard.tsx`, `PaymasterCapBanner.tsx`, `apps/web/app/components/DesktopOnlyBanner.tsx` — token recipes

## Decisions Made

- Tagline screen ships WITHOUT a `.brutal-textarea`: the AUTH-21 line is spec-locked display copy, not user input — adding an input would violate the restyle-only invariant (documented in the file header)
- WalletExportPrompt and PrivyFundButton intentionally unedited: the prompt renders `null` (toast chrome = rethemed Toast primitive) and the fund button is already a token-recipe Button; `git diff --stat` for both is empty
- Disputes "← Back to feed" link removed as redundant chrome — sidebar navigation (09.2-03) provides the same route persistently
- onboarding.spec.ts unchanged — all 15 Tier-1 + 4 Tier-2 assertions hold against the restyled sources (test count not reduced)
- Bracket-framed CTA copy (`[ Yes, show me ]`, `[ COMMIT ]`) flattened to the prototype's declarative voice; no test referenced the bracketed forms (grep-verified)

## Deviations from Plan

### Auto-fixed Issues

None requiring deviation rules — the two "planned files not modified" cases (onboarding.spec.ts, PrivyFundButton.tsx, WalletExportPrompt.tsx) are no-op-by-inspection, documented above, not behavior deviations.

## Verification

- Task 1 gate: web vitest 97/97; acceptance greps — no `data-testid` deletions across the onboarding diff (only doc-comment additions matched), `useSignIn`/`timeout: 300_000`/`url` identifiers present, `.brutal-input` (handle) / `.toggle-pill` (follow-graph) / `.stat-block` (fund) ≥1 each
- Task 2 gate: web vitest 97/97; NotificationInbox contains "No notifications yet" + `.activity-row` + 13 poll/mark-read identifier hits; disputes renders real data + mono-overline empty states only; terms content verbatim (style-only diff)
- Task 3 full gate: web vitest 97/97 → `pnpm --filter @call-it/shared build` 0 → `pnpm --filter @call-it/web build` 0 (all routes incl. /disputes, /terms, /onboarding/*, /profile/[address]/settings) → web lint 0 (project lint script is a stub — pre-existing)
- `git diff --stat apps/web/app/Providers.tsx` EMPTY (bit-identical); `git diff --stat apps/web/components/WalletExportPrompt.tsx` EMPTY
- AddressBookManager cooldown asserts hold: `cooldown-badge` testid, `isInCooldown`/`formatCooldownRemaining`, `/api/addressbook`, "remaining" all present (address-book-cooldown.spec source asserts)
- Visual goldens remain excluded from gates until plan 15 (D-15 phase decision)

## Known Stubs

- `apps/web/app/terms/page.tsx` — pre-existing "Full terms are coming soon." stub copy (the T&C body is a tracked user TODO, memory note `terms-and-conditions-todo.md`); restyled verbatim, not introduced by this plan

## Threat Flags

None — no new endpoints, auth paths, or trust-boundary changes. T-09.2-36 (onboarding gates) mitigated by the logic-untouched diff gates + green onboarding.spec assertions; T-09.2-37 (AUTH-44) — no new address rendering added anywhere; T-09.2-38 (banner triggers) — trigger identifiers byte-identical (WalletExportPrompt untouched, PaymasterCapBanner `isCapped`/`return null` intact).

## Issues Encountered

None.

## User Setup Required

None.

## Next Phase Readiness

- D-13 full coverage achieved: no routed screen ships in the old skin
- Plan 15 visual-golden refresh can now baseline every surface, including onboarding/disputes/settings/inbox
- Legacy hex literals retired from all touched files (remaining greps in untouched files are out of this plan's scope)

---
*Phase: 09.2-prototype-design-adoption*
*Completed: 2026-06-10*

## Self-Check: PASSED

- apps/web/app/onboarding/layout.tsx — FOUND
- apps/web/app/components/NotificationInbox.tsx — FOUND ("No notifications yet" + .activity-row)
- apps/web/app/profile/[address]/settings/page.tsx — FOUND
- Commit 1aa7d4b — FOUND
- Commit fc31005 — FOUND
- Commit 91a8282 — FOUND
