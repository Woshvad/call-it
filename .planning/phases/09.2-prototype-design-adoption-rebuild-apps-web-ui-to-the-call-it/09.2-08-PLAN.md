---
phase: 09.2-prototype-design-adoption
plan: 08
type: execute
wave: 4
depends_on: ["09.2-02", "09.2-06"]
files_modified:
  - packages/ui/src/compound/FollowFadeModal.tsx
  - packages/ui/src/compound/CallerExitModal.tsx
  - packages/ui/src/compound/PositionExitModal.tsx
  - apps/web/app/components/ChallengeFormModal.tsx
autonomous: true
requirements: [AUTH-44]
must_haves:
  truths:
    - "All money modals keep their existing amount inputs, preflight, and tx wiring — design CTAs open the same modals; only chrome changes (D-06)"
    - "The prototype's unused cream .modal-overlay/.modal-panel (styles.css:821-840) is the template for all modals (D-13)"
    - "packages/ui modal markup stays flexbox-only (Satori ban, D-14)"
    - "CallerExitModal frames exit as a slash with the warning color and real repDelta — no prototype mock numbers (D-05)"
  artifacts:
    - path: "packages/ui/src/compound/FollowFadeModal.tsx"
      provides: "Cream-panel chrome over untouched amount/tx logic"
      contains: "modal"
    - path: "packages/ui/src/compound/CallerExitModal.tsx"
      provides: "Cream panel + warning slash framing + real repDelta"
    - path: "apps/web/app/components/ChallengeFormModal.tsx"
      provides: "Cream-panel challenge creation form (.brutal-input recipes)"
  key_links:
    - from: "modal panels"
      to: "apps/web/app/globals.css"
      via: ".modal-overlay/.modal-panel recipes from 09.2-01"
      pattern: "modal-panel"
---

<objective>
Restyle the four money/social modals — FollowFadeModal, CallerExitModal, PositionExitModal, ChallengeFormModal — to the cream `.modal-panel` template, keeping every amount input, preflight, and transaction handler byte-identical in behavior.

Purpose: D-13/D-16 step 2 — the modals are where real USDC moves; they must look like the design without changing a single money path.
Output: Cream-panel modals with untouched wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-CONTEXT.md
@.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-UI-SPEC.md (Component Mapping + Copywriting Contract destructive rows)
@.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-PATTERNS.md (§9 — modal template + structural analogs)
</context>

## Artifacts this phase produces (this plan)

- No new files or exported symbols — chrome rewrites inside the four existing modal components; all props/callbacks unchanged

<tasks>

<task type="auto">
  <name>Task 1: Cream-panel retheme of FollowFadeModal, CallerExitModal, PositionExitModal (flexbox-only)</name>
  <files>packages/ui/src/compound/FollowFadeModal.tsx, packages/ui/src/compound/CallerExitModal.tsx, packages/ui/src/compound/PositionExitModal.tsx</files>
  <read_first>
    - packages/ui/src/compound/FollowFadeModal.tsx, CallerExitModal.tsx, PositionExitModal.tsx (FULL FILES — every amount input, validation, preflight, callback, and disabled/loading state stays; identify all text-color classes that must flip to inverse on cream)
    - "call it frontend/styles.css" lines 821-840 (.modal-overlay/.modal-panel recipe — the D-13 template)
    - .planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-PATTERNS.md (§9 — exact recipe values + the cream-contrast audit note)
  </read_first>
  <action>
    Retheme each modal's chrome to the cream template: overlay rgba(0,0,0,0.82) + backdrop-blur(4px) z-200 centered flex; panel bg cream (brand-cream alias or var(--bg-inverse)), BLACK text, 3px black border, shadow var(--shadow-brutal-lg), max-width 620px, padding 36px, max-height 90vh overflow-y-auto, radius 0. Every interior text token flips to inverse (black/near-black) — audit and replace text-brand-text/text-brand-muted usages inside the panels (they'd be invisible-on-cream otherwise). Amount inputs → .brutal-input recipe adapted for cream context (white/transparent bg with 2px black border). Mono labels → JBM overline style with black text. FollowFadeModal: follow side accent treatment uses the win color on DARK elements only (chartreuse-on-cream fails contrast — use black-bordered chips with the side name); fade side var(--accent-loss). CallerExitModal: frame the exit as a slash — warning #FB923C accent on the penalty line, render the REAL repDelta value from existing props (never a mock number, D-05); keep its existing confirmation copy semantics. PositionExitModal: same template; real position values from props. FLEXBOX ONLY in all three (packages/ui Satori constraint). Zero changes to props, callbacks, hooks, validation, or any tx-related identifier.
  </action>
  <verify>
    <automated>pnpm --filter @call-it/ui test && pnpm --filter @call-it/web test</automated>
  </verify>
  <done>All three packages/ui modals render cream panels with logic untouched; ui + web vitest green.</done>
  <acceptance_criteria>
    - Each of the three files contains the cream panel values: `0.82` overlay opacity, `blur(4px)`, 3px black border, and a cream background token
    - git diff shows zero changes to function/handler/prop identifiers (chrome-only: className/style/JSX structure)
    - grep -c "display: 'grid'\|grid-cols" on the three files returns 0
    - CallerExitModal.tsx contains `#FB923C` (or accent-warning alias) and renders the repDelta prop (identifier present in JSX)
    - No interior text uses text-brand-text/text-brand-muted on the cream panel (grep inside panel JSX returns 0)
    - `pnpm --filter @call-it/ui test && pnpm --filter @call-it/web test` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Cream-panel retheme of ChallengeFormModal</name>
  <files>apps/web/app/components/ChallengeFormModal.tsx</files>
  <read_first>
    - apps/web/app/components/ChallengeFormModal.tsx (FULL FILE — challenge creation form: stake input, side selection, validation, submit wiring — all logic stays)
    - "call it frontend/styles.css" (.modal-overlay/.modal-panel + .brutal-input + .toggle-pill recipes)
  </read_first>
  <action>
    Apply the same cream template to ChallengeFormModal: overlay/panel per the D-13 recipe; form fields → .brutal-input recipes (cream-context variant); side/option selectors → .toggle-pill (.on = inverse-on-inverse needs the black-filled active state — active pill bg #000 cream text); the challenge identity accent uses #A855F7 (accent-duel) for the DUEL framing pill/header only (D-03: purple is duel identity). Header in the mono "FINAL · CONFIRM"-style voice adapted to the challenge context. Keep every handler, validation rule, preflight, and data-testid. Page-level component — grid allowed but prefer flexbox.
  </action>
  <verify>
    <automated>pnpm --filter @call-it/web test && pnpm --filter @call-it/ui build && pnpm --filter @call-it/web build && pnpm --filter @call-it/web lint</automated>
  </verify>
  <done>ChallengeFormModal renders the cream panel with duel-purple identity accents; logic untouched; full gate green.</done>
  <acceptance_criteria>
    - ChallengeFormModal.tsx contains cream panel values and `#A855F7` (or accent-duel alias) used for duel identity only
    - git diff shows no handler/validation identifier changes
    - Existing data-testid attributes preserved
    - Full chain `pnpm --filter @call-it/web test && pnpm --filter @call-it/ui build && pnpm --filter @call-it/web build && pnpm --filter @call-it/web lint` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| modal confirm → chain | these modals gate real USDC approve + write paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.2-21 | Tampering | restyle silently breaking a money CTA (dead-CTA money flow) | mitigate | chrome-only git-diff gate (no handler identifier changes); ui + web vitest per task |
| T-09.2-22 | Repudiation | amount/penalty values hidden by bad cream contrast (user confirms what they can't read) | mitigate | inverse-text audit acceptance criterion; real repDelta render gate |
| T-09.2-23 | Spoofing | one-click stake semantics replacing amount inputs (prototype pattern) | mitigate | D-06: amount inputs preserved — props/validation untouched |
| T-09.2-SC | Tampering | package installs | accept | zero new packages this phase |
</threat_model>

<verification>
- ui + web vitest per task; full build/lint gate at plan end
</verification>

<success_criteria>
- All four modals carry the cream template with byte-identical money behavior
- Flexbox-only holds in packages/ui; duel purple confined to challenge identity
</success_criteria>

<output>
Create `.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-08-SUMMARY.md` when done.
</output>
