---
phase: 09.2-prototype-design-adoption
plan: 02
type: execute
wave: 2
depends_on: ["09.2-01"]
files_modified:
  - packages/ui/src/primitives/Button.tsx
  - packages/ui/src/primitives/Card.tsx
  - packages/ui/src/primitives/Stamp.tsx
  - packages/ui/src/primitives/Tag.tsx
  - packages/ui/src/primitives/VerifiedBadge.tsx
  - packages/ui/src/primitives/CornerBrackets.tsx
  - packages/ui/src/primitives/Skeleton.tsx
  - packages/ui/src/primitives/Toast.tsx
  - packages/ui/src/compound/ConvictionBar.tsx
  - packages/ui/src/compound/MarketPositioningBar.tsx
  - packages/ui/src/primitives/__tests__
autonomous: true
requirements: [UI-51, UI-46]
must_haves:
  truths:
    - "@call-it/ui primitives render the prototype ROOT recipes — cream primary CTA, all-black hard shadows, radius 0, Archivo display voice (D-01)"
    - "The CONTRARIAN HIT stamp path resolves to the win color #E8F542 — the documented stale #A855F7 contrarian token is fixed (D-03)"
    - "Every rethemed packages/ui component stays flexbox-only — no display:grid enters the package (Satori ban, lint-enforced, D-14)"
    - "Prototype components.jsx is a markup recipe donor only — no prototype state or mock data enters packages/ui (D-05)"
    - "Existing Button intent keys (primary/secondary/danger) and the Card accent prop keep working so ~30 consumer files compile untouched (D-02)"
  artifacts:
    - path: "packages/ui/src/primitives/Button.tsx"
      provides: "CVA intents: primary (cream press-physics), secondary (outline-white), danger, plus new cream/fade/duel/outline-white/ghost"
      contains: "fade"
    - path: "packages/ui/src/primitives/Card.tsx"
      provides: ".brutal-card recipe variants heavy/hero/cream/interactive; accent prop = 3px accent border; accent SHADOW removed"
      contains: "var(--bg-secondary)"
    - path: "packages/ui/src/primitives/Stamp.tsx"
      provides: "OutcomeStamp retheme: clamp display size, rotate(-1deg), black text-shadow, overshoot reveal"
      contains: "rotate(-1deg)"
    - path: "packages/ui/src/compound/ConvictionBar.tsx"
      provides: ".brutal-bar recipe over Radix slider logic; muted→accent fill interpolation preserved (UI-51)"
  key_links:
    - from: "packages/ui/src/primitives/Stamp.tsx"
      to: "packages/ui/src/tokens/colors.ts"
      via: "COLOR_MAP literal hexes for framer-motion (updated in 09.2-01)"
      pattern: "COLOR_MAP"
    - from: "packages/ui/src/primitives/Button.tsx"
      to: "apps/web/app/globals.css"
      via: "Tailwind classes resolving brand-* aliases to var(--*) tokens"
      pattern: "brand-"
---

<objective>
Retheme every `@call-it/ui` primitive and the two bar compounds to the prototype recipes — the cheap seam that restyles ~30 consumer files for free (09.2-RESEARCH.md §6 Step 0.2).

Purpose: Once Button/Card/Stamp/Tag/bars carry the brutal recipes, every page that consumes them inherits the new design before its own markup port.
Output: Rethemed CVA primitives, fixed contrarian stamp color, flexbox-only compliance, green ui + web test suites.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-CONTEXT.md
@.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-UI-SPEC.md
@.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-PATTERNS.md
</context>

## Artifacts this phase produces (this plan)

- New Button CVA intents: `cream`, `fade`, `duel`, `outline-white`, `ghost` (existing `primary`/`secondary`/`danger` keys retained — primary remaps to the cream recipe, secondary to outline-white)
- Card variants: `heavy`, `hero`, `cream`, `interactive` (new variant prop values on the existing Card)
- Rethemed Stamp constants: stamp font clamp `clamp(56px, 17vw, 120px)`, rotation -1deg, text-shadow `4px 4px 0 #000`, overshoot timing `cubic-bezier(0.34, 1.56, 0.64, 1)`
- No new files — all symbols live in existing packages/ui modules

<tasks>

<task type="auto">
  <name>Task 1: Retheme Button, Card, Tag, VerifiedBadge, CornerBrackets to the brutal recipes</name>
  <files>packages/ui/src/primitives/Button.tsx, packages/ui/src/primitives/Card.tsx, packages/ui/src/primitives/Tag.tsx, packages/ui/src/primitives/VerifiedBadge.tsx, packages/ui/src/primitives/CornerBrackets.tsx</files>
  <read_first>
    - packages/ui/src/primitives/Button.tsx (the CVA intent/size pattern every retheme follows — cva(baseArray, variants, defaultVariants) + cn() + VariantProps)
    - packages/ui/src/primitives/Card.tsx (current border-3 white + 6px accent shadow — the accent SHADOW dies, the accent PROP stays)
    - packages/ui/src/primitives/Tag.tsx, VerifiedBadge.tsx, CornerBrackets.tsx (current recipes to replace)
    - "call it frontend/styles.css" lines 339-463 (.brutal-card variants + .btn variants incl. cream press physics) and the .pill/.bracketed recipes
    - .planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-PATTERNS.md (§5 — the CVA retheme template with exact base classes)
  </read_first>
  <action>
    Button.tsx: rewrite the cva base to the prototype .btn recipe — font-display (Archivo) weight 700, 13px, uppercase, tracking 0.04em, padding 12px/20px, 2px border var(--border-active), radius 0, transition 0.08s linear. Intent map (PATTERNS §5 + UI-SPEC Component Mapping): primary → the .btn.cream recipe (bg var(--bg-inverse), black text, 2px black border, shadow-[4px_4px_0_0_#000], font-weight 800; hover translate-x-[2px] translate-y-[2px] + shadow-[2px_2px_0_0_#000]; active translate-x-[4px] translate-y-[4px] + shadow-none — NOTE: down-right, the opposite of the current up-left hover); secondary → .btn.outline-white (transparent bg, 2px white border, white text); danger → loss-styled (transparent bg, 2px var(--accent-loss) border, var(--accent-loss) text). Add NEW intents: cream (alias of primary recipe), fade (same as danger recipe — fade-side actions), duel (2px #A855F7 border + #A855F7 text — duel identity only per D-03), outline-white (alias of secondary), ghost (borderless, var(--text-secondary), hover white). Add size big → padding 18px/28px, 15px, 3px border. The accent chartreuse is NEVER a button background (UI-SPEC: "the primary CTA is CREAM, not chartreuse").
    Card.tsx: base → .brutal-card recipe (bg var(--bg-secondary), 2px border var(--border-subtle), p-6, radius 0). Variants: heavy and hero → 3px white border + shadow-[4px_4px_0_0_#000] (hero also p-8); cream → bg var(--bg-inverse) + black text + 3px black border + brutal shadow; interactive → hover white border + brutal shadow + -translate-x-[2px] -translate-y-[2px] transition 0.12s linear. The current shadow-[6px_6px_0_0_#E8F542] accent shadow is DELETED (prototype shadows are all-black). The `accent` prop STAYS functional and now renders a 3px var(--border-accent) border (tests/leaderboard.spec.ts Test 4 asserts `<Card accent` in consumer source — the prop must keep compiling).
    Tag.tsx + VerifiedBadge.tsx: .pill recipe — font-mono (JBM) 11px weight 600 uppercase tracking 0.06em, padding 3px/8px, 1px currentColor border, inline-flex (Satori parity — NEVER block/grid). VerifiedBadge keeps its existing badge-type API (x/fc/criteria etc.) and colors them via pill variant classes (win/duel/warn/neutral/muted).
    CornerBrackets.tsx: .bracketed recipe — 14px L-shaped 1px var(--border-active) corner marks, flexbox/absolute positioning only.
    All files: flexbox-only (no display grid — lint error in this package); keep every exported prop type and component name unchanged.
  </action>
  <verify>
    <automated>pnpm --filter @call-it/ui test && pnpm --filter @call-it/web test</automated>
  </verify>
  <done>Button has all 8 intent keys compiling; Card accent shadow gone, accent prop functional; pills are JBM uppercase; ui + web vitest green.</done>
  <acceptance_criteria>
    - Button.tsx contains intent keys: `primary`, `secondary`, `danger`, `cream`, `fade`, `duel`, `outline-white`, `ghost` (grep each)
    - Button.tsx cream/primary recipe contains `translate-x-[2px]` AND `translate-y-[2px]` (hover, down-right) and `shadow-none` or shadow 0 on active
    - Card.tsx does NOT contain `#E8F542` as a shadow value (grep -c "shadow.*E8F542" returns 0) but still accepts the `accent` prop
    - Tag.tsx and VerifiedBadge.tsx contain `uppercase` and `font-mono` (or explicit JBM var) and use inline-flex
    - grep -rn "display: 'grid'\|display:grid\|grid-cols" packages/ui/src/primitives | wc -l returns 0
    - `pnpm --filter @call-it/ui test && pnpm --filter @call-it/web test` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Retheme Stamp, Skeleton, Toast, ConvictionBar, MarketPositioningBar</name>
  <files>packages/ui/src/primitives/Stamp.tsx, packages/ui/src/primitives/Skeleton.tsx, packages/ui/src/primitives/Toast.tsx, packages/ui/src/compound/ConvictionBar.tsx, packages/ui/src/compound/MarketPositioningBar.tsx</files>
  <read_first>
    - packages/ui/src/primitives/Stamp.tsx (framer-motion overshoot + useReducedMotion fallback + hexColor override — the MECHANISMS stay; only the visual constants change; note the stale-token comment at lines 13-15 that this task resolves)
    - packages/ui/src/primitives/Skeleton.tsx, Toast.tsx (current recipes; Toast references drain/fadeIn keyframes that plan 09.2-01 relocated into apps/web/app/globals.css)
    - packages/ui/src/compound/ConvictionBar.tsx, MarketPositioningBar.tsx (Radix slider logic + fill interpolation — logic stays verbatim)
    - "call it frontend/styles.css" (.outcome-stamp + stampReveal recipe; .brutal-bar/.split recipe)
    - "call it frontend/components.jsx" (OutcomeStamp, OddsBar, ConfidenceBar markup recipes)
  </read_first>
  <action>
    Stamp.tsx: retheme visual constants to the prototype OutcomeStamp — font-size clamp(56px, 17vw, 120px), Archivo 900 (font-display), uppercase, line-height 0.85, rotate(-1deg) (currently -5deg), text-shadow '4px 4px 0 #000', framer-motion overshoot retimed to 0.4s with scale keyframes 1.3 → 0.96 → 1 and rotation -2deg → -1deg (matching stampReveal cubic-bezier(0.34,1.56,0.64,1)). KEEP: useReducedMotion fallback, the hexColor override prop, and the COLOR_MAP lookup (which now returns #E8F542 for contrarian after 09.2-01 — update/remove the stale-token comment at lines 13-15 to record that D-03 is fixed).
    Skeleton.tsx: radius 0, hard edges, pulse on var(--bg-tertiary).
    Toast.tsx: radius 0, 2px border, bg var(--bg-secondary); error variant border/accent var(--accent-loss) #F87171; success variant uses var(--accent-win); remove any tailwindcss-animate utility classes (the plugin was never registered — latent bug) and animate via the drain/fadeIn keyframes now in the app cascade or inline style animation names.
    ConvictionBar.tsx: keep the Radix slider logic and the muted→accent fill interpolation (UI-51 — fill interpolates as conviction rises) verbatim; retheme chrome to .brutal-bar — 10px track height, 1px border, radius 0; slider thumb 26px cream square with 2px black border and 2px 2px 0 #000 shadow (.brutal-slider thumb recipe).
    MarketPositioningBar.tsx: retheme to .brutal-bar.split — follow side var(--accent-win), fade side var(--accent-loss), 2px black gap between segments, fill transitions width 0.4s cubic-bezier(0.16,1,0.3,1). Keep all props and percentage logic.
    All flexbox-only; no prototype state ported (D-05).
  </action>
  <verify>
    <automated>pnpm --filter @call-it/ui test && pnpm --filter @call-it/web test && pnpm --filter @call-it/ui build && pnpm --filter @call-it/web build && pnpm --filter @call-it/web lint</automated>
  </verify>
  <done>Stamp renders the 120px-clamp brutal stamp with fixed contrarian color path; bars carry .brutal-bar recipes with logic untouched; full ui+web build/test/lint green.</done>
  <acceptance_criteria>
    - Stamp.tsx contains `rotate(-1deg)` and `4px 4px 0 #000` and `clamp(56px, 17vw, 120px)` and still contains `useReducedMotion` and `hexColor`
    - Stamp.tsx no longer documents #A855F7 as the LIVE contrarian color (comment updated to record D-03 fix)
    - ConvictionBar.tsx still imports from `@radix-ui` and retains its fill-interpolation logic (grep for the existing interpolation identifier after reading the file)
    - MarketPositioningBar.tsx contains `var(--accent-win)` and `var(--accent-loss)` (or the brand-accent/loss Tailwind aliases) and a 2px gap treatment
    - grep -rn "display: 'grid'\|display:grid\|grid-cols" packages/ui/src | wc -l returns 0
    - Full chain `pnpm --filter @call-it/ui test && pnpm --filter @call-it/web test && pnpm --filter @call-it/ui build && pnpm --filter @call-it/web build && pnpm --filter @call-it/web lint` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared package → all consumers | packages/ui renders on every page including public receipts and OG paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.2-04 | Tampering | Stamp outcome color (CONTRARIAN HIT) | mitigate | COLOR_MAP-driven (single source); settled-outcome-truth.test.ts + outcome-word.test.ts in per-task vitest gate; words never touched in this plan |
| T-09.2-05 | Denial of Service | grid entering Satori-shared components breaking OG rendering | mitigate | no-display-grid-in-og eslint rule is error-level and runs in the Task 2 lint gate; grep acceptance criterion = 0 |
| T-09.2-06 | Repudiation | Button intent remap silently changing a money CTA's visual affordance | accept | intent KEYS unchanged; money CTAs keep opening the same modals (no handler edits in this plan) |
| T-09.2-SC | Tampering | package installs | accept | zero new packages this phase |
</threat_model>

<verification>
- `pnpm --filter @call-it/ui test && pnpm --filter @call-it/web test` after each task
- Wave gate at Task 2: ui build + web build + lint all exit 0
- Visual goldens excluded until plan 15 (D-15)
</verification>

<success_criteria>
- All @call-it/ui primitives + bars carry the prototype recipes; consumers compile untouched
- D-03 contrarian fix flows through Stamp via COLOR_MAP
- Zero display:grid in packages/ui; lint green
</success_criteria>

<output>
Create `.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-02-SUMMARY.md` when done.
</output>
