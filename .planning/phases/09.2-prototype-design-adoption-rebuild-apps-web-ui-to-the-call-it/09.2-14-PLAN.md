---
phase: 09.2-prototype-design-adoption
plan: 14
type: execute
wave: 8
depends_on: ["09.2-11", "09.2-12", "09.2-13"]
files_modified:
  - apps/web/app (audit-driven fixes across route files)
  - packages/ui/src (audit-driven fixes)
  - apps/web/app/components/MobileDrawer.tsx
autonomous: true
requirements: [UI-48, UI-46]
must_haves:
  truths:
    - "The three legacy gray palettes (#18181B, #27272A, #A1A1AA, #2A2A30) and the old green/red (#22C55E, #EF4444) are fully retired from apps/web/app and packages/ui/src by end of phase (D-02, D-03)"
    - "All D-14 hard invariants verified phase-wide: flexbox-only in packages/ui + OG paths, Tailwind v3.4 + postcss.config.js, auth-kit CSS in cascade, Providers.tsx untouched, MiniAppReady on all /call branches, addresses only from @call-it/shared"
    - "The mobile clamp pass enforces the pinned clamp() values for the 56-132px display roles across all ported routes, preserving Phase 9's 375px requirements (D-16 final-wave mandate; clamps are the Claude's-discretion values pinned in plans 01/04/05)"
    - "OG Archivo refresh decision (Claude's discretion per CONTEXT): DEFERRED to Phase 09.1 — UI/OG font divergence stays accepted per D-04; documented here as the discretion record (D-16)"
  artifacts:
    - path: "apps/web/app/globals.css"
      provides: "verified pinned clamps (no changes expected — audit confirms)"
      contains: "clamp(34px, 9vw, 56px)"
  key_links:
    - from: "audit greps"
      to: "entire apps/web/app + packages/ui/src tree"
      via: "zero-count gates on retired palette"
      pattern: "#18181B|#27272A|#A1A1AA|#2A2A30"
---

<objective>
Phase-wide invariant + palette audit and the mobile clamp pass: prove the legacy palette is dead, every D-14 invariant holds, and every display-type role clamps correctly at 375px — fixing any straggler found.

Purpose: D-02's "retired by end of phase" and D-16's final-wave mobile pass are phase-level obligations no single route plan can prove.
Output: Clean audit, fixed stragglers, mobile-safe display type everywhere.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-CONTEXT.md
@.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-UI-SPEC.md
</context>

## Artifacts this phase produces (this plan)

- No new symbols — audit fixes only. Any straggler fix uses tokens/classes already produced by plans 01-13.

<tasks>

<task type="auto">
  <name>Task 1: Legacy palette + invariant audit with straggler fixes</name>
  <files>apps/web/app (any file flagged by audit), packages/ui/src (any file flagged by audit)</files>
  <read_first>
    - .planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-CONTEXT.md (D-14 invariant list — the audit checklist)
    - Output of the audit greps below (read each flagged file before fixing)
  </read_first>
  <action>
    Run the audit greps and fix every hit (replace with the equivalent token/alias; update stale comments too):
    1. Legacy grays: grep -rn "#18181B\|#27272A\|#A1A1AA\|#2A2A30" across apps/web/app, apps/web/components, packages/ui/src (exclude test snapshot dirs and node_modules) — target 0 (D-02).
    2. Old outcome green/red: grep -rn "#22C55E\|#EF4444" same scope — target 0 (D-03).
    3. Grid ban: grep -rn "display: 'grid'\|display:grid\|grid-cols" packages/ui/src and the OG paths (apps/web/app/og, apps/web/lib/og-fallback-render.ts) — target 0; plus `pnpm --filter @call-it/web lint` (no-display-grid-in-og error-level).
    4. Provider lock: git log/diff confirms apps/web/app/Providers.tsx has zero diffs across the phase (compare against the pre-phase commit); auth-kit styles import still present at its line.
    5. MiniAppReady: grep -c "MiniAppReady" apps/web/app/call/[id]/page.tsx >= 2.
    6. Addresses: grep -rn "0xaf88d065\|0x75faf114\|0xFF970A61" apps/web/app packages/ui/src — target 0 outside @call-it/shared imports (addresses only from shared, D-14).
    7. Tailwind pipeline: postcss.config.js exists and globals.css has no `@theme` (v4 marker) — target 0 hits for "@theme".
    8. data.jsx OUTCOMES: grep -rn "data.jsx\|--win\b" apps/web/app — target 0 (the broken prototype map never entered).
    Document the audit results (counts before/after) in the SUMMARY. Where a hit is in a stale COMMENT only, update the comment.
  </action>
  <verify>
    <automated>pnpm --filter @call-it/web test && pnpm --filter @call-it/web lint</automated>
  </verify>
  <done>All eight audit gates report 0 (or documented allowed exceptions, e.g. negative-test fixtures); fixes committed; vitest + lint green.</done>
  <acceptance_criteria>
    - bash -c "grep -rn '#18181B\|#27272A\|#A1A1AA\|#2A2A30' apps/web/app apps/web/components packages/ui/src --include='*.ts*' --include='*.css' | grep -v snapshots | wc -l" returns 0
    - bash -c "grep -rn '#22C55E\|#EF4444' apps/web/app apps/web/components packages/ui/src --include='*.ts*' --include='*.css' | grep -v snapshots | wc -l" returns 0
    - bash -c "grep -rn 'grid-cols\|display:grid' packages/ui/src --include='*.tsx' | wc -l" returns 0
    - apps/web/app/Providers.tsx has no diff vs the phase-start commit
    - apps/web/postcss.config.js exists; grep -c "@theme" apps/web/app/globals.css returns 0
    - `pnpm --filter @call-it/web test && pnpm --filter @call-it/web lint` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Mobile clamp + 375px pass across all ported routes</name>
  <files>apps/web/app/components/MobileDrawer.tsx, apps/web/app (route files needing clamp fixes)</files>
  <read_first>
    - apps/web/app/globals.css (the pinned clamps from 09.2-01: page-header clamp(34px, 9vw, 56px), .h-1 clamp(30px, 8vw, 48px), .outcome-stamp clamp(56px, 17vw, 120px))
    - apps/web/tests/responsive.spec.ts (the Phase-9 375px contract for the 7 critical pages — what must still hold)
    - apps/web/app/hooks/useIsMobile.ts (the JS-ternary responsive pattern preserved on ported screens)
  </read_first>
  <action>
    Sweep every ported route for mobile correctness at 375px (the pinned display-role clamps are the contract): verify the leaderboard hero numeral uses clamp(64px, 18vw, 132px) and the watermark clamp(160px, 48vw, 360px); profile hero clamp(64px, 18vw, 132px); landing hero clamp(64px, 10vw, 132px); stamp clamp via the Stamp component; .page-header h1 and .h-1/.h-2 clamps via globals.css. Fix any inline display-size that escapes its clamp (horizontal overflow at 375px is the failure signal — check feed cards, call receipt stat rows, duel VS columns, create two-column layout collapsing to one). Stat rows wrap 2-up at <768px; all interactive controls >= 44px tall; sidebar fully ceded to MobileDrawer below 768px with no dead nav (drawer contains all sidebar destinations — add any missing route links to MobileDrawer's registry). Build the web app as the final smoke.
  </action>
  <verify>
    <automated>pnpm --filter @call-it/web test && pnpm --filter @call-it/shared build && pnpm --filter @call-it/ui build && pnpm --filter @call-it/web build && pnpm --filter @call-it/web lint</automated>
  </verify>
  <done>All display roles clamp at 375px; drawer covers all nav; full gate green.</done>
  <acceptance_criteria>
    - grep -c "clamp(64px, 18vw, 132px)" apps/web/app/leaderboard/LeaderboardClient.tsx returns >= 1 and same for ProfileClient.tsx
    - grep -c "clamp(64px, 10vw, 132px)" apps/web/app/signin/page.tsx returns >= 1
    - MobileDrawer contains links for every sidebar destination (read both files and reconcile; list parity documented in SUMMARY)
    - Full chain exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase-wide source tree | the audit IS the mitigation verification for the phase's threat register |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.2-39 | Tampering | residual old-palette outcome colors mis-signaling wins/losses | mitigate | zero-count grep gates (Task 1 criteria 1-2) |
| T-09.2-40 | Elevation of Privilege | undetected Providers/middleware drift from earlier waves | mitigate | Providers.tsx phase-diff gate; middleware untouched all phase (no plan listed it) |
| T-09.2-41 | Information Disclosure | hardcoded contract addresses pasted during restyles | mitigate | address grep gate (Task 1 criterion 6) — only @call-it/shared sources |
| T-09.2-SC | Tampering | package installs | accept | zero new packages this phase |
</threat_model>

<verification>
- Both tasks gate on web vitest; Task 2 closes with the full shared→ui→web build + lint chain
</verification>

<success_criteria>
- All audit gates 0; every display role mobile-clamped; drawer nav complete; full gate green
- OG Archivo deferral documented (discretion record for D-16's optional item)
</success_criteria>

<output>
Create `.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-14-SUMMARY.md` when done.
</output>
