---
phase: 09.2-prototype-design-adoption
plan: 06
type: execute
wave: 3
depends_on: ["09.2-01", "09.2-02"]
files_modified:
  - apps/web/app/page.tsx
  - packages/ui/src/compound/CallCard.tsx
  - apps/web/app/components/FromYourNetworkSections.tsx
  - apps/web/components/FeedList.tsx
  - apps/web/tests/feed-shell.spec.ts
autonomous: true
requirements: [UI-48]
must_haves:
  truths:
    - "Follow/fade on feed cards stays amount-based via the real FollowFadeModal — the prototype's one-click toggles are NOT adopted (D-06)"
    - "Feed card data with no live source (pool $, follow/fade/challenge counts, caller rep/accuracy/streak, spicy/contrarian flags, tx footers) is HIDDEN, never faked (D-07)"
    - "Dead tabs (Following — unfiltered; Duels — fetches a nonexistent /api/duels) are CUT from the tab bar; no dead controls ship (D-08)"
    - "Feed keeps useFeed() infinite-query, 5s polling, and the FromYourNetwork gate — prototype is markup donor only (D-05)"
  artifacts:
    - path: "apps/web/app/page.tsx"
      provides: "Prototype tape markup: .tabs Live/Settled, stagger entry, FROM YOUR X divider, empty state"
    - path: "packages/ui/src/compound/CallCard.tsx"
      provides: "Rethemed flexbox-only call card (brutal-card, square avatar, countdown, conviction)"
  key_links:
    - from: "apps/web/app/page.tsx"
      to: "useFeed (lib/relayer-client.ts)"
      via: "existing infinite-query hook"
      pattern: "useFeed"
    - from: "feed card CTAs"
      to: "packages/ui/src/compound/FollowFadeModal.tsx"
      via: "existing modal-open handlers (amount-based, D-06)"
      pattern: "FollowFadeModal"
---

<objective>
Restyle the feed (`/`) to the prototype tape: rethemed CallCard, Live/Settled tabs, FROM YOUR X divider, stagger entry, and the "NOTHING ON THE TAPE" empty state — over the untouched useFeed data layer.

Purpose: D-16 step 1 closes with the highest-traffic surface; the tape is the first thing every signed-in user sees.
Output: Rethemed feed page + CallCard with honest degradation and no dead tabs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-CONTEXT.md
@.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-UI-SPEC.md
@.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-PATTERNS.md
</context>

## Artifacts this phase produces (this plan)

- No new files — markup rewrites in `page.tsx`, `CallCard.tsx`, `FromYourNetworkSections.tsx`, `FeedList.tsx`
- New empty-state copy block: heading "NOTHING ON THE TAPE" + body "Be the first to go on record." + cream CTA to /new

<tasks>

<task type="auto">
  <name>Task 1: Retheme CallCard (flexbox-only) with D-07 field degradation</name>
  <files>packages/ui/src/compound/CallCard.tsx</files>
  <read_first>
    - packages/ui/src/compound/CallCard.tsx (current props contract — FeedItem fields: id, caller, marketType, asset?, stake, conviction, expiry, createdAt, status, displayHandle?/handle?; props stay)
    - "call it frontend/screens/feed.jsx" (CallCard markup donor lines 9-28 + 105-110 — NOTE: its one-click follow/fade toggles with optimistic counters and its FABRICATED pool dollars (follows*12/fades*14) are explicitly NOT ported, D-06/D-07)
    - apps/web/lib/relayer-client.ts (FeedItem type at ~72-84 — the ONLY fields that may render)
  </read_first>
  <action>
    Retheme CallCard to the prototype card recipe — .brutal-card surface, square .avatar grad from handle, @handle + interpunct metadata overline (JBM), statement/market line in Archivo .h-statement voice (clamp to 22px on mobile), category/market-type .pill, conviction display, stake "$N" JBM, Countdown-style time remaining from expiry (D HH:MM:SS JBM format), status pill (LIVE = .live-dot + accent). RENDER NOTHING for fields FeedItem lacks (D-07): no pool $, no follow/fade/challenge counts, no caller rep/accuracy/streak, no spicy/contrarian flags, no tx/block footer. Card CTAs: keep whatever follow/fade affordance currently exists wired to the existing modal-open flow (amount-based FollowFadeModal, D-06) — if the current card has no CTAs and navigation-to-call-page is the action, keep that pattern (navigate; do not add one-click stake toggles). FLEXBOX ONLY (packages/ui Satori constraint, D-14). Props API unchanged so the feed and any OG-adjacent consumers keep compiling.
  </action>
  <verify>
    <automated>pnpm --filter @call-it/ui test && pnpm --filter @call-it/web test</automated>
  </verify>
  <done>CallCard renders the brutal recipe from existing FeedItem fields only; flexbox-only; tests green.</done>
  <acceptance_criteria>
    - CallCard.tsx contains `brutal-card` styling values (bg var(--bg-secondary) or brand-surface alias, 2px border) and a square avatar (no rounded-full/50%)
    - grep -ci "follows \* \|fades \* \|\*12\|\*14" packages/ui/src/compound/CallCard.tsx returns 0 (no fabricated pool math)
    - grep -ci "spicy\|contrarian" packages/ui/src/compound/CallCard.tsx returns 0
    - grep -c "display: 'grid'\|grid-cols" packages/ui/src/compound/CallCard.tsx returns 0
    - `pnpm --filter @call-it/ui test && pnpm --filter @call-it/web test` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Restyle the feed page — Live/Settled tabs, divider, stagger, empty state; cut dead tabs</name>
  <files>apps/web/app/page.tsx, apps/web/app/components/FromYourNetworkSections.tsx, apps/web/components/FeedList.tsx, apps/web/tests/feed-shell.spec.ts</files>
  <read_first>
    - apps/web/app/page.tsx (current 1127 lines: FeedTab type Live/Settled/Following/Duels, useFeed infinite-query, DuelsTab + trending-pin sections, COLORS inline constants at ~95-106 — data hooks stay; tab surface shrinks)
    - "call it frontend/screens/feed.jsx" (tape structure donor: pinned top card → FROM YOUR X divider → tape; tabs; 9 chips that do NOT filter — cut)
    - apps/web/app/components/FromYourNetworkSections.tsx + apps/web/components/FeedList.tsx (existing section gate + list renderer — wiring stays)
    - apps/web/tests/feed-shell.spec.ts (lockstep contract — update any assert on removed tabs/old markup in THIS task)
  </read_first>
  <action>
    page.tsx: rewrite render markup to the prototype tape keeping useFeed/useInfiniteQuery, polling, and FromYourNetwork wiring verbatim. Tab bar → .tabs/.tab recipe rendering ONLY 'Live' and 'Settled' (narrow the rendered tabs; keep the FeedTab type if other code references it): 'Following' is unfiltered (known-dead) and 'Duels' fetches the nonexistent /api/duels — both CUT per D-08 and the CONTEXT deferral ("do not reproduce, do not fix here"). The DuelsTab component and fetchDuels-driven sections are no longer rendered; the trending-duel pin in the Live tab may stay ONLY because it already degrades to hidden when /api/duels fails (current behavior — verify by reading; if it renders anything on failure, cut it too). Replace the local COLORS inline-constant values with token references (var(--*) / preset aliases) — retiring legacy gray usage in this file (D-02). FROM YOUR X section → .section-divider recipe ("FROM YOUR X · N CALLERS..." JBM overline) keeping the existing gate logic in FromYourNetworkSections.tsx. List entry animation → .stagger (or keep .card-enter; pick one, apply consistently). Empty state (no items, no error): mono overline heading "NOTHING ON THE TAPE" + body "Be the first to go on record." + cream Button CTA linking to /new (UI-SPEC Copywriting Contract). Error state: existing toast/error pathway restyled with var(--accent-loss) + "Couldn't load the tape. Retry." retry affordance. Keep all existing data-testid hooks (D-14). 375px (UI-48): single column, full-width cards, 44px targets. feed-shell.spec.ts: update any assert referencing the removed Following/Duels tab markup to assert absence (D-15; comment citing D-08); never delete tests.
  </action>
  <verify>
    <automated>pnpm --filter @call-it/web test && pnpm --filter @call-it/shared build && pnpm --filter @call-it/web build && pnpm --filter @call-it/web lint</automated>
  </verify>
  <done>Feed renders the prototype tape with Live/Settled only; empty/error states per contract; spec lockstep done; full gate green.</done>
  <acceptance_criteria>
    - page.tsx renders exactly two tab labels: grep -c "'Live'\|'Settled'" in the rendered tabs array context returns the narrowed set; no `<DuelsTab` render and no Following tab button in JSX
    - page.tsx contains `NOTHING ON THE TAPE` and `Be the first to go on record.`
    - page.tsx contains NO legacy grays: grep -c "#18181B\|#27272A\|#A1A1AA\|#2A2A30" apps/web/app/page.tsx returns 0
    - Existing data-testid attributes in page.tsx are preserved (git diff shows no data-testid deletions)
    - feed-shell.spec.ts test count not reduced; updated asserts carry D-08/D-15 comments
    - Full chain `pnpm --filter @call-it/web test && pnpm --filter @call-it/shared build && pnpm --filter @call-it/web build && pnpm --filter @call-it/web lint` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → relayer /api/feed | feed data rendering for authenticated users |
| feed CTA → money flow | card affordances lead to real USDC transactions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.2-14 | Tampering | one-click stake toggles entering the money path (prototype pattern) | mitigate | D-06 acceptance: follow/fade only via existing FollowFadeModal; no optimistic stake counters ported |
| T-09.2-15 | Spoofing | fabricated pool $/counts implying false market depth | mitigate | D-07 grep gates = 0 for fabricated math and flag fields |
| T-09.2-16 | Repudiation | dead Duels/Following tabs implying features that don't work | mitigate | tabs CUT per D-08; spec asserts absence |
| T-09.2-SC | Tampering | package installs | accept | zero new packages this phase |
</threat_model>

<verification>
- ui + web vitest per task; full build/lint gate at plan end
- Visual goldens deferred to plan 15 (D-15)
</verification>

<success_criteria>
- Tape renders the prototype design over untouched useFeed wiring; only real fields shown; no dead tabs
- Empty/error states match the copywriting contract
</success_criteria>

<output>
Create `.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-06-SUMMARY.md` when done.
</output>
