---
phase: 09.2-prototype-design-adoption
plan: 07
type: execute
wave: 4
depends_on: ["09.2-02", "09.2-06"]
files_modified:
  - apps/web/app/call/[id]/page.tsx
autonomous: true
requirements: [AUTH-44, SHARE-19]
must_haves:
  truths:
    - "Settled receipt outcome WORDS come from resolveSettledWord/lib/outcome-word.ts including the per-viewer FADED CORRECTLY guard; COLORS/tints come from prototype SETTLED_OUTCOMES (receipt.jsx:284-290) — never data.jsx OUTCOMES (D-09 fader guard via D-14, D-03)"
    - "The settled SHARE controls keep the app's existing working wiring (warpcast/twitter intents + SHARE AS FRAME) — the prototype's dead SHARE button is superseded (D-09)"
    - "The data/handler block (~lines 95-1390) is kept; only the settled render branch is replaced; MiniAppReady stays mounted on every branch (D-14)"
    - "The settled timeline ships as 2-node created→settled — middle milestones have no event source and are HIDDEN (D-07)"
    - "Metadata footer shows real provenance values only; identity is handles-only (D-14 AUTH-44)"
  artifacts:
    - path: "apps/web/app/call/[id]/page.tsx"
      provides: "Replaced settled/disputed render branch (was lines 1392-1863) in prototype receipt skin + cream-panel DisputeModal/ProvenanceModal chrome"
      contains: "resolveSettledWord"
  key_links:
    - from: "settled branch markup"
      to: "apps/web/lib/outcome-word.ts"
      via: "resolveSettledWord word + COLOR_MAP/SETTLED_OUTCOMES color"
      pattern: "resolveSettledWord"
    - from: "settled branch"
      to: "apps/web/app/call/[id]/MiniAppReady.tsx"
      via: "mounted on the settled render path"
      pattern: "MiniAppReady"
---

<objective>
Replace the settled/disputed render branch of the `/call/[id]` monolith (lines ~1392-1863) with the prototype settled-receipt design — 120px stamp, tinted hero, 4-stat row, winners/losers, 2-node timeline — and restyle the in-file DisputeModal/ProvenanceModal to the cream panel, keeping every handler and fetch verbatim.

Purpose: The settled receipt IS the product (core value: unfakeable, visually unmistakable). This is the screen the prototype designed best (D-16 step 2).
Output: Settled branch in the new skin over untouched data/handlers.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-CONTEXT.md
@.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-UI-SPEC.md (outcome color map + Copywriting Contract)
@.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-PATTERNS.md (§7 — replace-render-keep-handlers seam)
</context>

## Artifacts this phase produces (this plan)

- New in-file constant: `SETTLED_OUTCOME_STYLES` — the color/tint/sub map ported from prototype receipt.jsx:284-290 keyed by the resolveSettledWord outcome (CALLED IT #E8F542/rgba(232,245,66,0.04)/"Right."; LOUD AND WRONG #F87171/rgba(248,113,113,0.04)/"The market remembers."; COLD CALL #64748B/rgba(100,116,139,0.04)/"Didn't hit."; CONTRARIAN HIT #E8F542/rgba(232,245,66,0.05)/"Faded the consensus. Right."; FADED CORRECTLY #E8F542/rgba(232,245,66,0.04)/"Right to doubt.")
- No new files — the branch rewrite lives inside `apps/web/app/call/[id]/page.tsx`

<tasks>

<task type="auto">
  <name>Task 1: Replace the settled render branch with the prototype receipt markup</name>
  <files>apps/web/app/call/[id]/page.tsx</files>
  <read_first>
    - apps/web/app/call/[id]/page.tsx (READ the seam region first: the derived-values end at ~1385-1390, the settled branch 1392-1863 including resolveSettledWord at ~1413 and the share controls; the data/handler block 95-1390 is KEPT VERBATIM — read enough of it to know every derived variable name the new markup re-consumes: outcomeWord/outcomeColor, pnl/repDelta/finalValue/targetValue/conviction fields, positions winners/losers, provenance fields, modal open-state setters)
    - "call it frontend/screens/receipt.jsx" (ReceiptSettledScreen markup donor + SETTLED_OUTCOMES at 284-290 — the canonical color map; NEVER import or replicate data.jsx OUTCOMES which references broken CSS vars)
    - apps/web/lib/outcome-word.ts (words of record incl. PENDING RESULT neutral fallback + per-viewer FADED CORRECTLY — consumed, never modified)
    - apps/web/app/call/[id]/MiniAppReady.tsx (must remain mounted on the settled path)
    - .planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-UI-SPEC.md (outcome color table + metadata footer copy contract)
  </read_first>
  <action>
    Inside page.tsx, replace ONLY the settled/disputed render branch (the JSX returned when isSettled || (isCallerExited && callData?.outcome)) with the prototype ReceiptSettledScreen structure, re-consuming the existing derived variables: hero section with outcome tint background from the new SETTLED_OUTCOME_STYLES map (define it near the branch; keyed by resolvedSettled.word), Stamp component (rethemed in 09.2-02) rendering outcomeWord with hexColor from the map — the word ALWAYS comes from resolveSettledWord (08-05 GAP 1: never hardcode outcome words); sub-line from the map ("Right." / "The market remembers." / etc.); market line in .h-statement Archivo; 4-stat row (.stat-block): P&L, REP delta, FINAL vs TARGET value, CONVICTION — using the existing derived fields, hiding any stat whose field is null (D-07); WINNERS/LOSERS lists from the existing positions data (handles + amounts only — AUTH-44); "// WHAT HAPPENED" timeline as exactly 2 nodes — created (createdAt) → settled (settledAt) with real timestamps; middle milestones have no event source and are NOT rendered (D-07). Share row: keep the EXISTING share handlers and controls byte-for-byte in behavior — warpcast intent, twitter intent, SHARE AS FRAME outline control (D-09; SHARE-19 surface) — restyled as .btn recipes. Metadata footer: mono "settled · {txHash short} · block {n} · {UTC}" using ONLY real provenance fields where present, hidden where absent (D-07). Caller-exited-with-outcome variant keeps its banner treatment (warning #FB923C). MiniAppReady stays mounted. Pending-dispute affordances (raise dispute button → DisputeModal, provenance link → ProvenanceModal) keep their existing open-state wiring. Keep every existing data-testid (D-14). Mobile: stamp clamp comes from the Stamp component; stat row wraps 2-up at <768px (UI-48).
  </action>
  <verify>
    <automated>pnpm --filter @call-it/web test</automated>
  </verify>
  <done>Settled branch renders the prototype receipt from existing derived data; words from lib/outcome-word.ts; vitest (incl. settled-outcome-truth + outcome-word + miniapp-ready tests) green.</done>
  <acceptance_criteria>
    - page.tsx settled branch contains `resolveSettledWord` (word source unchanged) and a `SETTLED_OUTCOME_STYLES` (or equivalently named) map containing `rgba(232,245,66,0.04)` and `The market remembers.`
    - grep -c "CALLED IT\|LOUD AND WRONG" — outcome word string literals appear ONLY inside the style-map keys/comments, never as rendered fallbacks replacing resolveSettledWord output
    - grep -c "MiniAppReady" apps/web/app/call/[id]/page.tsx returns >= 2 (still mounted on all branches)
    - Share wiring intact: page.tsx still contains the warpcast intent URL builder, twitter intent, and SHARE AS FRAME control identifiers present before the change (git diff shows no handler deletions)
    - Timeline renders exactly created + settled nodes: no "hit 50 followers"/"pool exceeded" strings (grep returns 0)
    - The data/handler block is untouched: git diff for lines < 1390 region shows no logic changes (handlers/fetches identical)
    - `pnpm --filter @call-it/web test` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Restyle DisputeModal + ProvenanceModal chrome to the cream .modal-panel template</name>
  <files>apps/web/app/call/[id]/page.tsx</files>
  <read_first>
    - apps/web/app/call/[id]/page.tsx (DisputeModal defined at ~line 464, ProvenanceModal at ~803 — INSIDE the kept handler block; their tx/fetch logic — raiseDispute SM write, $5 USDC bond preflight, provenance fetch — stays verbatim; only JSX chrome changes)
    - "call it frontend/styles.css" lines 821-840 (.modal-overlay/.modal-panel — the designated template for ALL modals, D-13)
  </read_first>
  <action>
    Restyle the two in-file modal components' JSX to the cream template: overlay rgba(0,0,0,0.82) + backdrop blur(4px) z-200; panel bg var(--bg-inverse) cream, BLACK text (#000 — every text token inside flips to inverse; audit any text-brand-* usage inside these modals), 3px black border, var(--shadow-brutal-lg), max-width 620, padding 36, max-height 90vh overflow-y auto. DisputeModal: "RAISE DISPUTE" header in the FINAL·CONFIRM mono style; the $5 USDC bond amount stays EXPLICIT before the confirm CTA (UI-SPEC destructive contract); confirm = cream-on-cream needs contrast — use the black-bordered solid black-text outline button or the warning treatment; keep the existing dispute submission handler, IPFS evidence flow, and any disabled/loading states. ProvenanceModal: provenance fields in JBM mono rows (.activity-row recipe acceptable), real values only. Do not change the open/close state mechanism, props, or any fetch/write logic. Page-level grid is allowed here, but prefer flexbox for consistency.
  </action>
  <verify>
    <automated>pnpm --filter @call-it/web test && pnpm --filter @call-it/shared build && pnpm --filter @call-it/web build && pnpm --filter @call-it/web lint</automated>
  </verify>
  <done>Both modals render the cream panel with logic untouched; full gate green.</done>
  <acceptance_criteria>
    - DisputeModal JSX contains the cream panel values (`var(--bg-inverse)` or `#F5F1E8`, 3px black border, blur(4px) overlay) and still contains the $5 bond copy and the existing raiseDispute handler reference
    - ProvenanceModal renders only real provenance fields (no placeholder hashes)
    - git diff confirms no changes to the dispute write/preflight logic identifiers
    - Full chain `pnpm --filter @call-it/web test && pnpm --filter @call-it/shared build && pnpm --filter @call-it/web build && pnpm --filter @call-it/web lint` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public viewer → /call/[id] settled receipt | THE public shareable artifact; outcome semantics must be unfakeable |
| browser → SM contract | dispute raise = $5 USDC bond write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.2-17 | Information Disclosure | FADED CORRECTLY shown to public viewers (D-09 guard regression) | mitigate | word source stays resolveSettledWord (per-viewer guard lives in lib/outcome-word.ts, untouched); settled-outcome-truth.test.ts + outcome-word.test.ts in the per-task vitest gate |
| T-09.2-18 | Information Disclosure | wallet addresses in winners/losers lists (AUTH-44) | mitigate | handles-only acceptance criterion; AUTH-44 invariant test in vitest gate |
| T-09.2-19 | Spoofing | fake timeline milestones / fake provenance footers | mitigate | 2-node-only + real-values-only acceptance criteria (grep gates) |
| T-09.2-20 | Tampering | dispute bond CTA restyled into silently-broken state | mitigate | handler-identifier git-diff gate; bond amount explicit in copy |
| T-09.2-SC | Tampering | package installs | accept | zero new packages this phase |
</threat_model>

<verification>
- Web vitest after each task (settled-outcome-truth, outcome-word, miniapp-ready are the tripwires)
- Full build/lint gate at plan end
- Side-by-side vs screenshots/settled.png deferred to plan 15 checkpoint
</verification>

<success_criteria>
- Settled receipt renders the prototype design with words from the app and colors from SETTLED_OUTCOMES
- Share wiring, dispute flow, and MiniAppReady all intact; data/handler block unchanged
</success_criteria>

<output>
Create `.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-07-SUMMARY.md` when done.
</output>
