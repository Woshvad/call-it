---
phase: 09.2-prototype-design-adoption
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/globals.css
  - apps/web/app/layout.tsx
  - packages/ui/tailwind.preset.ts
  - packages/ui/src/tokens/colors.ts
autonomous: true
requirements: [UI-46, UI-47]
must_haves:
  truths:
    - "The prototype ROOT variant tokens are the single token layer in apps/web/app/globals.css as CSS custom properties (D-01, D-02)"
    - "Money/outcome colors are chartreuse #E8F542 win and soft red #F87171 loss everywhere the token layer defines them; contrarian maps to the win color (D-03)"
    - "Archivo 800/900, Inter, and JetBrains Mono load via next/font with CSS variables; OG Syne/SpaceGrotesk TTFs in apps/web/app/fonts/ are untouched (D-04)"
    - "Prototype styles.css is a token + class donor only — no prototype state, mock data, or hash routing entered the app (D-05)"
    - "tailwind.preset.ts brand-* alias KEYS are unchanged so ~30 @call-it/ui consumer files keep compiling (D-02)"
    - "Tailwind stays v3.4 classic with postcss.config.js — no v4 @theme port; @farcaster/auth-kit/styles.css stays in the cascade (D-14)"
    - "This plan is Wave 1 of the adoption sequence locked by D-16 (foundations before any screen port)"
  artifacts:
    - path: "apps/web/app/globals.css"
      provides: "Prototype :root token layer + all ported brutal-* primitive classes + pinned mobile clamps"
      contains: "--bg-inverse: #F5F1E8"
    - path: "apps/web/app/layout.tsx"
      provides: "next/font Archivo/Inter/JetBrains Mono variables on <html>; inline body styles deleted"
      contains: "next/font/google"
    - path: "packages/ui/tailwind.preset.ts"
      provides: "brand-*/outcome-* keys remapped to var(--*) values + display/body/mono font families"
      contains: "var(--accent-win)"
    - path: "packages/ui/src/tokens/colors.ts"
      provides: "COLOR_MAP literal hexes updated to prototype palette for framer-motion/Satori runtime use"
      contains: "#F87171"
  key_links:
    - from: "apps/web/app/layout.tsx"
      to: "apps/web/app/globals.css"
      via: "font CSS variables --font-archivo/--font-inter/--font-jetbrains-mono consumed by --font-display/--font-sans/--font-mono"
      pattern: "--font-archivo"
    - from: "packages/ui/tailwind.preset.ts"
      to: "apps/web/app/globals.css"
      via: "var(--*) custom property references resolved at runtime"
      pattern: "var\\(--bg-secondary\\)"
---

<objective>
Lay the design foundation for the prototype adoption: paste the prototype `:root` tokens into `globals.css` as the single token layer, port every brutal-* primitive CSS class, load Archivo/Inter/JetBrains Mono via next/font, and remap the Tailwind preset + COLOR_MAP values — keeping every existing alias key so all consumers keep compiling.

Purpose: This is the 20% that produces 80% of the visual change (09.2-RESEARCH.md §6 Step 0). Every later plan consumes these tokens and classes.
Output: New token layer, primitive class vocabulary, brand fonts rendering in-browser for the first time, rethemed preset values.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-CONTEXT.md
@.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-UI-SPEC.md
@.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-PATTERNS.md
</context>

## Artifacts this phase produces (this plan)

- CSS custom properties in `apps/web/app/globals.css`: `--bg-primary`, `--bg-secondary`, `--bg-tertiary`, `--bg-quaternary`, `--bg-ticker`, `--bg-inverse`, `--border-subtle`, `--border-active`, `--border-strong`, `--border-accent`, `--accent-win`, `--accent-loss`, `--accent-neutral`, `--accent-warning`, `--accent-duel`, `--text-primary`, `--text-secondary`, `--text-tertiary`, `--text-muted`, `--text-inverse`, `--shadow-brutal`, `--shadow-brutal-lg`, `--shadow-brutal-sm`, `--font-display`, `--font-sans`, `--font-mono`
- CSS classes ported into `globals.css`: `.brutal-card` (+`.heavy`/`.hero`/`.cream`/`.interactive`/`.bracketed`), `.btn` (+`.cream`/`.big`/`.full`/`.outline-white`/`.fade`/`.duel`/`.ghost`), `.brutal-input`, `.brutal-textarea`, `.brutal-select`, `.brutal-slider`, `.pill` (+variants), `.chip` (+`.active`), `.toggle-pill` (+`.on`), `.tabs`/`.tab`, `.brutal-table`, `.stat-block` (+`.bracketed`, `.br-bl`/`.br-br`), `.label-overline`, `.section-divider`, `.page-header`, `.h-statement`/`.h-1`/`.h-2`/`.h-3`/`.h-display`, `.outcome-stamp` (+`stampReveal` keyframes), `.live-dot`, `.stagger` (+`hardCut`), `.your-row-tint`, `.system-ticker`, `.activity-row` (+`stampIn`), `.modal-overlay`/`.modal-panel`, `.bracketed`, `.brutal-bar` (+`.split`), `.avatar` (+sm/lg/xl, grad-a..grad-f), `.brutal-search`, `.wallet-pill`, `.icon-btn`, `.nav-item`/`.nav-badge`/`.nav-dot`/`.nav-section-label`, `.sidebar-rep`, `.sidebar-meta`, `.row`/`.col`/`.spread`/`.mono`/`.muted`/`.text-win`/`.text-loss`/`.text-warn`/`.text-duel`/`.text-neutral`/`.bg-cream`/`.slot-num`, `.corner-stamp-tl/tr/bl/br`, `drain`/`fadeIn` keyframes (relocated from the orphaned packages/ui/src/styles/globals.css)
- Font exports in `apps/web/app/layout.tsx`: `archivo` (Archivo 700/800/900, variable `--font-archivo`), `inter` (Inter 400/500/600, variable `--font-inter`), `jbm` (JetBrains_Mono 500/600/700, variable `--font-jetbrains-mono`)
- New Tailwind preset tokens: `brand-cream`, `accent-neutral`, `accent-warning` (added alongside the remapped existing keys)
- New COLOR_MAP-adjacent constants: updated values for `OUTCOME_WIN` (#E8F542), `OUTCOME_LOSS` (#F87171), `OUTCOME_CONTRARIAN` (#E8F542 per D-03), `BRAND_TEXT` (#F1F5F9), `BRAND_MUTED` (#94A3B8), `BRAND_BORDER` (#1E1E2E), `BRAND_SURFACE` (#111118), plus net-new `BRAND_CREAM` (#F5F1E8), `ACCENT_DUEL` (#A855F7)

<tasks>

<task type="auto">
  <name>Task 1: Port prototype :root tokens + every primitive class into globals.css with pinned mobile clamps</name>
  <files>apps/web/app/globals.css</files>
  <read_first>
    - apps/web/app/globals.css (current state: @tailwind directives + cardEnter keyframe + reduced-motion fallback — keep all of it)
    - "call it frontend/styles.css" (FULL FILE, 918 lines — the donor: tokens lines 3-41, .brutal-card 339-367, .btn + cream press physics 387-463, .modal-overlay/.modal-panel 821-840, and every primitive class listed in the Artifacts section)
    - .planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-UI-SPEC.md (Primitive Class Inventory table + Typography table + Spacing exceptions — the contract for every recipe)
    - packages/ui/src/styles/globals.css (orphaned drain + fadeIn keyframes to relocate here)
  </read_first>
  <action>
    Append to apps/web/app/globals.css (keeping the @tailwind directives at top, the cardEnter keyframe, and the prefers-reduced-motion block):
    1. The prototype :root token block verbatim from styles.css:3-41, with ONE rewrite: the three font variables become next/font references — --font-display: var(--font-archivo), "Inter", system-ui, sans-serif; --font-sans: var(--font-inter), system-ui, sans-serif; --font-mono: var(--font-jetbrains-mono), ui-monospace, monospace. Never reference Google-host font family names directly (D-04: fonts load via next/font, not Google CSS).
    2. Global base rules (replacing the deleted layout.tsx inline body styles): html,body margin 0, background var(--bg-primary), color var(--text-primary), font 14px var(--font-sans), line-height 1.45, -webkit-font-smoothing antialiased; * { border-radius: 0 } per D-01 (radius 0 everywhere); ::selection background var(--accent-win) color #000; ::placeholder italic var(--text-muted).
    3. Every primitive class from the donor, per the UI-SPEC Primitive Class Inventory: .brutal-card and variants, .btn and variants with cream press physics EXACTLY (hover translate(2px,2px) + --shadow-brutal-sm; active translate(4px,4px) + no shadow; 0.08s linear — note this is down-right, the OPPOSITE of the current Button.tsx direction), .brutal-input/textarea/select/slider, .pill variants, .chip/.active, .toggle-pill/.on, .tabs/.tab/.count, .brutal-table, .stat-block + .br-bl/.br-br bracket spans, .label-overline, .section-divider, .page-header, .h-statement/.h-1/.h-2/.h-3/.h-display, .outcome-stamp + stampReveal keyframes (0.4s cubic-bezier(0.34,1.56,0.64,1), scale 1.3 rotate(-2deg) opacity 0 → 0.96/-1deg → 1/-1deg), .live-dot (6px accent square 1.4s pulse), .stagger + hardCut, .your-row-tint, .system-ticker (80s marquee, pause on hover), .activity-row + stampIn, .modal-overlay/.modal-panel (overlay rgba(0,0,0,0.82) blur(4px) z-200; panel cream #F5F1E8 black text 3px black border --shadow-brutal-lg max-width 620 padding 36 max-height 90vh), .bracketed, .brutal-bar/.split, .avatar sizes + grad-a..grad-f (#E8F542/#FB923C/#A855F7/#22D3EE/#F472B6/#FBBF24, square, Archivo 900 initials, black initials except grad-c white), .brutal-search, .wallet-pill, .icon-btn, .nav-item/.nav-badge/.nav-dot/.nav-section-label, .sidebar-rep, .sidebar-meta, row/col/spread/mono/muted/text-win/text-loss/text-warn/text-duel/text-neutral/bg-cream/slot-num utilities, corner-stamp positions.
    4. Pinned mobile clamps (the UI-SPEC checker required concrete values; D-16 final-wave audit verifies them) inside an @media (max-width: 767px) block or as clamp() on the base classes: .page-header h1 font-size clamp(34px, 9vw, 56px); .h-1 clamp(30px, 8vw, 48px); .h-2 clamp(24px, 6.5vw, 32px); .outcome-stamp clamp(56px, 17vw, 120px); .h-display clamp(48px, 13vw, 96px). At max-width 767px: .stat-block rows wrap to 2-up (flex-wrap, flex-basis calc(50% - 8px)); interactive controls min-height 44px (UI-48 carry-over).
    5. Relocate the drain and fadeIn keyframes from the orphaned packages/ui/src/styles/globals.css into this file (they back Toast countdown + Stamp reduced-motion; the package file is imported nowhere — latent bug from RESEARCH wiring-risk appendix).
    Translate prototype grid-based layout helper classes only where they are page-level chrome (.layout, .app-header land with the shell plan 03, NOT here). Do not port tweaks-panel styles, v1-clean styles, or any data/JS (D-05).
  </action>
  <verify>
    <automated>pnpm --filter @call-it/web test</automated>
  </verify>
  <done>globals.css contains the full :root token block, every primitive class from the inventory, pinned clamps, and the relocated keyframes; existing cardEnter block intact; web vitest exits 0.</done>
  <acceptance_criteria>
    - globals.css contains exact strings: `--bg-inverse: #F5F1E8`, `--accent-win: #E8F542`, `--accent-loss: #F87171`, `--shadow-brutal: 4px 4px 0 0 #000`, `var(--font-archivo)`
    - globals.css contains class selectors: `.brutal-card`, `.btn.cream`, `.modal-panel`, `.outcome-stamp`, `.label-overline`, `.toggle-pill`, `.activity-row`, `.your-row-tint`, `.wallet-pill`, `.sidebar-rep`, `.brutal-bar`, `.avatar`
    - globals.css contains `@keyframes stampReveal`, `@keyframes stampIn`, `@keyframes drain`, `@keyframes fadeIn`, and keeps `@keyframes cardEnter`
    - globals.css contains pinned clamps: `clamp(34px, 9vw, 56px)`, `clamp(56px, 17vw, 120px)`, `clamp(30px, 8vw, 48px)`
    - Cream press physics present: `translate(2px, 2px)` (hover, with --shadow-brutal-sm) and `translate(4px, 4px)` (active, shadow none) — verify with: grep -c "translate(2px" apps/web/app/globals.css returns >= 1
    - `@tailwind base` is still the first directive (Tailwind v3.4 classic pipeline, D-14)
    - No Google Fonts URL or @import appears: grep -c "fonts.googleapis\|@import url" apps/web/app/globals.css returns 0
    - `pnpm --filter @call-it/web test` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Load Archivo/Inter/JetBrains Mono via next/font and delete layout.tsx inline body styles</name>
  <files>apps/web/app/layout.tsx</files>
  <read_first>
    - apps/web/app/layout.tsx (current 41 lines: inline body style block lines 24-32 to DELETE; force-dynamic + metadata + ClientProviders/GlobalNav mount to KEEP verbatim)
    - .planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-PATTERNS.md (§3 Fonts + layout — the exact next/font pattern and weight contract)
  </read_first>
  <action>
    In apps/web/app/layout.tsx: import Archivo, Inter, and JetBrains_Mono from next/font/google. Instantiate at module scope: Archivo with subsets ['latin'], weight ['700','800','900'], variable '--font-archivo'; Inter with subsets ['latin'], weight ['400','500','600'], variable '--font-inter'; JetBrains_Mono with subsets ['latin'], weight ['500','600','700'], variable '--font-jetbrains-mono'. (Weight contract from UI-SPEC: Archivo only 700/800/900, Inter only 400/500/600, JBM only 500/600/700 — no other weights load.) Apply the three .variable class names to the <html> element className. DELETE the entire inline style object on <body> (margin/padding/backgroundColor/color/fontFamily/minHeight — these now live in the globals.css html,body rule from Task 1; RESEARCH wiring-risk #1: inline styles beat the cascade). Keep verbatim: the import './globals.css' line, export const dynamic = 'force-dynamic', the full metadata block, and the <ClientProviders><GlobalNav/>{children}</ClientProviders> mount order (the shell swap happens in plan 09.2-03, not here). Do NOT touch apps/web/app/fonts/*.ttf or lib/og-fonts.ts — OG keeps Syne/SpaceGrotesk this phase (D-04; tests/og-unit.test.ts:125-132 asserts them).
  </action>
  <verify>
    <automated>pnpm --filter @call-it/web test</automated>
  </verify>
  <done>layout.tsx has the three next/font instances with exact weights and variables; body has no inline style attribute; og-unit vitest font asserts still green.</done>
  <acceptance_criteria>
    - layout.tsx contains `next/font/google`, `--font-archivo`, `--font-inter`, `--font-jetbrains-mono`
    - layout.tsx does NOT contain `backgroundColor: '#09090E'` or `fontFamily: 'system-ui` (inline body styles deleted)
    - layout.tsx still contains `force-dynamic`, `ClientProviders`, and `GlobalNav` (shell swap deferred to plan 03)
    - apps/web/app/fonts/ directory unchanged (Syne/SpaceGrotesk TTFs present): ls apps/web/app/fonts | grep -ci "syne" returns >= 1
    - `pnpm --filter @call-it/web test` exits 0 (includes og-unit.test.ts font asserts)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Remap tailwind.preset.ts values to var(--*) keeping alias keys; update COLOR_MAP literals; full build gate</name>
  <files>packages/ui/tailwind.preset.ts, packages/ui/src/tokens/colors.ts</files>
  <read_first>
    - packages/ui/tailwind.preset.ts (current 51 lines — every existing color KEY stays; only VALUES change; the file-top Satori grid-ban comment MUST survive)
    - packages/ui/src/tokens/colors.ts (current 35 lines — literal hexes consumed at runtime by Stamp.tsx framer-motion boxShadow strings; CSS vars do NOT reach those reliably, so these stay literal hexes with NEW values)
    - .planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-PATTERNS.md (§2 — the exact remap table)
  </read_first>
  <action>
    In packages/ui/tailwind.preset.ts, remap color VALUES keeping every KEY (D-02 — ~30 @call-it/ui consumers keep compiling): brand-bg → var(--bg-primary); brand-accent → var(--accent-win); brand-text → var(--text-primary); brand-muted → var(--text-secondary); brand-border → var(--border-subtle); brand-surface → var(--bg-secondary); outcome-win → var(--accent-win) (D-03); outcome-loss → var(--accent-loss) (D-03); outcome-contrarian → var(--accent-duel) (the KEY stays; #A855F7 remains duel identity — the CONTRARIAN HIT stamp gets the win color via COLOR_MAP/Stamp in plan 02, fixing the documented stale token per D-03). Add net-new keys: brand-cream → var(--bg-inverse), accent-neutral → var(--accent-neutral), accent-warning → var(--accent-warning). Replace fontFamily values: display → ['var(--font-archivo)','system-ui','sans-serif'], body → ['var(--font-inter)','system-ui','sans-serif'], mono → ['var(--font-jetbrains-mono)','ui-monospace','monospace']. Keep borderWidth 3/4 and the Satori grid-ban comment. (Opacity-modifier safety: verified zero `brand-*/N` usages exist in the repo, so plain var() values are safe in Tailwind 3.4.)
    In packages/ui/src/tokens/colors.ts, update the literal hex constants to the prototype palette: BRAND_TEXT '#F1F5F9', BRAND_MUTED '#94A3B8', BRAND_BORDER '#1E1E2E', BRAND_SURFACE '#111118', OUTCOME_WIN '#E8F542', OUTCOME_LOSS '#F87171', OUTCOME_CONTRARIAN '#E8F542' (D-03: contrarian stamp uses the win color). BRAND_BG and BRAND_ACCENT keep their values (#09090E / #E8F542, already identical). Add exported constants BRAND_CREAM = '#F5F1E8' and ACCENT_DUEL = '#A855F7' (duel identity consumers need a named literal now that OUTCOME_CONTRARIAN no longer carries purple). Keep COLOR_MAP keys unchanged; add 'brand-cream' and 'accent-duel' entries. Update the doc comments to reference the prototype token layer.
    Then run the full wave gate: shared build, ui build/test, web test, web build, web lint — all must exit 0.
  </action>
  <verify>
    <automated>pnpm --filter @call-it/shared build && pnpm --filter @call-it/ui test && pnpm --filter @call-it/web test && pnpm --filter @call-it/web build && pnpm --filter @call-it/web lint</automated>
  </verify>
  <done>Preset values are var(--*) with all original keys intact; COLOR_MAP literals match the prototype palette; full build + test + lint gate exits 0.</done>
  <acceptance_criteria>
    - tailwind.preset.ts contains `'brand-accent': 'var(--accent-win)'` and `'brand-surface': 'var(--bg-secondary)'` and `'brand-cream': 'var(--bg-inverse)'`
    - tailwind.preset.ts contains NO literal `#18181B`, `#27272A`, `#A1A1AA`, `#22C55E`, `#EF4444` (legacy values retired from this file per D-02/D-03)
    - tailwind.preset.ts still contains the keys `brand-bg`, `brand-text`, `brand-muted`, `brand-border`, `outcome-win`, `outcome-loss`, `outcome-contrarian` and the Satori grid-ban comment
    - colors.ts contains `OUTCOME_CONTRARIAN = '#E8F542'` and `OUTCOME_LOSS = '#F87171'` and `BRAND_CREAM = '#F5F1E8'`
    - fontFamily display references `var(--font-archivo)`
    - Full command chain `pnpm --filter @call-it/shared build && pnpm --filter @call-it/ui test && pnpm --filter @call-it/web test && pnpm --filter @call-it/web build && pnpm --filter @call-it/web lint` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → CSS cascade | token layer governs what every viewer sees, including outcome semantics |
| build pipeline | preset/font changes affect every compiled page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.2-01 | Tampering | outcome color semantics (COLOR_MAP / preset) | mitigate | D-03 mapping pinned in acceptance criteria; outcome WORDS untouched (lib/outcome-word.ts not in files_modified); settled-outcome-truth.test.ts + outcome-word.test.ts run in the per-task vitest gate |
| T-09.2-02 | Denial of Service | next/font build-time fetch failure breaking the build | mitigate | full `next build` is the Task 3 gate; system-ui fallbacks pinned in every font stack |
| T-09.2-03 | Information Disclosure | none — no identity rendering changes in this plan | accept | layout.tsx body-style deletion renders no data |
| T-09.2-SC | Tampering | package installs | accept | zero new packages this phase — next/font ships inside next@16.2.6 already installed; no npm install tasks exist |
</threat_model>

<verification>
- `pnpm --filter @call-it/web test` green after every task (includes provider-order AST lock, AUTH-44 invariant, og-unit font asserts, outcome-word tests — the D-05 "markup donor only" tripwires)
- Full wave gate green at Task 3 (shared build → ui test → web test → web build → lint)
- Visual goldens are EXPECTED to fail from this plan onward — they encode the old design and are excluded from per-task gates until plan 15 (D-15)
</verification>

<success_criteria>
- Prototype token layer + full primitive class vocabulary live in globals.css with pinned mobile clamps
- Brand fonts render in-browser via next/font (first time ever); OG fonts untouched
- Preset + COLOR_MAP remapped with zero consumer-breaking key changes
- Full build/test/lint gate exits 0
</success_criteria>

<output>
Create `.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-01-SUMMARY.md` when done.
</output>
