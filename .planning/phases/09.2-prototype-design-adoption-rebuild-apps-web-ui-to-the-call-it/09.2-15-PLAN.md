---
phase: 09.2-prototype-design-adoption
plan: 15
type: execute
wave: 9
depends_on: ["09.2-14"]
files_modified:
  - apps/web/tests/visual-smoke.spec.ts-snapshots
  - apps/web/tests/og-thumbnail-200px.spec.ts-snapshots
  - apps/web/tests (snapshot dirs for design-system-snap + responsive)
autonomous: false
requirements: [UI-48]
must_haves:
  truths:
    - "Visual goldens (visual-smoke, design-system-snap, og-thumbnail-200px, responsive) are regenerated against the new design ONLY after the operator visually approves it — never mid-phase to silence a failure (D-15)"
    - "The full Playwright suite is green against the regenerated baselines, closing the phase with tests-of-record updated deliberately (D-15)"
  artifacts:
    - path: "apps/web/tests/visual-smoke.spec.ts-snapshots"
      provides: "New-design golden baselines"
  key_links:
    - from: "operator approval checkpoint"
      to: "golden regeneration"
      via: "blocking human-verify gate before --update-snapshots"
      pattern: "approved"
---

<objective>
Close the phase: operator visually approves the new design against the prototype ground truth, then regenerate every visual golden and run the full Playwright suite green.

Purpose: D-15 — goldens encode the design of record; they change only with deliberate approval.
Output: Approved design + regenerated baselines + fully green test suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-CONTEXT.md
@.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-VALIDATION.md (Manual-Only Verifications table — the checkpoint script)
</context>

## Artifacts this phase produces (this plan)

- Regenerated Playwright snapshot baselines for: visual-smoke, design-system-snap, responsive, og-thumbnail-200px (env-gated)
- No source-code symbols

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator design approval — side-by-side vs prototype ground truth</name>
  <files>none — verification-only checkpoint</files>
  <action>Pause execution and present the verification script below to the operator. Do NOT regenerate any snapshot or run --update-snapshots before the operator responds "approved". If the operator reports issues, convert each into a fix task and re-run this checkpoint after fixes land.</action>
  <what-built>Plans 01-14 rebuilt every apps/web surface to the `call it frontend/` ROOT design: token layer + brutal primitives, app shell (header/sidebar/flag-gated ticker), leaderboard, profile, feed, call receipt (live + settled + modals), /new, duel, landing(/signin), and all uncovered surfaces — with the legacy palette retired and mobile clamps applied.</what-built>
  <how-to-verify>
    1. Run the app locally: `pnpm --filter @call-it/shared build && pnpm --filter @call-it/web dev` (relayer env per .env.local).
    2. Side-by-side fidelity: open "call it frontend/screenshots/home.png" and "call it frontend/screenshots/settled.png" (ROOT ground truth) and compare against http://localhost:3000/signin (landing) and a settled call at /call/14 (known CallerLost → "LOUD AND WRONG"). Check: cream blocks, hard black 4/8px shadows, radius 0, Archivo display + JBM labels, chartreuse/soft-red money colors.
    3. Press physics: hover + press any cream CTA — it must translate down-right 2px/4px with the shadow shrinking, 0.08s linear. Watch a settled receipt load for the stamp overshoot reveal.
    4. Late-hydration flash: hard-reload / and /call/14 — shell chrome must render stable before Privy hydrates (no layout jump beyond the auth slots).
    5. Mobile: resize across 767/768px — sidebar collapses into MobileDrawer with no dead nav; at 375px check feed, receipt (live+settled), profile, leaderboard, signin, onboarding for overflow.
    6. Money flows: open (don't submit) FollowFadeModal, ChallengeFormModal, and the publish confirm — cream panels, readable black text, amounts/bond/permanence copy visible.
  </how-to-verify>
  <resume-signal>Type "approved" to regenerate goldens, or describe issues (issues route back as fix tasks before any golden regeneration)</resume-signal>
  <verify>
    <human-check>Operator completed the 6-step script above and replied "approved"</human-check>
  </verify>
  <done>Operator approval recorded; any reported issues fixed and re-verified before Task 2 runs</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate all visual goldens and run the full Playwright suite</name>
  <files>apps/web/tests/visual-smoke.spec.ts-snapshots, apps/web/tests/og-thumbnail-200px.spec.ts-snapshots, design-system-snap + responsive snapshot dirs</files>
  <read_first>
    - apps/web/playwright.config.ts (webServer setup, projects)
    - apps/web/tests/og-thumbnail-200px.spec.ts (env gate OG_200PX_BASELINES=1) and design-system-snap.spec.ts (env gate NEXT_PUBLIC_DEV_ROUTES=1)
  </read_first>
  <action>
    Only after Task 1 approval: regenerate baselines deliberately — run `pnpm --filter @call-it/web exec playwright test visual-smoke design-system-snap responsive --update-snapshots` with NEXT_PUBLIC_DEV_ROUTES=1, and `OG_200PX_BASELINES=1 pnpm --filter @call-it/web exec playwright test og-thumbnail-200px --update-snapshots` for the OG thumbnails (OG cards still render the Syne/SpaceGrotesk design per D-04 — their goldens may be unchanged; regenerate only if the suite reports diffs from incidental layout shifts). Then run the FULL Playwright suite (`pnpm --filter @call-it/web exec playwright test`) and fix any residual failures by updating the failing assert to the new design contract (never deleting tests, D-15) — except true regressions, which are fixed in source. Commit the new snapshot baselines. Record in the SUMMARY: which suites regenerated, which tests were updated and why.
  </action>
  <verify>
    <automated>pnpm --filter @call-it/web exec playwright test</automated>
  </verify>
  <done>Full Playwright suite green against regenerated baselines; snapshots committed.</done>
  <acceptance_criteria>
    - `pnpm --filter @call-it/web exec playwright test` exits 0
    - New snapshot files exist with post-approval timestamps in the visual-smoke and design-system-snap snapshot dirs
    - git log shows the snapshot regeneration as a deliberate commit AFTER the approval checkpoint
    - No test files deleted (git diff name-status shows no D entries under apps/web/tests)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| golden baselines | the visual record of what the product is allowed to look like |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.2-42 | Repudiation | goldens regenerated without human approval (laundering a broken design into the baseline) | mitigate | blocking human-verify checkpoint BEFORE --update-snapshots; commit-order acceptance criterion |
| T-09.2-43 | Tampering | failing behavior tests "fixed" by deletion during the green-up | mitigate | no-deletion acceptance criterion (git name-status gate) |
| T-09.2-SC | Tampering | package installs | accept | zero new packages this phase |
</threat_model>

<verification>
- Full Playwright suite is the phase-closing gate; vitest + builds already green from plan 14
</verification>

<success_criteria>
- Operator approved the design against prototype ground truth
- All goldens regenerated deliberately; full E2E suite green; phase closeable
</success_criteria>

<output>
Create `.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-15-SUMMARY.md` when done.
</output>
