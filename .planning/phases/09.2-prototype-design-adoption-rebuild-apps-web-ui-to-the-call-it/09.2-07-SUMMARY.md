---
phase: 09.2-prototype-design-adoption
plan: 07
subsystem: ui
tags: [settled-receipt, outcome-stamp, settled-outcomes, cream-modal, dispute, provenance, neobrutalism, d-07, d-08, d-09]

# Dependency graph
requires:
  - phase: 09.2 plan 01
    provides: globals.css token layer + brutal-* primitives (.brutal-card hero/bracketed, .btn cream/ghost/outline-white, .stat-block, .section-divider, .brutal-table, .pill, .label-overline, .h-statement, .avatar grad a-f, mobile 44px/2-up rules)
  - phase: 09.2 plan 02
    provides: rethemed Stamp (.outcome-stamp recipe — clamp 56-120px Archivo 900, rotate(-1deg), text-shadow 4px 4px 0 #000, hexColor override)
  - phase: 09.2 plan 06
    provides: feed tape + card-tap navigation contract (back-to-tape destination)
provides:
  - Settled/disputed render branch of /call/[id] in the prototype receipt skin (tinted hero, 120px stamp, sub-line, .h-statement market line, 4-stat row, winners/losers brutal-tables, 2-node timeline, real-provenance metadata footer)
  - SETTLED_OUTCOME_STYLES in-file map — canonical prototype SETTLED_OUTCOMES color/tint/sub port (receipt.jsx:284-290) keyed by resolveSettledWord word
  - DisputeModal + ProvenanceModal on the cream .modal-panel template (D-13) with all tx/fetch logic byte-identical
affects: [09.2-08 live branch (same monolith seam), 09.2-15 visual goldens]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Replace-render-keep-handlers seam (PATTERNS §7): only the settled if-branch JSX replaced; data/handler block 95-1390 verbatim"
    - "Outcome words from lib/outcome-word.ts; outcome COLORS/tints/subs from the prototype SETTLED_OUTCOMES map keyed by the resolved word — never data.jsx OUTCOMES"
    - "Cream modal template inline: overlay rgba(0,0,0,0.82)+blur(4px) z-200; panel var(--bg-inverse)/#000/3px black/--shadow-brutal-lg/620/36"
    - "D-07 stat rows: each stat-block conditioned on its raw callData field (undefined → hidden, no '—' fillers)"

key-files:
  created: []
  modified:
    - apps/web/app/call/[id]/page.tsx

key-decisions:
  - "Settled DISPLAY colors come from SETTLED_OUTCOME_STYLES (prototype map: CALLED IT #E8F542 chartreuse, COLD CALL #64748B slate — D-03) overriding outcome-word.ts COLOR_MAP hexes for render; the lib stays untouched as the word/guard source and supplies the neutral fallback color"
  - "SHARE THE RECEIPT cream CTA wired to the twitter web intent (the old settled-branch button had NO onClick — a dead control); SHARE AS FRAME warpcast wiring kept byte-equivalent; both omit when no real share URL (D-08)"
  - "VIEW ALL CALLS BY {handle} control CUT (D-08): it linked to /?caller= which the 09.2-06 feed rewrite does not read — a silently-ignored filter is a dead control"
  - "Winners/losers split by P&L sign (prototype semantic) instead of follow/fade side; sorted by P&L, capped 20/side, handles+amounts only (AUTH-44)"
  - "data-outcome-word hook moved onto the stamp wrapper with the stamp's clamp font-size inline so the Tier-2 legibility check (>=36px) still reads a real computed size"
  - "PENDING DISPUTE warning replaces the stamp section (not the whole hero) while disputed — stats/positions/provenance stay visible, matching prior semantics"

patterns-established:
  - "SETTLED_OUTCOME_STYLES is the single prototype color/tint/sub source for settled outcome chrome in apps/web pages"

requirements-completed: [AUTH-44, SHARE-19]

# Metrics
duration: 45min
completed: 2026-06-10
---

# Phase 09.2 Plan 07: Settled Receipt Restyle Summary

**The settled/disputed `/call/[id]` branch now renders the prototype receipt — tinted bracketed hero, 120px outcome stamp with words from resolveSettledWord and colors from the ported SETTLED_OUTCOMES map, honest 4-stat row, 2-node created→settled timeline, winners/losers brutal-tables — and both in-file modals carry the cream .modal-panel template, all over the byte-identical data/handler block**

## Performance

- **Duration:** ~45 min
- **Started:** 2026-06-10T13:30:59Z
- **Completed:** 2026-06-10T14:16:17Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments

- **Settled branch replaced wholesale** (was lines 1392-1863) with the prototype `ReceiptSettledScreen` structure re-consuming the existing derived variables: top meta row (`← Back to the tape` ghost link + mono `settled · {txShort} · {UTC}` metadata, each segment hidden when its field is absent — D-07, block number never rendered because no source exists), tinted `.brutal-card.hero.bracketed` hero (`background: outcomeStyle.tint`), centered 120px `Stamp` (rethemed 09.2-02) with `data-outcome-word` hook, `· {sub} ·` mono sub-line, lozenge pill (CONTRARIAN / FADER WIN), `.h-statement` market line, 4-stat row, `// WHAT HAPPENED` 2-node timeline, real-provenance footer with the `view oracle proof ↗` ProvenanceModal trigger
- **`SETTLED_OUTCOME_STYLES`** — the canonical color/tint/sub map ported from `call it frontend/screens/receipt.jsx:284-290` (never data.jsx OUTCOMES): CALLED IT `#E8F542`/`rgba(232,245,66,0.04)`/"Right."; LOUD AND WRONG `#F87171`/`rgba(248,113,113,0.04)`/"The market remembers."; COLD CALL `#64748B`/"Didn't hit."; CONTRARIAN HIT `#E8F542`/0.05 tint/"Faded the consensus. Right."; FADED CORRECTLY `#E8F542`/"Right to doubt." — keyed by the **resolveSettledWord** word, which stays the only word source (08-05 GAP 1: the neutral `PENDING RESULT` fail-safe degrades to slate, no sub, no share)
- **Share row keeps the working wiring (D-09 / SHARE-19):** `SHARE THE RECEIPT →` is now a real twitter web-intent anchor (`twitterIntentUrl` + `buildShareText` from @call-it/shared) and `SHARE AS FRAME →` keeps the warpcast compose-intent computation byte-equivalent; both require `outcomeWordResult != null` + OG base + real handle (never share a fabricated/neutral outcome; controls omitted, never dead)
- **D-07 honest stats:** P&L / Rep Δ (riding the existing `displayedRepDelta` UI-45 count-up) / Final (+`target` sub) / Conviction — each `.stat-block` renders only when its raw `callData` field exists; stat row wraps 2-up below 768px (UI-48 via the pinned mobile rules + `isMobile` flex ternaries)
- **2-node timeline (D-07):** created (`createdAt`) → settled (`settledAt`) with real UTC timestamps; the prototype's "Hit 50 followers"/"Pool exceeded $1k" middle milestones have no event source and are NOT rendered (grep = 0)
- **Winners/losers** from the existing `finalPositions` data as `.section-divider` + `.brutal-table` columns — handles + amounts only (AUTH-44), square grad avatars, P&L-signed split, capped 20/side
- **Caller-exited-with-outcome variant** keeps the warning (#FB923C) banner treatment — real `callerExitedAt`/`callerExitedPenalty` segments only
- **Dispute affordances unchanged:** the 24h-window/counter-claim-limit IIFE, `Dispute this settlement →` CTA (now warning-outline `.btn`), DisputeModal/ProvenanceModal open-state wiring, and `MiniAppReady enabled={!!callData}` all preserved
- **Both modals on the cream template (Task 2, D-13):** overlay `rgba(0,0,0,0.82)` + `blur(4px)` z-200; panel `var(--bg-inverse)` cream, black text, 3px black border, `var(--shadow-brutal-lg)`, max-width 620, padding 36 (24 mobile), 90vh scroll. DisputeModal: `RAISE DISPUTE` mono header (FINAL·CONFIRM voice), explicit `$5.00 USDC · refunded + $2 reward…` bond copy before the confirm CTA, warning-treatment confirm (`Submit dispute · stake $5 ▸`) for cream-on-cream contrast, black-outline Cancel; raiseDispute SM write, $5 USDC allowance preflight, IPFS evidence flow (WR-06 5MB cap), and disabled/loading states byte-identical. ProvenanceModal: `ORACLE PROOF` mono header, JBM rows, black underline links, black chainId-42161 badge, real values only

## Task Commits

Each task was committed atomically:

1. **Task 1: Replace the settled render branch with the prototype receipt markup** - `6ebb883` (feat)
2. **Task 2: Restyle DisputeModal + ProvenanceModal chrome to the cream .modal-panel template** - `e2a4957` (feat)

## Files Created/Modified

- `apps/web/app/call/[id]/page.tsx` — settled branch rewritten in the prototype skin; `SETTLED_OUTCOME_STYLES` + avatar-grad/shortHash/formatUtc helpers added at module scope; both in-file modals on the cream panel; data/handler block (fetches, useReadContracts 5s poll, all write handlers, modal state, derived values) untouched except the shared-import line extended with `twitterIntentUrl`

## Decisions Made

- **Display colors from the prototype map, words from the app:** `SETTLED_OUTCOME_STYLES[word].hex` drives the stamp/timeline/lozenge color (D-03 chartreuse wins, slate COLD CALL), overriding the legacy `#4ADE80`/`#94A3B8` hexes that `outcome-word.ts` returns — the lib stays verbatim as the word source (D-09 fader guard + neutral fail-safe) and supplies the fallback color for `PENDING RESULT`
- **`VIEW ALL CALLS BY {handle}` cut (D-08):** it pointed at `/?caller=` which the 09.2-06 feed no longer reads — keeping it would ship a silently-broken filter
- **The old settled-branch `SHARE THE RECEIPT →` buttons had no onClick (dead controls);** the new cream CTA is a real twitter intent — this *is* the "existing working wiring" contract (D-09: ShareButton's twitterIntentUrl pattern + the 08-04 warpcast frame control)
- **Winners/losers split by P&L sign** rather than follow/fade side — matches the prototype's WINNERS/LOSERS semantic while staying 100% real-data
- **PENDING DISPUTE** swaps only the stamp section inside the hero (stats/positions/provenance remain), preserving the previous branch's information availability under dispute

## Deviations from Plan

None - plan executed exactly as written. One seam note for the record: the kept data/handler block's only diff is the line-85 shared import extended with `twitterIntentUrl` (required by the plan's own twitter-intent must-have) plus pure module-scope additions (`SETTLED_OUTCOME_STYLES`, grad/hash/UTC helpers) inserted between ProvenanceModal and the page component — zero handler/fetch/logic changes (verified by identifier-deletion grep over the git diff).

## Verification

- Task 1 gate: `pnpm --filter @call-it/web test` 97/97 (settled-outcome-truth, outcome-word, miniapp-ready tripwires green); `tsc --noEmit` shows no new errors (the 3 pre-existing test-file errors documented in 09.2-03 remain, untouched)
- Task 2 full gate: web test 97/97 → shared `tsc --build` 0 → `next build` 0 (all routes; warnings pre-existing: lockfile inference) → web lint 0 (project lint script is a stub — pre-existing)
- Acceptance greps all pass: `resolveSettledWord` present (7) + `SETTLED_OUTCOME_STYLES` with `rgba(232,245,66,0.04)` and `The market remembers.`; outcome-word literals ONLY in map keys + comments (stampColor ternary removed); `MiniAppReady` count 5 (≥2); `warpcastComposeUrl`/`twitterIntentUrl`/`SHARE AS FRAME` all present, zero handler deletions in the diff; fake milestone strings 0; `data-outcome-word`/`data-receipt-action-row` hooks preserved; DisputeModal JSX contains `var(--bg-inverse)`, `3px solid #000`, `blur(4px)`, the $5 bond copy, and the raiseDispute references; ProvenanceModal has no placeholder hashes (honest `—` for absent fields)
- Side-by-side vs `screenshots/settled.png` deferred to the plan 15 checkpoint per the plan

## Known Stubs

None — every rendered value has a live source (relayer `/live-state` + `/positions` + contract reads); everything without one is hidden (D-07), and the two dead controls found (unwired SHARE button, `/?caller=` link) were wired or cut rather than stubbed (D-08/D-09).

## Threat Flags

None — no new endpoints, auth paths, or trust-boundary changes. T-09.2-17 (word source = resolveSettledWord, lib untouched, tripwire tests green), T-09.2-18 (handles-only winners/losers), T-09.2-19 (2-node-only timeline + real-values-only footers, grep gates), T-09.2-20 (dispute handler identifiers diff-verified, bond explicit) all mitigated via the gates above.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- The settled receipt — THE product surface — now renders the prototype design end-to-end; plan 09.2-08 can replace the live branch (lines ~1930-2848 in the same monolith) using the same seam pattern
- `SETTLED_OUTCOME_STYLES` + the avatar-grad helper are reusable references for any later settled-outcome chrome (OG refresh wave, feed settled cards)

---
*Phase: 09.2-prototype-design-adoption*
*Completed: 2026-06-10*

## Self-Check: PASSED

- `apps/web/app/call/[id]/page.tsx` — FOUND
- Commit `6ebb883` — FOUND
- Commit `e2a4957` — FOUND
