---
phase: 09.2-prototype-design-adoption
plan: 13
type: execute
wave: 7
depends_on: ["09.2-10"]
files_modified:
  - apps/web/app/onboarding/layout.tsx
  - apps/web/app/onboarding/handle/page.tsx
  - apps/web/app/onboarding/socials/page.tsx
  - apps/web/app/onboarding/follow-graph/page.tsx
  - apps/web/app/onboarding/fund/page.tsx
  - apps/web/app/onboarding/tagline/page.tsx
  - apps/web/app/components/SocialLinkControls.tsx
  - apps/web/app/disputes/page.tsx
  - apps/web/app/components/NotificationInbox.tsx
  - apps/web/app/terms/page.tsx
  - apps/web/app/profile/[address]/settings/page.tsx
  - apps/web/components/AddressBookManager.tsx
  - apps/web/components/CustodyDisclosureCard.tsx
  - apps/web/components/WalletExportPrompt.tsx
  - apps/web/components/PaymasterCapBanner.tsx
  - apps/web/components/PrivyFundButton.tsx
  - apps/web/app/components/DesktopOnlyBanner.tsx
  - apps/web/tests/onboarding.spec.ts
autonomous: true
requirements: [AUTH-44, UI-48]
must_haves:
  truths:
    - "ALL surfaces the prototype doesn't cover — onboarding (5 steps + progress dots), disputes, settings, terms, notifications inbox, banners — get the new design via the ported primitives: .brutal-input/.toggle-pill for forms, .activity-row for the inbox, cream .modal-panel vocabulary (D-13)"
    - "Every surface keeps its existing hooks, guards, redirects, and data-testid hooks (onboarding dots, etc.) — restyle only (D-05, D-14)"
    - "Empty states use prototype-voice mono microcopy ('No notifications yet'), never bare blanks; missing data hidden, never faked (D-07)"
  artifacts:
    - path: "apps/web/app/onboarding/layout.tsx"
      provides: "Rethemed onboarding chrome with preserved progress dots"
    - path: "apps/web/app/components/NotificationInbox.tsx"
      provides: ".activity-row inbox with 'No notifications yet' empty state"
      contains: "No notifications yet"
    - path: "apps/web/app/profile/[address]/settings/page.tsx"
      provides: "Rethemed settings (wallet export, address book, withdraw-authorize) over untouched logic"
  key_links:
    - from: "onboarding pages"
      to: "apps/web/app/globals.css"
      via: ".brutal-input/.toggle-pill recipes"
      pattern: "brutal-input"
---

<objective>
Restyle every surface the prototype doesn't cover — onboarding, disputes, settings, terms, notification inbox, and the system banners — using the ported primitive vocabulary, with zero behavior change.

Purpose: D-13 — full coverage; no screen ships in the old skin.
Output: All uncovered surfaces in the new design.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-CONTEXT.md
@.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-UI-SPEC.md (State & Degradation + Copywriting contracts)
@.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-PATTERNS.md
</context>

## Artifacts this phase produces (this plan)

- No new files or exported symbols — chrome rewrites across the listed surfaces using the 09.2-01 primitive classes

<tasks>

<task type="auto">
  <name>Task 1: Restyle the 5-step onboarding flow with preserved dots/testids/redirect chain</name>
  <files>apps/web/app/onboarding/layout.tsx, apps/web/app/onboarding/handle/page.tsx, apps/web/app/onboarding/socials/page.tsx, apps/web/app/onboarding/follow-graph/page.tsx, apps/web/app/onboarding/fund/page.tsx, apps/web/app/onboarding/tagline/page.tsx, apps/web/app/components/SocialLinkControls.tsx, apps/web/tests/onboarding.spec.ts</files>
  <read_first>
    - apps/web/app/onboarding/layout.tsx + each step page (FULL FILES — step order handle→socials→follow-graph→fund→tagline, progress-dot markup + testids, completion gates, redirect logic — ALL stay; taglineCommittedAt is the completion gate)
    - apps/web/app/components/SocialLinkControls.tsx (recently fixed Farcaster link flow + full-width linked-state indicators from quick task 260610-f6s — DO NOT regress the useSignIn url/timeout handling; chrome only)
    - apps/web/tests/onboarding.spec.ts (lockstep contract: progress-dot hooks)
    - "call it frontend/styles.css" (.brutal-input/.toggle-pill/.label-overline/.page-header recipes)
  </read_first>
  <action>
    Restyle each onboarding step to the new vocabulary keeping every gate/redirect/testid: layout gets the page-header voice (Archivo step titles, JBM overline step counter) + restyled progress dots (square, accent for active — keep the existing dot count/testids); handle step → .brutal-input with availability states (existing validation, loss color for taken); socials → SocialLinkControls restyled (cream linked-state indicators stay full-width; Farcaster QR/redirect flow untouched); follow-graph → existing opt-in flow with .toggle-pill; fund → existing useUsdcBalance/PrivyFundButton wiring with .stat-block balance display; tagline → .brutal-textarea + cream commit CTA. Voice: "Go on record."-family declarative copy where headings restyle. The privy-token/onboarding redirect chain is logic — untouched (D-12 adjacent). onboarding.spec.ts reconciled in lockstep (D-15).
  </action>
  <verify>
    <automated>pnpm --filter @call-it/web test</automated>
  </verify>
  <done>All 5 steps + layout in the new skin; dots/gates/redirects intact; vitest green.</done>
  <acceptance_criteria>
    - Onboarding layout keeps its progress-dot testid hooks (git diff shows no data-testid deletions across onboarding files)
    - SocialLinkControls.tsx keeps the useSignIn url/timeout identifiers (git diff gate — no logic changes)
    - Step pages contain .brutal-input/.toggle-pill class usage (grep >= 1 per form step)
    - onboarding.spec.ts test count not reduced
    - `pnpm --filter @call-it/web test` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Restyle disputes page, notification inbox, and terms</name>
  <files>apps/web/app/disputes/page.tsx, apps/web/app/components/NotificationInbox.tsx, apps/web/app/terms/page.tsx</files>
  <read_first>
    - apps/web/app/disputes/page.tsx (existing dispute list/data wiring — stays)
    - apps/web/app/components/NotificationInbox.tsx (30s poll + read-state wiring — stays)
    - apps/web/app/terms/page.tsx (legal copy — content stays; the signin disclaimer links here)
    - "call it frontend/styles.css" (.activity-row + stampIn — the designated inbox template, D-13; .brutal-table for dispute rows)
  </read_first>
  <action>
    Disputes page: .page-header + .brutal-table (or .brutal-card rows) for dispute entries — status pills (pending neutral #3B82F6, resolved win/loss colors), real dispute data only, empty state "No disputes." mono overline (D-07). NotificationInbox: .activity-row recipe with stampIn entry, type pills, relative time JBM, handles only (AUTH-44); empty state exactly "No notifications yet" (UI-SPEC copy contract); 30s poll + mark-read wiring untouched. Terms: typographic restyle only — Archivo headings, Inter body, mono section numbers; content verbatim.
  </action>
  <verify>
    <automated>pnpm --filter @call-it/web test</automated>
  </verify>
  <done>Disputes/inbox/terms in the new skin with untouched wiring; vitest green.</done>
  <acceptance_criteria>
    - NotificationInbox.tsx contains `No notifications yet` and an .activity-row treatment; poll wiring identifiers unchanged
    - disputes/page.tsx renders no placeholder/fake dispute data (real data + empty state only)
    - terms/page.tsx content text unchanged (git diff shows style/markup-only changes)
    - `pnpm --filter @call-it/web test` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Restyle settings + system banners; full gate</name>
  <files>apps/web/app/profile/[address]/settings/page.tsx, apps/web/components/AddressBookManager.tsx, apps/web/components/CustodyDisclosureCard.tsx, apps/web/components/WalletExportPrompt.tsx, apps/web/components/PaymasterCapBanner.tsx, apps/web/components/PrivyFundButton.tsx, apps/web/app/components/DesktopOnlyBanner.tsx</files>
  <read_first>
    - apps/web/app/profile/[address]/settings/page.tsx (wallet export, address book, withdraw-authorize sections — all logic stays)
    - apps/web/components/AddressBookManager.tsx (cooldown logic — address-book-cooldown.spec asserts it; chrome only)
    - apps/web/components/WalletExportPrompt.tsx + PaymasterCapBanner.tsx (mounted in Providers.tsx — restyle THESE files only; Providers.tsx is NEVER edited)
    - apps/web/components/CustodyDisclosureCard.tsx, PrivyFundButton.tsx, apps/web/app/components/DesktopOnlyBanner.tsx
  </read_first>
  <action>
    Settings page: section dividers + .brutal-card groups, .brutal-input forms, cream primary CTAs, warning #FB923C framing on destructive actions (withdraw-authorize); address rendering in settings is the user's OWN wallet management surface — existing address display patterns here are functional (export/address book) and stay, but no NEW address rendering is added elsewhere (AUTH-44 applies to public identity surfaces). AddressBookManager: chrome only — cooldown logic and copy asserts untouched. WalletExportPrompt + PaymasterCapBanner: restyle as token-recipe banners/toasts (warning color, mono microcopy, radius 0) — trigger logic (≥$50 balance watch, 5-tx cap) untouched; Providers.tsx not edited (verify bit-identical). CustodyDisclosureCard/PrivyFundButton/DesktopOnlyBanner: token recipes, logic untouched. Run the full wave gate.
  </action>
  <verify>
    <automated>pnpm --filter @call-it/web test && pnpm --filter @call-it/shared build && pnpm --filter @call-it/web build && pnpm --filter @call-it/web lint</automated>
  </verify>
  <done>Settings + banners in the new skin; Providers.tsx untouched; full gate green.</done>
  <acceptance_criteria>
    - apps/web/app/Providers.tsx bit-identical: git diff --stat empty for it
    - AddressBookManager cooldown identifiers unchanged (address-book-cooldown.spec source asserts hold)
    - WalletExportPrompt/PaymasterCapBanner trigger-logic identifiers unchanged (wallet-export-prompt.spec + paymaster-cap-handoff.spec source asserts hold)
    - Full chain exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| onboarding chain | identity + funding setup; redirect gates must stay intact |
| settings | wallet export + withdraw-authorize = highest-sensitivity user surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.2-36 | Elevation of Privilege | onboarding gate/redirect regression letting incomplete users through | mitigate | logic-untouched git-diff gates; onboarding.spec lockstep; middleware not in files_modified |
| T-09.2-37 | Information Disclosure | new public address rendering added during settings restyle (AUTH-44) | mitigate | restyle adds no new address surfaces; AUTH-44 invariant test in vitest gate |
| T-09.2-38 | Tampering | banner restyle breaking the paymaster-cap / wallet-export triggers | mitigate | trigger-identifier git-diff gates; related specs' source asserts hold |
| T-09.2-SC | Tampering | package installs | accept | zero new packages this phase |
</threat_model>

<verification>
- Web vitest per task; full build/lint gate at Task 3
</verification>

<success_criteria>
- Every uncovered surface wears the new vocabulary; zero logic diffs; all empty states in prototype voice
</success_criteria>

<output>
Create `.planning/phases/09.2-prototype-design-adoption-rebuild-apps-web-ui-to-the-call-it/09.2-13-SUMMARY.md` when done.
</output>
