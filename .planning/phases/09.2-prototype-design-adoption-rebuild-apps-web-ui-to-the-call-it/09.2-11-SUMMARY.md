---
phase: 09.2-prototype-design-adoption
plan: 11
subsystem: ui
tags: [duel, challenge-escrow, vs-layout, accent-duel, neobrutalism, d-05, d-07, d-08, d-14, auth-44]

# Dependency graph
requires:
  - phase: 09.2 plan 01
    provides: globals.css token layer + brutal-* primitives (.brutal-card hero/heavy, .btn cream/outline-white/fade/ghost/duel, .pill duel, .stat-block bracketed, .section-divider, .label-overline, .brutal-bar.split caller/challenger, .live-dot, square .avatar grads)
  - phase: 09.2 plan 03
    provides: AppShell chrome (page no longer owns full-bleed bg/header/max-width)
  - phase: 09.2 plan 07/09
    provides: replace-render-keep-handlers seam precedent + avatarGradClass/countdown module-helper sanction in the call-page monolith
  - phase: 09.2 plan 10
    provides: wave-6 completion (depends_on)
provides:
  - /duel/[challengeId] in the prototype duel.jsx skin — THE MARKET hero, VS duel card (caller #E8F542 / challenger #A855F7), consensus split-bar duel variant, per-side riders lists — over the untouched 5s-poll DuelLiveState layer
  - Side-with dead money CTAs + FollowFadeModal no-op stubs CUT (D-08) with in-code deferral comment
affects: [09.2-15 visual goldens + manual checkpoint]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Replace-render-keep-handlers seam (PATTERNS §7) round 3: duel page data/handler block byte-identical (diff-verified); only render markup + sanctioned import-line/module-helper changes"
    - "Identity-color constants (CALLER_ACCENT/DUEL_ACCENT) as single literal source feeding all inline identity styles"
    - "Honest status word from the real ChallengeStatus ordinal (challengeStatusMeta) replacing the old hardcoded LOCKED label"

key-files:
  created: []
  modified:
    - apps/web/app/duel/[challengeId]/page.tsx

key-decisions:
  - "Side-with CTAs + both FollowFadeModal stubs CUT (D-08): onSubmit was a verified no-op — no dead money buttons; riders + consensus stay as the spectator surface; wiring deferred to 09.1+ in a code comment"
  - "Live-spread hero stat + '±20 rep' payload sub HIDDEN (D-07) — no price-spread source exists; only sourced stats (pot when >0, settles-in countdown) render in the hero column"
  - "Meta status word now derives from the real ChallengeStatus ordinal — the old markup hardcoded LOCKED on every status (a faked state under D-07)"
  - "Explicit error state added when live-state never loads — the old page rendered a duel full of placeholder caller/challenger identities (D-07 violation); 5s poll keeps retrying so a transient failure self-heals"
  - "VerifiedBadge from the donor identity rows NOT rendered — DuelLiveState carries no verification flags (D-07 hidden-not-faked)"
  - "Streak fire line renders only at streak >= 3 (existing fire threshold); rep/accuracy/in-category render from the existing DuelLiveState fields as before"

patterns-established:
  - "Duel split-bar variant: .brutal-bar.split with .caller (var(--accent-win)) / .gap (2px black) / .challenger (#A855F7) fills"

requirements-completed: [AUTH-44]

# Metrics
duration: 13min
completed: 2026-06-10
---

# Phase 09.2 Plan 11: Duel Page Prototype Restyle Summary

**The duel page now renders the prototype VS design — Archivo 900 asset-pair hero with sourced-only stat blocks, caller #E8F542 vs challenger #A855F7 identity columns with position tints and DuelLiveState stats, a consensus footer on the .brutal-bar.split duel variant, and per-side riders lists — over the byte-identical 5s-poll + approve→acceptChallenge layer, with the no-op Side-with money CTAs cut per D-08**

## Performance

- **Duration:** ~13 min
- **Started:** 2026-06-10T15:44:10Z
- **Completed:** 2026-06-10T15:57:00Z
- **Tasks:** 2 (Task 2 audit-only — zero diff, see below)
- **Files modified:** 1

## Accomplishments

- **Data/handler layer untouched (D-05, diff-verified):** `fetchDuelLiveState`/`fetchRidingLists`, the 5s `setInterval` + focus-refetch poll, the USDC allowance preflight (T-3-06-06), and `handleCallerApprove`/`handleAccept`/`handleReject` are byte-identical against HEAD~1 (extracted-block `diff` = empty for both the three handlers and the allowance path); consensus still derives from the relayer-supplied FFM reserves
- **Top meta row:** `← Back to the tape` ghost link + JBM meta `duel #d/{id} · arbitrum · {status} · {time-left}` where the status word/color comes from the real `ChallengeStatus` ordinal via `challengeStatusMeta` (PENDING warning / LOCKED win / REJECTED·REFUNDED tertiary / SETTLED secondary) — the old hardcoded `LOCKED` is gone
- **THE MARKET hero (donor duel.jsx):** `clamp(44px, 9vw, 110px)` Archivo 900 uppercase asset pair with the accent-loss slash, market line as `.h-3`; right column renders ONLY sourced stats — POT `.stat-block.bracketed` ("winner takes all", no rep-payload sub) when `potTotal > 0n` (min·2 prize-pot formula kept verbatim incl. the CR-04 comment) and SETTLES IN with a new `DuelCountdown` (D HH:MM:SS JBM, 1s tick — same recipe as 09.2-09's LiveCountdown); the prototype's price live-spread stat has no source and is hidden (D-07)
- **VS duel card** (`.brutal-card.hero`, page-level grid `1fr auto 1fr`): caller column with `· CALLER` overline, square grad/image avatar (xl), #E8F542 Archivo 900 handle (AUTH-44 handles only), `staked $X` JBM line, chartreuse-tinted POSITION box (when `callerPosition` exists), Rep/Accuracy/In-category from the existing DuelLiveState fields, streak fire line at ≥3; rotated `VS` center (accent-loss, 3px black text-shadow); mirrored challenger column on #A855F7 with the purple position tint — duel purple confined to challenge UI (D-14)
- **Consensus footer** inside the card (border-strong top, bg-quaternary): live-dot `MARKET CONSENSUS · LIVE` + `· updating` stale note + real `{n} riders · {pot} pot` meta (hidden when no riders); 32px/24px Archivo percentage headline pair and the `.brutal-bar.split` duel variant (`.caller` accent-win / 2px black `.gap` / `.challenger` #A855F7); when reserves total 0 → `consensus data unavailable` mono note instead of a fake 50/50 bar
- **Riders:** per-side `.section-divider` (`RIDING {handle}` in the side's identity color + real `$X backed` sum when non-empty) over donor riders rows (square sm avatars, handle, amount in side color), `+N more` overflow, `No one riding yet` empty copy
- **CTAs kept honest:** caller pending block restyled to `.brutal-card.heavy` with the `⚔ 1V1 DUEL · PENDING` pill — cream APPROVE/ACCEPT, outline-white REJECT, ghost keep-open / fade confirm (mirrors the 09.2-09 call-page block); the wired `⚔ CHALLENGE {handle}` `.btn.duel` keeps its openToChallenges guard verbatim; the two Side-with buttons and both FollowFadeModal no-op stubs are CUT with a D-08 comment deferring real wiring to 09.1+
- **Mobile (UI-48):** `useIsMobile` single-column stacking for hero/VS-card/riders, full-width CTAs, ≥44px targets; loading skeleton + a new explicit `COULDN'T LOAD THE DUEL … RETRY` error state on token recipes (retry re-arms the existing `fetchAll`)

## Task Commits

1. **Task 1: Replace duel render markup with the prototype VS layout** - `ab82ff4` (feat)
2. **Task 2: Duel page polish pass + full gate** - no commit (audit-only: the Task-1 rewrite landed fully on token references; the legacy-literal grep and full gate were verified with zero further changes required)

## Files Created/Modified

- `apps/web/app/duel/[challengeId]/page.tsx` — render markup rewritten to the prototype duel skin; data/handler block byte-identical except the sanctioned diffs: `FollowFadeModal` import + two no-op modal renders + their two open-state hooks removed (the cut stubs), `useIsMobile` import added, module-scope display helpers added (`challengeStatusMeta`, `avatarGradClass`, `DuelCountdown`, `DuelAvatar`, restyled `RidingList`), `CALLER_ACCENT`/`DUEL_ACCENT` constants added

## Decisions Made

- **Side-with stubs cut, not styled over (D-08):** both buttons opened `FollowFadeModal` instances whose `onSubmit` was `async () => { void 0; }` — verified no-ops; shipping them styled would imply a working money flow (T-09.2-30). The modal open-state hooks and import went with them; the comment at the old render slot cites D-08 + the 09.1+ deferral
- **Live spread hidden (D-07 / T-09.2-32):** no price-spread source; the consensus percentages (which DO have a source — FFM reserves) remain in the consensus footer only
- **Honest status + error states:** hardcoded `LOCKED` replaced by the real ordinal mapping; a failed live-state fetch now shows an error card instead of a page of placeholder identities ('caller'/'challenger' defaults never render as real identities)
- **Donor VerifiedBadge omitted:** no verification flags in DuelLiveState (D-07)
- **Settled-duel terminal states:** this page renders no outcome words (no `resolveSettledWord` surface here) — the SETTLED meta word is a state label from the ordinal, not an outcome word; plan-07's SETTLED_OUTCOMES map is not applicable

## Deviations from Plan

**1. [Rule 2 - Missing critical functionality] Explicit error state when live-state never loads**
- **Found during:** Task 1
- **Issue:** with the old markup, a permanently failing fetch rendered a full duel page of fabricated placeholder identities — a D-07 violation the restyle would otherwise carry forward
- **Fix:** added an early-return error card (`COULDN'T LOAD THE DUEL` + RETRY re-arming the existing `fetchAll`); the untouched 5s poll self-heals transient failures
- **Files modified:** apps/web/app/duel/[challengeId]/page.tsx
- **Commit:** ab82ff4

Otherwise none — plan executed as written. Task 2 produced no diff because Task 1 wrote the file entirely on token references (legacy grep was already 0).

## Verification

- Task 1 gate: `pnpm --filter @call-it/web test` 97/97 (challenge-gates tripwire green); `tsc --noEmit` shows only the 3 pre-existing test-file errors (documented since 09.2-03)
- Task 2 full gate: web vitest 97/97 → shared `tsc --build` 0 → `next build` 0 (all routes incl. /duel/[challengeId]) → web lint 0 (project lint script is a stub — pre-existing)
- Acceptance greps: `grep -ci "Side with"` = 0 (comments use the hyphenated form); `A855F7` present (5 — const + docs, all styles flow through `DUEL_ACCENT`); legacy literals `#18181B|#27272A|#A1A1AA|#2A2A30|#22C55E|#EF4444` = 0; `#4ADE80` = 0 (toast success → `var(--accent-win)`); fabricated-spread literals = 0; handler identifiers present (6 refs) and handler/allowance blocks diff-identical vs HEAD~1; only remaining raw hexes are the prototype-canon identity constants, the VS `3px 3px 0 #000` text-shadow, and the two donor position tints

## Known Stubs

None — every rendered value has a live source (relayer duel live-state + parent-call riders + FFM reserves); everything without one is hidden (D-07), and the dead Side-with money CTAs were cut (D-08).

## Threat Flags

None — no new endpoints, auth paths, or trust-boundary changes. T-09.2-30 (dead money CTAs) mitigated by the cut + grep gate; T-09.2-31 (AUTH-44) — riders/identity render handles only, addresses stay internal; T-09.2-32 (fabricated spread) — hidden, grep = 0.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- D-16 step 4 duel leg complete — the #A855F7 identity showcase is live over the working accept path
- Plan 15's visual-golden regeneration + manual checkpoint can exercise the duel route (pending/accepted/error/375px states)
- When 09.1+ wires real follow/fade on the parent call from the duel page, the D-08 comment marks the re-entry point

---
*Phase: 09.2-prototype-design-adoption*
*Completed: 2026-06-10*

## Self-Check: PASSED

- `apps/web/app/duel/[challengeId]/page.tsx` — FOUND
- `09.2-11-SUMMARY.md` — FOUND
- Commit `ab82ff4` — FOUND
- No file deletions in the task commit
