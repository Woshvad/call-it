{
  "name": "@call-it/shared",
  "version": "0.0.1",
  "private": true,
  "description": "Shared constants, types, and zod schemas for the Call It monorepo",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run",
    "lint": "echo 'No lint configured for shared yet'"
  },
  "dependencies": {
    "viem": "2.50.4",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@call-it/config": "workspace:*",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
